---
title: "Nautilus: An Auto-Scheduling Tensor Compiler for Efficient Tiled GPU Kernels"
title_zh: Nautilus：一种用于高效分块 GPU 算子的自动调度张量编译器
authors: "Yifan Zhao, Yuchen Yang, Matei Budiu, Sasa Misailovic"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14825v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 用于GPU算子自动优化的张量编译器
tldr: Nautilus 是一款新型张量编译器，旨在实现从数学描述到高效 GPU 核函数的全自动优化。它通过逐级降低的设计，将高层代数优化与底层分块优化结合，并引入了创新的自动调度器。该系统首次实现了从数学公式自动生成类似 FlashAttention-3 的高性能核函数，显著减轻了开发者的负担，在多种 GPU 架构上表现优于现有编译器和手动优化的库。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14825v1/fig-001.webp\", \"caption\": \"Figure 7. Performance of Nautilus kernels vs. vendor libraries on GH200 and RTX 5090 platforms. The y-axis shows relative performance normalized to Nautilus for each setup.\", \"page\": 15, \"index\": 1, \"width\": 989, \"height\": 450}, {\"url\": \"assets/figures/arxiv/2604.14825v1/fig-002.webp\", \"caption\": \"Figure 3. An example program in Nautilus’s three IRs.\", \"page\": 7, \"index\": 2, \"width\": 1043, \"height\": 349}, {\"url\": \"assets/figures/arxiv/2604.14825v1/fig-003.webp\", \"caption\": \"Figure 6. Performance of Nautilus kernels vs. kernels by other tensor compilers on GH200 and RTX 5090 platforms. The y-axis shows relative performance of all compilers normalized to Nautilus for each setup.\", \"page\": 10, \"index\": 3, \"width\": 990, \"height\": 664}, {\"url\": \"assets/figures/arxiv/2604.14825v1/fig-004.webp\", \"caption\": \"Figure 1. System overview of Nautilus. Novel components from our contributions are shown in colored boxes.\", \"page\": 3, \"index\": 4, \"width\": 396, \"height\": 404}, {\"url\": \"assets/figures/arxiv/2604.14825v1/fig-005.webp\", \"caption\": \"Figure 2. An example of scheduling a block graph. A scalar IR program and its corresponding block graph are shown at the top, followed by examples of using scheduling primitives to transform the block graph. In this graph, ovals represent compute statements and rectangles represent loops.\", \"page\": 3, \"index\": 5, \"width\": 454, \"height\": 523}, {\"url\": \"assets/figures/arxiv/2604.14825v1/fig-006.webp\", \"caption\": \"Figure 4. Throughput (TFLOPs/s) on GH200 across different models. We evaluate Nautilus against baselines across five foundation models (GLM, Llama, Qwen2, Qwen3, ViT). The benchmarks cover FP16 and FP8 precisions with Batch Size 1 and 8, for sequence lengths ranging from 1k to 32k.\", \"page\": 9, \"index\": 6, \"width\": 939, \"height\": 780}, {\"url\": \"assets/figures/arxiv/2604.14825v1/fig-007.webp\", \"caption\": \"Figure 8. Kernel numerical stability comparison on Qwen across three attention settings.\", \"page\": 16, \"index\": 7, \"width\": 1040, \"height\": 238}]"
motivation: 现有的张量编译器难以在保持程序结构的同时，自动处理复杂的全局变换和分块优化，导致难以生成如 FlashAttention-3 等极致性能的核函数。
method: 采用逐级降低的设计架构，结合能够探索高层优化序列并保留分块结构的自动调度器，支持包括高级归约融合在内的全局变换。
result: "在 GH200 和 RTX 5090 上，Nautilus 的吞吐量分别比现有最先进编译器高出 23% 和 42%，且在长序列配置下达到或超过了手动编写的 cuDNN 性能。"
conclusion: Nautilus 证明了通过全自动化的编译器优化可以达到甚至超越专家级手动编写的 GPU 核函数性能，是张量编译领域的重要进展。
---

## 摘要
我们提出了 Nautilus，这是一种新型张量编译器，旨在实现从数学描述到算子优化的完全自动化。Nautilus 将张量算子的高级代数规范编译为高效的分块 GPU 算子。Nautilus 的逐级降低设计允许在单个端到端系统中共同应用高级优化、表达式重写和分块优化。Nautilus 提出了一种新型自动调度器，它在发现高级优化序列的同时，保留了分块优化器所需的规则程序结构。Nautilus 的自动调度器捕捉了高级优化中复杂的交互和权衡，包括诸如高级规约融合之类的激进全局变换。Nautilus 是首个能够从类数学的注意力机制描述出发，并自动发现类似 FlashAttention-3 算子的端到端张量编译器，将全部优化负担从程序员转移到了编译器。在 NVIDIA GH200 和 RTX 5090 GPU 上的 5 个基于 Transformer 的模型和 150 个评估配置中，Nautilus 在 GH200 上的吞吐量比最先进的编译器高出多达 23%，在 RTX 5090 上高出多达 42%，同时在许多长序列配置中达到或超过了手动编写的 cuDNN 算子。

## Abstract
We present Nautilus, a novel tensor compiler that moves toward fully automated math-to-kernel optimization. Nautilus compiles a high-level algebraic specification of tensor operators into efficient tiled GPU kernels. Nautilus's successive lowering design allows high-level optimizations, expression rewrites, and tile optimizations to be jointly applied in a single end-to-end system. Nautilus presents a novel auto-scheduler that discovers sequences of high-level optimizations, while preserving the regular program structure needed by tile optimizers. Nautilus's auto-scheduler captures complex interactions and trade-offs in the high-level optimizations, including aggressive global transformations like advanced reduction fusion. Nautilus is the first end-to-end tensor compiler capable of starting from a math-like description of attention and automatically discovering FlashAttention-3-like kernels, offloading the entire burden of optimization from the programmer to the compiler. Across five transformer-based models and 150 evaluation configurations on NVIDIA GH200 and RTX 5090 GPUs, Nautilus achieves up to 23% higher throughput than state-of-the-art compilers on GH200 and up to 42% on RTX 5090, while matching or exceeding manually written cuDNN kernels on many long-sequence configurations.

---

## 论文详细总结（自动生成）

以下是对论文《Nautilus: An Auto-Scheduling Tensor Compiler for Efficient Tiled GPU Kernels》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现代 Transformer 模型（如 Llama、Qwen）对 GPU 核函数（Kernel）的性能要求极高。目前开发者面临两难选择：要么使用 CUDA/CUTLASS 手动编写高度复杂且难以移植的代码；要么使用现有的张量编译器（如 TVM、Halide），但它们往往无法自动实现复杂的全局优化（如 FlashAttention 中的高级规约融合）。
*   **核心目标**：开发一种名为 **Nautilus** 的端到端编译器，实现“从数学公式到高效核函数”的完全自动化。它旨在让程序员只需编写简单的线性代数表达式，就能自动生成性能媲美甚至超越手动优化库（如 FlashAttention-3）的 GPU 代码。

### 2. 方法论
Nautilus 采用了**逐级降低（Successive Lowering）**的设计架构，包含三个关键中间表示（IR）和一套创新的自动调度算法：
*   **三层 IR 抽象**：
    1.  **Scalar IR（标量 IR）**：用于高层调度和算子融合，捕捉全局数据依赖。
    2.  **VR-Tile IR（虚拟寄存器分块 IR）**：**本文创新点**，将分块表达为局部变量而非内存访问，便于进行代数表达式重写（如利用硬件特性将 $e^x$ 重写为 $2^x$）。
    3.  **MA-Tile IR（内存访问分块 IR）**：对接底层分块优化器（如 Triton、TileLang），处理 SIMD 映射。
*   **自动调度器（Auto-Scheduler）**：
    *   **块图（Block Graph）**：一种结合了数据依赖和抽象语法树的数据结构，用于指导优化。
    *   **四类变换**：双层分块（Bi-level Tiling）、算子融合（引入了 **Rolling Update** 技术以融合多个规约操作）、数据局部性优化、以及针对分块语言的结构正则化。
    *   **搜索策略**：采用预序遍历块图的方式生成调度种子，并通过进化搜索和成本模型进行自动调优。

### 3. 实验设计
*   **模型与算子**：评估了 GLM-4、Llama 2、Qwen2、Qwen3 和 ViT 五种基础模型；涵盖了全局注意力（Global）、因果注意力（Causal）和分组查询注意力（GQA）。
*   **硬件平台**：NVIDIA GH200 (Hopper 架构) 和 NVIDIA RTX 5090 (Blackwell 架构)。
*   **对比基准（Benchmark）**：
    *   **编译器**：Triton、Tawa、TileLang、Helion、FlexAttention、TVM。
    *   **手动优化库**：cuDNN、FlashAttention-2、FlashInfer、ThunderKittens、PyTorch SDPA。
*   **测试配置**：150 种配置，包括不同的 Batch Size（1, 8）、序列长度（1k 到 32k）以及精度（FP16, FP8）。

### 4. 资源与算力
*   **硬件使用**：明确使用了 NVIDIA GH200 和 RTX 5090。
*   **调优开销**：
    *   自动调度搜索通常在 **1 分钟内**完成。
    *   自动调优（Auto-tuning）阶段针对每个设置进行 256 次实测，耗时从**几秒到 10 分钟**不等。
    *   论文强调其搜索效率远高于需要数小时的超优化器（如 Mirage）。

### 5. 实验数量与充分性
*   **实验规模**：进行了 150 组端到端和算子级的实验，规模庞大。
*   **充分性**：实验不仅对比了吞吐量和延迟，还进行了**消融实验**（验证自动调优、表达式重写和自动调度的贡献）以及**数值稳定性测试**（与 FP64 参考值对比 RMSE）。
*   **客观性**：对比了当前工业界最强的供应商库（cuDNN）和学术界最新的编译器，覆盖了最新的 Hopper 和 Blackwell 架构，实验设计客观且具有前瞻性。

### 6. 主要结论与发现
*   **性能突破**：在 GH200 上比最强编译器基准快 **23%**，在 RTX 5090 上快 **42%**。
*   **超越手动代码**：在长序列配置下，Nautilus 生成的核函数性能达到或超过了手动编写的 cuDNN 算子。
*   **自动化能力**：Nautilus 是首个能从纯数学描述自动发现类似 FlashAttention-3 优化策略的编译器。
*   **数值精度**：其生成的核函数在数值误差上与 Tri-Dao FlashAttention 等手动库处于同一水平。

### 7. 优点
*   **全自动流程**：真正实现了从高层数学定义到低层硬件优化代码的自动桥接，极大降低了高性能算子的开发门槛。
*   **多后端集成**：能够动态选择最适合当前硬件的底层编译器（如在 5090 上选 TileLang，在 GH200 上选 Tawa）。
*   **创新的 IR 设计**：VR-Tile IR 解决了在分块级别进行代数重写的难题。
*   **高级融合支持**：成功自动化了此前难以处理的规约融合（Reduction Fusion）。

### 8. 不足与局限
*   **算子覆盖面**：虽然方法论具有通用性，但论文重点展示的是注意力机制（Attention）相关的算子，对于非 Transformer 结构的算子表现尚待验证。
*   **底层依赖**：Nautilus 的最终性能仍受限于底层分块编译器（如 Triton）的代码生成质量。如果底层编译器在某些架构上表现不佳，Nautilus 的上限也会受限。
*   **调优时间**：虽然比超优化器快，但对于需要即时编译（JIT）的场景，几分钟的调优时间可能仍然较长。

（完）
