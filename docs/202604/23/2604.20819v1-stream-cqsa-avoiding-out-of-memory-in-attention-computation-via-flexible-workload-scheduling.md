---
title: "Stream-CQSA: Avoiding Out-of-Memory in Attention Computation via Flexible Workload Scheduling"
title_zh: Stream-CQSA：通过灵活的工作负载调度避免注意力计算中的显存溢出
authors: "Yiming Bian, Joshua M. Akey"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20819v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 内存自适应调度框架，避免长上下文LLM注意力计算中的内存溢出
tldr: 针对长文本大语言模型中自注意力机制导致的显存溢出问题，本文提出了Stream-CQSA框架。该框架利用循环法定人数集（CQS）理论将注意力计算分解为多个独立的子任务，使其能在有限显存下运行。该方法支持在单GPU上通过流式处理完成十亿级Token序列的精确计算，无需近似误差或跨设备通信，显著提升了长序列处理的硬件兼容性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20819v1/fig-001.webp\", \"caption\": \"Figure 4: Workload management using uniform scheduling (left) and hybrid scheduling (right). The corresponding tree structure are in Figure 9. The dashed line indicates the memory limit (80 GiB).\", \"page\": 6, \"index\": 1, \"width\": 826, \"height\": 204}, {\"url\": \"assets/figures/arxiv/2604.20819v1/fig-002.webp\", \"caption\": \"Table 3: Interest sets for more l values\", \"page\": 15, \"index\": 2, \"width\": 652, \"height\": 285}, {\"url\": \"assets/figures/arxiv/2604.20819v1/fig-003.webp\", \"caption\": \"Figure 10: Full coverage validation for c = 13, I = (0, 1, 3, 9)\", \"page\": 15, \"index\": 3, \"width\": 826, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2604.20819v1/fig-004.webp\", \"caption\": \"Figure 5: Performance curve of naïve kernel (left) and FA kernel (right)\", \"page\": 7, \"index\": 4, \"width\": 746, \"height\": 217}, {\"url\": \"assets/figures/arxiv/2604.20819v1/fig-005.webp\", \"caption\": \"Figure 8: Performance estimation of SDPA(baseline) and Stream-CQSA\", \"page\": 14, \"index\": 5, \"width\": 826, \"height\": 486}, {\"url\": \"assets/figures/arxiv/2604.20819v1/fig-006.webp\", \"caption\": \"Figure 9: Tree structure of uniform and hybrid scheduling schemes in Figure 4. Only leaves are computed to be equivalent to the root. Intermediate gray nodes do not need to be computed.\", \"page\": 14, \"index\": 6, \"width\": 826, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2604.20819v1/fig-007.webp\", \"caption\": \"Table 2: Core-path wall-clock time breakdown. The communication time tcomm = tH2D fwd+tD2H fwd+ tH2D bwd + tD2H bwd. The miscellaneous time tmis = twall-clock − tcomm − tcore. The superscript of t indicates the operation being performed on the host or device.\", \"page\": 13, \"index\": 7, \"width\": 827, \"height\": 764}, {\"url\": \"assets/figures/arxiv/2604.20819v1/fig-008.webp\", \"caption\": \"Figure 6: Memory and wall-clock time comparison between forward and backward pass\", \"page\": 8, \"index\": 8, \"width\": 748, \"height\": 234}, {\"url\": \"assets/figures/arxiv/2604.20819v1/fig-009.webp\", \"caption\": \"Figure 2: CQSA forward pass with c = 7 and I = (0, 1, 3). In step 3, we highlight a chunk pair (0, 0). It appears in subsequence 0, 4, and 6, but only one of them should be merged in the end and the rest should be masked out to ensure the correctness. This is why the CQS mask (Mi) is necessary. We also provide the coverage of attention matrix from the global view to show all chunk pairs are covered exactly once. Another caveat is in step 5 that although Numi and Deni are accumulated in the same way, their dimensions are different: dim(Num) = N ×D and dim(Den) = N × 1.\", \"page\": 4, \"index\": 9, \"width\": 826, \"height\": 1089}, {\"url\": \"assets/figures/arxiv/2604.20819v1/fig-010.webp\", \"caption\": \"Figure 7: CQSA backward pass. Step 5 only displays Seq0 because it is the same for all subsequences. dNum ∈ RN×D, dDen ∈ RN×1\", \"page\": 12, \"index\": 10, \"width\": 664, \"height\": 1037}, {\"url\": \"assets/figures/arxiv/2604.20819v1/fig-011.webp\", \"caption\": \"Figure 3: CQS masking on R = QK⊤ with N = 49, c = 7, and I = (0, 1, 3). Iteration 1 forms subsequences of 21 tokens with masked diagonal redundancies. Iteration 2 further partitions each subsequence into smaller chunks (size 3), producing 9-token subsequences with both local and inherited masks. Only Seq0,itr=1 is shown and other subsequences at itr=1 follow the same pattern.\", \"page\": 5, \"index\": 11, \"width\": 827, \"height\": 443}]"
motivation: 现有的注意力优化方法通常假设QKV张量能完全装入显存，这限制了超长序列在有限硬件资源上的处理能力。
method: 提出CQS Divide操作，将注意力计算分解为一组独立的子序列计算任务，通过内存自适应调度实现流式执行。
result: 实验证明该方法能在单块GPU上完成十亿级Token序列的精确注意力计算，且显存缩放具有高度可预测性。
conclusion: Stream-CQSA通过将注意力计算任务化，打破了显存对序列长度的物理限制，为长上下文模型的扩展提供了新路径。
---

## 摘要
长上下文大语言模型的可扩展性受到精确自注意力机制二次方内存开销的根本限制，这在现代硬件上经常导致显存溢出（OOM）故障。现有方法虽然将内存效率提高到了近线性复杂度，但仍假设完整的查询（query）、键（key）和值（value）张量能够全部容纳在设备内存中。在这项工作中，我们通过引入 CQS Divide 移除了这一假设。这是一种源自循环法定人数集（CQS）理论的操作，它将注意力分解为一组独立的子序列计算，其重组结果与全序列注意力完全一致。利用这种分解，我们提出了 Stream-CQSA，这是一个内存自适应调度框架，可将注意力划分为适合任意内存预算的子问题。这把注意力从逻辑上的单体操作重塑为一系列可调度的任务，从而实现了跨设备的灵活执行，且无需设备间通信。实验证明了可预测的内存扩展性，并表明通过流式传输，可以在单张 GPU 上执行针对十亿级标记（billion-token）序列的精确注意力计算，而无需改变注意力的底层数学定义或引入近似误差。

## Abstract
The scalability of long-context large language models is fundamentally limited by the quadratic memory cost of exact self-attention, which often leads to out-of-memory (OOM) failures on modern hardware. Existing methods improve memory efficiency to near-linear complexity, while assuming that the full query, key, and value tensors fit in device memory. In this work, we remove this assumption by introducing CQS Divide, an operation derived from cyclic quorum sets (CQS) theory that decomposes attention into a set of independent subsequence computations whose recomposition yields exactly the same result as full-sequence attention. Exploiting this decomposition, we introduce Stream-CQSA, a memory-adaptive scheduling framework that partitions attention into subproblems that fit within arbitrary memory budgets. This recasts attention from a logically monolithic operation into a collection of schedulable tasks, enabling flexible execution across devices without inter-device communication. Experiments demonstrate predictable memory scaling and show that exact attention over billion-token sequences can be executed on a single GPU via streaming, without changing the underlying mathematical definition of attention or introducing approximation error.

---

## 论文详细总结（自动生成）

这是一份关于论文《Stream-CQSA: Avoiding Out-of-Memory in Attention Computation via Flexible Workload Scheduling》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：长上下文大语言模型（LLM）的扩展受到自注意力机制二次方内存开销的限制。
*   **研究背景**：虽然 FlashAttention 等方法实现了近线性的内存效率，但它们仍假设完整的 Q（查询）、K（键）、V（值）张量能全部存入 GPU 显存。在处理超长序列（如十亿级 Token）时，仅物化这些张量本身就会导致显存溢出（OOM），使得现有的优化算法无法启动。
*   **整体含义**：本文旨在打破“显存必须容纳全量 QKV”的物理限制，通过数学分解将注意力计算转变为可流式处理的任务流。

### 2. 方法论
*   **核心思想**：利用**循环法定人数集（Cyclic Quorum Sets, CQS）**理论，将一个完整的注意力计算分解为多个相互独立、且能完全覆盖所有 Token 交互的子序列计算。
*   **关键技术细节**：
    *   **CQS Divide（CQS 分解）**：将序列分为 $c$ 个块，根据 Steiner 系统 $S(2, l, c)$ 构造 $c$ 个子序列。例如，当 $l=3, c=7$ 时，每个子序列包含 3 个块，7 个子序列的组合能精确覆盖所有块对的交互，且无冗余。
    *   **CQS Masking（CQS 掩码）**：由于子序列间存在结构化重叠（主要是块内的自交互），通过特定的掩码规则确保每个块对的计算在全局中仅被计入一次，从而保证结果与全序列注意力完全一致。
    *   **Stream-CQSA 框架**：将注意力计算从单体操作重塑为可调度的任务队列。它支持**递归分解**（增加迭代次数 $itr$ 以进一步减小子问题规模）和**混合调度**（根据显存动态调整不同任务的粒度）。
    *   **OOM 护栏机制**：实时监控显存，若发生 OOM 则自动增加分解粒度并减少并发任务数，确保计算在任何内存预算下都能完成。

### 3. 实验设计
*   **实验场景**：在单张 GPU 上模拟和执行超长序列的注意力前向与后向传播。
*   **Benchmark 与对比方法**：
    *   **基准**：PyTorch 官方的 `scaled_dot_product_attention` (SDPA)，使用 FlashAttention 后端。
    *   **对比内核**：对比了朴素 Python 实现的内核与基于 FlashAttention 的 CUDA 内核在 Stream-CQSA 框架下的表现。
*   **测试序列长度**：从 100K 到 1M Token（用于建立性能模型），并最终扩展到 **1B（十亿）Token** 的模拟计算。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA A100 GPU (80 GiB)**。
*   **算力消耗**：
    *   对于 1B Token 的计算，论文通过外推法估算：在 $itr=5$ 的粒度下，前向传播约需 9,510 GPU 小时，后向传播约需 23,411 GPU 小时。
    *   实验中实际执行了部分子序列以验证显存占用和单步耗时的准确性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了前向/后向传播的内存缩放曲线、不同内核的性能对比、1B Token 的扩展性分析以及最优分解粒度的理论推导。
*   **充分性评价**：实验设计较为科学，通过多项式拟合准确预测了 OOM 临界点。虽然没有在多个不同型号的 GPU 上做横向对比，但针对 A100 的深度剖析足以证明该算法在极端内存受限情况下的有效性。实验侧重于系统性能和数学等价性验证，而非下游 NLP 任务的精度（因为该方法在数学上是精确的）。

### 6. 主要结论与发现
*   **内存可预测性**：Stream-CQSA 的内存占用随分解粒度呈高度可预测的线性下降。
*   **打破限制**：成功证明了在单张 80GB GPU 上执行十亿级 Token 精确注意力的可行性，这在以前被认为是不可能的。
*   **性能权衡**：Stream-CQSA 将内存压力转移到了计算时间上。虽然总运行时间因数据搬运和掩码生成而增加，但它确保了任务的可完成性。
*   **最优粒度**：存在一个最优的分解粒度，可以平衡内存节省带来的开销与计算效率。

### 7. 优点
*   **数学精确性**：与稀疏注意力或线性注意力不同，该方法不引入任何近似误差，结果与标准注意力完全一致。
*   **内存自适应**：能够根据硬件显存动态调整任务大小，具有极强的硬件兼容性。
*   **无需跨设备通信**：子序列计算是完全独立的，这使得它在分布式环境下无需昂贵的 All-reduce 操作即可实现并行化。

### 8. 不足与局限
*   **运行时开销**：频繁的 H2D（主机到设备）数据传输、掩码生成和结果聚合引入了显著的时间开销（相比理想状态下的 FlashAttention）。
*   **内核限制**：目前的实现受限于底层内核（如 FlashAttention）的并行度，尚未能完全利用显存预算来最大化并行执行效率。
*   **实时性挑战**：对于需要极低延迟的推理场景，这种“以时间换空间”的流式处理可能不适用，更适合离线训练或超长文档的批处理分析。

（完）
