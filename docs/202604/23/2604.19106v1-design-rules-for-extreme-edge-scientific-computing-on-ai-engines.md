---
title: Design Rules for Extreme-Edge Scientific Computing on AI Engines
title_zh: AI 引擎上极边缘科学计算的设计规则
authors: "Zhenghua Ma, G Abarajithan, Dimitrios Danopoulos, Olivia Weng, Francesco Restuccia, Ryan Kastner"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19106v1"
tags: ["query:mlsys"]
score: 8.0
evidence: FPGA SoC上AI引擎的设计规则
tldr: 针对极端边缘科学计算中机器学习模型对低延迟和高吞吐的严苛要求，本研究探讨了在FPGA SoC的AI引擎（AIE）上部署模型的策略。通过系统性架构表征和微基准测试，提出了延迟调整资源等效（LARE）指标，并开发了针对低延迟推理的空间及API级数据流优化方法，成功实现了在传统可编程逻辑中无法容纳的大型神经网络部署。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19106v1/fig-001.webp\", \"caption\": \"Fig. 5. Latency reduction of tiling a GEMM workload / dense layer of size (8,128,128) spatially, across PK columns and PN rows of compute tiles. The API tile size (SM , SK , SN ) is fixed to (4,8,8). Each arrow shows the percentage change in latency upon doubling the number of compute tiles along row/column in the AIE array.\", \"page\": 6, \"index\": 1, \"width\": 474, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2604.19106v1/fig-002.webp\", \"caption\": \"Fig. 1. Our design rules for AIE allow larger NN implementations to meet and surpass the 40 MHz throughput requirement of the LHC trigger system [21], [22]. While PL is sufficient for small NNs such as Jet-tagger [23] and τ Event Selection [24], larger NNs such as VAE [25], Qubit Readout [26] and the Deep Autoencoder [21] can only meet the performance requirement on AIEs using our design rules.\", \"page\": 2, \"index\": 2, \"width\": 420, \"height\": 385}, {\"url\": \"assets/figures/arxiv/2604.19106v1/fig-003.webp\", \"caption\": \"Fig. 6. Latency impact of exhausting the available AIE columns. We use a dense model with 8 layers, each of size (8,192,192), datatype i8×i8, and API tile size (4, 8, 8) each implemented in PK×PN=12 compute tiles so other latency factors remain unchanged. We then vary the spatial tiling asymmetry (PK , PN ). The results show that column-wise expansion improves performance only up to the single-band column limit; once layers spill into multiple bands, resource contention introduces a latency penalty.\", \"page\": 7, \"index\": 3, \"width\": 524, \"height\": 516}, {\"url\": \"assets/figures/arxiv/2604.19106v1/fig-004.webp\", \"caption\": \"Fig. 7. Latency overhead of crossing the AIE-PL boundary. Each experiment has the same dense model with 16 equal layers, always 8 layers in AIE and 8 layers in the PL, varying only the number of crossings. The gradient of the line fit to total latency suggests a latency overhead of 3.9% per crossing.\", \"page\": 7, \"index\": 4, \"width\": 529, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2604.19106v1/fig-005.webp\", \"caption\": \"Fig. 2. HLS4ML performance scalability. Performance is measured by Interval, i.e., the time between output batches in steady-state execution. A smaller Interval indicates higher throughput and thus better performance. In the resource-abundant regime, HLS4ML can fully parallelize the design, so Interval remains nearly constant while resource consumption increases with workload sizes. In the constrained-resource regime, arithmetic units must be timemultiplexed with a reuse factor, thereby reducing throughput and increasing the interval. AIE performance is also shown for reference.\", \"page\": 3, \"index\": 5, \"width\": 470, \"height\": 339}, {\"url\": \"assets/figures/arxiv/2604.19106v1/fig-006.webp\", \"caption\": \"Fig. 3. Micro-benchmarking to understand resource–latency trade-off. Each colored line represents one layer shape on PL; each point on a colored line corresponds to a different reuse factor. Larger reuse factor (top-left) results in less resource usage and higher interval (weaker performance). The black points on colored lines mark the AIE performance (y axis) and LARE value (x axis) of the corresponding layer. The blue region represents congested PL resource, where large reuse factor is required for PL deployment, and the AIE achieves better latency. The red region indicates resource-redundant PL, where spatial dataflow PL implementations provide better latency than AIE.\", \"page\": 4, \"index\": 6, \"width\": 529, \"height\": 609}, {\"url\": \"assets/figures/arxiv/2604.19106v1/fig-007.webp\", \"caption\": \"Fig. 4. Performance analysis of GEMM workloads with batch size of 8 implemented in a single compute tile, to measure the performance impact of API-level tiling strategies. Each two-column group along the x-axis corresponds to a workload size, with the number of operations increasing linearly by 4096 across groups; the two workloads within a group have the same number of operations but differ in asymmetry (QK -larger vs QN -larger). The y-axis shows the aie::mmul API sizes (SM , SK , SN ) that are legal for the datatype i8×i8. We observe that the APIs (4, 8, 8) and (4, 16, 8) achieve the best performance across all workload sizes, and spatial tiling with larger QN consistently yields better performance.\", \"page\": 5, \"index\": 7, \"width\": 1032, \"height\": 312}]"
motivation: 传统的空间数据流实现在处理大型科学神经网络时面临资源扩展瓶颈，而AI引擎虽具潜力但其性能优势和适用场景尚不明确。
method: 提出延迟调整资源等效（LARE）指标来量化比较AI引擎与可编程逻辑的性能，并引入针对低延迟科学推理的空间及API级数据流优化技术。
result: 实验证明所提方法能有效识别AI引擎的优势区间，并成功部署了使用hls4ml工具链无法在可编程逻辑上运行的端到端神经网络。
conclusion: 本研究为极端边缘科学计算提供了明确的设计准则，证明了AI引擎在处理大规模、低延迟神经网络任务中优于传统可编程逻辑。
---

## 摘要
极边缘科学应用利用机器学习模型分析传感器数据并做出实时决策。其严苛的延迟和吞吐量要求需要较小的批处理大小，并要求模型权重完全保留在片上。空间数据流（Spatial dataflow）实现是极边缘应用的常用方案。空间数据流在小型网络中表现良好，但由于固有的资源扩展限制，难以扩展到大型模型。现代 FPGA SoC 上的 AI 引擎提供了具有高计算密度和额外片上存储的有前景的替代方案。然而，AI 引擎的架构、编程模型和性能扩展行为与可编程逻辑（programmable logic）有本质区别，这使得直接比较变得复杂，且使用 AI 引擎的优势尚不明确。本研究探讨了极边缘科学神经网络应在何时以及如何选择在 AI 引擎而非可编程逻辑上实现。我们提供了系统的架构特性分析和微基准测试，并引入了延迟调整资源等效（LARE）指标，用于识别 AI 引擎实现在何时优于可编程逻辑设计。我们进一步提出了针对低延迟科学推理定制的空间和 API 级数据流优化。最后，我们展示了在 AI 引擎上成功部署端到端神经网络的案例，而这些网络在使用 hlsml 工具链时无法容纳在可编程逻辑中。

## Abstract
Extreme-edge scientific applications use machine learning models to analyze sensor data and make real-time decisions. Their stringent latency and throughput requirements demand small batch sizes and require that model weights remain fully on-chip. Spatial dataflow implementations are common for extreme-edge applications. Spatial dataflow works well for small networks, but it fails to scale to larger models due to inherent resource scaling limitations. AI Engines on modern FPGA SoCs offer a promising alternative with high compute density and additional on-chip memory. However, the architecture, programming model, and performance-scaling behavior of AI Engines differ fundamentally from those of the programmable logic, making direct comparison non-trivial and the benefits of using AI Engines unclear. This work addresses how and when extreme-edge scientific neural networks should be implemented on AI Engines versus programmable logic. We provide systematic architectural characterization and micro-benchmarking and introduce a latency-adjusted resource equivalence (LARE) metric that identifies when AI Engine implementations outperform programmable logic designs. We further propose spatial and API-level dataflow optimizations tailored to low-latency scientific inference. Finally, we demonstrate the successful deployment of end-to-end neural networks on AI Engines that cannot fit on programmable logic when using the hlsml toolchain.

---

## 论文详细总结（自动生成）

这是一份关于论文《Design Rules for Extreme-Edge Scientific Computing on AI Engines》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：极边缘（Extreme-Edge）科学应用（如欧洲核子研究中心 LHC 的粒子触发系统）对实时推理有严苛要求：延迟需在微秒级，吞吐量需达 40 MHz。
*   **核心挑战**：
    *   **PL 的局限性**：传统基于 FPGA 可编程逻辑（PL）的空间数据流架构（如 `hls4ml`）在处理小型网络时表现优异，但随着模型增大，PL 资源会迅速枯竭，迫使设计者增加“复用因子”（Reuse Factor），导致延迟剧增，甚至导致 EDA 工具编译失败。
    *   **AIE 的不确定性**：现代 FPGA SoC（如 AMD Versal）集成了 AI 引擎（AIE），具有极高的计算密度，但其 VLIW 架构和编程模型与 PL 迥异。科学界尚不清楚在何种负载下应切换到 AIE，以及如何针对低延迟（而非高吞吐批处理）优化 AIE。
*   **研究目的**：填补 PL 与 AIE 之间的架构认知鸿沟，为科学计算提供明确的部署决策依据和优化准则。

### 2. 论文提出的方法论
论文通过系统性的架构表征，提出了以下核心贡献：
*   **LARE 指标（延迟调整资源等效）**：
    *   **核心思想**：定义一个决策边界。LARE 量化了“为了达到与 AIE 相同的延迟，PL 实现需要消耗多少资源”。
    *   **算法流程**：针对特定层形状，扫描 PL 的不同复用因子，获取其资源-性能曲线；同时测量 AIE 的性能点；通过插值找到 PL 性能等于 AIE 性能时的资源消耗量。若当前 PL 预算低于 LARE，则应选择 AIE。
*   **两级 GEMM 分块（Tiling）优化**：
    *   **API 级分块**：在单个 AIE 瓷砖（Tile）内，优化 `aie::mmul` 的参数选择。
    *   **空间级分块**：在 AIE 阵列间分配工作负载，利用级联总线（Cascade Bus）进行部分和传递。
*   **七大设计准则（Design Rules）**：基于微基准测试总结出关于 API 尺寸选择、负载非对称性处理、空间扩展方向（优先列扩展）、列耗尽惩罚、以及 PL-AIE 边界跨越开销（约 3.9% 延迟惩罚）的实战规则。

### 3. 实验设计
*   **硬件平台**：AMD-Xilinx Versal VEK280 评估板（含 AIE-ML 架构）。
*   **对比基准（Benchmark）**：
    *   **PL 基线**：使用扩展后的 `hls4ml` 工具链，采用“资源优先”策略。
    *   **AIE 实现**：对比了“朴素映射”（Naive）与应用了“设计规则”后的“优化映射”。
*   **测试场景/数据集**：
    *   **微基准测试**：针对不同形状的密集层（Dense Layer）进行扫频。
    *   **端到端模型**：LHC 变分自编码器（VAE）、多量子比特读取判别器（Qubit Readout）、MLPerf Tiny 深度自编码器。

### 4. 资源与算力
*   **硬件资源**：使用 Versal VEK280，包含 304 个 AIE-ML 计算瓷砖（每个瓷砖在 1GHz 下可提供等效于 58 个 DSP58 的算力）。
*   **时钟频率**：AIE 运行在 1 GHz，PL 侧运行在 312.5 MHz。
*   **训练/编译说明**：论文侧重于推理部署，性能通过周期精确（Cycle-accurate）的硬件仿真测量。文中未提及模型训练时长，因为重点在于硬件映射优化。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了详尽的单层扫频实验（图 3、图 4、图 5），涵盖了不同的矩阵维度和 API 配置。
    *   针对 AIE 列耗尽（Column Exhaustion）和 PL-AIE 边界跨越进行了专门的消融实验。
    *   验证了 3 个真实的端到端科学计算模型。
*   **充分性评价**：实验设计非常充分且具有针对性。它不仅对比了最终结果，还深入探讨了导致性能波动的底层架构原因（如 DMA 停顿、内存竞争等），实验结果客观地展示了 AIE 在特定规模下的优势。

### 6. 论文的主要结论与发现
*   **性能跨越**：对于大型神经网络，PL 实现因资源限制无法达到 40 MHz 的触发频率，而优化后的 AIE 实现不仅能容纳这些模型，且吞吐量可超过 50-90 MHz。
*   **优化效果**：应用设计规则后的 AIE 实现比朴素 AIE 映射性能提升高达 4 倍。
*   **边界成本**：量化了 PL 与 AIE 之间数据交换的代价，每增加一次边界跨越会带来约 3.9% 的额外延迟。
*   **扩展性**：AIE 的性能并非随瓷砖数量线性增长，存在边际效应递减点，且“列耗尽”会导致显著的性能下降。

### 7. 优点
*   **实用性极强**：直接给出了 7 条设计准则，对于使用 Versal 平台的工程师具有极高的参考价值。
*   **指标创新**：LARE 指标为异构计算中的“何时切换架构”提供了一个科学的量化工具，而非凭感觉决策。
*   **填补空白**：首次深入探讨了 AIE 在“极低延迟、权重驻留片上”这一特殊科学计算场景下的表现，而非通用的高吞吐 AI 推理。

### 8. 不足与局限
*   **算子覆盖有限**：目前主要集中在密集层（GEMM），虽然这是科学计算的主力，但对卷积（CNN）或注意力机制（Attention）的讨论较少。
*   **自动化程度**：虽然提出了规则，但 PL-AIE 的混合分区目前仍需较多人工干预，尚未完全集成进自动化的编译器流中。
*   **硬件依赖**：结论高度依赖于 AMD Versal AIE-ML 架构，在其他 AI 加速器（如 TPU 或其他 NPU）上的通用性有待验证。

（完）
