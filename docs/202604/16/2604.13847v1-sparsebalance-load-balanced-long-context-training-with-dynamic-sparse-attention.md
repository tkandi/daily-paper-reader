---
title: "SparseBalance: Load-Balanced Long Context Training with Dynamic Sparse Attention"
title_zh: SparseBalance：基于动态稀疏注意力的负载均衡长文本训练
authors: "Hongtao Xu, Jianchao Tan, Yuxuan Hu, Pengju Lu, Hongyu Wang, Pingwei Sun, Yerui Sun, Yuchen Xie, Xunliang Cai, Mingzhen Li, Weile Jia"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13847v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 针对负载均衡分布式长文本训练的算法系统协同设计
tldr: 针对长文本大模型训练中稀疏注意力导致的负载不均衡和模型精度受损问题，本文提出了SparseBalance算法-系统协同设计框架。该框架通过工作负载感知的动态稀疏度调节和稀疏感知批处理策略，在消除训练延迟瓶颈的同时利用计算空隙提升精度。实验表明，SparseBalance在提升长文本处理能力的同时，实现了高达1.33倍的端到端加速。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.13847v1/fig-001.webp\", \"caption\": \"Fig. 11: Trade-off between training speedup and downstream model performance under different configurations in SparseBalance.\", \"page\": 10, \"index\": 1, \"width\": 404, \"height\": 315}, {\"url\": \"assets/figures/arxiv/2604.13847v1/fig-002.webp\", \"caption\": \"Fig. 7: Overall performance and step-by-step speedups on two clusters and two datasets. We evaluate the normalized speedup ratios (measured by average iteration time) of SparseBalance and its two main components for specific model settings (from left to right: Qwen2.5-0.5B, Qwen2.5-3B, and Qwen2.5-3B in LoRA). Results demonstrate significant efficiency improvements in SparseBalance and confirm the effectiveness of each component.\", \"page\": 8, \"index\": 2, \"width\": 1046, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2604.13847v1/fig-003.webp\", \"caption\": \"Fig. 5: Overview of SparseBalance, which consists of three components: workload-aware dynamic sparsity tuning (DST), sparsity-aware batching (SAB) and latency prediction module.\", \"page\": 4, \"index\": 3, \"width\": 524, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2604.13847v1/fig-004.webp\", \"caption\": \"Fig. 10: Training loss comparison between the MoBA baseline and SparseBalance with different dynamic tuning configurations.\", \"page\": 9, \"index\": 4, \"width\": 509, \"height\": 299}, {\"url\": \"assets/figures/arxiv/2604.13847v1/fig-005.webp\", \"caption\": \"TABLE II: Average accuracy (%) of Needle-in-a-Haystack (NIAH) tasks evaluated on Qwen-2.5-3B model. Results are averaged across the niah_single_1, niah_single_2, and niah_single_3 evaluations for each model.\", \"page\": 9, \"index\": 5, \"width\": 505, \"height\": 238}, {\"url\": \"assets/figures/arxiv/2604.13847v1/fig-006.webp\", \"caption\": \"Fig. 6: Practical latency of sparse attention cannot be reliably predicted by either theoretical FLOPs or sequence length alone, motivating our profiling-based latency predictor.\", \"page\": 5, \"index\": 6, \"width\": 517, \"height\": 235}]"
motivation: 稀疏注意力在分布式长文本训练中存在序列长度和稀疏敏感度的异质性，导致严重的负载不均衡和模型精度下降。
method: 提出工作负载感知的动态稀疏度调节和稀疏感知批处理策略，通过双向调整稀疏度来平衡计算负载并优化精度。
result: "在LongBench基准测试中，该方法实现了1.33倍的端到端加速，并将长文本处理能力提升了0.46%。"
conclusion: SparseBalance通过算法与系统的协同优化，有效解决了长文本训练中的效率与精度权衡问题。
---

## 摘要
虽然稀疏注意力缓解了长文本大语言模型（LLM）训练的计算瓶颈，但其分布式训练过程在 1) 序列长度和 2) 稀疏敏感度方面表现出极端的异构性，导致了严重的负载不均衡问题和次优的模型准确率。现有的算法和训练框架通常只关注单一问题，未能系统地协同优化这两个问题。因此，我们提出了 SparseBalance，这是一种新颖的算法-系统协同设计框架，利用稀疏性和序列异构性来共同优化模型准确率和系统效率。首先，我们提出了工作负载感知的动态稀疏度调节，采用双向稀疏度调整来消除落后者，并利用固有的空隙来获得额外的准确率提升。其次，我们提出了一种稀疏感知的批处理策略以实现粗粒度的均衡，这与动态稀疏度调节相辅相成。实验结果表明，SparseBalance 实现了高达 1.33 倍的端到端加速，同时在 LongBench 基准测试中将长文本能力提升了 0.46%。

## Abstract
While sparse attention mitigates the computational bottleneck of long-context LLM training, its distributed training process exhibits extreme heterogeneity in both \textit{1)} sequence length and \textit{2)} sparsity sensitivity, leading to a severe imbalance problem and sub-optimal model accuracy. Existing algorithms and training frameworks typically focus on single issue, failing to systematically co-optimize these two problems. Therefore, we propose SparseBalance, a novel algorithm-system co-design framework, which exploits the sparsity and sequence heterogeneity to optimize model accuracy and system efficiency jointly. First, we propose workload-aware dynamic sparsity tuning, which employs a bidirectional sparsity adjustment to eliminate stragglers and exploit inherent bubbles for free accuracy. Second, we propose a sparsity-aware batching strategy to achieve coarse-grained balance, which complements dynamic sparsity tuning. Experimental results demonstrate that SparseBalance achieves up to a 1.33$\times$ end-to-end speedup while still improving the long-context capability by 0.46\% on the LongBench benchmark.

---

## 论文详细总结（自动生成）

### 论文总结：SparseBalance —— 基于动态稀疏注意力的负载均衡长文本训练

#### 1. 核心问题与整体含义（研究动机和背景）
长文本大语言模型（LLM）训练面临两大挑战：一是标准注意力的计算复杂度随序列长度呈平方级增长；二是分布式训练中的**负载不均衡**。
*   **现有痛点**：虽然稀疏注意力（Sparse Attention）能降低计算量，但引入了新的异构性：
    1.  **序列长度异构性**：数据集中长短文本混合（如 40% 长文本，60% 短文本），导致分布式节点间计算量差异巨大。
    2.  **稀疏敏感度异构性**：不同序列和不同 Transformer 层对稀疏度的敏感程度不同。
*   **研究动机**：现有的优化方案（如批处理或打包策略）通常只针对序列长度，且由于分箱问题的离散性，无法完全消除“落后者”（Straggler）效应。本文旨在通过**算法与系统的协同设计**，利用动态稀疏度调节来同时解决效率和精度问题。

#### 2. 方法论：核心思想与关键技术
SparseBalance 采用“由粗到细”的负载均衡策略，包含三个核心组件：
*   **工作负载感知动态稀疏度调节 (DST)**：
    *   **双向调节**：识别处于关键路径上的“瓶颈微批次”（Bottleneck Micro-batches），降低其注意力预算（增加稀疏度）以加速执行；同时，增加非瓶颈微批次的预算，利用流水线气泡（Bubbles）换取“免费的精度提升”。
    *   **锚点引导机制**：通过预设锚点（如均值、最小值）确定调节方向，并引入阈值 $p$ 限制累积注意力分数的损失，确保模型精度。
*   **稀疏感知批处理 (SAB)**：
    *   在 CPU 端进行粗粒度平衡。利用轻量级稀疏度估计器和延迟预测，将样本分配到不同的数据并行（DP）组和微批次中，为 DST 提供良好的初始分布。
*   **基于分析的延迟预测模块**：
    *   由于稀疏算子的实际延迟与理论 FLOPs 并不线性相关，该模块通过离线分析（Profiling）建立查找表，根据序列长度和稀疏度预算精确预测执行时间。

#### 3. 实验设计
*   **模型配置**：Qwen2.5-0.5B、Qwen2.5-3B 以及 Qwen2.5-3B LoRA。
*   **数据集**：使用真实长文本数据集 `ChatQA2-Long-SFT` 和 `LongAlign-10k`。
*   **基准测试 (Benchmark)**：
    *   **长文本能力**：LongBench（涵盖单/多文档问答、摘要、检索、代码）。
    *   **检索能力**：Needle-in-a-Haystack（大海捞针测试）。
    *   **通用能力**：ARC、BoolQ、HellaSwag、Piqa 等 7 个通用推理任务。
*   **对比方法**：以 MoBA（一种块稀疏注意力算法）为 Baseline，并对比了传统的基于长度的批处理（LBB）策略。

#### 4. 资源与算力
论文在两个 GPU 集群上进行了评估：
*   **集群 A**：4 个节点，共 32 张 **NVIDIA H200** GPU (141GB HBM3e)。
*   **集群 B**：4 个节点，共 32 张 **NVIDIA H20** GPU (141GB HBM3e)。
*   **软件栈**：基于 Megatron-LM 和 ms-swift 构建，使用 CUDA 12.4 和 PyTorch 2.5.1。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 0.5B 到 3B 不同规模的模型，验证了方法的可扩展性。
*   **消融实验**：详细分析了 DST 和 SAB 各自的贡献、不同锚点策略（Mean/Min/Max）的影响、稀疏度阈值 $p$ 的敏感度，以及微批次大小（MBS）的影响。
*   **充分性评价**：实验设计较为全面，不仅关注系统层面的加速比，还深入评估了模型在下游任务中的精度变化，证明了效率提升并非以牺牲精度为代价。

#### 6. 主要结论与发现
*   **效率提升**：SparseBalance 实现了高达 **1.33倍** 的端到端训练加速。
*   **精度优化**：在 LongBench 上，模型能力不仅没有下降，反而提升了 **0.46%**，验证了利用计算气泡提升精度的可行性。
*   **负载均衡**：相比传统的基于长度的批处理，SparseBalance 能更有效地减少分布式训练中的不均衡度（Imbalance Degree 从 1.81x 降至 1.34x）。
*   **帕累托前沿**：通过调节阈值 $p$，可以在训练速度和模型精度之间找到最佳平衡点（Mean 0.1 被证明是最佳实践配置）。

#### 7. 优点
*   **协同设计视角**：打破了算法（稀疏度）与系统（负载均衡）的壁垒，将稀疏度视为一种可调节的系统自由度。
*   **“免费精度”概念**：巧妙地将原本浪费的流水线等待时间转化为计算预算，提升了模型质量。
*   **低开销**：DST 模块复用了稀疏注意力原有的索引器分数，额外系统开销极低（小于总迭代时间的 1.5%）。

#### 8. 不足与局限
*   **依赖离线分析**：延迟预测模块需要针对特定硬件和算子实现进行预先分析，若硬件环境变化需重新构建查找表。
*   **模型规模限制**：实验主要集中在 3B 及以下规模的模型，虽然理论上可扩展，但在超大规模模型（如 70B+）上的表现尚未验证。
*   **特定稀疏算法**：目前主要基于块稀疏（Block-sparse）范式，对于非块状的稀疏模式（如随机稀疏）可能需要额外的适配工作。

（完）
