---
title: Does a Global Perspective Help Prune Sparse MoEs Elegantly?
title_zh: 全局视角是否有助于优雅地剪枝稀疏混合专家模型？
authors: "Zeliang Zhang, Nikhil Ghosh, Jiani Liu, Bin Yu, Xiaodong Liu"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06542v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 稀疏MoE模型的全局剪枝策略以减少内存消耗
tldr: "针对稀疏混合专家模型（MoE）因参数量巨大导致的内存开销问题，本文提出了一种名为 GRAPE 的全局冗余感知专家剪枝策略。不同于传统在各层均匀分配剪枝预算的方法，GRAPE 通过跨层动态分配预算来应对 MoE 内部的异构冗余。在 Mixtral、DeepSeek 和 Qwen 等多个主流模型上的实验表明，GRAPE 在相同剪枝比例下显著优于局部剪枝基准，平均准确率提升达 1.40%，最高提升 2.45%，为高效压缩大规模 MoE 模型提供了新思路。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06542v1/fig-001.webp\", \"caption\": \"Table 1: Accuracy (%) on pruning Mixtral-8x22B, and DeepSeek-MoE with 2 and 4 experts per MoE layer. Each cell reports accuracy in the format 2e/4e, where we denote e as the number of experts to prune in each layer.\", \"page\": 4, \"index\": 1, \"width\": 950, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2604.06542v1/fig-002.webp\", \"caption\": \"Table 2: Accuracy (%) on pruning GPT-OSS with 2 and 4 experts per MoE layer. Each cell reports accuracy in the format 2e/4e, where we denote e as the number of experts to prune in each layer.\", \"page\": 5, \"index\": 2, \"width\": 943, \"height\": 209}, {\"url\": \"assets/figures/arxiv/2604.06542v1/fig-003.webp\", \"caption\": \"Figure 2: Results of Mixtral-8x7B and Qwen-MoE.\", \"page\": 7, \"index\": 3, \"width\": 950, \"height\": 293}]"
motivation: 现有的 MoE 剪枝方法通常在各层均匀分配预算，忽略了不同层之间专家冗余程度的差异。
method: 提出 GRAPE 策略，通过全局视角评估跨层冗余，动态地为不同层分配剪枝预算。
result: "在 Mixtral、DeepSeek-MoE 等多个模型上，GRAPE 相比最强局部基准平均提升了 1.40% 的准确率。"
conclusion: 采用全局视角进行冗余感知剪枝是提升稀疏 MoE 模型压缩效率和性能的关键。
---

## 摘要
语言模型的经验性缩放法则（scaling laws）促使了规模日益庞大的大语言模型（LLMs）的发展，尽管其计算和内存成本也在不断增加。稀疏混合专家模型（MoEs）通过在每次前向传播中仅激活一部分专家，提供了一种极具前景的替代方案，在不牺牲性能的情况下提高了效率。然而，大量的专家参数仍然导致了巨大的内存消耗。现有的剪枝方法通常在各层之间均匀分配预算，忽略了稀疏 MoE 中出现的异构冗余。我们提出了 GRAPE（全局冗余感知专家剪枝），这是一种根据跨层冗余动态分配剪枝预算的全局剪枝策略。在 Mixtral-8x7B、Mixtral-8x22B、DeepSeek-MoE、Qwen-MoE 和 GPT-OSS 上的实验表明，在相同的剪枝预算下，GRAPE 始终能获得最佳的平均性能。在论文报告的三个主要模型上，与最强的局部基准方法相比，它在各种剪枝设置下的平均准确率平均提高了 1.40%，最高增幅达 2.45%。

## Abstract
Empirical scaling laws for language models have encouraged the development of ever-larger LLMs, despite their growing computational and memory costs. Sparse Mixture-of-Experts (MoEs) offer a promising alternative by activating only a subset of experts per forward pass, improving efficiency without sacrificing performance. However, the large number of expert parameters still leads to substantial memory consumption.   Existing pruning methods typically allocate budgets uniformly across layers, overlooking the heterogeneous redundancy that arises in sparse MoEs. We propose GRAPE (Global Redundancy-Aware Pruning of Experts, a global pruning strategy that dynamically allocates pruning budgets based on cross-layer redundancy. Experiments on Mixtral-8x7B, Mixtral-8x22B, DeepSeek-MoE, Qwen-MoE, and GPT-OSS show that, under the same pruning budget, GRAPE consistently achieves the best average performance. On the three main models reported in the paper, it improves average accuracy over the strongest local baseline by 1.40% on average across pruning settings, with gains of up to 2.45%.

---

## 论文详细总结（自动生成）

这篇论文探讨了稀疏混合专家模型（MoE）的剪枝优化问题，提出了名为 **GRAPE** 的全局剪枝策略。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：随着大语言模型（LLM）规模的扩大，稀疏 MoE 架构因其“计算量小但性能强”的特性受到青睐。然而，MoE 模型虽然在推理时只激活部分专家，但所有专家参数都必须驻留在内存中，导致了巨大的显存开销。
*   **核心问题**：现有的 MoE 剪枝方法（如基于访问频率、路由权重或特征相似度）通常采用**局部均匀分配**策略，即在每一层中剪掉相同数量的专家。
*   **研究动机**：作者观察到 MoE 模型不同层之间的专家冗余程度存在显著差异（异构性）。传统的均匀剪枝忽略了这种跨层差异，导致剪枝后的模型性能无法达到最优。

### 2. 方法论
*   **核心思想**：提出 **GRAPE（Global Redundancy-Aware Pruning of Experts）**，从全局视角出发，根据各层的冗余程度动态分配剪枝预算，而非每层“一刀切”。
*   **关键技术细节**：
    1.  **冗余度评估**：利用中心核对齐（CKA）或均方误差（MSE）计算每层专家之间的相似度矩阵，定义层级冗余分数。
    2.  **全局目标函数**：在总剪枝专家数量固定的前提下，最小化被剪枝专家之间的全局相似度总和。
    3.  **全局熵正则化**：为了防止剪枝过度集中在某些极高冗余的层（导致层级失衡或模型崩溃），引入了“全局熵”概念。熵值越高，表示保留的专家在各层分布越均匀。
    4.  **带重启的贪婪剪枝算法**：
        *   在每一轮迭代中，选择当前冗余度最高的层，合并该层中最相似的一对专家。
        *   实时监控全局熵，若熵值低于预设阈值，则暂时“冻结”该层，防止过度剪枝。
        *   若所有层都被冻结但尚未达到剪枝目标，则重置冻结状态并继续，确保达到预算。

### 3. 实验设计
*   **实验对象（Models）**：涵盖了多种主流 MoE 模型，包括 Mixtral-8x7B、Mixtral-8x22B、DeepSeek-MoE-16B、Qwen-MoE 和 GPT-OSS（20B）。
*   **基准测试（Benchmarks）**：在 MMLU（综合知识）、BoolQ（布尔问答）、OpenBookQA（开卷问答）和 RTE（蕴含关系推理）等数据集上进行评估。
*   **对比方法**：
    *   **Router-guided**：基于路由信息的剪枝。
    *   **Count-guided**：基于专家访问频率的剪枝。
    *   **Enumerate**：基于损失景观搜索的剪枝。
    *   **DEK**：基于输出表示相似度的局部剪枝。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量及总训练/剪枝时长。
*   **实现细节**：由于该方法属于“一发式（One-shot）”剪枝且无需微调（Task-agnostic），其计算开销主要集中在计算专家相似度矩阵上，相比模型训练而言，算力需求较低。

### 5. 实验数量与充分性
*   **实验规模**：作者在 5 种不同架构、不同规模的 MoE 模型上进行了实验，每种模型设置了剪掉 2 个专家（2e）和 4 个专家（4e）两种预算场景。
*   **充分性评价**：实验设计较为充分。通过跨模型、跨任务的对比，证明了全局视角优于局部视角。同时，附录中补充了 Mixtral-8x7B 和 Qwen-MoE 的结果，增强了结论的普适性。实验对比在相同的总专家预算下进行，保证了公平性。

### 6. 主要结论与发现
*   **全局策略占优**：在所有测试模型中，GRAPE 始终优于最强的局部剪枝基准。在主要模型上，平均准确率提升了 1.40%，最高提升达 2.45%。
*   **冗余异构性**：实验证实了 MoE 层的冗余度并非单调变化，通常中间层或后几层冗余度更高，这为非均匀分配预算提供了理论依据。
*   **熵的重要性**：如果没有熵正则化，全局剪枝容易导致某些层被“掏空”，从而引发模型性能骤降。

### 7. 优点
*   **视角创新**：打破了“每层对等”的思维定式，将 MoE 剪枝问题从局部优化提升到全局资源配置的高度。
*   **即插即用**：作为一种任务无关的剪枝方法，它不需要昂贵的重训练或微调过程，易于部署。
*   **鲁棒性强**：通过引入重启机制和熵阈值，有效解决了贪婪算法可能导致的层级失衡问题。

### 8. 不足与局限
*   **极端情况下的风险**：在 DeepSeek-MoE 等某些特定模型上，由于其本身设计（如共享专家），全局剪枝带来的增益相对较小，且仍存在模型崩溃的潜在风险。
*   **指标依赖性**：冗余度的衡量依赖于相似度指标（如 CKA），不同指标的选择可能会影响最终的剪枝决策。
*   **推理加速未体现**：论文主要关注内存压缩（减少参数量），未详细讨论剪枝对实际推理延迟（Latency）的具体影响。

（完）
