---
title: Comparative Characterization of KV Cache Management Strategies for LLM Inference
title_zh: 大语言模型推理中 KV 缓存管理策略的对比特性分析
authors: "Oteo Mamo, Olga Kogiou, Hyunjin Yi, Weikuan Yu"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05012v1"
tags: ["query:mlsys"]
score: 9.0
evidence: LLM推理中的KV缓存管理策略
tldr: 随着大语言模型规模和上下文长度增加，KV缓存带来的内存压力成为推理瓶颈。本文对vLLM、InfiniGen和H2O三种主流KV缓存管理框架进行了实证研究，分析了它们在张量卸载、令牌剔除和推测调度等技术上的差异。通过在不同请求率、模型大小和稀疏度下评估延迟、吞吐量和内存占用，本文揭示了各框架的优劣势及适用场景，为在资源受限下优化LLM推理性能提供了配置指南。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05012v1/fig-001.webp\", \"caption\": \"Fig. 6: Decode scaling with output length (10K-token input prompt, Llama-3.1-8B). (a) Decode throughput. (b) Walltime breakdown; logarithmic scale used to visualize both prefill and decode components.\", \"page\": 6, \"index\": 1, \"width\": 492, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2604.05012v1/fig-002.webp\", \"caption\": \"Fig. 7: Accuracy comparison of InfiniGen, H2O, and vLLM\", \"page\": 6, \"index\": 2, \"width\": 529, \"height\": 413}, {\"url\": \"assets/figures/arxiv/2604.05012v1/fig-003.webp\", \"caption\": \"Fig. 8: Retention accuracy for early-context facts (Llama3.1-8B, LMSYS-Chat-1M). (a) Accuracy by context-length bucket. (b) Accuracy evolution across conversation turns.\", \"page\": 7, \"index\": 3, \"width\": 529, \"height\": 314}, {\"url\": \"assets/figures/arxiv/2604.05012v1/fig-004.webp\", \"caption\": \"Fig. 1: TTFT scaling with prompt length. (a) Baseline comparison showing OOM boundaries for H2O and InfiniGen. (b) Extended TTFT with FA and CP optimizations. (c,d) MultiGPU scaling on larger models.\", \"page\": 4, \"index\": 4, \"width\": 529, \"height\": 496}, {\"url\": \"assets/figures/arxiv/2604.05012v1/fig-005.webp\", \"caption\": \"Fig. 2: InfiniGen memory and latency breakdown across optimization strategies (Llama-3.1-8B). (a) GPU memory at 10K–100K token contexts. (b) Prefill and decode latency at equivalent context lengths. Hatched bars indicate OOM. Percentages show reduction relative to the comparison baseline.\", \"page\": 4, \"index\": 5, \"width\": 529, \"height\": 244}, {\"url\": \"assets/figures/arxiv/2604.05012v1/fig-006.webp\", \"caption\": \"Fig. 4: Decode throughput (a) and memory efficiency (b) across batch sizes. Efficiency computed as throughput divided by GPU memory consumption. Error bars and shaded regions indicate variance across runs.\", \"page\": 5, \"index\": 6, \"width\": 492, \"height\": 255}, {\"url\": \"assets/figures/arxiv/2604.05012v1/fig-007.webp\", \"caption\": \"Fig. 5: End-to-end latency analysis across batch sizes. (a) Walltime decomposed into prefill, cache operations, and decode phases. (b) Total latency scaling.\", \"page\": 5, \"index\": 7, \"width\": 492, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2604.05012v1/fig-008.webp\", \"caption\": \"Fig. 3: GPU and CPU memory breakdown by component across batch sizes (Llama-3.1-8B, prompt lengths 1 to 10K tokens). Memory is decomposed into model weights, activations, KV cache, and runtime overhead.\", \"page\": 5, \"index\": 8, \"width\": 529, \"height\": 264}]"
motivation: KV缓存的快速增长给LLM推理带来了严峻的内存挑战，且现有不同管理框架间的性能权衡关系尚不明确。
method: 通过实证研究对比了vLLM、InfiniGen和H2O三种框架在不同请求速率、模型规模和稀疏度下的延迟、吞吐量及内存表现。
result: 实验确定了各框架在不同工作负载下的最佳表现区间，揭示了内存消耗与推理性能之间的关键权衡点。
conclusion: 本研究为在特定内存和性能约束下选择及配置最合适的KV缓存管理策略提供了实用的指导建议。
---

## 摘要
大语言模型（LLM）的高效推理日益依赖于键值（KV）缓存，用于存储每一层先前计算出的键向量和值向量。这些缓存对于减少自回归 Token 生成过程中的冗余计算至关重要，能将计算复杂度从平方级降低到线性级。然而，随着模型规模的扩大、上下文长度的增加以及并发请求对有限内存资源的竞争，KV 缓存的增长带来了显著的系统级挑战。尽管近期出现了几种 KV 缓存管理框架，但它们在内存消耗和推理性能之间的权衡关系尚未得到充分理解，特别是在不同的请求规模和模型配置下。在这项工作中，我们对三种最先进的 KV 缓存管理框架（vLLM、InfiniGen 和 H2O）进行了实证研究。这些框架采用了张量卸载、Token 逐出启发式算法和推测性调度等技术，以平衡内存使用和性能。我们根据延迟、吞吐量和内存使用等一系列指标，在包括请求率、模型大小和稀疏度水平在内的关键参数范围内评估了它们的性能。我们的结果确定了每个框架表现最佳的条件，揭示了在内存和性能约束下最合适的 KV 缓存策略选择和配置。

## Abstract
Efficient inference with Large Language Models (LLMs) increasingly relies on Key-Value (KV) caches to store previously computed key and value vectors at each layer. These caches are essential to minimize redundant computation during autoregressive token generation, lowering computational complexity from quadratic to linear. However, the growth of KV caches has posed significant system-level challenges, particularly as model sizes increase, context lengths grow, and concurrent requests compete for limited memory resources. Even though several recent frameworks for KV cache management have emerged, their comparative trade-offs in memory consumption and inference performance have not been fully understood, especially under varying request sizes and model configurations. In this work, we conduct an empirical study of three state-of-the-art KV cache management frameworks: vLLM, InfiniGen, and H2O. These frameworks employ techniques such as tensor offloading, token eviction heuristics, and speculative scheduling to balance memory usage and performance. We evaluate their performance in terms of a range of metrics such as latency, throughput, and memory usage across a spectrum of key parameters including request rates, model sizes, and sparsity levels. Our results pinpoint the conditions for each framework to perform the best, revealing the most suitable selection and configuration of KV cache strategies under memory and performance constraints.

---

## 论文详细总结（自动生成）

这是一份关于论文《Comparative Characterization of KV Cache Management Strategies for LLM Inference》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：随着大语言模型（LLM）上下文长度和并发请求的增加，KV Cache（键值缓存）消耗的内存呈爆炸式增长，甚至超过模型参数本身。虽然涌现出多种管理策略，但不同范式（如内存管理、静态稀疏化、动态选择）之间的性能权衡、内存效率与准确度损失的对比关系尚不明确。
*   **研究背景**：KV Cache 将注意力机制的计算复杂度从平方级降至线性级，但带来了巨大的显存压力。业界急需一份实证研究，指导开发者在特定硬件约束和业务需求下选择最合适的 KV 缓存管理框架。

### 2. 论文提出的方法论
论文对比了三种代表不同技术范式的最先进框架：
*   **vLLM（内存管理范式）**：核心是 **PagedAttention**。它将 KV 缓存划分为固定大小的块，允许非连续存储，减少内存碎片。它不剔除任何 Token，旨在通过高效的内存分配提高吞吐量。
*   **H2O（静态稀疏化范式）**：核心是 **Heavy Hitter Oracle**。基于注意力权重分布高度倾斜的观察，在 Prefill（预填充）阶段识别贡献最大的“重击者”Token 并保留，永久剔除其余 Token。这是一种不可逆的压缩策略。
*   **InfiniGen（动态选择范式）**：核心是 **分层存储与推测性预取**。将完整的 KV 缓存存储在 CPU 内存中，在 Decode（解码）阶段利用 SVD（奇异值分解）和推测机制，动态地将当前步骤需要的 KV 条目从 CPU 传输到 GPU。

### 3. 实验设计
*   **模型选择**：Llama-3.1-8B、Llama-3.1-70B、GPT-OSS-20B（均采用 GQA 分组查询注意力机制）。
*   **数据集**：
    *   **性能测试**：LMSYS-Chat-1M（真实对话）、Wikipedia（长文本压力测试）。
    *   **准确度测试**：PIQA、HellaSwag、COPA、WinoGrande、OpenBookQA、BoolQ 等推理基准。
    *   **长文本保留测试**：自定义的“事实检索”任务，测试模型对早期上下文信息的记忆能力。
*   **对比维度**：首字延迟（TTFT）、解码吞吐量、端到端延迟、GPU/CPU 内存占用、推理准确度。

### 4. 资源与算力
*   **硬件环境**：一台计算节点，配备 **4 张 NVIDIA H100 GPU (80GB HBM3)**。
*   **处理器**：双路 Intel Sapphire Rapids 处理器（每路 56 核心）。
*   **互联**：NVLink 和 PCIe 5.0。
*   **时长**：文中未明确说明总训练/测试时长，但详细记录了不同 Batch Size 和序列长度下的运行时间。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从单 GPU 到多 GPU（4x H100）的扩展性实验，Batch Size 从 16 扩展到 96，上下文长度最高测试到 128K。
*   **充分性**：实验设计非常全面，不仅关注系统指标（吞吐、延迟），还深入探讨了算法优化（如 FlashAttention-2 和分块预填充对 TTFT 的影响），并加入了长文本保留能力的定性分析。
*   **客观性**：通过在相同准确度约束下校准 H2O 和 InfiniGen 的参数，确保了系统级指标对比的公平性。

### 6. 主要结论与发现
*   **TTFT 瓶颈**：在长上下文中，H2O 和 InfiniGen 容易因 OOM 崩溃，除非集成 FlashAttention-2 或分块预填充（Chunked Prefill）。
*   **吞吐量差异**：vLLM 和 H2O 的吞吐量随 Batch Size 线性增长；而 InfiniGen 由于频繁的 CPU-GPU 数据传输，吞吐量比前两者低一个数量级。
*   **内存效率**：H2O 在 GPU 内存节省上表现最佳（最高减少 70% 占用）；InfiniGen 虽然节省 GPU 内存，但对 CPU 内存和带宽要求极高。
*   **准确度权衡**：InfiniGen 在高稀疏度下比 H2O 更能保持准确度，尤其是在长对话的事实保留任务中，H2O 的永久剔除策略会导致明显的记忆丢失。

### 7. 优点
*   **跨范式对比**：打破了单一框架研究的局限，首次系统性对比了“不剔除”、“静态剔除”与“动态卸载”三种逻辑。
*   **实战指导性强**：为不同场景（延迟敏感型 vs. 内存受限型）提供了明确的选择建议。
*   **深度分析**：不仅给出了结果，还解释了 OOM 的深层原因（如张量并行下注意力矩阵的局部计算瓶颈）。

### 8. 不足与局限
*   **框架覆盖有限**：仅选择了三个代表性框架，未涵盖量化（Quantization）等其他重要的 KV 缓存压缩技术。
*   **硬件依赖性**：InfiniGen 的性能高度受限于 PCIe 带宽，实验结论可能在具备更高带宽（如 NVLink-C2C）的系统上发生变化。
*   **实现偏差**：H2O 是在 FlexGen 基础上实现的，其性能表现可能受限于底层实现而非算法本身。

（完）
