---
title: Tight Convergence Rates for Online Distributed Linear Estimation with Adversarial Measurements
title_zh: 具有对抗性测量的在线分布式线性估计的紧致收敛率
authors: "Nibedita Roy, Vishal Halder, Gugan Thoppe, Alexandre Reiffers-Masson, Mihir Dhanakshirur, Naman, Alexandre Azor"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06282v1"
tags: ["query:mlsys"]
score: 7.0
evidence: 分布式参数服务器-工人架构与收敛率
tldr: 本研究针对分布式参数服务器架构下的线性均值估计问题，探讨了在存在对抗性测量（部分节点被破坏）和异步激活（每次仅一个节点活跃）挑战下的收敛性能。在前期提出的双尺度ℓ1最小化算法基础上，本文在类零空间属性（NSP）条件下建立了紧致的非渐近收敛率，并分析了在放宽条件下的部分分量恢复可能性，为分布式感知的鲁棒性和效率提供了统一的有限时间刻画。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06282v1/fig-001.webp\", \"caption\": \"Figure 1. Comparison of Algorithm 1’s performance with that of existing state of the art robust-aggregator-based methods (see the legend to know the various methods we compare against). Subplots (a), (b), and (c) correspond to the synchronous case, while subplots (d), (e), and (f) correspond to the asynchronous case. In the synchronous-scenario plots, BKT stands for the bucketing variant, while it denotes the buffered variant in the asynchronous case. Subplots (a) and (d) correspond to the case where we update xn in Algorithm 1 using the 1/ √ n + 1 stepsize and the xn in other methods with the 1/(n + 1)0.9 stepsize. In subplots (b) and (e), we update xn for all methods using the 1/ √ n + 1 stepsize. In subplots (c) and (f), we multiple A by 10 (to artificially add heterogeneity) and rerun the experiments with stepsizes as in subplots (b) and (e). Note that the error in all the subplots is obtained by averaging over 10 runs.\", \"page\": 8, \"index\": 1, \"width\": 1090, \"height\": 829}, {\"url\": \"assets/figures/arxiv/2604.06282v1/fig-002.webp\", \"caption\": \"Figure 2. Network setup, corresponding path-link matrix, and the matrix A in the associated decomposition.\", \"page\": 9, \"index\": 2, \"width\": 1004, \"height\": 305}]"
motivation: 旨在解决分布式线性估计中由于部分节点恶意篡改数据和异步通信带来的鲁棒性与收敛效率问题。
method: 基于双尺度ℓ1最小化算法，在类零空间属性条件下对算法的非渐近收敛性能进行理论推导。
result: 成功建立了紧致的非渐近收敛率，并识别出在无法完全恢复时仍能实现部分投影分量恢复的放宽条件。
conclusion: 该研究为对抗环境下分布式感知的鲁棒性、可辨识性和统计效率提供了统一的有限时间理论保障。
---

## 摘要
我们研究了在分布式参数服务器-工作节点架构下随机向量 $X$ 的均值估计问题。工作节点 $i$ 观测 $a_i^\top X$ 的样本，其中 $a_i^\top$ 是已知感知矩阵 $A$ 的第 $i$ 行。关键挑战在于对抗性测量和异步性：工作节点的一个固定子集可能会传输受损的测量值，且工作节点是异步激活的——在任何时刻只有一个节点处于活跃状态。在我们之前的工作中，我们提出了一种双时间尺度 $\ell_1$ 最小化算法，并在 $A$ 满足类零空间性质的条件下建立了渐近恢复。在本研究中，我们在相同的类零空间性质条件下建立了紧致的非渐近收敛率。我们还确定了 $A$ 的放宽条件，在这些条件下，精确恢复可能会失败，但恢复 $\mathbb{E}[X]$ 的投影分量仍然是可能的。总之，我们的结果为具有对抗性工作节点的分布式线性估计中的鲁棒性、可辨识性和统计效率提供了统一的有限时间表征，对网络拓扑测量及相关的分布式感知问题具有重要意义。

## Abstract
We study mean estimation of a random vector $X$ in a distributed parameter-server-worker setup. Worker $i$ observes samples of $a_i^\top X$, where $a_i^\top$ is the $i$th row of a known sensing matrix $A$. The key challenges are adversarial measurements and asynchrony: a fixed subset of workers may transmit corrupted measurements, and workers are activated asynchronously--only one is active at any time. In our previous work, we proposed a two-timescale $\ell_1$-minimization algorithm and established asymptotic recovery under a null-space-property-like condition on $A$. In this work, we establish tight non-asymptotic convergence rates under the same null-space-property-like condition. We also identify relaxed conditions on $A$ under which exact recovery may fail but recovery of a projected component of $\mathbb{E}[X]$ remains possible. Overall, our results provide a unified finite-time characterization of robustness, identifiability, and statistical efficiency in distributed linear estimation with adversarial workers, with implications for network tomography and related distributed sensing problems.