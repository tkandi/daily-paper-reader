---
title: "PipeLive: Efficient Live In-place Pipeline Parallelism Reconfiguration for Dynamic LLM Serving"
title_zh: PipeLive：面向动态 LLM 服务的高效在线原位流水线并行重配置
authors: "Xu Bai, Muhammed Tawfiqul Islam, Chen Wang, Adel N. Toosi"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12171v1"
tags: ["query:mlsys"]
score: 10.0
evidence: 针对动态LLM服务的实时就地流水线并行重配置
tldr: PipeLive 针对动态 LLM 推理环境，提出了一种高效的在线流水线并行重配置方案。它解决了现有系统重配置时需停机、显存受限及 KV 缓存一致性难维持的问题。通过重新设计 KV 缓存布局与 PageAttention 扩展实现动态缩放，并引入增量 KV 补丁机制实现无缝切换，在不中断推理的情况下显著降低了重配置开销和延迟。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12171v1/fig-001.webp\", \"caption\": \"Figure 4. Timeline of asynchronous PP reconfiguration protocol.\", \"page\": 6, \"index\": 1, \"width\": 504, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2604.12171v1/fig-002.webp\", \"caption\": \"Figure 10. End-to-end performance of PP reconfiguration with kv resizing disabled and enabled under mixed workload.\", \"page\": 11, \"index\": 2, \"width\": 824, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2604.12171v1/fig-003.webp\", \"caption\": \"Figure 9. Performance of different PP configurations for heterogeneous GPUs under the mixed decode-heavy and prefill-heavy workloads.\", \"page\": 10, \"index\": 3, \"width\": 1047, \"height\": 351}, {\"url\": \"assets/figures/arxiv/2604.12171v1/fig-004.webp\", \"caption\": \"Figure 2. Architecture of PipeLive.\", \"page\": 3, \"index\": 4, \"width\": 404, \"height\": 396}, {\"url\": \"assets/figures/arxiv/2604.12171v1/fig-005.webp\", \"caption\": \"Figure 3. Live in-place PP reconfiguration workflow in PipeLive.\", \"page\": 4, \"index\": 5, \"width\": 506, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2604.12171v1/fig-006.webp\", \"caption\": \"Figure 8. Performance of different PP configurations for heterogeneous GPUs under varying workloads.\", \"page\": 9, \"index\": 6, \"width\": 836, \"height\": 414}, {\"url\": \"assets/figures/arxiv/2604.12171v1/fig-007.webp\", \"caption\": \"Table 1. Notations\", \"page\": 5, \"index\": 7, \"width\": 490, \"height\": 275}]"
motivation: 现有的流水线并行配置是静态的，无法适应动态变化的 GPU 环境，且停机重配置会导致严重的推理中断和性能损失。
method: 重新设计了 KV 缓存布局与 PageAttention 扩展以支持动态缩放，并采用增量 KV 补丁机制实现源与目标配置间的状态同步。
result: "该方案将重配置开销从秒级降至 10ms 以下，并使首字延迟（TTFT）和单字生成延迟（TPOT）分别优化了高达 54.7% 和 14.7%。"
conclusion: PipeLive 实现了高效的在线原位流水线并行重配置，为动态和异构环境下的 LLM 服务提供了高可用性和高性能保障。
---

## 摘要
流水线并行（PP）被广泛用于在多 GPU 间划分大语言模型（LLM）的层，以实现大模型的可扩展推理。然而，现有系统依赖静态 PP 配置，难以适应无服务器平台和异构 GPU 环境等动态场景。通过停止并重新部署服务来进行 PP 重配置会导致高昂的停机成本，因此重配置必须在不中断推理的情况下在线原位（live and in-place）进行。然而，在线原位 PP 重配置面临严峻挑战：GPU 资源已被模型权重和 KV 缓存（KV cache）占满，难以容纳新层，且需要调整 KV 缓存大小，这与 vLLM 等预分配内存以优化吞吐量的系统相冲突。此外，在执行过程中保持 KV 一致性十分困难：“停止并复制”会引发长时间停顿，而后台同步则因状态演变存在不一致风险。本文提出 PipeLive，旨在以极小的中断实现在线原位 PP 重配置。PipeLive 引入了重新设计的 KV 缓存布局及协同设计的 PageAttention 扩展，构建了统一的在线 KV 调整机制。此外，它借鉴虚拟机在线迁移技术，采用增量 KV 补丁机制同步源与目标配置间的 KV 状态，并确定安全切换点。实验表明，与禁用 KV 调整相比，PipeLive 在避免 KV 缓存溢出的同时将首字延迟（TTFT）降低了 2.5 倍。与不使用 KV 补丁的方案相比，它将重配置开销从秒级降至 10 毫秒以下，并将 TTFT 和逐字延迟（TPOT）分别优化了高达 54.7% 和 14.7%。

## Abstract
Pipeline parallelism (PP) is widely used to partition layers of large language models (LLMs) across GPUs, enabling scalable inference for large models. However, existing systems rely on static PP configurations that fail to adapt to dynamic settings, such as serverless platforms and heterogeneous GPU environments. Reconfiguring PP by stopping and redeploying service incurs prohibitive downtime, so reconfiguration must instead proceed live and in place, without interrupting inference. However, live in-place PP reconfiguration is fundamentally challenging. GPUs are already saturated with model weights and KV cache, leaving little room for new layer placements and necessitating KV cache resizing, at odds with systems like vLLM that preallocate for throughput. Moreover, maintaining KV consistency during execution is difficult: stop-and-copy introduces large pauses, while background synchronization risks inconsistency as states evolve. We present PipeLive, which enables live in-place PP reconfiguration with minimal disruption. PipeLive introduces a redesigned KV cache layout together with a co-designed extension to PageAttention, forming a unified mechanism for live KV resizing. It further adopts an incremental KV patching mechanism, inspired by live virtual machine migration, to synchronize KV states between source and target configurations and identify a safe switch point. PipeLive achieves a 2.5X reduction in time-to-first-token (TTFT) without KV cache overflow compared to disabling KV resizing. Furthermore, compared to a variant without KV patching, it reduces reconfiguration overhead from seconds to under 10ms, and improves TTFT and time-per-output-token (TPOT) by up to 54.7% and 14.7%, respectively.

---

## 论文详细总结（自动生成）

### 论文总结：PipeLive — 面向动态 LLM 服务的高效在线原位流水线并行重配置

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）规模的扩大，流水线并行（Pipeline Parallelism, PP）成为跨多 GPU 部署的标准方案。然而，现有的推理系统（如 vLLM）通常采用**静态 PP 配置**，这在动态环境下存在显著缺陷：
*   **负载动态性：** “预填充（Prefill）密集型”和“解码（Decode）密集型”任务对计算和带宽的需求不同，最优的层划分方案随负载变化而变化。
*   **硬件异构性：** 在混合 GPU 环境中（如 A100 与 L40S 混搭），静态配置无法充分利用不同硬件的特性。
*   **重配置成本高：** 传统的重配置需要停止服务、清空队列并重启，导致分钟级的停机时间，无法应对秒级的负载波动。

**PipeLive 的核心目标**是在不中断推理服务的前提下，实现在线的、原位的（In-place）流水线并行重配置，使系统能动态切换到当前负载下的最优配置。

#### 2. 方法论：核心思想与关键技术
PipeLive 提出了一个自顶向下的框架，包含协调器（Coordinator）和工作节点（Worker）两层架构：

*   **重配置协议（五阶段流程）：**
    1.  **可行性评估：** 计算目标配置下各 GPU 显存是否足以容纳新层权重和 KV 缓存。
    2.  **KV 缓存收缩：** 动态缩小现有 KV 缓存容量，为即将加载的新层腾出显存空间。
    3.  **异步加载与迁移：** 在后台加载新层权重，同时利用 NCCL 异步迁移 KV 状态。
    4.  **收敛监控：** 跟踪迁移进度，确保目标位置的 KV 状态与源位置同步。
    5.  **原子切换：** 在极短的同步点（<10ms）切换到新配置，并清理旧层。

*   **关键技术细节：**
    *   **动态 KV 缓存管理：** 扩展了 PageAttention，支持**非连续显存块访问**。通过“块级分配”取代“预分配连续缓冲区”，实现了 KV 缓存的动态缩放。
    *   **层堆叠（Layer Stacking）：** 为解决 CUDA 显存分配粒度（2MB）与 KV 块（32-128KB）不匹配导致的碎片问题，将多个层的 KV 状态打包进一个物理块。
    *   **增量 KV 补丁（KV Patching）：** 借鉴虚拟机迁移技术，利用“脏位图（Dirty Bitmap）”记录推理过程中新生成的 KV 状态，并进行增量同步，避免了“停止并复制”带来的长停顿。
    *   **死锁避免协议：** 引入两阶段握手机制，解决推理流与迁移流在并发使用 NCCL 时的死锁风险。

#### 3. 实验设计
*   **实验场景：** 异构 GPU 测试床，模拟负载模式切换（Prefill-heavy 与 Decode-heavy 交替）。
*   **数据集/模型：** 使用 Llama 3-70B 和 Qwen3-30B 模型。
*   **Benchmark：** 构造了“模式切换基准负载”，包含 200 个请求，交替变换输入/输出长度比例。
*   **对比方法：**
    *   **静态基准：** Prefill-Optimal（预填充最优配置）、Decode-Optimal（解码最优配置）、Balanced（平衡配置）。
    *   **消融实验：** 禁用 KV 缩放的 PipeLive、禁用 KV 补丁的 PipeLive、同步迁移模式等。

#### 4. 资源与算力
*   **硬件配置：** 实验在一台配备 **1 张 NVIDIA A100 (80GB)** 和 **1 张 NVIDIA L40S (48GB)** 的异构服务器上完成。
*   **通信：** 两张显卡通过 **InfiniBand (100 Gbps)** 连接，使用 NCCL 进行数据传输。
*   **软件：** 基于 vLLM 框架开发，涉及约 5000 行 Python 和 CUDA/C++ 代码。

#### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **端到端性能：** 测试了不同模型在不同请求速率下的 TTFT、TPOT 和吞吐量。
*   **组件分析：** 针对 KV 缩放、层堆叠粒度（1, 2, 4, 8, 16 层堆叠）、异步加载与补丁机制分别做了详细的消融实验。
*   **充分性评价：** 实验设计较为充分，涵盖了异构环境下的核心痛点。通过对比多种静态配置，客观证明了动态重配置的必要性。实验不仅关注宏观吞吐量，还深入分析了重配置过程中的瞬时停顿时间。

#### 6. 主要结论与发现
*   **性能提升：** 在异构环境下，PipeLive 相比最优的静态配置，综合性能得分提升了 **33%–36%**。
*   **延迟优化：** 相比禁用 KV 缩放的方案，PipeLive 将 TTFT 降低了 **2.5 倍**；相比无补丁的迁移，TTFT 和 TPOT 分别优化了 **54.7%** 和 **14.7%**。
*   **极低中断：** 增量补丁机制将重配置导致的推理停顿时间从秒级降低至 **10ms 以下**。
*   **显存效率：** 层堆叠技术显著提升了显存利用率，在堆叠因子为 4 时达到了性能与灵活性的平衡。

#### 7. 优点
*   **实用性强：** 解决了 vLLM 等主流框架中 KV 缓存预分配导致的“无法原位调整”的硬伤。
*   **设计精巧：** 借鉴成熟的虚拟机迁移思路解决 AI 模型状态同步问题，逻辑清晰且效果显著。
*   **低侵入性：** 能够在不中断现有推理流的情况下完成复杂的层迁移和显存重组。

#### 8. 不足与局限
*   **策略缺失：** 论文重点在于“如何执行”重配置，但对于“何时触发”以及“如何计算最优目标配置”的决策算法未做深入探讨（作者将其列为未来工作）。
*   **并行模式局限：** 目前仅讨论了流水线并行（PP）的重配置，未涉及张量并行（TP）或数据并行（DP）的动态调整。
*   **硬件依赖：** 实验主要基于 InfiniBand 高速互连，在带宽受限的环境（如普通以太网）下，KV 迁移的收敛速度和对推理的影响可能更显著。

（完）
