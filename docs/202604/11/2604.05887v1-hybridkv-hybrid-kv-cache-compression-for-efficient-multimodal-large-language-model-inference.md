---
title: "HybridKV: Hybrid KV Cache Compression for Efficient Multimodal Large Language Model Inference"
title_zh: HybridKV：用于高效多模态大语言模型推理的混合 KV 缓存压缩
authors: "Bowen Zeng, Feiyang Ren, Jun Zhang, Xiaoling Gu, Ke Chen, Lidan Shou, Huan Li"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05887v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 针对多模态大模型推理的KV缓存压缩优化
tldr: 针对多模态大模型（MLLM）推理中视觉Token导致KV缓存激增的问题，本文提出HybridKV框架。该框架通过识别注意力头的异质性，将头分为静态和动态两类，并采用自顶向下的预算分配方案。针对静态头使用文本先验剪枝，动态头使用块状检索，在Qwen2.5-VL-7B上实现了高达7.9倍的显存压缩和1.52倍的推理加速，且几乎无性能损失。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05887v1/fig-001.webp\", \"caption\": \"Figure 8: Case study of HYBRIDKV on Video-ChatGPT using Qwen2.5-VL-7B. HYBRIDKV enables the decode tokens to adaptively focus on critical visual regions, highlighted by red font and boxes, while the model with full cache attend to the noisy tokens, leading to degraded accuracy. This demonstrates that retaining a small yet salient subset of tokens during decoding can not only preserve but even enhance the model’s capabilities.\", \"page\": 15, \"index\": 1, \"width\": 992, \"height\": 632}, {\"url\": \"assets/figures/arxiv/2604.05887v1/fig-002.webp\", \"caption\": \"Figure 7: Case study of HYBRIDKV on CL-CH using Qwen2.5-VL-7B. Other methods generate the wrong answer after KV cache compression, while HYBRIDKV still retains the correct answer.\", \"page\": 15, \"index\": 2, \"width\": 1009, \"height\": 560}, {\"url\": \"assets/figures/arxiv/2604.05887v1/fig-003.webp\", \"caption\": \"Table 1: Performance of five KV cache compression strategies on three MLLMs on image tasks. ROUGE-L (Lin, 2004) evaluates generated–reference consistency for CL-CH and STD, while exact match accuracy is used for other QA tasks. Higher values indicate better performance, and the best measures are highlighted in bold by default.\", \"page\": 7, \"index\": 3, \"width\": 800, \"height\": 340}, {\"url\": \"assets/figures/arxiv/2604.05887v1/fig-004.webp\", \"caption\": \"Table 2: Performance of five KV cache compression strategies on three MLLMs on video tasks. Evaluation metrics (e.g., BLEU-4 and Meteor) are explained in Section C; generally, higher measures indicate better performance.\", \"page\": 7, \"index\": 4, \"width\": 804, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2604.05887v1/fig-005.webp\", \"caption\": \"Figure 3: Cumulative attention distribution during prefill for selected heads in Qwen2.5-VL-7B. Each curve shows the fraction of attention captured by the top x% tokens across video tasks (VATEX, NextQA, Video-ChatGPT) and image tasks (STD, SlideVQA, TextCaps).\", \"page\": 4, \"index\": 5, \"width\": 948, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2604.05887v1/fig-006.webp\", \"caption\": \"Figure 4: Overview of HYBRIDKV. Section 3.1: Heads are classified by text-centric sparsity into static (anchor, few tokens) and dynamic (wave, broad focus). Section 3.2: A two-layer scheme allocates KV cache budgets first by head type and individual heads then. Section 3.3: Hybrid compression integrates static pruning (drop uninformative tokens) with dynamic retrieval (reactivate tokens when needed) to optimize cache usage.\", \"page\": 5, \"index\": 6, \"width\": 929, \"height\": 313}]"
motivation: 视觉输入产生的海量Token导致KV缓存随上下文线性增长，而现有压缩方法忽视了注意力头在处理不同任务时的异质行为。
method: 提出一种混合压缩框架，通过分类静态与动态头、层级化分配预算，并结合文本先验剪枝与块状检索技术进行差异化压缩。
result: 在11个多模态基准测试中，HybridKV将KV缓存显存降低了7.9倍，解码速度提升1.52倍，且保持了与全量缓存相当的性能。
conclusion: HybridKV证明了结合注意力头异质性与混合压缩策略能有效解决MLLM推理的显存瓶颈，实现高效且无损的推理。
---

## 摘要
多模态大语言模型（MLLM）在文本、图像和视频的统一推理方面取得了显著进展，但其推理过程受到键值（KV）缓存快速增长的阻碍。每个视觉输入都会扩展为数千个 token，导致缓存随上下文长度线性增长，并在整个解码过程中驻留在 GPU 显存中，即使在高端 GPU 上也会产生极高的内存开销和延迟。一种常见的解决方案是在固定分配预算下，以不同粒度压缩缓存：token 级统一丢弃次要 token，层级在不同层之间改变保留量，而头级则在不同注意力头之间重新分配预算。然而，这些方法仅止步于分配，忽视了注意力头表现出的异构行为，而这些行为需要不同的压缩策略。我们提出了 HybridKV，这是一个混合 KV 缓存压缩框架，分三个阶段整合了互补策略：首先利用以文本为中心的注意力机制将注意力头分类为静态或动态类型；然后采用自顶向下的预算分配方案分层分配 KV 预算；最后，通过文本先验剪枝压缩静态头，通过分块检索压缩动态头。在 Qwen2.5-VL-7B 上进行的 11 个多模态基准测试实验表明，HybridKV 可将 KV 缓存内存减少高达 7.9 倍，并实现 1.52 倍的解码加速，且相对于全缓存 MLLM 几乎没有性能下降，甚至在某些情况下表现更优。

## Abstract
Multimodal Large Language Models (MLLMs) have advanced unified reasoning over text, images, and videos, but their inference is hindered by the rapid growth of key-value (KV) caches. Each visual input expands into thousands of tokens, causing caches to scale linearly with context length and remain resident in GPU memory throughout decoding, which leads to prohibitive memory overhead and latency even on high-end GPUs. A common solution is to compress caches under a fixed allocated budget at different granularities: token-level uniformly discards less important tokens, layer-level varies retention across layers, and head-level redistributes budgets across heads. Yet these approaches stop at allocation and overlook the heterogeneous behaviors of attention heads that require distinct compression strategies. We propose HybridKV, a hybrid KV cache compression framework that integrates complementary strategies in three stages: heads are first classified into static or dynamic types using text-centric attention; then a top-down budget allocation scheme hierarchically assigns KV budgets; finally, static heads are compressed by text-prior pruning and dynamic heads by chunk-wise retrieval. Experiments on 11 multimodal benchmarks with Qwen2.5-VL-7B show that HybridKV reduces KV cache memory by up to $7.9\times$ and achieves $1.52\times$ faster decoding, with almost no performance drop or even higher relative to the full-cache MLLM.

---

## 论文详细总结（自动生成）

### 论文总结：HybridKV - 高效多模态大模型推理的混合 KV 缓存压缩

#### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）在处理高分辨率图像或长视频时，视觉输入会转化为数千个 Token，导致 **KV 缓存（Key-Value Cache）** 随上下文长度线性激增。这不仅消耗了海量的 GPU 显存（例如 72B 模型处理 20 张图片需 13GB 缓存），还因频繁的内存访问增加了推理延迟。
现有的压缩方法（如 Token 级、层级或头级分配）大多只关注“分配多少预算”，而忽视了**不同注意力头在推理过程中的行为异质性**。本文的研究动机在于发现：有些头是“静态”的（关注点固定），有些是“动态”的（关注点随解码演变），因此需要针对性地采用不同的压缩策略。

#### 2. 方法论：核心思想与关键技术
HybridKV 提出了一个三阶段的混合压缩框架：
*   **注意力头分类（Text-centric Classification）：** 在预填充（Prefill）阶段，利用“以文本为中心”的注意力稀疏度得分（$S_{l,h}$）来识别头的类型。如果一个头在文本引导下关注点集中，则归为**静态头**；若关注点分散，则归为**动态头**。
*   **自顶向下的预算分配（Top-down Allocation）：** 
    *   **头类型级：** 在静态和动态头之间分配总预算，通过系数 $r$ 平衡准确率与效率。
    *   **个体头级：** 对静态头根据其重要性得分进行比例分配；对动态头进行均匀分配以适配 CUDA 算子优化。
*   **混合压缩策略（Hybrid Compression）：**
    *   **静态头 - 文本先验剪枝：** 永久保留文本 Token、局部窗口 Token 以及最重要的视觉 Token，其余丢弃。
    *   **动态头 - 分块检索：** 将 KV 缓存分块存储在 CPU 内存中，解码时根据当前 Query 动态检索最相关的块加载回 GPU。

#### 3. 实验设计
*   **数据集/场景：** 涵盖了 11 个多模态基准测试，包括图像任务（MileBench 中的 DocVQA, WebQA, MM-QA 等）和视频任务（VATEX, NextQA, Video-ChatGPT）。
*   **模型（Backbones）：** Qwen2.5-VL-3B/7B、LLaVA-OneVision-7B。
*   **对比方法：** SnapKV（Token 级）、LOOK-M（模态感知）、MADA KV（层级分配）、SparseMM（头级剪枝）。
*   **设置：** 主要在 10% 的极低 KV 缓存预算下进行对比。

#### 4. 资源与算力
*   **硬件：** 实验主要在 **NVIDIA L40S GPU** 上运行。
*   **算力细节：** 论文未明确提及总训练时长（因为该方法是 **Plug-and-play**，无需重新训练或微调），但提供了推理效率数据：在 10% 预算下，Qwen2.5-VL-7B 的解码延迟从 58.94 ms/token 降至 38.65 ms/token。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在 3 个主流模型和 11 个数据集上进行了测试，涵盖了多图推理、文档理解、视频描述等多种复杂场景。
*   **消融实验：** 针对“头分类”的有效性、“预算分配策略”的影响以及“超参数敏感性（阈值 $\theta$ 和系数 $r$）”做了详细分析。
*   **评价：** 实验设计较为充分且客观。通过对比全量缓存（Full Cache）和多种最先进的基线方法，证明了该方法在大幅压缩显存的同时，性能损失极小，甚至在某些视频任务上因减少了噪声干扰而超过了全量缓存的表现。

#### 6. 主要结论与发现
*   **异质性规律：** 预填充阶段的文本中心注意力稀疏度可以有效预测解码阶段的注意力模式。
*   **性能表现：** HybridKV 在仅使用 10% 缓存的情况下，平均性能接近全量缓存，显著优于其他压缩方法。
*   **效率提升：** 在 Qwen2.5-VL-7B 上实现了 **7.9 倍** 的 KV 缓存显存缩减和 **1.52 倍** 的解码速度提升。
*   **任务适应性：** 静态/动态头的角色是随任务变化的，HybridKV 的上下文感知设计能自动适应不同输入。

#### 7. 优点（亮点）
*   **策略混合：** 首次将“静态剪枝”与“动态检索”结合，并应用到 MLLM 的头级压缩中。
*   **即插即用：** 无需任何额外的训练或微调，降低了部署成本。
*   **上下文感知：** 分类和预算分配都是基于当前输入动态生成的，比离线预定义的分配更灵活。
*   **显著的效率增益：** 在长视频理解等显存瓶颈严重的场景下表现极佳。

#### 8. 不足与局限
*   **超参数依赖：** 头分类依赖于预设的阈值 $\theta$，虽然论文做了敏感性分析，但在不同规模或架构的模型上可能需要重新调优。
*   **模态局限：** 目前主要针对图像和视频，尚未验证在音频或其他长序列模态（如纯长文本）上的表现。
*   **硬件开销：** 动态检索涉及 CPU 与 GPU 之间的数据传输，虽然通过分块优化了延迟，但在带宽受限的系统上可能存在瓶颈。

（完）
