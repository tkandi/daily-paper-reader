---
title: Mixture of Heterogeneous Grouped Experts for Language Modeling
authors: "Zhicheng Ma, Xiang Liu, Zhaoxiang Liu, Ning Wang, Yi Shen, Kai Wang, Shuming Shi, Shiguo Lian"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23108v1"
tags: ["query:mlsys"]
score: 8.5
evidence: 解决异构MoE架构中GPU利用率不均衡的系统级挑战
tldr: "针对标准MoE专家尺寸统一导致的资源分配僵化问题，本文提出MoHGE架构。通过两级路由机制和异构专家分组，结合组级辅助损失与组解耦分配策略，优化了推理效率并解决了GPU负载不均。实验显示MoHGE在保持性能的同时减少了约20%的参数量，为工业级高效部署提供了新方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23108v1/fig-001.webp\", \"caption\": \"Table 8: The evaluation results for varying coefficients of the auxiliary loss function. The highestperforming score for each benchmark is highlighted in bold, while the second-highest score is underlined.\", \"page\": 10, \"index\": 1, \"width\": 929, \"height\": 355}, {\"url\": \"assets/figures/arxiv/2604.23108v1/fig-002.webp\", \"caption\": \"Table 7: Architecture configurations of the evaluated models at both 1B, 3B and 14B parameter scales.\", \"page\": 9, \"index\": 2, \"width\": 460, \"height\": 411}]"
motivation: 传统MoE专家尺寸单一，无法根据Token复杂度灵活分配计算资源，且现有异构方案存在GPU利用率低和参数效率差的问题。
method: 提出混合异构分组专家架构（MoHGE），采用两级路由机制、组级辅助损失及全尺寸组解耦分配策略。
result: "在保持与标准MoE相当性能的前提下，MoHGE成功减少了约20%的总参数量，并实现了均衡的GPU负载分布。"
conclusion: MoHGE为资源受限场景下的MoE设计提供了可扩展且实用的范式，有效优化了工业部署中的推理成本。
---

## Abstract
Large Language Models (LLMs) based on Mixture-of-Experts (MoE) are pivotal in industrial applications for their ability to scale performance efficiently. However, standard MoEs enforce uniform expert sizes,creating a rigidity that fails to align computational costs with varying token-level complexity. While heterogeneous expert architectures attempt to address this by diversifying expert sizes, they often suffer from significant system-level challenges, specifically unbalanced GPU utilization and inefficient parameter utilization, which hinder practical deployment. To bridge the gap between theoretical heterogeneity and robust industrial application, we propose Mixture of Heterogeneous Grouped Experts (MoHGE) which introduces a two-level routing mechanism to enable flexible, resource-aware expert combinations. To optimize inference efficiency, we propose a Group-Wise Auxiliary Loss, which dynamically steers tokens to the most parameter-efficient expert groups based on task difficulty. To address the critical deployment challenge of GPU load balancing, we introduce an All-size Group-decoupling Allocation strategy coupled with an Intra-Group Experts Auxiliary Loss. These mechanisms collectively ensure uniform computation distribution across GPUs. Extensive evaluations demonstrate that MoHGE matches the performance of MoE architectures while reducing the total parameters by approximately 20% and maintaining balanced GPU utilization. Our work establishes a scalable paradigm for resource-efficient MoE design, offering a practical solution for optimizing inference costs in real-world scenarios.

---

## 论文详细总结（自动生成）

这篇论文由中国联通 AI 创新中心的研究团队提出，旨在解决混合专家模型（MoE）在工业化部署中遇到的计算资源分配僵化与硬件利用率不均的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：标准的 MoE 架构通常采用**同质专家**（所有专家参数量相同），这导致无论 Token 预测难度如何，消耗的计算量都是固定的，缺乏灵活性。
*   **研究动机**：虽然已有研究尝试引入**异构专家**（不同尺寸的专家），但面临两大挑战：
    1.  **参数利用率低**：路由机制往往无法将 Token 准确分配给最合适的专家。
    2.  **GPU 负载不均衡**：由于专家尺寸不一，简单的并行策略会导致某些 GPU 负载过重，而其他 GPU 闲置，严重阻碍大规模训练和推理效率。

### 2. 方法论：MoHGE 架构
论文提出了 **MoHGE（Mixture of Heterogeneous Grouped Experts）**，核心思想是通过分层分组和创新的路由/分配策略实现高效计算。
*   **异构专家分组**：将专家分为多个组（Groups），同一组内专家尺寸相同，不同组之间尺寸按梯度变化（如从窄到宽）。
*   **两级路由机制**：
    1.  **组路由（Group Gating）**：首先从 $N_g$ 个组中选出 Top-$K_g$ 个最相关的组。
    2.  **专家路由（Expert Gating）**：在选定组内进行细粒度选择，最终选出 Top-$K_e$ 个专家。
*   **关键损失函数**：
    1.  **组级辅助损失（Group-Wise Auxiliary Loss）**：对选择大尺寸专家的行为施加轻微惩罚，迫使模型在处理简单 Token 时优先选择小尺寸专家，提高参数效率。
    2.  **组内专家辅助损失（Intra-Group Experts Auxiliary Loss）**：确保同一组内的专家被选中的频率均衡。
*   **全尺寸组解耦分配策略（All-size Group-decoupling Allocation）**：这是解决硬件瓶颈的关键。将来自**每个不同尺寸组**的一个专家分配到同一个 GPU 上。这样，无论路由如何选择组，每个 GPU 承载的总参数量和计算压力趋于一致。

### 3. 实验设计
*   **模型规模**：构建了 1B、3B 和 14B 三种参数规模的变体。
*   **数据集**：在 0.58 万亿（0.58T）Token 的混合语料（DataComp-LM, FineWeb, The Pile）上进行预训练。
*   **Benchmark**：涵盖 MMLU（综合知识）、GSM8K/MATH（数学推理）、SIQA/PIQA（常识推理）、LAMBADA（语言建模）和 TriviaQA（问答）。
*   **对比方法**：
    *   **Dense 模型**：参数量与 MoE 激活参数量相当。
    *   **标准 MoE**：基于 DeepSeekV2 改进的同质专家模型。
    *   **异构 MoE 变体**：对比了 MoDSE 和 HMoE。

### 4. 资源与算力
*   **硬件设施**：使用了 16 个 GPU 节点，每个节点配备 8 张 NVIDIA GPU（共 **128 张 GPU**，具体型号推测为 A100 或 H100 等级，但文中仅标注为 NVIDIA GPUs）。
*   **训练配置**：采用 Megatron-LM 框架，序列长度 4096，训练 2 个 Epoch。

### 5. 实验数量与充分性
*   **实验覆盖面**：论文在三个参数量级（1B, 3B, 14B）上均验证了效果，并进行了详细的消融实验（针对辅助损失系数 $\alpha$）。
*   **分析深度**：提供了 Token 路由分布的统计分析，验证了“简单 Token 走小专家，难 Token 走大专家”的假设。
*   **客观性**：通过对比 GPU 利用率的标准差（Std），客观证明了其分配策略对负载均衡的有效性。实验设计较为严谨，对比基准（Baseline）选取了当前主流的架构。

### 6. 主要结论与发现
*   **性能卓越**：MoHGE 在所有规模下均优于 Dense 和标准 MoE。在 14B 规模下，MoHGE 的表现全面超越了同类模型。
*   **参数效率**：在保持相同甚至更好性能的前提下，MoHGE 比标准 MoE **减少了约 20% 的总参数量**，激活参数量也显著降低。
*   **负载均衡**：成功解决了异构 MoE 的 GPU 利用率不均问题，各 GPU 间的 Token 处理频率差异极小（标准差低至 0.002 左右）。
*   **推理效率**：尽管路由逻辑略微复杂，但由于激活参数减少，整体推理速度通常快于标准 MoE。

### 7. 优点
*   **工业实用性强**：通过“组解耦分配策略”直接解决了异构模型难以落地部署的痛点。
*   **动态资源分配**：实现了计算成本与任务难度的自动对齐，符合认知科学逻辑。
*   **扩展性好**：实验证明该方法在从 1B 到 14B 的跨度上均有效，具备扩展到更大规模的潜力。

### 8. 不足与局限
*   **复杂任务的延迟**：在 GSM8K 等复杂推理任务中，由于模型倾向于调用大专家，推理耗时会略有增加。
*   **路由开销**：两级路由机制虽然精准，但在极小规模模型或极高并发场景下，路由计算本身的开销可能成为微小的瓶颈。
*   **训练数据量**：虽然 0.58T Token 足够验证架构，但相比 Llama 3 等动辄 15T 的数据量，其在极限性能上的表现仍有待观察。

（完）
