---
title: Efficient Mixture-of-Experts LLM Inference with Apple Silicon NPUs
title_zh: 基于 Apple Silicon NPU 的高效混合专家模型大语言模型推理
authors: "Afsara Benazir, Felix Xiaozhu Lin"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18788v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 针对MoE推理的AI基础设施优化与NPU卸载
tldr: 本研究针对MoE架构LLM在Apple Silicon NPU上推理时面临的动态形状、不规则算子及调度开销等挑战，提出了NPUMoE推理引擎。该引擎通过将密集的静态计算卸载至NPU，并结合静态专家容量分层、分组专家执行及负载感知的计算图驻留技术，实现了高效的异构计算。实验证明，NPUMoE在Apple M系列芯片上显著降低了推理延迟，提升了能效并减少了CPU占用，为移动端高效部署大模型提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18788v1/fig-001.webp\", \"caption\": \"Figure 1. Our System: NPUMoE\", \"page\": 1, \"index\": 1, \"width\": 502, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2604.18788v1/fig-002.webp\", \"caption\": \"Figure 18. Cross-dataset validation for offline calibration. Overlap@K for top experts on BoolQ and HellaSwag datasets exhibit consistent overlap across dominant experts.\", \"page\": 11, \"index\": 2, \"width\": 476, \"height\": 323}, {\"url\": \"assets/figures/arxiv/2604.18788v1/fig-003.webp\", \"caption\": \"Table 5. Per-token latency & energy across expert grouping, capacity and execution mode for PhiMoE.\", \"page\": 10, \"index\": 3, \"width\": 502, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2604.18788v1/fig-004.webp\", \"caption\": \"Figure 19. Runtime latency and energy efficiency of PhiMoetiny running on M2 MAX.\", \"page\": 14, \"index\": 4, \"width\": 498, \"height\": 338}, {\"url\": \"assets/figures/arxiv/2604.18788v1/fig-005.webp\", \"caption\": \"Figure 5. Expert load imbalance in prefill and decode for Phi-3.5-MoE and Qwen1.5-MoE-A2.7B. Imbalance ratio is the maximum expert load divided by the average layer load; values above 1 indicate greater skew.\", \"page\": 4, \"index\": 5, \"width\": 502, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2604.18788v1/fig-006.webp\", \"caption\": \"Figure 16.Total CPU cycles consumption across prefill workloads for PhiMoE on M2 Ultra (Lower is better). Reduced usage indicate effective offloading of compute to NPU.\", \"page\": 9, \"index\": 6, \"width\": 502, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2604.18788v1/fig-007.webp\", \"caption\": \"Figure 9. Our system NPUMoE - incorporates key design (1) Static tiers for expert capacity (§4.1) (2) Grouped Expert Execution (§4.2) and (3) Load aware compute graph residency (§4.3). GPU resources remain available for background tasks while CPU and NPU synchronize between runtime operations.\", \"page\": 5, \"index\": 7, \"width\": 948, \"height\": 304}]"
motivation: MoE模型的动态路由和不规则算子与NPU的静态形状约束及调度开销存在冲突，限制了其在Apple Silicon NPU上的推理效率。
method: 提出NPUMoE推理引擎，通过静态专家容量分层、专家分组执行和负载感知的计算图驻留技术，将密集计算卸载至NPU。
result: 在Apple M系列芯片上，NPUMoE将推理延迟降低了1.32至5.55倍，能效提升了1.81至7.37倍，并显著减少了CPU占用。
conclusion: 通过合理分配NPU与CPU/GPU的任务负载，可以有效克服硬件限制，实现MoE模型在移动端芯片上的高效、低功耗推理。
---

## 摘要
Apple 神经网络引擎（ANE）是每颗 Apple Silicon 芯片中内置的专用神经网络处理器（NPU）。混合专家（MoE）大语言模型通过稀疏激活提高了推理效率，但在 NPU 上面临三大挑战：专家路由具有不可预测性，并引入了与 NPU 形状特定约束相冲突的动态张量形状；若干不规则算子（如 top-k、scatter/gather 等）对 NPU 不友好；以及启动大量小型专家内核会产生巨大的调度和同步开销。NPU 旨在分担 CPU 和 GPU 的 AI 计算任务；我们的目标是实现 MoE 推理的此类卸载，特别是在长上下文工作负载消耗大量系统资源的预填充（prefill）阶段。本文提出了 NPUMoE，这是一种运行时推理引擎，通过将密集的静态计算卸载到 NPU，同时为动态操作保留 CPU/GPU 回退路径，从而加速 Apple Silicon 上的 MoE 执行。NPUMoE 利用离线校准来估计专家容量和热度，并以此驱动三项关键技术：(1) 针对动态专家路由的专家容量静态分层；(2) 缓解 NPU 并发限制的分组专家执行；(3) 减少 CPU-NPU 同步开销的负载感知专家计算图驻留。在 Apple M 系列设备上使用三种代表性 MoE 大语言模型和四种长上下文工作负载进行的实验表明，NPUMoE 始终优于基准方案，通过有效的 NPU 卸载，将延迟降低了 1.32 倍至 5.55 倍，能效提升了 1.81 倍至 7.37 倍，并将 CPU 周期占用减少了 1.78 倍至 5.54 倍。

## Abstract
Apple Neural Engine (ANE) is a dedicated neural processing unit (NPU) present in every Apple Silicon chip. Mixture-of-Experts (MoE) LLMs improve inference efficiency via sparse activation but are challenging for NPUs in three ways: expert routing is unpredictable and introduces dynamic tensor shapes that conflict with the shape-specific constraints of NPUs; several irregular operators, e.g., top-k, scatter/gather, etc., are not NPU-friendly; and launching many small expert kernels incurs substantial dispatch and synchronization overhead. NPUs are designed to offload AI compute from CPU and GPU; our goal is to enable such offloading for MoE inference, particularly during prefill, where long-context workloads consume substantial system resources.   This paper presents NPUMoE, a runtime inference engine that accelerates MoE execution on Apple Silicon by offloading dense, static computation to NPU, while preserving a CPU/GPU fallback path for dynamic operations. NPUMoE uses offline calibration to estimate expert capacity and popularity that drives three key techniques: (1) Static tiers for expert capacity to address dynamic expert routing (2) Grouped expert execution to mitigate NPU concurrency limits (3) Load-aware expert compute graph residency to reduce CPU-NPU synchronization overhead. Experiments on Apple M-series devices using three representative MoE LLMs and four long-context workloads show that NPUMoE consistently outperforms baselines, reducing latency by 1.32x-5.55x, improving energy efficiency by 1.81x-7.37x, and reducing CPU-cycle usage by 1.78x-5.54x through effective NPU offloading.

---

## 论文详细总结（自动生成）

这篇论文针对在 Apple Silicon 的神经网络引擎（ANE/NPU）上高效运行混合专家模型（MoE）大语言模型（LLM）的挑战，提出了名为 **NPUMoE** 的推理引擎。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：MoE 架构通过稀疏激活提升了模型容量，但其动态特性与 NPU 的硬件设计存在根本冲突：
    1.  **形状敏感性**：NPU 要求静态、预定义的张量形状，而 MoE 的专家路由会导致动态变化的输入大小。
    2.  **算子不友好**：MoE 依赖的 Top-K、Scatter/Gather 等不规则算子在 NPU 上效率极低或不支持。
    3.  **调度开销**：MoE 包含大量小型专家内核，频繁启动这些内核会产生巨大的 CPU-NPU 同步和调度延迟。
*   **研究意义**：在移动端设备上，CPU 需处理系统任务，GPU 需处理图形渲染。将 MoE 的密集计算（尤其是预填充阶段）卸载到专用的 NPU 上，可以释放 CPU/GPU 资源，同时显著提升能效和响应速度。

### 2. 方法论：核心思想与关键技术
NPUMoE 的核心思想是**异构协同执行**：将密集的静态计算（如 Attention 和专家 FFN）卸载到 NPU，而将动态控制流和不规则算子保留在 CPU。

*   **关键技术一：专家容量静态分层 (Static Tiers for Expert Capacity)**
    *   通过离线校准（Offline Calibration）分析专家负载的偏斜度（Imbalance Ratio）。
    *   将专家分配到不同的静态容量层级（如 Top 专家分配 4 倍基础容量）。
    *   **处理溢出**：当路由到某专家的 Token 超过静态容量时，采用基于激活显著性得分（L2 范数）的 Token 剪枝策略，以极小的精度损失换取静态形状的兼容性。
*   **关键技术二：分组专家执行 (Grouped Expert Execution)**
    *   为了减少 NPU 调度开销，将多个专家（FFN 块）合并为一个单一的静态计算图。
    *   通过填充（Padding）和拼接（Concat）将不平衡的路由转换为密集的静态张量，实现一次调用执行多个专家。
*   **关键技术三：负载感知的计算图驻留 (Load-Aware Residency)**
    *   根据专家“热度”决定其执行位置。
    *   **热专家组**：常驻 NPU，通过批量计算摊销启动成本。
    *   **冷专家组**：由于调用频率极低，直接在 CPU/GPU 上执行，避免不必要的 CPU-NPU 同步开销。

### 3. 实验设计
*   **模型选择**：Phi-3.5-MoE (42.6B)、Phi-tiny-MoE (3.8B)、Qwen3-30B-A3B (30.5B)。
*   **硬件平台**：Apple M2 Max (64GB RAM)、M2 Ultra (192GB RAM)。
*   **数据集/场景**：针对长上下文预填充（Prefill）任务，使用 HellaSwag（常识推理）、BoolQ（问答）、RULER（长文本检索）。
*   **对比基准 (Baselines)**：
    1.  **Core ML (CPU only)**：全 CPU 执行。
    2.  **Core ML (naive)**：官方默认调度，专家 FFN 尝试在 NPU 运行。
    3.  **ANEMLL**：现有的 NPU 加速库（主要针对 Dense 模型优化）。

### 4. 资源与算力
*   **推理算力**：实验在 M2 系列芯片上完成，利用了 ANE 的 FP16 计算能力（M2 Ultra 最高可达 31.6 TOPS）。
*   **训练算力**：论文**未提及**模型训练所需的算力，因为该研究专注于推理阶段的系统优化。
*   **离线校准**：仅需在测试集子集上进行一次轻量级的路由统计分析。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三种不同规模的 MoE 模型，并在两种高端 Apple Silicon 芯片上进行了测试。
*   **维度丰富**：不仅测量了端到端延迟（TTFT），还深入分析了能效（EPT）、CPU 周期占用、Token 丢弃率对精度的影响。
*   **消融实验**：专门设计了消融实验来验证“静态分层”、“分组执行”和“负载感知驻留”三项技术的各自贡献。
*   **客观性**：使用了真实的路由轨迹（Real Routing Traces）进行评估，并进行了跨数据集的校准准确性验证，实验设计较为严谨、公平。

### 6. 主要结论与发现
*   **性能提升**：相比基准方案，预填充延迟降低了 **1.32x–5.55x**。
*   **能效卓越**：能效提升了 **1.81x–7.37x**，显著延长了电池续航能力。
*   **资源释放**：CPU 周期占用减少了 **1.78x–5.54x**，证明了 NPU 卸载的有效性。
*   **精度保持**：尽管存在 Token 丢弃，但在测试任务上的精度损失小于 **1.1%**，证明了静态容量设计的可行性。

### 7. 优点
*   **系统级创新**：首次系统性地解决了 MoE 动态性与移动端 NPU 静态约束之间的矛盾。
*   **实用性强**：针对 Apple 统一内存架构进行了深度优化，代码开源，具有很强的工程落地价值。
*   **多指标平衡**：在追求速度的同时，兼顾了功耗和 CPU 负载，符合移动端部署的真实需求。

### 8. 不足与局限
*   **解码阶段优化有限**：研究重点在预填充（Prefill）阶段，对于自回归解码（Decode）阶段，由于单 Token 处理难以摊销同步开销，提升不如预填充阶段显著。
*   **Token 丢弃风险**：虽然在测试集上精度损失小，但在极端不平衡的输入下，Token 丢弃可能对某些特定任务（如长链逻辑推理）产生不可预知的影响。
*   **硬件绑定**：该方法高度依赖 Apple Core ML 框架和 ANE 硬件特性，迁移到其他 NPU（如高通 Hexagon）可能需要重新适配。

（完）
