---
title: Optimizing High-Throughput Distributed Data Pipelines for Reproducible Deep Learning at Scale
title_zh: 针对大规模可复现深度学习的高吞吐量分布式数据流水线优化
authors: "Kashish Mittal, Di Yu, Roozbeh Ketabi, Arushi Arora, Brendon Lapp, Peng Zhang"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21275v1"
tags: ["query:mlsys"]
score: 10.0
evidence: 优化分布式GPU训练流水线和数据加载瓶颈
tldr: "本研究针对大规模深度学习训练中数据加载导致的GPU利用率低和不可复现问题，优化了基于Petastorm和Parquet的分布式数据流水线。通过引入下推式转换、本地磁盘缓存以及改进的多进程队列管理和随机数生成机制，成功解决了网络I/O和CPU瓶颈。该方案将GPU利用率从15%提升至60%以上，训练速度提高6倍，并实现了严格的确定性数据加载。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21275v1/fig-001.webp\", \"caption\": \"Fig. 4. Optimized architecture featuring dedicated queues and round-robin scheduling, guaranteeing strict data determinism without sacrificing parallel throughput.\", \"page\": 3, \"index\": 1, \"width\": 529, \"height\": 285}, {\"url\": \"assets/figures/arxiv/2604.21275v1/fig-002.webp\", \"caption\": \"Fig. 3. Baseline Petastorm architecture utilizing a shared ventilator and results queue, leading to non-deterministic race conditions based on thread execution speed.\", \"page\": 3, \"index\": 2, \"width\": 529, \"height\": 358}, {\"url\": \"assets/figures/arxiv/2604.21275v1/fig-003.webp\", \"caption\": \"Fig. 1. Baseline distributed training data flow. The main thread acts as a bottleneck by applying CPU-intensive transformations just-in-time.\", \"page\": 2, \"index\": 3, \"width\": 529, \"height\": 331}, {\"url\": \"assets/figures/arxiv/2604.21275v1/fig-004.webp\", \"caption\": \"Fig. 2. Optimized architecture leveraging push-down transformations to the worker pool and local disk caching via FanoutCache.\", \"page\": 2, \"index\": 4, \"width\": 529, \"height\": 331}, {\"url\": \"assets/figures/arxiv/2604.21275v1/fig-005.webp\", \"caption\": \"Fig. 7. Training loss versus step under the baseline architecture, demonstrating high variance between runs and a MAP shift of ∼0.5%.\", \"page\": 4, \"index\": 5, \"width\": 529, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2604.21275v1/fig-006.webp\", \"caption\": \"Fig. 8. Training loss versus step under the optimized deterministic architecture, showing highly reproducible runs and a reduced MAP shift of ∼0.13%.\", \"page\": 5, \"index\": 6, \"width\": 529, \"height\": 266}]"
motivation: 大规模深度学习训练中，网络I/O和CPU数据转换瓶颈导致GPU利用率极低，且分布式环境下的数据加载难以保证可复现性。
method: 提出一种优化架构，结合下推式工作节点转换、Fanout-Cache本地缓存以及改进的循环调度队列和随机数种子管理。
result: "训练时间从22小时缩短至3小时（加速6倍），GPU利用率提升至60%以上，并消除了多进程竞争导致的随机性。"
conclusion: 该优化方案显著提升了分布式数据流水线的吞吐量和稳定性，为大规模、可复现的深度学习训练提供了高效的技术支撑。
---

## 摘要
在跨越数十 TB 的数据集上训练大规模深度学习模型，对硬件利用率和训练可复现性提出了严峻挑战。本文识别并解决了在使用 Petastorm 数据加载器和 Apache Parquet 数据集的分布式 GPU 训练流水线中存在的严重数据加载瓶颈。通过系统性的性能分析，我们证明了网络 I/O 和受 CPU 限制的数据转换（例如 PyArrow 到 NumPy）将 GPU 利用率限制在低至 10-15%。为解决这一问题，我们提出了一种优化架构，其特点是下推工作节点级转换，并结合通过 Fanout-Cache 实现的本地磁盘缓存，从而最大限度地减少了不同训练周期中冗余的 I/O 和 CPU 开销。此外，我们通过实现专用的轮询分发器和结果队列，以及现代化的随机数生成器（RNG）处理，消除了多工作节点共享队列中的竞态条件，实现了严格的确定性数据加载。我们的优化实现了 6 倍的加速，将端到端训练时间从 22 小时缩短至 3 小时，将 GPU 利用率提高到 60% 以上，并显著降低了多次运行间的差异，从而实现了稳健、高吞吐量且可复现的大规模模型训练。

## Abstract
Training massive-scale deep learning models on datasets spanning tens of terabytes presents critical challenges in hardware utilization and training reproducibility. In this paper, we identify and resolve profound data-loading bottlenecks within distributed GPU training pipelines using the Petastorm data loader and Apache Parquet datasets. Through systematic profiling, we demonstrate that network I/O and CPU-bound data transformations (e.g., PyArrow to NumPy) constrain GPU utilization to as low as 10-15%. To address this, we propose an optimized architecture that features push-down worker-level transformations coupled with local-disk caching via Fanout-Cache, minimizing redundant I/O and CPU overhead across training epochs. Furthermore, we eliminate race conditions in multi-worker shared queues by implementing dedicated round-robin ventilator and result queues, alongside modernized RNG handling, achieving strict deterministic data loading. Our optimizations yield a 6x speedup, reducing end-to-end training time from 22 hours to 3 hours, increasing GPU utilization to over 60%, and drastically reducing run-to-run variance, enabling robust, high-throughput, and reproducible large-scale model training.

---

## 论文详细总结（自动生成）

以下是对论文《Optimizing High-Throughput Distributed Data Pipelines for Reproducible Deep Learning at Scale》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：工业级深度学习模型通常依赖于数十 TB、数百亿行规模的结构化（表格）数据集。随着 GPU 算力的飞速提升，数据加载流水线逐渐成为瓶颈。
*   **核心问题**：
    1.  **吞吐量瓶颈**：在基准测试中，高性能 GPU 的利用率仅为 10-15%，主要受限于网络 I/O（从 HDFS 读取）和 CPU 密集型的数据转换（PyArrow 到 NumPy 的转换）。
    2.  **不可复现性**：在多工作节点（Worker）的分布式环境下，由于共享队列导致的竞态条件，即使固定随机种子，不同批次的顺序也会因线程调度和网络波动而改变，导致模型评估指标（如 MAP）出现显著波动。

### 2. 论文提出的方法论
论文针对上述瓶颈提出了三项核心优化技术：
*   **下推式数据转换（Push-down Transformations）**：
    *   将原本在主线程中同步进行的 CPU 密集型转换逻辑（PyArrow 表转 NumPy 数组）下推到 Petastorm 的工作线程池中。
    *   工作节点在读取数据后立即完成转换，使主线程仅负责将预处理好的数据推送到 GPU，实现了 CPU 负载的并行化。
*   **配额管理本地磁盘缓存（Quota-Managed Local Disk Caching）**：
    *   利用 `FanoutCache` 在本地磁盘缓存已转换的数据。
    *   **算法流程**：系统优先检查本地缓存；若未命中，则从 HDFS 读取并转换，并在磁盘配额允许的情况下写入缓存。这消除了跨 Epoch 训练时重复的网络 I/O 和 CPU 转换开销。
*   **确定性工作节点调度（Deterministic Worker Scheduling）**：
    *   **架构重构**：废弃了所有工作节点共享的单一分发队列和结果队列，为每个工作线程建立专用的“分发-结果”队列对。
    *   **轮询策略**：采用严格的轮询（Round-robin）方式分配任务并收集结果，确保数据批次的顺序与工作节点的执行速度无关。
    *   **RNG 现代化**：使用 `np.random.default_rng` 替代旧版 API，确保分布式环境下的随机操作一致。

### 3. 实验设计
*   **应用场景**：大规模深度学习推荐模型。
*   **数据集**：存储在 HDFS 上的 Apache Parquet 格式表格数据，规模达数十 TB，包含数百亿行和数百个特征。
*   **基准测试（Benchmark）**：以 Uber 开源的 Petastorm 数据加载器作为基准。
*   **对比维度**：
    *   **吞吐量**：对比优化前后的 GPU 利用率和端到端训练时长。
    *   **复现性**：对比多次运行间的训练损失曲线和平均精度均值（MAP）的偏移量。

### 4. 资源与算力
*   **硬件环境**：多 GPU 计算实例（高性能 GPU，具体型号未指明，但强调了其高算力）。
*   **软件栈**：Ray 集群（资源管理）、Horovod（分布式梯度同步）、Petastorm（数据加载）。
*   **训练时长**：
    *   **基准方案**：每个 Epoch 耗时约 **22 小时**。
    *   **优化方案**：每个 Epoch 耗时约 **3 小时**。

### 5. 实验数量与充分性
*   **实验规模**：实验基于真实的工业级生产模型和 TB 级数据集，具有极高的实践参考价值。
*   **消融与分析**：论文通过系统性分析（如运行合成基准测试以排除模型复杂度不足、使用 RAM 缓存隔离网络 I/O 影响等）定位了瓶颈。
*   **充分性评价**：实验充分展示了从 10% 到 60% 以上 GPU 利用率的飞跃，并提供了多次运行的损失曲线对比图。虽然未在多种不同类型的模型（如 CV/NLP）上做广泛测试，但对于其针对的“大规模表格数据”场景，实验结果非常客观且具有说服力。

### 6. 主要结论与发现
*   **性能飞跃**：优化后的流水线实现了 **6 倍** 的端到端加速，GPU 利用率从 12% 提升至 **60% 以上**。
*   **成本降低**：由于训练时间大幅缩短，计算成本降低了近 **80%**。
*   **复现性增强**：通过消除竞态条件，多次运行间的 MAP 偏移从 0.5% 降低至 **0.13%**，使模型架构的微小改进能够被准确评估。

### 7. 优点
*   **全栈式优化**：不仅关注 I/O，还深入分析了 CPU 转换瓶颈和多线程调度逻辑。
*   **实用性极强**：针对工业界最常见的 Parquet + HDFS 架构，解决了实际生产中的痛点。
*   **确定性保障**：在追求高吞吐量的同时，没有牺牲分布式训练的严谨性，解决了长期困扰大规模训练的随机性问题。

### 8. 不足与局限
*   **内存权衡**：下推转换（将 Arrow 转为 NumPy）会增加内存占用，论文提到这是为了吞吐量而做的权衡，但在内存受限的机器上可能存在风险。
*   **适用范围**：主要针对结构化表格数据和 Petastorm 框架，对于非结构化数据（如图像、视频）或其他加载框架（如 WebDataset）的适用性未做讨论。
*   **硬件细节缺失**：未明确说明具体的 GPU 型号（如 A100 或 H100），这使得跨硬件平台的性能对比略显模糊。

（完）
