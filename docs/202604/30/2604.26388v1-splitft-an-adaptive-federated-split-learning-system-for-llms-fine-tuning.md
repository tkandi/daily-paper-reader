---
title: "SplitFT: An Adaptive Federated Split Learning System For LLMs Fine-Tuning"
title_zh: SplitFT：一种用于大语言模型微调的自适应联邦拆分学习系统
authors: "Yimeng Shan, Zhaorui Zhang, Sheng Di, Yu Liu, Xiaoyi Lu, Benben Liu"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26388v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 用于LLM微调的联邦拆分学习系统
tldr: 本研究针对大语言模型在联邦拆分学习微调中面临的设备异构性和高通信开销挑战，提出了SplitFT系统。该系统允许客户端根据计算资源自适应设置拆分层，并通过降低拆分层LoRA秩来减少通信量，同时引入基于长度的Dirichlet数据划分方法。实验证明，SplitFT在微调效率和模型性能上均优于现有技术，为分布式LLM训练提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26388v1/fig-001.webp\", \"caption\": \"Fig. 2: The Impact of LoRA Rank and Cutlayer on Model Performance and Quality.\", \"page\": 7, \"index\": 1, \"width\": 1064, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2604.26388v1/fig-002.webp\", \"caption\": \"Fig. 3: The Performance Comparison for SplitFT and Baselines.\", \"page\": 7, \"index\": 2, \"width\": 1064, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2604.26388v1/fig-003.webp\", \"caption\": \"Fig. 1: The System Overview of Our Proposed SplitFT.\", \"page\": 5, \"index\": 3, \"width\": 968, \"height\": 710}, {\"url\": \"assets/figures/arxiv/2604.26388v1/fig-004.webp\", \"caption\": \"Fig. 4: Generalizability of SplitFT Across Different Models\", \"page\": 8, \"index\": 4, \"width\": 1032, \"height\": 247}]"
motivation: 联邦拆分学习在微调大语言模型时，面临如何根据客户端异构资源自适应设置拆分层以及降低通信开销的严峻挑战。
method: 提出SplitFT系统，通过自适应拆分层选择、降低拆分层LoRA秩以及基于长度的Dirichlet数据划分来优化微调过程。
result: 在多个基准测试中，SplitFT在微调时间效率和模型性能方面均超越了现有的最先进方法。
conclusion: SplitFT是一种能有效解决异构环境下大语言模型联邦微调效率与性能问题的自适应拆分学习系统。
---

## 摘要
联邦拆分学习已被公认为解决传统联邦学习中客户端计算资源受限问题的有效方法，同时能确保跨数据所有者进行分布式模型训练时的数据隐私。然而，当该训练策略应用于大语言模型（LLMs）微调时，面临着一些关键挑战。这些挑战包括在不同客户端之间自适应地设置切分层，以应对显著影响系统性能的数据和设备异构性问题。此外，在微调过程中有效降低通信开销也是另一项挑战。目前尚无工作尝试解决这些问题。为了填补这一空白，我们提出了 SplitFT，一种用于大语言模型微调的自适应联邦拆分学习系统。SplitFT 允许不同客户端根据其计算资源和训练模型性能设置不同的切分层。SplitFT 还建议降低切分层中的 LoRA 秩以减少通信开销。除了为所提系统模拟现实应用中的异构数据外，我们还提出了一种基于长度的狄利克雷方法来将训练数据划分到不同客户端。大量实验结果表明，在各种流行基准测试中，我们提出的方法在微调时间效率和模型性能方面均优于现有最先进的方法。

## Abstract
Federated Split Learning has been identified as an efficient approach to address the computational resource constraints of clients in classical federated learning, while guaranteeing data privacy for distributed model training across data owners. However, it faces some critical challenges when such a training strategy meets large language models (LLMs) for fine-tuning. Such challenges include setting the cutlayer adaptively across different clients to address the data and device heterogeneity issues, which affect the system performance significantly. In addition, efficiently reducing the communication overhead during the fine-tuning procedure is also another challenge. No work tries to address these challenges.   To bridge this gap, we propose SplitTF, an adaptive federated split learning system for LLMs fine-tuning. SplitFT enables different clients to set different cut layers according to their computation resources and trained model performance. SplitFT also proposes to reduce the LoRA rank in cutlayer to reduce the communication overhead. In addition to simulating the heterogeneous data in real-world applications for our proposed split federated learning system, we propose a length-based Dirichlet approach to divide the training data into different clients. Extensive experimental results show that our proposed approach outperforms the state-of-the-art approach for fine-tuning time efficiency and model performance based on various popular benchmarks.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SplitFT** 的自适应联邦拆分学习系统，旨在解决大语言模型（LLM）在资源受限的分布式环境下进行微调的效率与性能问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **背景**：LLM 的微调（Fine-tuning）通常需要巨大的计算资源。传统的联邦学习（FL）要求客户端承担全部训练负载，而拆分学习（SL）虽然减轻了客户端负担，但在多客户端并发时效率较低。
*   **核心问题**：
    1.  **设备异构性**：不同客户端的计算能力差异大，固定拆分层会导致“木桶效应”，即高性能设备必须等待低性能设备。
    2.  **数据异构性**：非独立同分布（Non-IID）数据会影响模型收敛速度和最终性能。
    3.  **通信开销**：拆分学习在客户端与服务器之间传输中间层数据（Smashed Data）及其梯度，带来了巨大的通信压力。

### 2. 方法论
SplitFT 的核心思想是通过**自适应层分配**和**参数高效微调（LoRA）优化**来平衡负载与通信。
*   **自适应层分配策略**：
    *   系统根据每个客户端的测试准确率（$acc_i$）与平均准确率（$acc_{avg}$）动态调整其承担的层数。
    *   表现优异（计算资源充足或数据质量高）的客户端会被分配更多的模型层，从而分担服务器压力并提升全局模型质量。
*   **切分层 LoRA 秩缩减（Rank Reduction）**：
    *   在切分层（Cutlayer）使用较小的 LoRA 秩（如 $r=8$），而在其他层使用较大的秩（如 $r=16$）。
    *   **目的**：直接减少切分层处需要交换的梯度数据量，从而降低通信开销。
*   **基于长度的 Dirichlet 数据划分**：
    *   提出一种新的 Non-IID 模拟方法，根据文本长度将数据集分为 $K$ 类，再利用狄利克雷分布（$\alpha$ 参数控制）分配给各客户端，以更真实地模拟现实中的数据异构性。
*   **算法流程**：客户端执行前向传播 -> 发送中间数据 -> 服务器执行前向/反向传播 -> 返回梯度 -> 客户端更新本地 LoRA 参数 -> 通过 FedAvg 聚合客户端侧的 LoRA 权重。

### 3. 实验设计
*   **数据集**：使用流行的 **Wikitext2-v1** 数据集（包含 36,700 条训练样本）。
*   **评估模型（Benchmarks）**：
    1.  GPT2-small (124M)
    2.  OPT-125M
    3.  GPT-Neo 125M
*   **对比方法（Baselines）**：
    *   **Same Split**：所有客户端使用固定的拆分层（默认前 2 层在客户端，后 10 层在服务器）。
    *   **IID vs. Non-IID**：在不同数据分布下对比系统鲁棒性。
    *   **不同 LoRA 秩配置**：对比对称与非对称秩设置对性能的影响。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA GeForce RTX 3090 GPU** 上运行。
*   **软件**：基于 PyTorch 2.3 和 Flower 联邦学习框架实现。
*   **训练细节**：Batch size 为 4，学习率为 $5 \times 10^{-5}$，最大序列长度 512。实验中设置了 5 个客户端。
*   **时长**：文中未明确给出总训练时长，但提供了各配置下的平均每轮时间（Mean Round Time，约 0.03s - 0.07s 级别）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的消融实验和对比实验，包括：
    *   不同切分层位置（2, 4, 6, 8, 10 层）对准确率和通信量的影响。
    *   不同 LoRA 秩（1, 2, 4, 8）的对比。
    *   不同数据异构程度（$\alpha = 0.1$ 到 $100$）的鲁棒性测试。
*   **充分性评价**：实验设计较为全面，涵盖了主流的轻量级 LLM 架构，验证了自适应策略在处理异构性方面的优势。实验对比了准确率、耗时和通信开销，评估维度客观。

### 6. 主要结论
*   **性能提升**：SplitFT 在 Non-IID 环境下的收敛速度和最终困惑度（Perplexity）均优于固定拆分的基准方法。
*   **通信优化**：通过降低切分层的 LoRA 秩，可以在不显著损失模型精度的前提下，有效减少通信数据量。
*   **自适应优势**：动态调整层数能让系统自动适应客户端的资源状况，避免了慢节点的拖累，并缓解了过拟合问题。
*   **通用性**：在 GPT2、OPT 和 GPT-Neo 等多个模型上表现一致，证明了方案的普适性。

### 7. 优点与亮点
*   **双重自适应**：不仅考虑了计算资源的异构（层分配），还考虑了通信效率（LoRA 秩优化）。
*   **创新的数据划分**：提出的基于长度的 Dirichlet 划分方法为 LLM 联邦学习的研究提供了更贴近自然语言特性的实验环境。
*   **隐私保护**：结合了拆分学习的特性，客户端无需接触完整模型，服务器也无法接触原始数据。

### 8. 不足与局限
*   **模型规模较小**：实验主要集中在 125M 左右的小规模模型上，对于百亿级（7B+）参数的真正“大”语言模型，其自适应策略的计算开销和层调整的稳定性尚待验证。
*   **客户端数量有限**：实验仅使用了 5 个客户端，在大规模超分布式场景（如成千上万个移动设备）下的表现未知。
*   **评估指标单一**：主要依赖困惑度和准确率，缺乏针对特定下游任务（如摘要、翻译、逻辑推理）的生成质量评估（如 ROUGE, BLEU 或 GPT-4 评分）。

（完）
