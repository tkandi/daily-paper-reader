---
title: Versioned Late Materialization for Ultra-Long Sequence Training in Recommendation Systems at Scale
title_zh: 大规模推荐系统中超长序列训练的版本化延迟物化技术
authors: "Liang Guo, Ge Song, Litao Deng, Jianhui Sun, Chufeng Hu, Lu Zhang, Zhen Ma, Shouwei Chen, Weiran Liu, Sarang Masti Sreeshylan, Xiaoxuan Meng"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24806v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 超长序列训练的数据基础设施
tldr: 针对推荐系统中超长用户交互序列带来的存储和I/O瓶颈，本文提出了一种版本化延迟物化范式。该方法通过将序列数据存储在规范化的不可变层，并在训练时利用版本化指针实时重构，消除了传统胖行模式下的数据冗余。系统通过解耦预处理和流水线预取技术掩盖了重构延迟，在降低资源消耗的同时支持了超长序列训练，显著提升了HSTU等先进模型的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24806v1/fig-001.webp\", \"caption\": \"Figure 1: Feature snapshotting and pre-materialization architecture for RecSys training data generation.\", \"page\": 2, \"index\": 1, \"width\": 476, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2604.24806v1/fig-002.webp\", \"caption\": \"Figure 4: NE improvement of UIH sequence length scaling on two production recommendation surfaces.\", \"page\": 7, \"index\": 2, \"width\": 488, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2604.24806v1/fig-003.webp\", \"caption\": \"Table 2: Online A/B testing results of sequence length scaling enabled by versioned late materialization on two production recommendation platforms.\", \"page\": 8, \"index\": 3, \"width\": 508, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2604.24806v1/fig-004.webp\", \"caption\": \"Figure 3: Versioned Late Materialization Protocol\", \"page\": 5, \"index\": 4, \"width\": 1026, \"height\": 410}]"
motivation: 传统的胖行数据存储模式在处理超长序列时会导致严重的存储冗余和I/O瓶颈，限制了推荐模型的扩展性。
method: 提出版本化延迟物化技术，通过不可变存储层和版本化指针在训练时即时重构序列，并结合解耦预处理与预取优化性能。
result: 该系统显著降低了数据基础设施的资源占用，并成功支持了超长序列训练，带来了明显的模型质量提升。
conclusion: 版本化延迟物化为大规模推荐系统提供了高效的数据基础架构，是实现超长序列模型（如HSTU）的关键支撑。
---

## 摘要
现代深度学习推荐模型 (DLRMs) 遵循序列长度的缩放法则，推动前沿研究向超长用户交互历史 (UIH) 发展。然而，工业界标准的“胖行” (Fat Row) 范式将这些序列预先物化到每个训练样本中，导致存储和 I/O 瓶颈。由于数据冗余，数据基础设施的使用量超过了 GPU 的训练能力，这种冗余在多租户环境中被进一步放大，因为具有截然不同序列长度要求的模型共享同一个联合数据集。我们提出了一种“版本化延迟物化” (versioned late materialization) 范式，通过将 UIH 一次性存储在规范化的不可变层中，并在训练期间通过轻量级版本化指针即时重构序列，从而消除了这种冗余。该系统通过一种分叉协议确保线上线下 (O2O) 一致性，防止流式和批处理训练中的未来信息泄漏，同时读优化的不可变存储层为异构模型租户提供多维投影下推。具有流水线 I/O 预取和数据亲和性优化的解耦数据预处理掩盖了训练时序列重构的延迟，使训练吞吐量保持受限于 GPU 计算。该系统已部署在生产环境的 DLRM 中，在减少训练数据基础设施资源使用的同时，实现了激进的序列长度缩放，带来了显著的模型质量提升，成为包括 HSTU 和 ULTRA-HSTU 在内的现代推荐模型架构的基础数据基础设施。

## Abstract
Modern Deep Learning Recommendation Models (DLRMs) follow scaling laws with sequence length, driving the frontier toward ultra-long User Interaction History (UIH). However, the industry-standard "Fat Row" paradigm, which pre-materializes these sequences into every training example, creates a storage and I/O wall where data infrastructure usage exceeds GPU training capacity due to data redundancy that is amplified in multi-tenant environments where models with vastly different sequence length requirements share a union dataset. We present a \emph{versioned late materialization} paradigm that eliminates this redundancy by storing UIH once in a normalized, immutable tier and reconstructing sequences just-in-time during training via lightweight versioned pointers. The system ensures Online-to-Offline (O2O) consistency through a bifurcated protocol that prevents future leakage across both streaming and batch training, while a read-optimized immutable storage layer provides multi-dimensional projection pushdown for heterogeneous model tenants. Disaggregated data preprocessing with pipelined I/O prefetching and data-affinity optimizations masks the latency of training-time sequence reconstruction, keeping training throughput compute-bound by GPUs. Deployed on production DLRMs, the system reduces training data infrastructure resource usage while enabling aggressive sequence length scaling that delivers significant model quality gains, serving as the foundational data infrastructure for modern recommendation model architectures, including HSTU and ULTRA-HSTU.

---

## 论文详细总结（自动生成）

### 论文总结：大规模推荐系统中超长序列训练的版本化延迟物化技术

#### 1. 核心问题与研究动机
*   **核心问题**：现代推荐模型（如 HSTU）的性能随用户交互序列（UIH）长度的增加而提升，但工业界标准的**“胖行”（Fat Row）**数据范式成为了瓶颈。
*   **研究动机**：在“胖行”模式下，每个训练样本都会物理性地复制一份完整的用户历史序列。当序列长度达到 $10^5$ 级别时，这种 $K$ 倍的数据冗余导致存储和 I/O 资源消耗呈爆炸式增长，甚至超过了 GPU 训练本身的成本（即“存储与 I/O 墙”），限制了模型向更长序列扩展的可能性。
*   **背景**：多租户环境下，不同模型（如召回、粗排、精排）共享数据集，进一步放大了冗余问题，因为简单模型被迫读取为复杂模型准备的超长序列。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：利用 UIH “仅追加、时间有序、不可变”的特性，将预先物化转变为**延迟物化（Late Materialization）**。即：只存储一份规范化的历史数据，在训练时根据版本指针即时重构。
*   **关键技术细节**：
    *   **版本化协议**：在推理时仅记录轻量级的版本元数据（开始/结束时间戳、长度、校验和）和最近的“易变”序列；在训练时通过时间戳谓词进行范围扫描，确保线上线下（O2O）一致性并防止未来信息泄露。
    *   **混合存储架构**：分为处理实时事件的“易变存储层”和存储长期历史的“不可变存储层”。不可变层采用单层布局（无 LSM 压缩开销）和读优化设计。
    *   **多维投影下推**：支持按序列长度和特征组进行服务器端过滤，使不同需求的模型租户仅读取必要数据。
    *   **解耦数据预处理（DPP）**：将序列重构任务从训练循环中剥离到独立的 CPU 集群，通过流水线 I/O 预取和数据亲和性优化（按用户聚类样本）来掩盖 I/O 延迟，确保 GPU 始终处于满载计算状态。

#### 3. 实验设计
*   **场景与数据集**：Meta 生产环境的大规模推荐平台，涉及数万亿参数的模型。
*   **Benchmark**：工业界标准的“胖行”预物化范式。
*   **对比方法**：在不同序列长度（256 到 64K）下，对比了版本化延迟物化与胖行模式在系统效率和模型质量上的表现。
*   **评估指标**：
    *   **系统侧**：写入/读取带宽减少量、数据加载延迟、GPU 饥饿率。
    *   **模型侧**：归一化熵（NE）增益、在线 A/B 测试指标（点击、时长、互动等）。

#### 4. 资源与算力
*   **算力说明**：文中未明确给出具体的 GPU 型号（如 H100/A100）或确切的 GPU 数量，但强调了该系统支撑的是**万亿参数级别**的顺序转导模型（Sequential Transducers）。
*   **资源消耗对比**：指出在超长序列下，传统模式的数据支持服务功耗会超过 GPU 训练功耗，而新系统显著降低了这一比例。

#### 5. 实验数量与充分性
*   **实验规模**：在三个具有代表性的模型租户（长、中、短序列需求）上进行了系统效率评估，并在两个大型生产平台（Platform A & B）上进行了长达数周的在线 A/B 测试。
*   **充分性**：实验涵盖了从 256 到 64K 的广泛序列长度缩放，并进行了消融实验（如数据亲和性优化对带宽的影响）。通过 NE 曲线对比证明了延迟物化在不损失精度的情况下突破了“胖行墙”，实验设计客观且具有极高的工业参考价值。

#### 6. 主要结论与发现
*   **打破瓶颈**：成功突破了约 4K 长度的“胖行墙”，支持序列扩展至 64K 甚至更高。
*   **资源节省**：主数据集写入带宽减少 **46.2%**，读取带宽减少 **47%–70%**。
*   **模型增益**：在 Platform A 上，通过将序列从 4K 扩展到 64K，获得了额外 **1.2% 的 NE 增益**（总增益超过 5%），这在生产规模下是巨大的提升。
*   **一致性**：验证了延迟物化重构的序列与原始快照完全一致，无模型质量退化。

#### 7. 优点
*   **架构优雅**：将数据库领域的 MVCC 和延迟物化思想巧妙应用于推荐系统数据流。
*   **多租户友好**：通过投影下推解决了“一人点餐，全家买单”的冗余读取问题。
*   **运维便捷**：单次全量重生成即可完成特征回溯或合规性删除（如 GDPR 要求的删除权），无需复杂的增量补丁。

#### 8. 不足与局限
*   **系统复杂性**：引入了复杂的解耦预处理集群和混合存储维护，对中小型企业的工程实现门槛较高。
*   **硬件依赖**：高度依赖高性能的 CPU 预处理集群来掩盖 I/O 延迟，若 CPU 资源不足，可能导致 GPU 饥饿。
*   **适用范围**：主要针对“追加式”的序列特征，对于频繁修改历史值的非序列特征，该模式的优势会减弱。

（完）
