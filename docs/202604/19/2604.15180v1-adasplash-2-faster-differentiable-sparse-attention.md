---
title: "AdaSplash-2: Faster Differentiable Sparse Attention"
title_zh: AdaSplash-2：更快的可微稀疏注意力机制
authors: "Nuno Gonçalves, Hugo Pitorro, Vlad Niculae, Edoardo Ponti, Lei Li, Andre Martins, Marcos Treviso"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15180v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 长文本训练效率的稀疏注意力优化
tldr: "针对 Transformer 长序列训练中的计算瓶颈，本文提出 AdaSplash-2，一种高效的可微稀疏注意力机制。它改进了 α-entmax 注意力，通过直方图初始化技术将归一化因子 τ 的计算迭代次数降至 1-2 次，并结合稀疏感知的 GPU 实现。在稀疏度较高（>60%）的长序列场景下，其训练速度可媲美或超越 FlashAttention-2，且在长文本下游任务中表现优于 Softmax 基准。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15180v1/fig-001.webp\", \"caption\": \"Figure 1. Runtime (forward + backward) as a function of input sparsity for causal attention. ADASPLASH-2, implemented in Triton, improves the sparsity-efficiency tradeoff, outperforming a highly-optimized CUDA version of FlashAttention-2 in moderate sparsity regimes and yielding larger gains at high block sparsity.\", \"page\": 1, \"index\": 1, \"width\": 494, \"height\": 595}, {\"url\": \"assets/figures/arxiv/2604.15180v1/fig-002.webp\", \"caption\": \"Figure 4. Runtime efficiency of causal self-attention implementations across context lengths of 4K-128K tokens with varying 64x64 block sparsity. Bar heights are normalized to FlashAttention-2 (Triton), with the opaque part denoting forward and the lighter part denoting backward. Numeric labels report the total forward + backward step time. Lower bars represent faster runtimes.\", \"page\": 6, \"index\": 2, \"width\": 945, \"height\": 289}, {\"url\": \"assets/figures/arxiv/2604.15180v1/fig-003.webp\", \"caption\": \"Table 6. Downstream evaluations on short-context benchmarks Models are trained with 50B tokens from DCLM-Edu and have 4K context length. Entmax (converted) implies the softmax-based model was evaluated with ADASPLASH-2 without further training, while Entmax (CPT) is the converted model subjected to Continuous Pre-Training (CPT).\", \"page\": 21, \"index\": 3, \"width\": 908, \"height\": 440}, {\"url\": \"assets/figures/arxiv/2604.15180v1/fig-004.webp\", \"caption\": \"Table 1. RULER benchmark results for 1B parameter models trained up to 32K context-length. Best average results are in bold.\", \"page\": 7, \"index\": 4, \"width\": 1004, \"height\": 410}, {\"url\": \"assets/figures/arxiv/2604.15180v1/fig-005.webp\", \"caption\": \"Table 2. Results across context lengths for the In-Context Learning tasks in the HELMET benchmark. Best average result is in bold.\", \"page\": 7, \"index\": 5, \"width\": 494, \"height\": 805}, {\"url\": \"assets/figures/arxiv/2604.15180v1/fig-006.webp\", \"caption\": \"Figure 7. Average 64× 64 attention block sparsity ratio for the Entmax (NAPE) model with 32K context length. Panels correspond to evaluated context lengths (4K / 8K / 16K / 32K / 64K / 128K). We report the overall average sparsity across all layers and heads in the title on each plot.\", \"page\": 22, \"index\": 6, \"width\": 994, \"height\": 545}, {\"url\": \"assets/figures/arxiv/2604.15180v1/fig-007.webp\", \"caption\": \"Figure 5. Visualization of α-entmax for different values of α. We also include top-k softmax with k = 2 for completeness. Each panel shows how the probability mass of p0 varies for the input z = [0, z1, z2]. For softmax, p0 is always non-zero, regardless of z1 and z2. As α increases, α-entmax increasingly assigns exactly zero probability to z0. While α-entmax changes smoothly with the scores (yielding piecewise-smooth gradients), top-k softmax changes the “selected set” abruptly and, consequently, these non-differentiable boundaries induce discontinuous gradients that might lead to training instabilities.\", \"page\": 13, \"index\": 7, \"width\": 996, \"height\": 220}, {\"url\": \"assets/figures/arxiv/2604.15180v1/fig-008.webp\", \"caption\": \"Table 3. Short-context benchmark results for 350M and 1B models with 4K context length. Scores are computed with the OLMES framework using the core 9mcqa suite. Best perplexity and average results are in bold.\", \"page\": 8, \"index\": 8, \"width\": 933, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2604.15180v1/fig-009.webp\", \"caption\": \"Table 4. Histogram capacity analysis for different integer types and bin configurations with Bc = 64. Capacity indicates maximum keys per query tile; resolution h = 1/B is the bin width affecting threshold accuracy (Proposition 1).\", \"page\": 19, \"index\": 9, \"width\": 625, \"height\": 222}, {\"url\": \"assets/figures/arxiv/2604.15180v1/fig-010.webp\", \"caption\": \"Table 5. Downstream evaluations on short-context benchmarks. Models are trained with 50B tokens from DCLM-Edu and have 4K context length. Those marked with (32K) undergo context-length extension with 10B tokens of Prolong data. Best results are in bold.\", \"page\": 20, \"index\": 10, \"width\": 958, \"height\": 377}, {\"url\": \"assets/figures/arxiv/2604.15180v1/fig-011.webp\", \"caption\": \"Figure 2. Diagram of the ADASPLASH-2 histogram kernel. For each query block Qi and key block Kj , we compute a score block Zij , update a per-row histogram H in SRAM, and use it to estimate an initial threshold τh for each query.\", \"page\": 5, \"index\": 11, \"width\": 442, \"height\": 382}, {\"url\": \"assets/figures/arxiv/2604.15180v1/fig-012.webp\", \"caption\": \"Figure 3. Comparison of mean absolute error of previous rootfinding methods and our Hybrid approach with histogram initialization, measured against the exact solution for α = 1.5.\", \"page\": 5, \"index\": 12, \"width\": 494, \"height\": 883}]"
motivation: 旨在解决 α-entmax 稀疏注意力因计算归一化因子 τ 带来的巨大计算开销，使其在长序列训练中更具实用性。
method: 利用 SRAM 实时构建注意力分数直方图以实现精准初始化，并结合稀疏感知的 GPU 算子优化前向与反向传播。
result: "在稀疏度超过 60% 时，训练速度达到或超过 FlashAttention-2，且在长上下文任务中表现优于传统的 Softmax 模型。"
conclusion: AdaSplash-2 证明了可微稀疏注意力可以在保持高性能的同时实现极高的计算效率，是长文本建模的有力工具。
---

## 摘要
稀疏注意力机制被提出作为缓解 Transformer 二次计算开销的一种方法，而这种开销是长上下文训练中的核心瓶颈。一个极具前景的研究方向是 $α$-entmax 注意力，它是 softmax 的一种可微稀疏替代方案，能够实现输入相关的稀疏性，但由于计算归一化因子 $τ$ 所需的计算开销，其性能一直落后于 softmax。在本文中，我们介绍了 AdaSplash-2，它通过一种新颖的基于直方图的初始化方法解决了这一局限性，将计算 $τ$ 所需的迭代次数通常减少到 1-2 次。其核心思想是实时计算注意力分数的粗略直方图并将其存储在片上 SRAM 中，从而产生更准确的初始化，实现快速的前向和后向计算。结合一种能够以低开销跳过零块的稀疏感知 GPU 实现，当块稀疏度处于中高水平（例如 >60%）时，AdaSplash-2 的单步训练时间与 FlashAttention-2 相当甚至更优，而这种情况在长上下文长度下经常发生。在下游任务中，使用我们高效的 $α$-entmax 注意力训练的模型在短上下文长度下与 softmax 基准相当，并在长上下文设置中取得了显著提升。

## Abstract
Sparse attention has been proposed as a way to alleviate the quadratic cost of transformers, a central bottleneck in long-context training. A promising line of work is $α$-entmax attention, a differentiable sparse alternative to softmax that enables input-dependent sparsity yet has lagged behind softmax due to the computational overhead necessary to compute the normalizer $τ$. In this paper, we introduce AdaSplash-2, which addresses this limitation through a novel histogram-based initialization that reduces the number of iterations needed to compute $τ$ to typically 1--2. The key idea is to compute a coarse histogram of attention scores on the fly and store it in on-chip SRAM, yielding a more accurate initialization that enables fast forward and backward computation. Combined with a sparsity-aware GPU implementation that skips zero blocks with low overhead, AdaSplash-2 matches or improves per-step training time relative to FlashAttention-2 when block sparsity is moderate-to-high (e.g., $>$60\%), which often occurs at long-context lengths. On downstream tasks, models trained with our efficient $α$-entmax attention match softmax baselines at short-context lengths and achieve substantial gains in long-context settings.

---

## 论文详细总结（自动生成）

### AdaSplash-2：更快的可微稀疏注意力机制论文总结

#### 1. 核心问题与研究动机
*   **研究背景**：Transformer 的自注意力机制具有序列长度的二次方复杂度，是长文本训练的主要瓶颈。
*   **核心问题**：虽然 $\alpha$-entmax 是一种能够产生输入相关稀疏性的可微替代方案，且在长文本泛化上优于 Softmax，但其计算归一化因子 $\tau$ 需要复杂的迭代根寻找算法。
*   **动机**：现有的稀疏注意力实现（如 AdaSplash）在前向传播中仍需多次扫描 Key 矩阵，导致在中低稀疏度场景下效率不如高度优化的 FlashAttention-2。本文旨在通过硬件感知的设计，消除 $\alpha$-entmax 的计算冗余。

#### 2. 方法论
AdaSplash-2 的核心是通过硬件加速手段减少归一化因子 $\tau$ 的计算开销：
*   **片上直方图初始化（Histogram-based Initialization）**：
    *   在流式处理 Key 块时，利用片上 SRAM 实时构建注意力分数的粗略直方图。
    *   通过位包装（Bit-packing）技术，将多个 Bin 的计数压缩进单个 64 位整数，避免原子操作。
    *   证明了直方图能提供 $\tau$ 的严格下界，从而将后续根寻找算法的迭代次数减少到通常仅需 1-2 次。
*   **受保护的混合求解器（Safeguarded Hybrid Solver）**：
    *   根据 $\alpha$ 的取值（如 1.5 或 2.0）和函数的平滑性，动态选择 Halley、Newton 或正割法进行精细化求解，并辅以二分法保证收敛安全性。
*   **高效稀疏跳过机制**：
    *   在前向传播中生成轻量级的位包装块掩码（Bit-packed Block Mask）。
    *   利用 GPU 原生指令（如 `fns` 寻找下一个置位、`popc` 计数）在反向传播中高效跳过全零块，显著降低开销。
*   **Triton 实现**：所有算子均使用 Triton 编写，实现了 IO 感知的算子融合。

#### 3. 实验设计
*   **数据集与场景**：
    *   **预训练**：使用 DCLM-Edu 数据集（50B tokens）从头训练 350M 和 1B 参数模型。
    *   **长文本扩展**：使用 ProLong 方法将上下文扩展至 32K。
*   **基准测试（Benchmarks）**：
    *   **长文本**：RULER（综合长文本能力）、HELMET（长文本上下文学习 ICL）。
    *   **短文本**：OLMES 框架下的 9 个多项选择任务（如 ARC, PIQA, WG 等）、LAMBADA（困惑度）。
*   **对比方法**：
    *   **算子级**：FlashAttention-2 (CUDA 和 Triton 版本)、原始 AdaSplash。
    *   **模型级**：Softmax + RoPE、Softmax + NAPE（一种新型位置编码）。

#### 4. 资源与算力
*   **硬件**：单节点 4 张 NVIDIA H100 GPU 用于模型训练；单张 NVIDIA A6000 GPU 用于算子效率基准测试。
*   **训练量**：模型在 50B tokens 上进行预训练，随后在 10B tokens 的长文本数据上进行衰减/扩展训练。
*   **实现细节**：基于 `torchtitan` 库进行分布式训练。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从算子级微观基准（不同稀疏度下的耗时）到模型级宏观评估（短/长文本下游任务）。
*   **充分性**：
    *   进行了不同 $\alpha$ 值的消融研究。
    *   对比了不同位置编码（RoPE vs NAPE）的影响。
    *   包含了一个“从密集到稀疏”的模型转换实验（CPT），验证了将预训练好的 Softmax 模型转换为 Entmax 模型的可行性。
*   **客观性**：实验在相同参数规模和训练数据下进行，对比基准（FlashAttention-2）是目前工业界的事实标准，具有较高的说服力。

#### 6. 主要结论与发现
*   **效率提升**：当块稀疏度超过 60% 时，AdaSplash-2 的训练速度超过了高度优化的 FlashAttention-2 CUDA 版本。在极高稀疏度下，速度可达 FA2 的 2 倍以上。
*   **长文本优势**：在 RULER 和 HELMET 榜单上，$\alpha$-entmax 模型在长上下文任务中显著优于 Softmax 基准，尤其是在变量跟踪和信息聚合任务中。
*   **短文本保持**：稀疏注意力模型在短文本任务上的表现与密集模型持平，甚至在 LAMBADA 困惑度上表现更好。
*   **收敛性**：直方图初始化极大地加速了 $\tau$ 的收敛，使得稀疏注意力的前向开销接近 Softmax。

#### 7. 优点
*   **硬件感知优化**：充分利用了 SRAM 存储和位运算指令，解决了 $\alpha$-entmax 长期以来的计算效率痛点。
*   **理论与实践结合**：不仅提供了直方图误差的理论证明，还在大规模 LLM 训练中验证了实际效果。
*   **训练友好**：提供了完整的反向传播优化，使其成为一种“原生可训练”的稀疏方案，而非仅用于推理。

#### 8. 不足与局限
*   **前向传播开销**：在低稀疏度（<40%）场景下，由于需要构建直方图和迭代求解，前向传播速度仍略慢于 FlashAttention-2。
*   **推理优化**：论文重点在于训练效率，虽然提到推理可以使用简化版本，但尚未提供针对自回归解码（KV Cache）高度优化的推理内核。
*   **硬件依赖**：目前的优化高度依赖 NVIDIA GPU 的特定架构特性（如 SRAM 大小、特定 PTX 指令），在其他硬件平台上的移植性有待验证。

（完）
