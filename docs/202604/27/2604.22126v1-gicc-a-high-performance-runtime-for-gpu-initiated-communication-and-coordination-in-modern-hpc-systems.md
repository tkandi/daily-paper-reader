---
title: "GICC: A High-Performance Runtime for GPU-Initiated Communication and Coordination in Modern HPC Systems"
title_zh: GICC：现代高性能计算系统中 GPU 发起通信与协调的高性能运行时
authors: "Baodi Shan, Mauricio Araya-Polo, Barbara Chapman"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22126v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 高性能HPC系统GPU发起通信运行时
tldr: 针对现代HPC系统中分布式GPU应用跨节点协调开销大、重叠不足的问题，本文提出GICC框架。它允许GPU内核直接触发NIC操作而无需主机干预，并引入异步资源回收机制。在Slingshot和InfiniBand系统上的实验证明，GICC显著降低了通信延迟并提升了弱扩展效率，为高性能GPU驱动协调提供了高效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.22126v1/fig-001.webp\", \"caption\": \"Figure 5: GICC put example (pseudocode) on OFI/CXI: the host pre-stages two put operations into the deferred work queue (DWQ) with thresholds 1 and 2 on the same trigger counter; a single GPU counter update to 2 releases both simultaneously. gicc_wait_until polls GPU-visible flags using comparison operators (e.g., GICC_CMP_GE for ≥).\", \"page\": 6, \"index\": 1, \"width\": 540, \"height\": 483}, {\"url\": \"assets/figures/arxiv/2604.22126v1/fig-002.webp\", \"caption\": \"Figure 10: Speedup of GICC over MPI for distributed matrix multiplication across four matrix sizes (512–4096). Speedup > 1 indicates GICC outperforms MPI.\", \"page\": 10, \"index\": 2, \"width\": 506, \"height\": 249}, {\"url\": \"assets/figures/arxiv/2604.22126v1/fig-003.webp\", \"caption\": \"Figure 11: Performance comparison of GICC and MPI implementations for Minimod on Tioga (AMD MI250X + HPE Slingshot): (a) parallel efficiency and (b) communication time.\", \"page\": 12, \"index\": 3, \"width\": 846, \"height\": 274}, {\"url\": \"assets/figures/arxiv/2604.22126v1/fig-004.webp\", \"caption\": \"Figure 4: Boundary conditions for GPU-driven coordination on Slingshot (CXI) with libfabric manual progress. The host first arms deferred NIC work (DWQ) and programs counter thresholds (1). A GPU kernel can then trigger execution via a doorbell/counter update (2), after which the NIC executes the deferred operations and writes completion signals into GPUvisible memory (3), enabling the GPU to poll flags and proceed within kernel control flow. However, GPU-visible completion is not equivalent to libfabric progress or resource retirement: under FI_PROGRESS_MANUAL, the host must drive progress by polling completion objects (4) and then retire/reset and re-arm resources for the next epoch (5).\", \"page\": 5, \"index\": 4, \"width\": 963, \"height\": 448}]"
motivation: 现有的GPU通信框架在OFI互连上依赖主机驱动且缺乏资源回收机制，而在InfiniBand上则存在过高的同步和锁定开销。
method: GICC通过解耦协调语义与数据传输，并利用轻量级主机线程异步回收NIC资源，使GPU内核能直接触发NIC级操作。
result: 在Slingshot上将协调延迟降低高达229倍，在InfiniBand上比NVSHMEM降低1.95倍延迟，并显著提升了并行效率。
conclusion: GICC通过消除主机干预和优化资源管理，为现代HPC系统提供了一种高性能、可扩展的GPU启动通信与协调方案。
---

## 摘要
分布式 GPU 应用日益依赖内核级跨节点协调，以减少启动开销并提高计算与通信的重叠度，但目前尚缺乏此类支持。在基于 OFI 的互连网络（如 HPE Slingshot，它为 2025 年 11 月 Top500 前十名中的六个系统提供动力，包括前三名）上，GPU 内核无法自主驱动分布式协调：现有的运行时依赖主机驱动的进度，且缺乏在重复的 GPU 触发操作中回收预置网卡（NIC）工作的有界机制。在 InfiniBand 上，虽然可以实现 GPU 发起的通信，但当前的实现会产生不必要的同步和锁定开销。本文提出了 GICC，这是一个允许 GPU 内核在快速路径上无需主机参与即可直接触发 NIC 级操作的框架。在模板计算中，GPU 线程在计算完边界区域后立即发起光环区交换，从而实现内部计算与边界传输之间的细粒度重叠。GICC 将协调语义与数据移动解耦，并引入了异步资源回收机制：NIC 向 GPU 和主机内存同时发送完成信号，允许轻量级主机线程在不向协调路径注入延迟的情况下，与 GPU 执行并发回收 NIC 资源。这在有限的 NIC 状态下维持了 GPU 驱动的协调，而现有的基于 OFI 的运行时则缺乏这一功能。我们在基于 InfiniBand 和 Slingshot 的 NVIDIA 和 AMD GPU 上实现了 GICC。在 Slingshot 上，GICC 将单次协调延迟降低了高达 229 倍，并将弱扩展效率提高了多达 25%。在 InfiniBand 上，通过消除不必要的锁定和同步，它实现的 put 延迟比 NVSHMEM 低 1.95 倍。在 64 个 AMD MI250X GCD 上的工业级模板代理程序中，GPU-aware MPI 的通信时间比 GICC 高出 52% 以上，而 GICC 实现了 42% 的并行效率，相比之下 MPI 仅为 35.4%。

## Abstract
Distributed GPU applications increasingly rely on kernel-level, cross-node coordination to reduce launch overheads and improve compute-communication overlap, but such support is lacking. On OFI-based interconnects such as HPE Slingshot, which powers six of the top ten systems in the November 2025 Top500, including the top three, GPU kernels cannot autonomously drive distributed coordination: existing runtimes rely on host-driven progress and lack a bounded mechanism for recycling pre-staged NIC work across repeated GPU-triggered operations. On InfiniBand, GPU-initiated communication is possible, but current implementations incur unnecessary synchronization and locking overheads.   This paper presents GICC, a framework that enables GPU kernels to directly trigger NIC-level operations without host involvement on the fast path. In stencils, GPU threads initiate halo exchanges as soon as boundary regions are computed, enabling fine-grained overlap between interior computation and boundary transfer. GICC decouples coordination semantics from data movement and introduces asynchronous resource reclamation: the NIC signals completion to both GPU and host memory, letting a lightweight host thread recycle NIC resources concurrently with GPU execution without injecting latency into the coordination path. This sustains GPU-driven coordination under finite NIC state, absent from existing OFI-based runtimes.   We implement GICC on NVIDIA and AMD GPUs over InfiniBand and Slingshot. On Slingshot, GICC reduces per-coordination latency by up to 229x and improves weak scaling efficiency by up to 25%. On InfiniBand, it achieves up to 1.95x lower put latency than NVSHMEM by eliminating unnecessary locking and synchronization. On an industrial stencil proxy on 64 AMD MI250X GCDs, GPU-aware MPI incurs over 52% higher communication time than GICC, which achieves 42% parallel efficiency versus MPI's 35.4%.

---

## 论文详细总结（自动生成）

这是一份关于论文《GICC: A High-Performance Runtime for GPU-Initiated Communication and Coordination in Modern HPC Systems》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在现代高性能计算（HPC）系统中，分布式 GPU 应用的协调（如同步、屏障、数据交换）通常由主机（CPU）驱动。这种“主机驱动”模式导致了频繁的内核启动开销、主机-设备同步延迟，并限制了计算与通信的重叠。
*   **研究动机**：
    *   在 **OFI/Slingshot** 互连（如 Frontier、El Capitan 等顶级超算）上，GPU 无法自主驱动分布式协调，现有运行时依赖主机进度，且缺乏回收有限网卡（NIC）资源的机制。
    *   在 **InfiniBand** 上，虽然支持 GPU 发起通信，但现有实现（如 NVSHMEM）存在不必要的锁定和同步开销。
    *   **目标**：设计一个高性能运行时 GICC，使 GPU 内核能直接触发 NIC 操作，实现“GPU 驱动”的分布式协调。

### 2. 方法论
*   **核心思想**：将协调语义与数据移动解耦，采用“GPU 触发”模式。主机预先配置 NIC 操作，GPU 在内核执行期间通过轻量级信号（如更新计数器）直接触发这些操作。
*   **关键技术细节**：
    *   **GPU 触发模型**：在 OFI 环境下，由于 GPU 不能动态提交新的网络描述符，GICC 采用主机预置（Pre-stage）有界 NIC 工作、GPU 触发执行的模式。
    *   **异步资源回收**：引入轻量级主机监控线程（Host Monitor），在不干扰 GPU 快速路径的前提下，并发回收和重置 NIC 资源（如延迟工作队列 DWQ 和计数器）。
    *   **双缓冲预置机制（Double-buffered Stage-ahead）**：针对屏障（Barrier）等原语，在 GPU 执行当前协调点时，主机提前准备下一个协调点的资源，消除 CPU-GPU 设置停顿。
    *   **混合进度模型**：GPU 负责触发和观察完成情况，主机负责确保协议的活性（Liveness）和资源的循环利用。

### 3. 实验设计
*   **实验场景与 Benchmark**：
    *   **微基准测试**：点对点 Put 延迟、活动消息（Active Message）Ping-pong、协调开销测试。
    *   **应用级测试**：2D Jacobi 模板计算（弱扩展性）、Cannon 算法矩阵乘法（强扩展性）、Minimod（工业级地震模拟代理程序）。
*   **对比方法（Baselines）**：
    *   **GPU-aware MPI**（如 Cray MPICH）。
    *   **NVSHMEM**（NVIDIA 的 PGAS 库）。
    *   **GASNet-EX**（用于活动消息对比）。

### 4. 资源与算力
*   **硬件平台**：
    *   **Tioga 系统**：配备 AMD MI250X GPU（每个节点 8 个 GCD）和 HPE Slingshot 11 互连。
    *   **Maple 系统**：配备 NVIDIA GH200 GPU 和 Mellanox ConnectX-7 NIC（InfiniBand HDR）。
*   **规模**：实验最高扩展至 64 个 AMD MI250X GCD。
*   **训练/执行时长**：文中未明确提及总训练时长，主要关注推理/计算阶段的通信延迟和并行效率。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从底层延迟测量到大规模并行应用（64 GPU）的完整链条。
*   **充分性**：实验在 NVIDIA 和 AMD 两种主流 GPU 架构以及 Slingshot 和 InfiniBand 两种主流互连上均进行了验证，具有很强的说服力。
*   **客观性**：通过多次测量取平均值并报告标准差，对比了工业界标准的 MPI 和 NVSHMEM 实现，实验设计较为公平。

### 6. 主要结论与发现
*   **延迟大幅降低**：在 Slingshot 上，GICC 将单次协调延迟降低了高达 **229 倍**；在 InfiniBand 上，Put 延迟比 NVSHMEM 低 **1.95 倍**。
*   **扩展性提升**：在 Jacobi 弱扩展性实验中，64 GPU 规模下 GICC 的效率为 76.1%，而 MPI 仅为 60.6%。
*   **应用性能**：对于 Minimod 代理程序，GICC 相比 MPI 减少了 **52%** 的通信时间，并行效率从 35.4% 提升至 42%。
*   **结论**：GPU 驱动且具备资源感知能力的协调机制对于现代 HPC 系统是必要且可实现的。

### 7. 优点
*   **跨平台兼容性**：同时支持 NVIDIA/InfiniBand 和 AMD/Slingshot 生态。
*   **资源高效利用**：解决了 OFI 硬件资源（计数器、队列）有限导致的阻塞问题，通过异步回收实现了持续的 GPU 驱动协调。
*   **细粒度重叠**：允许 GPU 在计算边界数据后立即发起通信，无需等待整个内核结束，实现了更好的计算-通信重叠。

### 8. 不足与局限
*   **编程模型限制**：在 OFI 上仍需主机预置工作，这意味着它更适合通信模式相对固定的应用（如规则模板计算），对于完全动态、运行时的随机通信支持较弱。
*   **可移植性语义差异**：虽然 API 统一，但底层在 IB 上是“直接发起”，在 OFI 上是“触发预置”，这种语义差异可能导致应用在不同硬件上的资源耗尽风险不同。
*   **容错性**：目前假设是故障停止（Fail-stop）模型，未讨论节点失效或消息丢失后的恢复机制。

（完）
