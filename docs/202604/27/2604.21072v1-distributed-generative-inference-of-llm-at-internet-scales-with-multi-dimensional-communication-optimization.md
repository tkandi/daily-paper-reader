---
title: Distributed Generative Inference of LLM at Internet Scales with Multi-Dimensional Communication Optimization
title_zh: 互联网规模下具有多维通信优化的 LLM 分布式生成式推理
authors: "Jiu Chen, Shuangyan Yang, Xu Xiong, Hexiao Duan, Xinran Zhang, Jie Ren, Dong Li"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.21072v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 具有通信优化的互联网规模分布式LLM推理框架
tldr: 针对互联网规模下分布式大模型推理面临的低带宽瓶颈，本文提出BloomBee框架。该框架通过整合层分配、微批处理和张量卸载，并利用动态规划优化协调，同时结合无损压缩和投机采样技术，显著提升了跨节点推理性能。实验表明，BloomBee在吞吐量和延迟方面均优于现有最先进的去中心化推理系统。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21072v1/fig-001.webp\", \"caption\": \"Figure 4. Comparison of data layouts.\", \"page\": 6, \"index\": 1, \"width\": 500, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2604.21072v1/fig-002.webp\", \"caption\": \"Table 2. Notation used in the latency model.\", \"page\": 7, \"index\": 2, \"width\": 504, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2604.21072v1/fig-003.webp\", \"caption\": \"Figure 9. Cost efficiency (throughput per GPU dollar) with and without offloading.\", \"page\": 10, \"index\": 3, \"width\": 475, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2604.21072v1/fig-004.webp\", \"caption\": \"Figure 2. BloomBee with heterogeneous GPU nodes connected over the internet form a pipeline-parallel inference system. BloomBee jointly optimizes layer assignment (𝐿𝑖 ), offloading(𝛼𝑖 ), micro-batching(𝑀), compression, and speculative decoding (𝜏 and 𝑁 ) to reduce communication overhead. Nodes with slow internet or weak GPUs that cannot contribute to system throughput are excluded from the pipeline (𝐿𝑖 = 0).\", \"page\": 4, \"index\": 4, \"width\": 1000, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2604.21072v1/fig-005.webp\", \"caption\": \"Figure 12. Distribution of per-sample completion latency. The dashed vertical line indicates the wall-clock time of autoregressive (baseline) batch, at which all samples complete simultaneously. The dotted vertical line marks the P50 of SD+Prune. SD spreads completions across a wide range, with the majority of samples finished well before the autoregressive baseline.\", \"page\": 12, \"index\": 5, \"width\": 1034, \"height\": 227}]"
motivation: 旨在解决去中心化大模型推理中，由于互联网跨节点带宽受限导致的通信瓶颈问题。
method: 提出BloomBee框架，通过多维度通信优化（如层分配、微批处理、张量卸载）及动态规划求解最优配置。
result: "在多种网络环境下，BloomBee将服务吞吐量提升了高达1.76倍，并将平均延迟降低了43.20%。"
conclusion: BloomBee为互联网规模的分布式大模型推理提供了一种高效且低成本的通信优化解决方案。
---

## 摘要
去中心化大语言模型（LLM）推理将计算分布在互联网上的异构节点之间，提供了一种高性能且具有成本效益的解决方案，是传统集中式推理的替代方案。然而，较低的跨节点网络带宽使得通信成为主要瓶颈。在本文中，我们介绍了 BloomBee，一个互联网规模的分布式 LLM 推理框架。BloomBee 集成了 LLM 层分配、微批处理（micro-batching）和张量卸载（tensor offloading），从多个维度优化通信。此外，BloomBee 将这些技术的协调建模为一个优化问题，并使用动态规划进行求解。BloomBee 还根据低带宽网络环境定制了无损压缩和投机解码（speculative decoding），以减少通信开销。我们在多种网络环境下对 BloomBee 进行了评估，结果表明其服务吞吐量提高了多达 1.76 倍。与最先进的去中心化 LLM 推理系统相比，它还将平均延迟降低了多达 43.20%。BloomBee 已开源。

## Abstract
Decentralized LLM inference distributes computation among heterogeneous nodes across the internet, offering a performant and cost-efficient solution, alternative to traditional centralized inference. However, the low cross-node network bandwidth makes communication the primary bottleneck. In this paper, we introduce BloomBee, an internet-scale distributed LLM inference framework. BloomBee integrates LLM-layer assignment, micro-batching and tensor offloading to optimize communication from multiple dimensions. Additionally, BloomBee formulates the coordination of these techniques as an optimization problem and solves it using dynamic programming. BloomBee also customizes lossless compression and speculative decoding according to low-bandwidth network settings to reduce communication overhead. We evaluate BloomBee across a spectrum of network environments and show that it improves service throughput by up to 1.76x. It also reduces average latency by up to 43.20% compared to state-of-the-art decentralized LLM inference systems. BloomBee is open-sourced.

---

## 论文详细总结（自动生成）

这是一份关于论文《Distributed Generative Inference of LLM at Internet Scales with Multi-Dimensional Communication Optimization》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在互联网规模（低带宽、高延迟、节点异构）的环境下，高效地进行大语言模型（LLM）的分布式推理。
*   **研究动机**：
    *   **中心化瓶颈**：超大规模云服务商提供的 AI 服务存在成本高、单点故障风险以及 GPU 资源碎片化的问题。
    *   **去中心化潜力**：利用互联网上的闲置异构资源（如个人游戏 GPU、小型数据中心）可以提供弹性、低成本的推理能力。
    *   **通信挑战**：互联网带宽（20-500 Mbps）比数据中心内部连接慢 500-2000 倍。在去中心化环境下，通信开销（而非计算）成为了绝对的性能瓶颈。

### 2. 方法论
BloomBee 框架通过多维度通信优化来提升吞吐量，核心思想是“减少通信跳数”和“压缩单跳数据量”：
*   **联合优化调度**：
    *   **层分配（Layer Assignment）**：决定哪些 Transformer 层放置在哪个节点。
    *   **微批处理（Micro-batching）**：将 Batch 进一步拆分，实现计算与通信的流水线并行重叠。
    *   **张量卸载（Tensor Offloading）**：将部分 KV Cache 卸载到 CPU 内存，释放 GPU 空间以容纳更多模型层，从而减少跨节点通信的跳数。
    *   **动态规划（DP）求解**：将上述三个变量的协调建模为优化问题，利用 DP 在毫秒级时间内找到最优配置。
*   **定制化无损压缩**：
    *   提出了一种基于**字节拆分（Byte-split）**的转换方法。研究发现激活值的“高位字节”和“低位字节”具有不同的熵特征，拆分后配合 ZSTD 压缩比传统方法（如 ZipNN）更有效。
*   **互联网感知的投机解码（SD）**：
    *   针对 SD 在低带宽下会因传输候选 Token 树而导致带宽过载的问题，引入了**轻量级分类器进行剪枝**。在传输前剔除低概率候选 Token，平衡了“接受率”与“通信量”。

### 3. 实验设计
*   **数据集**：使用 AlpacaEval 和 Alpaca 数据集进行推理测试。
*   **对比基准（Benchmarks）**：
    *   **Petals**：现有的去中心化 LLM 推理和微调框架。
    *   **Helix**：针对异构 GPU 和网络优化的最先进 LLM 服务系统。
*   **实验场景**：
    *   **E1**：数据中心环境（45 Gbps）。
    *   **E2-E5**：模拟互联网环境，带宽从 500 Mbps 降至 20 Mbps。
    *   **E6**：真实跨国环境（加州 A100、新泽西 RTX 4090、加拿大 RTX 4090）。
*   **测试模型**：LLaMA (13B, 30B, 65B), Falcon (7B, 40B), Mixtral-8x7B。

### 4. 资源与算力
*   **GPU 型号**：NVIDIA RTX 5090, RTX 4090, A100。
*   **节点规模**：使用了 2 到 8 个不等的 GPU 节点构建流水线。
*   **训练/推理时长**：论文侧重于推理性能，未提及模型训练时长，但强调了调度算法（DP）的求解时间在 1ms 以内。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B 到 65B 不同参数规模的模型，并在 6 种不同的网络拓扑环境下进行了测试。
*   **消融实验**：分别对卸载（Offloading）、微批处理（Micro-batching）、压缩（Compression）和投机解码（SD）进行了单独的有效性验证。
*   **充分性评价**：实验设计较为全面，不仅有受控的模拟环境，还有真实的跨国互联网测试，客观地展示了在极低带宽（20 Mbps）下的系统表现。

### 6. 主要结论与发现
*   **性能提升**：相比 Petals 和 Helix，BloomBee 在某些环境下将吞吐量提升了高达 **1.76 倍**，平均延迟降低了 **43.20%**。
*   **通信主导**：在互联网规模下，通信占推理总时间的比例可达 56.6% 以上，证明了以通信为中心的优化路径是正确的。
*   **策略选择**：在高带宽下，投机解码贡献最大；在极低带宽下，压缩和微批处理的组合是维持吞吐量的关键。
*   **成本效益**：通过张量卸载减少 GPU 使用数量，可将单位美元的吞吐量提升高达 1.82 倍。

### 7. 优点
*   **多维协同**：不只是单一的优化，而是通过数学建模（DP）解决了层分配、卸载和批处理之间的复杂耦合关系。
*   **创新的压缩视角**：通过字节拆分揭示了激活值压缩的新机会，比通用的工业压缩算法更贴合 AI 张量特性。
*   **实用性强**：针对真实互联网环境（非 RDMA、有丢包、高延迟）设计，具有很强的落地参考价值。

### 8. 不足与局限
*   **拓扑限制**：目前主要针对线性流水线并行（Linear Pipeline），对于更复杂的网状或树状分布式拓扑讨论较少。
*   **离线推理导向**：虽然降低了延迟，但其核心优化目标仍是吞吐量，对于极高实时性要求的交互式对话场景，互联网延迟仍是物理瓶颈。
*   **模型覆盖**：虽然测试了多种模型，但主要集中在 Transformer 架构，未讨论如 Mamba 等新型架构的适用性。

（完）
