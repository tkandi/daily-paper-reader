---
title: A Delta-Aware Orchestration Framework for Scalable Multi-Agent Edge Computing
title_zh: 一种面向可扩展多智能体边缘计算的增量感知编排框架
authors: "Samaresh Kumar Singh, Joyjit Roy"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20129v1"
tags: ["query:mlsys"]
score: 7.0
evidence: 边缘计算编排框架与硬件调度
tldr: "针对大规模多智能体边缘计算中超过100个智能体时出现的性能崩溃问题，本文提出DAOEF框架。该框架通过差异化神经缓存减少冗余计算，利用基于关键性的动作空间剪枝降低协调复杂度，并结合硬件亲和性匹配优化资源调度。实验表明，DAOEF在200个智能体规模下实现了62%的延迟降低，有效解决了协同失效问题。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20129v1/fig-001.webp\", \"caption\": \"TABLE I COMPARISON OF DAOEF WITH STATE-OF-THE-ART MULTI-AGENT EDGE COMPUTING APPROACH\", \"page\": 2, \"index\": 1, \"width\": 836, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2604.20129v1/fig-002.webp\", \"caption\": \"TABLE V PERFORMANCE ACROSS FOUR REAL-WORLD DATASETS (DAOEF VS. BEST BASELINE)\", \"page\": 7, \"index\": 2, \"width\": 872, \"height\": 184}, {\"url\": \"assets/figures/arxiv/2604.20129v1/fig-003.webp\", \"caption\": \"Fig. 1. DAOEF architecture with three-tier priority filtering and delta-aware caching.\", \"page\": 3, \"index\": 3, \"width\": 968, \"height\": 712}, {\"url\": \"assets/figures/arxiv/2604.20129v1/fig-004.webp\", \"caption\": \"Fig. 2. Scalability comparison: DAOEF maintains sub-100 ms latency to 250 agents. Baseline methods exceed 300 ms beyond 100 agents due to superlinear growth in decision complexity. Error bars are 95% Confidence Intervals across 50 runs.\", \"page\": 8, \"index\": 4, \"width\": 516, \"height\": 256}]"
motivation: 传统多智能体系统在规模扩大时，由于动作空间爆炸、计算冗余和硬件调度不当，会导致性能超线性下降。
method: 提出DAOEF框架，集成差异化神经缓存、动作空间剪枝和硬件亲和性匹配三大核心机制。
result: "在200个智能体的部署中，该框架将延迟降低了62%，并实现了亚线性的延迟增长。"
conclusion: 协同优化计算、协调和硬件匹配是解决大规模多智能体边缘计算性能瓶颈的关键。
---

## 摘要
当规模超过100个智能体时，会发生协同崩溃（Synergistic Collapse），导致超线性的性能下降，且单一优化手段无法阻止。我们在使用MADDPG的智慧城市部署中观察到了这种崩溃，涉及150个摄像头，其截止日期满足率从78%降至34%，导致每年约18万美元的成本超支。先前的工作孤立地解决了各个影响因素：动作空间的指数级增长、空间相邻智能体间的计算冗余以及与任务无关的硬件调度。尚未有研究探讨这三个因素如何相互作用并相互放大。我们提出了DAOEF（面向边缘联邦的增量感知编排），该框架通过以下三种方式同时解决上述问题：(1) 微分神经缓存（Differential Neural Caching），存储中间层激活值并仅计算输入增量，通过经验校准的相似度阈值，在保持精度损失低于2%的同时，实现了比输出级缓存高2.1倍的命中率（72%对35%）；(2) 基于关键性的动作空间剪枝（Criticality-Based Action Space Pruning），将智能体组织成优先级层级，将协调复杂度从O(n²)降低到O(n log n)，且最优性损失小于6%；(3) 学习型硬件亲和性匹配（Learned Hardware Affinity Matching），将任务分配给最佳加速器（GPU、CPU、NPU或FPGA），以防止复合失配惩罚。受控因素隔离实验证实，每种机制都是必要但不充分的：移除任何单一机制都会使延迟增加40%以上，验证了增益是相互依赖而非简单叠加的。在四个数据集（100-250个智能体）和20台设备的物理测试平台上，DAOEF相比独立应用这三种机制实现了1.45倍的乘法增益。200个智能体的云端部署实现了62%的延迟降低（280毫秒对735毫秒），且在多达250个智能体的情况下实现了亚线性延迟增长。

## Abstract
The Synergistic Collapse occurs when scaling beyond 100 agents causes superlinear performance degradation that individual optimizations cannot prevent. We observe this collapse with 150 cameras in Smart City deployment using MADDPG, where Deadline Satisfaction drops from 78% to 34%, producing approximately $180,000 in annual cost overruns. Prior work has addressed each contributing factor in isolation: exponential action-space growth, computational redundancy among spatially adjacent agents, and task-agnostic hardware scheduling. None has examined how these three factors interact and amplify each other. We present DAOEF (Delta-Aware Orchestration for Edge Federations), a framework that addresses all three simultaneously through: (1) Differential Neural Caching, which stores intermediate layer activations and computes only the input deltas, achieving 2.1x higher hit ratios (72% vs. 35%) than output-level caching while staying within 2% accuracy loss through empirically calibrated similarity thresholds; (2) Criticality-Based Action Space Pruning, which organizes agents into priority tiers and reduces coordination complexity from O(n2) to O(n log n) with less than 6% optimality loss; and (3) Learned Hardware Affinity Matching, which assigns tasks to their optimal accelerator (GPU, CPU, NPU, or FPGA) to prevent compounding mismatch penalties. Controlled factor-isolation experiments confirm that each mechanism is necessary but insufficient on its own: removing any single mechanism increases latency by more than 40%, validating that the gains are interdependent rather than additive. Across four datasets (100-250 agents) and a 20-device physical testbed, DAOEF achieves a 1.45x multiplicative gain over applying the three mechanisms independently. A 200-agent cloud deployment yields 62% latency reduction (280 ms vs. 735 ms), sub-linear latency growth up to 250 agents.