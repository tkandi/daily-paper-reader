---
title: "STQuant: Spatio-Temporal Adaptive Framework for Optimizer Quantization in Large Multimodal Model Training"
title_zh: STQuant：大模型多模态训练中优化器量化的时空自适应框架
authors: "Minglu Liu, Cunchen Hu, Liangliang Xu, Fengming Tang, Ruijia Wang, Fu Yu"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06836v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 具有动态优化器量化的分布式训练框架
tldr: "针对大模型训练中优化器状态内存占用高的问题，本文提出STQuant框架。该框架打破了传统固定精度的局限，通过在层、状态变量和训练步骤间动态分配精度，在保证模型质量的同时显著降低内存。其核心包含近优因子选择策略和线性复杂度的动态决策算法，在GPT-2和ViT上实现了84.4%的内存节省。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06836v1/fig-001.webp\", \"caption\": \"Figure 1: Spatiotemporal evolution and correlation analysis of gradients and Adam optimizer states. (a) Sharpness of the gradient distribution (CV) across 48 core weight layers in 12 Transformer blocks, including QKV projections, attention projections, MLP expansion layers, and MLP compression layers, sampled every 50 training steps over 5000 total steps; (b) Correlation between gradients and the coefficient of variation (CV) of the first-order moment𝑚, showing a very strong linear relationship (Pearson 𝑅 = 0.9777); (c) Correlation between gradients and the second-order moment 𝑣 , where, despite the squaring operation introducing nonlinear mapping and noise amplification, the correlation remains strong (𝑅 = 0.7169).\", \"page\": 2, \"index\": 1, \"width\": 994, \"height\": 276}, {\"url\": \"assets/figures/arxiv/2604.06836v1/fig-002.webp\", \"caption\": \"Figure 6: Bit-width evolution of STQuant during pre-training. (a) shows the dynamic bit-width allocation across layers over training epochs, and (b) presents the bit-width evolution of different parameter groups within a Transformer block.\", \"page\": 7, \"index\": 2, \"width\": 1051, \"height\": 324}, {\"url\": \"assets/figures/arxiv/2604.06836v1/fig-003.webp\", \"caption\": \"Figure 2: Overview of the STQuant framework: The system consists of three collaborative engines: (1) the Score Engine extracts spatio-temporal gradient features across GPUs; (2) the Distributed Engine synchronizes global statistics to determine optimal bit-widths; and (3) the Quantization Engine executes dual-mode block-wise compression (for𝑚 and 𝑣).\", \"page\": 3, \"index\": 3, \"width\": 492, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2604.06836v1/fig-004.webp\", \"caption\": \"Table 3: Performance and optimizer-state memory savings of STQuant AdamW compared with full-precision and 8-bit AdamW(bnb) baselines across core fine-tuning tasks.\", \"page\": 8, \"index\": 4, \"width\": 1036, \"height\": 397}]"
motivation: 现有的固定精度量化方法忽略了优化器状态在不同层和训练阶段的分布差异，导致模型精度下降。
method: 提出一种时空自适应框架，利用近优因子选择和线性复杂度决策算法，在多维度动态调整量化精度。
result: "在GPT-2和ViT实验中，STQuant将优化器状态内存减少了84.4%，平均位宽降至5.1位，且计算开销极低。"
conclusion: STQuant通过动态精度分配有效平衡了训练效率与模型性能，为大规模多模态模型的高效训练提供了新方案。
---

## 摘要
量化是降低大规模模型训练内存开销的有效方法。然而，大多数现有方法采用固定精度策略，忽略了优化器状态分布在不同层和训练步骤之间存在显著差异的事实。这种统一的设计往往会导致明显的精度下降。为了超越固定量化，我们提出了 STQuant，这是一个分布式训练框架，通过在层、状态变量和训练步骤之间进行动态精度分配，在保持模型质量的同时减少优化器状态的内存占用。在训练过程中直接应用动态量化面临两个挑战：首先，优化器状态对数值敏感，量化噪声可能会破坏模型质量的稳定性；其次，同时考虑多个状态和层会导致巨大的组合搜索空间。STQuant 通过两项关键技术解决了这些挑战：1) 一种可证明的近乎最优的因子选择策略，能够准确识别对精度自适应影响最大的因子；2) 一种动态转换决策算法，将搜索成本从指数级降低到线性复杂度。在 GPT-2 和 ViT 上的实验表明，与现有解决方案相比，STQuant 将优化器状态内存减少了 84.4%，实现的平均位宽低至 5.1 位。此外，STQuant 仅产生 O(N/K) 的计算开销，且仅需 O(1) 的额外空间。

## Abstract
Quantization is an effective way to reduce the memory cost of large-scale model training. However, most existing methods adopt fixed-precision policies, which ignore the fact that optimizer-state distributions vary significantly across layers and training steps. Such uniform designs often introduce noticeable accuracy degradation. To move beyond fixed quantization, we propose STQuant, a distributed training framework that reduces the memory footprint of optimizer states via dynamic precision allocation across layers, state variables, and training steps, while maintaining model quality. Naively applying dynamic quantization during training is challenging for two reasons. First, optimizer states are numerically sensitive, and quantization noise can destabilize quality. Second, jointly considering multiple states and layers induces a large combinatorial search space. STQuant addresses these challenges with two key techniques: 1) a provably near-optimal factor selection strategy that accurately identifies the most influential factors for precision adaptation. 2) a dynamic transition decision algorithm that reduces the search cost from exponential to linear complexity. Experiments on GPT-2 and ViT show that STQuant reduces optimizer-state memory by 84.4%, achieving an average bit-width of as low as 5.1 bits, compared with existing solutions. Moreover, STQuant incurs only O(N/K) computational overhead and requires O(1) extra space.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **STQuant** 的时空自适应优化器量化框架，旨在解决大规模多模态模型训练中优化器状态（Optimizer States）带来的巨大内存压力。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在大模型训练中，优化器状态（如 Adam 的一阶矩 $m$ 和二阶矩 $v$）占用的内存通常是模型权重的 4 到 12 倍，成为训练规模扩张的主要瓶颈。
*   **研究动机**：
    *   **现有方法局限**：目前的量化方法（如 8-bit Adam）多采用固定精度策略，忽略了不同神经网络层、不同训练阶段对精度需求的动态差异。
    *   **时空异质性**：作者观察到梯度分布在时间（训练步数）和空间（模型层级）上具有显著的非平稳性，且优化器状态与梯度特征高度相关。
    *   **挑战**：动态量化面临优化器状态数值敏感（易导致不收敛）和搜索空间巨大（层与精度的组合爆炸）两大难题。

### 2. 方法论：STQuant 框架
STQuant 通过三个核心引擎实现时空自适应的精度分配：
*   **评分引擎 (Score Engine)**：
    *   **双因子敏感度代理**：定义强度因子 $n$（梯度均方根）和变化因子 $r$（梯度变异系数），利用 Fisher 信息矩阵理论近似 Hessian 迹，评估各层的量化敏感度。
    *   **时间退火因子 ($S_t$)**：引入基于双曲正割函数的调度机制，在训练初期（不稳定阶段）提供高精度保护，随训练进行平滑过渡到低精度。
    *   **层级特征因子**：计算层间相对重要性，确保关键层获得更多精度预算。
*   **分布式引擎 (Distributed Engine)**：
    *   **统一评分函数**：将上述因子通过对数形式融合为标量分数，将复杂的组合优化问题简化为线性复杂度的阈值映射。
    *   **同步机制**：在 GPU 间同步梯度统计数据，确保分布式训练中各节点量化决策的一致性。
*   **量化引擎 (Quantization Engine)**：
    *   **双模块块量化**：对一阶矩 $m$ 采用线性对称量化（保持方向信息），对二阶矩 $v$ 采用对数域量化（处理极大的数值动态范围）。

### 3. 实验设计
*   **数据集与场景**：
    *   **预训练**：OpenWebText (GPT-2 XL), COCO 2017 (ViT-Base)。
    *   **微调**：MNLI (RoBERTa-Large), COCO 2017 (ViT 分类/检测), Wikitext-103 (GPT-2 Medium), 图文检索任务。
*   **Benchmark 与对比方法**：
    *   **基准**：32-bit AdamW（全精度）。
    *   **对比方法**：工业界标准的 `bitsandbytes` 8-bit AdamW。
*   **评估指标**：训练损失（Loss）、困惑度（PPL）、准确率（Accuracy）、mAP、Recall@1 以及内存节省比例。

### 4. 资源与算力
*   **硬件环境**：使用 NVIDIA A800 (80GB) GPU。
*   **算力分配**：
    *   **预训练**：使用 4 张 GPU。
    *   **微调与消融实验**：使用单张 GPU。
*   **训练时长**：文中未明确给出总训练时长，但提到预训练采样了 5000 步进行分析，且 STQuant 的决策开销仅为 $O(N/K)$，对总时长影响极小。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.2 亿到 15 亿参数的模型，任务跨越 NLP、CV 和多模态检索。
*   **消融实验**：针对双因子、时间因子、空间因子分别做了剔除实验，验证了各组件对 PPL 和平均位宽的影响。
*   **充分性评价**：实验设计较为全面，不仅关注了收敛曲线，还通过线性探测（Linear Probing）验证了预训练表示的质量。对比实验在相同随机种子和超参数下进行，具有较高的客观性和公平性。

### 6. 主要结论与发现
*   **内存节省**：相比全精度 AdamW，STQuant 减少了约 **84.4%** 的优化器内存占用；相比 8-bit AdamW，进一步节省了约 36%-38% 的内存。
*   **极低位宽**：在保证不损失精度的前提下，实现了平均仅 **5.1 bit** 的优化器状态存储。
*   **性能保持**：在所有测试任务中，STQuant 的收敛速度和最终精度均与全精度基准持平，甚至在某些多模态任务中表现更优。
*   **层级规律**：实验发现 Embedding 层和 Head 层通常需要更高精度，而中间的 Transformer 块具有较高的压缩冗余。

### 7. 优点与亮点
*   **动态自适应**：打破了固定精度的传统，实现了“按需分配”精度，极大地挖掘了压缩潜力。
*   **低开销设计**：决策算法复杂度为线性，且仅需 $O(1)$ 的额外辅助空间，非常适合大规模分布式训练。
*   **理论支撑**：量化因子的选择基于 Fisher 信息理论，而非纯经验主义，增强了方法的鲁棒性。
*   **双模量化**：针对 $m$ 和 $v$ 的不同物理含义采用不同的量化模式（线性 vs 对数），设计精巧。

### 8. 不足与局限
*   **硬件加速限制**：虽然内存占用大幅下降，但由于目前主流硬件（如 H100/A100）对非标准位宽（如 5-bit 或 6-bit）缺乏原生算子支持，实际计算速度的提升可能受限（论文主要强调内存节省而非吞吐量提升）。
*   **优化器覆盖面**：主要针对 AdamW 及其变体，对于其他类型的优化器（如 SGD 或二阶优化器 Sophia）的适用性尚未验证。
*   **超参数依赖**：评分函数中的偏置项 $\Phi$ 和阈值设定可能需要针对不同架构进行微调，尽管文中给出了通用建议值。

（完）
