---
title: "AdaHOP: Fast and Accurate Low-Precision Training via Outlier-Pattern-Aware Rotation"
title_zh: AdaHOP：通过离群值模式感知的旋转实现快速且准确的低精度训练
authors: "Seonggon Kim, Alireza Khodamoradi, Kristof Denolf, Eunhyeok Park"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02525v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 大模型低精度训练优化
tldr: 针对大语言模型低精度训练中离群值分布不均的问题，本文提出AdaHOP。通过系统研究权重、激活和梯度的离群值模式（行、列或无），AdaHOP为每个矩阵乘法自适应选择最优的Hadamard变换方向或离群值提取策略。结合硬件感知的Triton算子，该方法在MXFP4精度下达到了BF16的训练质量，同时显著提升了内存压缩率和计算速度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02525v1/fig-001.webp\", \"caption\": \"Table 1: Distribution of outlier pattern pairs across computation paths for three LLM architectures, measured after 30 training steps on C4. Each cell shows the count of linear layers exhibiting the corresponding pattern pair. Patterns RR, NR, and CR do not appear because Weight tensors consistently exhibit the None pattern and Activation tensors exhibit either Column-wise or None patterns.\", \"page\": 6, \"index\": 1, \"width\": 850, \"height\": 204}, {\"url\": \"assets/figures/arxiv/2604.02525v1/fig-002.webp\", \"caption\": \"Table 5: Latency breakdown of AdaHOP’s OE pipeline for two representative matrix sizes corresponding to actual linear layer dimensions in the models (2048× 14336 from the MLP projection and 4096× 4096 from the attention projection of Llama3.1-8B), compared to BF16 GEMM baseline.\", \"page\": 11, \"index\": 2, \"width\": 490, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2604.02525v1/fig-003.webp\", \"caption\": \"Figure 1: Training loss curves and loss difference relative to BF16 for (Left) Llama3.2-1B and (Right) Instella-3B. AdaHOP consistently achieves the lowest loss gap relative to BF16 among all MXFP4-based methods.\", \"page\": 2, \"index\": 3, \"width\": 829, \"height\": 464}, {\"url\": \"assets/figures/arxiv/2604.02525v1/fig-004.webp\", \"caption\": \"Figure 5: The pipeline of AdaHOP. For each linear layer’s three matrix multiplications, AdaHOP selectively applies IHT, OE (Left or Right), or high-precision computation based on the detected outlier pattern pair (Pn). Here n ∈ {1, 3, 5} indexes the input tensors.\", \"page\": 7, \"index\": 4, \"width\": 829, \"height\": 213}, {\"url\": \"assets/figures/arxiv/2604.02525v1/fig-005.webp\", \"caption\": \"Table 3: Zero-shot accuracy (%) on four downstream benchmarks after training on C4. Best MXFP4based result is in bold; second best is underlined.\", \"page\": 10, \"index\": 5, \"width\": 776, \"height\": 554}, {\"url\": \"assets/figures/arxiv/2604.02525v1/fig-006.webp\", \"caption\": \"Figure 7: Hardware-aware implementation pipeline of AdaHOP. The pipeline consists of four stages: FOID, IHT and Quantization, Fused MXFP4+BF16 GEMM, and Fused Scatter-Add, all implemented as optimized Triton kernels targeting AMD CDNA4 architecture. Note that red block of Fast Outlier Index Detection represents first 64 elements along each column.\", \"page\": 20, \"index\": 6, \"width\": 826, \"height\": 197}, {\"url\": \"assets/figures/arxiv/2604.02525v1/fig-007.webp\", \"caption\": \"Figure 3: Improvement in quantization error when applying IHT for each outlier pattern pair. (Left) Synthetic outlier tensors (Row/Col kurtosis = 225.95, None kurtosis = 0). (Right) Real tensors from Llama3.2-3B’s layers.23.feed_forward.out_proj (Row/Col kurtosis = 197.81, None kurtosis = 6.29). IHT effectively reduces error for CR pairs but is ineffective or harmful for RC, RN, RR, NC, and CC pairs.\", \"page\": 5, \"index\": 7, \"width\": 829, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2604.02525v1/fig-008.webp\", \"caption\": \"Figure 6: Outlier patterns of Weight (W ), Activation (X), and Gradient (GY ) tensors across 300 training steps for all 16 representative blocks of Llama3.2-3B. Each row represents a tensor from a specific layer, and the color indicates the detected outlier pattern at each step. This extended view reveals depth-dependent transitions in gradient outlier patterns: Row-wise patterns appear in early blocks, fade to None in the middle layers, and re-emerge selectively in the K-projection gradient toward the final blocks.\", \"page\": 16, \"index\": 8, \"width\": 826, \"height\": 629}]"
motivation: 现有的低精度训练方法统一应用固定Hadamard变换，忽略了不同张量间离群值结构的显著差异，导致量化误差无法最小化。
method: 提出AdaHOP框架，根据离群值模式为每个矩阵乘法自适应分配内部Hadamard变换或结合选择性离群值提取的策略。
result: 在MXFP4精度下实现了与BF16相当的训练质量，并获得高达3.6倍的内存压缩和1.8倍的算子加速。
conclusion: 通过感知离群值模式并动态调整变换策略，可以在保证大模型训练精度的同时，大幅提升低精度训练的效率和硬件性能。
---

## 摘要
低精度训练（LPT）通常采用哈达玛变换（Hadamard transforms）来抑制离群值并减轻大语言模型（LLMs）中的量化误差。然而，尽管张量之间的离群值结构存在显著差异，先前的方法仍统一应用固定的变换。通过对 LLM 的权重、激活和梯度的离群值模式进行首次系统研究，我们表明这种策略存在根本性缺陷：基于哈达玛变换的抑制效果取决于变换的平滑方向如何与每个操作数的离群值结构对齐——这一特性在不同层和计算路径之间存在显著差异。我们将这些模式归纳为三种类型：行向（Row-wise）、列向（Column-wise）和无（None）。每一对操作数都需要定制的变换方向或离群值处理策略，以最小化量化误差。基于这一见解，我们提出了 AdaHOP（自适应哈达玛变换与离群值模式感知策略），它为每个矩阵乘法分配其最优策略：在内维度平滑有效的场景下使用内哈达玛变换（IHT），或者在无效场景下将 IHT 与选择性离群值提取（OE）相结合——将主要的离群值路由到高精度路径。结合硬件感知的 Triton 内核，AdaHOP 在 MXFP4 精度下实现了 BF16 的训练质量，同时相比 BF16 全精度训练，提供了高达 3.6 倍的内存压缩和 1.8 倍的内核加速。

## Abstract
Low-precision training (LPT) commonly employs Hadamard transforms to suppress outliers and mitigate quantization error in large language models (LLMs). However, prior methods apply a fixed transform uniformly, despite substantial variation in outlier structures across tensors. Through the first systematic study of outlier patterns across weights, activations, and gradients of LLMs, we show that this strategy is fundamentally flawed: the effectiveness of Hadamard-based suppression depends on how the transform's smoothing direction aligns with the outlier structure of each operand -- a property that varies substantially across layers and computation paths. We characterize these patterns into three types: Row-wise, Column-wise, and None. Each pair requires a tailored transform direction or outlier handling strategy to minimize quantization error. Based on this insight, we propose AdaHOP (Adaptive Hadamard transform with Outlier-Pattern-aware strategy), which assigns each matrix multiplication its optimal strategy: Inner Hadamard Transform (IHT) where inner-dimension smoothing is effective, or IHT combined with selective Outlier Extraction (OE) -- routing dominant outliers to a high-precision path -- where it is not. Combined with hardware-aware Triton kernels, AdaHOP achieves BF16 training quality at MXFP4 precision while delivering up to 3.6X memory compression and 1.8X kernel acceleration} over BF16 full-precision training.

---

## 论文详细总结（自动生成）

### 论文总结：AdaHOP - 基于离群值模式感知旋转的快速准确低精度训练

#### 1. 核心问题与整体含义（研究动机和背景）
低精度训练（LPT）是提升大语言模型（LLM）训练效率的关键，但其面临的核心挑战是**离群值（Outliers）**。离群值会放大化量化误差，导致训练不稳定或精度下降。
*   **研究动机**：现有的方法（如 HALO、Quartet）通常对所有层统一应用固定的哈达玛变换（Hadamard Transform）来平滑离群值。
*   **核心发现**：本文通过系统研究发现，权重、激活和梯度的离群值结构在不同层和计算路径（前向、反向梯度计算）中存在显著差异。**“一刀切”的变换策略是低效的**，因为变换的有效性取决于其平滑方向是否与离群值的分布模式（行向、列向或无）正交对齐。

#### 2. 论文提出的方法论：AdaHOP
AdaHOP 的核心思想是**自适应地为每个矩阵乘法选择最优的量化加速策略**。
*   **离群值模式分类**：将张量模式分为三类：行向（Row-wise, R）、列向（Column-wise, C）和无（None, N）。
*   **校准与检测**：在训练初期进行 30 个步长的 BF16 预训练，利用变异系数（CV）检测各层张量的离群值模式。由于这些模式在训练中高度稳定，一次校准即可固定后续策略。
*   **自适应策略选择**：
    *   **IHT（内部哈达玛变换）**：适用于内维度平滑有效的模式对（如 CN、NN）。
    *   **OE（离群值提取）+ IHT**：对于 IHT 无法有效处理的模式对（如 RN、RC、NC、CC），将前 $k=64$ 个最严重的离群值提取到高精度（BF16）路径处理，剩余部分进行 IHT 平滑后量化为 MXFP4。
    *   **分级变体**：AdaHOP-Lv1（对 CC 模式使用 OE）；AdaHOP-Lv2（对注意力机制关键的 CC 模式直接使用 BF16 以保证最高精度）。
*   **硬件感知实现**：基于 AMD CDNA4 架构，利用 Triton 编写融合算子，实现离群值检测、变换、量化及混合精度矩阵乘法（GEMM）的并行执行。

#### 3. 实验设计
*   **数据集**：使用 C4 数据集进行预训练。
*   **模型**：涵盖 Llama3.2-1B/3B、Instella-3B、Llama3.1-8B 等不同规模的模型。
*   **Benchmark**：评估了四个下游任务的零样本（Zero-shot）准确率：PIQA、HellaSwag、ARC-Easy 和 LAMBADA。
*   **对比方法**：
    *   BF16（全精度基准）。
    *   Naive MXFP4（无抑制量化）。
    *   MXFP4 + Hadamard（固定变换）。
    *   Tseng et al. (2025)（随机哈达玛策略）。
    *   HALO (2025)（针对梯度路径的固定旋转策略）。

#### 4. 资源与算力
*   **硬件平台**：实验主要在 **AMD Instinct™ MI350 系列 GPU**（基于 CDNA4 架构）上运行。
*   **训练规模**：根据 Chinchilla 定律缩放训练 Token 数，Llama3.2-1B 训练 40B tokens，3B 模型训练 60B tokens，8B 模型训练 160B tokens。
*   **算力细节**：文中强调了其 Triton 内核在 CDNA4 上的优化，但未详细列出总 GPU 小时数。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1B 到 8B 参数量的多个模型，验证了方法的可扩展性。
*   **消融与对比**：对比了当前最先进的多种低精度训练方案，并提供了训练损失曲线、下游任务精度、内存占用和吞吐量等多维度数据。
*   **客观性**：通过对离群值模式随训练时间变化的稳定性分析（图 4），证明了校准法的合理性。实验设计较为全面，涵盖了主流的开源模型架构。

#### 6. 主要结论与发现
*   **精度对齐**：AdaHOP 在 MXFP4 精度下达到了与 BF16 几乎一致的训练质量，解决了低比特训练中的不稳定性问题。
*   **效率提升**：相比 BF16，实现了高达 **3.6 倍** 的线性层内存压缩，以及 **1.59x - 1.80x** 的内核级加速。
*   **模式稳定性**：LLM 中的离群值模式在训练早期即形成并保持稳定，这使得静态的自适应策略成为可能。

#### 7. 优点
*   **精细化治理**：首次系统分析并利用了不同计算路径下离群值的结构化差异，而非盲目应用变换。
*   **软硬结合**：不仅有理论推导（证明了 OE 对误差界的改善），还针对 AMD 硬件特性优化了 Triton 算子，具有极强的实用价值。
*   **低开销**：校准过程仅占总训练预算的不到 0.01%，且无需在运行时动态检测模式。

#### 8. 不足与局限
*   **超参数固定**：离群值提取数量 $k$ 固定为 64，虽然是基于硬件对齐考虑，但对于不同层或不同模型，可能存在更优的动态 $k$ 值。
*   **架构覆盖**：主要验证了 Llama 类架构，对于混合专家模型（MoE）或其他非 Transformer 结构的适用性有待进一步验证。
*   **变换类型**：目前仅使用固定的哈达玛矩阵，未来若结合可学习的旋转矩阵（Learned Rotation）可能会进一步降低误差。

（完）
