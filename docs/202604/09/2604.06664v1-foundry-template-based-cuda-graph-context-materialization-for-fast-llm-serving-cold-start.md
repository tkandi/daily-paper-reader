---
title: "Foundry: Template-Based CUDA Graph Context Materialization for Fast LLM Serving Cold Start"
title_zh: Foundry：基于模板的 CUDA 图上下文物化，实现大语言模型服务的快速冷启动
authors: "Xueshen Liu, Yongji Wu, Yuncheng Yao, Danyang Zhuo, Ion Stoica, Z. Morley Mao"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06664v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 快速LLM推理服务与CUDA图上下文实例化
tldr: Foundry 针对大模型推理冷启动中 CUDA 图捕获耗时过长的问题，提出了一种基于模板的上下文实例化系统。它通过离线持久化图拓扑与执行上下文，并在在线阶段快速重建可执行图，解决了 CUDA 图难以序列化及与设备地址耦合的难题。该系统支持分布式部署，在保持 CUDA 图性能优势的同时，将 Qwen3-235B 等大模型的冷启动时间从分钟级缩短至秒级，显著提升了推理服务的弹性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06664v1/fig-001.webp\", \"caption\": \"Figure 4. Overview of Foundry’s workflow.\", \"page\": 4, \"index\": 1, \"width\": 1040, \"height\": 396}, {\"url\": \"assets/figures/arxiv/2604.06664v1/fig-002.webp\", \"caption\": \"Figure 8. [Cold-Start Latency Reduction] End-to-end engine initialization breakdown comparing vLLM, CUDAcheckpoint, and Foundry across four model configurations. Note: torch.compile only does bytecode transformation and cache loading.\", \"page\": 9, \"index\": 2, \"width\": 492, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2604.06664v1/fig-003.webp\", \"caption\": \"Figure 7. [Cold-Start Latency Reduction] Serving engine initialization latency across models and parallelism configurations on H200. Percentages indicate Foundry’s reduction relative to vLLM with CUDA graphs.\", \"page\": 9, \"index\": 3, \"width\": 1036, \"height\": 192}]"
motivation: CUDA 图捕获耗时远超模型加载，成为 LLM 服务冷启动的主要瓶颈，且现有方案难以兼顾灵活性与效率。
method: 通过离线阶段持久化图拓扑与执行上下文，并结合确定性内存布局与内核提取技术，在在线阶段实现极速的模板化重建。
result: "在多种大模型上实现了高达 99% 的冷启动延迟缩减，将 235B 参数模型的初始化时间从 10 分钟缩短至 3.9 秒。"
conclusion: Foundry 成功解决了 CUDA 图序列化难题，为大规模 LLM 服务的快速扩缩容和并行重构提供了高效的解决方案。
---

## 摘要
现代大语言模型（LLM）服务提供商日益依赖自动扩缩容和并行重配置来应对快速变化的工作负载，但冷启动延迟仍然是一个主要瓶颈。虽然最近的系统已将模型权重加载时间缩短至秒级，但 CUDA 图捕获（CUDA graph capture）仍需数十秒到数分钟，且往往在启动时间中占据主导地位。遗憾的是，CUDA 图无法进行简单的序列化：除了图拓扑结构外，它们还与执行上下文紧密耦合，包括嵌入在算子参数中的设备地址以及在预热期间延迟加载的算子代码。现有方法要么依赖于脆弱的针对特定算子的补丁，要么依赖于重量级的进程级检查点/恢复，这些方法在应对动态并行切换时缺乏灵活性。我们提出了 Foundry，这是一个基于模板的 CUDA 图上下文物化系统，它在离线处理阶段持久化图拓扑和执行上下文，并以极低的开销在线重建可执行图。Foundry 强制执行确定性内存布局，自动提取并重新加载捕获图所需的算子二进制文件，并通过基于拓扑的模板化降低在线重建成本。对于分布式推理，Foundry 进一步支持通过单 GPU 离线捕获来生成多 GPU 部署模板，仅需修补与 rank 相关的通信状态。在参数量高达 235B 的稠密模型和 MoE 模型上，Foundry 将冷启动延迟降低了高达 99%，将 Qwen3-235B-A22B 的初始化时间从 10 分钟缩短至 3.9 秒，同时保留了 CUDA 图带来的吞吐量提升。

## Abstract
Modern LLM service providers increasingly rely on autoscaling and parallelism reconfiguration to respond to rapidly changing workloads, but cold-start latency remains a major bottleneck. While recent systems have reduced model weight loading to seconds, CUDA graph capture still takes tens of seconds to minutes and often dominates startup. Unfortunately, CUDA graphs cannot be naively serialized: beyond graph topology, they are tightly coupled to execution context, including device addresses embedded in kernel arguments and kernel code lazily loaded during warmup. Existing approaches either rely on brittle kernel-specific patching or heavyweight process-level checkpoint/restore that are inflexible to dynamic parallelism switching. We present Foundry, a template-based CUDA graph context materialization system that persists both graph topology and execution context during an offline processing stage, and reconstructs executable graphs online with negligible overhead. Foundry enforces deterministic memory layouts, automatically extracts and reloads kernel binaries required by captured graphs, and reduces online reconstruction costs through topology-based templating. For distributed serving, Foundry further enables a single-GPU offline capture to generate templates for multi-GPU deployments by patching only rank-dependent communication state. Across dense and MoE models up to 235B parameters, Foundry reduces cold-start latency by up to 99%, cutting the initialization time of Qwen3-235B-A22B from 10 minutes to 3.9 seconds while preserving the throughput gains of CUDA graphs.

---

## 论文详细总结（自动生成）

### 论文总结：Foundry —— 基于模板的 CUDA 图上下文物化实现 LLM 快速冷启动

#### 1. 核心问题与研究动机
*   **核心问题**：在大规模大语言模型（LLM）推理服务中，为了应对波动的流量，系统需要频繁进行自动扩缩容和并行策略切换。虽然模型权重的加载已优化至秒级，但 **CUDA 图（CUDA Graph）的捕获过程**（涉及 CPU 端的模型前向逻辑执行和内核拦截）仍需数十秒甚至数分钟，成为冷启动的主要瓶颈。
*   **技术挑战**：CUDA 图无法直接序列化，因为它与执行上下文深度耦合，包括：
    1.  **设备地址依赖**：内核参数中嵌入了特定的 GPU 内存指针。
    2.  **内核加载延迟**：许多内核是在预热阶段延迟加载的，直接恢复会导致函数句柄失效。
*   **研究背景**：现有方案如 Medusa 依赖特定内核的补丁（难以维护），或进程级检查点（开销大且不支持动态并行切换）。

#### 2. 方法论：核心思想与关键技术
Foundry 提出了**上下文物化（Context Materialization）**，通过离线记录和在线重建来消除捕获开销。
*   **确定性内存布局**：通过拦截 CUDA 驱动的虚拟内存管理（VMM）API，强制所有内存分配在预定义的虚拟地址空间内连续进行。这确保了离线捕获时的指针在在线恢复时依然有效，无需修改内核参数。
*   **内核二进制提取与重载**：在离线阶段自动提取所有使用的内核二进制文件（cubin），在线阶段直接加载，跳过了复杂的预热和延迟加载过程。
*   **基于拓扑的模板化（Templating）**：
    *   **内部分组**：发现不同 Batch Size 的图通常具有相同的拓扑结构。Foundry 只为每种拓扑构建一个“模板图”，其他图通过轻量级的参数更新（`cuGraphExecUpdate`）生成。
    *   **分布式共享**：利用单 GPU 模拟分布式通信（Stub 层），捕获通用模板，在线时仅需修补 Rank 相关的通信状态，即可适配多 GPU 环境。

#### 3. 实验设计
*   **场景与模型**：
    *   **稠密模型**：Qwen3 (14B, 32B), Llama3-8B, Gemma3-12B。
    *   **混合专家模型（MoE）**：Qwen3-30B-A3B, Qwen3-235B-A22B。
*   **并行策略**：数据并行（DP1-DP8）、专家并行（EP2-EP8），涵盖 BF16 和 FP8 精度。
*   **Benchmark 与对比方法**：
    *   **vLLM (Vanilla)**：默认的流捕获模式（带图和不带图）。
    *   **CUDA-checkpoint**：NVIDIA 官方的进程级检查点/恢复工具。
    *   **指标**：冷启动延迟（初始化时间）、每输出 Token 耗时（TPOT，用于验证性能保持）。

#### 4. 资源与算力
*   **硬件平台**：
    *   NVIDIA DGX 节点，配备 **8 × H200 GPU**（Intel Xeon 8480C CPU, 2TB 内存）。
    *   NVIDIA DGX 节点，配备 **8 × B200 GPU**（用于验证最新架构的通用性）。
*   **软件环境**：CUDA 13.1, vLLM v0.11.2, PyTorch 2.9。
*   **训练/处理时长**：论文主要关注推理初始化，离线处理（SAVE 阶段）仅需运行一次正常的模型预热和捕获流程。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 8B 到 235B 参数量的多种主流模型架构，测试了 15 种不同的并行配置。
*   **充分性**：实验不仅对比了启动速度，还通过 TPOT 曲线验证了物化后的 CUDA 图在推理性能上与原生捕获完全一致。
*   **客观性**：对比了官方最新的驱动级检查点工具，并在 H200 和 B200 两种架构上进行了交叉验证，实验设计较为全面、客观。

#### 6. 主要结论与发现
*   **极速启动**：Foundry 将冷启动延迟降低了 **95% 至 99%**。
*   **典型案例**：对于超大规模模型 **Qwen3-235B**，初始化时间从 **10 分钟（650秒）缩短至 3.9 秒**。
*   **模板效率**：在 512 个不同 Batch Size 的图中，通常只有 12-25 个独特拓扑，证明了模板化方案的高效性。
*   **存储优势**：Foundry 的归档文件比进程级检查点小 4-5 倍（例如 14B 模型仅需 1.1GB）。

#### 7. 优点
*   **内核无关性**：无需像 Medusa 那样为每个新内核编写补丁规则，具有极强的通用性。
*   **支持复杂架构**：完美支持 MoE 模型和 FP8 算子（如 DeepGEMM, DeepEP）。
*   **分布式友好**：支持单 GPU 捕获、多 GPU 复用，极大地降低了离线处理的硬件成本。
*   **异步优化**：内存预分配机制允许图重建与模型权重加载并行执行。

#### 8. 不足与局限
*   **流水线并行（PP）支持有限**：目前不支持流水线并行，因为不同阶段的拓扑结构不一致（虽然论文辩称 PP 在延迟敏感场景不常用）。
*   **离线处理开销**：虽然是单次投入，但每种新的并行配置或模型更新仍需进行一次离线 SAVE 处理。
*   **内存碎片风险**：强制连续分配可能在极端复杂的动态内存分配场景下产生少量碎片（尽管在 LLM 推理中这种风险较低）。

（完）
