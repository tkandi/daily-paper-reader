---
title: "LP-GEMM: Integrating Layout Propagation into GEMM Operations"
title_zh: LP-GEMM：将布局传播集成到 GEMM 操作中
authors: "César Guedes Carneiro, Lucas Alvarenga, Guido Araujo, Sandro Rigo"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04599v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 针对机器学习工作负载优化 GEMM 算子
tldr: 针对科学计算和机器学习中连续矩阵乘法（GEMM）因BLAS API限制导致的冗余数据打包问题，本文提出LP-GEMM。该方法通过分解GEMM内核，实现了布局传播，消除了中间过程的重复打包与解包，同时保持了边界处的语义正确性。在x86和RISC-V架构上的实验表明，LP-GEMM显著提升了MLP和Attention等负载的性能，在Llama-3.2推理中也表现优异。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04599v1/fig-001.webp\", \"caption\": \"Fig. 4: Micro-kernel Layout.\", \"page\": 6, \"index\": 1, \"width\": 1040, \"height\": 520}, {\"url\": \"assets/figures/arxiv/2604.04599v1/fig-002.webp\", \"caption\": \"Fig. 7: Usage comparison of LP-GEMM with FlashGEMM and OpenBLAS, using benchmarks extracted from DNN [11].\", \"page\": 10, \"index\": 2, \"width\": 457, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2604.04599v1/fig-003.webp\", \"caption\": \"Fig. 2: Visual representation of GotoBLAS approach [10]. (a) shows how matrices are tiled for the architecture’s memory hierarchy, while (b) depicts how each block At and Bt is further tiled and packed for better usage of the processor’s registers during CT computing of µKernel. Finally, (c) shows the final GotoBLAS GEMM algorithm.\", \"page\": 3, \"index\": 3, \"width\": 1029, \"height\": 378}, {\"url\": \"assets/figures/arxiv/2604.04599v1/fig-004.webp\", \"caption\": \"Fig. 5: Speedup of a single GEMM extracted from gemmbench [25] using different state-of-art kernels compared to the three separate LP-GEMM kernels.\", \"page\": 8, \"index\": 4, \"width\": 407, \"height\": 628}, {\"url\": \"assets/figures/arxiv/2604.04599v1/fig-005.webp\", \"caption\": \"Fig. 3: Overview of sequential GEMM operations using different data layouts. The results are the same as using OpenBLAS with column-major or row-major, respectively.\", \"page\": 4, \"index\": 5, \"width\": 390, \"height\": 390}, {\"url\": \"assets/figures/arxiv/2604.04599v1/fig-006.webp\", \"caption\": \"Fig. 6: Speedup of Attention layer from LLaMA 3.2 model, with embedded dimension of 2048, and MLP weights with dimension of 8129. Comparing LP-GEMM and layout propagation to OpenBLAS with no propagation.\", \"page\": 9, \"index\": 6, \"width\": 1058, \"height\": 262}]"
motivation: 传统的BLAS库在处理连续GEMM时，由于API限制必须在每次调用间进行重复的矩阵打包和解包，造成了严重的计算资源浪费。
method: 提出一种GEMM内核分解方法，允许在连续操作间传播打包布局，从而消除不必要的数据重排。
result: 在Intel x86架构上，LP-GEMM相比OpenBLAS实现了平均2.25倍的加速，并能与Intel MKL等厂商优化库竞争。
conclusion: 通过在操作间利用数据布局传播，可以显著提升大模型推理等实际工作负载的执行效率。
---

## 摘要
在科学计算和现代机器学习（ML）工作负载中，相互依赖的通用矩阵乘法（GEMM）序列通常占据了主要的执行时间。尽管最先进的 BLAS 库对单个 GEMM 调用进行了深度优化，但它们仍受限于 BLAS API，该 API 要求每次调用都独立地对输入矩阵进行打包（packing），并将输出恢复为规范的内存布局。在连续的 GEMM 操作中，这些约束导致了冗余的打包和解包，浪费了宝贵的计算资源。本文介绍了 LP-GEMM，这是一种 GEMM 内核的分解方法，能够在连续的 GEMM 操作之间实现打包布局传播（packing-layout propagation）。该方法消除了不必要的数据重新打包，同时在边界处保持了完整的 BLAS 语义正确性。我们在 x86 (AVX-512) 和 RISC-V (RVV 1.0) 架构上，针对类 MLP 和类 Attention 的工作负载评估了 LP-GEMM。结果显示，在 Intel x86 平台上，对于连续 GEMM，LP-GEMM 相比 OpenBLAS 实现了平均 2.25 倍的加速，并且相对于 Intel MKL 等厂商优化库也具有竞争力的性能提升。我们通过仅使用 BLAS 级 GEMM 调用实现 Llama-3.2 推理路径的独立 C++ 版本，证明了该方法在微基准测试之外的实用性。这些结果证实，利用操作之间的数据布局传播可以显著提升性能。

## Abstract
In Scientific Computing and modern Machine Learning (ML) workloads, sequences of dependent General Matrix Multiplications (GEMMs) often dominate execution time. While state-of-the-art BLAS libraries aggressively optimize individual GEMM calls, they remain constrained by the BLAS API, which requires each call to independently pack input matrices and restore outputs to a canonical memory layout. In sequential GEMMs, these constraints cause redundant packing and unpacking, wasting valuable computational resources.   This paper introduces LP-GEMM, a decomposition of the GEMM kernel that enables packing-layout propagation across sequential GEMM operations. This approach eliminates unnecessary data repacking while preserving full BLAS semantic correctness at the boundaries. We evaluate LP-GEMM on x86 (AVX-512) and RISC-V (RVV 1.0) architectures across MLP-like and Attention-like workloads. Our results show average speedups of 2.25x over OpenBLAS on Intel x86 for sequential GEMMs and competitive gains relative to vendor-optimized libraries such as Intel MKL.   We demonstrate the practicality of the approach beyond microbenchmarks by implementing a standalone C++ version of the Llama-3.2 inference path using exclusively BLAS-level GEMM calls. These results confirm that leveraging data layout propagation between operations can significantly boost performance.