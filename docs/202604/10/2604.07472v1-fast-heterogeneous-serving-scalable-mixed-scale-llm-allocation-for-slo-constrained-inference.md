---
title: "Fast Heterogeneous Serving: Scalable Mixed-Scale LLM Allocation for SLO-Constrained Inference"
title_zh: 快速异构推理服务：面向 SLO 约束推理的可扩展混合规模 LLM 分配
authors: "Jiaming Cheng, Duong Tung Nguyen"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07472v1"
tags: ["query:mlsys"]
score: 9.0
evidence: GPU 资源分配与并行配置
tldr: 针对大规模LLM推理部署中模型选择、GPU配置及并行策略的复杂优化问题，本文提出了一种快速异构服务分配框架。由于传统的混合整数线性规划在处理大规模实例时效率低下，研究者设计了贪婪启发式（GH）和自适应贪婪启发式（AGH）算法。这些算法通过张量并行感知、成本效益排序和GPU整合等机制，在满足延迟、精度和预算约束的前提下，实现了亚秒级的快速分配，且在成本上接近最优解，显著提升了大规模推理任务的可扩展性和鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.07472v1/fig-001.webp\", \"caption\": \"Table 2. Stage-2 evaluation under varied budget and penalty settings.\", \"page\": 11, \"index\": 1, \"width\": 775, \"height\": 389}, {\"url\": \"assets/figures/arxiv/2604.07472v1/fig-002.webp\", \"caption\": \"Table 1. Comparison with related approaches across key design dimensions.\", \"page\": 3, \"index\": 2, \"width\": 627, \"height\": 219}, {\"url\": \"assets/figures/arxiv/2604.07472v1/fig-003.webp\", \"caption\": \"Table 4. Rolling-horizon re-optimization: mean±std cost ($) over the 24-hour rental period (30 trials, 288 windows each). Bold marks the lowest mean cost at each σ.\", \"page\": 13, \"index\": 3, \"width\": 725, \"height\": 259}, {\"url\": \"assets/figures/arxiv/2604.07472v1/fig-004.webp\", \"caption\": \"Fig. 2. (a)–(c): Model comparison under delay/error stress. (d)–(f): AGH sensitivity analysis over SLO thresholds ∆i, ϵi, budget δ, and rental cost pc.\", \"page\": 12, \"index\": 4, \"width\": 706, \"height\": 377}]"
motivation: 在大规模LLM推理中，如何在满足严格的延迟、精度和预算约束下，高效地进行模型选择与异构资源分配是一个极具挑战的组合优化问题。
method: 提出了贪婪启发式（GH）和自适应贪婪启发式（AGH），利用张量并行感知、成本效益排序及局部搜索等机制实现快速分配。
result: 实验表明AGH能在亚秒内生成可行解，比精确求解器提速260倍以上，且在成本上接近最优，同时在压力测试中表现出更好的稳定性。
conclusion: 该研究证明了启发式算法在解决受限的大规模异构LLM推理分配问题上，能够完美平衡求解效率、成本优化与系统鲁棒性。
---

## 摘要
大规模部署大语言模型（LLM）推理需要在严格的延迟、准确性和预算约束下，协同进行基础模型选择、异构 GPU 配置、并行性配置以及工作负载分配。精确的混合整数线性规划（MILP）方法虽然能保证最优性，但其可扩展性较差。我们提出了两种约束感知启发式算法：一种是用于单次分配的贪婪启发式算法（GH），另一种是通过多起点构建、基于重定位的局部搜索和 GPU 整合来增强 GH 的自适应贪婪启发式算法（AGH）。三种约束感知机制——张量并行（TP）感知可行性选择、单位有效覆盖成本排名以及 TP 升级——确保了在内存、延迟、误差和预算等紧耦合约束下的可行性。在基于 Azure LLM 推理追踪（2025）校准的工作负载上，两种启发式算法均能在不到一秒的时间内生成可行解，其中 AGH 在大规模实例上实现了超过 260 倍的加速，且成本接近最优。在参数膨胀高达 1.5 倍的样本外压力测试下，AGH 保持了可控的 SLO 违规率和稳定的成本，而精确求解器的部署方案则急剧恶化。

## Abstract
Deploying large language model (LLM) inference at scale requires jointly selecting base models, provisioning heterogeneous GPUs, configuring parallelism, and distributing workloads under tight latency, accuracy, and budget constraints. Exact mixed-integer linear programming (MILP) approaches guarantee optimality but scale poorly. We propose two constraint-aware heuristics: a Greedy Heuristic (GH) for single-pass allocation, and an Adaptive Greedy Heuristic (AGH) that enhances GH via multi-start construction, relocate-based local search, and GPU consolidation. Three constraint-aware mechanisms -- TP-aware feasibility selection, cost-per-effective-coverage ranking, and TP upgrade -- ensure feasibility under tightly coupled memory, delay, error, and budget constraints. On workloads calibrated with the Azure LLM Inference Trace (2025), both heuristics produce feasible solutions in under one second, with AGH closely approaching optimal cost while achieving over 260x speedup on large-scale instances. Under out-of-sample stress tests with up to 1.5x parameter inflation, AGH maintains controlled SLO violations and stable cost, whereas the exact solver's placement degrades sharply.

---

## 论文详细总结（自动生成）

这是一份关于论文《Fast Heterogeneous Serving: Scalable Mixed-Scale LLM Allocation for SLO-Constrained Inference》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）服务的规模化，服务提供商（SP）面临一个复杂的联合优化挑战：如何在满足严格的服务水平目标（SLO，如延迟、精度）和预算约束的前提下，从异构 GPU 资源池中选择基础模型、配置并行策略（张量并行 TP 和流水线并行 PP）并路由工作负载。
*   **研究动机**：现有的精确求解方法（如混合整数线性规划 MILP）虽然能保证最优性，但在处理大规模实例时计算时间呈指数级增长，无法满足实时重调度需求。而现有的启发式方法往往只优化单一维度，忽略了并行配置与负载分配之间的紧耦合关系。

### 2. 方法论：核心思想与算法流程
论文提出了一套从精确建模到快速启发式求解的框架：
*   **数学建模**：将问题建模为 MILP，引入了两阶段延迟模型（捕获 TTFT 和生成延迟），并考虑了内存、计算、存储、误差和预算等多重约束。
*   **核心机制（M1-M3）**：
    *   **M1 (约束感知配置选择)**：为每个候选分配选择满足内存和延迟要求的最小成本 TP/PP 组合。
    *   **M2 (单位有效覆盖成本排名)**：不按原始成本排名，而是按“单位有效服务需求”的边际成本进行排名，优先处理能完全覆盖需求的候选者。
    *   **M3 (并行度升级)**：对于已激活的 GPU，若延迟不达标，优先尝试提升其并行度而非新购资源。
*   **启发式算法**：
    *   **贪婪启发式 (GH)**：两阶段分配。第一阶段确保所有查询类型至少有一个可行配置；第二阶段按需求量降序进行顺序分配。
    *   **自适应贪婪启发式 (AGH)**：在 GH 基础上增加了**多起点构建**（多种排序策略）、**重定位局部搜索**（优化现有分配）和 **GPU 整合**（减少碎片化资源），显著提升了方案质量。

### 3. 实验设计
*   **数据集与场景**：使用 **Azure LLM Inference Trace (2025)** 校准的工作负载。涵盖 6 种查询类型（摘要、代码、翻译等）和 6 种 Llama-3 模型（1B 到 70B）。
*   **硬件环境**：模拟了 10 种异构 GPU 层级（包括 A6000, RTX 4090, A100, H100），涵盖 FP16/INT8/INT4 不同精度。
*   **Benchmark（对比方法）**：
    *   **DM (Deterministic MILP)**：使用 Gurobi 11 求解的精确最优解。
    *   **GH (Greedy Heuristic)**：本文提出的基础启发式。
    *   **AGH (Adaptive Greedy Heuristic)**：本文提出的增强启发式。
*   **测试维度**：成本优化、求解速度、样本外压力测试（参数膨胀）、滚动时界（Rolling-horizon）自适应能力。

### 4. 资源与算力
*   **软件环境**：Python 3.13, Gurobi 11。
*   **算力消耗**：论文主要关注算法本身的计算效率。实验结果显示，在处理大规模实例（如 20x20x20 的规模）时，精确求解器 DM 耗时超过 600 秒（超时），而 GH 耗时小于 1 秒，AGH 耗时约 2.3 秒。
*   **训练时长**：由于本文是关于推理资源分配的优化算法，不涉及模型训练，因此无训练时长数据。

### 5. 实验数量与充分性
*   **实验规模**：进行了 500 组场景模拟，考虑了延迟/误差 ±25% 和到达率 ±20% 的扰动。
*   **消融实验**：专门针对 M1、M2、M3 机制进行了消融分析，证明了 M1 和 M3 是确保“可行性”的前提，而 M2 是优化“成本”的关键。
*   **充分性评价**：实验设计非常充分。不仅对比了静态性能，还通过“滚动时界”模拟了 24 小时内的动态需求波动，验证了算法在实际运营中的鲁棒性和重优化价值。

### 6. 主要结论与发现
*   **高效性**：AGH 在大规模场景下比精确求解器快 **260 倍以上**，且成本非常接近最优解。
*   **鲁棒性**：在 1.5 倍延迟/误差膨胀的压力测试下，AGH 的表现优于精确求解器。这是因为启发式算法内置的约束感知机制提供了天然的“冗余度”，而精确求解器往往因追求极致成本而导致方案在波动面前过于脆弱。
*   **动态优势**：由于 AGH 运行极快，可以每 5 分钟重新优化一次。在需求高波动场景下，这种动态调整比静态最优方案节省了高达 **48%** 的成本。

### 7. 优点（亮点）
*   **联合优化**：首次在启发式框架下实现了模型选择、GPU 供应、TP/PP 配置和路由的四位一体联合优化。
*   **亚秒级响应**：解决了 MILP 无法实时应用的痛点，使大规模 LLM 集群的动态重调度成为可能。
*   **鲁棒性设计**：通过 M1-M3 机制将“可行性”嵌入分配循环，而非事后调整，确保了在极端约束下的解质量。

### 8. 不足与局限
*   **延迟模型简化**：目前的延迟模型主要基于内存带宽和计算量，尚未充分考虑复杂的排队论模型（Queuing Theory）和连续批处理（Continuous Batching）的动态非线性影响。
*   **真实集群验证**：实验主要基于模拟环境和 Azure 追踪数据，缺乏在真实物理 GPU 集群上的端到端部署验证。
*   **随机性风险**：AGH 依赖于多起点随机搜索，虽然在实验中表现稳定，但在极端极端的约束组合下，仍存在无法找到全局最优解的理论风险。

（完）
