---
title: Taming GPU Underutilization via Static Partitioning and Fine-grained CPU Offloading
title_zh: 通过静态分区和细粒度 CPU 卸载解决 GPU 利用率不足问题
authors: "Gabin Schieffer, Ruimin Shi, Jie Ren, Ivy Peng"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08451v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 针对AI和HPC工作负载的GPU资源分区与卸载
tldr: 针对GPU计算与内存资源利用不均衡的问题，本文深入评估了多实例GPU（MIG）在HPC、AI及数据分析应用中的表现。研究发现MIG虽能提升吞吐量和能效，但存在功率限制干扰及资源分配粒度过粗的问题。为此，作者提出一种利用NVLink-C2C互连的细粒度CPU内存卸载方案，旨在弥补静态分区与应用需求间的差距，进一步优化资源利用率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08451v1/fig-001.webp\", \"caption\": \"Fig. 3: GPU memory capacity (upper) and bandwidth (lower) utilization.\", \"page\": 6, \"index\": 1, \"width\": 519, \"height\": 436}, {\"url\": \"assets/figures/arxiv/2604.08451v1/fig-002.webp\", \"caption\": \"Fig. 4: GPU Performance-Resource Scaling for each application on a Nvidia Grace Hopper system.\", \"page\": 7, \"index\": 2, \"width\": 510, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2604.08451v1/fig-003.webp\", \"caption\": \"Fig. 5: System throughput for concurrent execution of seven workload copies, normalized to serial execution baseline.\", \"page\": 8, \"index\": 3, \"width\": 514, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2604.08451v1/fig-004.webp\", \"caption\": \"Fig. 6: Total energy consumed for concurrently running 7 GPU-sharing tasks, normalized to the total energy consumed for seven serial runs of the same application.\", \"page\": 8, \"index\": 4, \"width\": 513, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2604.08451v1/fig-005.webp\", \"caption\": \"Fig. 7: Power consumption and throttling behavior for (a) Qiskit and (b) LLM training. Left: single application on full GPU. Right: concurrent executions on 7×1g MIG partitions. Pink regions indicate power throttling periods.\", \"page\": 9, \"index\": 5, \"width\": 515, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2604.08451v1/fig-006.webp\", \"caption\": \"TABLE IV: Measured Nvlink-C2C bandwidth using cudaMemcpy and direct in-kernel access (GiB/s).\", \"page\": 5, \"index\": 6, \"width\": 448, \"height\": 351}]"
motivation: GPU性能的快速增长使得不同应用特征导致的资源利用不均衡问题愈发严重，亟需评估并改进现有的GPU共享机制。
method: 通过对NekRS、Llama3等真实应用进行系统级特性分析，并提出一种基于NVLink-C2C缓存一致性互连的内存卸载方案。
result: MIG显著降低了资源闲置并提升了系统吞吐量，但也暴露了功率限制导致的干扰以及粗粒度资源配置与应用需求不匹配的问题。
conclusion: 结合静态分区与细粒度CPU内存卸载能有效解决GPU资源错配，是提升异构计算系统整体效率的关键途径。
---

## 摘要
GPU 计算吞吐量和显存容量的提升为广泛的工作负载带来了重大机遇。然而，高效利用这些资源仍然具有挑战性，特别是由于多样化的应用特性可能导致利用率不均衡。多实例 GPU (MIG) 是一种通过将 GPU 计算和显存资源划分为具有隔离性的固定大小切片来提高利用率的有效方法。然而，其在支持高性能计算 (HPC) 工作负载方面的有效性和局限性仍是一个开放性问题。我们利用真实的科学、人工智能和数据分析应用（包括 NekRS、LAMMPS、Llama3 和 Qiskit），对不同的 GPU 共享选项进行了全面的系统级表征。我们的分析表明，虽然通过 MIG 进行 GPU 共享可以显著减少资源利用不足，并实现吞吐量和能效的系统级提升，但通过共享资源（如功率限制）仍会产生干扰。性能-资源扩展结果表明，紧耦合计算和显存资源的粗粒度配置往往与应用需求不匹配。为了解决这种不匹配，我们提出了一种内存卸载方案，该方案利用具有缓存一致性的 NVLink-C2C 互连来弥合粗粒度资源切片之间的差距，并进一步减少资源利用不足。

## Abstract
Advances in GPU compute throughput and memory capacity brings significant opportunities to a wide range of workloads. However, efficiently utilizing these resources remains challenging, particularly because diverse application characteristics may result in imbalanced utilization. Multi-Instance GPU (MIG) is a promising approach to improve utilization by partitioning GPU compute and memory resources into fixed-size slices with isolation. Yet, its effectiveness and limitations in supporting HPC workloads remain an open question. We present a comprehensive system-level characterization of different GPU sharing options using real-world scientific, AI, and data analytics applications, including NekRS, LAMMPS, Llama3, and Qiskit. Our analysis reveals that while GPU sharing via MIG can significantly reduce resource underutilization, and enable system-level improvements in throughput and energy, interference still occurs through shared resources, such as power throttling. Our performance-resource scaling results indicate that coarse-grained provisioning for tightly coupled compute and memory resources often mismatches application needs. To address this mismatch, we propose a memory-offloading scheme that leverages the cache-coherent Nvlink-C2C interconnect to bridge the gap between coarse-grained resource slices and reduce resource underutilization.

---

## 论文详细总结（自动生成）

这是一份关于论文《Taming GPU Underutilization via Static Partitioning and Fine-grained CPU Offloading》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着 GPU 计算吞吐量和显存容量的飞速增长（如从 V100 到 B200），单一应用往往难以完全消耗硬件资源，导致严重的**资源利用不足（Underutilization）**。虽然 NVIDIA 提供了多实例 GPU (MIG) 和多进程服务 (MPS) 等共享技术，但存在以下痛点：
*   **资源错配**：MIG 的资源划分（计算与内存耦合）是粗粒度的且呈倍数增长（如 12GB 直接跳到 24GB），而应用需求往往介于两者之间。
*   **缺乏深度评估**：现有研究多集中在深度学习，缺乏对高性能计算（HPC）和数据分析等多样化负载在 MIG 环境下的系统级表征。
*   **干扰问题**：尽管 MIG 提供了硬件隔离，但功率限制（Power Throttling）等共享资源仍会引起性能波动。

### 2. 论文提出的方法论
论文通过“表征-分析-优化”的思路解决上述问题：
*   **量化分析指标**：定义了 SM 占用率（Occupancy）、显存容量利用率、显存带宽利用率以及计算流水线（FP32/Tensor Core 等）利用率。
*   **NVLink-C2C 细粒度卸载**：针对应用内存需求略微超过 MIG 分区上限的情况，提出利用 Grace Hopper 架构中具有缓存一致性的 **NVLink-C2C** 互连，将部分数据卸载至 CPU 内存。这样应用可以留在较小的 MIG 实例中运行，避免为了少量内存需求而占用更大的计算资源。
*   **奖励模型（Reward Model）**：提出一个多维评估公式 $R = \frac{P/P_{GPU}}{\alpha + W_{MEM} + W_{SM}}$。
    *   $P/P_{GPU}$：相对于全 GPU 运行的性能比。
    *   $W_{MEM}$ 和 $W_{SM}$：显存和计算资源的浪费率。
    *   $\alpha$：策略参数，用于平衡“性能优先”或“利用率优先”。

### 3. 实验设计
*   **应用场景（Workloads）**：涵盖了 8 类真实世界应用：
    *   **科学计算**：NekRS（流体动力学）、LAMMPS（分子动力学）、AutoDock-GPU（分子对接）。
    *   **人工智能**：Llama3（推理）、GPT2（训练）。
    *   **数据分析与量子**：FAISS（向量搜索）、Qiskit（量子模拟）、Rodinia（微分方程）。
*   **Benchmark 与对比方法**：
    *   **对比技术**：MIG（不同切片配置）、MPS（多进程服务）、Time-slicing（时间片轮转）、Serial（串行执行）。
    *   **基准测试**：STREAM-GPU 和 STREAM-NVLink（用于测量带宽极限）。

### 4. 资源与算力
*   **硬件平台**：NVIDIA **Grace Hopper** 系统。
    *   **GPU**：H100 (96GB HBM3, 132个 SM)。
    *   **CPU**：72核 Arm Neoverse-V2。
    *   **互连**：NVLink-C2C（双向 450 GB/s）。
    *   **内存**：512GB LPDDR5X。
*   **软件环境**：CUDA 12.4, NVML 采样频率设为 20ms（功率）和 200ms（其他指标）。

### 5. 实验数量与充分性
*   **实验规模**：论文对 8 种应用在 5 种以上不同的 MIG 配置及 MPS/时间片模式下进行了详尽测试。
*   **充分性**：实验不仅关注性能（吞吐量），还深入分析了能效（Energy）、功率限制（Throttling）以及 NVLink-C2C 的实际带宽表现。
*   **客观性**：通过直接调用 NVML GPM 采样硬件计数器，避免了传统 Profiling 工具带来的高开销干扰，数据较为客观。

### 6. 主要结论与发现
*   **MIG 的优劣**：MIG 能显著提升系统总吞吐量（平均 1.4x）并降低能耗（平均节省 26%），但在带宽敏感型任务中，严格的硬件隔离可能导致性能低于 MPS。
*   **功率干扰**：即使在 MIG 硬件隔离下，多个实例并发运行仍会触发 GPU 总功率限制，导致频率下降（Power Throttling），这是 MIG 无法完全隔离的副作用。
*   **卸载方案的有效性**：通过 NVLink-C2C 卸载，Qiskit 和 Llama3 等应用能在更小的 MIG 实例中运行，虽然单任务性能受损，但显著提高了整机资源的填充率和奖励分值。
*   **资源错配普遍存在**：大多数 HPC 应用在全 GPU 模式下 SM 占用率极低（如 NekRS 仅 12%），证明了静态分区的必要性。

### 7. 优点
*   **跨领域覆盖**：不局限于 AI，对 HPC 应用在现代 GPU 架构上的表现提供了宝贵的数据支撑。
*   **软硬结合**：利用了 Grace Hopper 特有的 NVLink-C2C 硬件特性来解决软件层面的资源调度难题。
*   **实用性强**：提出的奖励模型为数据中心管理员在选择 MIG 配置时提供了量化的决策依据。

### 8. 不足与局限
*   **硬件依赖**：内存卸载方案高度依赖 NVLink-C2C 的高带宽和缓存一致性，在传统的 PCIe 连接系统上可能因延迟过高而失效。
*   **静态配置限制**：研究仍基于静态 MIG 分区，未涉及动态重构（Dynamic Reconfiguration）的开销分析。
*   **应用修改成本**：虽然使用了托管内存（Unified Memory），但对于某些复杂应用，实现高效的细粒度内存卸载仍需手动调整代码。

（完）
