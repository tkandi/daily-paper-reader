---
title: "GENSERVE: Efficient Co-Serving of Heterogeneous Diffusion Model Workloads"
title_zh: GENSERVE：异构扩散模型工作负载的高效协同服务
authors: "Fanjiang Ye, Zhangke Li, Xinrui Zhong, Ethan Ma, Russell Chen, Kaijian Wang, Jingwei Zuo, Desen Sun, Ye Cao, Triston Cao, Myungjin Lee, Arvind Krishnamurthy, Yuke Wang"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04335v2"
tags: ["query:mlsys"]
score: 9.0
evidence: 在共享GPU集群上协同调度异构扩散模型工作负载
tldr: GENSERVE是一个专为文本到图像（T2I）和文本到视频（T2V）扩散模型设计的异构协同推理系统。针对两者在计算需求和延迟要求上的巨大差异，GENSERVE利用扩散过程离散且可预测的特性，引入了步级资源自适应机制，包括智能视频抢占、弹性序列并行与动态批处理以及SLO感知调度器，显著提升了共享GPU集群的资源利用率和任务成功率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04335v2/fig-001.webp\", \"caption\": \"Figure 7: System overview of GENSERVE. Incoming requests are admitted by the Scheduler, which maintains cluster states and queries the Profiler for offline latency estimates to invoke the Solver for joint optimization. The resulting decisions are passed to the Allocator and the Controller, which dispatches tasks and preemption signals to Workers. Workers execute inference on assigned GPUs and report step-level progress back through the Result Queue.\", \"page\": 6, \"index\": 1, \"width\": 494, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2604.04335v2/fig-002.webp\", \"caption\": \"Figure 1: Serving 4 videos (V1–V4) and 3 images (I1–I3) on 4 GPUs. (a) FIFO: videos occupy all GPUs until completion; images must wait, causing HOL blocking and 5 deadline misses (SLO 2/7). (b) GENSERVE: when I1, I2 arrive, the scheduler preempts V3 and V4 at step boundaries, batches I1+I2 on GPU 3, and serves I3 on GPU 2. After images complete, V3 and V4 resume with SP degree switching (each scaling to 2 GPUs) to recover lost slack. All 7 requests meet their deadlines (SLO 7/7).\", \"page\": 2, \"index\": 2, \"width\": 493, \"height\": 458}, {\"url\": \"assets/figures/arxiv/2604.04335v2/fig-003.webp\", \"caption\": \"Table 4: Key notation in the §4.4.\", \"page\": 8, \"index\": 3, \"width\": 482, \"height\": 324}]"
motivation: 现有的推理系统在同时处理计算需求和并行特性差异巨大的T2I与T2V异构负载时，难以满足严格的延迟服务水平目标（SLO）。
method: 利用扩散推理步级可预测且易抢占的特点，通过智能视频抢占、弹性序列并行及SLO感知调度器实现细粒度的资源动态管理。
result: "实验结果显示，GENSERVE在多种异构负载配置下，相比现有最强基准系统将SLO达成率提升了高达44%。"
conclusion: 通过在步级粒度上优化资源分配和调度，GENSERVE有效解决了异构扩散模型在生产环境中的高效协同推理难题。
---

## 摘要
扩散模型已成为文本生成图像 (T2I) 和文本生成视频 (T2V) 的主流方法，然而，生产平台日益需要在共享 GPU 集群上同时提供这两种模态的服务，并满足严格的延迟服务水平目标 (SLO)。协同服务此类异构工作负载具有挑战性：T2I 和 T2V 请求在计算需求、并行特性和延迟要求方面存在巨大差异，导致现有服务系统出现严重的 SLO 违规。我们提出了 GENSERVE，这是一个利用扩散过程固有的可预测性来优化服务效率的协同服务系统。核心见解是，扩散推理以离散、可预测的步骤进行，并且在步骤边界处具有天然的可抢占性，这为异构感知的资源管理开辟了新的设计空间。GENSERVE 通过三种协调机制引入了步级资源自适应：智能视频抢占、带动态批处理的弹性序列并行，以及一个联合优化所有并发请求资源分配的 SLO 感知调度器。实验结果表明，在各种配置下，GENSERVE 相比最强的基准方案，将 SLO 达成率提高了多达 44%。

## Abstract
Diffusion models have emerged as the prevailing approach for text-to-image (T2I) and text-to-video (T2V) generation, yet production platforms must increasingly serve both modalities on shared GPU clusters while meeting stringent latency SLOs. Co-serving such heterogeneous workloads is challenging: T2I and T2V requests exhibit vastly different compute demands, parallelism characteristics, and latency requirements, leading to significant SLO violations in existing serving systems. We present GENSERVE, a co-serving system that leverages the inherent predictability of the diffusion process to optimize serving efficiency. A central insight is that diffusion inference proceeds in discrete, predictable steps and is naturally preemptible at step boundaries, opening a new design space for heterogeneity-aware resource management. GENSERVE introduces step-level resource adaptation through three coordinated mechanisms: intelligent video preemption, elastic sequence parallelism with dynamic batching, and an SLO-aware scheduler that jointly optimizes resource allocation across all concurrent requests. Experimental results show that GENSERVE improves the SLO attainment rate by up to 44% over the strongest baseline across diverse configurations.

---

## 论文详细总结（自动生成）

这是一份关于论文《GENSERVE: Efficient Co-Serving of Heterogeneous Diffusion Model Workloads》的结构化深度总结：

### 1. 核心问题与研究动机
*   **研究背景**：随着扩散模型（Diffusion Models）在文本生成图像（T2I）和文本生成视频（T2V）领域的爆发，生产平台需要在共享 GPU 集群上同时处理这两类任务。
*   **核心挑战**：T2I 和 T2V 负载具有极高的**异构性**。T2V 的计算量比 T2I 高出几个数量级（单步成本可达 20 倍差距），且两者延迟要求不同。
*   **现有系统不足**：
    *   **FIFO（先来先服务）**：长运行的视频请求会导致严重的“队头阻塞”（HOL Blocking），使图像请求超时。
    *   **SJF（短作业优先）**：图像请求始终优先，导致视频请求饥饿并错过截止日期。
    *   **静态配置**：现有系统通常为单一任务优化，无法根据实时负载动态调整批处理（Batching）和并行度（Parallelism）。

### 2. 方法论与关键技术
GENSERVE 的核心思想是利用扩散推理**步级可预测**和**天然可抢占**的特性，将反应式调度转变为主动优化。

*   **智能视频抢占（Intelligent Video Preemption）**：
    *   利用扩散模型每一步计算成本固定的特性，计算视频请求的“松弛时间”（Slack）。
    *   当紧急图像请求到达时，抢占松弛时间最长的视频任务。由于扩散模型中间状态（Latent）很小（约几十 MB），抢占和恢复的开销极低（毫秒级）。
*   **弹性资源分配（Elastic Resource Allocation）**：
    *   **弹性序列并行（Elastic SP）**：针对视频任务，根据分辨率动态调整序列并行度。高分辨率视频增加并行度以降低延迟，低负载时降低并行度以节省资源。
    *   **SLO 感知动态批处理**：对相同分辨率的图像请求进行批处理，但前提是批处理后的延迟不会导致任何请求违反 SLO。
*   **SLO 感知在线调度器（SLO-aware Scheduler）**：
    *   将资源分配建模为一个类似“背包问题”的动态规划（DP）问题。
    *   在每个调度轮次（步边界），求解最大化全局 SLO 达成率的分配方案，决定哪些视频继续、抢占或调整并行度，以及图像如何批处理。

### 3. 实验设计
*   **模型与数据集**：
    *   **T2I**：Stable Diffusion 3.5 Medium (2.5B)，使用 DiffusionDB 提示词。
    *   **T2V**：Wan2.2-T2V-5B (5B)，使用 VBench 提示词。
*   **对比基准（Benchmarks）**：
    *   **FCFS**：先来先服务。
    *   **SJF**：短作业优先。
    *   **SRTF**：最短剩余时间优先（带抢占）。
    *   **RASP**：分辨率感知的静态序列并行。
*   **测试场景**：涵盖了泊松到达和突发（Bursty）到达模式，以及不同比例的图像/视频混合负载（轻量、平衡、重量级）。

### 4. 资源与算力
*   **硬件环境**：单台服务器，配备 **8 × NVIDIA RTX PRO 6000 Blackwell GPUs** (每张显卡 96 GB GDDR7 显存)。
*   **连接方式**：PCIe Gen5 ×16，使用 NCCL 进行通信。
*   **软件框架**：基于 PyTorch 2.8 和 Hugging Face Diffusers 构建，集成 xFuser 实现序列并行。
*   **训练时长**：本文侧重于推理服务（Serving），未涉及模型训练时长。

### 5. 实验数量与充分性
*   **实验规模**：进行了多维度的评估，包括 SLO 严格程度（σ 从 0.8 到 1.3）、负载比例、到达率（12-36 req/min）以及端到端延迟分布。
*   **消融实验**：详细拆解了抢占机制、DP 调度器和弹性 SP 切换各自带来的增益。
*   **敏感性分析**：测试了分辨率倾斜分布、调度器计算开销、抢占开销及显存占用。
*   **评价**：实验设计全面且客观，考虑了生产环境中的多种极端情况（如突发流量和分辨率分布不均），对比基准涵盖了主流调度策略，具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：在各种配置下，GENSERVE 的 SLO 达成率比最强基准（SRTF）提升了多达 **44%**。
*   **调度开销极低**：DP 调度器的决策时间小于 2 毫秒，仅占 720p 视频单步计算时间的 0.25% 以下。
*   **抢占成本可忽略**：视频抢占的恢复开销仅占单步时间的 0.1% 左右，显存占用仅增加约 27MB。
*   **协同服务优于隔离**：在共享集群上动态分配资源（Replicated Co-serving）比将 GPU 静态划分为图像池和视频池（Dedicated Partitioning）效果更好，有效避免了资源碎片化。

### 7. 优点与亮点
*   **领域洞察深刻**：敏锐捕捉到扩散模型与 LLM 在推理特性上的本质区别（步级确定性 vs 长度不确定性），并以此构建调度逻辑。
*   **机制协同高效**：将抢占、并行度调整和批处理有机结合，通过轻量级 DP 算法实现了全局最优。
*   **实用性强**：系统开销极低，且能显著改善混合负载下的用户体验（尤其是保护了对延迟敏感的图像请求）。

### 8. 不足与局限
*   **硬件范围**：实验主要在单机 8 卡 Blackwell 环境下完成，尚未验证在跨节点的大规模分布式集群上的扩展性和通信瓶颈。
*   **模型架构限制**：虽然 DiT 是主流，但对于非 Transformer 架构（如纯 U-Net）的适配性及并行收益未做深入探讨。
*   **视频尾部延迟**：为了保证整体 SLO，极少数视频请求可能会经历多次抢占，导致其绝对完成时间被拉长（虽然这符合 SLO 优先的原则，但对特定用户可能有影响）。

（完）
