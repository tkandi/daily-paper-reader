---
title: "STQuant: Spatio-Temporal Adaptive Framework for Optimizer Quantization in Large Multimodal Model Training"
title_zh: STQuant：大语言多模态模型训练中优化器量化的时空自适应框架
authors: "Minglu Liu, Cunchen Hu, Liangliang Xu, Fengming Tang, Ruijia Wang, Fu Yu"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06836v2"
tags: ["query:mlsys"]
score: 9.0
evidence: 带有优化器量化的分布式训练框架
tldr: 针对大规模多模态模型训练中优化器状态占用内存过高的问题，本文提出STQuant框架。该框架打破了传统固定精度的局限，通过在空间（层间、变量间）和时间（训练步数）维度上动态分配量化精度，在保证模型质量的同时显著降低内存。其核心贡献包括近优因子选择策略和线性复杂度的动态决策算法，实现了极低位宽的优化器状态存储。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06836v2/fig-001.webp\", \"caption\": \"Figure 1: Spatiotemporal evolution and correlation analysis of gradients and Adam optimizer states. (a) Sharpness of the gradient distribution (CV) across 48 core weight layers in 12 Transformer blocks, including QKV projections, attention projections, MLP expansion layers, and MLP compression layers, sampled every 50 training steps over 5000 total steps; (b) Correlation between gradients and the coefficient of variation (CV) of the first-order moment𝑚, showing a very strong linear relationship (Pearson 𝑅 = 0.9777); (c) Correlation between gradients and the second-order moment 𝑣 , where, despite the squaring operation introducing nonlinear mapping and noise amplification, the correlation remains strong (𝑅 = 0.7169).\", \"page\": 2, \"index\": 1, \"width\": 994, \"height\": 276}, {\"url\": \"assets/figures/arxiv/2604.06836v2/fig-002.webp\", \"caption\": \"Figure 6: Bit-width evolution of STQuant during pre-training. (a) shows the dynamic bit-width allocation across layers over training epochs, and (b) presents the bit-width evolution of different parameter groups within a Transformer block.\", \"page\": 7, \"index\": 2, \"width\": 1051, \"height\": 324}, {\"url\": \"assets/figures/arxiv/2604.06836v2/fig-003.webp\", \"caption\": \"Figure 2: Overview of the STQuant framework: The system consists of three collaborative engines: (1) the Score Engine extracts spatio-temporal gradient features across GPUs; (2) the Distributed Engine synchronizes global statistics to determine optimal bit-widths; and (3) the Quantization Engine executes dual-mode block-wise compression (for𝑚 and 𝑣).\", \"page\": 3, \"index\": 3, \"width\": 494, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2604.06836v2/fig-004.webp\", \"caption\": \"Table 3: Performance and optimizer-state memory savings of STQuant AdamW compared with full-precision and 8-bit AdamW(bnb) baselines across core fine-tuning tasks.\", \"page\": 8, \"index\": 4, \"width\": 1036, \"height\": 397}]"
motivation: 现有的固定精度量化方法忽略了优化器状态在不同层和训练阶段的分布差异，导致模型精度下降且内存利用率非最优。
method: 提出一种时空自适应框架，利用近优因子选择策略识别关键影响因素，并通过动态转换决策算法将搜索复杂度从指数级降至线性。
result: "在GPT-2和ViT上的实验表明，STQuant将优化器状态内存减少了84.4%，平均位宽降至5.1比特，且计算和空间开销极低。"
conclusion: STQuant通过精细化的动态精度管理，为大规模模型训练提供了一种高效且保质的优化器状态压缩方案。
---

## 摘要
量化是降低大规模模型训练内存开销的有效方法。然而，现有的大多数方法采用固定精度策略，忽略了优化器状态分布在不同层和训练步骤之间存在显著差异的事实。这种统一的设计往往会导致明显的精度下降。为了超越固定量化，我们提出了 STQuant，这是一个分布式训练框架，通过在层、状态变量和训练步骤之间进行动态精度分配，在保持模型质量的同时减少了优化器状态的内存占用。在训练过程中直接应用动态量化面临两个挑战

## Abstract
Quantization is an effective way to reduce the memory cost of large-scale model training. However, most existing methods adopt fixed-precision policies, which ignore the fact that optimizer-state distributions vary significantly across layers and training steps. Such uniform designs often introduce noticeable accuracy degradation. To move beyond fixed quantization, we propose STQuant, a distributed training framework that reduces the memory footprint of optimizer states via dynamic precision allocation across layers, state variables, and training steps, while maintaining model quality. Naively applying dynamic quantization during training is challenging for two reasons. First, optimizer states are numerically sensitive, and quantization noise can destabilize quality. Second, jointly considering multiple states and layers induces a large combinatorial search space. STQuant addresses these challenges with two key techniques: 1) a provably near-optimal factor selection strategy that accurately identifies the most influential factors for precision adaptation. 2) a dynamic transition decision algorithm that reduces the search cost from exponential to linear complexity. Experiments on GPT-2 and ViT show that STQuant reduces optimizer-state memory by 84.4%, achieving an average bit-width of as low as 5.1 bits, compared with existing solutions. Moreover, STQuant incurs only O(N/K) computational overhead and requires O(1) extra space.

---

## 论文详细总结（自动生成）

这是一份关于论文《STQuant: Spatio-Temporal Adaptive Framework for Optimizer Quantization in Large Multimodal Model Training》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在大规模基础模型（如 GPT-4、ViT）的训练中，优化器状态（如 Adam 的一阶矩 $m$ 和二阶矩 $v$）占用的内存通常是模型权重的 4 到 12 倍，成为训练扩展的主要瓶颈。
*   **研究动机**：现有的量化方法（如 8-bit Adam）多采用**固定精度**策略，忽略了优化器状态在不同神经网络层（空间维度）和不同训练阶段（时间维度）的分布差异。
*   **核心发现**：作者通过实验证明，梯度分布的尖锐度与优化器状态之间存在极强的时空相关性，这为动态调整量化精度提供了理论依据。

### 2. 论文提出的方法论：STQuant 框架
STQuant 是一个分布式训练框架，旨在通过时空自适应的精度分配来最小化内存占用。其核心由三个引擎组成：
*   **评分引擎 (Score Engine)**：
    *   **双因子代理**：定义强度因子 $n$（RMS 梯度大小）和变化因子 $r$（变异系数 CV），作为 Hessian 矩阵灵敏度的轻量级代理。
    *   **时间退火因子 ($S_t$)**：使用双曲正割函数（sech）在训练初期提供高精度保护，防止冷启动时的数值不稳定。
    *   **层级特征因子**：评估各层相对于全局平均水平的重要性。
*   **分布式引擎 (Distributed Engine)**：
    *   将上述因子整合进一个基于对数的统一评分函数，将复杂的组合搜索空间简化为线性复杂度的决策过程。
    *   根据得分将各层动态映射到不同的位宽（4、8、16 或 32-bit）。
*   **量化引擎 (Quantization Engine)**：
    *   **双模块量化**：对一阶矩 $m$ 采用线性对称量化（保持方向信息），对二阶矩 $v$ 采用对数量化（处理极大的数值范围）。
    *   采用分块（Block-wise）策略以抑制离群值的影响。

### 3. 实验设计
*   **数据集与场景**：
    *   **预训练**：OpenWebText (GPT-2 XL), COCO 2017 (ViT-Base)。
    *   **微调**：MNLI (RoBERTa-Large), COCO 2017 (ViT 分类/检索), Wikitext-103 (GPT-2 Medium)。
*   **Benchmark 与对比方法**：
    *   基准：32-bit AdamW（全精度）。
    *   对比：bitsandbytes 提供的工业级 8-bit AdamW。
*   **评估指标**：训练损失（Loss）、Top-1 准确率、mAP、Perplexity (PPL)、Recall@1 以及优化器状态内存节省率。

### 4. 资源与算力
*   **硬件**：使用 NVIDIA A800 (80GB) GPU。
*   **配置**：
    *   预训练任务使用 4 张 GPU。
    *   微调和消融实验使用单张 GPU。
    *   所有实验均在 FP16 混合精度训练环境下进行。
*   **开销**：STQuant 仅引入 $O(N/K)$ 的计算开销和 $O(1)$ 的额外空间开销，决策过程相对于总训练时间几乎可以忽略。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从数亿到数十亿参数的模型，跨越了自然语言处理（NLP）、计算机视觉（CV）和跨模态检索三大领域。
*   **消融实验**：专门针对双因子、时间因子和空间因子进行了拆解实验，验证了各组件对最终性能和压缩率的贡献。
*   **充分性评价**：实验设计较为全面，不仅关注训练曲线的收敛性，还通过线性探测（Linear Probing）验证了预训练表示的质量，确保了结果的客观性。

### 6. 主要结论与发现
*   **内存节省**：相比全精度 AdamW，STQuant 将优化器状态内存减少了约 **84.4%**；相比 8-bit AdamW，进一步减少了约 36%-38% 的内存。
*   **极低位宽**：在保持模型性能不下降的前提下，实现了低至 **5.1 bits** 的平均位宽。
*   **性能保持**：在所有下游任务中，STQuant 的表现均与全精度基准持平，甚至在某些多模态检索任务中略有胜出。

### 7. 优点与亮点
*   **动态性**：打破了固定位宽的传统，实现了层级、步骤和变量级别的精细化控制。
*   **高效决策**：通过启发式评分机制，将原本指数级的搜索空间降为线性，使其具备在大规模生产环境中部署的可行性。
*   **跨模态通用性**：在视觉和语言模型上均表现出极强的鲁棒性。

### 8. 不足与局限
*   **硬件原生支持**：虽然内存占用显著降低，但目前主流硬件对非标准位宽（如 5-bit 或 6-bit）的计算加速支持有限，实际训练速度的提升可能不如内存节省那样显著。
*   **超参数依赖**：评分函数中的阈值（如 6.8, 12, 24）是基于特定模型分布设定的，对于全新的架构可能需要重新校准。
*   **实验覆盖**：虽然涵盖了多模态，但尚未在万亿级（Trillion-parameter）参数规模或超长序列任务上进行验证。

（完）
