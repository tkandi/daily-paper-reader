---
title: "ARGUS: Agentic GPU Optimization Guided by Data-Flow Invariants"
title_zh: ARGUS：由数据流不变式引导的智能体 GPU 优化
authors: "Haohui Mai, Xiaoyan Guo, Xiangyun Ding, Daifeng Li, Qiuchu Yu, Chenzhun Guo, Cong Wang, Jiacheng Zhao, Christos Kozyrakis, Binhang Yuan"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.18616v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 用于GPU算子优化和指令调度的智能体框架
tldr: Argus是一个针对GPU内核优化的智能体框架，旨在解决LLM生成的代码性能远低于手工优化库的问题。它引入了基于数据流不变量的编译时规范和Pythonic DSL，通过符号标注和约束强制执行提供结构化反馈。结合上下文强化学习规划器，Argus在AMD GPU上实现了与顶级手工优化库相当的性能，显著超越了现有智能体系统，为高性能计算自动化提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18616v1/fig-001.webp\", \"caption\": \"Figure 2. Ablation of optimizations on flash attention kernel.\", \"page\": 11, \"index\": 1, \"width\": 473, \"height\": 259}, {\"url\": \"assets/figures/arxiv/2604.18616v1/fig-002.webp\", \"caption\": \"Table 2. Effective throughputs (TFLOPS) and speedups relative to Argus for GEMM, flash attention, and fused MoE kernels. Speedups are geometric means over the five configurations of each workload family.\", \"page\": 10, \"index\": 2, \"width\": 832, \"height\": 711}, {\"url\": \"assets/figures/arxiv/2604.18616v1/fig-003.webp\", \"caption\": \"Table 1. GEMM optimizations across Argus, hand-optimized libraries and multiple kernel generation frameworks. ✓, -, and ✕ denote implemented, absent, and incorrectly implemented optimizations.\", \"page\": 9, \"index\": 3, \"width\": 932, \"height\": 477}]"
motivation: 现有LLM智能体在优化GPU内核时缺乏对硬件约束的全局理解，导致生成的代码性能远低于专家级手工优化水平。
method: 引入数据流不变量和Pythonic DSL，通过编译时验证提供具体反例反馈，并利用强化学习规划器指导内核优化。
result: "生成的内核在AMD MI300X上达到了手工优化汇编代码99-104%的性能，且在KernelBench任务中表现优异。"
conclusion: 结合形式化验证与智能体规划，Argus成功实现了高性能GPU内核的自动化生成，填补了AI生成代码与极致性能之间的鸿沟。
---

## 摘要
基于大语言模型（LLM）的代码智能体能够生成功能正确的 GPU 核函数，但在矩阵乘法、注意力机制和混合专家模型（MoE）等关键计算任务上，其性能仍远低于手工优化的库。GPU 的峰值性能需要对紧密耦合的优化手段进行协同推理，包括分块（tiling）、共享内存暂存（shared-memory staging）、软件流水线（software pipelining）和指令调度（instruction scheduling），而现有的智能体依赖于稀疏的通过/失败反馈，无法诊断全局约束冲突。我们提出了 Argus，这是一个通过数据流不变式（data-flow invariants）解决上述问题的智能体框架：数据流不变式是编码数据在核函数执行过程中如何编排的编译时规范。Argus 引入了一种基于分块的类 Python 领域特定语言（DSL），在隐藏底层表示的同时暴露硬件指令和编译器策略。该 DSL 提供标签函数以通过数据流和控制流传播符号注释，并提供标签断言以在调用处强制执行关系约束。当发生冲突时，编译器会返回具体的反例，识别出线程、数据元素和程序点，从而为针对性修复提供密集的结构化反馈。不变式在编译时通过布局代数上的抽象解释和 SMT 求解进行验证，具有零运行时开销。一个上下文内强化学习规划器在精选的 GPU 优化技术知识库支持下，学习选择优化方案并合成有效的不变式。我们在 AMD MI300X GPU 上评估了 Argus，涵盖了占 LLM 推理 GPU 时间 90% 以上的 GEMM、Flash Attention 和 MoE 核函数。生成的核函数达到了最先进的手工优化汇编吞吐量的 99-104%，且比现有的智能体系统快 2-1543 倍。Argus 进一步推广到 200 个 KernelBench 任务，解决了 100% 的 1 级问题和 90% 的 2 级问题。

## Abstract
LLM-based coding agents can generate functionally correct GPU kernels, yet their performance remains far below hand-optimized libraries on critical computations such as matrix multiplication, attention, and Mixture-of-Experts (MoE). Peak GPU performance requires coordinated reasoning over tightly coupled optimizations, including tiling, shared-memory staging, software pipelining, and instruction scheduling, while existing agents rely on sparse pass/fail feedback, leaving them unable to diagnose global constraint violations.   We present Argus, an agentic framework that addresses this through data-flow invariants: compile-time specifications encoding how data must be choreographed throughout kernel execution. Argus introduces a tile-based, Pythonic DSL exposing hardware instructions and compiler policies while hiding low-level representations. The DSL provides tag functions to propagate symbolic annotations through data and control flow, and tag assertions to enforce relational constraints at use sites. When violations occur, the compiler returns concrete counterexamples identifying the thread, data element, and program point, enabling dense, structured feedback for targeted fixes. Invariants are verified at compile time via abstract interpretation over a layout algebra and SMT solving, with zero runtime overhead. An in-context reinforcement learning planner learns to select optimizations and synthesize effective invariants, supported by a curated knowledge base of GPU optimization techniques.   We evaluate Argus on the AMD MI300X GPU across GEMM, flash attention, and MoE kernels accounting for over 90% of GPU time in LLM inference. Generated kernels achieve 99-104% of state-of-the-art hand-optimized assembly throughput and are 2-1543x faster than existing agentic systems. Argus further generalizes to 200 KernelBench tasks, solving 100% of Level 1 and 90% of Level 2 problems.

---

## 论文详细总结（自动生成）

### ARGUS: 由数据流不变式引导的智能体 GPU 优化论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管基于大语言模型（LLM）的编程智能体能生成功能正确的 GPU 核函数，但其性能远低于专家手工优化的库（如在 AMD MI300X 上比最先进库慢 2-600 倍）。
*   **研究动机**：GPU 峰值性能依赖于分块、共享内存暂存、软件流水线和指令调度等复杂技术的协同。现有智能体仅依赖单元测试的“通过/失败”反馈，缺乏对全局数据流约束的理解，无法诊断复杂的性能瓶颈或细微的正确性错误。
*   **整体含义**：论文提出了 **Argus** 框架，通过引入“数据流不变式”为智能体提供密集的结构化反馈，弥补了自动化生成与极致性能之间的鸿沟。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：利用编译时规范（数据流不变式）来描述数据在执行过程中的编排方式，并通过静态分析为 LLM 提供具体的错误反例（如哪个线程、哪个数据点出错）。
*   **关键技术细节**：
    *   **Argus DSL**：一种基于分块（Tile-based）的 Pythonic 领域特定语言。它引入了**标签函数（Tag Functions）**为张量元素附加符号注释，以及**标签断言（Tag Assertions）**来强制执行数据间的关系约束（如矩阵乘法的操作数必须匹配）。
    *   **静态分析验证**：编译器在编译时通过布局代数上的抽象解释和 SMT 求解器（Z3）验证不变式。该过程具有**零运行时开销**，且能生成精确的违规报告。
    *   **智能体优化环路**：采用**上下文强化学习（ICRL）**。
        *   **知识库**：存储专家策划的 GPU 优化技巧。
        *   **规划器**：根据编译器反馈和性能分析，从知识库选择优化方案并合成不变式。
        *   **文本梯度更新**：利用 TextGrad 技术，根据奖励信号（性能和不变式违规情况）更新规划器的提示词（Prompt）。

#### 3. 实验设计：场景、Benchmark 与对比方法
*   **实验场景**：针对 LLM 推理中占据 90% 以上 GPU 时间的三大核心算子：
    1.  **GEMM**（通用矩阵乘法）：不同规模的 bf16 矩阵。
    2.  **Flash Attention**（闪速注意力）：GQA 架构，不同序列长度。
    3.  **Fused MoE**（融合混合专家模型）：fp8 块量化配置。
*   **对比基准（Baselines）**：
    *   **手工优化库**：HipBlasLt, AITER, Triton, HipKittens。
    *   **智能体框架**：KernelBench, KSearch, CUDAForge, KernelFalcon。
*   **泛化性测试**：在 **KernelBench** 的 200 个任务（Level 1 & 2）上进行评估。

#### 4. 资源与算力
*   **硬件环境**：使用了配备 **8 个 AMD Instinct MI300X GPU** 的服务器，每个 GPU 拥有 192 GB HBM3 显存。CPU 为两颗 64 核 AMD EPYC 9554。
*   **训练/运行时间**：论文未明确给出 ICRL 策略更新的总时长，但提到每个任务进行 10 次迭代优化。编译器和 SMT 求解在编译阶段完成，对运行性能无负面影响。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 类生产级算子（多种参数组合）和 200 个通用算子任务。
*   **消融实验**：针对 Flash Attention 进行了详细的消融实验，量化了异步拷贝、银行冲突消除、软件流水线和指令调度等 6 个层级的优化贡献。
*   **客观性**：对比了当前最强的开源智能体和厂商级闭源库，实验设计较为全面，涵盖了从高层算法到低层 ISA 指令的各个维度。

#### 6. 主要结论与发现
*   **性能卓越**：Argus 生成的核函数达到了手工优化汇编库 **99%–104%** 的吞吐量。
*   **大幅领先智能体**：在几何平均吞吐量上，比现有智能体系统快 **2–1543 倍**。
*   **泛化能力强**：成功解决了 100% 的 KernelBench Level 1 任务和 90% 的 Level 2 任务。
*   **反馈有效性**：数据流不变式显著提升了 Pass@1 成功率（Level 1 从 60% 提升至 75%），并减少了优化所需的 Token 消耗。

#### 7. 优点：亮点与创新
*   **密集的结构化反馈**：改变了以往智能体“盲目试错”的模式，通过 SMT 求解器提供的精确反例引导 LLM 进行侵入式重构。
*   **软硬件协同**：DSL 既保持了 LLM 易学的 Python 语法，又暴露了硬件底层的矩阵核（MFMA）、累加寄存器（AGPR）和指令调度屏障。
*   **零开销验证**：将复杂的正确性检查完全移至编译时，确保了最终代码的极致执行效率。

#### 8. 不足与局限
*   **知识库依赖**：优化效果高度依赖于专家预先定义的优化技巧知识库，对于全新的、未见过的硬件架构可能需要人工干预。
*   **算法优化限制**：数据流不变式主要解决“如何正确实现给定算法”，无法自动发明全新的算法（如将标准 Softmax 替换为 Online Softmax）。
*   **硬件覆盖**：目前主要针对 AMD MI300X 进行了深度优化，虽然理论上可移植，但在 NVIDIA 或其他架构上的表现尚需进一步验证。

（完）
