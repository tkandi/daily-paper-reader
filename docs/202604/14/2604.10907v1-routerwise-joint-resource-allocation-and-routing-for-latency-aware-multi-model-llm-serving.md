---
title: "RouterWise: Joint Resource Allocation and Routing for Latency-Aware Multi-Model LLM Serving"
title_zh: RouterWise：面向延迟感知型多模型 LLM 服务的联合资源分配与路由
authors: "Hossein Hosseini Kasnavieh, Christopher Leckie, Adel N. Toosi"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.10907v1"
tags: ["query:mlsys"]
score: 9.0
evidence: GPU 集群中的联合资源分配与路由
tldr: 针对多模型大语言模型（LLM）推理服务，本文提出了RouterWise框架，旨在解决传统路由方法忽略资源分配对延迟影响的问题。RouterWise通过联合优化GPU资源分配与路由策略，在满足延迟服务水平目标（SLO）的前提下最大化整体输出质量。该方法结合了基于系统剖析的延迟模型与对偶价格路由算法，在有限的GPU资源下实现了性能的最优平衡，是提升多模型服务效率的关键技术。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10907v1/fig-001.webp\", \"caption\": \"Fig. 1. Resource allocation strategies in multi-model LLM serving. Models can be deployed in isolation or co-located on shared GPUs. Co-location enables flexible compute and memory resource sharing and improves utilization.\", \"page\": 1, \"index\": 1, \"width\": 511, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2604.10907v1/fig-002.webp\", \"caption\": \"Fig. 5. Score-latency scatter plots of retained setups under two settings of G, where retained setups denote deployable setups that also pass the computeutilization filter. Row (a) shows the case G = 4, and row (b) shows the case G = 8.\", \"page\": 8, \"index\": 2, \"width\": 1070, \"height\": 504}, {\"url\": \"assets/figures/arxiv/2604.10907v1/fig-003.webp\", \"caption\": \"Fig. 4. Average TTFT as a function of input load under different tensor parallelism levels and maximum GPU compute shares (ρ) for three models. Each curve corresponds to a different setup, showing that the latency-load relationship varies substantially across model sizes and deployment configurations.\", \"page\": 5, \"index\": 3, \"width\": 1054, \"height\": 201}]"
motivation: 传统路由方法假设模型延迟固定，忽略了GPU资源共享及请求负载对延迟的动态影响，导致路由与资源分配脱节。
method: 提出RouterWise框架，通过系统剖析建立延迟模型，并结合对偶价格公式在不同系统配置中搜索最优的资源分配与路由策略。
result: "实验显示，在相同GPU集群上，不同资源配置导致的输出质量得分差异高达87%，凸显了资源分配对路由效果的决定性作用。"
conclusion: 联合优化资源分配与路由策略是实现延迟敏感型多模型LLM服务的核心，RouterWise能显著提升系统在满足SLO下的输出质量。
---

## 摘要
多模型 LLM 路由已成为一种有效的方法，通过将每个提示词分配给合适的模型，在保持输出质量的同时降低服务成本和延迟。然而，先前的路由方法通常假设每个模型具有固定的延迟。在实际部署中，这一假设并不准确：多个模型通常共享有限的 GPU 资源，且模型的延迟在很大程度上取决于其分配的资源以及由路由策略产生的请求负载。因此，路由与资源分配是紧密耦合的。在这项工作中，我们研究了 GPU 集群中延迟感知型多模型 LLM 服务的联合资源分配与路由问题。给定一组已部署的模型和延迟服务水平目标（SLO），我们寻求一种系统设置和路由策略，在满足延迟目标的同时最大化整体输出质量。我们将该问题形式化为针对部署设置和路由比例的受限联合优化，并提出了 RouterWise。RouterWise 将用于得分最大化路由的对偶价格公式与源自系统性能剖析的特定设置延迟模型相结合。RouterWise 在可行的系统设置中进行搜索，并针对每个固定设置计算延迟目标下的最佳路由策略。我们的结果表明，即使在同一个 GPU 集群上，不同设置下可实现的输出质量得分差异可达 87%，这突显了资源分配是路由性能的关键决定因素。

## Abstract
Multi-model LLM routing has emerged as an effective approach for reducing serving cost and latency while maintaining output quality by assigning each prompt to an appropriate model. However, prior routing methods typically assume that each model has a fixed latency. In real deployments, this assumption is inaccurate: multiple models often share limited GPU resources, and a model's latency depends strongly on both its allocated resources and the request load induced by the routing policy. Consequently, routing and resource allocation are tightly coupled.   In this work, we study joint resource allocation and routing for latency-aware multi-model LLM serving in GPU clusters. Given a set of deployed models and a latency service-level objective (SLO), we seek a system setup and routing policy that maximize overall output quality while satisfying the latency target. We formalize this problem as a constrained joint optimization over deployment setup and routing fractions, and propose RouterWise, which combines a dual-price formulation for score-maximizing routing with setup-specific latency models derived from system profiling. RouterWise searches over feasible system setups and, for each fixed setup, computes the best routing policy under the latency target. Our results show that even on the same GPU cluster, achievable output-quality score can vary by up to 87% across retained setups, highlighting that resource allocation is a key determinant of routing performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《RouterWise: Joint Resource Allocation and Routing for Latency-Aware Multi-Model LLM Serving》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：多模型 LLM 路由（Routing）通过将简单请求分配给小模型、复杂请求分配给大模型，来平衡推理成本与输出质量。
*   **核心痛点**：现有的路由研究通常假设模型延迟是**固定不变**的。但在实际 GPU 集群部署中，多个模型往往共享资源（如计算单元、显存），模型的延迟高度依赖于：
    1.  **资源分配**（如张量并行度 TP、GPU 线程百分比）；
    2.  **实时负载**（由路由策略决定的请求流量）。
*   **研究动机**：路由决策会改变模型负载，而负载和资源分配又决定了延迟。因此，路由与资源分配是深度耦合的，必须进行**联合优化**。

### 2. 方法论
RouterWise 提出了一个联合优化框架，旨在延迟服务水平目标（SLO）约束下最大化整体输出质量得分。
*   **核心思想**：将问题分解为“系统设置搜索”和“固定设置下的路由优化”两个层面。
*   **关键技术细节**：
    *   **对偶价格路由（Dual-Price Routing）**：将复杂的全局流量分配问题转化为针对每个 Prompt 的独立决策。通过引入“模型价格”（对偶变量 $\alpha$），路由决策变为：选择 `预测得分 - 模型价格` 最大的模型。
    *   **特定设置的延迟模型**：通过离线性能剖析（Profiling），针对不同的 TP 级别和 GPU 线程占比（$\rho$），构建分段线性的“延迟-负载”曲线模型。
    *   **三阶段优化流程**：
        1.  **路由松弛**：在固定系统设置下，利用梯度上升法优化路由比例。
        2.  **乘子搜索**：通过二分查找调整拉格朗日乘子 $\beta$，寻找满足延迟 SLO 的最佳平衡点。
        3.  **设置搜索与剪枝**：在离散的配置空间中搜索，利用显存容量和计算利用率阈值进行剪枝，剔除不可行或低效的配置。

### 3. 实验设计
*   **数据集**：使用 **RouterBench**，包含 36,000 个 Prompt 及其在不同模型上的真实质量得分。
*   **模型池**：选取了三个不同规模的模型：**Mistral-7B-Chat**、**WizardLM-13B** 和 **Yi-34B-Chat**。
*   **对比基线（Baselines）**：
    *   **Equal-Split**：将 GPU 计算资源平均分配给所有模型。
    *   **Size-Proportional**：按模型参数量比例分配计算资源。
    *   **Isolated**：传统策略，模型独占 GPU，不进行资源共享（Co-location）。
*   **场景设置**：分别在 4 GPU 和 8 GPU 的集群规模下，针对不同的请求到达率（RPS）和延迟目标（$\tau$）进行测试。

### 4. 资源与算力
*   **硬件型号**：**NVIDIA A100 80GB PCIe GPU**。
*   **算力规模**：实验涵盖了 4 卡和 8 卡集群。
*   **软件环境**：使用 **vLLM** 作为推理后端，利用 **NVIDIA MPS**（多进程服务）实现 GPU 线程百分比的精细化控制。
*   **训练细节**：路由模型（Router）基于 **DeBERTa-v3-Large** 编码器进行微调，用于预测 Prompt 在各模型上的得分。

### 5. 实验数量与充分性
*   **实验规模**：论文在多种负载（50-80 RPS）和多种延迟约束（400ms-800ms）下进行了详尽测试。
*   **充分性分析**：
    *   展示了数以百计的可行系统设置在“得分-延迟”平面上的分布图（Scatter Plots），直观证明了不同配置对性能的巨大影响。
    *   进行了扩展性分析，对比了 3 模型和 4 模型场景下的搜索空间变化。
    *   **客观性**：通过对比多种启发式分配策略（均分、按比例），证明了联合优化的必要性。实验设计较为全面，涵盖了从底层系统参数到上层路由逻辑的完整链路。

### 6. 主要结论与发现
*   **资源分配至关重要**：在相同的 GPU 集群上，仅通过改变资源分配方式，可实现的最高输出质量得分差异竟高达 **87%**。
*   **共存部署的优势**：允许模型共享 GPU（Co-location）比隔离部署（Isolated）更有效，因为它能根据模型需求灵活转移计算力。
*   **延迟的非线性**：实验证实延迟随负载增加呈非线性增长，且受 TP 级别和线程占比的强烈调制，简单的固定延迟假设在实际中会失效。

### 7. 优点
*   **视角创新**：打破了机器学习领域（关注路由算法）与系统领域（关注资源调度）的壁垒，实现了跨层优化。
*   **数学支撑**：利用对偶理论将复杂的受限优化问题转化为可高效求解的形式。
*   **落地性强**：基于 MPS 和 vLLM 等工业界常用工具，提出的延迟模型和搜索算法具有实际部署参考价值。

### 8. 不足与局限
*   **搜索空间挑战**：虽然有剪枝，但随着模型数量和 GPU 规模增加，离散配置组合仍可能面临维度灾难。
*   **静态分布假设**：实验基于固定的 Prompt 分布。在实际应用中，如果流量特征发生剧烈偏移（Distribution Shift），预先剖析的延迟模型和路由策略可能需要重新调整。
*   **离线剖析开销**：该方法依赖于详尽的离线 Profiling 数据，对于频繁更新的模型或环境，维护成本较高。

（完）
