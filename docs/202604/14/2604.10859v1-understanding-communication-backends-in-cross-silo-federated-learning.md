---
title: Understanding Communication Backends in Cross-Silo Federated Learning
title_zh: 深入理解跨机构联邦学习中的通信后端
authors: "Amir Ziashahabi, Chaoyang He, Salman Avestimehr"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10859v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 分布式联邦学习系统中 MPI 和 PyTorch RPC 等通信后端的基准测试
tldr: 本文针对跨机构联邦学习中大模型传输的通信瓶颈，深入评估了MPI、gRPC和PyTorch RPC等后端性能。研究发现现有后端在地理分布式环境下处理大模型时效率受限，为此提出了一种结合对象存储的混合后端gRPC+S3。该方案显著提升了端到端训练速度，并为不同网络配置下的后端选择提供了实践指导。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10859v1/fig-001.webp\", \"caption\": \"Fig. 1: Three common deployment environments for federated learning: (1) LAN: participants in a single building; (2) GeoProximal: participants in close geographical proximity; (3) Geo-Distributed: participants in distant locations. Contextaware selection of communication backends is crucial for optimizing performance in each setting.\", \"page\": 1, \"index\": 1, \"width\": 527, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2604.10859v1/fig-002.webp\", \"caption\": \"Fig. 3: Architecture of the proposed gRPC+S3 backend.\", \"page\": 3, \"index\": 2, \"width\": 529, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2604.10859v1/fig-003.webp\", \"caption\": \"Fig. 5: Per-state duration in end-to-end LAN experiments (communication, CPU-GPU migration, serialization, waiting; plus training on clients and aggregation on server).\", \"page\": 5, \"index\": 3, \"width\": 959, \"height\": 312}]"
motivation: 随着大模型在联邦学习中的应用，多样化的网络环境使得选择和配置高效的通信后端成为优化系统性能的关键。
method: 通过对多种主流通信后端进行基准测试，并设计了一种利用S3存储优化大文件传输的gRPC+S3混合后端。
result: 实验显示gRPC+S3在地理分布式部署中传输大模型时，比原生gRPC实现了高达3.8倍的端到端加速。
conclusion: 针对联邦学习的具体任务和网络条件，采用定制化的通信后端（如混合存储方案）能有效克服大模型传输的性能瓶颈。
---

## 摘要
联邦学习（FL）已成为一种实用的隐私保护分布式机器学习手段。FL 的多功能设计使其适用于各种训练场景，从跨设备 FL 中的物联网（IoT）边缘设备到跨机构 FL 中的高性能服务器。这种多功能性的一个关键结果是 FL 应用的网络配置具有高度的多样性。结合对大语言模型等大规模模型日益增长的需求，明智地选择和配置通信后端对于确保 FL 系统的最佳性能至关重要。本研究专注于跨机构联邦学习，对包括 MPI、gRPC 和 PyTorch RPC 在内的各种通信后端进行了深入的基准测试。此外，我们引入了 gRPC+S3，这是一种混合后端，旨在克服现有方法的局限性，特别是在地理分布式部署中传输大模型时，其端到端速度比 gRPC 提升了高达 3.8 倍。我们的基准测试考察了在现实网络条件下运行的各种模型规模的点对点和端到端性能。我们的研究结果为根据特定的联邦学习任务和网络配置选择及配置合适的通信后端提供了实用的见解。

## Abstract
Federated learning (FL) has emerged as a practical means for privacy-preserving distributed machine learning. FL's versatile design makes it suitable for various training settings, from IoT edge devices in cross-device FL to powerful servers in cross-silo FL. A key consequence of this versatility is the high level of diversity found in the networking configuration of FL applications. Coupled with the rising demand for large-scale models such as large language models, well-informed selection and configuration of communication backends become crucial for ensuring optimal performance in FL systems. This work focuses on cross-silo federated learning, presenting in-depth benchmarks of various communication backends, including MPI, gRPC, and PyTorch RPC. In addition, we introduce gRPC+S3, a hybrid backend designed to overcome the limitations of existing approaches, particularly for transmitting large models across geo-distributed deployments, achieving up to $3.8\times$ end-to-end speedup over gRPC. Our benchmarks examine point-to-point and end-to-end performance for a broad range of model sizes running under realistic network conditions. Our findings provide practical insights for selecting and configuring suitable communication backends tailored to the specific federated learning tasks and network configurations.

---

## 论文详细总结（自动生成）

这篇论文对跨机构联邦学习（Cross-Silo FL）中的通信后端进行了深入的基准测试与优化研究。以下是详细的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在跨机构联邦学习中，参与者通常是拥有高性能计算资源的组织，但它们之间的网络环境差异巨大（从高速局域网到高延迟的广域网）。随着大模型（如 LLM、ViT）的普及，通信成为系统瓶颈。现有的通信后端（如 MPI、gRPC、PyTorch RPC）在不同网络条件下的表现缺乏系统性对比，且在处理大规模参数传输时存在效率低下或内存占用过高的问题。
*   **研究动机**：旨在通过量化分析，指导开发者根据网络环境和模型大小选择最优后端，并解决现有后端在地理分布式（Geo-Distributed）环境下传输大模型的局限性。

### 2. 论文提出的方法论
论文不仅进行了基准测试，还提出了一种名为 **gRPC+S3** 的混合通信后端：
*   **核心思想**：**控制流与数据流分离**。利用 gRPC 处理轻量级的元数据和控制指令，利用 Amazon S3（对象存储）处理大规模的模型权重传输。
*   **关键技术细节**：
    *   **发送端**：将模型序列化并上传至 S3 桶，仅通过 gRPC 向接收方发送包含 S3 对象键（Object Key）的 Protobuf 消息。
    *   **接收端**：接收到元数据后，通过多个独立连接从 S3 并行下载模型并反序列化。
    *   **优势**：规避了 Python gRPC 默认单连接传输大数据的瓶颈；服务器在广播模型时无需为每个客户端在内存中缓存副本，显著降低了内存开销。

### 3. 实验设计
*   **实验场景**：
    1.  **LAN（局域网）**：单数据中心，InfiniBand 连接（5 GB/s）。
    2.  **Geo-Proximal（近距离地理分布）**：同一 AWS 区域内的不同可用区。
    3.  **Geo-Distributed（跨国地理分布）**：跨越北美、香港、巴林等全球 7 个 AWS 区域。
*   **Benchmark 模型**：
    *   **Small** (ResNet56, 2.39 MB)
    *   **Medium** (MobileNetV3, 19.85 MB)
    *   **Big** (DistilBERT, 253.19 MB)
    *   **Large** (ViT-Large, 1.24 GB)
*   **对比方法**：MPI (Generic & Mem Buff 变体)、gRPC (Unary/Streaming)、PyTorch RPC (TensorPipe)。

### 4. 资源与算力
*   **GPU 型号与数量**：
    *   本地测试：2 台机器，每台配备 8 块 **NVIDIA Quadro RTX 5000** GPU。
    *   云端测试：使用 AWS **g4dn.2xlarge** 实例（配备 **NVIDIA T4** GPU）。
*   **网络配置**：使用了 AWS VPC 对等连接（Peering）和 InfiniBand 链路。
*   **训练时长**：文中未给出总训练时长，但明确说明端到端实验中每个客户端每轮仅训练 1 个 Epoch，以突出通信耗时。

### 5. 实验数量与充分性
*   **实验维度**：涵盖了点对点（P2P）延迟测试、并发传输下的内存占用测试、以及完整的端到端（E2E）联邦学习训练测试。
*   **充分性**：实验设计较为全面，考虑了模型大小（从 MB 到 GB 级）、网络延迟（从微秒到百毫秒级）以及并发客户端数量对性能的影响。
*   **客观性**：通过在真实 AWS 全球节点部署，模拟了真实的跨机构协作环境，结果具有较高的工程参考价值。

### 6. 论文的主要结论与发现
*   **环境决定论**：没有万能的后端。在 **LAN** 环境下，**MPI** 和 **PyTorch RPC** 表现最好，因为它们序列化开销小；在 **广域网（WAN）** 下，多连接并发能力成为关键。
*   **gRPC 的局限**：原生 gRPC 在传输大模型时，由于单连接限制和高昂的序列化/内存拷贝开销，性能较差且极度消耗服务器内存。
*   **gRPC+S3 的优越性**：在地理分布式场景下，对于超过 10MB 的消息，gRPC+S3 优势明显。在传输 1.24GB 的大模型时，比原生 gRPC 快 **3.5–3.8 倍**。
*   **内存效率**：gRPC+S3 的内存占用不随客户端数量增加而增长，非常适合大规模跨机构联邦学习。

### 7. 优点
*   **实用性强**：针对工业界常见的跨机构场景提供了清晰的后端选择指南。
*   **架构创新**：gRPC+S3 的设计巧妙地利用了云基础设施的带宽优势，解决了长肥网络（LFN）中的吞吐量问题。
*   **真实环境**：实验基于真实物理分布的云节点，而非简单的网络模拟工具（如 NetEm）。

### 8. 不足与局限
*   **依赖性**：gRPC+S3 方案高度依赖云厂商的对象存储服务，可能引入额外的存储成本和供应商锁定风险。
*   **模型规模上限**：虽然测试了 1.24GB 的模型，但对于目前数百亿参数（数十 GB 权重）的超大语言模型，该方案在 S3 上传/下载阶段的绝对耗时仍可能非常可观。
*   **安全性讨论较少**：虽然提到了 TLS 加密，但未深入探讨引入第三方存储（S3）后对联邦学习数据隐私合规性的潜在影响。

（完）
