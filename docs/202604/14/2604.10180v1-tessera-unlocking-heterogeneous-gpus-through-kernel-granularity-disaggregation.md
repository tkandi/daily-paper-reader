---
title: "Tessera: Unlocking Heterogeneous GPUs through Kernel-Granularity Disaggregation"
title_zh: Tessera：通过内核粒度解耦释放异构 GPU 的潜力
authors: "Tiancheng Hu, Jin Qin, Zheng Wang, Junhao Hu, Yuzheng Wang, Lei Chen, Yizhou Shan, Mingxing Zhang, Ting Cao, Chunwei Xia, Huimin Cui, Tao Xie, Chenxi Wang"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10180v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 异构GPU集群的算子级解耦系统
tldr: Tessera是首个针对大模型推理的内核级异构GPU解耦系统。针对现有方案粒度粗且耦合特定架构的问题，Tessera通过分析PTX提取内核依赖，并结合流水线执行与动态调度，实现了计算任务与硬件能力的精准匹配。在多种GPU和模型上的实验表明，其吞吐量和成本效益显著提升，甚至能让异构GPU组合超越同构高端GPU的表现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10180v1/fig-001.webp\", \"caption\": \"Fig. 1: Simplified example of kernel disaggregation.\", \"page\": 2, \"index\": 1, \"width\": 472, \"height\": 303}, {\"url\": \"assets/figures/arxiv/2604.10180v1/fig-002.webp\", \"caption\": \"Fig. 6: Offline throughput across five workloads and three heterogeneous GPU pairs. Left y-axis: tokens/s for LLM, MLLM, and SSM workloads, right y-axis: images/min for SD. Red X marks indicate that the disaggregation method is inapplicable.\", \"page\": 8, \"index\": 2, \"width\": 1035, \"height\": 198}]"
motivation: 现有的异构GPU解耦方案粒度过粗且与特定模型架构紧密耦合，无法充分利用不同内核对硬件资源的差异化需求。
method: 通过离线分析PTX提取内核依赖，并结合流水线执行模型实现通信与计算重叠，同时采用轻量级运行时自适应调度。
result: 在多种异构GPU和模型架构下，Tessera将推理吞吐量和成本效益分别提升了高达2.3倍和1.6倍。
conclusion: 内核级解耦是优化异构GPU集群性能的关键，能有效打破硬件限制并显著降低大模型推理成本。
---

## 摘要
解耦技术将 AI 工作负载的部分映射到不同类型的 GPU 上，为利用现代异构 GPU 集群提供了一条路径。然而，现有解决方案的运行粒度较粗，且与特定模型架构紧密耦合，在性能提升方面仍有很大空间。本文提出了 Tessera，这是首个通过内核解耦来提高大模型推理在异构 GPU 上的性能和成本效益的系统。我们的核心见解是，单个应用程序中的内核（kernel）表现出多样化的资源需求，使其成为将计算与硬件能力对齐的最合适粒度。Tessera 通过从 PTX 中提取精确的内核间依赖关系以确保正确性，通过流水线执行模型实现通信与计算的重叠，并采用具有轻量级运行时自适应的工作负载感知调度，从而将离线分析与在线自适应相结合。在五种异构 GPU 和四种模型架构（扩展至 16 个 GPU）上的广泛评估表明，与现有的解耦方法相比，Tessera 将服务吞吐量和成本效益分别提高了高达 2.3 倍和 1.6 倍，同时能够推广到先前方法不适用的模型架构。令人惊讶的是，在 Tessera 框架下，一对异构 GPU 的吞吐量甚至可以超过两个同构高端 GPU，且成本更低。

## Abstract
Disaggregation maps parts of an AI workload to different types of GPUs, offering a path to utilize modern heterogeneous GPU clusters. However, existing solutions operate at a coarse granularity and are tightly coupled to specific model architectures, leaving much room for performance improvement. This paper presents Tessera, the first kernel disaggregation system to improve performance and cost efficiency on heterogeneous GPUs for large model inference. Our key insight is that kernels within a single application exhibit diverse resource demands, making them the most suitable granularity for aligning computation with hardware capabilities. Tessera integrates offline analysis with online adaptation by extracting precise inter-kernel dependencies from PTX to ensure correctness, overlapping communication with computation through a pipelined execution model, and employing workload-aware scheduling with lightweight runtime adaptation. Extensive evaluations across five heterogeneous GPUs and four model architectures, scaling up to 16 GPUs, show that Tessera improves serving throughput and cost efficiency by up to 2.3x and 1.6x, respectively, compared to existing disaggregation methods, while generalizing to model architectures where prior approaches do not apply. Surprisingly, a heterogeneous GPU pair under Tessera can even exceed the throughput of two homogeneous high-end GPUs at a lower cost.

---

## 论文详细总结（自动生成）

这是一份关于论文《Tessera: Unlocking Heterogeneous GPUs through Kernel-Granularity Disaggregation》的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：现代数据中心由于采购周期和成本限制，普遍存在异构 GPU 集群（如 A100 与 L40s 并存）。
*   **核心问题**：现有的异构 GPU 利用方案（如“预填充-解码解耦”或“算子块级解耦”）粒度过粗。论文发现，即使在同一个计算块内，不同内核（Kernel）对计算和带宽的需求也截然不同。
*   **研究动机**：粗粒度方案会导致大量内核被错误地分配到不匹配的硬件上（例如将带宽敏感型内核放在高算力但低带宽的 GPU 上），从而限制了性能和成本效益。

### 2. 方法论
Tessera 是首个实现**内核粒度（Kernel-granularity）**解耦的系统，其核心流程如下：
*   **内核分析器（Kernel Analyzer）**：
    *   针对不透明的内核（如 JIT 编译或用户定义内核），通过在 **PTX（汇编级）**层面注入指令，追踪内存访问边界。
    *   提取精确的内核间数据依赖关系（RAW 依赖），构建数据依赖图（DDG）。
*   **策略规划器（Policy Planner）**：
    *   将内核调度建模为**混合整数线性规划（MILP）**问题。
    *   支持两种目标：**吞吐量导向**（最小化流水线最慢阶段的时间）和**延迟导向**（最小化单次请求的关键路径耗时）。
*   **执行单元（GPU Workers）**：
    *   采用**流水线请求处理**：通过多流（Multi-stream）并发执行多个请求，利用优先级感知调度（Priority-aware scheduling）错开不同请求的通信阶段，实现计算与通信的深度重叠。
*   **在线监控器（Online Monitor）**：
    *   根据运行时队列压力，动态在吞吐量策略和延迟策略之间切换。

### 3. 实验设计
*   **模型覆盖**：涵盖了主流的 AI 架构，包括 LLM（Llama3-8B, GPT-oss-20B）、SSM（Mamba-7B）、MLLM（Qwen2.5-VL-7B）和扩散模型（Stable Diffusion 3.5）。
*   **硬件组合**：测试了三组典型的异构对：A100+L40s、H100+RTX Pro 6000、B200+H100。
*   **Benchmark 与对比方法**：
    *   **PD Dis.**：预填充-解码解耦（如 DistServe）。
    *   **AF Dis.**：Attention-FFN 解耦（如 MegaScale-Infer）。
    *   **Homogeneous**：同构 GPU 基准。
*   **数据集**：使用 Splitwise 对话数据集、COCO 图像描述数据集和 PartiPrompts 提示词数据集。

### 4. 资源与算力
*   **GPU 型号**：使用了 NVIDIA A100 (80GB)、H100 (80GB)、B200 (192GB)、L40s (48GB) 和 RTX Pro 6000 (96GB)。
*   **集群规模**：实验从单对异构 GPU 扩展到了集群规模，包括“2*A100 + 1*L40s”以及“8*B200 + 8*H100”的配置（共 16 块 GPU）。
*   **网络**：配备了 200 Gbps 或 400 Gbps 的 RDMA 网络。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了端到端吞吐量测试、在线服务延迟测试、成本效益分析、集群扩展性实验，以及针对流水线效率、网络带宽敏感度和 MILP 求解时间的消融实验。
*   **充分性评价**：实验设计非常充分且具有说服力。它不仅对比了最先进的解耦方案，还跨越了多种完全不同的模型架构（如非 Transformer 的 Mamba 和非自回归的 SD），证明了系统的通用性。

### 6. 主要结论与发现
*   **性能提升**：相比现有解耦方案，Tessera 将服务吞吐量提升了高达 **2.3 倍**，成本效益提升了 **1.6 倍**。
*   **打破瓶颈**：在相同 SLO（服务水平目标）下，Tessera 能多承载 1.3 倍的请求量。
*   **以弱胜强**：令人惊讶的发现是，通过 Tessera 优化的**异构 GPU 对（如 H100 + RTX 6000）在吞吐量上甚至可以超过两个同构高端 GPU（2*H100）**，且成本大幅降低。
*   **通用性**：Tessera 能够应用于 PD 或 AF 解耦无法处理的模型（如扩散模型）。

### 7. 优点
*   **粒度创新**：首次将解耦推向内核级，精准匹配了硬件异构性与应用异构性。
*   **系统完备性**：通过 PTX 分析解决了内核不透明带来的依赖分析难题，通过优先级流水线解决了通信开销问题。
*   **模型无关性**：不依赖于特定的 Transformer 结构，具有极强的泛化能力。
*   **成本优化**：为云服务商提供了一种通过廉价 GPU 增强高端 GPU 性能的切实路径。

### 8. 不足与局限
*   **内存开销**：目前方案倾向于在所有 GPU 上复制模型权重，虽然文中提到可以进行选择性回收，但在显存极度受限的场景下可能存在压力。
*   **间接访问限制**：PTX 分析器无法处理通过全局变量（而非内核参数）进行的间接内存访问（尽管这种情况在主流框架中很少见）。
*   **跨厂商限制**：目前仅针对 NVIDIA 生态（依赖 PTX 和 NCCL），尚未扩展到 AMD 或其他加速器。

（完）
