---
title: "TensorHub: Scalable and Elastic Weight Transfer for LLM RL Training"
title_zh: TensorHub：面向大语言模型强化学习训练的可扩展且弹性的权重传输系统
authors: "Chenhao Ye, Huaizheng Zhang, Mingcong Han, Baoquan Zhong, Xiang Li, Qixiang Chen, Xinyi Zhang, Weidong Zhang, Kaihua Jiang, Wang Zhang, He Sun, Wencong Xiao, Andrea C. Arpaci-Dusseau, Remzi H. Arpaci-Dusseau"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09107v1"
tags: ["query:mlsys"]
score: 10.0
evidence: LLM强化学习训练的可扩展权重传输
tldr: 针对大语言模型强化学习训练中权重传输效率低和扩展性差的问题，本文提出了 TensorHub 系统。该系统引入“引用导向存储”（ROS）抽象，通过跟踪 GPU 节点上的权重版本而非物理存储副本，实现了按需的高效传输。TensorHub 结合了拓扑优化、强一致性和容错机制，显著降低了 GPU 停顿时间，支持跨数据中心和弹性扩展的 RL 训练任务。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09107v1/fig-001.webp\", \"caption\": \"Table 2. TensorHub APIs. open() returns a handle, and the rest of the APIs are methods of this handle.\", \"page\": 6, \"index\": 1, \"width\": 1052, \"height\": 287}, {\"url\": \"assets/figures/arxiv/2604.09107v1/fig-002.webp\", \"caption\": \"Figure 11. Elastic Rollout Results.\", \"page\": 11, \"index\": 2, \"width\": 494, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2604.09107v1/fig-003.webp\", \"caption\": \"Figure 2. Reference-Oriented Storage Workflow. The server only operates on lightweight references. The bulk weight transfer is directly between clients.\", \"page\": 4, \"index\": 3, \"width\": 500, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2604.09107v1/fig-004.webp\", \"caption\": \"Figure 7. Microbenchmark Results.\", \"page\": 9, \"index\": 4, \"width\": 496, \"height\": 431}]"
motivation: 现有的权重传输方法在动态扩展集群时缺乏灵活性，且存在严重的数据移动开销，限制了 LLM RL 训练的效率。
method: 提出引用导向存储（ROS）抽象，通过直接利用 GPU 内存中的权重副本并结合拓扑优化技术，构建了高性能的 TensorHub 系统。
result: 实验表明 TensorHub 显著降低了 GPU 停顿时间，在弹性推理和跨数据中心场景下分别实现了 4.8 倍和 19 倍的性能提升。
conclusion: TensorHub 为大规模 LLM RL 训练提供了一个高效、弹性且生产可用的权重传输解决方案。
---

## 摘要
现代大语言模型（LLM）强化学习（RL）任务需要高效的权重传输系统，以便在异构计算资源上扩展训练规模。然而，现有的权重传输方法要么无法为动态扩展的集群提供灵活性，要么会产生根本性的数据移动开销，导致性能不佳。我们提出了面向引用的存储（Reference-Oriented Storage, ROS），这是一种用于 RL 权重传输的新型存储抽象，它利用了原位的高度复制模型权重。ROS 提供了一种幻觉，即特定版本的模型权重已被存储并可按需获取。在底层，ROS 并不物理存储权重的任何副本；相反，它跟踪在 GPU 上持有这些权重用于推理的工作节点。收到请求时，ROS 直接利用这些节点来提供读取服务。我们构建了 TensorHub，这是一个生产级的系统，它扩展了 ROS 的理念，并结合了拓扑优化传输、强一致性和容错性。评估结果显示，TensorHub 能够完全饱和 RDMA 带宽，并能以极低的工程工作量适应三种不同的采样（rollout）负载。具体而言，TensorHub 将独立采样的总 GPU 停顿时间减少了高达 6.7 倍，将弹性采样的权重更新速度提升了 4.8 倍，并将跨数据中心采样的停顿时间缩减了 19 倍。TensorHub 已在生产环境中部署，用于支持前沿的 RL 训练。

## Abstract
Modern LLM reinforcement learning (RL) workloads require a highly efficient weight transfer system to scale training across heterogeneous computational resources. However, existing weight transfer approaches either fail to provide flexibility for dynamically scaling clusters or incur fundamental data movement overhead, resulting in poor performance.   We introduce Reference-Oriented Storage (ROS), a new storage abstraction for RL weight transfer that exploits the highly replicated model weights in place. ROS presents the illusion that certain versions of the model weights are stored and can be fetched on demand. Underneath, ROS does not physically store any copies of the weights; instead, it tracks the workers that hold these weights on GPUs for inference. Upon request, ROS directly uses them to serve reads. We build TensorHub, a production-quality system that extends the ROS idea with topology-optimized transfer, strong consistency, and fault tolerance. Evaluation shows that TensorHub fully saturates RDMA bandwidth and adapts to three distinct rollout workloads with minimal engineering effort. Specifically, TensorHub reduces total GPU stall time by up to 6.7x for standalone rollouts, accelerates weight update for elastic rollout by 4.8x, and cuts cross-datacenter rollout stall time by 19x. TensorHub has been deployed in production to support cutting-edge RL training.

---

## 论文详细总结（自动生成）

### TensorHub: 面向大语言模型强化学习训练的可扩展且弹性的权重传输系统

#### 1. 论文的核心问题与整体含义
在现代大语言模型（LLM）的强化学习（RL）训练中，系统通常由**训练器（Trainer）**和**采样器（Rollout）**组成。两者之间存在频繁且海量的模型权重传输需求（TB 级）。现有的传输方案存在明显缺陷：
*   **集体通信（如 NCCL）**：吞吐量高但缺乏弹性，无法处理节点动态加入或退出，且需要全局同步，易受“木桶效应”影响。
*   **点对点通信（如 UCX）**：灵活但缺乏全局拓扑感知，在多对多传输时易导致网络拥塞。
*   **分布式存储（如 Ray Plasma）**：实现了逻辑解耦，但引入了额外的数据拷贝（GPU-CPU-Network）和存储空间开销，性能极低。

论文的核心动机是设计一个既能像存储系统一样**解耦**、又能像集体通信一样**高效**，且支持**动态弹性扩展**的权重传输系统。

#### 2. 论文提出的方法论
论文提出了**引用导向存储（Reference-Oriented Storage, ROS）**抽象，并在此基础上实现了生产级系统 **TensorHub**。

*   **核心思想**：ROS 不再物理上“拥有”或“存储”权重数据，而是通过一个中心化的引用服务器跟踪哪些 GPU 节点当前持有特定版本的权重。它将分布在集群各处的 GPU 内存视为一个天然的、高度冗余的存储池。
*   **关键技术细节**：
    *   **可变性契约（Mutability Contract）**：规定工作节点在持有权重引用时必须保证数据不可变；若需修改（如开始新一轮训练），必须先执行 `unpublish` 撤销引用。
    *   **保留协议（Retention Protocol）**：确保最新版本不因所有节点同时释放而丢失。若某个版本即将消失，系统会强制将其一份副本卸载（Offload）到 CPU 内存中。
    *   **流水线复制（Pipeline Replication）**：利用 RDMA 全双工特性，让正在接收权重的节点同时作为源向其他节点转发数据，将传输拓扑从“星型”转变为“带宽放大的 DAG”。
    *   **模型并行一致性**：通过事务性语义确保模型并行组内的所有工作节点看到一致的权重版本，防止 SPMD 程序运行分叉。
    *   **拓扑感知调度**：优先选择同机架、同数据中心的源节点，最小化跨地域流量。

#### 3. 实验设计
论文通过微观基准测试（Microbenchmarks）和三个生产场景的案例研究（Case Studies）进行评估：
*   **对比方法（Benchmarks）**：NCCL、UCX、Ray Plasma Object Store。
*   **测试场景**：
    1.  **独立采样（Standalone Rollout）**：专用 GPU 集群进行采样。
    2.  **弹性采样（Elastic Rollout）**：使用易被抢占的竞价实例（Spot Instances），模拟节点频繁上下线。
    3.  **跨数据中心采样（Cross-DC Rollout）**：训练器和采样器分布在不同地域，通过慢速 TCP 链路连接。
*   **模型规模**：涵盖 9B、36B、260B 以及模拟的 1T 参数模型。

#### 4. 资源与算力
*   **硬件环境**：NVIDIA Hopper GPU（如 H100/H800 级别），配备 400 Gbps Mellanox InfiniBand RDMA 网卡和 200 Gbps VPC 网络。
*   **算力规模**：
    *   最大实验规模达到了 **1024 块 GPU**（768 块用于 Trainer，256 块用于 Rollout）。
    *   单机配置为 8-GPU 节点，拥有 1800 GB 内存。

#### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **微观测试**：测试了不同数据量（最高 50GB/shard）下的带宽利用率、流水线扩展性以及故障恢复时间。
*   **端到端测试**：在四种不同规模的模型上对比了 GPU 停顿时间。
*   **场景覆盖**：专门针对弹性扩展和跨 DC 传输做了针对性实验。
*   **充分性评价**：实验设计非常充分且具有高度的工程实践意义。它不仅对比了学术界常用的基准，还直接在字节跳动的生产环境框架（基于 veRL）中进行了集成测试，证明了系统在真实大规模负载下的有效性。

#### 6. 论文的主要结论与发现
*   **性能卓越**：TensorHub 能达到 RDMA 理论带宽的 88% 左右，远超分布式存储方案。
*   **减少停顿**：在 1024 GPU 规模下，相比 NCCL 将总 GPU 停顿时间减少了 **6.7 倍**。
*   **弹性优势**：在弹性采样场景下，通过动态负载均衡，权重更新速度比 UCX 快 **4.8 倍**。
*   **跨 DC 优化**：通过“单次种子传输 + 本地 RDMA 流水线”模式，将跨数据中心采样的停顿时间缩减了 **19 倍**，且大幅降低了跨地域流量。
*   **工程友好**：集成到现有框架仅需约 40 行代码改动。

#### 7. 优点
*   **零拷贝与零额外空间**：巧妙利用了 RL 任务中权重天然的高度冗余性，避免了传统存储系统昂贵的内存占用和数据搬运。
*   **极强的鲁棒性**：原生支持节点故障和动态扩缩容，解决了 NCCL 在不稳定集群中易崩溃的问题。
*   **解耦同步**：通过存储接口实现了 Trainer 和 Rollout 的异步化，消除了全局屏障（Global Barrier）带来的长尾延迟。

#### 8. 不足与局限
*   **中心化瓶颈风险**：虽然目前单台引用服务器可支撑千卡规模，但在超大规模（如万卡以上）或极高频率更新时，中心化服务器可能成为元数据处理的瓶颈（尽管文中提到了分区方案，但未详细展开）。
*   **网络环境依赖**：虽然支持 TCP，但其核心优势（如流水线复制）高度依赖 RDMA 环境。在纯 TCP 环境下，性能提升可能受限。
*   **应用范围**：该系统高度针对 LLM RL 这种“高频率、大容量、高冗余”的权重传输场景，对于其他类型的分布式训练任务（如梯度聚合）可能并不适用。

（完）
