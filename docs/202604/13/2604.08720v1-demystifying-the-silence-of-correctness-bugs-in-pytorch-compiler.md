---
title: Demystifying the Silence of Correctness Bugs in PyTorch Compiler
title_zh: 揭秘 PyTorch 编译器中的静默正确性缺陷
authors: "Meiziniu Li, Dongze Li, Jianmeng Liu, Shing-Chi Cheung"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08720v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 对PyTorch编译器（torch.compile）正确性缺陷的研究
tldr: PyTorch 编译器（torch.compile）在优化深度学习模型时常出现导致输出错误但不报错的正确性缺陷，严重威胁模型可靠性。本文对该类缺陷进行了首次系统性实证研究，分析了其特征及现有模糊测试工具的局限性。基于研究发现，提出了利用大语言模型进行测试用例变异的检测技术 AlignGuard。该工具已成功发现 23 个新缺陷，其中 14 个被标记为高优先级，显著提升了 PyTorch 编译器的可靠性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08720v1/fig-001.webp\", \"caption\": \"Table 1. Step-by-step filtering process for identifying correctness bug issues in torch.compile\", \"page\": 6, \"index\": 1, \"width\": 814, \"height\": 259}, {\"url\": \"assets/figures/arxiv/2604.08720v1/fig-002.webp\", \"caption\": \"Fig. 4. Distribution of Bug Types\", \"page\": 7, \"index\": 2, \"width\": 577, \"height\": 383}, {\"url\": \"assets/figures/arxiv/2604.08720v1/fig-003.webp\", \"caption\": \"Table 2. Characteristics of correctness bugs in torch.compile.\", \"page\": 8, \"index\": 3, \"width\": 819, \"height\": 531}, {\"url\": \"assets/figures/arxiv/2604.08720v1/fig-004.webp\", \"caption\": \"Fig. 5. Benchmarking studied techniques’ bug detection performance\", \"page\": 19, \"index\": 4, \"width\": 483, \"height\": 287}, {\"url\": \"assets/figures/arxiv/2604.08720v1/fig-005.webp\", \"caption\": \"Fig. 6. Distribution of detected and missed bugs. The name of each bug category is abbreviated using its initials (e.g., ‘OTB’ for Operator-Transformation Bugs, ‘LCG’ for Low-Level Code Generation Bugs).\", \"page\": 20, \"index\": 5, \"width\": 810, \"height\": 535}, {\"url\": \"assets/figures/arxiv/2604.08720v1/fig-006.webp\", \"caption\": \"Fig. 3. Distribution of faulty components (bottom and top left) and symptoms of high-priority issues in torch.compile (top right).\", \"page\": 5, \"index\": 6, \"width\": 668, \"height\": 384}]"
motivation: 针对 PyTorch 编译器中普遍存在且危害巨大的隐蔽正确性缺陷（导致输出错误但不报错），目前缺乏系统性研究和有效的检测手段。
method: 通过对现有缺陷进行实证研究总结特征，并提出基于大语言模型（LLM）变异现有测试用例的检测技术 AlignGuard。
result: AlignGuard 成功检测出 23 个此前未知的正确性缺陷，且全部得到官方确认或修复，其中超过半数为高优先级缺陷。
conclusion: 本研究揭示了 PyTorch 编译器正确性缺陷的特性，并证明了结合 LLM 的针对性测试技术在保障 AI 基础设施可靠性方面的巨大潜力。
---

## 摘要
AI 基础设施的性能优化是大语言模型（LLM）快速普及的关键。PyTorch 编译器（torch.compile）作为深度学习（DL）模型（包括 LLM）的核心优化工具，受到了广泛关注。然而，torch.compile 容易出现正确性缺陷，这些缺陷会导致编译后的深度学习模型产生错误输出，且不会触发异常、崩溃或警告。这些缺陷对下游 LLM 应用的可靠性构成了严重威胁。来自 PyTorch 社区的数据显示，19.2% 的高优先级问题是由 torch.compile 缺陷导致的编译后深度学习模型输出错误，是第二常见的缺陷类别（仅次于占比 19.57% 的程序崩溃）。然而，目前尚无系统性研究专门针对这些缺陷进行特征分析并据此进行检测。在本文中，我们对 torch.compile 中的正确性缺陷进行了首次实证研究，分析了它们的特征，并评估了现有模糊测试工具在检测这些缺陷方面的有效性。基于我们的发现，我们提出了一种名为 AlignGuard 的概念验证测试技术，专门用于检测 torch.compile 中的正确性缺陷。AlignGuard 结合了从实证研究中提取的缺陷特征，将基于 LLM 的测试变异应用于现有测试用例，以进行正确性缺陷检测。截至撰稿时，AlignGuard 已在最近的 torch.compile 版本中成功检测到 23 个新的正确性缺陷。所有这些缺陷均已得到 PyTorch 开发团队的确认或修复，其中超过一半（14/23）甚至被标记为高优先级缺陷，凸显了我们技术的实用性。

## Abstract
Performance optimization of AI infrastructure is key to the fast adoption of large language models (LLMs). The PyTorch compiler (torch.compile), a core optimization tool for deep learning (DL) models (including LLMs), has received due attention. However, torch.compile is prone to correctness bugs, which cause incorrect outputs of compiled DL models without triggering exceptions, crashes, or warnings. These bugs pose a serious threat to the reliability of downstream LLM applications. Data from the PyTorch community shows that 19.2% of high-priority issues are incorrect outputs of compiled DL models induced by torch.compile bugs, the second-most-common bug category (only behind program crashes at 19.57%). However, no systematic study has been conducted to specifically characterize and thereby detect these bugs. In this paper, we present the first empirical study of the correctness bugs in torch.compile, examine their characteristics, and assess the effectiveness of existing fuzzers in detecting them. Based on our findings, we propose a proof-of-concept testing technique named AlignGuard, tailored specifically for detecting correctness bugs in torch.compile. AlignGuard incorporates bug characteristics distilled from our empirical study, applying LLM-based test mutation to existing test cases for correctness bug detection. At the time of writing, AlignGuard has successfully detected 23 new correctness bugs in recent torch.compile. All these bugs have been confirmed or fixed by the PyTorch development team, and over half (14/23) of them are even marked as high-priority bugs, underscoring the usefulness of our technique.

---

## 论文详细总结（自动生成）

这是一份关于论文《Demystifying the Silence of Correctness Bugs in PyTorch Compiler》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：研究 PyTorch 编译器（`torch.compile`）中的“正确性缺陷”（Correctness Bugs）。这类缺陷极其隐蔽且危险，因为它们会导致模型输出错误结果，但不会触发任何崩溃、异常或警告（即静默错误）。
*   **研究动机**：
    *   **重要性**：`torch.compile` 是 LLM 优化的核心工具，被 vLLM 和 AWS 等广泛采用。
    *   **严重性**：统计显示，PyTorch 社区 19.2% 的高优先级问题属于此类缺陷，仅次于程序崩溃。
    *   **研究空白**：现有研究多关注静态图编译器（如 TVM），而 `torch.compile` 采用动态即时编译（JIT），其缺陷特征和触发模式尚未被系统研究。

### 2. 方法论
论文采用了“实证研究 + 工具评估 + 新工具开发”的思路：
*   **实证研究**：收集并分析了 116 个真实的 `torch.compile` 正确性缺陷，将其分为三大类：
    1.  **图相关缺陷**（Graph-related）：前端图构建错误，如符号追踪不完整。
    2.  **算子相关缺陷**（Operator-related）：算子转换或底层代码生成（如 Triton 代码）时的语义丢失或数值不稳定。
    3.  **内存相关缺陷**（Memory-related）：最常见（33.6%），涉及原地操作（In-place）处理不当和内存布局冲突。
*   **AlignGuard 工具设计**：基于实证研究得到的知识，利用 LLM 进行测试用例变异：
    *   **模式提取**：利用 LLM 从历史缺陷报告和修复代码中提取触发模式。
    *   **规则合成**：将模式转化为“条件-动作”对的变异规则。
    *   **测试变异**：将规则应用于现有的种子测试用例，生成能触发新缺陷的复杂场景。

### 3. 实验设计
*   **基准测试集（Benchmark）**：从 116 个缺陷中筛选出 77 个可复现且有修复补丁的缺陷作为 Benchmark。
*   **对比方法**：评估了 5 种最先进的深度学习编译器测试工具：
    *   语法驱动：**NNSmith**, **NeuRI**
    *   测试迁移：**Opera**
    *   LLM 驱动：**WhiteFox**, **DeepConstr**
*   **AlignGuard 验证**：在 PyTorch 最新版本（2.8.0 阶段）上运行，使用 341 个种子测试用例进行变异。

### 4. 资源与算力
*   **硬件环境**：一台配备 32 核 CPU、500 GB RAM 的服务器，以及 **3 张 NVIDIA RTX 3090Ti GPU**。
*   **模型使用**：
    *   使用 **GPT-4o** 进行缺陷模式提取和变异规则合成。
    *   使用 **GPT-4o-mini** 进行大规模的测试用例变异生成（出于成本考虑）。
*   **时长**：文中未明确给出总训练/运行总时长，但提到变异是在最新稳定版上进行的实时检测。

### 5. 实验数量与充分性
*   **实验规模**：
    *   分析了过去两年（2023-2025）的所有相关 GitHub Issue。
    *   对 5 种现有工具在 77 个缺陷上的表现进行了详尽的交叉对比。
    *   AlignGuard 成功发现了 **23 个新缺陷**。
*   **充分性与客观性**：实验设计较为公平，所有对比工具均使用默认配置。通过开发者确认和修复（14 个高优先级）来验证结果，确保了研究的客观性和实际影响力。

### 6. 主要结论与发现
*   **现有工具局限性**：现有工具仅能检测到 Benchmark 中 33.8% 的缺陷，且对“图相关缺陷”的检测率为 0。
*   **核心诱因**：原地操作（In-place）作用于张量别名（Alias）是导致正确性错误的最主要诱因（占 21.6%）。
*   **测试盲区**：现有的模糊测试工具缺乏对非计算操作（如控制流、上下文切换）和复杂内存布局变换的覆盖。
*   **LLM 潜力**：结合领域知识（Domain Knowledge）的 LLM 变异技术比纯随机或纯语法驱动的方法更有效。

### 7. 优点（亮点）
*   **首次系统研究**：填补了 PyTorch 编译器正确性缺陷实证研究的空白。
*   **知识驱动**：AlignGuard 不仅仅是盲目使用 LLM，而是通过实证研究提炼出具体的缺陷特征来指导 LLM 变异。
*   **实战价值高**：发现的 23 个新缺陷全部得到官方确认，直接提升了 PyTorch 这一核心 AI 基础设施的可靠性。

### 8. 不足与局限
*   **人工分析偏差**：缺陷分类和根因分析涉及大量人工标注，可能存在主观偏差。
*   **覆盖范围限制**：研究主要集中在 `torch.compile` 及其核心后端（TorchInductor），对其他第三方后端的覆盖较少。
*   **LLM 依赖与成本**：AlignGuard 依赖闭源的高性能 LLM，存在 API 调用成本和生成代码随机性的问题。
*   **硬件依赖**：部分缺陷可能与特定硬件架构（如特定型号的 GPU）相关，实验环境可能无法捕捉所有硬件相关的正确性问题。

（完）
