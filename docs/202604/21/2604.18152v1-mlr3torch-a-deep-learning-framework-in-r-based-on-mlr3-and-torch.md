---
title: "mlr3torch: A Deep Learning Framework in R based on mlr3 and torch"
title_zh: mlr3torch：基于 mlr3 和 torch 的 R 语言深度学习框架
authors: "Sebastian Fischer, Lukas Burk, Carson Zhang, Bernd Bischl, Martin Binder"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18152v1"
tags: ["query:mlsys"]
score: 9.5
evidence: 基于torch构建的深度学习框架
tldr: 本研究推出了 R 语言包 mlr3torch，它是基于 torch 构建的 mlr3 生态深度学习框架。该框架简化了表格和张量数据的神经网络定义、训练与评估，支持将模型表示为包含预处理和增强的计算图。通过与 mlr3 集成，它实现了便捷的重采样、基准测试和超参数优化，为 R 用户提供了灵活且可扩展的深度学习工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18152v1/fig-001.webp\", \"caption\": \"Figure 4: ROC curve for the multimodal neural network evaluated using holdout resampling.\", \"page\": 29, \"index\": 1, \"width\": 371, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2604.18152v1/fig-002.webp\", \"caption\": \"Figure 5: Runtime results for AdamW (left) and SGD (right) for GPU (top) and CPU (bottom). The facets correspond to different latent dimensions. The number of hidden layers is 0, 4, 8, 12, 16. The lines represent the median and the shaded area is defined by the upper and lower 10% quantile of the 10 repetitions.\", \"page\": 31, \"index\": 2, \"width\": 921, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2604.18152v1/fig-003.webp\", \"caption\": \"Figure 6: Median runtime of mlr3torch and torch relative to PyTorch.\", \"page\": 31, \"index\": 3, \"width\": 921, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2604.18152v1/fig-004.webp\", \"caption\": \"Figure 2: Training and prediction phase for a generating Graph.\", \"page\": 16, \"index\": 4, \"width\": 921, \"height\": 365}]"
motivation: 旨在为 R 语言的 mlr3 生态系统提供一个可扩展且易用的深度学习框架，以简化神经网络的构建与实验流程。
method: 基于 torch 包构建，利用 mlr3pipelines 的图语言定义包含预处理、数据增强和网络架构的完整建模工作流。
result: 实现了预定义架构、模型转换及多模态数据处理功能，并通过超参数调优和微调等案例展示了其灵活性。
conclusion: mlr3torch 成功整合了深度学习与 mlr3 的强大功能，为 R 语言用户提供了一个高效、模块化且功能全面的深度学习开发环境。
---

## 摘要
深度学习（DL）已成为现代机器学习（ML）实践的基石。我们介绍了 R 语言包 mlr3torch，这是一个为 mlr3 生态系统设计的可扩展深度学习框架。它基于 torch 包构建，简化了针对表格数据和通用张量（如图像）进行分类与回归任务时神经网络的定义、训练和评估。该包实现了预定义的架构，且 torch 模型可以轻松转换为 mlr3 学习器（learners）。它还允许用户将神经网络定义为图（graphs）。这种表示法基于 mlr3pipelines 中定义的图语言，允许用户在单个图中定义整个建模工作流，包括预处理、数据增强和网络架构。通过与 mlr3 生态系统的集成，该包支持便捷的重采样、基准测试、预处理等功能。我们解释了该包的设计和特性，并展示了如何针对新问题进行定制和扩展。此外，我们通过三个用例展示了该包的能力，即超参数调优、微调以及为多模态数据定义架构。最后，我们展示了一些运行时间基准测试。

## Abstract
Deep learning (DL) has become a cornerstone of modern machine learning (ML) praxis. We introduce the R package mlr3torch, which is an extensible DL framework for the mlr3 ecosystem. It is built upon the torch package, and simplifies the definition, training, and evaluation of neural networks for both tabular data and generic tensors (e.g., images) for classification and regression. The package implements predefined architectures, and torch models can easily be converted to mlr3 learners. It also allows users to define neural networks as graphs. This representation is based on the graph language defined in mlr3pipelines and allows users to define the entire modeling workflow, including preprocessing, data augmentation, and network architecture, in a single graph. Through its integration into the mlr3 ecosystem, the package allows for convenient resampling, benchmarking, preprocessing, and more. We explain the package's design and features and show how to customize and extend it to new problems. Furthermore, we demonstrate the package's capabilities using three use cases, namely hyperparameter tuning, fine-tuning, and defining architectures for multimodal data. Finally, we present some runtime benchmarks.

---

## 论文详细总结（自动生成）

以下是对论文《mlr3torch: A Deep Learning Framework in R based on mlr3 and torch》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：深度学习（DL）已成为机器学习的核心，但在 R 语言生态中，用户往往面临两难选择：要么使用基于 Python 包装的库（如 `keras3`、`fastai`），这会带来复杂的环境依赖和调试困难；要么使用底层的 `torch` 原生库，但这需要编写大量样板代码。
*   **核心问题**：如何在 R 语言中构建一个既具备原生性能，又能深度集成于成熟机器学习框架（如 `mlr3`）的高层深度学习框架？
*   **研究动机**：旨在为 `mlr3` 生态系统提供一个可扩展、模块化且易于使用的 DL 接口，支持从表格数据到复杂张量（如图像）的统一处理，并利用 `mlr3` 已有的重采样、超参数调优和基准测试功能。

### 2. 方法论
*   **核心思想**：基于 `torch` 包构建，利用 `mlr3pipelines` 的图语言（Graph Language）将神经网络架构、数据预处理和增强流程统一表示为一个有向无环图（DAG）。
*   **关键技术细节**：
    *   **`lazy_tensor` 数据类型**：一种延迟加载机制，允许处理无法全部装入内存的大型数据集（如高清图像），仅在训练批次需要时才进行实例化。
    *   **`LearnerTorch` 类**：封装了训练循环、预测逻辑、损失函数、优化器和回调函数（Callbacks）。
    *   **图表示法（Graph Representation）**：
        *   **Module Graph**：定义神经网络的前向传播逻辑。
        *   **Generating Graph**：通过 `PipeOpTorch` 算子动态构建网络，能够根据输入数据的维度自动推断网络层的参数（如线性层的输入特征数）。
    *   **回调机制**：支持在训练的特定阶段（如反向传播后、Epoch 结束后）插入自定义逻辑，如早停、学习率调度或梯度裁剪。

### 3. 实验设计
论文通过三个实际用例和一个性能基准测试验证了框架的有效性：
*   **用例 1：神经架构搜索（NAS）**：在 `mtcars` 表格数据集上，同时对网络深度、宽度、激活函数和学习率进行超参数调优。
*   **用例 2：图像微调（Fine-tuning）**：使用 `torchvision` 中的预训练 ResNet-18 模型，在 Dogs vs. Cats 数据集上进行二分类任务，涉及图像增强和权重冻结。
*   **用例 3：多模态数据处理**：在 Melanoma 数据集上构建了一个同时接收图像输入和表格特征（年龄、性别等）的混合架构。
*   **性能基准测试（Benchmark）**：
    *   **对比对象**：`mlr3torch`、原生 R 包 `torch` 以及 Python 的行业标准 `PyTorch`。
    *   **变量控制**：测试了不同隐藏层数（0-16层）、不同潜在维度（100-9000）、不同优化器（SGD, AdamW）以及不同硬件（CPU vs. GPU）。

### 4. 资源与算力
*   **GPU 算力**：使用了 **NVIDIA HGX A100 (80 GB VRAM)** 进行 GPU 相关的基准测试。
*   **CPU 算力**：使用了 **Intel Xeon Gold 6336Y** (2.40GHz, 1 TB RAM) 和 **6226R** (2.90GHz, 128 GB RAM)。
*   **训练时长**：基准测试中每个配置运行 20 个 Epoch，并包含 4 个 Epoch 的预热阶段，每组实验重复 10 次以获取中位数和分位数。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从简单的表格回归到复杂的图像分类及多模态任务，实验设计较为全面。
*   **充分性与客观性**：基准测试不仅在 R 内部对比，还跨语言对比了 `PyTorch`，这在 R 语言论文中较为罕见且具有说服力。通过改变网络规模（层数和维度）来观察框架开销的变化，证明了在大规模计算任务中，框架带来的额外开销几乎可以忽略不计。

### 6. 论文的主要结论与发现
*   **功能性**：`mlr3torch` 成功实现了深度学习与 `mlr3` 生态的无缝集成，简化了复杂 DL 工作流的定义。
*   **性能表现**：在 GPU 上使用 AdamW 优化器训练大型网络时，`mlr3torch` 的运行速度与 `PyTorch` 非常接近。
*   **灵活性**：通过图语言，用户可以像搭积木一样组合预处理算子和神经元模块，极大地提高了开发效率。

### 7. 优点
*   **原生集成**：无需 Python 环境，避免了跨语言调用的内存开销和环境配置难题。
*   **统一语言**：将数据增强、预处理和模型架构整合在同一个 `mlr3pipelines` 图中，增强了实验的可重复性。
*   **自动化**：自动推断张量形状，减少了手动计算网络层维度的错误。

### 8. 不足与局限
*   **任务覆盖面**：目前主要侧重于监督学习（分类和回归），对生成模型（如 GAN、VAE）、强化学习或复杂的 NLP 任务（如 Transformer 预训练）支持尚处于早期阶段。
*   **生态丰富度**：虽然支持 `torchvision`，但相比 Python 生态中海量的预训练模型库（如 Hugging Face），R 语言可直接调用的成熟架构仍较少。
*   **学习曲线**：对于不熟悉 `mlr3` 或 `R6` 面向对象编程的用户，理解其内部的图逻辑和回调机制可能存在一定门槛。

（完）
