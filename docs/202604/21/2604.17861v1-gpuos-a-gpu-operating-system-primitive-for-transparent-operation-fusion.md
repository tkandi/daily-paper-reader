---
title: "GPUOS: A GPU Operating System Primitive for Transparent Operation Fusion"
title_zh: GPUOS：一种用于透明算子融合的 GPU 操作系统原语
authors: "Yiwei Yang, Xiangyu Gao, Yuan Zhou, Yuhang Gan, Yusheng Zheng, Andi Quinn"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17861v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 减少内核启动开销的GPU运行时JIT系统
tldr: 针对深度学习中大量小算子导致的内核启动开销瓶颈，本文提出GPUOS。它采用持久化内核架构，通过NVRTC实现运行时算子即时编译与注入，避免了频繁的内核启动。该系统支持任意张量形状并与PyTorch透明集成，在小算子密集型任务中实现了显著的性能提升，有效解决了推理和微批次训练中的效率问题。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17861v1/fig-001.webp\", \"caption\": \"Figure 2.Attention Throughput compared to FlashAttention and Pytorch Compiled\", \"page\": 7, \"index\": 1, \"width\": 488, \"height\": 285}, {\"url\": \"assets/figures/arxiv/2604.17861v1/fig-002.webp\", \"caption\": \"Figure 3.Multi-Process GPU Sharing Performance\", \"page\": 7, \"index\": 2, \"width\": 486, \"height\": 284}, {\"url\": \"assets/figures/arxiv/2604.17861v1/fig-003.webp\", \"caption\": \"Figure 4.MIG Simulated Partition Performance\", \"page\": 8, \"index\": 3, \"width\": 488, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2604.17861v1/fig-004.webp\", \"caption\": \"Figure 1. GPUOS Architecture\", \"page\": 5, \"index\": 4, \"width\": 994, \"height\": 366}]"
motivation: 深度学习中频繁的小算子内核启动开销严重影响了推理和微批次训练的执行效率。
method: 开发了一个基于持久化内核架构的运行时系统，通过NVRTC即时编译算子并利用函数指针表实现动态注入。
result: 在小算子密集型任务中，GPUOS相比原生PyTorch实现了最高15.3倍的性能加速。
conclusion: GPUOS通过消除内核启动开销并保持生态兼容性，为提升细粒度GPU计算任务的利用率提供了有效途径。
---

## 摘要
现代深度学习工作负载通常包含许多小型张量操作，特别是在推理、注意力机制和微批次训练中。在这些场景下，内核启动开销（kernel launch overhead）可能成为主要瓶颈，有时甚至超过实际计算时间。我们提出了 GPUOS，这是一个 GPU 运行时即时编译（JIT）系统，通过具有运行时算子注入功能的持久化内核架构来减少启动开销。GPUOS 运行一个单一的长生命周期 GPU 内核，持续处理来自主机管理的工作队列的任务，从而消除了重复的内核启动。为了支持多样化的操作，GPUOS 使用 NVIDIA NVRTC 在运行时即时编译算子，并通过设备函数指针表将其注入到运行中的内核中。这种设计使得算子更新无需重启内核或重新编译系统即可实现。GPUOS 引入了四个核心思想：(1) 带有原子任务队列的持久化工作内核；(2) 基于 NVRTC 和可重定位设备代码的运行时算子注入机制；(3) 用于安全并发算子更新的双槽别名方案；(4) 通过 TorchDispatch 实现的透明 PyTorch 集成，将微操作批处理为统一提交。该系统通过通用张量抽象支持任意张量形状、步长（strides）、数据类型和广播。实验表明，在以小型操作为主的工作负载（包括微批次推理和注意力模式）中，GPUOS 相比标准 PyTorch 实现了高达 15.3 倍的加速。GPUOS 在保持与 PyTorch 生态系统兼容的同时，提高了资源利用率。

## Abstract
Modern deep learning workloads often consist of many small tensor operations, especially in inference, attention, and micro-batched training. In these settings, kernel launch overhead can become a major bottleneck, sometimes exceeding the actual computation time.   We present GPUOS, a GPU runtime JIT system that reduces launch overhead using a persistent kernel architecture with runtime operator injection. GPUOS runs a single long-lived GPU kernel that continuously processes tasks from a host-managed work queue, eliminating repeated kernel launches. To support diverse operations, GPUOS uses NVIDIA NVRTC to just-in-time compile operators at runtime and inject them into the running kernel through device function pointer tables. This design enables operator updates without restarting the kernel or recompiling the system.   GPUOS introduces four key ideas: (1) a persistent worker kernel with atomic task queues, (2) a runtime operator injection mechanism based on NVRTC and relocatable device code, (3) a dual-slot aliasing scheme for safe concurrent operator updates, and (4) transparent PyTorch integration through TorchDispatch that batches micro-operations into unified submissions. The system supports arbitrary tensor shapes, strides, data types, and broadcasting through a generic tensor abstraction.   Experiments show that GPUOS achieves up to 15.3x speedup over standard PyTorch on workloads dominated by small operations, including micro-batched inference and attention patterns. GPUOS improves utilization while remaining compatible with the PyTorch ecosystem.

---

## 论文详细总结（自动生成）

### 论文总结：GPUOS - 用于透明算子融合的 GPU 操作系统原语

#### 1. 核心问题与研究动机
*   **核心问题**：在现代深度学习（尤其是推理、注意力机制和微批次训练）中，存在大量小型张量操作。传统的 GPU 计算模型为每个操作启动独立的内核（Kernel），这会产生显著的 **CPU-GPU 同步与内核启动开销**（通常为 3-7 微秒）。对于执行时间仅为微秒级的任务，这种开销可能占总响应时间的 50% 以上，成为系统瓶颈。
*   **研究动机**：现有的优化手段（如 CUDA Graphs）在面对动态形状、复杂控制流或频繁更新的模型时显得过于僵化。作者旨在建立一种既能消除启动开销，又能保持算子灵活性和透明集成能力的运行时系统。

#### 2. 方法论
GPUOS 的核心思想是将“内核启动”这一动作从热路径中移除，转而采用一种类似操作系统的持久化任务调度机制。
*   **持久化工作内核（Persistent Worker Kernel）**：在程序启动时仅启动一次内核，该内核常驻 GPU 并在各流处理器（SM）上运行。它通过轮询（Polling）设备内存中的环形缓冲区（Ring Buffer）来获取任务。
*   **运行时算子注入（Runtime Operator Injection）**：
    *   利用 **NVIDIA NVRTC** 对新算子进行即时编译（JIT）生成 PTX 代码。
    *   通过 **CUDA Driver API** 动态加载模块，并将函数指针写入设备端的“算子跳转表”。
*   **双槽别名方案（Dual-slot Aliasing）**：为了在不停止当前任务的情况下更新算子，系统维护两个版本的算子表，通过原子版本计数器实现无缝切换。
*   **透明 PyTorch 集成**：通过 `TorchDispatch` 拦截 PyTorch 操作，自动将符合条件的微操作（Micro-operations）聚合并提交至 GPUOS 队列，无需修改用户代码。
*   **通用张量抽象**：支持任意形状、步长、数据类型和广播语义。

#### 3. 实验设计
*   **硬件环境**：
    *   NVIDIA H100 (96GB) + Intel Xeon 6787P
    *   NVIDIA RTX 5090 (32GB) + Intel 285k
    *   NVIDIA Digit Spark (GB10 GPU, 128GB)
*   **对比基准（Benchmarks）**：
    *   **Eager PyTorch**：原生 PyTorch 2.2 模式。
    *   **TorchScript**：PyTorch 的 JIT 编译追踪模式。
    *   **CUDA Graphs**：手动捕获的静态图执行模式。
*   **测试场景**：
    *   **逐元素操作（Element-wise）**：100 个连续的小型张量操作。
    *   **注意力解码（Attention Decoding）**：逐 Token 生成的推理场景。
    *   **混合流水线（Mixed Pipelines）**：结合了注意力机制、FFN、归一化等动态控制流的真实推理负载。

#### 4. 资源与算力
*   **算力设备**：使用了顶级企业级 GPU（H100）、消费级旗舰 GPU（RTX 5090）以及下一代架构（GB10）。
*   **算力占用**：GPUOS 默认仅占用每个 SM 中的一个线程块（约总资源的 2-4%），以确保不干扰大型内核的执行。
*   **训练/执行时长**：论文未提及模型训练时长，重点在于推理阶段的算子执行延迟和吞吐量。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了微观基准测试、端到端推理吞吐量测试、多进程共享（MPS）性能测试以及 MIG（多实例 GPU）隔离环境下的扩展性测试。
*   **充分性**：实验对比了不同代际的硬件架构，并针对 CUDA Graphs 的弱点（形状变化）进行了专门对比。引入了能耗分析（20-22% 的节能），证明了该系统在性能之外的实际价值。整体实验设计较为全面，覆盖了从单算子到复杂流水线的多个维度。

#### 6. 主要结论与发现
*   **显著加速**：在 H100 上，针对小型算子密集型任务实现了 **15.3倍** 的加速；在注意力解码场景下实现 **8.7倍** 的提升。
*   **消除“空白期”**：通过持久化内核，GPU 时间线上的内核间隙（Whitespace）几乎消失，调度延迟从微秒级降至纳秒级。
*   **能效提升**：由于减少了 CPU-GPU 的频繁握手和等待，系统节省了约 **20-22%** 的能耗。
*   **硬件敏感性**：在 GB10 等具有更强硬件级启动优化（CPU-GPU fabric）的设备上，加速效果（2.8倍）虽不如旧架构显著，但依然存在优势。

#### 7. 优点
*   **透明性**：通过 PyTorch 插件形式实现，用户无需重写模型即可获得加速。
*   **灵活性**：支持运行时热插拔算子，解决了 CUDA Graphs 难以处理动态形状和新算子注入的问题。
*   **低开销调度**：将复杂的驱动层调度简化为设备端的函数指针查找，极大地压缩了长尾延迟。

#### 8. 不足与局限
*   **适用范围有限**：主要针对“小算子”密集型任务。对于计算密集型的大型矩阵乘法（GEMM），GPUOS 的优势不明显，甚至可能因占用少量 SM 资源而产生微小干扰。
*   **安全风险**：运行时代码注入（JIT）引入了潜在的安全攻击面，虽然文中提到了模板化编译和审计日志，但在多租户环境下的安全性仍需更严苛的验证。
*   **硬件依赖**：虽然支持多种 GPU，但其核心优化逻辑高度依赖 NVIDIA 的 NVRTC 和 Driver API，跨厂商（如 AMD/华为）的移植性未被讨论。

（完）
