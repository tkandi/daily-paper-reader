Title: mlr3torch: A Deep Learning Framework in R based on mlr3 and torch

URL Source: https://arxiv.org/pdf/2604.18152v1

Published Time: Tue, 21 Apr 2026 03:01:16 GMT

Number of Pages: 38

Markdown Content:
JSS Journal of Statistical Software 

MMMMMM YYYY, Volume VV, Issue II. doi: 10.18637/jss.v000.i00 

# mlr3torch : A Deep Learning Framework in R based on mlr3 and torch 

Sebastian Fischer 

LMU Munich MCML 

Lukas Burk 

LMU Munich MCML Leibniz Institute for Prevention Research and Epidemiology - BIPS University of Bremen 

Carson Zhang 

LMU Munich 

Bernd Bischl 

LMU Munich MCML 

Martin Binder 

LMU Munich MCML 

Abstract 

Deep learning (DL) has become a cornerstone of modern machine learning (ML) praxis. We introduce the R package mlr3torch , which is an extensible DL framework for the mlr3 

ecosystem. It is built upon the torch package, and simplifies the definition, training, and evaluation of neural networks for both tabular data and generic tensors (e.g., images) for classification and regression. The package implements predefined architectures, and 

torch models can easily be converted to mlr3 learners. It also allows users to define neu-ral networks as graphs. This representation is based on the graph language defined in 

mlr3pipelines and allows users to define the entire modeling workflow, including prepro-cessing, data augmentation, and network architecture, in a single graph. Through its integration into the mlr3 ecosystem, the package allows for convenient resampling, bench-marking, preprocessing, and more. We explain the package’s design and features and show how to customize and extend it to new problems. Furthermore, we demonstrate the package’s capabilities using three use cases, namely hyperparameter tuning, fine-tuning, and defining architectures for multimodal data. Finally, we present some runtime bench-marks. 

Keywords : deep learning, machine learning, R.

> arXiv:2604.18152v1 [stat.ML] 20 Apr 2026

2 mlr3torch : A Deep Learning Framework in R based on mlr3 and torch 

# 1. Introduction and related work 

The recent impact of deep learning (DL) across various fields, including computer vision, natural language processing, and, more recently, tabular data (Borisov et al. 2022), can hardly be overstated. To train neural networks efficiently, one needs GPU-accelerated tensor libraries, as well as automatic differentiation. However, many other mathematical algorithms also rely on tensor operations and gradients. Consequently, these toolkits are useful beyond the training of neural networks. One example for this is the fitting of Gaussian processes in the GPyTorch package (Gardner et al. 2018). The default language for DL is Python (Van Rossum and Drake Jr 1995) with its libraries 

TensorFlow (Abadi et al. 2016), PyTorch (Paszke et al. 2019), and JAX (Bradbury et al. 

2018). Initially, TensorFlow exclusively supported static execution, which relies on building up a computational graph that is just-in-time (JIT) compiled. This is great for deployment because it allows for more optimizations during the compilation phase. In contrast, PyTorch 

became popular because of its dynamic approach, which is often called eager execution. This allows for a simpler mental model because less is happening behind the scenes, comes with more flexibility, and allows for easier debugging. Nowadays, TensorFlow also supports eager execution, while PyTorch also allows for static compilation, which shows that both approaches have their strengths. Nonetheless, PyTorch is arguably the de facto standard for DL as of today. More recently, JAX introduced what can be described as a functional code transformation framework for NumPy programs (Harris et al. 2020), with the gradient being one such transformation. Another noteworthy Python library is tinygrad (tinygrad 

Team 2025), which strives to be a lightweight framework that can easily be extended to new hardware platforms. These libraries focus primarily on the core building blocks of DL and mostly leave other functionality, such as training loops, preprocessing, or data augmentation, to other frameworks. One example of such a framework is Keras (Chollet et al. 2018), which offers a more high-level API that is both integrated into TensorFlow but also available as its own multi-backend library with support for PyTorch , TensorFlow , and JAX . Furthermore, 

PyTorch Lightning (Falcon and The PyTorch Lightning Team 2019) and fastai offer higher-level frameworks for PyTorch , and Flax (Flax Team 2025) offers similar capabilities for JAX .Other languages than Python also have DL libraries. These include Julia (Bezanson et al. 

2017), with Flux (Innes 2018), Rust (Matsakis and Klock 2014) with its crates Burn (Simard 

et al. 2024) and Candle (Face 2025), and Go (Go Team 2025) with GoMLX (GoMLX Team 2025). In C++ there is LibTorch , the C++ backend for PyTorch , which can also be used directly in C++ .In the R language ( R Core Team 2025), many DL packages are available, but nowadays, most people are primarily using a few selected packages, all of which have corresponding counterparts in the Python ecosystem. One of these is torch (Falbel and Luraschi 2025), which is a native R package built directly on top of LibTorch and works similarly to PyTorch .Various packages exist that extend torch . One such extension is torchvision , which implements various pre-trained image models, datasets, and preprocessing operations (Falbel 2025b). Furthermore, luz (Falbel 2025a) and cito (Amesoeder et al. 2024) are two high-level DL libraries for R built on top of torch . Other popular packages are keras3 (Kalinowski et al. 

2025), tensorflow (Allaire and Tang 2024), and fastai (Abdullayev 2024), all of which wrap their corresponding Python libraries via reticulate (Ushey et al. 2025). The main advantage of torch over these reticulate wrappers is that it does not have the Python dependency, which Journal of Statistical Software 3therefore allows for easier debugging and requires less marshaling, as it does not require converting objects between R and Python . Also, the R package is less dependent on the development of the Python library, which can be an advantage, but also comes with a higher development cost for features that are not part of the C++ backend. Besides these general-purpose DL frameworks, more specialized libraries exist that focus on specific architectures or training procedures. This includes the packages nnet , neuralnet ,

deepnet , RSNNS (Venables and Ripley 2002; Fritsch et al. 2019; Rong 2022; Bergmeir and Benítez 2012). While these packages are good for simple use cases, they do not offer GPU acceleration and are less flexible in terms of model expressiveness. While h2o does allow for GPU training, it is similarly restricted with respect to the available architectures (Fryda et al. 

2024). In addition to specialized DL toolkits, there are other, more general-purpose machine learning (ML) libraries that primarily focus on tabular data. In Python , there is scikit-learn (Pedregosa 

et al. 2011), in Julia there is MLJ (Blaom et al. 2020), and R has tidymodels (Kuhn and Wickham 2020) and mlr3 (Lang et al. 2019) with their respective predecessors caret (Kuhn 2008) and mlr (Bischl et al. 2016), both of which are still widely used. From the perspective of these general ML frameworks, neural networks are just one type of learning algorithm, next to other methods like decision trees or support vector machines. Connector packages exist that integrate DL methods into their generic APIs. For example, the skorch package (Tietz 

et al. 2017) integrates PyTorch into scikit-learn , MLJFlux (MLJFlux.jl Team 2025) for Flux 

and MLJ , and brulee (Kuhn and Falbel 2025) for torch and tidymodels .The core idea of mlr3torch 1 is to extend the mlr3 ecosystem with DL capabilities in a way that feels familiar to its user base. For example, other ML models can be seamlessly replaced by DL methods, and out-of-memory data can be preprocessed almost like standard tabular data. Through this integration, mlr3torch comes with mlr3 ’s features – such as resampling, benchmarking, and hyperparameter tuning (Becker et al. 2025) – which is one of its key strengths. mlr3torch provides high-level abstractions, including training and evaluation loops, that considerably simplify the training of neural networks compared to directly using torch 

or tensorflow . Although cito offers similar functionality, its interface is simpler and less extensible. The same goes for brulee , which only supports relatively simple architectures. 

luz , by contrast, is currently more flexible than mlr3torch , as it allows users to more easily define custom training loops, e.g., to train variational autoencoders, whereas mlr3torch is currently focused on supervised learning, specifically regression and classification. However, in cases where mlr3torch is applicable, it arguably offers a richer feature set, including predefined tabular architectures and more evaluation measures. The R package most similar to mlr3torch 

is keras3 , which wraps the corresponding Python library and offers considerably more features than mlr3torch . For example, it also supports text processing, for which there is currently no proper support in mlr3torch . Nevertheless, being a Python wrapper, it comes with the disadvantages discussed earlier. Moreover, one advantage of mlr3torch over keras3 is its unified graph language that goes beyond neural networks. This, for example, allows combining neural networks with other ML models – such as gradient boosted trees – via stacking (Binder 

et al. 2024). The comparison to fastai is similar: as a Python wrapper, it has the strengths and limitations of building on a more mature Python library. However, both of these packages also do not offer the extensive hyperparameter tuning capabilities available for mlr3torch .

> 1https://github.com/mlr-org/mlr3torch

4 mlr3torch : A Deep Learning Framework in R based on mlr3 and torch 

Contributions The mlr3torch package adds DL capabilities to the mlr3 machine learning framework, which is a collection of R packages that provide a unified interface to ML in R.Currently, it supports working with tabular data and generic tensors in supervised classifica-tion and regression settings. Neural network architectures can be defined with different levels of customizability. First, there are predefined architectures, such as vision learners from the 

torchvision R package (Falbel 2025b), as well as tabular architectures (Gorishniy et al. 2021). It is also possible to define custom neural network modules in R torch and easily convert them to mlr3 Learner s. Furthermore, mlr3torch allows to define neural network architec-tures using the graph language defined in mlr3pipelines . This makes it possible to represent the entire ML pipeline, including preprocessing and data augmentation, as well as the neural network architecture, in a single graph. The package is also designed to be extendable. For example, the training logic can be customized via a callback mechanism, and one can inherit from predefined classes to provide custom method implementations. In the following sections, we provide an overview of the package. We start by summarizing the main R packages mlr3torch depends upon, as discussed in Section 2. Next, Section 3 will describe the package’s main building blocks. This includes the data representation, the 

Learner class, as well as the neural network Graph representation. Section 4 will show how to extend the package. Then, we will demonstrate the package’s capabilities using some practical use cases in Section 5. There, we will cover hyperparameter tuning, fine-tuning, and the construction of neural networks for multimodal problems. Afterwards, we will present some runtime benchmarks in Section 6. Finally, we will summarize the package and discuss future development in Section 7. Journal of Statistical Software 5

# 2. Background 

2.1. Deep learning 

Deep learning is a subset of machine learning which uses artificial neural networks to model relationships in data. These are highly expressive, overparameterized models that do not rely on manual feature engineering but instead learn the feature representation in an end-to-end manner. The simplest architecture is the multi-layer perceptron (MLP), which combines affine-linear transformations with nonlinear activation functions: 

fθ(x) = ϕL

(WL ϕL−1

(WL−1 · · · ϕ1(W1x + b1)) + bL−1

) + bL (1) with Wℓ ∈ Rdℓ×dℓ−1 , bℓ ∈ Rdℓ , θ = {W1, . . . , W L, b 1, . . . , b L} and ϕl : Rdl → Rdl for ℓ =1, . . . , L .Almost all neural network architectures can be represented as directed acyclic graphs (DAGs). Each node in the graph represents a – possibly parametrized – differentiable transformation that takes in one or more tensors as input and outputs one or more tensors. Edges go from layer outputs to layer inputs, defining the data flow between operations. One such graph, a simple transformer-based neural network, is depicted in Figure 1. First, the input (e.g., a text string) is split into tokens. These tokens are then converted to vector representations through an embedding layer. The output tensor is then fed into an attention module (Vaswani et al. 

2017) and subsequently added to the original tokenized input, in a technique commonly called a "skip connection" (He et al. 2016). Note that the addition layer has no learnable parameters. The output is then processed using layer normalization (Ba et al. 2016), followed by a linear layer, a Rectified Linear Unit (ReLU) activation (Glorot et al. 2011), and a dropout layer (Srivastava et al. 2014), the output of which is again added to the output of the normalization layer, creating another skip connection. The final linear layer is the network head. Input Tokenizer Embedding     

> Attention
> Add LayerNorm
> Linear ReLU Dropout
> Add Linear

Figure 1: Simple transformer-based neural network architecture represented as a graph. With fθ denoting the whole network, where θ represents the union of all the parameters of the individual layers, the objective of training a neural network is to find a parameter vector 

θ∗ that minimizes a loss function L given a data-generating process P.

θ∗ ∈ argmin θ∈Θ E(x,y )∼P [L(fθ(x), y )] (2) This solution is approximated by minimizing the empirical loss for a dataset D =

{

(x(i), y (i)}ni=1 

that was sampled from P. Typically, optimization is performed using gradient-based methods, such as stochastic gradient descent (SGD). We do not discuss the theoretical foundations of DL, because the topic is both too broad to cover in depth, and most readers will already be familiar with the basic concepts. For a more 6 mlr3torch : A Deep Learning Framework in R based on mlr3 and torch 

thorough coverage, please refer to Bishop and Bishop (2023). 

2.2. Main dependencies 

The mlr3torch R package builds upon the R packages mlr3 (Lang et al. 2019), mlr3pipelines 

(Binder et al. 2021), and torch (Falbel and Luraschi 2025) which we will now briefly introduce. The mlr3 framework offers a modern, feature-rich, and extensible framework for various ML tasks in the R programming language, including regression and classification. The ecosystem relies on R6 (Chang 2025) for most user-facing objects and hence uses reference semantics. At its core, mlr3 allows access to and application of many different ML algorithms implemented across different R packages through a unified interface. In the example below, we train a simple decision tree Learner from the rpart package (Therneau and Atkinson 2025) on the predefined mtcars example Task (Henderson and Velleman 1981), where the goal is to predict a car’s miles per gallon (mpg) based on its other features. We train the Learner on two-thirds of the data and then make predictions for the remaining observations. The predictions are then evaluated using the root mean squared error (RMSE) Measure .

R> library("mlr3") R> set.seed(42) R> task <- tsk("mtcars") R> learner <- lrn("regr.rpart") R> split <- partition(task, ratio = 2/3) R> learner$train(task, split$train) R> pred <- learner$predict(task, split$test) R> pred$score(msr("regr.rmse")) 

regr.rmse 4.736051 

The mlr3pipelines package extends the mlr3 ecosystem with support for complete learning pipelines. This allows composing new Learner s from individual steps such as preprocessing, postprocessing, and learning algorithms. The nodes in such a Graph are PipeOp s, which, just like the Learner class, have a train and predict method. Each PipeOp has one or more typed input channels and one or more typed output channels. Output channels can be connected to input channels of succeeding PipeOp s, and the types of these channels may differ between training and prediction. We can construct such a Graph by combining individual operations using the Graph concatenation operator %>>% . A Graph can be converted to a GraphLearner ,which inherits from mlr3::Learner . Below, we combine a principal component preprocessing step with the decision tree from above. 

R> library("mlr3pipelines") R> graph_learner <- as_learner(po("pca") %>>% lrn("regr.rpart")) 

To evaluate the performance of the above GraphLearner , we can resample it using a three-fold Cross-Validation Resampling and then score the predictions using RMSE. Journal of Statistical Software 7

R> resampling <- rsmp("cv", folds = 3) R> rr <- resample(task, graph_learner, resampling) R> rr$aggregate(msr("regr.rmse")) 

regr.rmse 4.274766 

The mlr3 ecosystem also contains many other components, including packages for hyperpa-rameter tuning, feature selection, or analysis tools. For an in-depth overview, we refer to our freely accessible book (Bischl et al. 2024). For the underlying tensor operations in mlr3torch , we are using torch , which comes with many GPU-accelerated tensor operations, a system for automatic differentiation, as well as various gradient-based optimization routines and other neural network abstractions. In the example below, we multiply two scalars and compute the gradient with respect to one of the inputs. 

R> library("torch") R> torch_manual_seed(42) R> x <- torch_tensor(1, device = "cpu") R> w <- torch_tensor(2, requires_grad = TRUE, device = "cpu") R> y <- w * x R> y$backward() R> w$grad 

torch_tensor 1[ CPUFloatType{1} ] 

# 3. Building blocks 

3.1. Data representation 

The lazy_tensor type 

In the mlr3 ecosystem, the ML problem and dataset are represented by the R6 class Task .A task contains the data itself, as well as metadata, such as the task type (e.g., regression or classification), the target column, and the feature names. To support not only standard tabular features, but also generic torch tensors, mlr3torch defines the lazy_tensor type, which represents a vector of tensors with possibly heterogeneous shapes. It is built upon the 

torch::dataset class, and therefore only needs to define how to obtain its elements, but not necessarily store them in memory – hence the name "lazy". We will demonstrate this data type using the MNIST digit classification example problem (Deng 2012), a supervised learning task where the goal is to categorize an image of a handwritten digit into one of the ten classes (0-9). 8 mlr3torch : A Deep Learning Framework in R based on mlr3 and torch 

R> library("mlr3torch") R> mnist <- tsk("mnist") R> mnist 

<TaskClassif> (70000x2): MNIST Digit Classification * Target: label * Target classes: 1 (11%), 7 (10%), 3 (10%), 2 (10%), 9 (10%), 0 (10%), 6 (10%), 8 (10%), 4 (10%), 5 (9%) * Properties: multiclass * Features (1): * lt (1): image 

The underlying data includes two columns, one of which is a factor vector containing the class labels, and the other is a lazy_tensor containing greyscale images with 28 × 28 pixels. 

R> rows <- mnist$data(1:2) R> rows 

label image <fctr> <lazy_tensor> 1: 5 <tnsr[1x28x28]> 2: 0 <tnsr[1x28x28]> 

We can call the materialize() function to convert a lazy_tensor to a list of torch_tensor s. 

R> str(materialize(rows$image)) 

List of 2 $ :Float [1:1, 1:28, 1:28] $ :Float [1:1, 1:28, 1:28] 

A lazy_tensor can be constructed from a torch::dataset using the as_lazy_tensor con-verter, which will be demonstrated in Section 5.2. 

Data preprocessing and augmentation 

A core feature of the lazy_tensor type is that it can be transformed by PipeOp s. Below, we flatten the images so they can later be fed into a simple MLP. 

R> po_flat <- po("trafo_reshape", shape = c(-1, 28 * 28)) R> mnist_flat <- po_flat$train(list(mnist))[[1L]] R> mnist_flat$head(2) 

label image <fctr> <lazy_tensor> 1: 5 <tnsr[784]> 2: 0 <tnsr[784]> Journal of Statistical Software 9The same reshaping would also be applied when calling the $predict() method of the PipeOp .Just like loading the data, these preprocessing steps are not applied eagerly, but lazily. They build up an internal preprocessing graph that will be executed when the lazy_tensor is materialized. Similarly, data augmentation steps are also available as PipeOp s, but their identifiers are prefixed with "augment_" instead of "trafo_" . The full list of currently available operators can be found on the package website 2. However, it is also relatively simple to create cus-tom ones via pipeop_preproc_torch . Both the transformation and augmentation PipeOp sinherit from the PipeOpTaskPreprocTorch . The only difference between them is that prepro-cessing PipeOp s apply their transformation during both the train and predict stage, whereas augmentation PipeOp s are only active during the former. 

3.2. The LearnerTorch class 

The LearnerTorch R6 class is the abstract base class that inherits from the mlr3::Learner 

class and defines the train and predict logic, currently only for supervised classification and regression. Each learner in mlr3torch inherits from this base class and additionally defines the network architecture, the hyperparameters, supported feature types, and other metadata, as well as how to construct the torch::dataset from the input Task . One simple example is LearnerTorchMLP , which represents a simple multi-layer perceptron with dropout. 

Configuration 

An mlr3torch Learner can be configured via construction arguments, hyperparameters, and fields. The standard construction arguments are: 

• loss :: TorchLoss , which encapsulates a torch::nn_loss and represents a loss function for training, 

• optimizer :: TorchOptimizer , which wraps a torch::optimizer and specifies how the network parameters are learned, and 

• callbacks :: list() of TorchCallback s, which customize the training process. All of the above objects can themselves be configured via their own hyperparameters, and while mlr3torch comes with predefined, ready-to-use losses, optimizers, and callbacks, its modular design allows for easy customization, as demonstrated in Section 4. Below, we construct a LearnerTorchMLP classifier which uses AdamW (Loshchilov and Hutter 2017) to minimize the cross-entropy loss and maintains the training history for inspection afterwards: 

R> mlp <- lrn("classif.mlp", + loss = t_loss("cross_entropy"), + optimizer = t_opt("adamw", lr = 0.001), + callbacks = t_clbk("history")) 

> 2https://mlr3torch.mlr-org.com/articles/preprocessing_list.html

10 mlr3torch : A Deep Learning Framework in R based on mlr3 and torch 

The hyperparameters of the Learner are represented as a ParamSet , as defined in the paradox 

package, which offers something like a type system for hyperparameter spaces (Lang et al. 

2024). It automatically verifies these types on assignment, but can also be used to sample from such spaces, which drives mlr3 ’s tuning extensions. The hyperparameter space of the constructed Learner is the union of the ParamSet s from the three construction arguments, some hyperparameters common to all LearnerTorch classes, and some learner-specific options that, e.g., parameterize the network architecture. To avoid name collisions, the loss, optimizer, and callback parameters are prefixed with "loss." ,

"opt." and "cb.<cb-id>." respectively. For the MLP, the learner-specific hyperparameters include the latent dimensions ( neurons ), the activation function ( activation ), and the dropout probability ( p). The parameter values can be set using the $set_values() method of the ParamSet . Below, we configure several hyperparameters, including the device, the number of epochs, the batch size, as well as the weight decay for the optimizer, and the measures we track during training. 

R> mlp$param_set$set_values( + neurons = c(100, 200), activation = torch::nn_relu, + p = 0.3, opt.weight_decay = 0.01, measures_train = msr("classif.logloss"), + epochs = 10, batch_size = 32, device = "cpu") 

Besides construction arguments, mlr3torch Learner s can be configured through fields, whose main difference from hyperparameters is that they have predefined semantics that are shared between all Learner s. These include the predict type ( $predict_type ), how to construct the validation data ( $validate ), whether to run the training and prediction in a separate process ( $encapsulation ), and which Learner to use as a $fallback , if the primary Learner 

fails. Below, we configure the Learner to make probability predictions via the $configure() 

setter, which can set fields and hyperparameters simultaneously. 

R> mlp$configure(predict_type = "prob") 

Training and prediction 

The primary way to use a LearnerTorch is through its $train() and $predict() methods. The former takes a Task as input and stores the trained model in the $model field. The 

$predict() method also takes a Task as input, but outputs an mlr3::Prediction object. To allow the code to easily run without a GPU, we train the MLP only on the first 1000 rows of the flattened MNIST task from above: 

R> mlp$train(mnist_flat, row_ids = 1:1000) 

After training, we can access the training results via the $model slot. Most importantly, the model contains the trained network (an nn_module ), the optimizer state, and the states of the callbacks. Journal of Statistical Software 11 

R> mlp$model$network 

An `nn_module ` containing 100,710 parameters. Modules * 0: <nn_linear> #78,500 parameters * 1: <nn_relu> #0 parameters * 2: <nn_dropout> #0 parameters * 3: <nn_linear> #20,200 parameters * 4: <nn_relu> #0 parameters * 5: <nn_dropout> #0 parameters * 6: <nn_linear> #2,010 parameters 

R> head(mlp$model$callbacks$history, n = 2) 

epoch train.classif.logloss <num> <num> 1: 1 4.266895 2: 2 1.296983 

After training, it can be used to make predictions on unseen data. Here, we evaluate the predictions using the classification error Measure .

R> pred <- mlp$predict(mnist_flat, row_ids = 1001:1100) R> pred$score(msr("classif.ce")) 

classif.ce 0.21 

Learner characteristics 

As with any other Learner , Learner s in mlr3torch are annotated with standardized meta-data that describe their capabilities. These are used internally for checking the validity of operations, but also provide valuable information to users. The available options for these characteristics are defined in the mlr3 base package, but they can be extended via a reflection mechanism. They include the task type, prediction types, feature types, and properties. 

• The task_type defines the type of Task on which the Learner operates. Currently, 

mlr3torch only supports regression and classification. 

• The predict_types indicates the types of predictions the Learner can make. mlr3torch 

regression Learner s can currently only make response predictions, while classifiers can make both response and probability predictions. 

• The feature_types specifies which features the Learner can handle. Here, mlr3torch 

extends the available types with the lazy_tensor .

• The properties are usually special abilities of a Learner , such as being able to perform feature selection, but can also be other traits. We will cover the selected properties 

"marshal" and "validation" , as well as "internal_tuning" below. 12 mlr3torch : A Deep Learning Framework in R based on mlr3 and torch 

Marshaling is the process of converting an in-memory object so that it can be (de)-serialized without loss of information. Learner s with this property require such processing before and after (de-)serialization. This is necessary for mlr3torch Learner s because their weights are torch_tensor s, which are external pointers that are not natively handled by 

R’s serialization mechanism. Before serializing an mlr3torch Learner , one must therefore 

$marshal() , and after deserialization call $unmarshal() .

R> pth <- tempfile() R> mlp$marshal() R> saveRDS(mlp, pth) R> mlp2 <- readRDS(pth) R> mlp2$unmarshal() 

Validation When training iterative learning procedures such as deep neural networks, one often wants to monitor the model’s performance during training. In mlr3 , this is possible through a standardized mechanism. Learners with this ability have the "validation" prop-erty. These Learner s have a $validate field through which one configures how to create the validation Task . One option is to set this field to the ratio of data to use for validation. 

R> set_validate(mlp, validate = 0.3) 

This setting is also taken into account by preprocessing PipeOp s, which apply apply their prediction logic to the validation data during training. 

Internal tuning While mlr3 has extensive support for standard offline hyperparameter tuning through packages like mlr3tuning (Becker et al. 2025), some learning algorithms can also optimize hyperparameters internally during training. For deep neural networks, this is possible for the epochs hyperparameter and is achieved through early stopping. Although internal tuning can be considered just another hyperparameter of a Learner , standardizing this property in the mlr3 base package allows for seamless combination with standard offline tuning. In Section 5.1, we will demonstrate how to do so. For an in-depth coverage of the validation and internal tuning mechanisms, refer to the relevant chapter in the mlr3 book (Fischer 2024). 

3.3. Neural networks as Graph s

In torch , neural networks are represented as nn_module s. To create a new model, one needs to define a constructor method that initializes the network weights and submodules, as well as a $forward() method that defines how the weights are used to transform inputs. This allows for great flexibility, but there is little structure to the architecture. Below, we create a simple feed-forward network with one latent layer and ReLU activation. Journal of Statistical Software 13 

R> nn_simple <- nn_module("nn_simple", + initialize = function(d_in, d_latent, d_out) { + self$linear1 = nn_linear(d_in, d_latent) + self$activation = nn_relu() + self$linear2 = nn_linear(d_latent, d_out) + }, + forward = function(x) { + x = self$linear1(x) + x = self$activation(x) + self$linear2(x) + }+ )

The resulting nn_simple is a module generator. Below, we create a specific instance of this architecture by providing the necessary arguments for the constructor. 

R> net <- nn_simple(10, 100, 1) 

We can use this neural network by simply calling it on a tensor, which will internally call the 

$forward method. 

R> net(torch_randn(1, 10)) 

torch_tensor 0.01 * -9.4603 [ CPUFloatType{1,1} ][ grad_fn = <AddmmBackward0> ] 

Module Graph 

While mlr3torch also allows the construction of Learner s from generic nn_module s (see Sec-tion 4.3.1), it also offers a systematic representation via directed acyclic graphs. This rep-resentation makes it easier to inspect or change individual components, for example. Such an architecture is built by assembling PipeOpModule s in an mlr3pipelines::Graph . Each 

PipeOpModule wraps an instantiated nn_module and the edges of the Graph define the data flow between the layers. Note that this uses Graph objects in an unusual way, as mlr3pipelines Graph s are usually used to define preprocessing or stacking pipelines that are executed exactly once per model training run, on the entire dataset. However, the Graph s built from PipeOpModule s encode only a single neural network forward pass and are often executed many times per training run on individual batches of samples. Below, we define a simple feed-forward network with an input dimension of 10 , a latent dimension of 100 , ReLU activation, and an output layer that returns a scalar value. Note that po("module_1", ...) is shorthand for po("module", id = "module_1", ...) and can represent an arbitrary torch module that is passed via the argument module .14 mlr3torch : A Deep Learning Framework in R based on mlr3 and torch 

R> module_graph <- po("module_1", module = nn_linear(10, 100)) %>>% + po("module_2", module = nn_relu()) %>>% + po("module_3", module = nn_linear(100, 1)) 

To perform a forward pass, we can call its $train() method. In this case, the method expects one input tensor and returns one output tensor. Currently, the $predict() phase of the Graph does nothing. To work with this Graph using a more familiar interface, we can convert it into an nn_graph 

which inherits from nn_module . When doing so, we only need to specify the input shapes. Here, NA indicates that the first (batch) dimension is of an unknown shape. The names of 

shapes_in are given by module_graph$input . Below, we evaluate the resulting module on two randomly generated observations. 

R> net <- nn_graph(module_graph, shapes_in = list(module_1.input = c(NA, 10))) R> net(torch_randn(2, 10)) 

torch_tensor -0.1218 -0.0636 [ CPUFloatType{2,1} ][ grad_fn = <AddmmBackward0> ] 

Generating Graph 

Although it is possible to manually define module graphs, it requires specifying auxiliary parameters (such as the input dimension of a linear layer), which is both tedious and unnec-essary, as they can be inferred from the shape of the input data. Therefore, one will usually create such a module Graph through another generating Graph , which is similar in spirit to the module generator from earlier. The latter primarily consists of PipeOpTorch objects, but it can also contain other components. The generating Graph for the above module Graph 

is specified below, where nn("<key>") is short for po("nn_<key>", id = "<key>") . Note that we neither specify the input dimension for the first layer nor the output dimension of the output layer. 

R> graph <- po("torch_ingress_ltnsr") %>>% + nn("linear", out_features = 10) %>>% nn("relu") %>>% nn("head") 

While the module Graph takes in tensors, the generating Graph operates on a Task during both the training and prediction stages. During training, the output type is a list contain-ing a ModelDescriptor that is initialized by the "torch_ingress_ltnsr" operator. The 

ModelDescriptor is the communication object between the different PipeOpTorch opera-tors. It contains the module Graph , the Task , data loading instructions, as well as other metadata. When the generating Graph is trained, the network layers ( nn("<key>") ) instan-tiate a PipeOpModule and attach it to the module Graph . Auxiliary parameters, such as the input dimension of a linear layer, are inferred from the metadata that includes the output shape of the current position in the built-up intermediate network architecture. Then, the 

PipeOpTorch computes the new output shapes and updates this and other metadata from the Journal of Statistical Software 15 

ModelDescriptor and returns it. Because the module Graph is a reference object, parallel branches in the generating Graph can modify the module Graph independently. Note that the weights of the neural network that is being built up are not yet fit to the data at this point. Different ingress operators exist for different feature types, including for numeric and cate-gorical features, as well as lazy tensors. An ingress operator is an entry point into a neural network and specifies how the input tensor for this specific entry point is generated from the Task during training. For instance, po("torch_ingress_categ") loads the categorical features from the Task and converts them into a label-encoded, integer-valued tensor. It is also possible to have more than one entry point into the neural network, as we will see later. Below, we will train the generating Graph on the flattened MNIST Task from earlier. 

R> md <- graph$train(mnist_flat)[[1L]] R> md 

<ModelDescriptor: 4 ops> * Ingress: torch_ingress_ltnsr.input: [(NA,784)] * Task: mnist [classif] * Callbacks: N/A * Optimizer: N/A * Loss: N/A * pointer: nn_head.output [(NA,10)] 

If we not only want to create the neural network, but also fit its weights, we need to at least configure the optimizer and loss function in the ModelDescriptor . We could also set some callbacks using po("torch_callbacks") .

R> graph <- graph %>>% + po("torch_loss", t_loss("cross_entropy")) %>>% + po("torch_optimizer", t_opt("adamw", lr = 0.001)) 

The actual training of the neural network happens via PipeOpTorchModel . This PipeOp 

takes in the ModelDescriptor , converts it to a LearnerTorchModel , and trains it on the 

Task stored in the ModelDescriptor . Through its hyperparameters, the PipeOp also allows specifying the generic LearnerTorch hyperparameters. 

R> graph <- graph %>>% po("torch_model_classif", epochs = 10, batch_size = 16) 

During the prediction phase of the above Graph , PipeOpTorch operators mostly forward the input Task , possibly after combining multiple ones via a PipeOpFeatureUnion . Only the 

PipeOpTorchModel will call the $predict() method of the LearnerTorchModel that was stored in its state during the training phase. This process is visualized in Figure 2. This mlr3pipelines::Graph can be converted to an mlr3::Learner , making it interoperable with all other components from the mlr3 ecosystem. 

R> glrn <- as_learner(graph) R> glrn$train(mnist_flat, row_ids = 1:1000) 16 mlr3torch : A Deep Learning Framework in R based on mlr3 and torch $train()                

> $predict()
> Train Task
> Predict Task Prediction
> torch_ ingress_num
> torch_ ingress_num
> -
> nn_linear
> nn_linear
> -
> nn_relu
> nn_relu
> -
> nn_head
> nn_head
> -
> torch_loss "mse"
> torch_loss "mse"
> -
> torch_opt "adamw"
> torch_opt "adamw"
> -
> torch_model _classif
> torch_model _classif
> LearnerTorch Model
> •Network:
> •Loss: -
> •Optimizer: -
> •Task: Train Task
> ModelDescriptor:
> •Network:
> •Loss: "mse"
> •Optimizer: -
> •Task: Train Task

Figure 2: Training and prediction phase for a generating Graph .The ParamSet of the resulting Learner contains the hyperparameters of all the individual 

PipeOp s that are part of its Graph . This means that they can be changed afterwards, but more importantly, they can easily be tuned via the mlr3tuning extension, see Section 5.1. 

Non-linear Graph s

In this section, we will show how to create more complex architectures than the single-layer MLP from earlier. To demonstrate the construction of non-linear graph structures, we will define a residual layer. We start by defining two independent branches for the linear and nonlinear paths, respectively. 

R> path_lin <- nn("linear_1") R> path_nonlin <- nn("linear_2") %>>% nn("relu") 

The two graphs can be combined using a merge operation, in this case, summation. The resulting graph segment is visualized in Figure 3a. 

R> residual_layer <- list(path_lin, path_nonlin) %>>% nn("merge_sum") 

Multi-input architectures 

It is also possible to create neural networks with more than one input (ingress). This is necessary for defining multimodal architectures. Here, we show how to build a tabular neural network that accepts both categorical and numeric inputs. First, we implement two independent entry points into the network. For the numeric path, we first select the numeric features. This is a standard PipeOp from mlr3pipelines and it simply outputs a modified Task . Next, po("torch_ingress_num") initializes the ModelDescriptor 

that represents the numeric network entry. Subsequently, we embed the scalar values into a 10-dimensional vector using nn("tokenizer_num") (Gorishniy et al. 2021). The pipeline for the categorical features is defined analogously. Journal of Statistical Software 17 

R> path_num <- po("select_1", selector = selector_type("numeric")) %>>% + po("torch_ingress_num") %>>% + nn("tokenizer_num", d_token = 10) R> path_categ <- po("select_2", selector = selector_type("factor")) %>>% + po("torch_ingress_categ") %>>% + nn("tokenizer_categ", d_token = 10) 

The two independent entry points can be combined via another merge operation, in this case by concatenation of tensors along the second dimension. The resulting network has two inputs, where the first one expects a floating-point tensor and the second one an integer-valued tensor representing the categorical features. Note that the ingress operators also define how the tabular columns from a Task are converted to their tensor representation, e.g., via one-hot encoding for factors. The graph structures are displayed in Figure 3b. 

R> graph <- list(path_num, path_categ) %>>% nn("merge_cat", dim = 2) nn_linear nn_relu 

> nn_linear
> nn_merge_sum

(a) Residual layer. select_num torch_ingress_num nn_tokenizer_num    

> select_categ torch_ingress_categ nn_tokenizer_categ
> nn_merge_cat

(b) Neural network graph with multiple inputs. 

Figure 3: More complex network segments. One also often wants to repeat a specific neural network segment multiple times. This can be achieved by using the PipeOpTorchBlock operator. This PipeOp takes in another Graph 

primarily consisting of PipeOpTorch objects. During training, the PipeOp will repeatedly attach the same network segment to the neural network. 

R> blocks <- nn("block", residual_layer, n_blocks = 5) 

As both the number of repetitions ( n_blocks ) as well as the hyperparameters of the wrapped 

Graph are exposed through the PipeOpBlock ’s ParamSet , both can be tuned over. 

# 4. Extending the package 

While the configuration options shown in the previous section already cover a lot of ground, it is also essential to be able to customize the training and prediction further. Consequently, 

mlr3torch allows users to customize losses, optimizers, callbacks, and complete Learner s. 

4.1. Loss and optimizer 

The loss and optimizer in mlr3torch are represented via the TorchLoss and TorchOptimizer 

classes. They wrap a loss generator (such as torch::nn_mse_loss ) and an optimizer gener-ator (e.g., torch::optim_sgd ) respectively and annotate them with meta-information, most 18 mlr3torch : A Deep Learning Framework in R based on mlr3 and torch 

importantly, a ParamSet . Constructing a custom loss or optimizer, therefore, requires under-lying class generators that are constructible via torch::nn_module and torch::optimizer .The resulting constructors can be converted to their mlr3torch representation via the func-tions as_torch_loss and as_torch_optimizer . Without specifying additional arguments, all the meta-information will be inferred as precisely as possible. It is also possible to provide more specific details, such as the types of hyperparameters and their valid ranges. Below, we create a custom torch loss function – the winsorized MSE – and convert it to its mlr3torch 

counterpart: 

R> nn_winsorized_mse <- nn_module(c("nn_winsorized_mse", "nn_loss"), + initialize = function(max_loss) { + self$max_loss <- max_loss + }, + forward = function(input, target) { + torch_clamp(nnf_mse_loss(input, target), max = self$max_loss) + }+ )R> tloss <- as_torch_loss(nn_winsorized_mse) R> tloss 

<TorchLoss:nn_winsorized_mse> nn_winsorized_mse * Generator: nn_winsorized_mse * Parameters: list() * Packages: torch,mlr3torch * Task Types: classif,regr 

This object could now be passed as construction argument loss when initializing a new 

LearnerTorch .

4.2. Callbacks 

The callback mechanism in mlr3torch allows hooking into the training loop without having to write everything from scratch. It is implemented via the R6 class TorchCallback , which wraps the CallbackSet class. Analogously to the optimizer and loss, the latter can be converted to the former via as_torch_callback() . The design of the callback mechanism is similar to how it is implemented in frameworks such as Keras or luz (Chollet et al. 2018; Falbel 2025a). A CallbackSet is a collection of hooks, which are invoked at specific points in the training process. The name of the method in the callback defines at which time in the training process it is called, such as on_after_backward . Information from the training loop is communicated to the callback via a ContextTorch , which stores the network, the current training batch, and many other objects. Different callback hooks from a CallbackSet can communicate with each other by storing information in the CallbackSet itself. A callback can also store information in the Learner after training by implementing the $state_dict and $load_state_dict 

methods. A TorchCallback object can be created via torch_callback . We demonstrate this below by implementing gradient clipping, which has two hyperparameters representing the maximally Journal of Statistical Software 19 allowed norm value and the type of norm. It only implements the hook that is run after the backward call. After training, it writes the history of norms into the state of the Learner for inspection. 

R> gradient_clipper <- torch_callback("gradient_clipper", + initialize = function(max_norm, norm_type) { + self$norms <- numeric() + self$max_norm <- max_norm + self$norm_type <- norm_type + }, + on_after_backward = function() { + norm <- nn_utils_clip_grad_norm_(self$ctx$network$parameters, + self$max_norm, self$norm_type) + self$norms <- c(self$norms, norm$item()) + }, + state_dict = function() { + self$norms + }, + load_state_dict = function(state_dict) { + self$norms = state_dict + }+ )

This object could now be passed to a LearnerTorch during construction and would limit the norms of the gradients to the specified upper bound. 

4.3. Learner s and task types 

When the construction of custom architectures, as shown in Section 3.3.3, is not enough, it is also possible to inherit from LearnerTorch . While it is in principle possible to overwrite the whole training and prediction logic (private methods $.train() and $.predict() ), it is usually sufficient to overwrite one or more of the following methods: 

• $initialize(id, loss, optimizer, callbacks, ...) , which allows defining learner-specific hyperparameters, as well as other metadata like the supported feature types or the task type. 

• $.network(task, param_vals) , which is the network constructor and must return an 

nn_module for the specified hyperparameters and input Task .

• $.dataset(task, param_vals) , which must construct a torch::dataset for the given input Task and hyperparameters. The dataset must return a named list, where x is a list of feature tensors, y is the target tensor, and .index contains the batch indices. The help page of LearnerTorch describes all of this in more detail. 20 mlr3torch : A Deep Learning Framework in R based on mlr3 and torch 

Custom Learner s for classification and regression 

If one only wants to customize the neural network architecture, it is usually not necessary to create a custom class. Instead, one can use the LearnerTorchModule class, which allows exactly this and has the following additional construction arguments: 

• module_generator , which must be an nn_module_generator that takes as construction argument a task and possibly other parameters. 

• ingress_tokens , which is a named list of TorchIngressToken s that define how the data is loaded from the input Task . The names must correspond to the arguments of the module_generator ’s $forward method. We showcase this using a simple MLP with two architecture parameters, namely the latent dimension and the network depth. Note that all auxiliary parameters are inferred from the 

task .

R> nn_ffn <- nn_module("nn_ffn", + initialize = function(task, latent_dim, n_layers) { + dims <- c(task$n_features, rep(latent_dim, n_layers), + length(task$class_names)) + modules <- unlist(lapply(seq_len(length(dims) - 1), function(i) { + if (i < length(dims) - 1) { + list(nn_linear(dims[i], dims[i + 1]), nn_relu()) + } else { + list(nn_linear(dims[i], dims[i + 1])) + }+ }), recursive = FALSE) + self$network <- do.call(nn_sequential, modules) + }, + forward = function(x) self$network(x) + )

Next, we need to define how a Task is converted to torch_tensor s during training and prediction. In our case, our model will load all numeric columns and concatenate them into a single tensor that is passed as argument x to the network. 

R> num_input <- list(x = ingress_num()) R> num_input 

$x Ingress: Task[selector_type(c("numeric", "integer"))] --> Tensor() 

Next, we create the LearnerTorchModule and set the network’s hyperparameters, which are part of the Learner ’s ParamSet .

R> lrn_ffn <- lrn("classif.module", + module_generator = nn_ffn, + ingress_tokens = num_input, + latent_dim = 100, n_layers = 5) Journal of Statistical Software 21 We could now proceed and train this object on an mlr3 classification Task with numeric features. 

Other task types 

Because the focus of mlr3torch is currently on supervised learning, extending the package to non-supervised task types would require more effort, including the definition of a custom 

Task class, implementation of the respective Measure s, and creating a new base class, e.g., 

LearnerTorchUnsupervised .

# 5. Use cases 

We will now demonstrate some of mlr3torch ’s features by tuning a neural network on a tabular regression problem (Section 5.1), finetuning a pre-trained image network from torchvision 

(Section 5.2) and constructing a network architecture for a problem that contains both tabular features and images (Section 5.3). 

5.1. Simple neural architecture search 

In this section, we will construct a tabular neural network and simultaneously tune its archi-tecture, optimizer, and number of epochs. This will be done using the predefined "mtcars" 

example problem (Henderson and Velleman 1981), which we have already used earlier. We have chosen this dataset because its small size allows tuning hyperparameters in little time on a CPU, not because it requires expensive tuning or a complex neural network. 

R> task <- tsk("mtcars") R> task 

<TaskRegr> (32x11): Motor Trends * Target: mpg * Properties: -* Features (10): * dbl (10): am, carb, cyl, disp, drat, gear, hp, qsec, vs, wt 

Defining the learning pipeline 

The input of our network architecture will be a single numeric tensor, so the first step in the learning pipeline is the initialization of a numeric network entry via PipeOpTorchIngressNum .

R> ingress <- po("torch_ingress_num") 

The subsequent PipeOp s define the layers of the neural network. In this example, we are using an architecture that repeats the same block multiple times. One such block consists of a linear transformation followed by an activation function and a dropout layer. We can allow different choices of activation functions via the ppl("branch") construct, which has a branch.selection parameter. In the example below, we allow for two choices, namely a ReLU and a sigmoid activation function. 22 mlr3torch : A Deep Learning Framework in R based on mlr3 and torch 

R> block <- nn("linear", out_features = 32) %>>% + ppl("branch", list(relu = nn("relu"), sigmoid = nn("sigmoid"))) %>>% + nn("dropout") 

This also demonstrates one of the strengths of mlr3torch : because PipeOpTorch operators are just PipeOp s, they can be combined with other operators from mlr3pipelines as long as their input and output types are compatible. To repeat the above-defined segment one or more times, we can use the PipeOpTorchBlock 

class, which we have already covered earlier. 

R> architecture <- nn("block", block) %>>% nn("head") 

Next, we set the loss function to MSE and the optimizer to AdamW. 

R> config <- po("torch_loss", loss = t_loss("mse")) %>>% + po("torch_optimizer", optimizer = t_opt("adamw")) 

The final step is the PipeOpTorchModelRegr , which takes in a ModelDescriptor and does the actual training: 

R> model <- po("torch_model_regr", device = "cpu", batch_size = 32) 

Next, we combine all pieces into a singe learning pipeline and convert it to a Learner .

R> pipeline <- ingress %>>% + architecture %>>% config %>>% model R> learner <- as_learner(pipeline) R> learner$id <- "custom_nn" 

The ParamSet of the GraphLearner is the union of the ParamSet s of the individual PipeOps s, over which we will now tune. 

Defining the search space 

In order to tune our Learner , we need to define the search space. We will tune the network’s latent dimension between 20 and 500 , repeat the layer between 1 and 5 times, use ReLU or sigmoid, and set the dropout ratio to a value in [0 .1, 0.9] . Furthermore, we search the optimizer’s learning rate in the interval [10 −4, 10 −1] on a logarithmic scale. 

R> library("mlr3tuning") R> learner$param_set$set_values( + block.linear.out_features = to_tune(20, 500), + block.n_blocks = to_tune(1, 5), + block.branch.selection = to_tune(c("relu", "sigmoid")), + block.dropout.p = to_tune(0.1, 0.9), + torch_optimizer.lr = to_tune(10^-4, 10^-1, logscale = TRUE)) Journal of Statistical Software 23 We will tune all the above hyperparameters in the standard offline way using random search. In contrast, we will tune the epochs hyperparameter using the learner-internal early stopping procedure that was covered in Section 3.2.3. By setting the $validate field to "test" , we will early stop on the test set of the resampling used for the offline hyperparameter tuning, i.e., the validation data of the tuning process. 

R> set_validate(learner, "test") 

Finally, we set the patience parameter for the early stopping, the maximum number of epochs, and the validation measure. By setting internal = TRUE for the epochs, we enable tuning of the epochs via early stopping. 

R> learner$param_set$set_values( + torch_model_regr.patience = 5, + torch_model_regr.measures_valid = msr("regr.mse"), + torch_model_regr.epochs = to_tune(upper = 100, internal = TRUE)) 

Running the tuning procedure 

As the tuning method, we use random search via the mlr3tuning extension (Becker et al. 

2025) for simplicity, but more complex optimization routines such as Bayesian optimization are also supported via the mlr3mbo extension (Schneider et al. 2025; Bischl et al. 2023). For the evaluation of the hyperparameters, we select holdout resampling (for demonstrative purposes) and use the internal validation score – which we configured via the measures_valid 

hyperparameter – as the tuning measure. We run the tuning loop for 30 iterations. 

R> ti <- tune( + tuner = tnr("random_search"), + resampling = rsmp("holdout"), + measure = msr("internal_valid_score", minimize = TRUE), + learner = learner, + term_evals = 30, + task = task) + pvals <- ti$result_learner_param_vals[1:6] R> cat(paste("*", names(pvals), "=", pvals, + collapse = "\n"), "\n") 

* block.n_blocks = 4 * block.linear.out_features = 248 * block.branch.selection = sigmoid * block.dropout.p = 0.861211954243481 * torch_optimizer.lr = 0.00310737353560635 * torch_model_regr.epochs = 44 

We could now re-fit our network on the whole data with the configuration found above to obtain our final model. 24 mlr3torch : A Deep Learning Framework in R based on mlr3 and torch 

5.2. Fine-tuning an image network 

Instead of starting with randomly initialized weights, fine-tuning is a common approach in DL, which starts with pre-trained networks and adjusts them to new data. This often allows the model to transfer some of the learned representations from the pre-training to the new task at hand. This both reduces training time and improves performance, especially if task-specific data is limited. In this section, we show how mlr3torch allows fine-tuning a pre-trained image classification network from the torchvision package. We will fine-tune the ResNet-18 model (He et al. 2016) on the dogs vs. cats dataset (Cukierski 2013), which is a binary image classification problem. 

Task definition 

As a first step, we have to construct the classification Task , the data for which we access via the torchdatasets package (Falbel 2024). 

R> library("torchdatasets") R> dogs_vs_cats_dataset("data", download = TRUE) 

Next, we create a lazy_tensor vector for the images. To do so, we create a custom 

torch::dataset class that stores the image paths and loads them from disk. Note that 

mlr3torch requires the $.getitem() method to return a named list of torch_tensor s. Dur-ing data retrieval, we also permute the first and third dimensions of the tensor, as torchvision 

models expect the RGB channels to be the first dimension. 

R> ds <- torch::dataset("dogs_vs_cats", + initialize = function(pths) { + self$pths <- pths + }, + .getitem = function(i) { + image <- torchvision::base_loader(self$pths[i]) + list(image = torch_tensor(image)$permute(c(3, 1, 2))) + }, + .length = function() { + length(self$pths) + }+ )

In order to initialize this dataset, we need to create a vector containing the image paths. 

R> paths <- list.files(file.path("data", "dogs-vs-cats/train"), + full.names = TRUE) R> dogs_vs_cats <- ds(paths) 

To convert this into a lazy_tensor , we can use the as_lazy_tensor converter. Here we specify the output shapes of the images to be NULL , which denotes that the items have varying shapes. Journal of Statistical Software 25 

R> lt <- as_lazy_tensor(dogs_vs_cats, list(image = NULL)) 

Next, we construct the target vector. In this example, this information is part of the file names. 

R> labels <- ifelse(grepl("dog\\.\\d+\\.jpg", paths), "dog", "cat") R> table(labels) 

labels cat dog 12500 12500 

Next, we construct a data.table (Barrett et al. 2025) that contains the feature and target column and convert it to an mlr3::Task .

R> tbl <- data.table(image = lt, class = labels) R> task <- as_task_classif(tbl, target = "class", id = "dogs_vs_cats") R> task 

<TaskClassif> (25000x2) * Target: class * Target classes: cat (positive class, 50%), dog (50%) * Properties: twoclass * Features (1): * lt (1): image 

Learning pipeline 

Besides fine-tuning, we will also integrate data augmentation and preprocessing PipeOps into our learning pipeline. Below, we define the data augmentation. 

R> augment <- po("augment_random_vertical_flip", p = 0.5) 

For preprocessing, we will reshape each image to size (3 , 224 , 224) , which is expected by the ResNet-18 architecture. By default, this is done using bilinear interpolation. 

R> preprocess <- po("trafo_resize", size = c(224, 224)) 

For better training performance, we will freeze all but the head of the network for the first three epochs and only then train the remaining parameters. We achieve this via the "unfreeze" 

callback and only need to specify which parameters to train from the beginning and when to unfreeze the other weights. The names of the weights (e.g., "fc.weight" ) are defined by the underlying nn_module and can, for example, be looked up in its source code. 

R> unfreezer <- t_clbk("unfreeze", + starting_weights = select_name(c("fc.weight", "fc.bias")), + unfreeze = data.table(epoch = 3, weights = select_all())) 26 mlr3torch : A Deep Learning Framework in R based on mlr3 and torch 

The final step is the construction of the ResNet-18, which has a parameter pretrained that determines whether to use the pre-trained weights. 

R> resnet <- lrn("classif.resnet18", + pretrained = TRUE, epochs = 5, device = "cpu", batch_size = 32, + opt.lr = 1e-4, measures_valid = msr("classif.acc"), + callbacks = list(unfreezer, t_clbk("history"))) 

By instructing the Learner to use the pre-trained weights, torchvision will download (and cache) those weights upon first use, and the mlr3torch Learner will replace the output layer of the network with a new head with the correct output dimension for the problem at hand. 

Training 

Now, we combine the three steps into a single new GraphLearner , configure its validation data, train it, and print the validation loss. We conduct the training on only a subset of the data – 100 rows from each class – to make the code run quickly. Due to the use of pretrained weights, the model quickly reaches a reasonable performance despite being trained on only a few examples. 

R> learner <- as_learner(augment %>>% preprocess %>>% resnet) R> learner$id <- "resnet" R> set_validate(learner, 1 / 3) R> learner$train(task, c(12400:12600)) R> learner$model$classif.resnet18$model$callbacks$history 

epoch valid.classif.acc <num> <num> 1: 1 0.4179104 2: 2 0.4477612 3: 3 0.8955224 4: 4 0.8955224 5: 5 0.9253731 

5.3. Multimodal data 

In this section, we will define a network architecture for the "melanoma" example task, where the goal is to predict whether a tumor is malignant or benign (Collaboration et al. 2020). The dataset contains both images, as well as three tabular features: the approximate age, location, and sex of the individual. Furthermore, the data is grouped by patient_id , which 

mlr3 will automatically respect during resampling. 

R> task <- tsk("melanoma") R> task Journal of Statistical Software 27 

<TaskClassif> (32701x5): Melanoma Classification * Target: outcome * Target classes: malignant (positive class, 2%), benign (98%) * Properties: twoclass, groups * Features (4): * fct (2): anatom_site_general_challenge, sex * int (1): age_approx * lt (1): image * Groups: patient_id 

Because malignant tumors are much rarer than benign ones, the class distribution is unbal-anced. 

R> table(task$truth()) 

malignant benign 581 32120 

Also, for some observations, the approximate age is missing. 

R> task$missings("age_approx") 

age_approx 44 

The network architecture we will create for this dataset will take one tensor input for the tabular features and one for the image, similarly to Figure 3b. We start by defining the segment for the tabular features, which consists of first selecting the tabular features, followed by sampling the missing values from their empirical distribution, and then encoding them one-hot. Then, we mark the entry point of the neural network and define a simple MLP that will process the tabular data. 

R> block_ffn <- nn("linear", out_features = 500) %>>% + nn("relu") %>>% nn("dropout") R> path_tabular <- po("select_1", + selector = selector_type(c("integer", "factor"))) %>>% + po("imputehist") %>>% + po("encode", method = "one-hot") %>>% + po("torch_ingress_num") %>>% + nn("block_1", block = block_ffn, n_blocks = 3) 

For the image column, less preprocessing is required, and we simply select the feature and then define three convolutional layers with max pooling and batch normalization, ending in a flattening operation. We keep the convolutional layers rather small in size, so the code can be run on a CPU in a reasonable amount of time. 28 mlr3torch : A Deep Learning Framework in R based on mlr3 and torch 

R> path_image <- po("select_2", selector = selector_name("image")) %>>% + po("torch_ingress_ltnsr", shape = c(NA, 3, 128, 128)) %>>% + nn("conv2d_1", out_channels = 16, kernel_size = 7, stride = 2, + padding = 3) %>>% + nn("batch_norm2d_1") %>>% + nn("relu_1") %>>% + nn("max_pool2d_1", kernel_size = 3, stride = 2, padding = 1) %>>% + nn("conv2d_2", out_channels = 32, kernel_size = 3, stride = 1, + padding = 1) %>>% + nn("batch_norm2d_2") %>>% + nn("relu_2") %>>% + nn("conv2d_3", out_channels = 64, kernel_size = 3, stride = 1, + padding = 1) %>>% + nn("batch_norm2d_3") %>>% + nn("relu_3") %>>% + nn("flatten") 

Next, we merge the two branches by concatenation and afterwards add one more dense layer with dropout, followed by the classification head. 

R> architecture <- list(path_tabular, path_image) %>>% + nn("merge_cat") %>>% nn("linear_1", out_features = 500) %>>% + nn("relu_4") %>>% nn("dropout_2") %>>% nn("head") 

Finally, we set the loss function, optimizer, and training arguments. Because the problem is highly imbalanced, we increase the weight of the positive class by 10 . For the optimizer, we use again AdamW, and we train for 4 epochs with a batch size of 32 .

R> model <- architecture %>>% + po("torch_loss", + t_loss("cross_entropy", class_weight = torch_tensor(10))) %>>% + po("torch_optimizer", t_opt("adamw", lr = 0.0005)) %>>% + po("torch_model_classif", epochs = 4, batch_size = 32, device = "cpu", + predict_type = "prob") 

As further measures to address the imbalanced class distribution, we upsample the minority class by a factor of 4 and use various data augmentation methods. 

R> preprocessing <- po("classbalancing", ratio = 4, reference = "minor", + adjust = "minor") %>>% + po("augment_random_horizontal_flip") %>>% + po("augment_random_vertical_flip") %>>% + po("augment_random_crop", size = c(128, 128), pad_if_needed = TRUE) + glrn <- as_learner(preprocessing %>>% model) 

Finally, we resample the pipeline on the melanoma data using a simple holdout resampling (which will respect the grouping variable patient_id ), and display the resulting ROC curve Journal of Statistical Software 29 in Figure 4 using mlr3viz (Lang et al. 2025; Wickham 2016). Again, we resample the learner on only 10% of the data to allow reproducing the code more easily. 

R> library("mlr3viz") R> glrn$id <- "multimodal" R> task_subset <- task$clone(deep = TRUE) R> subset <- partition(task_subset, ratio = 0.1)$train R> task_subset$filter(subset) R> rr <- resample(task_subset, glrn, rsmp("holdout")) R> autoplot(rr, type = "roc") 

Figure 4: ROC curve for the multimodal neural network evaluated using holdout resampling. 

# 6. Runtime evaluation 

The primary goal of this work is to provide a high-level interface to DL in R and the main computational tasks are handled by torch . The package primarily influencing the runtime performance is therefore torch and not mlr3torch . Nonetheless, training speed is an important consideration when deciding whether to adopt a package. Furthermore, we want to ensure that mlr3torch does not add a significant framework overhead when compared with a pure 

torch implementation. In this section, we present runtime benchmarks that compare a simple training loop using mlr3torch with an equivalent implementation in pure torch and PyTorch .We include PyTorch in the evaluation scenario, because it can, due to its popularity and heavy industry investment, be reasonably considered a best-case scenario for the runtime performance of a DL framework. Despite torch and PyTorch being built upon the same 

LibTorch C++ library, there is still a considerable engineering effort in the PyTorch "frontend" to make it run as fast as it does. 

6.1. Setup 

One main reason for the success of DL is the use of GPUs for their training, but small neural 30 mlr3torch : A Deep Learning Framework in R based on mlr3 and torch 

networks, for example for tabular data, can also be trained on CPUs. We therefore evaluate the runtime performance on both hardware backends. We also vary various aspects of the training setup, where the main computational steps are 

• the forward pass of the neural network, i.e., computing predictions for a given feature vector, as well as the loss calculation, 

• computing the gradients of the loss function w.r.t. the network’s parameters, 

• performing the optimization step to update the network weights. We train a simple MLP with ReLU activation, different numbers of layers, and latent di-mensions. Note that the number of floating point operations (FLOPs) scales linearly with the number of layers, but quadratically with the latent dimension. Moreover, increasing the number of layers causes the creation of more intermediate tensors that are managed by R and have to eventually be cleaned up by the R garbage collector. We also use two optimizers: a simple SGD and the more computationally intensive (but more commonly used) AdamW. In order to narrow the performance gap between torch and 

PyTorch , we have contributed various improvements to torch , most notably the addition of "ignite" optimizers in torch version 0.14.0. Instead of implementing the optimization step in 

R, they wrap the C++ implementation of the optimizers provided in LibTorch .Each training loop runs for 20 epochs on batches of size 32 from a synthetically generated regression dataset with 2000 observations and 1000 features. Note that we do not vary the number of features because it only influences the computation in the first layer. We also train the network for four epochs as a warm-up in order to mitigate the impact of constant overheads on our measurements, ensuring that our reported runtimes more accurately reflect long-running training scenarios. Every configuration is repeated 10 times, and the number of threads for the CPU measurements was set to 1. Further computational details can be found in Section A. 

6.2. Results 

We focus on the results for the ignite optimizers, as they are considerably faster than their corresponding R implementation and are therefore used in mlr3torch . The difference between the two versions is especially prevalent for the more expensive AdamW optimizer, where, on the GPU, the relative overhead for a latent size of 1000 ranges from around 1.6 for 0 latent layers to around 5 for 16 layers. For the same scenario using SGD, the overhead is only around 1.1. Usually, however, more complex optimizers like AdamW are used to train neural networks. Figure 5 displays the absolute time per batch and Figure 6 shows relative results for the ignite optimizer version. In all experiments, the results of torch and mlr3torch are relatively similar. Only when the neural network is relatively small is the relative overhead more noticeable, which is expected. When training on the GPU, the relative overhead of torch w.r.t. PyTorch diminishes when increasing the latent dimension. This is not always the case when increasing the number of hidden layers. For example, for latent dimensions 1000 and 3000, the absolute overhead also increases with more layers, but when the network becomes very large, all three implementa-tions are very similar. Surprisingly, mlr3torch is slightly faster than torch for the AdamW Journal of Statistical Software 31 

Figure 5: Runtime results for AdamW (left) and SGD (right) for GPU (top) and CPU (bot-tom). The facets correspond to different latent dimensions. The number of hidden layers is 0, 4, 8, 12, 16. The lines represent the median and the shaded area is defined by the upper and lower 10% quantile of the 10 repetitions. 

Figure 6: Median runtime of mlr3torch and torch relative to PyTorch .optimizer when there are many layers and the number of latent dimensions is 9000, but this is likely an artifact explained by some internal mechanism of torch or LibTorch .On the CPU, both the relative and absolute overhead of the R implementations is larger for SGD than it is for the more compute and memory-intensive AdamW optimizer. Also, the relative overhead for the AdamW optimizer decreases with an increase in the number of hidden layers, which is not the case for the SGD optimizer. This shows that there is some optimization potential to be explored for the latter. The benchmark also demonstrates the runtime advantage of the GPU over the CPU. When changing the latent dimension from 100 to 1000, this causes – due to the quadratic scaling – an approximately 100-fold increase in the number of FLOPs. However, the time per batch is relatively similar, due to the different hardware backends, indicating a speed-up of around two orders of magnitude when using a GPU. Overall, the runtime performance of mlr3torch and torch can be considered reasonable, espe-cially when focusing on the arguably most realistic case of training a neural network on the GPU using the AdamW optimizer. 32 mlr3torch : A Deep Learning Framework in R based on mlr3 and torch 

# 7. Conclusion 

In this work, we introduced mlr3torch , which equips mlr3 with DL capabilities. Through this integration, neural networks can be easily resampled, tuned, benchmarked, and more. The package’s functionality includes the definition, training, and evaluation of neural net-works. It works with both standard tabular data and generic tensors for supervised regression and classification problems. Neural network architectures are available in different levels of control: predefined architectures are readily available, torch modules can be easily converted to mlr3torch learners, and custom architectures can be defined as graphs. Through the in-tegration with mlr3pipelines , the whole modeling workflow can be defined in a unified graph language that encompasses preprocessing, data augmentation, network architecture, training configuration, and more. A key goal for the design of mlr3torch was to make it extensible. New architectures can easily be defined, the training process can be customized via callbacks, and it is also possible to extend the package to new task types. In this article, we showcased the package’s capabilities using three practical use cases. First, we performed a simple neural architecture search on a tabular regression problem. Second, we fine-tuned a pre-trained ResNet-18 model to distinguish cats and dogs. Finally, we demon-strated how to create, train, and evaluate a neural network architecture for a multimodal learning problem. Furthermore, the presented benchmarks demonstrated reasonable runtime overhead of mlr3torch 

compared to a pure torch implementation. Future development will focus on extending support to other learning tasks, such as survival analysis, adding new architectures, and further performance improvements. 

# Acknowledgements 

Sebastian Fischer is supported by the Deutsche Forschungsgemeinschaft (DFG, German Re-search Foundation) – 460135501 (NFDI project MaRDI). We also thank Florian Pfisterer for fruitful discussions on the development of the package. 

# References 

Abadi M, Barham P, Chen J, Chen Z, Davis A, Dean J, Devin M, Ghemawat S, Irving G, Isard M, et al. (2016). “TensorFlow : A System for Large-Scale Machine Learning.” In 

12th USENIX symposium on operating systems design and implementation (OSDI 16) , pp. 265–283. Abdullayev T (2024). fastai : Interface to ’fastai’ . doi:10.32614/CRAN.package.fastai . R

package version 2.2.2, URL https://CRAN.R-project.org/package=fastai .Allaire J, Tang Y (2024). tensorflow : R Interface to TensorFlow . R package version 2.16.0, URL https://CRAN.R-project.org/package=tensorflow .Amesoeder C, Hartig F, Pichler M (2024). “ cito : An R Package for Training Neural Networks using torch .” Ecography . doi:10.1111/ecog.07143 .Journal of Statistical Software 33 Ba JL, Kiros JR, Hinton GE (2016). “Layer Normalization.” 1607.06450 , URL https: //arxiv.org/abs/1607.06450 .Barrett T, Dowle M, Srinivasan A, Gorecki J, Chirico M, Hocking T, Schwendinger B (2025). 

data.table : Extension of ‘data.frame‘ . R package version 1.17.8, URL https://CRAN. R-project.org/package=data.table .Becker M, Lang M, Richter J, Bischl B, Schalk D (2025). mlr3tuning : Hyperparameter Optimization for mlr3 . R package version 1.4.0, URL https://CRAN.R-project.org/ package=mlr3tuning .Bergmeir C, Benítez JM (2012). “Neural Networks in R Using the Stuttgart Neural Network Simulator: RSNNS .” Journal of Statistical Software , 46 (7), 1–26. Bezanson J, Edelman A, Karpinski S, Shah VB (2017). “ Julia : A Fresh Approach to Numerical Computing.” SIAM review , 59 (1), 65–98. Binder M, Pfisterer F, Becker M, Wright MN (2024). “Non-sequential Pipelines and Tuning.” In B Bischl, R Sonabend, L Kotthoff, M Lang (eds.), Applied Machine Learning Using mlr3 

in R. CRC Press. URL https://mlr3book.mlr-org.com/non-sequential_pipelines_ and_tuning.html .Binder M, Pfisterer F, Lang M, Schneider L, Kotthoff L, Bischl B (2021). “ mlr3pipelines -Flexible Machine Learning Pipelines in R.” Journal of Machine Learning Research , 22 (184), 1–7. URL https://jmlr.org/papers/v22/21-0281.html .Bischl B, Binder M, Lang M, Pielok T, Richter J, Coors S, Thomas J, Ullmann T, Becker M, Boulesteix AL, et al. (2023). “Hyperparameter Optimization: Foundations, Algorithms, Best Practices, and Open Challenges.” Wiley Interdisciplinary Reviews: Data Mining and Knowledge Discovery , 13 (2), e1484. Bischl B, Lang M, Kotthoff L, Schiffner J, Richter J, Studerus E, Casalicchio G, Jones ZM (2016). “mlr : Machine Learning in R.” Journal of Machine Learning Research , 17 (170), 1–5. URL https://jmlr.org/papers/v17/15-066.html .Bischl B, Sonabend R, Kotthoff L, Lang M (eds.) (2024). Applied Machine Learning Using 

mlr3 in R. CRC Press. ISBN 9781032507545. URL https://mlr3book.mlr-org.com .Bishop CM, Bishop H (2023). Deep Learning: Foundations and Concepts . Springer Nature. Blaom AD, Kiraly F, Lienart T, Simillides Y, Arenas D, Vollmer SJ (2020). “MLJ : A Ju-lia Package for Composable Machine Learning.” arXiv preprint arXiv:2007.12285 . URL 

https://github.com/JuliaAI/MLJ.jl .Borisov V, Leemann T, Seßler K, Haug J, Pawelczyk M, Kasneci G (2022). “Deep Neural Networks and Tabular Data: A Survey.” IEEE transactions on neural networks and learning systems .Bradbury J, Frostig R, Hawkins P, Johnson MJ, Leary C, Maclaurin D, Necula G, Paszke A, VanderPlas J, Wanderman-Milne S, Zhang Q (2018). “ JAX : Composable Transformations of Python +NumPy Programs.” Github repository. URL https://github.com/jax-ml/ jax .34 mlr3torch : A Deep Learning Framework in R based on mlr3 and torch 

Chang W (2025). R6 : Encapsulated Classes with Reference Semantics . R package ver-sion 2.6.1, URL https://CRAN.R-project.org/package=R6 .Chollet F, et al. (2018). “Keras : The Python Deep Learning Library.” Astrophysics source code library , pp. ascl–1806. Collaboration ISI, et al. (2020). “Siim-isic 2020 Challenge Dataset.” International Skin Imag-ing Collaboration .Cukierski W (2013). “Dogs vs. Cats.” https://kaggle.com/competitions/dogs-vs-cats .Kaggle. Deng L (2012). “The MNIST Database of Handwritten Digit Images for Machine Learning Research.” IEEE Signal Processing Magazine , 29 (6), 141–142. Face H (2025). Candle Software, Version 0.9.1 . URL https://github.com/huggingface/ candle .Falbel D (2024). torchdatasets : Ready to Use Extra Datasets for torch . R package ver-sion 0.3.1, URL https://CRAN.R-project.org/package=torchdatasets .Falbel D (2025a). luz : Higher Level API for torch . R package version 0.5.1, URL https: //CRAN.R-project.org/package=luz .Falbel D (2025b). torchvision : Models, Datasets and Transformations for Images . R package version 0.7.0, URL https://torchvision.mlverse.org .Falbel D, Luraschi J (2025). torch : Tensors and Neural Networks with ’GPU’ Acceleration .

R package version 0.16.0, URL https://torch.mlverse.org/docs .Falcon W, The PyTorch Lightning Team (2019). “ PyTorch Lightning .” doi:10.5281/zenodo. 3828935 . URL https://github.com/Lightning-AI/lightning .Fischer S (2024). “Predict Sets, Validation and Internal Tuning.” In B Bischl, R Son-abend, L Kotthoff, M Lang (eds.), Applied Machine Learning Using mlr3 in R.CRC Press. URL https://mlr3book.mlr-org.com/predict_sets,_validation_and_ internal_tuning_(+).html .Fritsch S, Guenther F, Wright MN (2019). neuralnet : Training of Neural Networks . doi: 10.32614/CRAN.package.neuralnet . R package version 1.44.2, URL https://CRAN. R-project.org/package=neuralnet .Fryda T, LeDell E, Gill N, Aiello S, Fu A, Candel A, Click C, Kraljevic T, Nykodym T, Aboyoun P, Kurka M, Malohlava M, Poirier S, Wong W (2024). h2o : R Interface for the ’H2O’ Scalable Machine Learning Platform . doi:10.32614/CRAN.package.h2o . R package version 3.44.0.3, URL https://CRAN.R-project.org/package=h2o .Gardner J, Pleiss G, Weinberger KQ, Bindel D, Wilson AG (2018). “ GPyTorch : Blackbox Matrix-Matrix Gaussian Process Inference with GPU Acceleration.” Advances in neural information processing systems , 31 .Journal of Statistical Software 35 Glorot X, Bordes A, Bengio Y (2011). “Deep Sparse Rectifier Neural Networks.” In Proceedings of the fourteenth international conference on artificial intelligence and statistics , pp. 315– 323. Journal of machine learning research Workshop and Conference Proceedings. Gorishniy Y, Rubachev I, Khrulkov V, Babenko A (2021). “Revisiting Deep Learning Models for Tabular Data.” Advances in neural information processing systems , 34 , 18932–18943. Harris CR, Millman KJ, Van Der Walt SJ, Gommers R, Virtanen P, Cournapeau D, Wieser E, Taylor J, Berg S, Smith NJ, et al. (2020). “Array Programming with NumPy .” Nature ,

585 (7825), 357–362. He K, Zhang X, Ren S, Sun J (2016). “Deep Residual Learning for Image Recognition.” In Proceedings of the IEEE conference on computer vision and pattern recognition , pp. 770–778. Henderson HV, Velleman PF (1981). “Building Multiple Regression Models Interactively.” 

Biometrics , pp. 391–411. Innes M (2018). “Flux : Elegant Machine Learning with Julia .” Journal of Open Source Software , 3(25), 602. Kalinowski T, Allaire J, Chollet F (2025). keras3 : R Interface to Keras . R package ver-sion 1.3.0, URL https://CRAN.R-project.org/package=keras3 .Kuhn M (2008). “Building Predictive Models in R Using the caret Package.” Jour-nal of Statistical Software , 28 (5), 1–26. doi:10.18637/jss.v028.i05 . URL https: //www.jstatsoft.org/index.php/jss/article/view/v028i05 .Kuhn M, Falbel D (2025). brulee : High-Level Modeling Functions with torch . R package version 0.4.0, URL https://CRAN.R-project.org/package=brulee .Kuhn M, Wickham H (2020). tidymodels : A Collection of Packages for Modeling and Machine Learning using tidyverse Principles . URL https://www.tidymodels.org .Lang M, Binder M, Richter J, Schratz P, Pfisterer F, Coors S, Au Q, Casalicchio G, Kotthoff L, Bischl B (2019). “ mlr3 : A Modern Object-oriented Machine Learning Framework in R.” 

Journal of Open Source Software , 4(44). doi:10.21105/joss.01903 .Lang M, Bischl B, Richter J, Sun X, Binder M (2024). paradox : Define and Work with Parameter Spaces for Complex Algorithms . R package version 1.0.1, URL https://CRAN. R-project.org/package=paradox .Lang M, Schratz P, Sonabend R, Becker M, Richter J, Zobolas J (2025). mlr3viz : Visualiza-tions for mlr3 . R package version 0.10.1, URL https://CRAN.R-project.org/package= mlr3viz .Loshchilov I, Hutter F (2017). “Decoupled Weight Decay Regularization.” arXiv preprint arXiv:1711.05101 .Matsakis ND, Klock FS (2014). “The Rust Language.” In Proceedings of the 2014 ACM SIGAda annual conference on High integrity language technology , pp. 103–104. 36 mlr3torch : A Deep Learning Framework in R based on mlr3 and torch 

Paszke A, Gross S, Massa F, Lerer A, Bradbury J, Chanan G, Killeen T, Lin Z, Gimelshein N, Antiga L, et al. (2019). “ PyTorch : An Imperative Style, High-Performance Deep Learning Library.” Advances in neural information processing systems , 32 .Pedregosa F, Varoquaux G, Gramfort A, Michel V, Thirion B, Grisel O, Blondel M, Pretten-hofer P, Weiss R, Dubourg V, Vanderplas J, Passos A, Cournapeau D, Brucher M, Perrot M, Duchesnay E (2011). “ Scikit-learn : Machine Learning in Python .” Journal of Machine Learning Research , 12 , 2825–2830. URL http://jmlr.org/papers/v12/pedregosa11a. html .

Flax Team (2025). Flax Software, Version 0.11.2 . URL https://github.com/google/flax .

GoMLX Team (2025). GoMLX Software, Version 0.22.1 . URL https://github.com/gomlx/ gomlx .

MLJFluxjl Team (2025). MLJFlux.jl Software, Version 0.6.6 . URL https://github.com/ FluxML/MLJFlux.jl .

tinygrad Team (2025). tinygrad Software, Version 0.11.0 . URL https://github.com/ tinygrad/tinygrad .

Go Team (2025). Go Software, Version 1.25 . URL https://go.dev/ .

R Core Team (2025). R: A Language and Environment for Statistical Computing . R Founda-tion for Statistical Computing, Vienna, Austria. URL https://www.R-project.org/ .Rong X (2022). deepnet : Deep Learning Toolkit in R. doi:10.32614/CRAN.package. deepnet . R package version 0.2.1, URL https://CRAN.R-project.org/package=deepnet .Schneider L, Richter J, Becker M, Lang M, Bischl B, Pfisterer F, Binder M, Fischer S (2025). 

mlr3mbo : Flexible Bayesian Optimization . R package version 0.3.1, URL https://CRAN. R-project.org/package=mlr3mbo .Simard N, Fortier-Dubois L, Tadjibaev D, Lagrange G, Burn Framework Contributors (2024). “Burn .” URL https://github.com/tracel-ai/burn .Srivastava N, Hinton G, Krizhevsky A, Sutskever I, Salakhutdinov R (2014). “Dropout: A Simple way to Prevent Neural Networks from Overfitting.” The journal of machine learning research , 15 (1), 1929–1958. Therneau T, Atkinson B (2025). rpart : Recursive Partitioning and Regression Trees . R

package version 4.1.24, URL https://CRAN.R-project.org/package=rpart .Tietz M, Fan TJ, Nouri D, Bossan B, skorch Developers (2017). skorch : A scikit-learn Com-patible Neural Network Library that wraps PyTorch . URL https://skorch.readthedocs. io/en/stable/ .Ushey K, Allaire J, Tang Y (2025). reticulate : Interface to Python . R package version 1.43.0, URL https://CRAN.R-project.org/package=reticulate .Van Rossum G, Drake Jr FL (1995). “ Python Tutorial.” Technical Report CS-R9526 , Centrum voor Wiskunde en Informatica, Amsterdam, The Netherlands. Journal of Statistical Software 37 Vaswani A, Shazeer N, Parmar N, Uszkoreit J, Jones L, Gomez AN, Kaiser Ł, Polosukhin I (2017). “Attention is all you Need.” Advances in neural information processing systems ,

30 .Venables WN, Ripley BD (2002). Modern Applied Statistics with S. Fourth edition. Springer Verlag, New York. ISBN 0-387-95457-0, URL https://www.stats.ox.ac.uk/pub/MASS4/ .Wickham H (2016). ggplot2 : Elegant Graphics for Data Analysis (1st ed.) . Springer-Verlag New York. ISBN 978-3-319-24277-4. URL https://ggplot2.tidyverse.org .38 mlr3torch : A Deep Learning Framework in R based on mlr3 and torch 

# A. Reproducibility 

All relevant files for reproducing the results from the paper are provided in the ./paper subdi-rectory of the software’s GitHub repository: https://github.com/mlr-org/mlr3torch . It contains a detailed README on how to reproduce the results. For reproducibility purposes, we provide two linux Docker images – one for GPU (CUDA) and one for CPU – that can be downloaded from Zenodo: https://doi.org/10.5281/zenodo. 17130368 . They are primarily intended for reproducing the results from the runtime bench-mark, as setting up PyTorch and torch , especially using CUDA – can be tricky, but we also used the CPU Docker image for generating the results shown in the paper. Note that 

torch comes with its own BLAS implementation, so R being installed with the default BLAS does not hurt performance. The ./paper/envs directory in the git repository contains the Dockerfiles, as well as an renv.lock file that can be restored to run the code without Docker. All experiments were run on Linux. Code requiring access to a GPU was run on an NVIDIA HGX A100 with 80 GB VRAM. The CPU was an Intel(R) Xeon(R) Gold 6336Y CPU @ 2.40GH with 1 TB RAM and 48 physical cores. The CPU benchmark was run on an Intel(R) Xeon(R) Gold 6226R CPU @ 2.90GHz with 32 physical cores and 128 GB RAM. We were able to exactly reproduce the results from the paper (excluding the runtime bench-mark) throughout multiple runs on the same Linux machine using the CPU Docker image. However, when compared to the results on an M1 MacBook, there were minor differences for some of the examples, presumably due to differences in the LibTorch kernel implementations on the two different operating systems and architectures (ARM64 vs. x86-64). For a discus-sion on reproducibility in PyTorch , which uses the same LibTorch library as torch , see https: //docs.pytorch.org/docs/stable/notes/randomness.html#reproducibility . The run-time benchmark is not exactly reproducible, but repetitions on the same (not identical) hard-ware yielded rather similar with some minor differences. When run on different hardware, the differences in runtime measurements were non-negligible, but still compatible with the conclusions drawn in this paper. 

Affiliation: 

Sebastian Fischer Insitut für Statistik Ludwig-Maximilians-Universität München, Germany Ludwigstr. 33, 80539 Munich, Germany E-mail: sebastian.fischer@stat.uni-muenchen.de 

## Journal of Statistical Software http://www.jstatsoft.org/ 

published by the Foundation for Open Access Statistics http://www.foastat.org/ 

MMMMMM YYYY, Volume VV, Issue II Submitted: yyyy-mm-dd 

doi:10.18637/jss.v000.i00 Accepted: yyyy-mm-dd