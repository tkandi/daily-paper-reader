---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v3"
tags: ["query:mlsys"]
score: 9.0
evidence: AI智能体统一训练与推理框架
tldr: "针对记忆增强型大模型智能体开发中存在的框架碎片化问题，本文推出了首个统一且高度模块化的训练与推理框架 MemFactory。该框架采用“乐高式”架构，将记忆的提取、更新和检索等生命周期抽象为可插拔组件，并原生集成 GRPO 算法以优化内存管理策略。MemFactory 支持多种前沿范式，在 MemAgent 架构上的实验证明其能显著提升模型性能，最高增益达 14.8%，为长短期记忆智能体的研究提供了标准化的基础设施。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v3/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2603.29493v3/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 219}]"
motivation: 旨在解决记忆增强型智能体在集成、训练和评估过程中缺乏统一且模块化基础设施的现状。
method: 通过抽象记忆生命周期为原子化组件，并结合 GRPO 强化学习算法，实现对内存管理策略的自动化微调。
result: "实验验证表明，MemFactory 在开源架构上相比基准模型实现了最高 14.8% 的相对性能提升。"
conclusion: MemFactory 显著降低了记忆驱动型 AI 智能体的开发门槛，为未来长效智能体的创新奠定了基础。
---

## 摘要
记忆增强的大语言模型（LLMs）对于开发具备长期能力的 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为了填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以在多维环境奖励的驱动下微调内部记忆管理策略。MemFactory 为 Memory-R1、RMM 和 MemAgent 等近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在各评估集上，MemFactory 相比相应的基座模型平均提升了性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为未来记忆驱动型 AI 智能体的创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across the evaluation sets, MemFactory improves performance over the corresponding base models on average, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

### MemFactory: 智能体记忆统一推理与训练框架论文总结

#### 1. 论文的核心问题与整体含义
*   **研究动机**：虽然记忆增强型大语言模型（LLMs）在长程对话和个性化交互中至关重要，但目前的记忆管理（提取、更新、检索）多依赖启发式规则，难以处理冗余和冲突。
*   **核心问题**：近期研究开始利用强化学习（RL）优化记忆策略（如 Memory-R1, MemAgent），但这些实现高度**碎片化**、**任务特定**且**难以复用**。开发者缺乏一个像 LLaMA-Factory 那样统一、模块化且易于使用的基础设施来构建、训练和评估记忆驱动的智能体。
*   **整体含义**：本文推出了 **MemFactory**，这是首个专为记忆增强智能体设计的统一训练与推理框架，旨在通过“乐高式”的模块化设计降低 Memory-RL 研究的门槛。

#### 2. 论文提出的方法论
MemFactory 采用了四层解耦架构，核心思想是将复杂的记忆生命周期拆解为原子操作：
*   **模块层 (Module Layer)**：定义了记忆的原子操作，包括 **Extractor**（提取事实）、**Updater**（执行 ADD/DEL/UPDATE 等 CRUD 操作）、**Retriever**（语义检索或重排序）以及 **Agent Module**（端到端递归记忆处理）。
*   **智能体层 (Agent Layer)**：作为策略执行器，允许用户通过组合不同的模块来构建自定义智能体（如组合 Reranker 和特定的 Updater）。
*   **环境层 (Environment Layer)**：负责数据加载和奖励管理。支持 `MemoryBankEnv`（长期记忆库）和 `LongcontextEnv`（短期长文本记忆），并提供格式奖励和 LLM-as-a-Judge 等多维奖励信号。
*   **训练层 (Trainer Layer)**：核心优化引擎，原生集成 **GRPO (Group Relative Policy Optimization)** 算法。
    *   **关键技术细节**：GRPO 通过在一组采样响应中进行奖励归一化来计算优势（Advantage），无需额外的 Critic 网络，极大地降低了长文本训练时的显存开销。

#### 3. 实验设计
*   **数据集/场景**：使用了 **MemAgent** 公开的训练和评估数据集。
*   **Benchmark**：
    *   `eval_50` 和 `eval_100`：主任务测试集（包含不同数量的文档上下文）。
    *   `eval_fwe_16384`：分布外（OOD）测试集，用于验证策略的泛化能力。
*   **对比方法**：以 **Qwen3-1.7B** 和 **Qwen3-4B-Instruct** 作为基座模型（Base Checkpoint），对比经过 MemFactory RL 微调后的性能提升。
*   **评估指标**：采用 `avg@4`（每道题进行 4 次独立试验并取平均分），以减少生成随机性的影响。

#### 4. 资源与算力
*   **硬件设备**：论文明确指出，整个训练和评估流水线可以在 **单张 NVIDIA A800 80GB GPU** 上运行。
*   **训练细节**：每个模型训练了 **250 个有效步骤**。
*   **效率体现**：通过简化训练样本（将上下文长度缩减至原数据集的约 1/3，即每样本 50-80 个文档），在有限算力下实现了有效的策略优化。

#### 5. 实验数量与充分性
*   **实验规模**：针对两种不同规模的基座模型（1.7B 和 4B）进行了实验，涵盖了从基础模型到指令微调模型的跨度。
*   **充分性评价**：
    *   **客观性**：通过 `avg@4` 多次采样评估，增加了结果的可信度。
    *   **覆盖面**：实验不仅关注主任务性能，还引入了 OOD 数据集来测试模型的鲁棒性。
    *   **公平性**：使用了公开的第三方数据集和标准的强化学习指标，实验设计较为严谨。

#### 6. 论文的主要结论与发现
*   **性能提升显著**：MemFactory 显著提升了基座模型的记忆管理能力。Qwen3-1.7B 的平均性能提升了 **14.8%**；Qwen3-4B-Instruct 提升了 **7.3%**。
*   **模型规模效应**：较大的模型（4B）在 OOD 任务上表现出更强的泛化能力，而较小模型（1.7B）虽然在主任务提升巨大，但在 OOD 任务上略有下降。
*   **框架有效性**：证明了通过 GRPO 优化递归记忆策略（Recurrent Memory Policy）在单卡环境下是可行且高效的。

#### 7. 优点
*   **高度模块化**：像“乐高”一样组装记忆组件，极大方便了研究人员复现和改进 SOTA 算法（如 Memory-R1, RMM）。
*   **显存友好**：原生支持 GRPO 和 FlashAttention-2，使得在消费级或单卡企业级 GPU 上训练长文本智能体成为可能。
*   **开箱即用**：预置了多种前沿记忆范式的实现，降低了从零开发的工程负担。

#### 8. 不足与局限
*   **范式覆盖有限**：目前仅集成了三种代表性范式，对于更复杂的分布式记忆或多模态记忆支持尚待开发。
*   **训练效率空间**：虽然支持单卡训练，但在处理极长上下文（如 128k+）时的吞吐量和并行优化仍有提升空间。
*   **评估依赖**：实验部分较为依赖 MemAgent 的数据集，未来需在更多样化的记忆任务（如长程规划、复杂推理）上验证框架的通用性。

（完）
