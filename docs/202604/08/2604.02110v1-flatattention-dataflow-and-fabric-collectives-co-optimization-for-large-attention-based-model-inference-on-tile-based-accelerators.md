---
title: "FlatAttention: Dataflow and Fabric Collectives Co-Optimization for Large Attention-Based Model Inference on Tile-Based Accelerators"
title_zh: FlatAttention：针对基于瓦片加速器的大型注意力模型推理的数据流与织物集合通信协同优化
authors: "Chi Zhang, Luca Colagrande, Renzo Andri, Luca Benini"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02110v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 大模型推理的基础设施与数据流优化
tldr: 针对大模型（如MoE）推理中Attention计算占比增加及HBM带宽瓶颈问题，本文提出FlatAttention。这是一种专为瓦片式（Tile-based）加速器设计的并行数据流，通过协同优化数据流与片上网络集合通信原语，显著减少了HBM访问。在多种Attention变体和DeepSeek-v3模型上，该方法实现了极高的硬件利用率和显著的推理加速。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02110v1/fig-001.webp\", \"caption\": \"Fig. 8: Runtime breakdown (bars) and average HBM BW utilization (star markers) for different MHA implementations and layer sizes. +Runtime not overlapped with matrix engine. ++Runtime not overlapped with either vector or matrix engine. *Implementations without double buffering.\", \"page\": 10, \"index\": 1, \"width\": 519, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2604.02110v1/fig-002.webp\", \"caption\": \"Fig. 9: Runtime breakdown for different (square) flattening scales and layer sizes. Percentage labels above the bars indicate the average utilization of the matrix engine when active. +Runtime not overlapped with matrix engine. ++Runtime not overlapped with either vector or matrix engine.\", \"page\": 10, \"index\": 2, \"width\": 519, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2604.02110v1/fig-003.webp\", \"caption\": \"Fig. 10: A general tiling and group-scaling strategy for FlatAttention.\", \"page\": 10, \"index\": 3, \"width\": 508, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2604.02110v1/fig-004.webp\", \"caption\": \"Fig. 3: (a) SoA LLM model architecture overview and schematic for (b) MHA in prefill (c) MHA in auto-regressive decoding as well as (d) GQA in auto-regressive decoding.\", \"page\": 4, \"index\": 4, \"width\": 1055, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2604.02110v1/fig-005.webp\", \"caption\": \"TABLE III: Related Work Comparison\", \"page\": 12, \"index\": 5, \"width\": 520, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2604.02110v1/fig-006.webp\", \"caption\": \"Fig. 13: (a) DeepSeek-v3-671B decoding performance on 64-accelerator system comparing using FlatAttention and FlashMLA dataflow, as well as (b) runtime breakdown for decoding with 256 batch size per chip. Effect of expert parallelism: (c) Performance on the wafer-scale system; (d) D2D communication overhead at batch size of 256.\", \"page\": 12, \"index\": 6, \"width\": 1006, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2604.02110v1/fig-007.webp\", \"caption\": \"TABLE II: DeepSeek-v3-671B Decoding Performnace Comaprision with SoA GPU/NPU Solutions\", \"page\": 12, \"index\": 7, \"width\": 1081, \"height\": 204}]"
motivation: 大模型推理中Attention计算占比日益增加，且传统架构面临严重的HBM带宽瓶颈，亟需优化片上数据流以提升硬件利用率。
method: 提出FlatAttention数据流，通过利用片上网络集成的集合通信原语实现瓦片间数据交换，从而最小化高昂的HBM访问开销。
result: "在瓦片式加速器上实现最高92.3%的利用率，较FlashAttention-3加速4.1倍，并在DeepSeek-v3推理中提升1.9倍吞吐量。"
conclusion: 协同优化数据流与硬件互联原语是提升瓦片式加速器在大模型推理性能和能效的关键途径。
---

## 摘要
在混合专家（MoE）模型的推理过程中，注意力机制在总计算量中所占的比例日益增大，因此高效加速变得至关重要。用于大模型推理的新兴领域专用加速器正转向芯片级和晶圆级基于瓦片的架构。瓦片包含大型矩阵和向量引擎，并通过片上互连进行连接，支持瓦片间的通信，以减轻瓦片到主内存的通信瓶颈。因此，数据流管理对于实现高利用率至关重要。我们提出了 FlatAttention，一种适用于基于瓦片加速器的现代注意力变体的数据流。FlatAttention 通过利用集成在片上网络织物中的集合原语，最大限度地减少了昂贵的高带宽内存（HBM）访问，实现了高达 92.3% 的利用率，相比 FlashAttention-3 提速 4.1 倍，并降低了 16 倍的 HBM 流量。在峰值性能与 NVIDIA GH200 相当的 32x32 瓦片配置上，FlatAttention 可推广至多种注意力变体，在计算受限的注意力机制中实现了平均 86% 的利用率，在内存受限的机制中实现了 78% 的 HBM 带宽利用率，相比 GH200 上的注意力实现平均提速 1.9 倍。最后，我们在晶圆级多芯片系统上评估了使用 FlatAttention 的端到端 DeepSeek-v3 FP8 解码，尽管其峰值系统性能比现有最先进方案低 1.5 倍，但系统吞吐量提高了 1.9 倍，单用户 Token 输出延迟降低了 1.4 倍。

## Abstract
Attention accounts for an increasingly dominant fraction of total computation during inference for mixture-of-experts (MoE) models, making efficient acceleration critical. Emerging domain-specific accelerators for large model inference are shifting toward chip-scale and wafer-scale tile-based architectures. Tiles contain large matrix and vector engines and are connected through on-chip interconnects, which support tile-to-tile traffic to reduce the tile-to-main-memory traffic bottleneck. Hence, dataflow management is crucial to achieve high utilization. We propose FlatAttention, a dataflow for modern attention variants on tile-based accelerators. FlatAttention minimizes expensive high-bandwidth memory (HBM) accesses by exploiting collective primitives integrated into the on-chip network fabric, achieving up to 92.3% utilization, 4.1x speedup over FlashAttention-3, and 16x lower HBM traffic. On a 32x32 tile configuration with peak performance comparable to NVIDIA GH200, FlatAttention generalizes across multiple attention variants, achieving an average of 86% utilization for compute-bound attentions and 78% HBM bandwidth utilization for memory-bound ones, resulting in an average 1.9x speedup over attention implementations on GH200. Finally, we evaluate end-to-end DeepSeek-v3 FP8 decoding with FlatAttention on a wafer-scale multi-die system, achieving a 1.9x improvement in system throughput and a 1.4x reduction in per-user token output latency, despite operating with 1.5x lower peak system performance compared to the state-of-the-art solution.

---

## 论文详细总结（自动生成）

这是一份关于论文《FlatAttention: Dataflow and Fabric Collectives Co-Optimization for Large Attention-Based Model Inference on Tile-Based Accelerators》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）特别是混合专家模型（MoE，如 DeepSeek-v3）的发展，注意力机制（Attention）在推理计算中的占比显著增加（在 DeepSeek-v3 解码阶段可达 71%）。然而，现有的 GPU 架构在处理这些内核时利用率较低（仅为理论峰值的 26%-64%），且面临严重的 HBM（高带宽内存）访问瓶颈。
**研究动机：** 针对新兴的“瓦片式”（Tile-based）加速器（如晶圆级或多芯片阵列），如何通过协同优化数据流与片上网络（NoC）的硬件通信能力，减少昂贵的 HBM 访问，从而提升大模型推理的效率。

### 2. 方法论：核心思想与关键技术
论文提出了 **FlatAttention**，其核心思想是将注意力计算在多个瓦片（Tile）之间进行“扁平化”并行处理，利用片上通信替代片外内存访问。
*   **核心思想：** 不同于 FlashAttention 将一个 Head 分配给一个计算单元，FlatAttention 让一组瓦片（Group）协作处理一个大块的 Attention 计算，通过共享 L1 缓存来增加数据复用。
*   **关键技术细节：**
    *   **硬件集合原语（Fabric Collectives）：** 利用 NoC 织物层面的硬件支持，实现低延迟的**多播（Multicast）**和**归约（Reduction）**。例如，Q、K、V 矩阵在瓦片间多播，Softmax 的最大值和求和结果在行/列间归约。
    *   **异步调度（Asynchronous Scheduling）：** 采用双缓冲和异步执行机制，使矩阵引擎（Matrix Engine）的计算与向量引擎（Vector Engine）的 Softmax 处理及 DMA 数据传输完全重叠。
    *   **通用化适配：** 提出了“权重吸收”（Weight-absorption）技巧，将复杂的 MLA（多头潜在注意力）转换为类似 MQA 的形式，使其能统一应用 FlatAttention 数据流。
    *   **瓦片分组策略：** 提出先优化单个瓦片的计算效率（确定最佳 Tiling 大小），再根据模型形状扩展分组规模（Group Scale），以避免“过度扁平化”导致的同步开销。

### 3. 实验设计
*   **场景与数据集：** 涵盖了 LLM 推理的两个关键阶段：**Prefill（预填充）**和 **Decoding（解码）**。
*   **Benchmark 与对比方法：**
    *   **硬件基准：** 模拟了一个 32x32 瓦片的加速器，峰值性能和 HBM 带宽与 NVIDIA GH200 相当。
    *   **对比算法：** FlashAttention-2、FlashAttention-3 以及针对 MLA 优化的 FlashMLA。
    *   **端到端评估：** 在 64 颗芯片组成的晶圆级系统上运行 **DeepSeek-v3-671B** 模型。

### 4. 资源与算力
*   **模拟平台：** 使用了基于事件的 **GVSoC** 仿真框架，并针对 RedMulE 矩阵引擎和 FlooNoC 互连进行了 RTL 级的精度校准（误差在 0.17% - 12% 之间）。
*   **算力配置：** 
    *   单个加速器：988 TFLOPS (FP16)，2 TB/s HBM 带宽。
    *   晶圆级系统：64 个加速器，运行频率 1.9 GHz，支持 FP8 计算。
    *   对比对象：96 颗 NVIDIA H800 GPU 组成的集群。

### 5. 实验数量与充分性
论文进行了多维度的详尽实验：
*   **微观分析：** 测试了不同序列长度（512 到 4096）、头维度（64, 128）下的性能。
*   **消融实验：** 对比了硬件集合原语 vs 软件模拟通信（顺序/树状）的性能差异。
*   **变体覆盖：** 验证了 MHA、GQA、MLA 等多种注意力变体。
*   **充分性评价：** 实验设计非常充分，不仅有内核级的 Roofline 模型分析，还有端到端的系统级吞吐量和延迟（TPOT）评估，逻辑严密且对比公平。

### 6. 主要结论与发现
*   **性能提升：** 在相同硬件上，FlatAttention 比 FlashAttention-3 快 **4.1 倍**，HBM 流量减少 **16 倍**。
*   **高利用率：** 在计算受限场景下实现 **86%** 的平均利用率，在内存受限场景下实现 **78%** 的 HBM 带宽利用率。
*   **端到端优势：** 在 DeepSeek-v3 解码任务中，尽管系统峰值算力比 H800 集群低 1.5 倍，但实现了 **1.9 倍** 的吞吐量提升和 **1.4 倍** 的延迟降低。
*   **通信关键性：** 硬件支持的集合通信原语是实现加速的核心，软件实现的通信会因同步开销抵消数据流带来的收益。

### 7. 优点
*   **软硬协同：** 深入到 NoC 路由层面的硬件优化与上层算法紧密结合。
*   **极高的灵活性：** 能够处理从标准 MHA 到复杂的 MLA（DeepSeek 特有）的所有主流注意力机制。
*   **前瞻性：** 针对晶圆级（Wafer-scale）和芯片组（Chiplet）架构提供了切实可行的优化路径。

### 8. 不足与局限
*   **拓扑依赖：** 实验主要基于 2D Mesh 拓扑，对于其他复杂拓扑（如 Torus 或 DragonFly）的泛化性讨论较少。
*   **同步假设：** 晶圆级系统的评估采用了较为简单的同步屏障模型，未充分考虑大规模系统中的时钟偏移或非均匀延迟。
*   **应用限制：** 该方法高度依赖于具有强大片上互连和可编程 L1 缓存的瓦片式架构，在传统 GPU 上的移植可能受限于其固有的内存层级结构。

（完）
