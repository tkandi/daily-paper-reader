---
title: "GENSERVE: Efficient Co-Serving of Heterogeneous Diffusion Model Workloads"
title_zh: GENSERVE：异构扩散模型负载的高效协同推理
authors: "Fanjiang Ye, Zhangke Li, Xinrui Zhong, Ethan Ma, Russell Chen, Kaijian Wang, Jingwei Zuo, Desen Sun, Ye Cao, Triston Cao, Myungjin Lee, Arvind Krishnamurthy, Yuke Wang"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04335v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 在共享GPU集群上协同提供异构工作负载服务
tldr: "随着文本生成图像（T2I）和视频（T2V）需求的增长，在共享GPU集群上协同提供异构扩散模型服务面临巨大挑战。由于两类任务在计算需求和延迟要求上差异显著，现有系统难以满足服务等级目标（SLO）。GENSERVE利用扩散过程的可预测性和步级可抢占性，通过智能视频抢占、弹性序列并行与动态批处理以及SLO感知调度器，实现了高效的资源管理。实验表明，GENSERVE在多种配置下将SLO达成率提升了高达44%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04335v1/fig-001.webp\", \"caption\": \"Figure 7: System overview of GENSERVE. Incoming requests are admitted by the Scheduler, which maintains cluster states and queries the Profiler for offline latency estimates to invoke the Solver for joint optimization. The resulting decisions are passed to the Allocator and the Controller, which dispatches tasks and preemption signals to Workers. Workers execute inference on assigned GPUs and report step-level progress back through the Result Queue.\", \"page\": 6, \"index\": 1, \"width\": 494, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2604.04335v1/fig-002.webp\", \"caption\": \"Figure 1: Serving 4 videos (V1–V4) and 3 images (I1–I3) on 4 GPUs. (a) FIFO: videos occupy all GPUs until completion; images must wait, causing HOL blocking and 5 deadline misses (SLO 2/7). (b) GENSERVE: when I1, I2 arrive, the scheduler preempts V3 and V4 at step boundaries, batches I1+I2 on GPU 3, and serves I3 on GPU 2. After images complete, V3 and V4 resume with SP degree switching (each scaling to 2 GPUs) to recover lost slack. All 7 requests meet their deadlines (SLO 7/7).\", \"page\": 2, \"index\": 2, \"width\": 492, \"height\": 458}, {\"url\": \"assets/figures/arxiv/2604.04335v1/fig-003.webp\", \"caption\": \"Table 4: Key notation in the §4.4.\", \"page\": 8, \"index\": 3, \"width\": 482, \"height\": 324}]"
motivation: 现有的扩散模型服务系统在同时处理计算需求差异巨大的T2I和T2V异构负载时，难以满足严格的延迟服务等级目标（SLO）。
method: GENSERVE利用扩散推理的步级可预测性，引入了智能视频抢占、弹性序列并行结合动态批处理以及SLO感知调度机制。
result: "实验结果显示，与最强基准系统相比，GENSERVE在不同配置下的SLO达成率最高提升了44%。"
conclusion: 通过在步级粒度上进行异构资源管理和调度，GENSERVE显著提升了多模态扩散模型协同服务的效率和可靠性。
---

## 摘要
扩散模型已成为文本生成图像 (T2I) 和文本生成视频 (T2V) 的主流方法，然而生产平台必须越来越多地在共享 GPU 集群上同时提供这两种模态的服务，同时满足严格的延迟服务水平目标 (SLO)。协同提供此类异构负载具有挑战性：T2I 和 T2V 请求表现出截然不同的计算需求、并行特性和延迟要求，导致现有推理系统出现严重的 SLO 违规。我们提出了 GENSERVE，这是一个利用扩散过程固有的可预测性来优化推理效率的协同推理系统。一个核心见解是，扩散推理以离散、可预测的步骤进行，并且在步骤边界处自然可抢占，这为感知异构性的资源管理开辟了新的设计空间。GENSERVE 通过三种协调机制引入了步骤级资源自适应：智能视频抢占、带有动态批处理的弹性序列并行，以及一个联合优化所有并发请求资源分配的 SLO 感知调度器。实验结果表明，在各种配置下，GENSERVE 相比最强的基准系统，将 SLO 达成率提高了多达 44%。

## Abstract
Diffusion models have emerged as the prevailing approach for text-to-image (T2I) and text-to-video (T2V) generation, yet production platforms must increasingly serve both modalities on shared GPU clusters while meeting stringent latency SLOs. Co-serving such heterogeneous workloads is challenging: T2I and T2V requests exhibit vastly different compute demands, parallelism characteristics, and latency requirements, leading to significant SLO violations in existing serving systems. We present GENSERVE, a co-serving system that leverages the inherent predictability of the diffusion process to optimize serving efficiency. A central insight is that diffusion inference proceeds in discrete, predictable steps and is naturally preemptible at step boundaries, opening a new design space for heterogeneity-aware resource management. GENSERVE introduces step-level resource adaptation through three coordinated mechanisms: intelligent video preemption, elastic sequence parallelism with dynamic batching, and an SLO-aware scheduler that jointly optimizes resource allocation across all concurrent requests. Experimental results show that GENSERVE improves the SLO attainment rate by up to 44% over the strongest baseline across diverse configurations.

---

## 论文详细总结（自动生成）

这是一份关于论文《GENSERVE: Efficient Co-Serving of Heterogeneous Diffusion Model Workloads》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：扩散模型（Diffusion Models）已成为文本生成图像（T2I）和视频（T2V）的主流技术。生产环境通常需要在共享 GPU 集群上同时处理这两类任务。
*   **核心问题**：T2I 和 T2V 负载具有极高的**异构性**。T2V 的计算需求比 T2I 高出约 20 倍，且两者延迟要求不同。现有系统（如 FIFO 或 SJF 调度）会导致严重的“队头阻塞”（HOL Blocking）或任务饥饿，导致服务等级目标（SLO）达成率低下。
*   **核心见解**：扩散推理过程具有高度的**可预测性**（固定步数、每步成本稳定）和**步级可抢占性**（在去噪步骤边界暂停/恢复成本极低，仅需保留 MB 级的潜张量）。

### 2. 方法论
GENSERVE 提出了一个协同推理框架，通过以下三个核心机制实现步级资源自适应：
*   **智能视频抢占（Intelligent Video Preemption）**：
    *   利用每步执行的可预测性计算“松弛时间”（Slack），即截止日期与预计剩余执行时间之差。
    *   当紧急图像请求到达且资源不足时，优先抢占松弛时间最长的视频任务。
    *   采用截止日期感知的恢复策略，确保被抢占的视频在预算收紧前重新上线。
*   **弹性资源分配（Elastic Resource Allocation）**：
    *   **弹性序列并行（SP）**：针对视频任务，根据分辨率动态调整并行度（SP Degree）。高分辨率视频增加并行度以缩短延迟，低负载时降低并行度释放 GPU。
    *   **SLO 感知动态批处理**：针对图像任务，在不违反截止日期的前提下，将相同分辨率的请求合并成批次，以提高吞吐量。
*   **步级 SLO 感知调度器**：
    *   将资源分配建模为一个类似“背包问题”的动态规划（DP）优化问题。
    *   在每个调度轮次（步骤边界），调度器联合考虑所有并发请求，选择能最大化全局 SLO 达成率的抢占、批处理和并行配置组合。

### 3. 实验设计
*   **模型与数据集**：
    *   **T2I**：使用 Stable Diffusion 3.5 Medium (2.5B)，数据集来自 DiffusionDB。
    *   **T2V**：使用 Wan2.2-T2V-5B (5B)，数据集来自 VBench。
*   **对比基准（Benchmarks）**：
    *   **FCFS**：先来先服务。
    *   **SJF**：短作业优先。
    *   **SRTF**：最短剩余时间优先（带抢占）。
    *   **RASP**：分辨率感知的静态序列并行。
*   **评估指标**：SLO 达成率（SAR）、端到端延迟 CDF、调度开销等。
*   **场景配置**：涵盖了不同的 SLO 紧迫度（Scale 0.8-1.3）、负载比例（视频占比 20%-80%）、到达模式（泊松分布与突发流量）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **8 × NVIDIA RTX PRO 6000 Blackwell GPUs** (每张显卡 96GB GDDR7) 的服务器上运行。
*   **连接方式**：通过 PCIe Gen5 ×16 连接。
*   **软件栈**：Ubuntu 24.04, CUDA 12.9, PyTorch 2.8, NCCL。
*   **训练/推理时长**：论文未提及模型训练，主要关注推理服务阶段。调度器的 DP 求解时间极短（平均 < 0.5ms），对推理性能影响微乎其微。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽测试，包括端到端性能对比、消融实验（逐步开启抢占、DP 调度、SP 切换）、敏感性分析（分辨率分布、调度开销、内存占用）。
*   **充分性评价**：实验设计非常充分。作者不仅测试了平均情况，还专门针对“突发流量”和“分辨率偏斜（Skewed）”等极端生产环境场景进行了验证。对比方法涵盖了工业界常用的基础策略和最新的学术研究成果，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：在各种配置下，GENSERVE 的 SLO 达成率比最强基准（SRTF）提升了高达 **44%**。
*   **延迟优化**：对于图像请求，GENSERVE 将 P90 延迟缩短了 **3.1 倍**（相比 FCFS），有效消除了队头阻塞。
*   **资源效率**：通过动态 SP 切换和复制式协同服务（Replicated Co-serving），避免了静态资源分区导致的碎片化问题。
*   **极低开销**：视频抢占的恢复开销仅占单步执行时间的 **< 0.2%**，暂停状态的显存占用仅为 **27.2 MB**（对于 720p 视频），几乎可以忽略不计。

### 7. 优点
*   **特性利用充分**：敏锐捕捉到扩散模型与 LLM 在推理特性上的本质区别（扩散模型步长固定、状态小），并据此设计了极低成本的抢占机制。
*   **调度精细化**：通过 DP 算法实现了全局最优的资源权衡，而不是简单的启发式规则。
*   **系统鲁棒性**：在处理异构分辨率和突发流量时表现出极强的适应能力。

### 8. 不足与局限
*   **单节点限制**：实验主要在单台 8-GPU 服务器上完成，虽然证明了多卡并行，但在超大规模分布式集群（跨节点）上的通信开销和调度复杂性有待进一步验证。
*   **模型覆盖**：虽然使用了主流的 DiT 架构，但对于非 Transformer 架构的扩散模型或其他多模态模型（如原生多模态 LLM）的适用性未做深入讨论。
*   **显存假设**：系统假设 GPU 显存足以同时容纳 T2I 和 T2V 的模型权重（约 39GB/96GB），如果模型规模进一步扩大到显存临界点，其调度策略可能需要调整。

（完）
