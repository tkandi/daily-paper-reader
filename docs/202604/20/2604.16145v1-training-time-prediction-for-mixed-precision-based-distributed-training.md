---
title: Training Time Prediction for Mixed Precision-based Distributed Training
title_zh: 基于混合精度的分布式训练时间预测
authors: "Minchul Kang, Changyong Shin, Jinwoo Jeong, Hyunho Lee, Younghun Go, Gyeongmin Kim, Gyeongsik Yang, Chuck Yoo"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16145v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 混合精度分布式训练的时间预测，用于资源分配与调度优化
tldr: "本研究针对分布式深度学习中训练时间预测不准确的问题，指出浮点精度设置是影响时间的关键因素（差异可达2.4倍）。由于现有方法基于静态计算图且未考虑精度变化，导致预测误差巨大。为此，本文提出了一种感知精度的预测框架，通过捕捉混合精度等设置下的性能差异，将预测误差显著降低至9.8%，为资源分配和成本估算提供了更可靠的依据。"
source: arxiv
selection_source: fresh_fetch
motivation: "现有的分布式训练时间预测研究忽略了浮点精度（尤其是混合精度）对训练时间的显著影响，导致预测误差高达147.85%。"
method: 提出一种感知精度的分布式训练时间预测器，能够捕捉包括混合精度在内的不同精度设置对计算图和执行时间的影响。
result: "该预测器在多种精度设置下均表现出稳健的准确性，将平均绝对百分比误差（MAPE）显著降低至9.8%。"
conclusion: 在分布式训练时间预测中引入精度感知机制，对于提高资源调度效率和成本估算的准确性至关重要。
---

## 摘要
在分布式深度学习中，准确预测训练时间对于资源分配、成本估算和作业调度至关重要。我们观察到，浮点精度设置是训练时间的决定性因素，会导致训练时间相对于其最小值产生约 2.4 倍的波动。然而，现有的分布式训练时间预测研究依赖于静态模型计算图，无法捕捉包括混合精度在内的精度变化。根据我们的实验，不考虑精度的训练时间预测会导致显著的预测误差，其平均绝对百分比误差（MAPE）最高可达 147.85%。为解决这一问题，我们提出了一种精度感知的分布式训练时间预测器，该预测器在包括混合精度在内的多种精度设置下均能实现稳健的准确性，MAPE 为 9.8%。

## Abstract
Accurate prediction of training time in distributed deep learning is crucial for resource allocation, cost estimation, and job scheduling. We observe that the floating-point precision setting is a key determinant of training time, leading to training time variations of ~2.4x over its minimum. However, existing studies on distributed training time prediction rely on static model computation graphs that do not capture precision variations, including mixed precision. According to our experiments, training time prediction without considering precision results in significant prediction errors - reaching up to 147.85% in mean absolute percentage error (MAPE). To address this issue, we propose a precision-aware distributed training time predictor that achieves robust accuracy across diverse precision settings, including mixed precision, with 9.8% MAPE.

---

## 论文详细总结（自动生成）

这篇论文针对分布式深度学习中“训练时间预测”这一关键问题提出了改进方案。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着深度学习模型规模的剧增，分布式训练已成为标准。准确预测训练时间（单次迭代时间）对于资源分配、成本估算和作业调度至关重要。
*   **核心问题**：现有的预测工具（如 NeuSight, vTrain）通常基于静态计算图，假设精度设置（如 FP32）是固定的。然而，实际训练中广泛采用混合精度（Mixed Precision），不同精度设置会导致训练时间产生高达 2.4 倍的差异。忽略精度变化会导致预测误差（MAPE）高达 147.85%。

### 2. 方法论
论文提出了一种**精度感知（Precision-aware）**的分布式训练时间预测器，其核心思想是将总时间拆解为计算时间与三种并行策略产生的通信开销之和：
*   **核心公式**：$T = T_{comp} + T_{dp} + T_{tp} + T_{pp}$
    *   $T_{comp}$：计算图执行时间。
    *   $T_{dp}, T_{tp}, T_{pp}$：分别为数据并行、张量并行和流水线并行的通信/气泡开销。
*   **关键技术细节**：
    1.  **算子提取与切分**：利用 `torch.fx` 提取模型算子，并根据 DP/TP/PP 的配置将计算图切分为 GPU 特定的子图。
    2.  **精度识别**：通过挂钩（Hook）`torch.amp` 库，自动识别混合精度模式下每个算子实际执行的精度（如哪些算子被转换为 FP16，哪些保留 FP32）。
    3.  **算子级分析**：在特定精度和张量形状下对算子进行性能分析（Profiling），累加得到 $T_{comp}$。
    4.  **通信建模**：根据识别出的精度计算梯度大小（通信量），结合带宽估算 $T_{dp}$ 和 $T_{tp}$；通过流水线级数和计算时间估算流水线气泡（$T_{pp}$）。

### 3. 实验设计
*   **模型与数据集**：使用 **LLaMA 3.1-8B** 模型和 **C4** 数据集。
*   **实验场景**：涵盖了 DP、TP、PP 的多种组合配置（如 (8,1,1), (4,2,1), (2,2,2) 等）。
*   **Benchmark（对比方法）**：
    *   **NeuSight**：基于瓦片（Tile）延迟累加的预测方法。
    *   **vTrain**：基于 CUDA Graph 和内核基准测试的预测方法。
*   **对比维度**：在混合精度（Mixed Precision）和纯 FP16 两种设置下，对比各方法的平均绝对百分比误差（MAPE）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H100 GPU**。
*   **互联技术**：GPU 之间通过 **NVLink** 高速互联。
*   **训练时长**：文中未明确提及总训练时长，因为研究重点是预测“单次迭代（Single Iteration）”的时间。

### 5. 实验数量与充分性
*   **实验规模**：针对 10 种不同的并行策略组合（DP/TP/PP 的不同程度）进行了详尽测试。
*   **充分性**：实验覆盖了当前大模型训练主流的并行技术和精度设置。通过与两种最先进的（SOTA）方法进行对比，证明了在精度变化场景下，传统方法的失效以及本方法的稳健性。实验设计较为客观，直接展示了在不同并行度下的误差波动。

### 6. 主要结论与发现
*   **精度是关键变量**：浮点精度设置对训练时间影响巨大，不考虑精度的预测模型在实际应用中（尤其是混合精度训练）几乎不可用。
*   **高精度预测**：该预测器在混合精度下的 MAPE 为 **9.8%**，在 FP16 下为 **10.64%**。
*   **显著提升**：相比于 NeuSight 和 vTrain，该方法的预测精度提升了约 **15.08 倍**。

### 7. 优点
*   **动态感知**：能够自动识别 `torch.amp` 导致的算子精度变化，解决了混合精度“黑盒”导致的预测偏差。
*   **细粒度建模**：结合了算子级分析和并行策略感知的图切分，能够处理复杂的混合并行场景。
*   **实用性强**：对于云服务商和大型模型训练团队进行成本控制和资源调度具有直接参考价值。

### 8. 不足与局限
*   **硬件单一性**：实验仅在 H100 上完成，未验证在旧款 GPU（如 A100, V100）或其他架构（如昇腾、AMD）上的泛化能力。
*   **规模限制**：目前仅在单机 8 卡环境下验证，尚未探讨跨节点（Multi-node）在复杂网络拓扑（如 InfiniBand vs Ethernet）下的表现。
*   **模型多样性**：仅测试了 LLaMA 3.1-8B，对于非 Transformer 架构或带有复杂自定义算子的模型支持情况尚不明确。

（完）
