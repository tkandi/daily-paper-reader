---
title: Communication-Efficient Collaborative LLM Inference over LEO Satellite Networks
title_zh: 低地球轨道（LEO）卫星网络中通信高效的协作式大语言模型（LLM）推理
authors: "Songge Zhang, Wen Wu, Liang Li, Ye Wang, Xuemin, Shen"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04654v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 用于协作式大模型推理的流水线并行
tldr: "针对低轨卫星单机内存有限且推理延迟高的问题，本文提出一种高效的协作式大语言模型推理方案。通过将模型切分部署在多颗卫星上，利用流水线并行技术重叠计算与传输，并结合自适应激活压缩算法降低通信开销。该方案通过改进的A*算法优化模型切分与压缩策略，在保证精度的前提下显著提升了推理效率。"
source: arxiv
selection_source: fresh_fetch
motivation: 单颗低轨卫星受限于内存和计算资源，难以独立运行大语言模型，且传统协作推理面临巨大的通信延迟挑战。
method: "提出一种结合模型切分、流水线并行和自适应激活压缩的协作推理框架，并利用基于有向无环图的改进A*算法优化推理延迟。"
result: "实验表明，该方案在推理精度损失低于1%的情况下，可降低高达42%的推理延迟和71%的通信开销。"
conclusion: 该研究为在资源受限的卫星网络中部署大规模AI模型提供了一种高效且低延迟的协作推理新范式。
---

## 摘要
低地球轨道（LEO）卫星通过利用人工智能模型，在智能地球观测中发挥着至关重要的作用。然而，有限的星载内存和过高的推理延迟阻碍了在单颗卫星上实际部署大语言模型（LLM）。在本文中，我们提出了一种针对LEO卫星网络的通信高效协作式LLM推理方案。具体而言，整个LLM被拆分为多个子模型，每个子模型部署在一颗卫星上，从而通过在卫星之间交换中间激活值来实现协作式LLM推理。该方案还利用流水线并行机制，使子模型推理与中间激活值传输重叠，从而降低LLM推理延迟。设计了一种自适应激活压缩方案，以减轻多阶段模型拆分带来的累积误差，同时保持推理准确性。此外，我们通过在星载内存和推理准确性约束下联合优化模型拆分和压缩比，构建了LLM推理延迟最小化问题。该问题被转化为有向无环图上的最短路径搜索问题，其中边权重明确量化了由模型拆分和压缩策略引起的推理延迟，并采用改进的A*搜索算法进行求解。广泛的仿真结果表明，与现有基准方案相比，所提方案可将推理延迟降低高达42%，通信开销降低高达71%，同时保持推理准确性损失低于1%。

## Abstract
Low Earth orbit (LEO) satellites play an essential role in intelligent Earth observation by leveraging artificial intelligence models. However, limited onboard memory and excessive inference delay prevent the practical deployment of large language models (LLMs) on a single satellite. In this paper, we propose a communication-efficient collaborative LLM inference scheme for LEO satellite networks. Specifically, the entire LLM is split into multiple sub-models, with each deployed on a satellite, thereby enabling collaborative LLM inference via exchanging intermediate activations between satellites. The proposed scheme also leverages the pipeline parallelism mechanism that overlaps sub-model inference with intermediate activation transmission, thereby reducing LLM inference delay. An adaptive activation compression scheme is designed to mitigate cumulative errors from multi-stage model splitting while preserving inference accuracy. Furthermore, we formulate the LLM inference delay minimization problem by jointly optimizing model splitting and compression ratios under onboard memory and inference accuracy constraints. The problem is transformed into a shortest-path search problem over a directed acyclic graph that edge weights explicitly quantify the inference delay induced by model splitting and compression strategies, which is solved via a modified A Star-based search algorithm. Extensive simulation results indicate that the proposed solution can reduce inference delay by up to 42% and communication overhead by up to 71% compared to state-of-the-art benchmarks, while maintaining the inference accuracy loss of less than 1%.

---

## 论文详细总结（自动生成）

以下是对论文《Communication-Efficient Collaborative LLM Inference over LEO Satellite Networks》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在资源受限（内存有限、计算力弱、带宽波动）的低轨（LEO）卫星网络中高效部署和运行参数量巨大的大语言模型（LLM，如 Vision Transformer）。
*   **研究动机**：
    *   **单星瓶颈**：单个卫星的内存无法容纳完整的 LLM，且处理高分辨率遥感图像时延迟过高。
    *   **通信挑战**：传统的“卫星采集-地面处理”模式受限于下行链路带宽，导致实时性差。
    *   **协作需求**：需要一种能将模型切分到多颗卫星协作推理，同时能有效控制卫星间通信开销的方案。

### 2. 方法论：核心思想与关键技术
论文提出了一种通信高效的协作式 LLM 推理框架，包含以下三个核心组件：
*   **模型切分与协作推理**：将 LLM 划分为 $K$ 个子模型，顺序部署在 $K$ 颗卫星上。通过交换中间激活值（Activations）实现端到端推理。
*   **流水线并行（Pipeline Parallelism）**：引入流水线机制，使卫星在传输当前批次的激活值时，同步计算下一个批次，通过计算与通信的重叠（Overlap）来隐藏传输延迟。
*   **自适应激活压缩方案**：
    *   **Gumbel-Mask 稀疏化**：利用可学习的 Gumbel-Sigmoid 掩码选择最重要的特征，通过直通估计器（STE）实现端到端训练。
    *   **量化与熵编码**：对稀疏化后的特征进行低位量化（如 8-bit）并结合哈夫曼编码进一步压缩。
*   **联合优化算法**：
    *   将最小化总推理延迟问题建模为混合整数非线性规划（MINLP）。
    *   **算法实现**：将问题转化为有向无环图（DAG）中的最短路径搜索。采用**改进的 A* 搜索算法**：外层循环搜索最优切分点，内层循环通过离散网格搜索确定最优压缩比。

### 3. 实验设计
*   **数据集**：
    *   **EuroSAT**：27,000 张 Sentinel-2 卫星图像，10 类土地利用分类。
    *   **RESISC45**：31,500 张遥感图像，45 类场景分类。
*   **模型方案**：采用不同规模的 Vision Transformer (ViT-B, ViT-L, ViT-H, ViT-G)，参数量从 86M 到 1.8B。
*   **基准方法（Benchmarks）**：
    *   **Ground-only**：卫星仅作为中继，原始图像传回地面处理。
    *   **Single-satellite**：在单颗卫星上部署完整模型（若内存允许）。
    *   **Top-k 压缩**：对比传统的基于数值大小的硬剪枝压缩。
*   **场景设置**：模拟 Walker Delta 星座，考虑 Ka 频段下行链路和激光星间链路（ISL）。

### 4. 资源与算力
*   **地面端**：使用 NVIDIA RTX 4070ti GPU 进行模型训练和基准对比。
*   **卫星端（仿真）**：使用 **4 台 NVIDIA Jetson AGX Orin** 嵌入式设备模拟卫星节点，通过配置不同的功耗模式（15W, 30W, 50W）来模拟异构计算能力。
*   **软件栈**：基于 PyTorch 和 FastAPI 构建，利用多线程实现流水线并行。
*   **训练细节**：压缩模块采用离线训练方式，在地面完成参数优化后部署至卫星。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽测试，包括不同图像分辨率（240p 到 1080p）、不同下行速率（0.2-0.8 Gbit/s）、不同卫星数量（4-12 颗）以及不同模型规模。
*   **消融实验**：针对压缩方法（稀疏化 vs 量化 vs 熵编码）和切分策略（启发式 vs 均匀 vs 本文优化）进行了对比。
*   **评价**：实验设计覆盖了卫星通信的主要变量，使用了真实的嵌入式硬件进行半实物仿真，数据量充足，对比实验客观，能够有效支撑论文结论。

### 6. 主要结论与发现
*   **延迟降低**：相比地面处理方案，所提方案在 1080p 分辨率下降低了约 **42%** 的端到端延迟。
*   **通信节省**：通过自适应压缩，通信开销减少了高达 **71.7%**。
*   **精度保持**：在实现高倍率压缩的同时，推理准确率损失控制在 **1%** 以内，优于传统的 Top-k 策略。
*   **扩展性**：随着卫星数量增加，流水线并行的加速效果更明显，但受限于星间通信开销的边际效应。

### 7. 优点与亮点
*   **软硬结合**：不仅有理论公式推导，还使用了 Jetson Orin 这种真实的航天级 AI 硬件进行验证，具有很强的工程参考价值。
*   **联合优化**：将模型切分位置与每层的压缩比进行联合搜索，而非固定策略，适应了卫星网络的异构性。
*   **可学习压缩**：引入 Gumbel-mask 使压缩过程具备任务感知能力，比简单的数值剪枝更鲁棒。

### 8. 不足与局限
*   **离线依赖**：压缩模块需要针对特定任务进行离线训练，若在线任务发生剧烈变化，可能需要重新训练掩码。
*   **拓扑限制**：目前主要考虑链式协作，对于更复杂的网状星座拓扑或动态变化的星间链路切换讨论较少。
*   **能耗开销**：虽然降低了延迟，但多星协作涉及频繁的无线电/激光通信，论文对通信带来的额外能耗（Energy Consumption）未做深入的定量分析。

（完）
