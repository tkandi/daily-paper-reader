---
title: The Energy Cost of Execution-Idle in GPU Clusters
title_zh: GPU 集群中执行空闲状态的能源成本
authors: "Yiran Lei, Jared Fernandez, Vasilis Kypriotis, Dimitrios Skarlatos, Emma Strubell, Justine Sherry, Daniel Vosler"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04745v1"
tags: ["query:mlsys"]
score: 9.0
evidence: GPU 集群功耗遥测与能效原型设计
tldr: "本研究揭示了GPU集群中一种被称为“执行空闲（execution-idle）”的高能耗状态，即GPU在几乎没有可见活动时仍保持高功率。通过分析大型学术AI集群的秒级遥测数据，研究发现该状态占执行时间的19.7%和总能耗的10.7%。为此，研究者提出了自动降频和利用负载不均衡减少暴露两种原型方案，旨在降低此类能耗，并建议未来系统应将其视为核心运行状态进行优化。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04745v1/fig-001.webp\", \"caption\": \"Table 4. Installed GPU fleet composition and default power limits reported by nvidia-smi.\", \"page\": 15, \"index\": 1, \"width\": 510, \"height\": 336}, {\"url\": \"assets/figures/arxiv/2604.04745v1/fig-002.webp\", \"caption\": \"Table 1. Representative signals collected by our passive profiling pipeline. These signals cover GPU power, activity, clocks, communication, host activity, timing, and job metadata.\", \"page\": 3, \"index\": 2, \"width\": 1052, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2604.04745v1/fig-003.webp\", \"caption\": \"Figure 2. Time-aligned power, SM and DRAM utilization, and normalized frequency for a job on an L40S GPU, illustrating the execution-idle state.\", \"page\": 4, \"index\": 3, \"width\": 479, \"height\": 306}]"
motivation: 针对GPU在低活动状态下仍消耗大量电力的“执行空闲”现象，探索其在实际集群中的影响及优化空间。
method: 通过分析大型AI集群的实时遥测数据来量化该状态，并开发了自动降频和负载调整两种原型系统进行验证。
result: "实验表明执行空闲状态占据了约19.7%的运行时间和10.7%的能耗，且提出的原型方案能有效缓解这一问题。"
conclusion: 未来的节能GPU系统设计必须将“执行空闲”视为一种关键的运行状态，并采取针对性的能效管理策略。
---

## 摘要
GPU 正成为数据中心功耗的主要来源，但与 CPU 不同，即使在可见活动接近于零时，GPU 仍可能保持高功耗。我们将这种状态称为“执行空闲”（execution-idle）。通过分析来自大型学术 AI 集群的每秒遥测数据，我们将执行空闲刻画为实际部署中一种反复出现的低活动、高功耗状态。在多种工作负载和多代 GPU 中，该状态占执行时间的 19.7% 和能耗的 10.7%。这表明既需要降低执行空闲的成本，也需要减少处于该状态的频率。为此，我们构建了两个原型：一个在执行空闲期间采用自动降频，另一个利用负载不平衡来减少处于该状态的时间，两者均涉及性能权衡。这些研究结果表明，未来的节能 GPU 系统应将执行空闲视为一种主要的操作状态。

## Abstract
GPUs are becoming a major contributor to data center power, yet unlike CPUs, they can remain at high power even when visible activity is near zero. We call this state execution-idle. Using per-second telemetry from a large academic AI cluster, we characterize execution-idle as a recurring low-activity yet high-power state in real deployments. Across diverse workloads and multiple GPU generations, it accounts for 19.7% of in-execution time and 10.7% of energy. This suggests a need to both reduce the cost of execution-idle and reduce exposure to it. We therefore build two prototypes: one uses automatic downscaling during execution-idle, and the other uses load imbalance to reduce exposure, both with performance trade-offs. These findings suggest that future energy-efficient GPU systems should treat execution-idle as a first-class operating state.

---

## 论文详细总结（自动生成）

### 论文总结：GPU 集群中执行空闲（Execution-Idle）状态的能源成本分析

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是 GPU 在 AI 数据中心中日益增长的功耗问题。研究发现，与 CPU 不同，GPU 在程序已加载但几乎没有计算或通信活动时，仍会消耗远高于基础功耗的电量。作者将这种状态定义为**“执行空闲”（Execution-Idle）**。
*   **动机**：目前的能耗评估多基于粗粒度的指标（如 TDP 或 GPU 小时数），忽略了运行时的动态行为。
*   **核心发现**：在实际部署中，GPU 经常处于这种“出工不出力”的高能耗状态，这成为了能源浪费的主要来源。

#### 2. 论文提出的方法论
作者通过构建被动遥测系统和原型策略来研究并缓解该问题：
*   **状态定义**：
    *   **执行空闲（Execution-Idle）**：SM/张量核心/DRAM 活动均低于 5%，且 PCIe/NVLink 通信低于 1GB/s，且该状态持续 5 秒以上。
    *   **深度空闲（Deep Idle）**：未加载程序，处于基础功耗。
    *   **活跃执行（Active Execution）**：程序运行且活动超过阈值。
*   **关键技术/算法**：
    *   **被动遥测流水线**：每秒采集一次 GPU 功耗、时钟、利用率及作业元数据。
    *   **频率控制算法（Algorithm 1）**：监测到持续 $X$ 秒的低活动后，强制降低 GPU 核心和显存频率；当活动恢复时立即恢复频率，并设置冷却时间以防震荡。
    *   **负载不均衡策略**：在推理服务中故意将请求集中到少数 GPU，让其余 GPU 进入深度空闲，而非让所有 GPU 都处于轻载的执行空闲状态。

#### 3. 实验设计
研究结合了真实集群观测和受控实验回放：
*   **数据集/场景**：
    *   **学术集群追踪**：对一个大型 AI 集群进行为期 31 天的连续监测。
    *   **工业负载回放**：使用 vLLM 框架回放来自 OpenAI (BurstGPT)、Qwen 和 Azure 的真实推理服务轨迹。
*   **Benchmark 与对比**：
    *   对比了不同工作负载（训练、批处理推理、在线服务）。
    *   对比了默认 DVFS（动态电压频率调整）与作者提出的“执行空闲感知”频率控制原型。
    *   对比了负载均衡与负载不均衡（整合）模式下的能耗与延迟。

#### 4. 资源与算力
该研究涉及的硬件规模非常大，涵盖了多代 NVIDIA GPU：
*   **GPU 型号与数量**：共 756 颗 GPU，包括 **B200 (8个)**、H100 (24个)、A100 (64个)、L40/L40S (408个)、RTX 6000 Ada (52个) 和 A6000 (200个)。
*   **时间跨度**：2026 年 2 月 4 日至 3 月 7 日，共 31 天。
*   **数据量**：收集了 162 GB 的秒级遥测数据。

#### 5. 实验数量与充分性
*   **实验充分性**：研究分析了超过 11,791 个长期运行的作业和 450 万个执行空闲区间。实验覆盖了从最新的 Blackwell (B200) 到较旧的 Ampere 架构，具有极强的代表性。
*   **客观性**：通过引入工业级推理轨迹回放，弥补了学术集群负载可能存在的偏差。作者还进行了敏感性分析（如改变 5 秒的定义阈值），证明结论具有稳健性。

#### 6. 主要结论与发现
*   **能耗占比**：执行空闲状态占 GPU 总执行时间的 **19.7%**，消耗了 **10.7%** 的总能量。
*   **服务负载最严重**：在在线推理服务中，执行空闲能耗占比高达 **48%**（学术）或 **7%-65%**（工业回放），主因是请求的突发性。
*   **瓶颈来源**：约 48% 的执行空闲紧随 PCIe 传输之后，17% 紧随网络 I/O 之后，表明 I/O 瓶颈是导致 GPU 等待并进入该状态的主要诱因。
*   **性能权衡**：虽然通过降频可减少高达 34% 的功耗，但会导致 P95 延迟增加（最高达 160%），证明了该问题在现有硬件下难以“免费”解决。

#### 7. 优点
*   **视角新颖**：首次系统性地定义并量化了 GPU 的“执行空闲”这一中间状态，而非简单的“忙”或“闲”。
*   **数据真实**：基于大规模生产集群的秒级遥测，而非模拟器或小规模实验室环境。
*   **跨代验证**：涵盖了从 A6000 到 B200 的全系列硬件，证明了该问题是架构性的，而非特定型号的缺陷。

#### 8. 不足与局限
*   **采样频率限制**：1Hz 的采样率可能漏掉亚秒级的微爆发（micro-bursts），对极短任务的捕捉不够精确。
*   **黑盒限制**：由于无法深入用户代码内部，部分原因分析（如 I/O 瓶颈）是基于相关性推断的，而非因果律证明。
*   **硬件控制权**：目前的降频手段依赖于厂商提供的 API（如 nvidia-smi），缺乏更细粒度的组件级（如仅针对 Tensor Core）功耗控制。
*   **应用限制**：提出的优化原型虽然节能显著，但延迟增加明显，对于极度敏感的实时业务可能难以直接应用。

（完）
