---
title: Leaf-centric Logical Topology Design for OCS-based GPU Clusters
title_zh: 面向基于 OCS 的 GPU 集群的以叶节点为中心的逻辑拓扑设计
authors: "Xinchi Han, Weihao Jiang, Yingming Mao, Yike Liu, Zhuoran Liu, Yongxi Lv, Peirui Cao, Zhuotao Liu, Ximeng Liu, Xinbing Wang, Changbo Wu, Zihan Zhu, Wu Dongchao, Yang Jian, Zhang Zhanbang, Yuansen Chen, Shizhen Zhao"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28168v2"
tags: ["query:mlsys"]
score: 9.0
evidence: 基于OCS的GPU集群逻辑拓扑设计
tldr: 针对基于光电路交换（OCS）的GPU集群中存在的路由极化问题，本文提出了一种以叶交换机为中心的逻辑拓扑设计方案。通过理论分析确定了避免路由极化的充分条件，并开发了多项式复杂度的算法，旨在平衡叶到脊交换机的链路负载，从而显著提升大规模机器学习训练的吞吐量并降低计算开销。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28168v2/fig-001.webp\", \"caption\": \"Fig. 1: LumosCore adopts a typical three-tier topology comprising leaf layer, spine layer, and am optical core layer.\", \"page\": 3, \"index\": 1, \"width\": 1068, \"height\": 283}]"
motivation: 现有的OCS集群拓扑会导致Pod间带宽分配不均（路由极化），造成链路拥塞并降低机器学习训练效率。
method: 提出一种以叶交换机为中心的范式，通过理论推导建立避免极化的充分条件，并设计了高效的逻辑拓扑生成算法。
result: "仿真结果显示，该方法相比基于混合整数规划的方法，吞吐量提升高达19.27%，且拓扑计算开销降低了99.16%。"
conclusion: 该研究通过优化逻辑拓扑设计，有效解决了OCS集群中的路由极化问题，为大规模GPU集群的高效互联提供了新思路。
---

## 摘要
近年来，针对机器学习（ML）工作负载优化的商用 GPU 集群（例如 Google A3 GPU 集群）中，光电路交换机（OCS）的部署日益增多。此类集群采用三层 leaf-spine-OCS 拓扑结构，服务器连接到叶层电子分组交换机（EPS）；这些叶交换机汇聚到脊层 EPS 以形成 Pod；多个 Pod 通过核心层 OCS 互连。与 EPS 不同，OCS 仅支持直接连接的脊交换机之间的基于电路的路径，这可能会引发一种被称为“路由极化”（routing polarization）的现象，即特定 Pod 对之间的带宽需求通过不同脊交换机之间的链路得到不均匀满足的情况。由此产生的不平衡会导致特定叶到脊链路上的流量争用和瓶颈，最终降低 ML 训练吞吐量。为了缓解这一问题，我们引入了一种以叶节点为中心的范式，以确保源自同一叶交换机的流量能够负载均衡地均匀分布在多个脊交换机上。通过严谨的理论分析，我们建立了避免路由极化的充分条件，并提出了一种具有多项式时间复杂度的相应逻辑拓扑设计算法。大规模仿真验证表明，与基于混合整数规划（MIP）的方法相比，该方法可提升高达 19.27% 的吞吐量，并减少 99.16% 的逻辑拓扑计算开销。

## Abstract
Recent years have witnessed the growing deployment of optical circuit switches (OCS) in commercial GPU clusters (e.g., Google A3 GPU cluster) optimized for machine learning (ML) workloads. Such clusters adopt a three-tier leaf-spine-OCS topology, servers attach to leaf-layer electronic packet switches (EPSes); these leaf switches aggregate into spine-layer EPSes to form a Pod; and multiple Pods are interconnected via core-layer OCSes. Unlike EPSes, OCSes only support circuit-based paths between directly connected spine switches, potentially inducing a phenomenon termed routing polarization, which refers to the scenario where the bandwidth requirements between specific pairs of Pods are unevenly fulfilled through links among different spine switches. The resulting imbalance induces traffic contention and bottlenecks on specific leaf-to-spine links, ultimately reducing ML training throughput. To mitigate this issue, we introduce a leaf-centric paradigm to ensure traffic originating from the same leaf switch is evenly distributed across multiple spine switches with balanced loads. Through rigorous theoretical analysis, we establish a sufficient condition for avoiding routing polarization and propose a corresponding logical topology design algorithm with polynomial-time complexity. Large-scale simulations validate up to 19.27% throughput improvement and a 99.16% reduction in logical topology computation overhead compared to Mixed Integer Programming (MIP)-based methods.

---

## 论文详细总结（自动生成）

以下是对论文《Leaf-centric Logical Topology Design for OCS-based GPU Clusters》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：随着机器学习（ML）规模扩大，Google A3 等商用 GPU 集群开始采用光电路交换机（OCS）构建三层（Leaf-Spine-OCS）拓扑。
*   **核心问题**：论文识别并定义了 **“路由极化”（Routing Polarization）** 问题。在 OCS 集群中，由于 OCS 仅支持一对一的脊交换机（Spine）连接，若采用传统的“以 Pod 为中心”的拓扑设计，会导致特定叶交换机（Leaf）到脊交换机的链路成为瓶颈，造成带宽分配不均和流量争用，严重降低 ML 训练吞吐量。
*   **研究动机**：现有的设计忽略了脊交换机内部的链路细节。本文旨在通过“以叶节点为中心”的设计范式，消除这一瓶颈。

### 2. 方法论
*   **核心思想**：将逻辑拓扑设计的视角从 Pod 级别细化到 Leaf 级别，确保源自同一 Leaf 的流量能均匀分布在多个 Spine 上。
*   **关键技术细节**：
    *   **LumosCore 框架**：统一了物理拓扑设计与逻辑拓扑算法。
    *   **物理拓扑约束**：证明了当内层物理拓扑满足 $\tau=2$（每个 Leaf 与每个 Spine 之间有两条链路）时，路由极化问题可在多项式时间内解决；而 $\tau=1$ 时该问题是 NP-Hard。
    *   **启发式分解算法（Heuristic-Decomposition）**：
        1.  **对称矩阵分解**：利用对称矩阵分解定理，将叶级需求矩阵 $L$ 分解为 $A + A^T$。
        2.  **整数矩阵分解**：将 $A$ 进一步分解为多个子矩阵，对应不同的 OCS 组。
        3.  **逻辑拓扑构建**：根据分解结果配置 OCS，建立 Spine 间的电路连接。
*   **算法优势**：该算法具有多项式时间复杂度 $O(\eta^6 \log \eta)$，远优于工业界常用的混合整数规划（MIP）方法。

### 3. 实验设计
*   **场景与规模**：使用流级模拟器 RapidAISim，模拟了 2,048 到 16,384 个 GPU 规模的集群。
*   **数据集**：基于 SenseTime（商汤）公开的生产环境工作负载轨迹，包含 1,000 个 ML 任务（涵盖 TP、PP、DP、EP 等通信模式）。
*   **对比方法（Benchmarks）**：
    *   **Pod-centric**：Google Jupiter 采用的以 Pod 为中心的设计（基于 MIP 求解）。
    *   **Helios**：经典的基于二分图匹配的 OCS 集群设计。
    *   **Clos**：传统的三层全电交换（EPS）无阻塞拓扑。
    *   **Best**：理想化的性能上限（假设 Spine 具有无限容量）。

### 4. 资源与算力
*   **模拟配置**：每台服务器 8 个 GPU，内部带宽 400 Gbps，节点间 RDMA 网络 200 Gbps。
*   **算力说明**：文中**未明确说明**运行模拟实验所使用的具体物理服务器硬件型号或总训练时长，但重点对比了算法本身的计算开销（秒级 vs 百秒级）。

### 5. 实验数量与充分性
*   **实验维度**：
    *   **扩展性实验**：覆盖了从 2k 到 16k GPU 的四种规模。
    *   **鲁棒性实验**：测试了不同负载均衡策略（ECMP、Rehashing）和不同工作负载水平（0.7 到 0.8）。
    *   **消融/对比实验**：对比了 $\tau=1$ 与 $\tau=2$ 的性能差异。
*   **充分性评价**：实验设计较为充分，涵盖了大规模集群的关键变量，对比对象包括了工业界标准和学术界经典方案，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：LumosCore（Leaf-centric, $\tau=2$）相比 Pod-centric 方法，任务运行时间（JRT）提升高达 **19.27%**。
*   **开销降低**：逻辑拓扑的计算时间相比基于 MIP 的方法减少了 **99.16%**（在 16k 规模下从 541 秒降至 4.5 秒）。
*   **物理拓扑的重要性**：实验证明 $\tau=2$ 的物理设计是避免路由极化的关键，即使算法再优，错误的物理布线（如 $\tau=1$）也会导致不可避免的拥塞。
*   **接近理想性能**：该方案在性能上接近昂贵的全电 Clos 架构，且由于跳数更少，在长尾延迟上表现更优。

### 7. 优点
*   **理论支撑深厚**：通过严谨的数学证明（NP-困难性证明、充分条件证明）指导架构设计。
*   **实用性强**：多项式时间算法解决了大规模集群在任务级重构拓扑时的计算瓶颈。
*   **视角独特**：首次深入探讨了 OCS 集群中 Leaf-to-Spine 链路不平衡导致的“路由极化”现象。

### 8. 不足与局限
*   **缺乏真实环境验证**：所有结论均基于模拟器得出，尚未在真实的万卡 OCS 集群中进行实测。
*   **需求矩阵生成**：论文假设“叶级网络需求矩阵”是预先给定的，但在实际动态任务中，如何精准、实时地生成该矩阵仍是一个挑战。
*   **拓扑限制**：该研究主要针对三层 Leaf-Spine-OCS 架构，对于更复杂或层数更多的光电混合拓扑的适用性有待研究。

（完）
