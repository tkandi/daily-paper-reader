---
title: "AutoSP: Unlocking Long-Context LLM Training Via Compiler-Based Sequence Parallelism"
authors: "Ahan Gupta, Zhihao Wang, Neel Dani, Masahiro Tanaka, Olatunji Ruwase, Minjia Zhang"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.27089v1"
tags: ["query:mlsys"]
score: 10.0
evidence: 基于编译器的序列并行技术，用于长文本LLM训练
tldr: AutoSP是首个自动优化大语言模型长文本训练的编译器方案。针对现有训练库在长文本优化上缺乏易用抽象、需手动重写代码的痛点，AutoSP通过自动序列并行和长文本感知激活检查点技术，在不损失吞吐量的前提下显著提升了训练能力，在NVIDIA和AMD硬件上分别实现了高达2.7倍和2.5倍的上下文长度提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.27089v1/fig-001.webp\", \"caption\": \"Figure 2: A sample neural network compiled using PyTorch-2.0. We illustrate the lowering to Torch-IR and Aten-IR that occur within Dynamo and the extra operators inserted during the lowering process.\", \"page\": 3, \"index\": 1, \"width\": 320, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2604.27089v1/fig-002.webp\", \"caption\": \"Figure 8: Average execution time of various Llama 3.2 model sizes at different sequence lengths on NVIDIA and AMD hardware. AutoSP matches the performance of hand-written baselines and supports longer sequence training.\", \"page\": 8, \"index\": 2, \"width\": 387, \"height\": 447}, {\"url\": \"assets/figures/arxiv/2604.27089v1/fig-003.webp\", \"caption\": \"Figure 3: An overview of AutoSP. AutoSP enables an automated approach to scale input context lengths for long-context training through a targeted set of compiler optimization passes: an automated sequence-parallelism pass (Section 3.1), and a SP-aware long-context AC-pass: SAC (Section 3.2).\", \"page\": 4, \"index\": 3, \"width\": 824, \"height\": 248}]"
motivation: 现有LLM训练库主要针对大参数量优化，缺乏针对长文本训练的易用抽象，导致开发者需手动实现复杂的序列并行。
method: 提出AutoSP编译器，通过自动应用序列并行和长文本感知的激活检查点技术来优化模型训练。
result: 在NVIDIA和AMD硬件上，AutoSP相比手写基准线分别将训练上下文长度提升了2.7倍和2.5倍，且运行性能损耗极低。
conclusion: AutoSP通过自动化编译优化，有效解决了长文本LLM训练的复杂性与资源限制问题，显著提升了开发效率和训练能力。
---

## Abstract
Large-language-models (LLMs) demonstrate enormous utility in long-context tasks which require processing prompts that consist of tens to hundreds of thousands of tokens. However, existing LLM training libraries do not provide easy to use abstractions to optimize for long-context training, instead focusing on optimizations for models with large parameter counts through ZeRO-3/FSDP, Tensor and Pipeline parallelism. This forces users to rewrite LLM training libraries to incorporate compositions of various complex long-context optimizations, such as sequence-parallelism, to training pipelines; a process that requires in-depth expertise, reducing developer productivity. To tackle these challenges, we introduce AutoSP: the first automated solution to automatically optimize LLM training for longer-contexts. AutoSP compiles models and applies a targeted set of optimizations: automated sequence parallelism, and long-context aware activation-checkpointing, to drastically enhance LLM trainability at negligible cost to throughput. Our evaluation demonstrates AutoSP's capability on both NVIDIA and AMD hardware, increasing training contexts by upto 2.7$\times$ and 2.5$\times$ respectively over competitive hand-written baseline at negligible cost to runtime performance.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **AutoSP** 的编译器驱动方案，旨在自动化解决大语言模型（LLM）在长文本训练中的内存瓶颈和开发复杂性问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：随着长文本任务（如文档理解、多轮对话）的需求增加，LLM 需要处理数万甚至数十万个 Token。这导致激活值内存（Activation Memory）需求激增。
*   **核心痛点**：
    *   **现有方案局限**：现有的分布式训练库（如 ZeRO-3/FSDP）主要针对大参数量模型优化，而非长文本。
    *   **开发成本高**：现有的序列并行（SP）方案（如 DeepSpeed-Ulysses, RingAttention）通常需要侵入式地修改模型代码，手动插入通信算子（all-to-all）并管理张量布局，缺乏易用的抽象。
    *   **优化冲突**：传统的自动激活检查点（AC）技术在长文本场景下过于保守，无法充分释放内存潜力。

### 2. 方法论
AutoSP 建立在 PyTorch 2.0 编译栈之上，通过两个核心编译器 Pass 实现自动化优化：
*   **自动化序列并行 Pass (SP-Pass)**：
    *   **IR 选择**：在 Torch-IR 层级进行分析和转换（而非更细粒度的 Aten-IR），因为 Torch-IR 保留了更高层的算子信息（如 Linear、Attention），便于识别序列维度。
    *   **程序分析**：自动推断 Batch、Sequence 和 Hidden 维度，识别需要调整大小的缓冲区。
    *   **自动转换**：自动在注意力层前后插入 `all-to-all` 通信算子，并重新计算手动索引的张量（如 Causal Mask）。
*   **长文本感知激活检查点 Pass (SAC-Pass)**：
    *   **核心发现**：论文通过公式推导发现，随着序列长度 $s$ 的增加，线性层和 MLP 层的计算量占比趋于 $O(1/s)$。
    *   **策略优化**：不同于 PyTorch 默认 AC 策略（禁止重算矩阵乘法等重计算算子），SAC 允许在注意力层之外重算这些算子。由于长文本下这些算子的计算耗时占比极低，重算它们可以显著节省内存，而对吞吐量的影响微乎其微。

### 3. 实验设计
*   **模型对象**：涵盖了不同规模和结构的开源模型，包括 Llama-3.2 (1B, 3B)、Llama-3.1 (8B) 和 Llama-2 (13B)，涉及 GQA（分组查询注意力）和全注意力机制。
*   **对比基准 (Benchmarks)**：
    *   **ZeRO-3 (FSDP)**：PyTorch 2.0 编译优化的参数分片方案。
    *   **手写 SP 方案**：高度优化的 DeepSpeed-Ulysses 和 RingAttention 的手写实现（均在 Inductor 后端编译）。
*   **评估指标**：最大可训练序列长度（Trainability）和端到端训练迭代耗时（Throughput）。

### 4. 资源与算力
实验在多种硬件平台上进行了验证，体现了良好的移植性：
*   **NVIDIA 平台**：8 × A100 (80GB) 显卡集群；2 × GH200 (96GB) 超级芯片。
*   **AMD 平台**：2 × MI250 (64GB) 显卡。
*   **软件环境**：PyTorch 2.7, CUDA 12.8, ROCm 6.4。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的测试，包括不同模型规模（1B 到 13B）、不同硬件厂商（NVIDIA vs AMD）、以及不同序列长度下的性能表现。
*   **消融实验**：专门对比了仅开启 SP-Pass 与同时开启 SP+SAC-Pass 的效果，验证了 SAC 在长文本下的关键作用。
*   **充分性评价**：实验设计较为全面，不仅对比了自动化程度，还对比了性能上限。通过跨硬件验证，证明了该编译器方案的通用性，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **训练能力大幅提升**：相比于手写的 DeepSpeed-Ulysses，AutoSP 在 NVIDIA 硬件上将最大训练长度提升了高达 **2.7倍**，在 AMD 上提升了 **2.5倍**。
*   **性能损耗极低**：AutoSP 达到了手写优化基准约 **97%** 的运行速度。在长文本场景下，增加的重计算开销被注意力机制的巨大计算量所掩盖。
*   **自动化优势**：开发者只需几行代码即可启用优化，无需手动重构复杂的分布式通信逻辑。

### 7. 优点
*   **易用性**：将复杂的并行策略下沉到编译器层，极大提高了开发者的生产力。
*   **内存效率**：SAC 策略精准捕捉了长文本训练的计算特性，通过极小的计算代价换取了巨大的内存收益。
*   **平台无关性**：作为 PyTorch 原生编译器的扩展，能够无缝支持 NVIDIA 和 AMD 等多种后端。

### 8. 不足与局限
*   **算子集依赖**：SP-Pass 仍需要手动维护一套“需要调整大小的算子列表”（RESIZE_BUFS 等），对于非 Transformer 架构或极其特殊的自定义算子，可能需要额外的手动适配。
*   **策略单一**：目前主要集成了类 Ulysses 的序列并行方案，对于超大规模集群下可能需要的多种并行模式（如 SP+TP+PP）的自动组合，文中探讨较少。
*   **显存占比限制**：在 13B 等较大模型上，由于优化器状态占据了近 50% 的显存，AutoSP 带来的训练长度提升比例会有所下降。

（完）
