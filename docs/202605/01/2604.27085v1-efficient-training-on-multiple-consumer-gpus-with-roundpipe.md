---
title: Efficient Training on Multiple Consumer GPUs with RoundPipe
title_zh: 使用 RoundPipe 在多台消费级 GPU 上进行高效训练
authors: "Yibin Luo, Shiwei Gao, Huichuan Zheng, Youyou Lu, Jiwu Shu"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.27085v1"
tags: ["query:mlsys"]
score: 10.0
evidence: 在消费级GPU上进行LLM训练的新型流水线调度
tldr: 针对消费级GPU显存受限和PCIe带宽低导致的流水线并行效率低下问题，本文提出RoundPipe调度方案。它将GPU视为无状态执行单元，通过轮询方式动态分发计算阶段，打破了权重绑定限制并实现了近乎零气泡的流水线。实验证明，RoundPipe在8卡RTX 4090上实现了最高2.16倍的加速，并支持在单机微调235B超大模型，显著降低了LLM训练成本。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.27085v1/fig-001.webp\", \"caption\": \"Figure 17. Operational intensity vs. batch size for representative dense and MoE models at 𝑠=2048. Horizontal lines mark the ridge-point OI for GPUs in Table 2.\", \"page\": 15, \"index\": 1, \"width\": 485, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2604.27085v1/fig-002.webp\", \"caption\": \"Figure 1. Looped BFS schedule and RoundPipe schedule when training a 12-layer model with language model head (layer 13) on 4 GPUs. F/B denotes forward/backward, and numbers indicate the index of layers contained in the stage. Looped BFS schedule processes 8 microbatches at a time while RoundPipe processes them in two rounds.\", \"page\": 2, \"index\": 2, \"width\": 1039, \"height\": 429}, {\"url\": \"assets/figures/arxiv/2604.27085v1/fig-003.webp\", \"caption\": \"Figure 8. (a) The blocking approach copies weights (P cp) and gradients (G cp) on the main thread. (b) RoundPipe’s event-based protocol offloads copies to the optimizer worker (O) and uses per-layer events. The four arrows from left to right correspond to ordering constraints (1)-(4), respectively.\", \"page\": 8, \"index\": 3, \"width\": 935, \"height\": 341}]"
motivation: 传统流水线并行在消费级GPU上因权重绑定导致负载不均，产生严重的流水线气泡并限制了整体吞吐量。
method: 提出RoundPipe调度器，通过轮询分发计算阶段、优先级感知传输引擎及自动化层切分，实现GPU资源的动态高效利用。
result: 在8卡RTX 4090服务器上，RoundPipe相比现有基准实现了1.48至2.16倍的加速，并成功支持了Qwen3-235B模型的长序列微调。
conclusion: RoundPipe有效解决了消费级硬件在大模型微调中的瓶颈，为低成本训练超大规模模型提供了高效的开源解决方案。
---

## 摘要
在消费级 GPU 上微调大语言模型（LLMs）具有极高的成本效益，但受限于有限的 GPU 显存和缓慢的 PCIe 互连。流水线并行（Pipeline Parallelism, PP）结合 CPU 卸载（CPU offloading）通过减少通信开销缓解了这些硬件瓶颈。然而，现有的流水线并行调度方案面临一个被称为“权重绑定问题”（weight binding issue）的固有局限。将不均匀的模型阶段（例如，LM head 很大）绑定到 GPU 会将流水线的吞吐量限制在负载最重的 GPU 上，从而导致严重的流水线气泡（pipeline bubbles）。在本文中，我们提出了 RoundPipe，这是一种打破了消费级 GPU 服务器上权重绑定约束的新型流水线调度方案。RoundPipe 将 GPU 视为无状态执行工作节点的池，并以轮询（round-robin）方式在设备间动态调度计算阶段，从而实现近乎零气泡的流水线。为了确保训练的正确性和系统效率，RoundPipe 集成了优先级感知传输调度引擎、细粒度分布式基于事件的同步协议以及自动化层划分算法。在 8$	imes$ RTX 4090 服务器上的评估表明，在微调 1.7B 到 32B 模型时，RoundPipe 相比最先进的基准方案实现了 1.48--2.16 倍的加速。值得注意的是，RoundPipe 能够在单台服务器上对具有 31K 序列长度的 Qwen3-235B 模型进行 LoRA 微调。RoundPipe 已作为具有详尽文档的开源 Python 库公开发布。

## Abstract
Fine-tuning Large Language Models (LLMs) on consumer-grade GPUs is highly cost-effective, yet constrained by limited GPU memory and slow PCIe interconnects. Pipeline parallelism combined with CPU offloading mitigates these hardware bottlenecks by reducing communication overhead. However, existing PP schedules suffer from an inherent limitation termed the weight binding issue. Binding uneven model stages (e.g., the LM head is large) to GPUs limits the pipeline's throughput to that of the GPU with the heaviest load, leading to severe pipeline bubbles.   In this paper, we propose RoundPipe, a novel pipeline schedule that breaks the weight binding constraint on consumer GPU servers. RoundPipe treats GPUs as a pool of stateless execution workers and dynamically dispatches computation stages across devices in a round-robin manner, achieving a near-zero-bubble pipeline. To ensure training correctness and system efficiency, RoundPipe integrates a priority-aware transfer scheduling engine, a fine-grained distributed event-based synchronization protocol, and an automated layer partitioning algorithm. Evaluations on an 8$\times$ RTX 4090 server demonstrate that RoundPipe achieves 1.48--2.16$\times$ speedups over state-of-the-art baselines when fine-tuning 1.7B to 32B models. Remarkably, RoundPipe enables LoRA fine-tuning of the Qwen3-235B model with 31K sequence length on a single server.   RoundPipe is publicly available as an open-source Python library with comprehensive documentation.