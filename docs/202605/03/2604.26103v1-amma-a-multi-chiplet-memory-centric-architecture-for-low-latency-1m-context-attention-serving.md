---
title: "AMMA: A Multi-Chiplet Memory-Centric Architecture for Low-Latency 1M Context Attention Serving"
title_zh: AMMA：一种用于低延迟百万级上下文 Attention 推理服务的内存中心化多芯粒架构
authors: "Zhongkai Yu, Haotian Ye, Chenyang Zhou, Ohm Rishabh Venkatachalam, Zaifeng Pan, Zhengding Hu, Junsung Kim, Won Woo Ro, Po-An Tsai, Shuyi Pei, Yangwook Kang, Yufei Ding"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.26103v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 以内存为中心的多芯粒LLM推理架构
tldr: 针对大语言模型在长文本（1M tokens）推理中面临的内存受限和高延迟问题，本文提出了AMMA架构。AMMA采用以内存为中心的多芯粒设计，利用HBM-PNM立方体取代传统GPU计算核心，通过逻辑芯片微架构、混合并行方案和优化的通信流程，显著提升了内存带宽利用率。实验表明，AMMA在注意力机制推理延迟上比H100降低了15.5倍，能耗降低了6.9倍。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26103v1/fig-001.webp\", \"caption\": \"Figure 6: AMMA architecture hierarchy. (a) Package and cube level integration and (b) Core and SA level microarchitecture.\", \"page\": 6, \"index\": 1, \"width\": 1033, \"height\": 212}, {\"url\": \"assets/figures/arxiv/2604.26103v1/fig-002.webp\", \"caption\": \"Figure 7: (a) Three conventional SA dataflows: WS, OS, IS. (b) Tiling strategy for small 𝑁 and large 𝑁 . (c) Continuous tiling for large 𝑁 : fill of tile 𝑖+1 overlaps drain of tile 𝑖.\", \"page\": 6, \"index\": 2, \"width\": 467, \"height\": 323}, {\"url\": \"assets/figures/arxiv/2604.26103v1/fig-003.webp\", \"caption\": \"Figure 15: Hardware parameter exploration on Qwen3-235B\", \"page\": 11, \"index\": 3, \"width\": 435, \"height\": 383}, {\"url\": \"assets/figures/arxiv/2604.26103v1/fig-004.webp\", \"caption\": \"Figure 1: (a) Existing serving systems rely on GPUs for decode attention. AMMA replaces compute dies with HBM-PNM cubes to accelerate decode attention, pairing with LPUs for low-latency serving and GPUs for high-throughput serving. (b) HBM4 and beyond adopt advanced logic dies, enabling sophisticated PNM integration on the base logic die.\", \"page\": 2, \"index\": 4, \"width\": 1036, \"height\": 251}, {\"url\": \"assets/figures/arxiv/2604.26103v1/fig-005.webp\", \"caption\": \"Figure 8: Parallelism design for AMMA. (a) Naïve TP16 distributes every attention stage across all 16 cubes. (b) Our two-level hybrid parallelism maps KV heads to four cube groups via TP (Level 1), then partitions the KV cache by sequence within each group via CP (Level 2). (c) The same hierarchy combined with the reordered collective flow described in section 6.\", \"page\": 7, \"index\": 5, \"width\": 894, \"height\": 687}, {\"url\": \"assets/figures/arxiv/2604.26103v1/fig-006.webp\", \"caption\": \"Figure 11: Energy and power analysis\", \"page\": 10, \"index\": 6, \"width\": 498, \"height\": 313}, {\"url\": \"assets/figures/arxiv/2604.26103v1/fig-007.webp\", \"caption\": \"Figure 10: Decode latency speedup, normalized to H100.\", \"page\": 10, \"index\": 7, \"width\": 985, \"height\": 375}, {\"url\": \"assets/figures/arxiv/2604.26103v1/fig-008.webp\", \"caption\": \"Figure 9: Reordered collective operation flow for proj O. (a) Default two-level hybrid flow. (b) Our reordered flow.\", \"page\": 8, \"index\": 8, \"width\": 944, \"height\": 386}]"
motivation: 传统以GPU为中心的架构在处理长文本推理的内存受限型注意力计算时，存在计算资源浪费和高延迟的瓶颈。
method: 提出一种基于HBM-PNM的多芯粒架构，结合专用逻辑芯片微架构、两级混合并行方案及重排序的集合通信流程。
result: 与NVIDIA H100相比，AMMA在处理长文本注意力机制时实现了15.5倍的延迟降低和6.9倍的能耗节省。
conclusion: 以内存为中心的架构设计是解决超长上下文LLM推理性能瓶颈的有效途径，为未来硬件设计提供了重要参考。
---

## 摘要
目前所有的大语言模型（LLM）推理服务系统都以 GPU 为中心，从生产级的 Attention-FFN 解耦到 NVIDIA 的 Rubin GPU-LPU 异构平台。即使是学术界的 PIM/PNM 提案，仍将 GPU 视为跨设备通信的中心枢纽。然而，GPU 的计算密集型架构与解码阶段 Attention 的内存受限（memory-bound）特性根本不匹配，这在增加推理延迟的同时，还在闲置的计算单元上浪费了功耗和芯片面积。随着推理和智能体（agentic）工作负载将上下文长度推向一百万个 token，这一问题变得更加严重，使得 Attention 延迟成为面向用户的主要瓶颈。为了解决这些效率低下的问题，我们提出了 AMMA，一种用于低延迟长上下文 Attention 的多芯粒、以内存为中心的架构。AMMA 用 HBM-PNM 立方体取代了 GPU 计算芯粒，使可用内存带宽大约翻倍，从而更好地服务于内存受限的 Attention 工作负载。为了将这种带宽转化为成比例的性能提升，我们引入了：(i) 一种逻辑芯粒微架构，在极低的功耗和面积预算下充分利用每个立方体的内部带宽进行解码 Attention；(ii) 一种两级混合并行方案；以及 (iii) 一种重排的集合通信流，以减少芯片内芯粒间（die-to-die）的通信开销。我们进一步对每个立方体的计算能力和芯片内 D2D 链路带宽进行了设计空间探索，为硬件设计者提供了可操作的指导。评估结果显示，与 NVIDIA H100 相比，AMMA 的 Attention 延迟降低了 15.5 倍，能耗降低了 6.9 倍。

## Abstract
All current LLM serving systems place the GPU at the center, from production-level attention-FFN disaggregation to NVIDIA's Rubin GPU-LPU heterogeneous platform. Even academic PIM/PNM proposals still treat the GPU as the central hub for cross-device communication. Yet the GPU's compute-rich architecture is fundamentally mismatched with the memory-bound nature of decode-phase attention, inflating serving latency while wasting power and die area on idle compute units. The problem is compounded as reasoning and agentic workloads push context lengths toward one million tokens, making attention latency the primary user-facing bottleneck.   To address these inefficiencies, we present AMMA, a multi-chiplet, memory-centric architecture for low-latency long-context attention. AMMA replaces GPU compute dies with HBM-PNM cubes, roughly doubling the available memory bandwidth to better serve memory-bound attention workloads. To translate this bandwidth into proportional performance gains, we introduce (i) a logic-die microarchitecture that fully exploits per-cube internal bandwidth for decode attention under a minimal power and area budget, (ii) a two-level hybrid parallelism scheme, and (iii) a reordered collective flow that reduces intra-chip die-to-die communication overhead. We further conduct a design-space exploration over per-cube compute power and intra-chip D2D link bandwidth, providing actionable guidance for hardware designers. Evaluations show that AMMA achieves 15.5X lower attention latency and 6.9X lower energy consumption compared with the NVIDIA H100.

---

## 论文详细总结（自动生成）

### AMMA：用于低延迟百万级上下文 Attention 推理服务的内存中心化多芯粒架构

#### 1. 核心问题与研究动机
*   **核心问题**：当前大语言模型（LLM）推理系统以 GPU 为中心，但解码阶段（Decode Phase）的 Attention 计算是典型的**内存受限型（Memory-bound）**任务。
*   **研究动机**：
    *   **架构不匹配**：GPU 拥有极高的算力，但在长文本推理时，计算单元利用率不足 5%，大量面积和功耗浪费在闲置的算力上。
    *   **长文本挑战**：随着推理任务向百万级（1M）上下文演进，Attention 延迟成为主要瓶颈。
    *   **现有方案局限**：现有的内存处理（PIM/PNM）方案仍将 GPU 作为通信中心，且算力不足以应对 GQA（分组查询注意力）等现代技术。

#### 2. 方法论与关键技术
AMMA 提出了一种从“以 GPU 为中心”转向“以内存为中心”的全新架构：
*   **硬件架构**：将 GPU 计算芯粒完全替换为 16 个集成了近存计算（PNM）功能的 HBM 立方体，通过 4×4 2D Mesh 互联。
*   **逻辑芯粒微架构**：
    *   **小型脉动阵列（SA）**：采用 96 个 16×16 的小型脉动阵列，以匹配解码阶段极小的 Batch Size。
    *   **无末级缓存（LLC-free）**：取消 LLC 以节省 20% 的功耗，改用两级交叉开关（Crossbar）进行数据共享。
    *   **输出驻留（OS）数据流**：采用 OS 数据流配合“连续分块（Continuous Tiling）”技术，消除流水线排空开销。
*   **两级混合并行方案**：
    *   **Level 1 (TP)**：将 KV Head 映射到不同的立方体组。
    *   **Level 2 (CP)**：在组内按序列维度（Sequence Dim）拆分 KV Cache，充分利用聚合带宽。
*   **重排序集合通信流**：将传统的 AllReduce 优化为 ReduceScatter 和点对点 Reduce，减少了 2 次 AllGather 操作，显著降低了 D2D 通信延迟。

#### 3. 实验设计
*   **实验场景**：针对 1M 超长上下文的解码阶段 Attention 推理。
*   **测试模型**：Qwen3-235B、Llama4-Maverick（代表 GQA 架构）以及 DeepSeek-V3（代表 MLA 架构）。
*   **Benchmark 与对比方法**：
    *   **硬件基准**：NVIDIA H100、NVIDIA Rubin（基于规格预测）。
    *   **学术方案**：NeuPIMs（基于 PIM 的异构加速方案）。
    *   **并行策略对比**：TP16（默认张量并行） vs. HP（混合并行） vs. HP_RO（混合并行+重排序通信）。

#### 4. 资源与算力
*   **算力规格**：AMMA 单芯片提供 1536 TFLOPS (FP8) 算力，聚合 HBM 带宽达 44 TB/s。
*   **模拟工具**：使用 **ScaleSim** 模拟单立方体性能，使用 **AstraSim** 模拟多芯粒互联性能。
*   **实测资源**：在 8×H100 服务器上进行真实 GPU 性能分析（Profiling），用于校准模拟器。
*   **训练时长**：本文属于硬件架构设计论文，不涉及模型训练时长。

#### 5. 实验数量与充分性
*   **实验维度**：涵盖了不同 Batch Size (1-32)、不同序列长度 (4K-1M)、不同模型架构（GQA/MLA）以及硬件参数探索（算力 vs. 带宽）。
*   **充分性**：实验对比了当前最先进的商业 GPU（H100/Rubin）和学术前沿方案，并进行了详细的消融实验（Ablation Study）来验证通信优化的有效性。
*   **客观性**：通过对 H100 的真实功耗拆解（如 LLC 功耗占比）来支撑设计动机，实验数据具有较强的说服力。

#### 6. 主要结论与发现
*   **性能飞跃**：在 GQA 模型上，AMMA 的 Attention 延迟比 H100 降低了 **15.5 倍**，比 Rubin 降低了约 2 倍。
*   **能效极高**：能耗比 H100 降低了 **6.9 倍**，且在提供更高性能的同时，整片功耗控制在 1500W 以内。
*   **通信优化**：重排序通信流在 1M 上下文下实现了 **65.4 倍** 的通信加速。
*   **硬件洞察**：对于内存受限任务，增加计算单元的边际收益递减，提升 HBM 频率或优化通信拓扑更为有效。

#### 7. 优点
*   **范式转移**：大胆挑战了以 GPU 为中心的传统思维，提出了独立的内存中心化加速器概念。
*   **软硬协同**：不仅有硬件微架构创新，还配合了针对长文本优化的并行策略和数学证明（Softmax 正确性证明）。
*   **前瞻性**：紧扣 HBM4 技术节点和 1M 上下文的行业趋势，具有很强的实用参考价值。

#### 8. 不足与局限
*   **模型覆盖**：虽然讨论了 MLA 架构，但在单芯片算力较低时，AMMA 处理 MLA 的表现不如 GPU（因为 MLA 计算密度更高），需要额外增加算力配置。
*   **应用限制**：AMMA 专注于 Attention 层的加速，FFN 层仍需依赖 LPU 或 GPU，这增加了系统集成的复杂性。
*   **模拟偏差**：主要基于模拟器数据，尽管有 H100 实测校准，但真实芯片的散热和制造良率等物理约束可能影响最终表现。

（完）
