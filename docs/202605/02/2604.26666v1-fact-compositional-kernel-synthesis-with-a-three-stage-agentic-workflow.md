---
title: "FACT: Compositional Kernel Synthesis with a Three-Stage Agentic Workflow"
title_zh: FACT：基于三阶段智能体工作流的可组合算子合成
authors: "Sina Heidari, Dimitrios S. Nikolopoulos"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26666v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 通过算子合成优化 PyTorch 模块的框架
tldr: 本研究提出FACT框架，利用三阶段智能体工作流实现CUTLASS算子合成。针对深度学习编译器优化受限及手动编写CUDA难度大的问题，FACT通过模式发现、自动调优实现及多模式组合，将PyTorch模块转化为高性能算子。实验显示其在GEMM和MiniGPT任务中显著优于基准，有效提升了算子开发效率与性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26666v1/fig-001.webp\", \"caption\": \"Figure 5: Backend speedup comparison across Problems 1, 3, 6, and 44. Inductor andTorch-TensorRT speedups are computed from local synced benchmark_results.json files; FACT speedups are taken from the corresponding local pattern-table benchmark artifacts. For GEMMs, eager is PyTorch+cuBLAS with TF32 enabled (allow_tf32=True). This figure is intentionally separate from the pattern-ablation figure to keep compiler-backend effects distinct from pattern-composition effects.\", \"page\": 10, \"index\": 1, \"width\": 922, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2604.26666v1/fig-002.webp\", \"caption\": \"Figure 6: KernelBench 44_MiniGPTBlock on A100 with (𝐵,𝑇 ,𝐶) = (128, 512, 768). (a) End-to-end latency for PyTorch baseline, single-pattern optimizations, and both patterns composed. (b) Speedup relative to the PyTorch baseline measured in the same benchmark run as each optimized configuration (ablations leave the non-target subgraph in eager PyTorch).\", \"page\": 10, \"index\": 2, \"width\": 926, \"height\": 320}, {\"url\": \"assets/figures/arxiv/2604.26666v1/fig-003.webp\", \"caption\": \"Figure 1: Pattern mapping hierarchy. The agent identifies optimization patterns from the computation graph, each associated with a rule (e.g., GEMM, FMHA). The pattern table T indexes kernels by a tuple (𝑟, 𝜏, 𝛼) capturing rule, data type, and target architecture, enabling retrieval of workloadmatched CUTLASS implementations.\", \"page\": 3, \"index\": 3, \"width\": 364, \"height\": 445}, {\"url\": \"assets/figures/arxiv/2604.26666v1/fig-004.webp\", \"caption\": \"Figure 2: Three-stage agentic workflow for whole-model kernel optimization. Stage 1 (Pattern Discovery) extracts and analyzes the computation graph to identify optimization opportunities. Stage 2 (Pattern Realization) synthesizes CUTLASS kernels for each pattern, verifying correctness and benchmarking performance before adding to the dynamic pattern table. Stage 3 (Pattern Composition) assembles the optimized kernels into a deployable model with PyTorch extensions and measures end-to-end speedup. LLM-based actions enable systematic execution of complex optimization tasks across all stages.\", \"page\": 4, \"index\": 4, \"width\": 764, \"height\": 655}, {\"url\": \"assets/figures/arxiv/2604.26666v1/fig-005.webp\", \"caption\": \"Figure 4: Auto-tuning results for all three Level 1 problems on A100. Each point is one configuration; stars mark the best. Dashed lines indicate parity with PyTorch (cuBLAS, TF32).\", \"page\": 9, \"index\": 5, \"width\": 1056, \"height\": 407}, {\"url\": \"assets/figures/arxiv/2604.26666v1/fig-006.webp\", \"caption\": \"Table 2: Performance summary for all three Level 1 problems on A100. Throughput in TFLOPS. The PyTorch baseline uses cuBLAS with TF32 tensor cores enabled (allow_tf32=True). Default configurations are those used during initial verification; auto-tuned configurations are selected by exhaustive search over tile shapes and pipeline stages. The Scheduling column indicates the grid-level policy chosen by the agent at the Grid Level of the CUTLASS synthesis hierarchy.\", \"page\": 9, \"index\": 6, \"width\": 840, \"height\": 229}, {\"url\": \"assets/figures/arxiv/2604.26666v1/fig-007.webp\", \"caption\": \"Table 1: CUTLASS examples organized by KernelBench difficulty level and GPU architecture. Level 1: single linear algebra operators; Level 2: fused operators with epilogue or data movement fusion; Level 3: complex multi-operator patterns requiring interdependent scheduling decisions.\", \"page\": 5, \"index\": 7, \"width\": 1112, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2604.26666v1/fig-008.webp\", \"caption\": \"Figure 3: Three-level hierarchy of CUTLASS kernel synthesis. From innermost to outermost: (1) Tile-level primitives configure Tensor Core operations; (2) Kernel-level pipelining determines how memory-copy and compute overlap—warpspecialized designs onHopper versusmultistage pipelines on Ampere; (3) Grid-level scheduling controls work distribution across thread blocks. Each level presents distinct configuration decisions that the agent must resolve correctly for the target architecture and workload.\", \"page\": 5, \"index\": 8, \"width\": 508, \"height\": 358}]"
motivation: 现有的深度学习编译器受限于预设算子库，而手动编写高性能CUDA算子门槛高且LLM直接生成代码难以利用成熟库的优化。
method: 提出一种三阶段工作流，包括基于LLM的图模式发现、基于CUTLASS的算子实现与自动调优，以及多算子的端到端组合。
result: 在A100上，FACT生成的算子在GEMM任务中比cuBLAS快1.06-1.18倍，在MiniGPT块上实现了2.79倍的端到端加速。
conclusion: FACT通过自动化CUTLASS算子合成与调优，为从PyTorch模型到高性能部署算子的转化提供了一条实用且高效的路径。
---

## 摘要
深度学习编译器和厂商库提供了强大的基准性能，但受限于有限的、由工程师维护的算子目录。当这些目录缺少所需的优化时，从业者通常会改用手写的 CUDA 或 CUTLASS，这要求具备 GPU 微架构和 C++ 模板元编程的专业知识。最近基于大语言模型（LLM）的智能体目标是生成原始 CUDA 算子，但这迫使系统重新发现那些已经编码在成熟库中的优化。我们提出了 FACT（Framework for Agentic CUTLASS Transpilation），这是一个采用三阶段智能体驱动工作流的框架，通过多模式组合优化 PyTorch 模块，同时将合成过程建立在 CUTLASS C++ 之上。(1) 模式发现：LLM 智能体检查追踪图，将子图与优化规则匹配，从特定架构的索引中检索经过验证的示例，并输出优先级排序的模式。(2) 模式实现：每个模式被实现为封装在 PyTorch 扩展中的 CUTLASS 算子，经过验证，并通过扫描从 CUTLASS 层级结构中推断出的参数进行自动调优。(3) 模式组合：将扩展共同加载到单个组合模块中进行端到端基准测试。我们在 NVIDIA A100 上使用 KernelBench 评估框架和提供的模块对该工作流进行了评估。在 Level 1 中，我们将该工作流应用于三种 GEMM 工作负载（方阵乘法、批处理矩阵乘法和大 K 矩阵乘法）。自动调优的 CUTLASS 算子相比 PyTorch cuBLAS 基准提升了 1.06 倍至 1.18 倍。在 Level 3 MiniGPT 块上，将融合多头注意力与融合 MLP GEMM+GELU 组合，实现了 2.79 倍的端到端加速。我们的工作将智能体图级模式发现与自动调优和动态模式表相结合，通过自动化 CUTLASS 算子合成和自动调优，为从追踪的 PyTorch 到可部署算子提供了一条实用路径。

## Abstract
Deep learning compilers and vendor libraries deliver strong baseline performance but are bounded by finite, engineer-curated catalogs. When these omit needed optimizations, practitioners substitute hand-written CUDA or CUTLASS, demanding expertise in GPU microarchitecture and C++ template metaprogramming. Recent LLM-based agents target kernel generation in raw CUDA, forcing rediscovery of optimizations already encoded in mature libraries.   We present FACT (Framework for Agentic CUTLASS Transpilation), a framework that employs a three-stage, agent-driven workflow optimizing PyTorch modules through multi-pattern composition while grounding synthesis in CUTLASS C++. (1) Pattern discovery: an LLM agent inspects the traced graph, matches subgraphs to optimization rules, retrieves vetted examples from an architecture-specific index, and outputs prioritized patterns. (2) Pattern realization: each pattern is implemented as a CUTLASS kernel wrapped in a PyTorch extension, verified, and auto-tuned by sweeping parameters inferred from the CUTLASS hierarchy. (3) Pattern composition: extensions are loaded together into a single composed module for end-to-end benchmarking.   We evaluate the workflow using KernelBench's evaluation framework and provided modules on an NVIDIA A100. On Level 1, we apply the workflow to three GEMM workloads (square matrix multiply, batched matrix multiply, and large-$K$ matrix multiply). Auto-tuned CUTLASS kernels improve over PyTorch cuBLAS baseline by $1.06\times$--$1.18\times$. On Level 3 MiniGPT block, composing fused multi-head attention with fused MLP GEMM+GELU yields $2.79\times$ end-to-end speedup. Our work couples agentic graph-level pattern discovery with auto-tuning and a dynamic pattern table, offering a practical path from traced PyTorch to deployable kernels by automating CUTLASS kernel synthesis and auto-tuning.

---

## 论文详细总结（自动生成）

# FACT: 基于三阶段智能体工作流的可组合算子合成论文总结

### 1. 核心问题与整体含义
*   **研究动机**：深度学习编译器（如 TVM、PyTorch Inductor）虽然自动化程度高，但受限于预设的算子库，难以快速适配新硬件特性。手动编写高性能 CUDA 或 CUTLASS 代码则需要极高的专业知识。
*   **现有方案缺陷**：目前基于大语言模型（LLM）的智能体通常直接生成原始 CUDA 代码，这导致智能体必须“重新发明”那些早已在成熟库（如 CUTLASS）中实现的复杂架构优化。
*   **核心目标**：提出 **FACT** 框架，通过 LLM 智能体驱动 CUTLASS 模板的自动化合成、调优与组合，将 PyTorch 模型转化为高性能的可部署算子，弥补编译器灵活性不足与手动开发成本过高之间的鸿沟。

### 2. 方法论
FACT 采用三阶段智能体驱动的工作流：
*   **模式发现 (Pattern Discovery)**：LLM 智能体分析 PyTorch 计算图，利用结构化匹配识别可优化的子图（如 GEMM、注意力机制），并从特定架构的索引中检索经过验证的 CUTLASS 示例。
*   **模式实现 (Pattern Realization)**：
    *   **三层级合成**：在 CUTLASS 的 **Tile 级**（配置 Tensor Core 指令）、**Kernel 级**（配置流水线策略，如 Ampere 的多级流水线）和 **Grid 级**（配置调度策略，如 Stream-K）进行代码合成。
    *   **自动调优 (Auto-tuning)**：智能体自动推断需要扫描的参数空间（如分块形状、流水线阶段数），生成参数化代码并执行性能搜索，选出最优配置。
*   **模式组合 (Pattern Composition)**：将多个优化后的算子封装为 PyTorch 扩展并重新组合成一个完整的模块，进行端到端性能评估。
*   **动态模式表 (Dynamic Pattern Table)**：建立持久化的优化目录，存储已验证的算子实现，实现跨任务的知识复用。

### 3. 实验设计
*   **评估框架**：使用 **KernelBench** 标准化基准。
*   **实验场景**：
    *   **Level 1 (单算子)**：方阵乘法 (Square GEMM)、批处理矩阵乘法 (Batched GEMM)、长 K 轴矩阵乘法 (Large-K GEMM)。
    *   **Level 3 (复杂块)**：MiniGPT 块（包含融合多头注意力 FMHA 和 MLP+GELU 融合算子）。
*   **对比基准 (Baselines)**：
    *   PyTorch Eager 模式（调用 cuBLAS TF32）。
    *   PyTorch Inductor (max-autotune 模式)。
    *   Torch-TensorRT。

### 4. 资源与算力
*   **硬件平台**：NVIDIA A100-SXM4-80GB GPU (Ampere 架构)。
*   **软件环境**：CUTLASS 3.8.0, CUDA 12.4, PyTorch 2.4.1。
*   **算力细节**：文中未明确总训练时长，但详细记录了自动调优的搜索规模。例如，在方阵乘法任务中，智能体扫描了 98 种不同的 Tile 形状与流水线阶段组合。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 GEMM 的三种核心调度模式（Data-Parallel, Batched, Stream-K）以及一个完整的 Transformer 模块。
*   **消融实验**：针对 Level 3 任务进行了消融分析，分别测试了单独优化 FMHA、单独优化 MLP 以及两者组合后的性能贡献。
*   **客观性**：实验对比了目前工业界主流的编译器后端（Inductor 和 TensorRT），并使用了 KernelBench 这一第三方基准，评估过程较为客观、公平。

### 6. 主要结论与发现
*   **性能超越基准库**：在 Level 1 任务中，FACT 生成的算子比高度优化的 cuBLAS 快 **1.06x 至 1.18x**。
*   **端到端加速显著**：在 MiniGPT 块上，通过多算子组合实现了 **2.79x** 的端到端加速，远超 PyTorch Inductor (1.86x) 和 Torch-TensorRT (1.81x)。
*   **调优至关重要**：实验显示，自动调优后的算子效率（如 79.8% 的峰值性能）远高于默认配置（45.8%），证明了智能体驱动的参数搜索的必要性。
*   **混合精度处理**：智能体能自动识别精度风险（如 Large-K 任务中的 FP16 溢出），并自动切换至混合精度（FP16 输入 + FP32 累加）以保证正确性。

### 7. 优点
*   **库驱动的可靠性**：基于 CUTLASS 模板而非原始 CUDA，保证了生成的代码具有工业级的优化基础。
*   **全栈自动化**：实现了从图分析、代码合成、正确性验证到自动调优的全流程闭环。
*   **架构感知**：能够根据目标 GPU 架构（如 Ampere vs Hopper）自动选择不同的流水线和调度策略。

### 8. 不足与局限
*   **硬件覆盖面**：目前主要针对 Ampere 架构进行了验证，对最新的 Hopper 架构（H200）的支持仍处于开发阶段。
*   **调优成本**：自动调优过程涉及大量的编译和运行测试，对于实时性要求极高的场景可能存在开销。
*   **依赖底层库**：FACT 的上限取决于 CUTLASS 模板库的覆盖范围，对于库中尚未包含的极新算子模式，其优化能力可能受限。

（完）
