---
title: A Workflow-Oriented Framework for Asynchronous Human-AI Collaboration in Hybrid and Compute-Intensive HPC Environments
title_zh: 面向工作流的混合及计算密集型 HPC 环境异步人机协作框架
authors: "Sergio Mendoza, Cedric Bhihe, Natalia Zamora, David Modesto, Jose Martin Bugallo Batalla, Jesus Gomez Canovas, Rafel Palomo Avellaneda, Miguel Perez Espinosa"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03743v1"
tags: ["query:mlsys"]
score: 8.5
evidence: HPC环境中异步协作的工作流框架
tldr: 针对国防安全等高风险领域中AI系统对人类参与的需求，以及高性能计算（HPC）环境下实时交互困难的问题，本文提出了一种面向工作流的异步人机协作框架。该框架支持在HPC、本地和云端等混合基础设施上运行，通过在预定义检查点暂停并等待人类输入，实现了非阻塞式监管，避免了资源闲置。该方案在MareNostrum 5等系统上得到验证，显著提升了模型训练的效率、可移植性和监管能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.03743v1/fig-001.webp\", \"caption\": \"Figure 2: UML sequence of asynchronous HITL in CIF. Two stakeholders interact through the CIF-CLI: the CIF user (who submits the workflow) and the HITL supervisor (who reviews and adapts it). The user submits a configuration file in TOML, which is parsed by the TaskScheduler. Tasks are executed on different sites, such as MN5 via SLURM, through dedicated TaskExecutors. The Watcher polls HPC jobs and notifies the JobRegistry when tasks finish. At HITL checkpoints, the supervisor provides feedback through CIF-CLI, such as adding tasks or changing parameters. The TaskScheduler then updates the workflow and continues execution. This sequence shows how CIF keeps workflows running while separating human review time from compute time.\", \"page\": 6, \"index\": 1, \"width\": 881, \"height\": 566}, {\"url\": \"assets/figures/arxiv/2605.03743v1/fig-002.webp\", \"caption\": \"Figure 5: Validation workflow for CIF (flowchart view). Inference runs on the Tactical Unit, while training and evaluation are executed on MN5-ACC. The CIF framework and CIF-CLI operate on the Tactical Unit and control remote tasks without being installed on MN5. Two HITL checkpoints guide the flow: inference validation and model validation. This figure shows how CIF combines local inference on the Tactical Unit, remote HPC training, and asynchronous HITL to adapt workflows dynamically.\", \"page\": 11, \"index\": 2, \"width\": 660, \"height\": 491}, {\"url\": \"assets/figures/arxiv/2605.03743v1/fig-003.webp\", \"caption\": \"Figure 1: Layered architecture of CIF. At the top, CIF-CLI receives the workflow file and starts execution. The middle layer (TaskScheduler, TaskExecutor, WorkflowExecution) organizes tasks and controls their flow. The Watcher and JobRegistry manage monitoring and state across the system. At the bottom, tasks run on different sites (local, HPC, cloud). This layered view shows that all modules act according to the workflow, making CIF workflow-centric, adaptive, and ready for asynchronous HITL.\", \"page\": 4, \"index\": 3, \"width\": 864, \"height\": 425}, {\"url\": \"assets/figures/arxiv/2605.03743v1/fig-004.webp\", \"caption\": \"Figure 6: Qualitative inference results before and after HITL-supervised retraining. (a) Baseline: generic “vehicle” detection, missing ships. (b) Post-retraining: multiple ships detected with higher precision.\", \"page\": 12, \"index\": 4, \"width\": 1002, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2605.03743v1/fig-005.webp\", \"caption\": \"Table 1: Comparison of selected workflow frameworks against CIF.\", \"page\": 12, \"index\": 5, \"width\": 1010, \"height\": 215}, {\"url\": \"assets/figures/arxiv/2605.03743v1/fig-006.webp\", \"caption\": \"Figure 4: Workflow structure for the ship-detection case study. The figure shows the initial configuration of the workflow defined in the configuration file. Dependencies between tasks are visible together with two HITL checkpoints. The Inference Reviewer can approve results or trigger retraining on MN5, while the Model Reviewer validates the retrained model. This structure illustrates how CIF workflows adapt dynamically under human supervision.\", \"page\": 9, \"index\": 6, \"width\": 964, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2605.03743v1/fig-007.webp\", \"caption\": \"Figure 3: Abstract workflow for the ship-detection case study. Two execution sites are used: the Tactical Unit and MN5 — HPC Resources in BSC. In a real mission, input images would come from drones, satellites, or other sensors (left side). Inference runs on the Tactical Unit because it is lightweight, while training is computeintensive and runs on MN5. Two HITL checkpoints are included: one where the supervisor reviews inference results and decides whether to trigger retraining, and another where the updated model is validated after training to decide if it can be deployed or needs improvement.\", \"page\": 9, \"index\": 7, \"width\": 864, \"height\": 350}]"
motivation: 解决高性能计算环境下因计算密集和资源限制导致的人机实时交互困难及资源浪费问题。
method: 开发了一种支持异步协作的工作流框架，允许在不停止底层计算作业的情况下通过检查点获取人类反馈。
result: 成功在MareNostrum 5等混合架构中实现了支持SLURM调度和容器化任务的非阻塞式人机协作。
conclusion: 该框架为复杂计算环境下的AI模型训练提供了高效、灵活且具备强监管能力的协作方案。
---

## 摘要
在高风险的国防和安全背景下，人类参与对于 AI 系统的训练和部署至关重要。然而，由于计算强度和资源限制，在 HPC 环境中进行实时交互是不切实际的。我们提出了一种工作流框架，能够在包括 HPC 集群、本地机器和云平台在内的混合基础设施上实现异步人机协作。工作流可以在定义的检查点暂停以等待人工输入，而无需停止底层的计算作业，从而防止资源闲置并实现非阻塞式监管。该框架支持与基于 SLURM 的调度、容器化和原生任务的交互，并针对需要人类判断和适应性的场景进行了定制。我们在 MareNostrum 5 等系统上的模型训练中演示了其应用，突出了其在操作性 AI 工作流中的可移植性、效率和监督方面的优势。

## Abstract
Human involvement is critical in training and deploying AI systems in high-stakes defence and security contexts. However, real-time interaction is impractical in HPC environments due to compute intensity and resource constraints. We present a workflow framework that enables asynchronous human-AI collaboration across hybrid infrastructures, including HPC clusters, local machines, and cloud platforms. Workflows can pause at defined checkpoints for human input without halting underlying compute jobs, preventing idle resources and enabling non-blocking supervision. The framework supports interaction with SLURM-based scheduling, containerized and native tasks, and is customized for scenarios requiring human judgment and adaptability. We demonstrate its application in model training on systems like MareNostrum 5, highlighting benefits in portability, efficiency, and oversight in operational AI workflows.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **协作创新框架（Collaborative Innovation Framework, CIF）** 的系统，旨在解决高性能计算（HPC）环境下 AI 模型训练与人机协作（HITL）之间的矛盾。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心矛盾**：在国防、安全等高风险领域，AI 系统需要人类专家进行实时监督和调整（HITL）。然而，传统的 HPC 环境（如基于 SLURM 的集群）采用批处理模式，一旦任务提交就无法交互。
*   **现有局限**：现有的工作流管理工具（如 Airflow, Nextflow）要么是代码中心化的（难以动态修改），要么在处理人类干预时会阻塞计算资源，导致昂贵的 HPC 资源在等待人类决策时处于闲置状态。
*   **研究目标**：开发一种面向工作流的架构，支持在混合基础设施（本地、云、HPC）上实现**异步、非阻塞**的人机协作，允许在不停止底层计算任务的情况下进行人类干预和工作流重构。

### 2. 论文提出的方法论
*   **核心思想**：提出**面向工作流的架构（WOA）**，将工作流配置文件（TOML 格式）作为系统的核心。通过将 HITL 检查点定义为异步任务，实现计算与决策的解耦。
*   **关键技术组件**：
    *   **TaskScheduler（任务调度器）**：解析 TOML 配置，管理任务依赖，处理运行时分支跳转。
    *   **TaskExecutor（任务执行器）**：处理跨环境（本地/HPC/云）的任务提交，支持容器化（Docker/Singularity）。
    *   **Watcher（监控器）**：针对 HPC 环境，通过轮询特定信号文件（如 `.done` 标记）来异步检测任务完成状态。
    *   **JobRegistry（作业注册表）**：维护全局状态，确保人类决策能实时反馈到执行逻辑中。
*   **算法流程**：解析配置 -> 调度任务 -> 异步执行 -> 触发 HITL 检查点（逻辑暂停相关分支） -> 人类审查并反馈（如修改参数或增加任务） -> 动态重构工作流 -> 继续执行。

### 3. 实验设计
*   **应用场景**：**海上监视中的船只检测**。
    *   **本地端（Tactical Unit）**：运行轻量级推理任务。
    *   **HPC 端（MareNostrum 5）**：运行计算密集型的模型重训练和评估任务。
*   **实验流程**：推理任务完成后进入 HITL 检查点，人类专家审查检测结果。若结果不佳，专家触发重训练任务；重训练在 HPC 上完成后，再次通过 HITL 验证模型指标，决定是否部署。
*   **对比基准（Benchmark）**：论文通过功能维度（配置方式、HPC 支持、容器化、异步 HITL、运行时重构）将 CIF 与 Airflow, Nextflow, Snakemake, COMPSs 等 9 种主流框架进行了定性对比分析。

### 4. 资源与算力
*   **硬件平台**：使用了西班牙巴塞罗那超级计算中心（BSC）的 **MareNostrum 5 (MN5)** 超级计算机。
*   **具体配置**：提到了使用 MN5 的加速器分区（MN5-ACC），涉及 GPU 计算。
*   **明确性说明**：文中**未明确列出**具体的 GPU 型号（推测为 NVIDIA H100/A100 级别）、具体使用的 GPU 数量以及重训练的确切时长。实验重点在于框架的调度能力而非模型本身的训练性能。

### 5. 实验数量与充分性
*   **实验规模**：主要展示了一个完整的端到端战术用例（船只检测）。
*   **充分性评价**：
    *   **功能验证充分**：成功演示了跨节点（本地到 HPC）的调度和异步 HITL 的闭环。
    *   **统计数据不足**：缺乏定量性能指标（如调度延迟、资源利用率提升百分比、多并发工作流下的稳定性测试）。
    *   **客观性**：对比分析涵盖了主流框架，评价标准较为客观，但实验部分更偏向于“概念验证（PoC）”。

### 6. 主要结论与发现
*   **异步协作可行性**：证明了可以在不阻塞 HPC 作业的前提下引入人类决策，显著提高了高风险 AI 任务的灵活性。
*   **动态适应性**：通过 TOML 配置文件的运行时修改，工作流能够根据人类反馈动态增加任务或改变路径，实现了“快速适应（Rapid Adaptation）”。
*   **性能优化**：通过将推理留在边缘端、训练放在 HPC 端，减少了不必要的大规模数据传输，优化了响应时间。

### 7. 优点
*   **非阻塞设计**：解决了 HPC 资源浪费的痛点，人类审查时间与计算时间分离。
*   **声明式配置**：使用 TOML 降低了非开发人员（如领域专家）的使用门槛，提高了工作流的可移植性。
*   **混合环境支持**：原生集成 SLURM 调度器和容器技术，适配现代科研与国防的复杂基础设施。

### 8. 不足与局限
*   **信号机制简单**：依赖文件系统的 `.done` 标记进行状态同步，在复杂网络或大规模并行环境下可能存在可靠性风险。
*   **定量分析缺失**：论文缺乏关于框架开销（Overhead）和资源节省比例的具体数值支撑。
*   **安全限制**：在国防场景下，跨环境的数据交换和网络连接可能受到严格限制，论文对安全协议和加密传输的讨论较少。
*   **应用范围**：目前仅在单一用例上验证，对于超大规模、具有复杂循环依赖的工作流支持尚待观察。

（完）
