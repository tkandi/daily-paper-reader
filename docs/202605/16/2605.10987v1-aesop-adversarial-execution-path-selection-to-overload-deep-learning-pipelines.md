---
title: "AESOP: Adversarial Execution-path Selection to Overload Deep Learning Pipelines"
title_zh: AESOP：通过对抗性执行路径选择使深度学习流水线过载
authors: "Tingxi Li, Mingfang Ji, Ravishka Shemal Rathnasuriya, Simin Chen, Yitao Hu, Wei Yang"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.10987v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 动态深度学习推理流水线的效率与负载分析
tldr: 本研究针对现代深度学习动态推理流水线提出了一种名为AESOP的对抗攻击框架。由于流水线上游组件决定了下游的负载，研究者发现通过诱导模型选择计算量最大的执行路径，可以造成严重的资源枯竭。AESOP结合了路径排序与自适应损失加权，在多种流水线和生产环境下验证了其有效性。实验表明，该方法比传统单模型攻击更具破坏性，能迫使系统在吞吐量崩溃和数据丢失之间做抉择，揭示了流水线架构的安全隐患。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.10987v1/fig-001.webp\", \"caption\": \"TABLE VI: Randomly mix clean inputs with perturbed inputs. Total number of inputs is 100, covers ratio: clean:perturbed=9:1, 9.5:0.9 and 9.9:0.1. Condition denotes the defense technique applied, respectively: no defense, gaussian noise, spatial smoothing, SVM input filtering, and the combination of batch size 16 and confidence threshold 0.5. The unit of throughput is input per second; the unit of average end-to-end latency (AvgE2E), p50, p95, p99 is second; LPR# denotes the amount of workload processed by the license-plate-recognition module; Filtered denotes the number of workload filtred by SVM. Mean ± std over 5 random seeds.\", \"page\": 11, \"index\": 1, \"width\": 1058, \"height\": 398}, {\"url\": \"assets/figures/arxiv/2605.10987v1/fig-002.webp\", \"caption\": \"TABLE I: Performance of Attacks on Different Pipelines. FLOPs× and Latency× denote multiplicative increases in end-to-end computational cost and runtime relative to benign inputs. The unit of latency is second.\", \"page\": 7, \"index\": 2, \"width\": 854, \"height\": 706}, {\"url\": \"assets/figures/arxiv/2605.10987v1/fig-003.webp\", \"caption\": \"Fig. 2: Approach Overview.\", \"page\": 3, \"index\": 3, \"width\": 924, \"height\": 964}, {\"url\": \"assets/figures/arxiv/2605.10987v1/fig-004.webp\", \"caption\": \"Fig. 4: Pipeline applications used in evaluation. Implementation details are stated in Appendix A.\", \"page\": 8, \"index\": 4, \"width\": 537, \"height\": 591}, {\"url\": \"assets/figures/arxiv/2605.10987v1/fig-005.webp\", \"caption\": \"TABLE VII: Randomly mix clean inputs with perturbed inputs. Total number of inputs is 100, covers ratio: clean:perturbed=9:1, 9.5:0.9 and 9.9:0.1. The unit of throughput is input per second; the unit of average end-to-end latency (AvgE2E), p50, p95, p99 is second; LPR# denotes the amount of workload processed by the license-plate-recognition module; Filtered denotes the number of workload filtred by SVM.\", \"page\": 12, \"index\": 5, \"width\": 914, \"height\": 347}, {\"url\": \"assets/figures/arxiv/2605.10987v1/fig-006.webp\", \"caption\": \"TABLE VIII: Pipeline performance under scheduling, batching, buffering, and defense configurations. The unit of throughput is input per second; the unit of average end-to-end latency (AvgE2E), p50, p95, p99 is second; LPR# denotes the amount of workload processed by the license-plate-recognition module; Drops denotes the number of workload dropped by bounded buffering.\", \"page\": 12, \"index\": 6, \"width\": 936, \"height\": 206}, {\"url\": \"assets/figures/arxiv/2605.10987v1/fig-007.webp\", \"caption\": \"TABLE III: Ablation Study. FLOPs× and Latency× denote multiplicative increases in end-to-end computational cost and runtime relative to benign inputs. The unit of latency is second.\", \"page\": 9, \"index\": 7, \"width\": 508, \"height\": 243}]"
motivation: 现代机器学习流水线中组件间的动态耦合创造了现有单模型攻击无法利用的新效率攻击面。
method: 提出AESOP框架，通过漏洞引导的路径排序和自适应损失加权，寻找并诱导流水线执行高开销路径。
result: 在白盒环境下使计算量增加2407倍、延迟增加419倍，并能使受保护系统的吞吐量从0.578降至0.006输入/秒。
conclusion: 动态流水线的结构化特征使其极易受到路径选择攻击，现有的系统级防御难以在不牺牲性能的情况下抵御此类威胁。
---

## 摘要
现代机器学习部署越来越多地将专门化模型组合成动态推理流水线，其中上游组件生成的中间预测决定了下游组件的工作负载和输入。因此，处理输入的成本并非由单个模型决定，而是由两个耦合因素决定：每个被调用组件的单次推理成本及其工作负载量。由于这些流水线在严格的实时约束下运行，效率是系统可用性的基本要求。我们展示了这种结构创造了一个针对效率的攻击面，而现有的针对单个模型的方法无法利用这一点：在相同的输入和预算下，路径感知攻击可使 FLOPs 膨胀 2,407 倍，而最强的单模型基准仅达到 117 倍——这 20 倍的差距完全归因于攻击指向的位置。我们将此形式化为对抗性路径选择问题，并提出了 AESOP，这是一个结合了漏洞引导路径排序与自适应损失加权的框架。我们在五个流水线以及一个包含批处理、有界缓冲和置信度阈值防御的生产级真实部署变体上评估了 AESOP。AESOP 在白盒设置下实现了高达 2,407 倍的 FLOPs 和 419 倍的延迟膨胀，在灰盒设置下实现了 58 倍的 FLOPs 和 17 倍的延迟膨胀。在系统级防御下，攻击并未被消除而是被重定向：流水线被迫在吞吐量崩溃（从 0.578 降至 0.006 输入/秒）和为维持吞吐量而损失 96.7% 的数据之间做出选择。

## Abstract
Modern machine learning deployments increasingly compose specialized models into dynamic inference pipelines, where upstream components produce intermediate predictions that determine the workload and inputs of downstream components. The cost of processing an input is therefore not determined by any single model, but by two coupled factors: the per-inference cost of each invoked component and its workload volume. Because these pipelines run under hard real-time constraints, efficiency is a fundamental requirement for system availability. We show that this structure creates an efficiency-attack surface that existing methods targeting single models cannot exploit: on identical inputs and budgets, path-aware targeting inflates FLOPs by $2,407\times$ while the strongest single-model baseline achieves $117\times$ -- a $20\times$ gap attributable entirely to where the attack is directed. We formalize this as the adversarial path-selection problem and present AESOP, a framework combining vulnerability-guided path ranking with adaptive loss weighting. We evaluate AESOP on five pipelines plus a production-realistic deployment variant with batching, bounded buffering, and confidence-threshold defenses. AESOP achieves up to $2,407\times$ FLOPs and $419\times$ latency inflation in white-box setting and 58$\times$ FLOPs / 17$\times$ latency in gray-box settings. Under system-level defenses, the attack is not neutralized but redirected: pipelines are forced to choose between throughput collapse ($0.578 \to 0.006$ input/s) and $96.7\%$ data loss to sustain throughput.

---

## 论文详细总结（自动生成）

### AESOP: 对抗性执行路径选择以使深度学习流水线过载 —— 论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：现代机器学习系统已从单一模型转向由多个专门化模型组成的**动态推理流水线**（如自动驾驶、视频监控）。在这些流水线中，上游组件的输出决定了下游组件的工作负载。
*   **研究动机**：现有的效率攻击（旨在增加计算开销或延迟）主要针对单一模型（如增加NMS开销或延迟终止符）。然而，流水线的总成本由**执行路径的选择**决定。作者发现，仅仅通过诱导输入进入计算最密集的路径，就能产生远超单模型攻击的破坏力。

#### 2. 方法论：AESOP 框架
AESOP（Adversarial Execution-path Selection to Overload Pipelines）将攻击分为两个协调阶段：
*   **阶段 1：漏洞引导的路径排序（Vulnerability-Guided Path Ranking）**
    *   **剖析**：测量每个组件的单次推理成本（$c_v$）、输出基数（$o_v$）和门控行为（$g_v$）。
    *   **评分**：定义“组件漏洞得分”，衡量对抗输入相对于正常输入的计算放大倍数。
    *   **路径选择**：计算所有可能执行路径的总漏洞得分，选择得分最高的路径 $\pi^*$ 作为攻击目标。
*   **阶段 2：自适应损失加权（Adaptive Loss Weighting）**
    *   **核心思想**：根据各组件对路径总漏洞的贡献度，动态分配损失函数的权重。
    *   **后端无关性**：AESOP 作为一个系统级框架，可以集成任何现有的单模型效率攻击（如 Overload, SlowTrack）作为其底层扰动生成器。

#### 3. 实验设计
*   **应用场景**：设计了 5 个基准流水线（交通监控、野生动物保护、智能家居、高速监控、Amber警报）和 1 个**生产级真实变体**（包含 RT-DETR, GPT-2 等复杂组件）。
*   **数据集**：MS COCO 和 Animal Faces。
*   **对比基准（Benchmarks）**：
    *   **Overload**：针对目标检测延迟的攻击。
    *   **Phantom Sponges**：利用非极大值抑制（NMS）的攻击。
    *   **SlowTrack**：针对视频跟踪延迟的攻击。
*   **攻击设置**：包括白盒设置（完全访问模型参数）和灰盒设置（仅知拓扑，通过代理模型迁移攻击）。

#### 4. 资源与算力
*   **硬件设备**：
    *   扰动生成：4 × NVIDIA RTX 3090 (24GB) GPU，Intel Xeon E5-2686 v4 CPU。
    *   大模型托管：Qwen2-70B-Instruct 模型运行在 4 × NVIDIA A100 (80GB) 节点上。
*   **训练/优化时长**：每个攻击样本进行 400 次迭代优化，扰动预算 $\ell_\infty$ 限制为 4%。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 种不同拓扑的流水线，进行了白盒攻击、灰盒迁移攻击、消融实验以及针对生产环境防御手段的鲁棒性测试。
*   **充分性评价**：实验设计非常充分且具有说服力。作者不仅在理想环境下测试，还引入了批处理（Batching）、有界缓冲区（Bounded Buffering）和置信度阈值等实际系统防御措施，证明了攻击在现实系统中的生存能力。

#### 6. 主要结论与发现
*   **巨大的放大效应**：在相同预算下，AESOP 实现的 FLOPs 放大倍数高达 **2,407倍**，而最强的单模型基准仅为 117倍，差距达 20倍。
*   **路径选择是关键**：攻击的破坏力主要取决于“攻击哪里”而非“扰动多强”。在非对称流水线中，选错路径会导致攻击效能下降数个数量级。
*   **防御的副作用**：现有的系统级防御（如缓冲区限制）无法消除攻击，只能将其转化为**严重的数据丢失**（丢弃 96.7% 的检测结果），从而导致系统功能性失效。
*   **灰盒有效性**：即使在灰盒设置下，通过公开的架构信息进行路径排序，仍能实现高达 58倍的计算量膨胀。

#### 7. 优点
*   **视角独特**：首次将效率攻击从“单模型优化”提升到“流水线路径选择”的高度。
*   **系统级深度**：实验考虑了生产环境中的调度、缓冲和排队机制，具有极高的工程参考价值。
*   **通用性强**：框架与具体的扰动算法解耦，能够随单模型攻击技术的进步而增强。

#### 8. 不足与局限
*   **拓扑依赖性**：灰盒攻击仍假设攻击者了解流水线的拓扑结构和组件类型，对于完全黑盒（拓扑未知）的情况讨论较少。
*   **参数估计**：对于非标准或私有架构，准确估计其计算成本（$c_v$）可能需要更复杂的侧信道分析。
*   **防御对策较少**：论文虽然揭示了现有防御的失效，但提出的“路由感知防御”仍处于概念阶段，缺乏具体的算法实现和验证。

（完）
