---
title: "Pion: A Spectrum-Preserving Optimizer via Orthogonal Equivalence Transformation"
title_zh: Pion：一种通过正交等价变换实现的谱保持优化器
authors: "Kexuan Shi, Hanxuan Li, Zeju Qiu, Yandong Wen, Simon Buchholz, Weiyang Liu"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12492v1"
tags: ["query:mlsys"]
score: 8.5
evidence: 用于大语言模型训练的光谱保持优化器
tldr: 本研究提出Pion，一种基于正交等价变换的大语言模型优化器。与Adam等加性优化器不同，Pion通过左右正交变换更新权重矩阵，在训练过程中保持奇异值不变，仅调整矩阵几何结构。实验证明Pion在LLM预训练和微调中表现稳定且具有竞争力，为模型优化提供了新视角。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12492v1/fig-001.webp\", \"caption\": \"Figure 15: Indicators for stable pretraining. These figures show the maximum attention logit in the attention block of Layer 1, 12, 24.\", \"page\": 26, \"index\": 1, \"width\": 824, \"height\": 192}, {\"url\": \"assets/figures/arxiv/2605.12492v1/fig-002.webp\", \"caption\": \"Figure 16: Comparison of final spectrum with the initial spectrum.\", \"page\": 27, \"index\": 2, \"width\": 806, \"height\": 1099}]"
motivation: 旨在开发一种能够保持权重矩阵谱特性（奇异值）不变的优化机制，以探索几何结构对模型训练的影响。
method: 通过对权重矩阵进行左、右正交变换来实现更新，从而在固定谱范数的同时调制矩阵的几何形态。
result: 实验表明Pion在LLM的预训练和微调任务中均表现出良好的稳定性和极具竞争力的性能。
conclusion: Pion作为一种谱保持优化器，为大语言模型提供了一个稳定且有效的替代方案，展示了正交变换在优化中的潜力。
---

## 摘要
我们介绍了 Pion，这是一种基于正交等价变换的大语言模型（LLM）训练谱保持优化器。与 Adam 和 Muon 等加性优化器不同，Pion 通过左、右正交变换更新每个权重矩阵，在整个训练过程中保持其奇异值不变。这产生了一种优化机制，在保持权重矩阵谱范数固定的同时调节其几何结构。我们推导了 Pion 的更新规则，系统地检查了其设计选择，并分析了其收敛行为及若干关键特性。实验结果表明，对于 LLM 的预训练和微调，Pion 为标准优化器提供了一个稳定且具有竞争力的替代方案。

## Abstract
We introduce Pion, a spectrum-preserving optimizer for large language model (LLM) training based on orthogonal equivalence transformation. Unlike additive optimizers such as Adam and Muon, Pion updates each weight matrix through left and right orthogonal transformations, preserving its singular values throughout training. This yields an optimization mechanism that modulates the geometry of weight matrices while keeping their spectral norm fixed. We derive the Pion update rule, systematically examine its design choices, and analyze its convergence behavior along with several key properties. Empirical results show that Pion offers a stable and competitive alternative to standard optimizers for both LLM pretraining and finetuning.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Pion** 的新型优化器，旨在通过保持权重矩阵的奇异值谱（Spectrum）不变，提升大语言模型（LLM）训练的稳定性和泛化能力。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在 LLM 规模化过程中，训练稳定性至关重要。现有的优化器（如 AdamW）是加性的，会导致权重矩阵的谱范数在训练中漂移。虽然 Muon 等优化器通过更新正交化解决了部分问题，但权重的谱特性仍不稳定。
*   **核心目标**：设计一种在优化过程中**完全保持权重矩阵奇异值不变**的优化器。这意味着训练只改变权重的“方向”（行/列子空间），而不改变其“拉伸比例”（奇异值），从而实现极高的训练稳定性。

### 2. 方法论
*   **核心思想**：将权重更新视为在正交群上的变换。公式表达为 $W_{t+1} = R_t W_t P_t$，其中 $R_t$ 和 $P_t$ 是左、右正交变换矩阵。
*   **关键技术细节**：
    *   **李代数投影**：将梯度 $G_t$ 投影到李代数（反对称矩阵空间），生成输入侧梯度 $G_{in}$ 和输出侧梯度 $G_{out}$。
    *   **矩阵指数映射**：通过矩阵指数函数 $\exp(\cdot)$ 将李代数元素映射回正交群。为了效率，论文采用了**二阶泰勒展开**作为近似。
    *   **RMS 缩放（RMS Scaling）**：为了确保不同层之间更新幅度的尺度一致性，引入了基于权重矩阵大小的自动缩放系数。
    *   **动量设计**：在李代数空间内累积一阶和二阶动量，以加速收敛。
    *   **交替更新（Alternate Update）**：为了节省算力，可以每一步只更新左侧或右侧的正交变换，而非同时更新两侧。

### 3. 实验设计
*   **实验场景**：
    1.  **预训练**：在 C4 数据集上训练 1.3B 参数的 Llama 模型（54B tokens）。
    2.  **有监督微调（SFT）**：使用 Qwen2.5-1.5B 和 Llama3.2-3B 在数学（MetaMathQA）和代码（Magicoder）数据集上微调。
    3.  **强化学习（RLVR）**：使用 GRPO 算法在数学推理任务上进行训练。
*   **对比基准（Benchmarks）**：
    *   **优化器对比**：AdamW、Muon。
    *   **评估指标**：ARC、HellaSwag、MMLU、GSM8K、HumanEval 等主流评测集，以及训练过程中的激活值、梯度范数等稳定性指标。

### 4. 资源与算力
*   **硬件环境**：论文明确提到实验使用了 **8 台 NVIDIA H100 GPU**，并配备了 NVLink。
*   **效率数据**：论文提供了详细的算力开销分析。相比 AdamW，Pion 的单步训练时间增加了约 44%（主要源于矩阵乘法），但与 Muon 相当（仅慢 3% 左右）。在显存占用上，完整版 Pion 比 AdamW 高约 16%，但通过去除二阶动量可降至比 AdamW 更低的水平。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 60M（消融实验）到 3B 参数的模型，训练 Token 数最高达 54B。
*   **消融实验**：非常充分。作者对动量设计、更新频率（双边 vs 交替）、指数函数近似阶数、RMS 缩放等关键组件均做了详细对比。
*   **极端测试**：作者进行了“无归一化层（Norm-free）”训练和“超深模型（200层）”训练，这些极端场景下 AdamW 和 Muon 均崩溃，而 Pion 依然稳定，有力证明了其鲁棒性。

### 6. 主要结论与发现
*   **极高的稳定性**：Pion 能在整个训练过程中保持激活值范数和注意力 Logit 的平稳，有效防止梯度爆炸。
*   **性能优异**：在预训练和微调任务中，Pion 的最终表现均优于或持平于 AdamW 和 Muon。
*   **$\mu$P 兼容性**：Pion 天然符合最大更新参数化（$\mu$P）的谱条件，使得超参数在不同宽度的模型间具有极佳的可迁移性。
*   **RL 适配性**：在强化学习任务中，Pion 表现出比 AdamW 更快的收敛速度和更高的最终精度。

### 7. 优点
*   **理论优雅**：基于流形优化和李群理论，将谱保持从一种约束转变为优化器自身的动力学特性。
*   **无需重参数化**：相比前作 POET，Pion 直接作用于模型权重，不改变模型结构，易于集成到现有框架。
*   **解决深层训练难题**：在极深网络和去除 LayerNorm 的情况下依然能成功训练，展示了其作为底层优化器的强大潜力。

### 8. 不足与局限
*   **计算复杂度**：虽然在大 Batch 训练下开销被摊薄，但对于小 Batch 或小模型，矩阵乘法的额外开销（$O(d^3)$）相对明显。
*   **显存占用**：存储李代数空间的动量需要额外的显存，对于显存受限的场景可能需要权衡。
*   **参数规模验证**：目前实验最高只到 3B 参数，尚未在百亿（70B+）规模的超大模型上验证其长期训练的优势。

（完）
