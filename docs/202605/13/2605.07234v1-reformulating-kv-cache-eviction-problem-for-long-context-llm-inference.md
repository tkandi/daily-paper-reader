---
title: Reformulating KV Cache Eviction Problem for Long-Context LLM Inference
title_zh: 重新定义长上下文大语言模型推理中的 KV 缓存逐出问题
authors: "Tho Mai, Joo-Young Kim"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07234v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 长文本LLM推理的KV缓存优化
tldr: "针对长上下文LLM推理中KV缓存带来的巨大内存开销，本文提出LaProx。该方法将KV缓存驱逐问题重新定义为层级矩阵乘法近似问题，通过建模注意力图与投影值状态的乘法交互，实现了跨头部的全局重要性评分。实验表明，LaProx在仅保留5%缓存的情况下仍能保持高性能，显著优于现有方法。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.07234v1/fig-001.webp\", \"caption\": \"Figure 2: Average scores among 16 datasets of LongBench under different cache budgets.\", \"page\": 6, \"index\": 1, \"width\": 818, \"height\": 221}, {\"url\": \"assets/figures/arxiv/2605.07234v1/fig-002.webp\", \"caption\": \"Figure 3: Comparison across 3 NIAH variants at 32K context length.\", \"page\": 7, \"index\": 2, \"width\": 817, \"height\": 520}, {\"url\": \"assets/figures/arxiv/2605.07234v1/fig-003.webp\", \"caption\": \"Table 3: Details of each dataset in LongBench.\", \"page\": 14, \"index\": 3, \"width\": 818, \"height\": 400}, {\"url\": \"assets/figures/arxiv/2605.07234v1/fig-004.webp\", \"caption\": \"Figure 1: Pattern and magnitude of A and VWO.\", \"page\": 3, \"index\": 4, \"width\": 376, \"height\": 420}, {\"url\": \"assets/figures/arxiv/2605.07234v1/fig-005.webp\", \"caption\": \"Figure 5: Efficiency Analysis.\", \"page\": 8, \"index\": 5, \"width\": 821, \"height\": 193}, {\"url\": \"assets/figures/arxiv/2605.07234v1/fig-006.webp\", \"caption\": \"Figure 6: Retained tokens and variation per head\", \"page\": 9, \"index\": 6, \"width\": 407, \"height\": 407}]"
motivation: 现有的KV缓存驱逐方法主要依赖局部注意力权重，忽略了值表示、输出投影及多头间的相互影响。
method: 提出LaProx策略，将驱逐问题转化为输出感知的层级矩阵乘法近似，并引入统一的全局重要性评分机制。
result: "在LongBench等基准测试中，该方法仅需5%的KV缓存即可维持性能，且在极端压缩下准确率损失比现有基准降低达2倍。"
conclusion: LaProx通过更精确地量化Token贡献和考虑跨头依赖，为长上下文LLM的高效推理提供了一种强有力的KV缓存管理方案。
---

## 摘要
大语言模型（LLMs）支持长上下文推理，但由于键值（KV）缓存的增长，面临巨大的内存和运行开销。现有的 KV 缓存逐出方法主要依赖于局部注意力权重，忽略了值表示、输出投影以及多头之间交互的影响。在这项工作中，我们将 KV 缓存逐出从传统的基于多头权重平均的方法，重新定义为一种感知输出的层级矩阵乘法近似问题。我们提出了 LaProx，这是一种新颖的逐出策略，它显式地建模了注意力图与投影后的值状态之间的乘法交互，从而在考虑多头依赖关系的同时，准确量化 Token 的贡献。基于这一指标，我们提出了首个统一的逐出策略，为 Token 分配全局可比的重要性分数，从而实现全模型范围的选择，而非局部的多头决策。在长上下文基准测试 LongBench 和 Needle-In-A-Haystack 的 19 个数据集上的实验结果表明，我们的方法在仅保留 5% 的 KV 缓存的情况下仍能维持模型性能，并在所有配置下始终优于以往的工作。值得注意的是，在极端压缩场景下，与现有的最先进基准相比，我们的方法在保持极低开销的同时，实现了高达 2 倍的准确率损失降低。

## Abstract
Large language models (LLMs) support long-context inference but suffer from substantial memory and runtime overhead due to Key-Value (KV) Cache growth. Existing KV Cache eviction methods primarily rely on local attention weights, neglecting the influence of value representations, output projection, and inter-head interactions. In this work, we reformulate KV Cache eviction from a conventional head-wise, weight-averaging approach into an output-aware, layer-wise matrix multiplication approximation problem. We introduce LaProx, a novel eviction strategy that explicitly models the multiplicative interaction between attention maps and projected value states to accurately quantify token contributions while accounting for inter-head dependencies. Building on this metric, we propose the first unified eviction strategy that assigns globally comparable importance scores to tokens, enabling model-wide selection instead of local, head-wise decisions. Experimental results across 19 datasets on long-context benchmarks LongBench and Needle-In-A-Haystack demonstrate that our approach maintains model performance with only 5\% of the KV cache and consistently outperforms prior works across all configurations. Notably, our method achieves up to 2$\times$ accuracy loss reduction under extreme compression scenarios compared to existing state-of-the-art baselines with minimal overhead.

---

## 论文详细总结（自动生成）

以下是对论文《Reformulating KV Cache Eviction Problem for Long-Context LLM Inference》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：长上下文大语言模型（LLM）在推理时，KV 缓存（Key-Value Cache）随序列长度线性增长，导致巨大的内存压力和计算延迟。
*   **现有问题**：目前的 KV 缓存逐出（Eviction）方法（如 H2O, SnapKV 等）主要依赖**局部注意力权重（Attention Weights）**。作者发现这存在两个缺陷：
    1.  **指标不完整**：仅靠注意力权重忽略了值状态（Value states）和输出投影矩阵（Output Projection）的影响，无法准确衡量 Token 对最终输出的实际贡献。
    2.  **决策局部化**：现有的逐出决策通常在每个注意力头（Head）内独立进行，忽略了跨头和跨层的交互，导致资源分配不均。
*   **核心目标**：重新定义逐出问题，从“权重平均”转向“输出感知的矩阵乘法近似”，实现更精准的缓存管理。

### 2. 方法论：LaProx (Layer Approximated Cache)
*   **核心思想**：将 KV 逐出建模为一个**矩阵乘法近似问题**。目标是选择一组 Token 子集，使得压缩后的注意力输出最接近原始输出 $A \times (V W_O)$。
*   **关键技术细节**：
    *   **逐出指标（Eviction Indicator）**：基于蒙特卡洛矩阵乘法理论，Token $i$ 的重要性分数 $p_i$ 定义为注意力向量范数与投影值向量范数的乘积：$p_i \propto \|A[:, i]\|_2 \cdot \|V W_O[i, :]\|_2$。这直接反映了该 Token 对层输出的数值贡献。
    *   **全局逐出决策（Eviction Action）**：
        1.  **跨头打平**：不再为每个头预设固定预算，而是将全层所有头的 Token 分数放在一起比较。
        2.  **层级归一化**：由于不同层的数值量级不同，采用层级归一化（Layer-wise Normalization）使分数在全模型范围内可比。
        3.  **全局选择**：在整个模型范围内根据归一化分数选择 Top-K 个最重要的 Token。
*   **算法流程**：在 Prefill 阶段后，计算注意力权重和投影值，计算每个 Token 的全局重要性得分，根据总预算一次性逐出低分 Token。

### 3. 实验设计
*   **数据集/场景**：
    *   **LongBench**：包含 16 个长文本任务（单/多文档问答、摘要、Few-shot、合成任务、代码）。
    *   **Needle-In-A-Haystack (NIAH)**：测试不同复杂度（1 针、4 针等）下的信息检索能力。
    *   **RULER & InfiniteBench**：测试极端长上下文（高达 128K+）下的鲁棒性。
*   **对比方法**：FullKV（全缓存）、StreamingLLM、SnapKV、AdaKV、CriticalKV、CAKE。
*   **测试模型**：Llama-3.1-8B-Instruct、Mistral-7B-Instruct-v0.3、Qwen3-8B。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA H100 (80GB) GPU** 上完成。
*   **算力开销**：该方法属于 **Training-free（无需训练）** 且 **Calibration-free（无需校准）**。
*   **效率表现**：在 128K 上下文下，相比 FullKV 实现了 **2.3 倍的解码加速**，并显著降低了峰值内存（从 63.3GB 降至 47.5GB）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流模型系列、19 个以上的数据集、以及从 128 到 2048 不等的多种缓存预算配置。
*   **消融实验**：详细分析了 $V$ 矩阵、输出投影 $W_O$、全局分配策略以及观察窗口大小对性能的影响。
*   **充分性评价**：实验设计非常充分，不仅对比了最终精度，还通过余弦相似度分析了矩阵近似的数学有效性，并可视化了不同层/头的 Token 保留分布，证明了全局分配的必要性。

### 6. 主要结论与发现
*   **性能卓越**：LaProx 在仅保留 **5% 缓存** 的极端情况下，性能显著优于所有基准方法。在 NIAH 检索任务中，准确率提升可达 1.5-3 倍。
*   **数学验证**：实验证明，结合了 $V W_O$ 的指标比单纯的注意力权重能更真实地还原注意力层的输出（余弦相似度更高）。
*   **动态分配**：发现不同层和不同头对 Token 的需求高度非均匀且随输入动态变化，证明了“模型级全局逐出”优于“头级局部逐出”。

### 7. 优点
*   **理论严谨**：将启发式的逐出问题提升到了矩阵近似的数学高度。
*   **全局视角**：打破了“头独立逐出”的传统范式，实现了模型范围内的资源优化。
*   **易于部署**：无需微调、无需离线校准、超参数极少，且能无缝适配 MoE（混合专家）架构。

### 8. 不足与局限
*   **归一化策略简单**：目前采用的层级归一化较为基础，未来可能存在更复杂的跨层重要性对齐方案。
*   **首字延迟**：虽然解码速度提升，但在 Prefill 阶段计算 $V W_O$ 会引入微小的额外计算开销（尽管文中显示该开销可忽略不计）。
*   **极端长文本限制**：虽然在 128K 表现良好，但在百万级别（1M+）超长文本下的表现仍需进一步验证。

（完）
