---
title: "GriNNder: Breaking the Memory Capacity Wall in Full-Graph GNN Training with Storage Offloading"
title_zh: GriNNder：通过存储卸载打破全图 GNN 训练中的内存容量壁垒
authors: "Jaeyong Song, Seongyeon Park, Hongsun Jang, Jaewon Jung, Hunseong Lim, Junguk Hong, Jinho Lee"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11517v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 利用存储卸载优化大规模GNN训练的AI基础设施
tldr: 本研究针对全图图神经网络（GNN）训练中内存容量受限的问题，提出了首个利用存储设备（NVMe SSD）进行卸载的框架GriNNder。该框架通过结构化存储卸载（SSO）机制，协调GPU、主机内存与存储之间的层次结构，并引入分区缓存、梯度重聚合及轻量级分区策略，有效解决了存储访问模式与GNN数据依赖的冲突。实验表明，GriNNder在单GPU上实现了高达9.78倍的加速，使超大规模全图训练成为可能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11517v1/fig-001.webp\", \"caption\": \"Table 10. Training time/epoch (min) for various-sized Kronecker synthetic graphs. ‘-’ denotes when the number of partitions is not enough for running. Bold is the fastest training time in each (#layers, dataset) pair.\", \"page\": 26, \"index\": 1, \"width\": 483, \"height\": 539}, {\"url\": \"assets/figures/arxiv/2605.11517v1/fig-002.webp\", \"caption\": \"Figure 16. Advantage of partition-wise cache management compared to vertex-wise one.\", \"page\": 21, \"index\": 2, \"width\": 439, \"height\": 396}, {\"url\": \"assets/figures/arxiv/2605.11517v1/fig-003.webp\", \"caption\": \"Figure 17. Overview of overlapping cache management with computation.\", \"page\": 21, \"index\": 3, \"width\": 391, \"height\": 384}, {\"url\": \"assets/figures/arxiv/2605.11517v1/fig-004.webp\", \"caption\": \"Figure 1. Full-graph training procedure with a two-layer GNN.\", \"page\": 2, \"index\": 4, \"width\": 491, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2605.11517v1/fig-005.webp\", \"caption\": \"Figure 7. Motivation and a high-level overview of switching-aware partitioning.\", \"page\": 7, \"index\": 5, \"width\": 487, \"height\": 387}, {\"url\": \"assets/figures/arxiv/2605.11517v1/fig-006.webp\", \"caption\": \"Figure 18. Profiling results of GriNNder’s forward and backward pass.\", \"page\": 22, \"index\": 6, \"width\": 1017, \"height\": 328}, {\"url\": \"assets/figures/arxiv/2605.11517v1/fig-007.webp\", \"caption\": \"Figure 2. Naı̈ve storage extension of full-graph training.\", \"page\": 3, \"index\": 7, \"width\": 442, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2605.11517v1/fig-008.webp\", \"caption\": \"Figure 8. Framework structure of GriNNder.\", \"page\": 8, \"index\": 8, \"width\": 492, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2605.11517v1/fig-009.webp\", \"caption\": \"Figure 4. GriNNder forward and backward procedures for layer 1.\", \"page\": 4, \"index\": 9, \"width\": 491, \"height\": 387}, {\"url\": \"assets/figures/arxiv/2605.11517v1/fig-010.webp\", \"caption\": \"Figure 5. Details and rationales of partition-wise graph caching. Pt.1 denotes Partition 1.\", \"page\": 4, \"index\": 10, \"width\": 489, \"height\": 409}, {\"url\": \"assets/figures/arxiv/2605.11517v1/fig-011.webp\", \"caption\": \"Figure 20. User interface of GriNNder. Users inherit GriNNderGNN and implement layer forward to enable layer-wise execution for partition-based full-graph training.\", \"page\": 24, \"index\": 11, \"width\": 488, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2605.11517v1/fig-012.webp\", \"caption\": \"Table 1. Results of training time (min)/epoch.\", \"page\": 9, \"index\": 12, \"width\": 483, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2605.11517v1/fig-013.webp\", \"caption\": \"Figure 6. Advantage of (c) grad-engine activation regathering compared to (a) PyTorch autograd and (b) HongTu.\", \"page\": 5, \"index\": 13, \"width\": 490, \"height\": 480}]"
motivation: 现有的全图GNN训练受限于GPU和主机内存容量，难以在单机环境下处理超大规模图数据。
method: 提出GriNNder框架，利用NVMe SSD进行存储卸载，并通过结构化存储卸载、分区缓存和梯度重聚合等策略优化数据流。
result: 在多种模型和数据集上实现了最高9.78倍的加速，吞吐量可媲美分布式系统，支持在单GPU上进行大规模全图训练。
conclusion: GriNNder通过高效利用存储设备打破了内存容量瓶颈，为低成本、大规模全图GNN训练提供了高效的解决方案。
---

## 摘要
图神经网络（GNN）的全图训练被广泛使用，因为它通过保留完整的邻域信息，能够直接验证算法的改进。然而，它通常需要多个 GPU 或服务器，从而产生大量的硬件和设备间通信成本。虽然现有的单服务器方法降低了基础设施需求，但随着图规模的增加，它们仍然受限于 GPU 和主机内存容量。为了解决这一局限性，我们推出了 GriNNder，这是首个利用存储设备在内存有限的情况下实现全图训练的工作。由于现代 NVMe SSD 提供数 TB 的容量和超过 10 GB/s 的带宽，当内存资源稀缺时，它们提供了一个极具吸引力的选择。然而，直接应用其他领域的基于存储的方法无法解决全图 GNN 训练中独特的访问模式和数据依赖性。GriNNder 通过结构化存储卸载（SSO）应对这些挑战，该框架通过协调的缓存、(重新)聚合和绕过机制管理 GPU-主机-存储层级结构。为了实现该框架，我们设计了：(i) 一种针对主机内存的分区缓存策略，利用了对跨分区依赖性的观察；(ii) 一种用于梯度计算的重新聚合策略，消除了冗余的存储操作；(iii) 一种轻量级分区方案，减轻了现有图分区器的内存需求。在针对各种模型和数据集进行的实验中，GriNNder 与最先进的基准测试相比实现了高达 9.78 倍的加速，吞吐量可与分布式系统媲美，即使在单个 GPU 上也能实现以前无法进行的大规模全图训练。

## Abstract
Full-graph training of graph neural networks (GNNs) is widely used as it enables direct validation of algorithmic improvements by preserving complete neighborhood information. However, it typically requires multiple GPUs or servers, incurring substantial hardware and inter-device communication costs. While existing single-server methods reduce infrastructure requirements, they remain constrained by GPU and host memory capacity as graph sizes increase. To address this limitation, we introduce GriNNder, which is the first work to leverage storage devices to enable full-graph training even with limited memory. Because modern NVMe SSDs offer multi-terabyte capacities and bandwidths exceeding 10 GB/s, they provide an appealing option when memory resources are scarce. Yet, directly applying storage-based methods from other domains fails to address the unique access patterns and data dependencies in full-graph GNN training. GriNNder tackles these challenges by structured storage offloading (SSO), a framework that manages the GPU-host-storage hierarchy through coordinated cache, (re)gather, and bypass mechanisms. To realize the framework, we devise (i) a partition-wise caching strategy for host memory that exploits the observation on cross-partition dependencies, (ii) a regathering strategy for gradient computation that eliminates redundant storage operations, and (iii) a lightweight partitioning scheme that mitigates the memory requirements of existing graph partitioners. In experiments performed over various models and datasets, GriNNder achieves up to 9.78x speedup over state-of-the-art baselines and throughput comparable to distributed systems, enabling previously infeasible large-scale full-graph training even on a single GPU.

---

## 论文详细总结（自动生成）

# GriNNder：通过存储卸载打破全图 GNN 训练中的内存容量壁垒

### 1. 论文的核心问题与整体含义
*   **研究动机**：全图（Full-graph）GNN 训练因保留了完整的邻域信息，在算法验证和精度上具有优势。然而，全图训练需要同时将所有层的激活值和梯度存储在内存中，对于大规模图（如亿级节点），其内存需求可达 TB 级别，远超现代 GPU 甚至主机内存（DRAM）的容量。
*   **核心问题**：现有的单机方案受限于内存容量（Memory Wall），而分布式方案虽然能扩展内存，但面临高昂的硬件成本和严重的跨设备通信瓶颈（通信开销常占总时长的 80%-98%）。
*   **整体含义**：本文提出了 **GriNNder**，这是首个利用 **NVMe SSD** 作为存储层级来支持超大规模全图 GNN 训练的框架。它通过将存储设备集成到训练流水线中，打破了单机训练的容量限制。

### 2. 论文提出的方法论
GriNNder 引入了**结构化存储卸载（Structured Storage Offloading, SSO）**框架，核心思想是协调 GPU-主机-存储的三级内存层级，具体包含三大关键技术：
*   **分区缓存策略（Partition-wise Graph Caching）**：
    *   利用图分区依赖关系的幂律分布特性（少数分区被频繁访问），在主机内存中以“分区”为粒度建立缓存。
    *   通过层级 LRU 策略管理缓存，最大限度减少对慢速 SSD 的随机访问。
*   **梯度引擎重聚合（Grad-engine Activation Regathering）**：
    *   针对 PyTorch 等框架在反向传播时需要存储大量冗余激活值快照（Snapshot）的问题，GriNNder 放弃存储冗余快照。
    *   在反向传播时，利用原始激活值即时“重聚合”出所需数据，将存储 I/O 需求从 $\alpha$ 倍（扩展因子）降低到常数倍，显著减少了数据放大效应。
*   **切换感知分区（Switching-aware Partitioning）**：
    *   传统分区器（如 METIS）在处理超大图时自身会消耗数倍于图规模的内存，导致 OOM。
    *   GriNNder 设计了一种轻量级分区算法，仅需 CSR 格式的极小辅助空间，通过迭代优化顶点分配来降低跨分区依赖。

### 3. 实验设计
*   **数据集**：
    *   **Products** (2.4M 节点, 61.9M 边)
    *   **IGBM** (10M 节点, 120.1M 边)
    *   **Papers100M** (1.11亿节点, 1.6B 边)
    *   **Kronecker 随机图**（用于可扩展性测试）
*   **对比方法（Benchmarks）**：
    *   **单机方法**：Betty (Micro-batch), Ginex (存储增强型 Mini-batch), HongTu (主机内存卸载)。
    *   **分布式方法**：CAGNET (标准分布式全图), Sancus (带陈旧梯度的分布式优化)。
    *   **存储基准**：Naïve Storage Extension (简单存储扩展)。

### 4. 资源与算力
*   **单机配置**：
    *   **GPU**：单个 NVIDIA RTX A5000 (24GB VRAM)。
    *   **CPU**：AMD Ryzen 9 7950X (16C/32T)。
    *   **内存**：128GB DDR5 RAM。
    *   **存储**：4TB PCIe 5.0 NVMe SSD。
*   **分布式配置**（用于对比）：4 节点集群，共 16 个 RTX A6000 GPU，通过 NVLink 和 Infiniband 连接。
*   **软件环境**：PyTorch, PyG, 支持 NVIDIA GPUDirect Storage (GDS)。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽测试，包括 3 层和 5 层的 GCN、GAT、GraphSAGE 模型。
*   **充分性**：
    *   涵盖了从百万级到亿级节点的真实数据集。
    *   进行了**消融实验**，验证了缓存机制、重聚合策略和分区算法各自的贡献。
    *   测试了不同 SSD 带宽（Gen4 vs Gen5 vs RAID）对性能的影响。
    *   分析了 SSD 的写入寿命（Endurance），证明了方案的实用性。
*   **客观性**：对比了当前最先进的单机和分布式框架，并指出了分布式系统在低带宽网络下的局限性，实验对比具有说服力。

### 6. 主要结论与发现
*   **性能飞跃**：GriNNder 相比最先进的单机卸载方案（HongTu）实现了高达 **9.78倍** 的加速。
*   **单机胜分布式**：在处理 1.11 亿节点的 Papers 数据集时，单 GPU 的 GriNNder 性能甚至优于 16 GPU 的分布式集群（受限于跨节点通信瓶颈）。
*   **内存压缩**：通过重聚合技术，主机内存占用降低了约 5.75 倍，存储 I/O 量减少了约 8.5 倍。
*   **分区效率**：提出的轻量级分区器内存占用比 METIS 低 7.10-24.37 倍，且分区质量足以支持高效训练。

### 7. 优点
*   **经济高效**：证明了在普通工作站（单 GPU + SSD）上进行工业级规模全图训练的可行性，大幅降低了科研门槛。
*   **系统深度优化**：并非简单的“数据搬运”，而是针对 GNN 的数据流特性（幂律分布、冗余快照）进行了底层重构。
*   **高兼容性**：作为 PyTorch 扩展，用户只需修改几行代码即可使用，且不改变算法逻辑，保证了精度一致性。

### 8. 不足与局限
*   **I/O 依赖性**：虽然有缓存，但性能仍高度依赖 SSD 带宽。在极低速存储环境下，性能优势会收窄。
*   **极端图结构风险**：如果图结构极其均匀（非幂律分布），分区缓存的命中率会下降，可能导致频繁的存储交换。
*   **硬件特性绑定**：最佳性能依赖于 NVIDIA GDS 等特定硬件加速技术，在非 NVIDIA 平台或旧型号硬件上的表现可能受限。
*   **动态图支持**：目前主要针对静态图，虽然论文讨论了动态图的潜力，但未给出详细的实验验证。

（完）
