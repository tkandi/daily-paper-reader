---
title: "VDCores: Resource Decoupled Programming and Execution for Asynchronous GPU"
title_zh: VDCores：面向异步 GPU 的资源解耦编程与执行
authors: "Zijian He, Adrian Sampson, Yiying Zhang, Zhiyuan Guo"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.03190v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 异步GPU的解耦编程与执行模型
tldr: 现代GPU的异步硬件单元因传统的单体内核模型而利用率不足。本文提出VDCores，一种资源解耦的编程与执行模型。它将异步硬件抽象为资源隔离的虚拟核心，并将工作负载表示为由依赖关系连接的微操作，从而实现内存与计算的自动重叠。在LLM推理任务中，VDCores显著提升了吞吐量并大幅降低了编程复杂度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.03190v1/fig-001.webp\", \"caption\": \"Figure 1. Comparing VDCores and monolith kernel programming and execution models. On an NVIDIA H100 GPU with asynchronous hardware units.\", \"page\": 2, \"index\": 1, \"width\": 650, \"height\": 278}, {\"url\": \"assets/figures/arxiv/2605.03190v1/fig-002.webp\", \"caption\": \"Figure 9. VDCores virtual executor. The example shows executors on a single H100 SM, with two VCC and single VMC.\", \"page\": 7, \"index\": 2, \"width\": 435, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2605.03190v1/fig-003.webp\", \"caption\": \"Figure 13. Dynamic uneven-context evaluation (H100). x-axis labels: 0.5K = 1×512, 0.5K-B = 64×512, 64K = 1×65536, and RAND = 64×random[256,1024].\", \"page\": 10, \"index\": 3, \"width\": 476, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2605.03190v1/fig-004.webp\", \"caption\": \"Figure 4. Memory-bandwidth utilization comparison on a single Llama3-8B layer. MPK underutilize memory bandwidth due to inefficient inter-task overlapping.\", \"page\": 3, \"index\": 4, \"width\": 506, \"height\": 382}, {\"url\": \"assets/figures/arxiv/2605.03190v1/fig-005.webp\", \"caption\": \"Figure 3. Asynchronous unit utilization under the kernel execution model. VDCores overcomes this limitation with asynchronous and independent execution.\", \"page\": 3, \"index\": 5, \"width\": 506, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2605.03190v1/fig-006.webp\", \"caption\": \"Figure 6. VDCores Execution Example.\", \"page\": 4, \"index\": 6, \"width\": 494, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2605.03190v1/fig-007.webp\", \"caption\": \"Figure 10. End-to-end decoding performance over LLM inference. Throughput is normalized to VDCores. The number on each group shows VDCores’s performance gain over the best baseline system. TK: ThunderKittens.\", \"page\": 9, \"index\": 7, \"width\": 1010, \"height\": 384}, {\"url\": \"assets/figures/arxiv/2605.03190v1/fig-008.webp\", \"caption\": \"Figure 7. Simplified new compute 𝜇op matvec. Kernel programmers use dependency queues m2c and c2m, to acquire and release memory resources with push/pop.\", \"page\": 5, \"index\": 8, \"width\": 496, \"height\": 258}]"
motivation: 传统的单体内核模型与现代GPU的异步硬件特性不匹配，导致硬件资源利用率低下。
method: 通过将异步硬件抽象为虚拟核心并将任务分解为微操作，实现基于依赖和资源就绪情况的自动执行重叠。
result: "在GH200等GPU上的LLM推理实验显示，解码吞吐量平均提升24%，且编程工作量减少了90%。"
conclusion: VDCores证明了资源解耦模型能有效提升异步GPU的性能并简化复杂内核的开发流程。
---

## 摘要
现代 GPU 越来越依赖专用和异步硬件单元来提供高性能。然而，由于当今的 GPU 软件栈仍围绕着与异步硬件不匹配的单体算子（monolithic kernel）模型来组织编程和执行，这些单元往往利用不足。为了解决这一问题，虚拟解耦引擎（VDCores）提出了一种面向异步 GPU 的新型解耦编程与执行模型。VDCores 将异步硬件执行单元抽象为资源隔离的虚拟核心，并将工作负载表示为由依赖关系连接的微操作（micro-ops）。这种抽象消除了程序员的静态编排负担，能够根据依赖关系和资源就绪情况实现访存与计算的自动重叠，从而提高异步硬件资源的利用率。在当今的 GPU 上高效实现这种解耦抽象本身就具有挑战性，VDCores 通过 GPU 专用的编程模型和 GPU 运行时设计解决了这一问题，在保持灵活性的同时最大限度地减少了实现开销。在 GH200、H100 和 RTX 6000 Pro GPU 上的四种大语言模型（LLM）推理工作负载中，VDCores 将解码吞吐量平均提高了 24%，在动态输入下最高可提高 77%，同时将算子编程和定制化工作量减少了 90%。我们已在 https://github.com/vdcores/vdcores 开源了 VDCores。

## Abstract
Modern GPUs increasingly rely on specialized and asynchronous hardware units to deliver high performance. Yet these units are often underutilized because today's GPU software stacks still organize programming and execution around a monolithic kernel model that mismatches asynchronous hardware. To address this issue, Virtual Decoupled Engines (VDCores) presents a new decoupled programming and execution model for asynchronous GPUs. VDCores abstracts asynchronous hardware execution units as resource isolated virtual cores and represents workloads as dependency-connected micro-operations (micro-ops). this abstraction removes static orchestration from the programmer, enables automatic overlap of memory and compute based on dependency and resource readiness, and thereby improves utilization of asynchronous hardware resources.   Realizing such a decoupled abstraction efficiently on today's GPUs is itself challenging, VDCores addresses this through a GPU-specialized programming model and GPU runtime design that preserves the flexibility while minimizing implementation overhead. Across four LLM inference workloads on GH200, H100, and RTX 6000 Pro GPUs, VDCores significantly improves decoding throughput by 24% on average and by up to 77% under dynamic inputs, while reducing kernel programming and specialization effort by 90%. We have open sourced VDCores at https://github.com/vdcores/vdcores.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **VDCores** 的新型 GPU 编程与执行框架，旨在解决现代异步 GPU 硬件与传统单体内核（Monolithic Kernel）软件模型之间的失配问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代 GPU（如 NVIDIA Hopper/Blackwell）引入了大量异步硬件单元（如 Tensor Cores、TMA 内存加速器），但现有的软件栈仍采用“单体内核”模型。
*   **研究动机**：
    *   **编程复杂性高**：程序员必须手动编排内存移动、计算和同步，以实现流水线重叠，极易产生性能气泡。
    *   **资源利用率低**：单体内核将资源打包在一起，导致内核边界处出现利用率空隙，且静态调度无法适应动态输入（如 LLM 推理中波动的序列长度）。
    *   **开发成本大**：为了追求极致性能，需要针对不同形状和算子组合编写大量专门化的融合内核（Kernel Specialization）。

### 2. 方法论：VDCores 架构设计
VDCores 的核心思想是将 GPU 资源**解耦**，从“以内核为中心”转向“以资源为中心”。
*   **核心抽象**：
    *   **虚拟解耦核心**：将 GPU 硬件抽象为**虚拟存储核心（VMC）**和**虚拟计算核心（VCC）**。
    *   **微操作 ($\mu$ops)**：将工作负载分解为细粒度的微操作流，通过依赖关系（而非指令顺序）连接。
*   **关键技术细节**：
    *   **$\mu$op 生成器**：将高层算子图转换为 $\mu$op 流。支持**自适应分块（Adaptive Tiling）**和**动态融合（Dynamic Fusion）**，无需重新编译即可在运行时优化内存布局。
    *   **GPU 执行器**：在 GPU 上实现了一个软件模拟的超标量流水线。VMC 负责地址生成和内存搬运，VCC 负责计算。
    *   **依赖驱动执行**：利用 GPU 的异步屏障（mbarrier）和消息队列实现跨核心的依赖解析，允许就绪的操作绕过阻塞的操作（虚拟流技术）。
    *   **死锁避免**：采用“顺序分配、乱序执行”的协议，确保资源循环等待不会发生。

### 3. 实验设计
*   **实验场景**：大语言模型（LLM）离线解码推理。
*   **测试模型**：Qwen3-1.7B/8B、Llama3.1-8B、Llama3.2-1B。
*   **Benchmark 与对比方法**：
    *   **算子级执行框架**：vLLM、SGLang（代表当前工业界最优的内核调度）。
    *   **专家级优化内核**：Mirage（持久化内核编译器）、ThunderKittens（专家手动优化的 Megakernel）。
    *   **消融实验**：对比了无重叠、无虚拟流、无动态融合等多种配置。

### 4. 资源与算力
*   **硬件平台**：NVIDIA **H100** (80GB)、**GH200**、**RTX 6000 Pro**。
*   **算力细节**：论文主要关注推理性能，未涉及模型训练时长。实验涵盖了从单批次（Batch Size 1）到多批次（Batch Size 8）的吞吐量测试。
*   **实现规模**：运行时系统采用 C++/CUDA 编写（约 5000 行），高层接口采用 Python 编写（约 4000 行）。

### 5. 实验数量与充分性
*   **实验规模**：在 3 种不同架构的 GPU 上测试了 4 种模型，涵盖了 48 种不同的配置组合。
*   **充分性**：实验不仅对比了端到端吞吐量，还深入分析了单算子效率、内存带宽利用率、运行时开销（微秒级）以及动态输入（如长短不一的上下文、LoRA 适配器切换）下的表现。
*   **客观性**：对比了目前学术界和工业界最顶尖的基准（如 ThunderKittens），实验设计较为严谨。

### 6. 主要结论与发现
*   **性能提升**：VDCores 平均将解码吞吐量提升了 **24%**，在动态输入场景下最高提升 **77%**。
*   **编程效率**：将算子编程和专门化工作量减少了 **90%**（例如，实现 Llama3 仅需 6 个可重用的 $\mu$ops 和约 700 行代码，而基准方法需要数千行）。
*   **资源利用**：通过自动重叠，VDCores 能够填补内核边界的气泡，使内存带宽利用率更加平滑。
*   **低开销**：尽管是软件管理的解耦执行，其运行时开销仅占总核心时间的 **3.1%**。

### 7. 优点与亮点
*   **范式转移**：打破了传统的单体内核限制，提出了一种更符合现代异步硬件特性的“虚拟核心”模型。
*   **动态性**：能够根据运行时资源就绪情况自动重叠计算与访存，极大地增强了对动态工作负载（如动态 Batching、LoRA）的适应能力。
*   **高可重用性**：通过组合少量的 $\mu$ops 即可构建复杂模型，显著降低了高性能 GPU 编程的门槛。

### 8. 不足与局限
*   **硬件依赖**：虽然论文声称具有可移植性，但目前的实现高度依赖 NVIDIA 的特定指令（如 mbarrier、TMA、WGMMA），在 AMD 或其他加速器上的表现尚待验证。
*   **小块数据开销**：在处理极小块数据（如小于 4KB 的写入）时，虚拟核心的软件开销会变得明显，无法完全掩盖延迟。
*   **复杂性转移**：虽然简化了算子开发，但将复杂性转移到了 VDCores 运行时系统的维护上，对运行时的健壮性要求极高。

（完）
