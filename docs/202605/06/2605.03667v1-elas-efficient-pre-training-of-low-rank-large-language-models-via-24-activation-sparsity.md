---
title: "ELAS: Efficient Pre-Training of Low-Rank Large Language Models via 2:4 Activation Sparsity"
title_zh: "ELAS：通过 2:4 激活稀疏性实现低秩大语言模型的高效预训练"
authors: "Jiaxi Li, Lu Yin, Li Shen, Jinjin Xu, Yuhui Liu, Wenwu Wang, Shiwei Liu, Xilu Wang"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03667v1"
tags: ["query:mlsys"]
score: 8.0
evidence: "利用英伟达GPU对2:4结构化稀疏格式的支持"
tldr: "4结构化激活稀疏技术，通过在FFN中使用平方ReLU激活函数并对输出应用2:4稀疏化，在保持模型性能的同时，显著降低了内存开销并提升了训练与推理速度，为高效预训练提供了新方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.03667v1/fig-001.webp\", \"caption\": \"Table 1: We report the comparative performance in terms of perplexity, along with parameter statistics (Param, in millions) and memory requirements (G, in gigabytes) for each method. Here, r indicates the target rank while d refers to the model’s hidden dimension. The memory consumption includes model parameters and gradients but excludes activations. The reported baseline results are obtained from (Li et al., 2025; Zhao et al., 2024; Han et al., 2024)\\\".\", \"page\": 8, \"index\": 1, \"width\": 973, \"height\": 387}, {\"url\": \"assets/figures/arxiv/2605.03667v1/fig-002.webp\", \"caption\": \"Figure 1: Feed-forward network architecture of the ELAS. The input is first multiplied by the low-rank matrices of the up projection layer, then passes through the ReLU2 activation function. The activation is applied with 2:4 structured sparsity and then multiplied with the low-rank matrix of the down layer using sparse matrix multiplication to obtain the output of the FFN layer.\", \"page\": 5, \"index\": 2, \"width\": 882, \"height\": 444}]"
motivation: 现有的低秩训练方法中激活矩阵仍保持全秩，导致大批量训练时内存消耗巨大且吞吐量受限。
method: 4结构化稀疏。
result: 在60M至1B参数的LLaMA模型上的实验表明，ELAS在几乎不损失性能的前提下，实现了训练加速并显著减少了激活内存。
conclusion: ELAS证明了结合低秩结构与激活稀疏性是实现大语言模型高效预训练和推理的有效途径。
---

## 摘要
大语言模型（LLMs）已展现出卓越的能力，但其训练过程中巨大的计算需求仍是广泛应用的关键瓶颈。近年来，低秩训练因其能显著减少训练内存占用而受到关注。同时，将 2:4 结构化稀疏应用于权重和激活，以利用 NVIDIA GPU 对 2:4 结构化稀疏格式的支持，已成为一个极具前景的方向。然而，现有的低秩方法通常保留全秩的激活矩阵，这在训练大批量数据时占据了主要的内存消耗并限制了吞吐量。此外，直接对权重应用稀疏性往往会导致不可忽视的性能下降。为了实现 LLM 的高效预训练，本文提出了 ELAS：通过 2:4 激活稀疏性实现低秩 LLM 的高效预训练，这是一个通过 2:4 激活稀疏性构建低秩模型的新型框架。ELAS 在低秩模型的前馈网络中应用平方 ReLU（squared ReLU）激活函数，并在平方 ReLU 操作后的激活上实现 2:4 结构化稀疏。我们通过在参数量从 60M 到 1B 的 LLaMA 模型上进行预训练实验评估了 ELAS。结果表明，ELAS 在应用 2:4 激活稀疏性后，在保持性能几乎不下降的同时，实现了训练和推理加速。此外，ELAS 减少了激活内存开销，特别是在大批量训练的情况下。代码可在 ELAS 仓库获取。

## Abstract
Large Language Models (LLMs) have achieved remarkable capabilities, but their immense computational demands during training remain a critical bottleneck for widespread adoption. Low-rank training has received attention in recent years due to its ability to significantly reduce training memory usage. Meanwhile, applying 2:4 structured sparsity to weights and activations to leverage NVIDIA GPU support for 2:4 structured sparse format has become a promising direction. However, existing low-rank methods often leave activation matrices in full-rank, which dominates memory consumption and limits throughput during large-batch training. Furthermore, directly applying sparsity to weights often leads to non-negligible performance degradation. To achieve efficient pre-training of LLMs, this paper proposes ELAS: Efficient pre-training of Low-rank LLMs via 2:4 Activation Sparsity, a novel framework for low-rank models via 2:4 activation sparsity. ELAS applies squared ReLU activation functions to the feed-forward networks in low-rank models and implements 2:4 structured sparsity on the activations after the squared ReLU operation. We evaluated ELAS through pre-training experiments on LLaMA models ranging from 60M to 1B parameters. The results demonstrate that ELAS maintains performance with minimal degradation after applying 2:4 activation sparsity, while achieving training and inference acceleration. Moreover, ELAS reduces activation memory overhead, particularly with large batch sizes. Code is available at ELAS Repo.

---

## 论文详细总结（自动生成）

这是一份关于论文《ELAS: Efficient Pre-Training of Low-Rank Large Language Models via 2:4 Activation Sparsity》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：大语言模型（LLMs）预训练面临巨大的计算和内存瓶颈。现有的**低秩训练**（如GaLore、LORO）虽能减少权重和梯度的内存占用，但**激活矩阵（Activation）**仍保持全秩，在大批量（Large Batch）训练时成为主要的内存负担。
*   **核心问题**：如何在不显著损失模型性能的前提下，同时降低权重内存和激活内存，并利用硬件加速特性提升训练/推理效率？
*   **整体含义**：论文提出 ELAS 框架，首次将低秩权重更新与硬件原生的 2:4 结构化激活稀疏相结合，旨在实现一种在内存和计算上都高度经济的预训练方案。

### 2. 方法论
*   **核心思想**：利用低秩分解减少参数量，通过修改激活函数诱导天然稀疏性，并应用 2:4 结构化稀疏以适配 NVIDIA GPU 的硬件加速。
*   **关键技术细节**：
    *   **低秩框架**：基于 LORO 框架，将线性层权重 $W$ 分解为两个低秩矩阵 $A$ 和 $B$（$W=AB$），并在训练中保持正交梯度更新约束。
    *   **架构调整**：将 LLaMA 原始的 SwiGLU 激活替换为 **Squared ReLU ($ReLU^2$)**。实验发现 $ReLU^2$ 能产生极高的天然稀疏度（84%-98%），非常适合进一步压缩。
    *   **2:4 激活稀疏化**：在前向传播中，对 $ReLU^2$ 后的激活张量应用 2:4 模式（每 4 个连续元素保留绝对值最大的 2 个）。
    *   **梯度流处理**：使用**直通估计器（Straight-Through Estimator, STE）**，在反向传播时跳过不可微的稀疏掩码操作，使梯度直接回传。
    *   **训练策略**：引入 **Dense Warmup** 阶段（如前 1000 步保持稠密训练），待模型稳定后再开启 2:4 稀疏化。

### 3. 实验设计
*   **数据集**：使用标准的大规模网页抓取数据集 **C4 (Colossal Clean Crawled Corpus)**。
*   **模型规模**：涵盖了从 **60M、130M、350M 到 1B** 参数量的 LLaMA 变体。
*   **Benchmark 与对比方法**：
    *   **基准**：全秩预训练（Full-Rank）。
    *   **对比方法**：LoRA、ReLoRA、GaLore、CoLA、SLTrain、LORO。
*   **评估指标**：困惑度（Perplexity）、参数量、内存占用（GB）、计算加速比（Speedup）。

### 4. 资源与算力
*   **硬件型号**：实验在 **NVIDIA RTX 3090** 和 **A100** GPU 上进行。
*   **算力细节**：文中未明确给出总的训练时长或总 GPU 小时数，但强调了在 1B 模型上测试了不同 Batch Size（最高 128）和序列长度（最高 65536）下的表现。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 种不同规模的模型进行了完整预训练对比。
*   **消融实验**：
    1.  **Warmup 步数**：测试了 0 到 3000 步对收敛性的影响。
    2.  **稀疏化策略**：对比了朴素 Top-2 选择与复杂的软阈值（Soft-thresholding）方法。
*   **充分性评价**：实验设计较为客观、公平，对比了当前主流的多种参数高效训练技术，并从性能、内存、速度三个维度进行了全面验证。

### 6. 主要结论与发现
*   **性能保持**：ELAS 在所有规模上均表现出与全秩训练相当的困惑度，性能下降极小（仅 0.07-0.28 PPL）。
*   **内存节省**：在 1B 模型中，ELAS 显著降低了 FFN 模块的激活内存开销（例如在 Batch Size 128 时，从 181GB 降至 101GB）。
*   **计算加速**：在推理阶段，FFN 模块实现了 **1.5x 至 2.75x** 的加速。
*   **正相关性**：模型规模越大、序列长度越长，ELAS 的加速效果和内存节省优势越明显。

### 7. 优点
*   **硬件协同设计**：紧密结合了 NVIDIA Ampere 架构及之后的 2:4 结构化稀疏硬件特性，具有极高的实用价值。
*   **双重优化**：同时解决了权重内存（通过低秩）和激活内存（通过稀疏）的瓶颈。
*   **部署友好**：相比于其他增加额外稀疏权重分支的方法，ELAS 保持了简洁的推理路径，没有额外的计算负担。

### 8. 不足与局限
*   **模型规模上限**：实验最高仅验证至 1B 参数，尚未在 7B、70B 等超大规模模型上验证其扩展性。
*   **短序列开销**：在短序列（如 512 长度）或极小模型上，由于算子启动开销，加速效果不明显甚至可能出现负优化。
*   **任务单一**：主要关注语言建模的困惑度，未在广泛的下游任务（如逻辑推理、代码生成等）上进行微调评估。

（完）
