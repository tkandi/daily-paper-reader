---
title: "Requests of a Feather Must Flock Together: Batch Size vs. Prefix Homogeneity in LLM Inference"
title_zh: 物以类聚：大语言模型推理中的批大小与前缀同质性
authors: "Saksham Rathi, Preeti, Mythili Vutukuru"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06046v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 提高LLM推理引擎的解码吞吐量和效率
tldr: 本研究针对大语言模型推理中的KV缓存内存瓶颈，发现具有相同前缀的小批次请求比异构的大批次请求具有更好的访存局部性，从而能实现更高的解码吞吐量。为此，作者提出了Feather调度器，利用强化学习在批次大小与前缀同质性之间寻找最优平衡，并引入分块哈希树（CHT）降低前缀检测的CPU开销。实验表明，Feather在vLLM和SGLang中可提升2-10倍吞吐量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.06046v1/fig-001.webp\", \"caption\": \"Figure 20. Sensitivity to Chunk Size\", \"page\": 11, \"index\": 1, \"width\": 504, \"height\": 351}, {\"url\": \"assets/figures/arxiv/2605.06046v1/fig-002.webp\", \"caption\": \"Figure 27. Key Observations on Prefix Homogeneity\", \"page\": 15, \"index\": 2, \"width\": 506, \"height\": 416}, {\"url\": \"assets/figures/arxiv/2605.06046v1/fig-003.webp\", \"caption\": \"Figure 11. Chunked Hash Tree Operations\", \"page\": 8, \"index\": 3, \"width\": 1052, \"height\": 217}, {\"url\": \"assets/figures/arxiv/2605.06046v1/fig-004.webp\", \"caption\": \"Table 3. Time complexity of CHT scheduler operations\", \"page\": 19, \"index\": 4, \"width\": 504, \"height\": 484}, {\"url\": \"assets/figures/arxiv/2605.06046v1/fig-005.webp\", \"caption\": \"Figure 30. FindBest cases: 𝑟1 and 𝑟2 are both present in the active batch, and hence the working set is the union of all of their chunks. The shared prefix tip is at the third level, and the set 𝑆 of all shared prefix chunks are marked in red. In the first case, 𝑟𝑆 fully matches the shared prefix tip, whereas 𝑟𝑊 hangs from the middle. For this case,𝑚𝑊 (𝑟𝑊 ) = 2,𝑚𝑊 (𝑟𝑆 ) = 1,𝑚𝑆 (𝑟𝑊 ) = 1,𝑚𝑆 (𝑟𝑆 ) = 0. In the second case, both the requests 𝑟𝑆 and 𝑟𝑊 do not fully match the shared prefix tip, however 𝑟𝑊 hangs off from an earlier chunk. For this case,𝑚𝑊 (𝑟𝑊 ) = 3,𝑚𝑊 (𝑟𝑆 ) = 2,𝑚𝑆 (𝑟𝑊 ) = 2,𝑚𝑆 (𝑟𝑆 ) = 1. Therefore, for both the cases, 𝑚𝑊 and 𝑚𝑆 exhibit similar behaviour.\", \"page\": 20, \"index\": 5, \"width\": 504, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2605.06046v1/fig-006.webp\", \"caption\": \"Figure 7. Radix Tree Sharing Patterns Figure 8. Throughput vs. Batch Size Figure 9. Batch Size vs. Homogeneity\", \"page\": 5, \"index\": 6, \"width\": 1048, \"height\": 207}, {\"url\": \"assets/figures/arxiv/2605.06046v1/fig-007.webp\", \"caption\": \"Table 2. Variables and Notations Used\", \"page\": 16, \"index\": 7, \"width\": 504, \"height\": 390}]"
motivation: 现有调度器忽视了前缀同质性对访存局部性的提升作用，且传统基数树前缀检测存在显著的CPU计算开销。
method: 提出Feather调度器，利用强化学习平衡批次大小与前缀同质性，并采用分块哈希树（CHT）实现轻量级前缀检测。
result: 在vLLM和SGLang上的评估显示，Feather实现了2至10倍的端到端吞吐量提升，且在无前缀共享场景下不降低性能。
conclusion: 通过优化批次的前缀一致性并降低调度算法复杂度，可以大幅超越现有前缀感知推理引擎的性能极限。
---

## 摘要
大语言模型中的自回归 Token 生成受内存带宽限制，因为它需要“关注”所有先前 Token 的键值张量（KV 缓存）。先前的工作旨在通过将多个请求批处理在一起，并在 GPU 内存限制下最大化批大小来提高该解码过程的效率。我们工作的关键观察是，在具有前缀共享的工作负载中，较小的、前缀同质的批次（即所有请求共享一个公共前缀）可以比更大的、异质的批次实现更高的解码吞吐量，这是由于在 KV 缓存访问期间具有更好的空间和时间局部性。然而，最先进推理引擎中的前缀感知调度器仅为了减少 KV 缓存内存占用而最大化批次内的前缀重用，但并不会在可能表现更好的较小同质批次处停止批次形成。此外，我们展示了现有调度器中的共享前缀检测依赖于基数树遍历，从而产生了巨大的 CPU 开销，这通常与 GPU 执行时间相当。本文提出了 Feather，一种前缀感知调度器，它使用强化学习（RL）来学习批大小与前缀同质性之间的最佳权衡。我们还引入了分块哈希树（CHT），这是一种轻量级数据结构，可实现快速前缀检测和 RL 调度器的高效请求选择，从而避免昂贵的树遍历。我们将 Feather 集成到 vLLM 和 SGLang 中，评估结果表明，与现有调度器相比，Feather 实现了 2-10 倍的端到端吞吐量提升，而在工作负载没有足够前缀共享时，其表现也不逊于现状。Feather 通过减少 KV 缓存访问的总数来实现这些增益，超越了具有相同目标的前缀感知注意力算子的性能。

## Abstract
Auto-regressive token generation in large language models is memory-bound because it requires "attending to" key and value tensors (KV cache) of all previous tokens. Prior work aims to improve the efficiency of this decode process by batching multiple requests together, and maximizing batch size subject to GPU memory constraints. The key observation of our work is that with prefix-sharing workloads, smaller, prefix-homogeneous batches -- where all requests share a common prefix -- can achieve higher decode throughput than larger, heterogeneous batches, due to better spatial and temporal locality during KV cache accesses. However, prefix-aware schedulers in state-of-the-art inference engines maximize prefix reuse within a batch only to reduce KV cache memory footprint, but do not stop batch formation at smaller homogeneous batches that could have performed better. Further, we show that shared prefix detection in existing schedulers relies on radix-tree traversals, incurring substantial CPU overhead that is often comparable to GPU execution time. This paper presents Feather, a prefix-aware scheduler that uses reinforcement learning (RL) to learn the optimal tradeoff between batch size and prefix homogeneity. We also introduce Chunked Hash Tree (CHT), a lightweight data structure that enables fast prefix detection and efficient request selection for the RL scheduler, avoiding expensive tree traversals. We integrate Feather into vLLM and SGLang, and our evaluation shows that Feather achieves 2--10$\times$ higher end-to-end throughput as compared to existing schedulers, while doing no worse than the status quo when the workload does not have enough prefix sharing. Feather achieves these gains by reducing the total number of KV cache accesses, surpassing the performance of prefix-aware attention kernels that have the same goal.