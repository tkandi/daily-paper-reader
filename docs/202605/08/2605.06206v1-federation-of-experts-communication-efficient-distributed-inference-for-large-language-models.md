---
title: "Federation of Experts: Communication Efficient Distributed Inference for Large Language Models"
title_zh: 专家联邦：大语言模型的高效通信分布式推理
authors: "Muhammad Shahir Abdurrahman, Chun Deng, Azalia Mirhoseini, Philip Levis"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06206v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 针对大语言模型推理的通信高效分布式架构，采用专家并行技术
tldr: 针对大语言模型混合专家（MoE）架构在分布式推理中存在的通信瓶颈，本文提出“专家联邦”（FoE）架构。通过将MoE块重组为多个专家集群，并将专家并行限制在节点内或特定集群内，FoE显著减少了跨节点的All-to-All通信开销。实验表明，该方法在保持生成质量的同时，大幅提升了推理吞吐量并降低了延迟。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.06206v1/fig-001.webp\", \"caption\": \"Table 5: Single-Node inference performance metrics across various Poisson request arrival scales. Time-to-First-Token (TTFT), Time-Between-Tokens (TBT), and End-to-End Latency (E2E).\", \"page\": 7, \"index\": 1, \"width\": 848, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2605.06206v1/fig-002.webp\", \"caption\": \"Table 9: End-to-end forward-pass latency (seconds) measured across synthetic batch sizes.\", \"page\": 14, \"index\": 2, \"width\": 736, \"height\": 285}, {\"url\": \"assets/figures/arxiv/2605.06206v1/fig-003.webp\", \"caption\": \"Figure 2: Comparison in expert communication between MoE and FoE.\", \"page\": 3, \"index\": 3, \"width\": 794, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2605.06206v1/fig-004.webp\", \"caption\": \"Table 8: Training configuration for the small (∼1B) and full (∼7B) FoE and size-matched MoE pretraining runs. Architectural rows that differ between architectures (e.g., expert groups) are marked accordingly; all other settings are shared between the FoE model and its MoE baseline.\", \"page\": 13, \"index\": 4, \"width\": 548, \"height\": 788}, {\"url\": \"assets/figures/arxiv/2605.06206v1/fig-005.webp\", \"caption\": \"Table 6: Multi-Node inference performance metrics on two identical 8-GPU servers connected via InfiniBand. Both Time-to-First-Token (TTFT) and tail distributions scale efficiently due to localized routing shielding traffic from slow inter-node networks.\", \"page\": 8, \"index\": 5, \"width\": 848, \"height\": 213}, {\"url\": \"assets/figures/arxiv/2605.06206v1/fig-006.webp\", \"caption\": \"Table 1: Notation used throughout the Design section.\", \"page\": 4, \"index\": 6, \"width\": 818, \"height\": 427}, {\"url\": \"assets/figures/arxiv/2605.06206v1/fig-007.webp\", \"caption\": \"Figure 4: Side-by-side assessment of Structural Efficiency at Expert Parallel (EP) size = 8. (a) FoE structurally guarantees all tokens are processed locally, achieving a perfect LAR of 1.0. (b) Comparison of GPU load discrepancy (max/median token assignments), with values for MoETuner and Sem-MoE sourced from their papers, highlighting FoE’s perfect load balancing.\", \"page\": 9, \"index\": 7, \"width\": 752, \"height\": 239}, {\"url\": \"assets/figures/arxiv/2605.06206v1/fig-008.webp\", \"caption\": \"Table 2: Per-layer compute for MoE and FoE on an intermediate layer (1 < l < L). All equations are per token i ∈ [S]. The first and last MoE layers in FoE differ only at the input/output boundaries (see text).\", \"page\": 5, \"index\": 8, \"width\": 836, \"height\": 204}]"
motivation: 传统的混合专家模型在分布式推理时，专家间的令牌嵌入通信（All-to-All）成为了严重的性能瓶颈。
method: 提出FoE架构，将MoE层划分为多个集群，每个集群负责特定的KV头，并将专家并行限制在集群内部以减少通信。
result: 在LongBench测试中，FoE将端到端前向传播延迟降低了5.2倍，首字生成时间（TTFT）缩短了3.62倍，且模型质量保持不变。
conclusion: FoE通过优化分布式通信结构，为大规模MoE模型的高效推理提供了一种极具竞争力的解决方案。
---

## 摘要
混合专家（Mixture of Experts, MoE）已成为提高大语言模型（LLMs）计算效率的主要机制。然而，在分布式环境下，专家之间令牌嵌入（token embeddings）的通信是一个显著的瓶颈。我们提出了一种新颖的专家联邦（Federation of Experts, FoE）架构。FoE 将 Transformer 层的 MoE 块重构为多个 MoE 集群。每个集群仅负责其中一个 KV 头，并在这些专家之间应用专家并行（expert parallelism）。在集群之间，通过求和同步注意力后的残差，进而驱动下一个 MoE 块的路由和分发。在单节点设置下，由于组内的所有专家都包含在同一个 GPU 上，FoE 完全消除了全对全（all-to-all）通信。在多节点设置下，FoE 将全对全通信限制在节点内织网（intra-node fabric）中，从而显著降低了通信开销。FoE 的实现结果表明，在 LongBench 上，FoE 在单节点和多节点设置下均显著提高了推理吞吐量并降低了延迟，将端到端前向传播延迟降低了高达 5.2 倍，首字延迟（TTFT）降低了 3.62 倍，逐字延迟（TBT）降低了 1.95 倍。在实现这些性能提升的同时，它还达到了与相同规模和训练配置的混合专家模型相当的生成质量。

## Abstract
Mixture of experts has emerged as the primary mechanism for making Large Language Models (LLMs) computationally efficient. However, in distributed settings, communicating token embeddings between experts is a significant bottleneck.   We present the novel Federation of Experts (FoE) architecture. FoE restructures the MoE block of a transformer layer into multiple MoE clusters. Each cluster is responsible for only one of the KV heads and expert parallelism is applied between those experts. Between clusters, a sum synchronizes the post-attention residuals, which then drives routing and dispatch for the next MoE block. In a single-node setting, FoE completely eliminates all-to-all communication as all experts within a group are contained on the same GPU. In multi-node settings, FoE confines all-to-all communication to the intra-node fabric, thus significantly reducing communication overhead.   An implementation of FoE finds that on LongBench, FoE significantly improves inference throughput and latency in both single-node and multi-node settings, reducing the end-to-end forward-pass latency by up to 5.2x, TTFT by 3.62x, and TBT by 1.95x. It does so while achieving comparable generation quality to a mixture of experts model of the same size and training configuration.

---

## 论文详细总结（自动生成）

这篇论文由斯坦福大学的研究团队提出，旨在解决大语言模型（LLM）在分布式推理中由于混合专家（MoE）架构导致的通信瓶颈问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在分布式环境下，MoE 模型的**专家并行（Expert Parallelism, EP）**存在严重的通信瓶颈。当令牌（Token）被路由到不同 GPU 上的专家时，需要进行昂贵的 **All-to-All** 通信。
*   **背景**：随着模型规模扩大，通信开销可占端到端延迟的 68% 以上。尤其在多节点部署中，节点内带宽（如 NVLink，约 900 GB/s）与节点间带宽（如 InfiniBand，约 50 GB/s）存在近 18 倍的巨大差距。
*   **研究动机**：现有的优化方法（如调度、内核融合）仍受限于全局 All-to-All 通信。本文试图通过**架构重构**，从结构上限制通信范围，提高“本地激活率”（Local Activation Rate, LAR）。

### 2. 方法论：专家联邦（Federation of Experts, FoE）
*   **核心思想**：将传统的单一全局 MoE 块拆分为 $H$ 个独立的**专家组（Expert Groups）**。
*   **关键技术细节**：
    *   **资源划分**：每个专家组拥有 $1/H$ 的 KV 头、 $1/H$ 的专家以及 $1/H$ 的 GPU 资源。
    *   **组内路由（Per-group Routing）**：不再进行全局 Top-k 路由，而是每个 Token 在每个组内独立选择 $k/H$ 个专家。这意味着每个 Token 都会与所有专家组交互。
    *   **通信重构**：将原本跨越所有 GPU 的巨大 All-to-All 通信，分解为 $H$ 个并行的、局限在组内的（通常是单节点或单 GPU 内）小型 All-to-All 通信。
    *   **残差同步**：各组在注意力机制后通过一个轻量级的跨组 **All-Reduce**（求和）来同步残差，作为下一层路由的输入。
*   **算法流程**：输入 $\rightarrow$ 分组注意力 $\rightarrow$ 跨组同步残差 $\rightarrow$ 组内路由与专家计算 $\rightarrow$ 组内 All-to-All 交换 $\rightarrow$ 输出。

### 3. 实验设计
*   **数据集与场景**：
    *   **推理性能**：使用 **LongBench** 基准测试，模拟真实世界的长文本处理。
    *   **请求分布**：采用泊松到达过程（Poisson arrival process）模拟真实的服务器请求负载。
    *   **模型质量**：在 ARC-Easy, BoolQ, COPA, HellaSwag, PIQA, SciQ 等零样本（Zero-shot）推理任务上验证。
*   **Benchmark 与对比方法**：
    *   **基准**：标准专家并行（EP），模拟 vLLM 和 SGLang 的通信逻辑。
    *   **结构效率对比**：与 Sem-MoE, MoETuner, ExFlow 等先进的专家放置与调度框架对比本地激活率（LAR）和负载均衡度。

### 4. 资源与算力
*   **硬件环境**：
    *   **单节点**：1 台 8 卡 NVIDIA H100 (SXM, 80GB HBM3) 服务器。
    *   **多节点**：2 台上述服务器，通过 InfiniBand 互连。
*   **训练细节**：
    *   从头预训练（Pre-training from scratch）了两个规模的模型：**1B** 和 **7B**。
    *   使用 FineWeb-Edu 数据集，训练 Token 量约为 4.7B - 4.9B。
    *   使用了 AdamW 优化器，bfloat16 精度。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了单节点与多节点的对比、不同请求到达率（Scale 0.5 到 1.5）的压力测试、以及从 1 到 24,576 不同 Batch Size 的前向传播延迟测试。
*   **充分性评价**：实验设计较为全面。不仅验证了推理速度（TTFT, TBT, E2E），还通过预训练损失曲线和下游任务准确率证明了架构修改不会损害模型性能。通过详细的通信追踪（Flamegraph）展示了通信开销的实际缩减，论证具有说服力。

### 6. 主要结论与发现
*   **延迟大幅降低**：在多节点环境下，FoE 将端到端前向传播延迟降低了高达 **5.2倍**，首字延迟（TTFT）降低了 **3.62倍**，逐字延迟（TBT）降低了 **1.95倍**。
*   **通信效率**：在单节点设置下，FoE 将通信量精确降低了 $k$ 倍（$k$ 为 Top-k 专家数）。
*   **完美负载均衡**：FoE 实现了 **1.0 的本地激活率（LAR）**和完美的 GPU 负载均衡（Max/Median 差异为 1.0），彻底消除了标准 MoE 中的“掉队者”（Straggler）问题。
*   **质量保持**：FoE 模型的收敛轨迹与标准 MoE 几乎重合，零样本任务准确率在误差范围内持平。

### 7. 优点
*   **结构化优势**：不同于启发式调度算法，FoE 从架构设计上保证了通信的局部性，具有确定性的性能提升。
*   **多节点友好**：极大地缓解了跨节点带宽瓶颈，使 MoE 模型在更大规模的集群上扩展更高效。
*   **正交性**：该方法与现有的内核优化（如 FlashMoE）或算子融合技术正交，可以叠加使用。

### 8. 不足与局限
*   **训练成本**：由于改变了路由结构和 KV 头分配，FoE **无法直接使用现有的 MoE 模型权重**，必须从头训练或进行大规模微调。
*   **单卡无效**：该架构专门为分布式推理设计，在单 GPU 环境下不仅没有收益，反而会因为额外的 All-Reduce 产生微小开销。
*   **超参数敏感性**：将专家强制分组可能需要对路由超参数进行更精细的调节，以匹配不同任务的专家分布需求。
*   **模型规模限制**：实验目前最高仅验证到 7B 规模，在千亿级（100B+）参数模型上的表现仍需进一步验证。

（完）
