---
title: "LLM-Emu: Native Runtime Emulation of LLM Inference via Profile-Driven Sampling"
authors: "Wei Da, Evangelia Kalyvianaki"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00616v1"
tags: ["query:mlsys"]
score: 8.0
evidence: LLM推理服务系统和调度的原生运行时仿真
tldr: 针对LLM推理系统在GPU上进行在线评估成本高昂的问题，本文提出了LLM-Emu，一个面向vLLM的原生运行时仿真器。它保留了生产环境的HTTP、调度和KV缓存路径，仅将GPU前向计算替换为基于性能分析采样的延迟和合成令牌。实验表明，LLM-Emu在多种GPU和工作负载下能准确模拟真实推理行为，各项性能指标误差极低，为LLM服务系统的低成本实验提供了有效工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.00616v1/fig-001.webp\", \"caption\": \"Fig. 1. LLM-Emu plugs in at the executor boundary; everything else is vLLM’s own code.\", \"page\": 1, \"index\": 1, \"width\": 529, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2605.00616v1/fig-002.webp\", \"caption\": \"Fig. 2. Timer-based Future preserves scheduler–worker overlap.\", \"page\": 3, \"index\": 2, \"width\": 529, \"height\": 395}, {\"url\": \"assets/figures/arxiv/2605.00616v1/fig-003.webp\", \"caption\": \"TABLE I PER-CELL RELATIVE ERROR (EMU − REAL)/REAL ACROSS SIX CELLS AND FIVE REQUEST RATES. BOLD ENTRIES MARK THE MAXIMUM ABSOLUTE ERROR FOR EACH METRIC.\", \"page\": 4, \"index\": 3, \"width\": 504, \"height\": 849}]"
motivation: 在GPU上进行大规模LLM推理系统的在线评估和调度实验成本极高，且现有模拟器难以完全还原真实的系统行为。
method: 开发了LLM-Emu，通过在vLLM中保留原生调度和缓存逻辑，并利用预先采样的性能数据模拟GPU执行延迟和输出。
result: "在多种模型和负载下，LLM-Emu的吞吐量误差小于1.9%，端到端延迟误差在5.3%以内，紧密追踪真实系统的推理表现。"
conclusion: 轻量级的原生服务仿真能够支持LLM推理系统的高效在线实验，且具备极高的准确性。
---

## Abstract
Realistic evaluation of LLM serving systems requires online workloads, dynamic arrivals, queueing, and the serving engine's local scheduling for execution batching, but running such experiments on GPUs is expensive. Existing simulators reduce this cost, but often operate offline or in time-warped mode, re-implement serving-engine schedulers, or require accurate operator/kernel-level latency models. We present LLM-Emu, a serving-native emulator for vLLM that preserves the production HTTP, scheduling, KV-cache, and output-processing paths while replacing only GPU forward execution with profile-sampled latency and synthetic output tokens. Tested on two different GPUs, four model variants, two model families, two attention backends, and both Poisson and bursty ShareGPT workloads, LLM-Emu closely tracks real vLLM serving behavior: TPOT and ITL stay within $4.8\%$ absolute error, E2E latency within $5.3\%$, and output throughput within $1.9\%$; TTFT is less stable, with maximum error $10.4\%$, reflecting its sensitivity to admission and queue state. These results suggest that lightweight, serving-native emulation can support practical online experimentation for LLM-serving systems. LLM-Emu is open sourced at https://github.com/AKafakA/llm-emu.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **LLM-Emu** 的原生运行时仿真工具，旨在解决大语言模型（LLM）推理服务系统在评估时对昂贵 GPU 资源过度依赖的问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在 GPU 上对 LLM 推理服务系统（如 vLLM）进行在线评估成本极高。
*   **研究动机**：
    *   **现有模拟器的缺陷**：现有的 LLM 模拟器（如 Vidur, LLMServingSim）通常需要重新实现调度逻辑，容易与生产引擎产生版本偏差（Drift）；或者依赖复杂的算子级延迟模型，难以校准和泛化。
    *   **缺乏真实性**：许多模拟器在离线或时间扭曲（Time-warped）模式下运行，无法直接测试真实的 HTTP 流量、动态到达、排队行为以及生产堆栈的运行时开销。
    *   **目标**：创建一个既能保留生产级服务引擎逻辑，又不需要实时 GPU 执行的轻量级仿真器。

### 2. 方法论
LLM-Emu 的核心思想是**“原生服务仿真” (Serving-native Emulation)**，即保留 vLLM 的绝大部分生产代码路径，仅替换 GPU 执行部分。
*   **关键技术细节**：
    *   **运行时插件设计**：作为 vLLM 的插件运行，保留了 HTTP 栈、调度器、KV 缓存管理和输出处理流水线。
    *   **密度感知延迟预测器 (Density-aware Latency Oracle)**：使用离线分析（Profiling）生成的 2D 查找表（以“总 Token 数”和“并发请求数”为索引）。
    *   **Shepard 加权采样算法**：对于查找表中的稀疏区域，采用自适应近邻扩展和 Shepard 加权算法来预测延迟，以保留真实的性能波动（Variance）。
    *   **定时器解析 Future (Timer-resolved Future)**：通过异步定时器模拟 GPU 执行延迟，确保调度器与工作进程之间的重叠行为（Overlap）与真实情况一致。
    *   **合成 Token 输出**：不进行实际计算，而是返回合成的 Token ID，从而绕过 GPU 前向计算。

### 3. 实验设计
*   **数据集/工作负载**：使用 **ShareGPT** 真实对话数据集。
*   **测试场景**：
    *   **到达模式**：泊松到达（Poisson）和突发流量（Bursty, $\gamma=0.25$）。
    *   **请求速率**：$r \in \{2, 4, 8, 16, 32\}$ 请求/秒。
*   **Benchmark**：以**真实的 vLLM (v0.18.1) 运行结果**作为基准（Ground Truth）。
*   **对比维度**：涵盖了两种 GPU、四种模型变体、两个模型家族（Qwen, Llama）以及两种注意力后端（FlashInfer, FlashAttention 2）。

### 4. 资源与算力
*   **硬件设备**：
    *   **RTX 8000 (48GB)**：搭配 Intel Xeon Gold 5218 CPU。
    *   **NVIDIA A40 (48GB)**：搭配 AMD EPYC 处理器。
*   **算力消耗**：
    *   **离线分析时长**：为每个模型/硬件组合生成性能 Profile 大约需要 **3.5 到 4.5 小时**的 GPU 墙钟时间。
    *   **仿真阶段**：一旦生成 Profile，仿真过程**无需 GPU**。

### 5. 实验数量与充分性
*   **实验规模**：共设计了 6 组核心实验单元（Cell），每组单元在 5 种不同的请求速率下运行，总计至少 30 组对比实验。
*   **覆盖范围**：实验涵盖了模型缩放（4B, 8B, 14B）、硬件迁移、模型家族迁移和工作负载形状改变。
*   **充分性评价**：实验设计较为全面，通过控制变量法验证了仿真器在不同变量下的鲁棒性。使用了 `num-gpu-blocks-override` 等手段确保内存压力对齐，体现了实验的严谨性和公平性。

### 6. 主要结论与发现
*   **高精度**：在稳态服务指标上，LLM-Emu 与真实 vLLM 行为高度一致。
    *   **TPOT（每 Token 延迟）和 ITL（迭代延迟）**：绝对误差在 **4.8%** 以内。
    *   **E2E（端到端延迟）**：误差在 **5.3%** 以内。
    *   **吞吐量 (TPS)**：误差在 **1.9%** 以内。
*   **TTFT 的敏感性**：首个 Token 延迟（TTFT）的误差较大（最高 **10.4%**），这反映了 TTFT 对排队状态、CUDA 图捕获和缓存预热等启动效应的高度敏感性。

### 7. 优点
*   **低维护成本**：无需重新实现复杂的调度逻辑，随 vLLM 版本更新的适配压力小。
*   **高保真度**：由于运行的是 vLLM 原生代码，能够捕捉到真实的系统开销（如 HTTP 处理、调度开销）。
*   **易用性**：通过环境变量即可切换真实与仿真模式，下游客户端无需任何修改。
*   **开源贡献**：代码已开源，有助于社区复现和进一步研究。

### 8. 不足与局限
*   **Profile 依赖**：虽然仿真不需 GPU，但前期生成 Profile 仍需数小时 GPU 时间，且对硬件和模型配置高度敏感。
*   **单节点限制**：目前的验证仅限于单节点部署，尚未在多 GPU 张量并行或多节点集群规模下进行验证。
*   **TTFT 预测偏差**：在处理排队剧烈波动或启动阶段时，预测精度仍有提升空间。
*   **工作负载匹配**：目前的准确性建立在 Profile 负载与评估负载形状一致的前提下，对于完全未知的负载分布，其泛化能力有待验证。

（完）
