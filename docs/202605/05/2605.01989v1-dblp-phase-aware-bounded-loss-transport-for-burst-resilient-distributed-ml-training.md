---
title: "DBLP: Phase-Aware Bounded-Loss Transport for Burst-Resilient Distributed ML Training"
title_zh: DBLP：面向抗突发分布式机器学习训练的阶段感知有界丢包传输协议
authors: "Zechen Ma, Zixi Qu, Jinyan Yi, David Lin, Yashar Ganjali"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.01989v1"
tags: ["query:mlsys"]
score: 10.0
evidence: 分布式机器学习训练的阶段感知传输协议
tldr: 针对分布式机器学习训练中网络微突发流量导致的尾延迟问题，本文提出动态有界丢包协议（DBLP）。该协议感知训练阶段并利用模型对梯度的容错性，动态调整丢包阈值。实验表明，DBLP在保证模型精度的同时，显著缩短了训练时间并缓解了网络拥塞对性能的影响。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01989v1/fig-001.webp\", \"caption\": \"Fig. 2: Send Latency Comparison Under Microbursts\", \"page\": 7, \"index\": 1, \"width\": 1076, \"height\": 720}, {\"url\": \"assets/figures/arxiv/2605.01989v1/fig-002.webp\", \"caption\": \"Fig. 1: Preliminary Results on DenseNet169\", \"page\": 3, \"index\": 2, \"width\": 527, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2605.01989v1/fig-003.webp\", \"caption\": \"Fig. 4: CDF Curve: Microburst, ResNet50\", \"page\": 8, \"index\": 3, \"width\": 407, \"height\": 306}, {\"url\": \"assets/figures/arxiv/2605.01989v1/fig-004.webp\", \"caption\": \"Fig. 3: CDF Curve: Microburst, EfficientNetB0\", \"page\": 8, \"index\": 4, \"width\": 403, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2605.01989v1/fig-005.webp\", \"caption\": \"Fig. 5: Training Time Comparison\", \"page\": 9, \"index\": 5, \"width\": 906, \"height\": 452}, {\"url\": \"assets/figures/arxiv/2605.01989v1/fig-006.webp\", \"caption\": \"Fig. 6: Evaluation Accuracy Results Across Models\", \"page\": 9, \"index\": 6, \"width\": 1022, \"height\": 687}]"
motivation: 现有的网络优化忽略了模型训练阶段的差异性，导致在网络微突发拥塞时出现严重的尾延迟和训练效率下降。
method: 提出一种感知训练阶段的动态有界丢包传输协议，根据模型在不同阶段的容错能力动态调整梯度传输的丢包率。
result: "相比基准方案，DBLP平均缩短了24.4%的端到端训练时间，并在网络拥塞时实现了高达5.88倍的单轮通信加速。"
conclusion: DBLP通过结合模型层面的容错特性与网络传输优化，有效提升了分布式机器学习训练在不稳定网络环境下的鲁棒性和效率。
---

## 摘要
随着十亿至万亿参数规模模型的普及，分布式机器学习（ML）训练已成为必然。虽然先前的工作从应用层的 ML 角度提高了训练效率，但往往未能解决网络层的瞬时拥塞事件，这些事件会引入严重的尾部延迟和训练时间波动，从而损害分布式 ML 训练系统的服务质量（QoS）。现有的网络优化将所有梯度一视同仁，因此未能将足够的模型训练见解整合到通信协议设计中。在本文中，我们提出了动态有界丢包协议（DBLP），这是一种抗突发、训练阶段感知且与硬件无关的传输协议，它将模型级的容错特性融入到梯度通信中。通过在不同训练阶段动态调整梯度丢包容忍度，DBLP 减少了总训练时间，并缓解了瞬时高丢包事件（即微突发）期间的尾部延迟崩溃。与当前最先进的解决方案（基准）相比，DBLP 在实现相当的测试准确率的同时，能够容忍显著更高的丢包率，并将端到端训练时间平均缩短了 24.4%，最高缩短了 33.9%。在微突发事件中，DBLP 的单轮通信延迟比基准方案提升了高达 5.88 倍，防止了由突发引起的尾部延迟峰值，并保持了稳定的训练性能。

## Abstract
Distributed machine learning (ML) training has become a necessity with the prevalence of billion to trillion-parameter-scale models. While prior work has improved training efficiency from the ML perspective at the application layer, it often fails to address transient congestion events at the network layer that introduce severe tail latency and training-time variability, thereby undermining the quality of service (QoS) of distributed ML training systems. Existing network optimizations treat all gradients equally and thus fail to integrate sufficient model-training insights into communication protocol design.   In this paper, we present Dynamic Bounded-Loss Protocol (DBLP), a burst-resilient, training-phase-aware, and hardware-agnostic transport protocol that incorporates model-level tolerance properties into gradient communication. By dynamically adjusting gradient loss tolerance across training phases, DBLP reduces overall training time and mitigates tail-latency collapse during transient high-loss events (i.e., microbursts).   Compared to the current state-of-the-art solution (baseline), DBLP tolerates significantly higher loss while achieving comparable test accuracy, and reduces end-to-end training time by an average of 24.4% and a maximum of 33.9%. At microburst events, DBLP achieves up to 5.88x single-round communication latency speedups over the baseline, preventing burst-induced tail-latency spikes and maintaining stable training performance.

---

## 论文详细总结（自动生成）

### DBLP: 面向抗突发分布式机器学习训练的阶段感知有界丢包传输协议

#### 1. 核心问题与整体含义（研究动机和背景）
在超大规模分布式机器学习（ML）训练中，**通信开销**已成为主要的性能瓶颈。传统的网络传输协议（如 TCP）追求 100% 的可靠交付，但在数据中心网络中，频繁发生的**微突发（Microbursts）**拥塞会导致严重的丢包，进而引发大量的重传，造成**尾延迟（Tail Latency）**激增。

虽然先前的工作（如 MLT）提出利用模型对梯度丢失的内在容忍度（Bounded-Loss Tolerance）来减少重传，但存在两个核心缺陷：
*   **阶段盲目性**：将训练全过程视为同等重要，忽略了模型在不同训练阶段（如初期与收敛期）对梯度丢失的敏感度差异。
*   **硬件依赖性**：往往需要特定的交换机配置或硬件支持，难以在通用云环境中部署。

**DBLP** 的研究动机在于设计一种**纯软件实现、能感知训练阶段、且具备抗突发能力**的传输协议，通过动态调整丢包容忍度来优化训练效率。

#### 2. 方法论：核心思想与关键技术
DBLP 的核心思想是：**在模型训练的非关键阶段允许更高的梯度丢失，以换取更低的通信延迟；在关键阶段则严格保证传输质量。**

*   **有界丢包传输机制**：
    *   采用 **UDP** 传输梯度数据块（Chunks），以支持乱序到达和快速处理。
    *   采用 **TCP** 传输控制信号（Probe, Bitmap, Stop），确保元数据的可靠性。
    *   当接收端收到的数据包比例达到设定的阈值时，立即发送 `Stop` 信号，终止该轮重传。
*   **关键学习状态（CLR）检测**：
    *   利用梯度范数（L2-norm）的变化来识别模型是否进入敏感期。
    *   **公式**：当 $\frac{|\|G_{prev}\| - \|G_{curr}\||}{\|G_{prev}\|} \ge \eta$ 时（$\eta$ 设为 0.5），判定进入关键阶段。
*   **动态阈值调整策略**：
    *   **关键阶段（CLR）**：使用低丢包容忍度 $P_{low}$（通常为 0.8% - 2.4%），确保模型收敛。
    *   **非关键阶段**：自动切换至高丢包容忍度 $P_{high}$（在 $P_{low}$ 基础上增加约 40%），以吸收网络突发拥塞的影响。
*   **硬件无关性**：完全在应用层和传输层软件中实现，无需修改交换机配置。

#### 3. 实验设计
*   **数据集与模型**：
    *   图像分类：EfficientNetB0 (CIFAR-10), ResNet50 (CIFAR-100), AlexNet (CIFAR-100)。
    *   语言模型：GPT-2-Small (WikiText-2)。
*   **Benchmark（基准方案）**：
    *   采用固定丢包阈值的半可靠传输协议（模拟最先进的 MLT 方案，但在软件层面实现以公平对比）。
*   **对比场景**：
    *   **正常网络环境**：评估端到端训练时间的缩短。
    *   **微突发环境**：手动注入 70% 的瞬时丢包率，测试协议的鲁棒性和尾延迟控制。

#### 4. 资源与算力
论文使用了中等规模的实验床进行验证：
*   **节点配置**：1 台服务器（Server），3 台工作站（Worker）。
*   **GPU 型号**：
    *   图像任务：每台 Worker 配备 1 块 **NVIDIA RTX A4500** (20GB)。
    *   语言模型任务：每台 Worker 配备 1 块 **NVIDIA RTX 4090** (24GB)。
*   **其他**：CPU 核心数分别为 4 核和 32 核，内存分别为 32GB 和 128GB。
*   **训练时长**：文中未明确给出总训练小时数，但给出了各模型训练的 Epoch 数（如 10-15 Epochs）。

#### 5. 实验数量与充分性
*   **实验覆盖面**：涵盖了卷积神经网络（CNN）和生成式预训练模型（GPT），证明了方法的通用性。
*   **消融与对比**：对比了有无微突发、不同丢包阈值下的表现，并提供了 CDF（累积分布函数）曲线分析尾延迟。
*   **客观性**：通过多次重复实验（如初步实验重复 5 次）并取平均值，确保了结果的可靠性。
*   **局限性**：实验规模较小（3 个 Worker），尚未在大规模集群（如数百个 GPU）上验证其扩展性。

#### 6. 主要结论与发现
*   **训练加速**：DBLP 平均减少了 **24.4%** 的端到端训练时间，最高加速达 **33.9%**（AlexNet）。
*   **抗突发能力**：在微突发事件中，DBLP 的单轮通信延迟比基准方案快 **5.88 倍**，有效消除了延迟峰值。
*   **精度保持**：尽管在非关键阶段丢失了高达 40% 的梯度，但最终模型的测试准确率（或困惑度）与全量传输几乎一致（差异在 1% 以内）。
*   **尾延迟控制**：DBLP 实现了高达 **1.91 倍** 的尾延迟降低。

#### 7. 优点
*   **阶段感知**：首次将“关键学习机制”引入传输层协议设计，实现了通信与算法的深度耦合。
*   **部署友好**：纯软件方案，不依赖特定的网络硬件（如可编程交换机），适合云环境。
*   **鲁棒性强**：通过动态阈值设计，能够“消化”极高比例的瞬时丢包，保证了训练任务的 QoS。

#### 8. 不足与局限
*   **超参数依赖**：$P_{high}$ 的增量（40%）和检测阈值 $\eta$ 主要基于经验设定，缺乏严谨的数学推导或自动调优机制。
*   **规模验证不足**：在 3 节点的小规模环境下表现优异，但在更大规模、更复杂的网络拓扑（如多层交换架构）下的表现有待观察。
*   **适用范围**：目前主要针对数据并行模式，对于张量并行或流水线并行等更复杂的并行策略，其有效性尚未验证。

（完）
