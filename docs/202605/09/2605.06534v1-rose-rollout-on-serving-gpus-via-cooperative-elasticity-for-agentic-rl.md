---
title: "ROSE: Rollout On Serving GPUs via Cooperative Elasticity for Agentic RL"
title_zh: ROSE：通过协作弹性在推理服务 GPU 上实现智能体强化学习的 Rollout
authors: "Wei Gao, Yuheng Zhao, Dilxat Muhtar, Dakai An, Xuchun Shang, Tianyuan Wu, Lunxi Cao, Shaopan Xiong, Weixun Wang, Ju Huang, Teng Ma, Siran Yang, Jiamang Wang, Lin Qu, Bo Zheng, Wei Wang"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06534v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 通过协作弹性进行GPU资源调度
tldr: Agentic RL受限于长尾Rollout效率，静态资源分配导致浪费。ROSE提出协作弹性理念，动态利用推理集群空闲GPU加速Rollout。它包含SLO安全执行器、跨集群权重传输引擎和弹性调度器，在保障推理服务质量的同时，显著提升了RL训练的端到端吞吐量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.06534v1/fig-001.webp\", \"caption\": \"Figure 5. System Architecture of ROSE.\", \"page\": 6, \"index\": 1, \"width\": 502, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2605.06534v1/fig-002.webp\", \"caption\": \"Table 4. ROSE vs. alternative serving engines: rollout time and serving SLO metrics.\", \"page\": 13, \"index\": 2, \"width\": 475, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2605.06534v1/fig-003.webp\", \"caption\": \"Figure 10. [Transfer Engine] (a) Cross-cluster weight transfer time under different optimizations; each optimization is additive over the previous one. (b) Timeline breakdown of shard-aware and sparsity-aware transfer for Qwen3-32B. D2S denotes the dense-to-sparse conversion, and S2D denotes the sparse-to-dense conversion. (c) Sensitivity of shard-aware and sparsity-aware transfer of different LLMs to cross-cluster bandwidth.\", \"page\": 12, \"index\": 3, \"width\": 1036, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2605.06534v1/fig-004.webp\", \"caption\": \"Figure 3. Characterization of serving clusters and workloads: (a) Fluctuating serving traffic; (b) Serving GPU underutilization; (c) High allocation overhead; (d) Substantial communication overhead.\", \"page\": 5, \"index\": 4, \"width\": 1034, \"height\": 180}]"
motivation: 针对Agentic RL中长尾Rollout导致的静态GPU资源分配效率低下及推理集群资源闲置问题。
method: 提出ROSE系统，通过SLO安全执行器、跨集群权重同步引擎和弹性调度器，实现推理与Rollout任务的资源共享。
result: 在不同模型和集群规模下，ROSE相比现有基准将平均端到端吞吐量提升了1.20至3.31倍。
conclusion: 协作弹性资源管理能有效利用闲置算力加速Agentic RL训练，且不损害推理服务的服务水平目标。
---

## 摘要
智能体强化学习（Agentic RL）已成为提升大语言模型（LLMs）多步推理和工具使用能力的关键驱动力。然而，其效率受限于具有多轮环境交互的长尾 Rollout，使得静态 GPU 配置难以适配：过度配置会在落后者（stragglers）上浪费 GPU 资源，而配置不足则会增加竞争并减慢训练速度。我们观察到，生产环境中的推理服务集群通常会留有大量的 GPU 计算和显存余量。基于这一观察，我们提出了协作弹性（cooperative elasticity）：机会性地重新利用未充分利用的推理服务 GPU 来执行 Rollout。实现协作弹性并非易事，因为它必须在突发流量下保证推理服务的服务水平目标（SLOs），并最小化通信开销。为了应对这些挑战，我们提出了 ROSE，这是一个协作式资源弹性后训练系统，它能安全地利用推理服务 GPU 上的空闲计算和显存资源，以加速智能体强化学习的 Rollout。ROSE 由三个组件组成：(1) 一个 SLO 安全的共存执行器，通过高效的 GPU 显存和计算共享，在保证推理服务 SLO 的同时提高 Rollout 吞吐量；(2) 一个跨集群权重传输引擎，利用权重分片和稀疏性实现跨集群的快速权重同步；(3) 一个弹性 Rollout 调度器，动态配置协作容量，并在专用 Rollout GPU 和机会性推理服务 GPU 之间路由轨迹 Rollout。在多种模型大小和集群规模上的实验表明，与最先进的资源固定和弹性基准相比，ROSE 将平均端到端吞吐量提高了 1.20-3.31 倍。

## Abstract
Agentic reinforcement learning (RL) has emerged as a key driver for improving the multi-step reasoning and tool-use capabilities of LLMs. However, its efficiency is bottlenecked by long-tail rollouts with multi-turn environment interactions, making static GPU provisioning a poor fit: overprovisioning wastes GPUs on stragglers, while underprovisioning increases contention and slows training.   We observe that production serving clusters routinely leave substantial GPU compute and memory headroom. Based on this observation, we argue for cooperative elasticity: opportunistically repurposing underutilized serving GPUs to execute rollouts. Realizing cooperative elasticity is non-trivial because it must preserve serving Service Level Objectives (SLOs) under bursty traffic and minimize communication overhead. To address these challenges, we present ROSE, a cooperative, resource-elastic post-training system that safely harvests idle compute and memory on serving GPUs to accelerate agentic RL rollouts. ROSE consists of three components: (1) an SLO-safe co-serving executor that improves rollout throughput while preserving serving SLOs through efficient GPU memory and compute sharing; (2) a cross-cluster weight transfer engine that leverages weight shards and sparsity for fast weight synchronization across clusters; and (3) an elastic rollout scheduler that dynamically provisions cooperative capacity and routes trajectory rollouts across dedicated rollout GPUs and opportunistic serving GPUs. Experiments across multiple model sizes and cluster scales show that ROSE improves average end-to-end throughput by 1.20-3.31 x compared with state-of-the-art resource-fixed and elastic baselines.

---

## 论文详细总结（自动生成）

### 论文总结：ROSE —— 通过协作弹性在推理服务 GPU 上实现智能体强化学习的 Rollout

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：智能体强化学习（Agentic RL）涉及多轮环境交互，其 Rollout 阶段占据了总训练时间的 70% 以上，且呈现明显的“长尾分布”（部分轨迹执行极慢）。
*   **核心矛盾**：
    *   **静态配置低效**：固定 GPU 资源会导致在等待长尾样本时资源闲置，或在需求高峰时资源竞争严重。
    *   **现有弹性方案局限**：利用竞价实例（Spot Instances）或无服务器 GPU（Serverless）存在供应不稳定、模型初始化开销大（数十秒）以及成本波动等问题。
*   **核心发现**：生产环境中的推理服务（Serving）集群通常为了应对突发流量而过度配置，导致 GPU 平均计算利用率仅约 18.9%，显存利用率约 14.3%。
*   **整体含义**：论文提出“协作弹性”（Cooperative Elasticity）概念，即在保障推理服务 SLO（服务水平目标）的前提下，动态“窃取”推理集群的闲置资源来加速 RL 的 Rollout 过程。

#### 2. 方法论：核心思想与关键技术
ROSE 系统由三个核心组件构成：
*   **SLO 安全的共存执行器（Co-serving Executor）**：
    *   **显存共享**：利用 **CUDA VMM（虚拟内存管理）** 实现异构模型间的 KV Cache 动态重平衡，无需重启进程即可调整显存配额。
    *   **抢占式内存策略**：保留 Rollout 的前缀缓存（Prefix Caching）以提升效率，但在推理流量激增时立即强制回收显存。
    *   **双 SLO 准入控制**：基于预分析的算力开销，实时计算推理任务的 TTFT（首字延迟）和 TPOT（单字延迟）余量，仅在不违背 SLO 的情况下插入 Rollout 计算任务。
*   **跨集群权重传输引擎（Weight Transfer Engine）**：
    *   **异步与分片感知**：通过中继层解耦训练与推理集群，支持不同并行策略（如 TP8 到 TP4）的自动分片映射。
    *   **稀疏性感知压缩**：发现 RL 训练中相邻 Step 的权重增量（Delta）具有 **>95% 的无损稀疏性**，通过仅传输非零值，将跨数据中心（带宽受限）的同步时间从分钟级降至 20 秒以内。
*   **弹性 Rollout 调度器（Elastic Rollout Scheduler）**：
    *   **轮次级路由**：在多轮交互的每一轮动态决定是否卸载到推理 GPU。
    *   **缓存亲和性放置**：优先将任务路由到持有该轨迹 KV Cache 的 GPU，以最大化前缀复用。

#### 3. 实验设计
*   **数据集/场景**：
    *   **FrozenLake**：用于测试 Qwen3-8B 模型。
    *   **ALFWorld**：用于测试 Qwen3-32B 模型。
*   **算法**：GRPO（数学/逻辑常用）和 DAPO（支持冗余采样，需求波动更大）。
*   **Benchmark 与对比方法**：
    *   **资源固定基准**：ROLL（SOTA 框架）、AReaL（异步 RL 系统）。
    *   **资源弹性基准**：RLBoost（基于竞价实例）、$\lambda$RL（基于 Serverless GPU 模式）。
    *   **消融实验**：针对内存抢占、前缀缓存、双 SLO 控制、权重稀疏性等模块进行了详细验证。

#### 4. 资源与算力
*   **GPU 型号**：NVIDIA **H800**。
*   **算力规模**：
    *   **训练/专用 Rollout 集群**：16 至 48 块 GPU。
    *   **推理服务集群**：64 块 GPU。
*   **网络环境**：集群内 400 Gbps InfiniBand，跨集群模拟 200 Gbps（及以下）以太网。
*   **训练时长**：实验运行至模型收敛（如 GRPO 运行 40-100 Step）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 8B 到 32B 不同参数规模的模型，以及从固定资源到完全异步的多种对比维度。
*   **充分性**：
    *   进行了端到端吞吐量对比。
    *   针对推理服务 SLO 违例情况进行了压力测试。
    *   对跨集群带宽敏感度、权重稀疏度随训练步数的变化进行了深入分析。
*   **客观性**：使用了真实的微软生产环境推理流量轨迹（Trace）进行回放，确保了实验背景的真实性。

#### 6. 主要结论与发现
*   **吞吐量提升**：相比资源固定的基准，ROSE 提升了 **1.20x 至 3.31x** 的端到端吞吐量。
*   **SLO 保障**：在大幅提升 Rollout 速度的同时，推理服务的 P99 延迟始终保持在预设的 SLO 阈值内。
*   **稀疏性价值**：RL 权重的极高稀疏性（>95%）是解决跨数据中心同步瓶颈的关键，即使在 5 Gbps 的低带宽下也能高效工作。
*   **弹性优势**：相比 Serverless 方案，ROSE 消除了频繁的模型加载开销（从 26% 的耗时占比降至接近 0%）。

#### 7. 优点
*   **工业界落地价值高**：充分利用了企业内部现有的推理集群余量，无需额外购买 GPU 即可加速训练。
*   **技术融合创新**：巧妙地将 CUDA VMM、稀疏压缩和 SLO 感知调度结合在一起，解决了异构模型共存的难题。
*   **发现新特性**：首次量化并利用了 Agentic RL 权重更新的超高稀疏性。

#### 8. 不足与局限
*   **模型配对限制**：为了高效利用 VMM，通常需要推理模型和 Rollout 模型在架构或并行配置上具有一定的兼容性。
*   **环境依赖**：依赖于能够同时访问训练和推理集群的统一基础设施管理，对于小型机构或纯云端用户可能难以实施。
*   **冷启动开销**：虽然避免了频繁加载，但首次将 Rollout 模型部署到推理集群仍需一定的初始显存占用（约 2GB）。

（完）
