---
title: Training Transformers for KV Cache Compressibility
title_zh: 训练 Transformer 以提高 KV 缓存的可压缩性
authors: "Yoav Gelberg, Yam Eitan, Michael Bronstein, Yarin Gal, Haggai Maron"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05971v1"
tags: ["query:mlsys"]
score: 7.0
evidence: 长文本建模中KV缓存压缩率的优化
tldr: 针对长文本语言模型中KV缓存带来的内存和计算瓶颈，本文提出KV压缩感知训练（KV-CAT）。研究发现KV缓存的可压缩性取决于模型的内部表示而非仅由上下文决定。KV-CAT通过在持续预训练阶段引入KV稀疏化策略，强制模型学习更易压缩的表示。实验证明，该方法显著提升了下游压缩算法在检索、长文本问答等任务中的性能表现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.05971v1/fig-001.webp\", \"caption\": \"Table 5: KV-CAT continued-pretraining hyperparameters for QWEN2.5 checkpoints.\", \"page\": 26, \"index\": 1, \"width\": 822, \"height\": 467}, {\"url\": \"assets/figures/arxiv/2605.05971v1/fig-002.webp\", \"caption\": \"Figure 1: KV-Compression Aware Training (KV-CAT). A context a goes through both masked (left) and dense (right) forward passes. In the masked forward pass, routers (orange) compute masks for groups of consecutive layers, marking KV slots as active (green) or inactive (muted green). In the dense forward pass (blue), all KV slots are kept. The output of the masked forward pass is used to compute Lmask, router distributions are used to compute Lbudget, and the outputs of the dense forward pass are used to compute Lanchor. These are jointly used to update the parameters.\", \"page\": 2, \"index\": 2, \"width\": 805, \"height\": 337}, {\"url\": \"assets/figures/arxiv/2605.05971v1/fig-003.webp\", \"caption\": \"Figure 2: KV-CAT speeds up gradient-based KV cache compression. We plot the gap in suffix perplexity under full/compressed-prefix inference throughout gradient-based KV cache optimization. Each panel fixes a different KV keep ratio. Across ratios, the KV-CAT checkpoint achieves a comparable ∆PPL in fewer optimization steps than the base model, yielding up to a 5× speedup.\", \"page\": 7, \"index\": 3, \"width\": 829, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2605.05971v1/fig-004.webp\", \"caption\": \"Table 3: Suffix completion under prefix compression. Each example uses a 768-token prefix and a 256-token suffix. Prefix KVs are compressed using Attention Matching. We report: (1) ∆PPL, the difference in suffix perplexity under the full prefix vs. compressed prefix; (2) KL, the divergence between the two token distributions; and (3) Top-1, percentage of tokens on which the two distributions’ top-1 agree.\", \"page\": 8, \"index\": 4, \"width\": 383, \"height\": 516}, {\"url\": \"assets/figures/arxiv/2605.05971v1/fig-005.webp\", \"caption\": \"Figure 4: KV-CAT speeds up gradient-based KV cache compression. We plot the gap in suffix perplexity under full/compressed-prefix inference throughout gradient-based KV cache optimization. Each panel fixes a different KV keep ratio. Across ratios, the KV-CAT checkpoint achieves a comparable ∆PPL in fewer optimization steps than the base model, yielding up to a 5× speedup.\", \"page\": 32, \"index\": 5, \"width\": 829, \"height\": 479}, {\"url\": \"assets/figures/arxiv/2605.05971v1/fig-006.webp\", \"caption\": \"Figure 3: QWEN2.5-0.5B KV-CAT continued pretraining run. We plot dense validation nexttoken-prediction loss, compressed-path validation next-token-prediction loss, and the realized KV retention ratio over the 5.24B-token KV-CAT continued-pretraining run. The retention trace reports the fraction of KV slots kept by the learned routers, whose budget target is 50%.\", \"page\": 27, \"index\": 6, \"width\": 814, \"height\": 584}]"
motivation: 现有的KV缓存压缩方法多为事后处理，受限于预训练模型固有的表示能力，难以达到最优压缩效果。
method: 提出KV-CAT持续预训练方法，通过在训练时随机掩码KV槽位，引导模型生成更具压缩潜力的内部表示。
result: 实验表明KV-CAT在检索、长文本问答及困惑度评估中均改善了压缩质量与资源预算之间的权衡。
conclusion: 通过在训练阶段显式引入压缩约束，可以有效提升Transformer模型对KV缓存压缩的适应性和效率。
---

## 摘要
长上下文语言建模日益受到键值（KV）缓存的限制，其内存和解码时的访问成本随前缀长度线性增长。这一瓶颈催生了一系列上下文压缩方法，从令牌级摘要到最近基于优化的 KV 压缩方法。这些事后（post-hoc）方法作用于固定预训练模型的 KV 缓存，因此其有效性从根本上受限于模型内部表示的可压缩程度。在这项工作中，我们形式化了 KV 可压缩性的概念，并证明它是学习到的表示的一种属性，而不仅仅取决于上下文本身。我们证明了几乎任何序列到向量函数都存在高度可压缩和本质上不可压缩的 Transformer 实现，这突显了在训练期间引导 Transformer 学习可压缩表示的必要性。受此启发，我们提出了 KV 压缩感知训练（KV-CAT），这是一种持续预训练过程，旨在激励可压缩表示的出现。我们引入了一种训练时的 KV 稀疏化策略，在训练过程中掩蔽 KV 插槽。这迫使模型使用更少的 KV 插槽，并鼓励其学习适用于事后压缩的表示。实验表明，KV-CAT 在检索、长上下文问答以及基于困惑度的压缩前缀续写评估中，改善了下游压缩方法的质量-预算权衡。

## Abstract
Long-context language modeling is increasingly constrained by the Key-Value (KV) cache, whose memory and decode-time access costs scale linearly with the prefix length. This bottleneck has motivated a range of context-compression methods, from token-level summarization to recent optimization-based KV compression methods. These post-hoc methods operate on the KV cache of a fixed pretrained model, so their effectiveness is fundamentally limited by how well the model's internal representations can be compressed. In this work, we formalize the notion of KV compressibility and show that it is a property of the learned representations, rather than of the context alone. We prove that almost any sequence-to-vector function admits both highly compressible and inherently non-compressible transformer implementations, highlighting the need to guide transformers toward compressible representations during training. Motivated by this, we propose KV-Compression Aware Training (KV-CAT), a continued pretraining procedure that incentivizes the emergence of compressible representations. We introduce a train-time KV sparsification policy that masks KV slots during training. This forces the model to use fewer KV slots and encourages it to learn representations amenable to post-hoc compression. Empirically, we show that KV-CAT improves the quality-budget tradeoff of downstream compression methods across retrieval, long-context question answering, and perplexity-based evaluation of compressed-prefix continuation.