---
title: "FedQueue: Queue-Aware Federated Learning for Cross-Facility HPC Training"
title_zh: FedQueue：面向跨设施 HPC 训练的队列感知联邦学习
authors: "Yijiang Li, Emon Dey, Zilinghan Li, Krishnan Raghavan, Ravi Madduri, Kibaek Kim"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02125v2"
tags: ["query:mlsys"]
score: 9.0
evidence: 针对HPC训练和调度延迟管理的队列感知联邦学习
tldr: 针对跨HPC设施联邦学习中批处理调度器带来的随机排队延迟问题，本文提出FedQueue协议。该方法通过在线预测各设施的排队延迟来规划本地工作量，利用基于截止时间的准入控制缓冲延迟到达以限制陈旧度，并结合感知陈旧度的聚合策略稳定异构负载。FedQueue在非凸目标下具有收敛保证，实验证明其在真实部署和模拟环境中均显著优于基准算法，大幅缩短了达到目标精度所需的时间。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02125v2/fig-001.webp\", \"caption\": \"Table 7. Controlled experiment configuration and hyperparameters. This table consolidates the controlled-workload specification (MNIST + CNN) and the concrete run configuration/hyperparameters used in Appendix C sweeps (queue variability, admission tolerance, and prediction quality).\", \"page\": 25, \"index\": 1, \"width\": 1010, \"height\": 958}, {\"url\": \"assets/figures/arxiv/2605.02125v2/fig-002.webp\", \"caption\": \"Figure 5. Facility-level loss trajectories for all methods. Each panel shows test loss over time for individual facilities (colored dashed lines) and the federated global model (purple solid line). FedQueue configurations achieve better balance between facility utilization and global model quality.\", \"page\": 21, \"index\": 2, \"width\": 1017, \"height\": 763}, {\"url\": \"assets/figures/arxiv/2605.02125v2/fig-003.webp\", \"caption\": \"Figure 6. Staleness under queue variance. Empirical CDF of clients arrived beyond Tsync under a sweep of ρ. This plot audits the bounded-staleness behavior induced by ρ.\", \"page\": 22, \"index\": 3, \"width\": 998, \"height\": 315}, {\"url\": \"assets/figures/arxiv/2605.02125v2/fig-004.webp\", \"caption\": \"Figure 8. Staleness under EWMA rate (α) variation. Empirical CDF of clients arrived beyond Tsync under a sweep of α. This plot audits the bounded-staleness behavior induced by α.\", \"page\": 23, \"index\": 4, \"width\": 900, \"height\": 285}, {\"url\": \"assets/figures/arxiv/2605.02125v2/fig-005.webp\", \"caption\": \"Figure 7. Time-to-quality varying admission parameter. Validation accuracy versus elapsed time under a sweep of admission parameter γ (low/medium/high or a multi-level sweep). FEDQUEUE consistently converges faster and reaches the highest accuracy compared to the baselines for both IID and non-IID cases.\", \"page\": 23, \"index\": 5, \"width\": 1017, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2605.02125v2/fig-006.webp\", \"caption\": \"Figure 4. Impact of admission buffer. (Left) Effect of scaling the buffer δ on histogram of arrival times and (Right) corresponding time to quality (95%). This test exposes the trade-off between better convergence value and convergence time in the presence of client arrival variability. Note that the histograms of δ = 1, 2 are almost identical and overlapped each other.\", \"page\": 8, \"index\": 6, \"width\": 492, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2605.02125v2/fig-007.webp\", \"caption\": \"Figure 1. Illustration of the FedQueue Algorithm. The FedQueue server first obtains initial queuing delay estimates for each client (q̂(0)k ) during a warm-up stage, and accordingly assigns the number of local training steps (E(r) k ). In each round r, the FedQueue server updates the estimates based on the recent queuing delay (q(r)k ) and performs global aggregation using all client updates received before the admission deadline (rTsync). If a certain client fails to return its update within the deadline due to an unexpected delay (e.g., Client 1 in Round 2), its update will be deferred and incorporated in the next aggregation round, with the staleness factor applied.\", \"page\": 4, \"index\": 7, \"width\": 845, \"height\": 400}, {\"url\": \"assets/figures/arxiv/2605.02125v2/fig-008.webp\", \"caption\": \"Table 8. Bound verification grid (controlled). For each setting in a representative (ρk, γ, α) grid, we report the empirical probability of client arrival beyond the cutoff, P, Normalized expectation of delay, Êd, Normalized maximum delay, Rd, and the wall-clock time to reach target accuracy A⋆ (95%).\", \"page\": 24, \"index\": 8, \"width\": 504, \"height\": 298}]"
motivation: 跨HPC设施的联邦学习受批处理调度器排队延迟影响，导致同步模式效率低下且异步模式存在模型陈旧问题。
method: 提出一种感知队列的FL协议，通过在线预测排队延迟、实施截止时间准入控制及感知陈旧度的聚合来优化训练过程。
result: "在真实跨设施部署中性能提升20.5%，在高排队方差和非独立同分布场景下，达到目标精度的时间缩短了约34%。"
conclusion: FedQueue通过将调度延迟纳入训练和聚合决策，有效解决了HPC环境下的异构延迟挑战，显著提升了联邦学习的收敛效率。
---

## 摘要
跨多个高性能计算（HPC）设施的联邦学习（FL）面临着来自批处理调度器的随机准入延迟，这些延迟在总运行时间中占据主导地位。同步联邦学习会受到严重的掉队者影响，而异步联邦学习在队列激增时会积累过时的更新。我们提出了 FedQueue，这是一种队列感知的联邦学习协议，它将调度器延迟直接纳入训练和聚合中，具体包括：(i) 在线预测每个设施的队列延迟以规划本地工作量；(ii) 应用基于截止时间的准入控制，通过缓冲晚到的更新来限制陈旧性；(iii) 执行陈旧性感知的聚合，以稳定异构的本地工作负载。我们证明了在有界陈旧性下，非凸目标的收敛速度为 $\mathcal{O}(1/\sqrt{R})$，并表明在队列预测误差下，准入控制能以高概率产生有界陈旧性。FedQueue 在真实跨设施部署中的表现比基准算法提高了 20.5%。受控队列模拟显示了相对于基准算法的稳健改进；特别是在高队列方差和非独立同分布（non-IID）分区下，达到目标准确率所需的时间减少了约 34%。

## Abstract
Federated learning (FL) across multiple HPC facilities faces stochastic admission delays from batch schedulers that dominate wall-clock time. Synchronous FL suffers from severe stragglers, while asynchronous FL accumulates stale updates when queues spike. We propose FedQueue, a queue-aware FL protocol that incorporates scheduler delays directly into training and aggregation, which (i) predicts per-facility queue delays online to budget local work, (ii) applies cutoff-based admission that buffers late arrivals to bound staleness, and (iii) performs staleness-aware aggregation to stabilize heterogeneous local workloads. We prove the convergence for non-convex objectives at rate $\mathcal{O}(1/\sqrt{R})$ under bounded staleness, and show that the admission controls yield bounded staleness with high probability under queue-prediction error. Real-world cross-facility deployment of FedQueue shows 20.5% improvement over baseline algorithms. Controlled queue simulations demonstrate robust improvement over the baselines; in particular, about 34% reduction in time to reach a target accuracy level under high queue variance and non-IID partitions.

---

## 论文详细总结（自动生成）

### FedQueue: 面向跨设施 HPC 训练的队列感知联邦学习深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
在科学研究和工业应用中，跨设施联邦学习（FL）允许不同机构在不共享原始数据的情况下协作训练模型。然而，当客户端是高性能计算（HPC）设施时，任务必须通过**批处理调度器**（如 Slurm, PBS）排队。
*   **研究动机**：HPC 调度器带来的“准入延迟”（排队时间）具有高度随机性和不可预测性，往往占总运行时间的绝大部分。
*   **现有挑战**：同步协议（如 FedAvg）会因个别设施排队过久而导致严重的“掉队者”问题；异步协议（如 FedAsync）在队列激增时会积累过高的模型陈旧度（Staleness），导致训练不稳定。
*   **核心目标**：设计一种能够感知并主动管理 HPC 队列延迟的 FL 协议，以优化实际墙钟时间（Wall-clock time）内的收敛效率。

#### 2. 方法论：核心思想与关键技术
论文提出了 **FedQueue** 协议，其核心思想是将调度延迟直接建模并纳入训练决策中。主要包含四个关键组件：
*   **在线队列预测**：服务器利用指数加权移动平均（EWMA）实时预测每个设施的排队延迟 $\hat{q}_k$。该方法对预测器形式具有通用性，只要预测误差满足亚高斯分布即可。
*   **自适应工作量预算**：根据预测的排队时间，动态调整每个设施在当前同步周期（$T_{sync}$）内的本地计算时间预算 $J_k$ 和本地步数 $E_k$。排队预估长的设施分配较少的步数，以确保其能按时返回。
*   **截止时间准入控制与缓冲**：设定严格的截止时间。若更新在截止时间内到达则参与当轮聚合；若超时到达则不丢弃，而是存入缓冲区并在下一轮聚合，从而在保证系统利用率的同时通过算法控制陈旧度的上界。
*   **陈旧度感知聚合与逆学习率缩放**：
    *   **陈旧度衰减**：使用调和衰减函数对延迟更新进行降权。
    *   **逆学习率缩放**：为了平衡不同设施因步数 $E_k$ 不同而产生的漂移，本地学习率与步数成反比（$\eta_k \propto 1/E_k$），确保异构负载下的优化稳定性。

#### 3. 实验设计
实验分为真实世界部署和受控合成模拟两个部分：
*   **真实部署场景**：
    *   **设施**：连接了美国四大顶级 HPC 中心：Aurora (ALCF), Polaris (ALCF), Perlmutter (NERSC), Frontier (OLCF)。
    *   **任务**：使用 LLaMA2-7B 模型在化学指令数据集 SMolInstruct 上进行全参数微调。
*   **受控模拟场景**：
    *   **数据集**：MNIST，采用 Dirichlet 分布构建非独立同分布（Non-IID）分区。
    *   **模型**：卷积神经网络（CNN）。
*   **基准方法（Benchmarks）**：对比了 FedAvg（同步）、FedAsync（异步）、FedBuff（缓冲异步）和 FedCompass（计算能力感知）。

#### 4. 资源与算力
论文明确记录了大规模实验的算力配置：
*   **GPU 型号与数量**：
    *   **Aurora**：24 个 GPU。
    *   **Polaris**：8 个 GPU。
    *   **Frontier**：16 个 GPU。
    *   **Perlmutter**：8 个 GPU。
*   **训练时长**：真实部署实验设定了 17,000 秒（约 4.7 小时）的固定墙钟时间预算。
*   **软件框架**：基于 APPFL 框架实现，并利用 Globus 计算平台进行跨设施作业提交。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从数千亿参数规模的大模型（LLaMA2-7B）到基础视觉模型的验证，跨度极大。
*   **消融实验**：非常充分。作者分别验证了 EWMA 预测、陈旧度衰减权重、逆学习率缩放对性能的影响，证明了每个组件的必要性。
*   **客观性与公平性**：通过在相同时间段运行不同算法以模拟相似的系统负载，并针对排队方差（$\rho$）、准入阈值（$\gamma$）等参数进行了详尽的网格搜索和敏感度分析，实验设计严谨。

#### 6. 主要结论与发现
*   **性能提升**：在真实部署中，FedQueue 的最终 Loss 比基准算法提升了 20.5%；在模拟实验中，达到目标精度的时间缩短了 34%。
*   **收敛保证**：理论证明了在非凸目标下，FedQueue 具有 $\mathcal{O}(1/\sqrt{R})$ 的收敛率，且准入控制能以高概率将陈旧度限制在常数范围内。
*   **资源效率**：相比 FedAvg，FedQueue 减少了约 67% 的模型传输量，并显著提高了本地资源的有效利用率。

#### 7. 优点
*   **系统与算法协同**：不同于纯算法研究，该工作深入解决了 HPC 调度系统这一实际物理限制，具有极强的工程落地价值。
*   **陈旧度可控**：通过准入控制将“随机陈旧”转变为“有界陈旧”，为异步 FL 提供了更坚实的理论支撑。
*   **灵活性**：预测器和衰减函数均可插拔，能够适应不同的 HPC 调度策略。

#### 8. 不足与局限
*   **预测假设**：理论分析依赖于预测误差的亚高斯分布假设，在极端繁忙或调度策略剧烈波动的对抗性环境下，预测失效可能导致性能下降。
*   **故障处理**：目前模型主要处理延迟，未显式建模作业失败（Job Failure）或被调度器强制取消（Preemption）的情况，这在超大规模 HPC 任务中时有发生。
*   **通信开销**：虽然减少了传输次数，但频繁的跨设施小规模通信在某些网络受限的设施间仍可能成为瓶颈。

（完）
