---
title: Accelerating Compound LLM Training Workloads with Maestro
title_zh: 使用 Maestro 加速复合大语言模型（LLM）训练负载
authors: "Xiulong Yuan, Hongqing Chen, Jiaxuan Peng, Fan Zhou, Zhixiang Ruan, Zekun Wang, Bo Zheng, Rui Men, Haiquan Wang, Zhipeng Zhang, Langshi Chen, Man Yuan, Jiaqi Gao, Zhengping Qian, Junyang Lin, Yong Li, Wei Lin, Junhua Wang, Jingren Zhou"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10501v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 加速具有异构组件的复合大模型训练负载
tldr: "针对知识蒸馏和多模态大模型等复合LLM训练任务中存在的静态组件差异和动态计算路径不规则问题，本文提出了Maestro框架。该框架通过将工作负载重构为粗粒度的“分段图”，允许各部分独立配置并行策略，并引入波前调度算法动态重排输入样本以优化并发执行。在生产环境中，Maestro显著提升了GPU利用率，将关键任务的资源消耗降低了约40%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.10501v1/fig-001.webp\", \"caption\": \"Figure 5: Illustration of fan-out mechanism.\", \"page\": 6, \"index\": 1, \"width\": 477, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2605.10501v1/fig-002.webp\", \"caption\": \"Figure 9: Normalized throughput and peak memory of the teacher model across micro-batch sizes.\", \"page\": 11, \"index\": 2, \"width\": 571, \"height\": 306}, {\"url\": \"assets/figures/arxiv/2605.10501v1/fig-003.webp\", \"caption\": \"Figure 10: Maestro performance on distillation training.\", \"page\": 11, \"index\": 3, \"width\": 477, \"height\": 360}, {\"url\": \"assets/figures/arxiv/2605.10501v1/fig-004.webp\", \"caption\": \"Figure 6: Illustration of data resharding and communication across sections.\", \"page\": 7, \"index\": 4, \"width\": 667, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2605.10501v1/fig-005.webp\", \"caption\": \"Figure 8: Maestro performance on multimodal training.\", \"page\": 10, \"index\": 5, \"width\": 761, \"height\": 330}, {\"url\": \"assets/figures/arxiv/2605.10501v1/fig-006.webp\", \"caption\": \"Figure 1: Architecture of Qwen3-VL model.\", \"page\": 3, \"index\": 6, \"width\": 477, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2605.10501v1/fig-007.webp\", \"caption\": \"Figure 3: Overview of Maestro.\", \"page\": 4, \"index\": 7, \"width\": 761, \"height\": 461}, {\"url\": \"assets/figures/arxiv/2605.10501v1/fig-008.webp\", \"caption\": \"Figure 7: Data scheduling for VLM training with fanout = 4 over a global batch of 12 samples.\", \"page\": 9, \"index\": 8, \"width\": 950, \"height\": 637}, {\"url\": \"assets/figures/arxiv/2605.10501v1/fig-009.webp\", \"caption\": \"Figure 4: Section construction in knowledge distillation training.\", \"page\": 5, \"index\": 9, \"width\": 477, \"height\": 305}]"
motivation: 传统训练框架难以应对复合LLM训练中组件间的静态异构性以及由数据依赖导致的动态运行时不规则性，导致GPU利用率低下。
method: 提出以分段为中心的框架，通过分段图实现组件感知的资源分配，并利用波前调度算法动态优化并发执行路径。
result: "在数百万GPU小时的生产部署中，Maestro在知识蒸馏和多模态训练等关键任务上减少了约40%的GPU消耗。"
conclusion: Maestro通过解耦组件配置和动态调度样本，有效解决了复合LLM训练的效率瓶颈，具有显著的实际应用价值。
---

## 摘要
复合大语言模型（LLM）训练负载（如知识蒸馏和多模态大语言模型（MLLM）训练）正日益受到关注。这些负载通常由异构组件组成，这些组件在参数规模、执行模式（仅前向或完整前向-后向）以及序列长度方面各不相同。此外，组件的激活可能具有数据依赖性：在 MLLM 训练中，特定模态的部分仅在输入包含相应模态时才会激活，从而导致动态计算路径和不规则的运行时负载。传统的框架专为单体模型设计，无法处理静态（跨组件）和动态（运行时）的双重异构性。由于在所有组件中强制执行统一的训练配置并忽略了输入引起的差异，这些框架的吞吐量不佳且 GPU 利用率较低。在本文中，我们介绍了 Maestro，这是一个以分段（section）为中心的训练框架，旨在解决上述挑战。Maestro 首先将工作负载重构为粗粒度的分段图。每个分段独立配置其并行策略、微批次大小（micro-batch size）和数据并行度，从而实现细粒度的、组件感知的资源分配，以应对静态异构性。为了解决运行时不规则性，Maestro 引入了一种波前（wavefront）调度算法，该算法动态重新排序输入样本，以协调并发的分段执行，同时保持跨分段的数据依赖关系。这最大化了分段间的并行性并减少了停顿，从而提高了硬件利用率。Maestro 已在生产环境中部署并运行了数百万 GPU 小时，在包括知识蒸馏和 MLLM 训练在内的关键负载上，将 GPU 消耗降低了约 40%，验证了其在现实世界中的影响力。

## Abstract
Compound LLM training workloads-such as knowledge distillation and multimodal LLM (MLLM) training-are gaining prominence. These typically comprise heterogeneous components differing in parameter scale, execution mode (forward-only or full forward-backward), and sequence length. Besides, component activation can be data-dependent: in MLLM training, modality-specific parts activate only when inputs contain corresponding modalities, causing dynamic computational paths and irregular runtime workloads. Conventional frameworks, designed for monolithic models, cannot handle the dual heterogeneity-static (across components) and dynamic (runtime). By enforcing one-size-fits-all training configurations across components and ignoring input-induced variations, they suffer suboptimal throughput and poor GPU utilization. In this paper, we introduce Maestro, a section-centric training framework that addresses both challenges. Maestro first restructures the workload into a coarse-grained section graph. Each section independently configures its parallelism strategy, micro-batch size, and data-parallel degree-enabling fine-grained, component-aware resource allocation to tackle static heterogeneity. To tackle runtime irregularity, Maestro introduces a wavefront scheduling algorithm that dynamically reorders input samples to orchestrate concurrent section execution while preserving cross-section data dependencies. This maximizes inter-section parallelism and minimizes stalls, boosting hardware utilization. Deployed in production for millions of GPU hours, Maestro reduces GPU consumption by ~40% on key workloads-including knowledge distillation and MLLM training-validating its real-world impact.

---

## 论文详细总结（自动生成）

### 论文总结：使用 Maestro 加速复合大语言模型（LLM）训练负载

#### 1. 核心问题与整体含义（研究动机和背景）
随着大模型的发展，训练范式正从单一的 Transformer 堆栈转向由多个异构组件组成的**复合负载（Compound Workloads）**，如多模态大模型（MLLM）和知识蒸馏（Knowledge Distillation）。
*   **静态异构性**：不同组件（如视觉编码器与 LLM 主干）在参数规模、执行模式（仅前向或前向+后向）和序列长度上差异巨大。
*   **动态不规则性**：计算路径依赖于输入数据。例如，在多模态训练中，纯文本样本会跳过视觉编码器，导致计算图动态变化。
*   **传统框架局限**：如 Megatron-LM 等框架采用“一刀切”的配置，强制所有组件共享相同的并行策略和微批次大小，导致 GPU 利用率低下和严重的流水线气泡。

#### 2. 方法论：核心思想与关键技术
Maestro 提出了一个**以分段（Section）为中心**的训练框架，核心技术包括：
*   **分段重构（Section Construction）**：将复杂的计算图分解为逻辑独立的分段。每个分段可以根据其计算、内存和通信特性，独立配置并行策略（TP, PP, CP, DP）、微批次大小（MBS）和数据并行度。
*   **扇出机制（Fan-out Mechanism）**：允许不同分段间拥有不同的数据并行度。例如，计算较快的教师模型（仅前向）可以通过扇出机制，用较少的 GPU 资源支撑多个学生模型实例的训练。
*   **波前调度算法（Wavefront Scheduling）**：针对数据依赖导致的运行时不规则性，Maestro 引入了一种启发式调度算法（复杂度 $O(N^2)$）。它动态重排样本顺序，优先保证“关键分段”（瓶颈组件）持续饱和运行，从而最大化分段间的并行度。
*   **异步非对称消息队列**：基于单边 RDMA 操作，实现跨并行域的高效张量传输和重组（Resharding），减少 CPU 参与和 GPU 开销。

#### 3. 实验设计
*   **场景与数据集**：
    1.  **多模态训练**：训练 Qwen3.5-400B 和 Qwen3-Next-80B，使用 32K 序列长度的多模态数据集（包含文本、图像等混合数据）。
    2.  **知识蒸馏**：使用 Qwen3.5-400B 作为教师模型，Qwen3-Next-80B 作为学生模型，基于 KL 散度进行蒸馏。
*   **Benchmark（基准）**：以 **Megatron-LM** 作为主要对比对象，确保在相同的底层优化条件下进行公平比较。
*   **评估指标**：端到端 Token 吞吐量、单 GPU Token 吞吐量、相对于纯文本训练的效率比。

#### 4. 资源与算力
*   **硬件环境**：阿里云大规模 GPU 集群。
*   **算力规模**：
    *   在 VLM 实验中，为 LLM 主干分配了 **512 张 GPU**，并为视觉编码器（ViT）分配了额外的资源。
    *   论文提到 Maestro 已在生产环境中部署，累计运行超过**数百万 GPU 小时**。
    *   具体 GPU 型号未在文中明确标出（通常为 A100/H100 级别），但强调了是大规模集群。

#### 5. 实验数量与充分性
*   **实验覆盖**：针对多模态（400B 和 80B 两个量级）和知识蒸馏两个核心场景进行了深入测试。
*   **充分性**：实验不仅对比了总吞吐量，还通过“单 GPU 吞吐量”排除了资源规模的影响，并验证了在不同微批次大小下的内存与性能表现。
*   **客观性**：对比实验中，Maestro 的关键分段资源配置与基准测试完全一致，确保了性能提升确实源于框架调度而非单纯增加算力。

#### 6. 主要结论与发现
*   **性能提升**：在知识蒸馏任务中，Maestro 的端到端吞吐量比 Megatron-LM 高出 **1.75 倍**，单 GPU 效率提升 1.4 倍。
*   **资源节省**：在生产环境中，Maestro 将关键负载的 GPU 资源消耗降低了约 **40%**。
*   **消除开销**：在多模态训练中，通过波前调度，Maestro 实现了 100% 的相对效率（即多模态训练的吞吐量与纯文本训练持平），完全掩盖了视觉编码器的计算开销。

#### 7. 优点
*   **灵活性高**：打破了传统框架的同构假设，允许组件级的精细化资源配置。
*   **动态感知**：波前调度算法有效解决了多模态数据配比导致的流水线气泡问题。
*   **生产验证**：经过数百万 GPU 小时的实战检验，证明了其在大规模工业级场景下的稳定性和有效性。

#### 8. 不足与局限
*   **调度算法局限**：波前调度采用的是启发式贪心策略，虽然高效，但在极端复杂的依赖关系下可能不是全局最优解。
*   **分段构建成本**：虽然提供了指导原则，但针对特定新模型（如超大规模全模态模型）的分段划分和参数优化仍可能需要一定的专家经验。
*   **通信依赖**：高效的跨分段通信高度依赖于高性能网络（如 RDMA），在低带宽环境下性能提升可能受限。

（完）
