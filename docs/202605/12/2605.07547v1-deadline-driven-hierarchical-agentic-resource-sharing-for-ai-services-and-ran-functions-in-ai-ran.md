---
title: Deadline-Driven Hierarchical Agentic Resource Sharing for AI Services and RAN Functions in AI-RAN
title_zh: AI-RAN中面向AI服务与RAN功能的截止日期驱动层级代理资源共享
authors: "Haiyuan Li, Yulei Wu, Dimitra Simeonidou"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07547v1"
tags: ["query:mlsys"]
score: 8.5
evidence: AI-RAN中GPU/CPU分配的分层框架
tldr: 本研究针对AI-RAN中实时RAN功能与异构AI服务共享GPU基础设施的挑战，提出了分层智能体框架（HAF）。该框架结合了负责慢尺度部署决策的LLM智能体和负责快尺度资源分配的截止日期感知凸算法，并利用预测评论器减少无效迁移。实验表明，HAF显著提升了SLO达成率，为AI与通信融合架构下的资源调度提供了高效解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.07547v1/fig-001.webp\", \"caption\": \"TABLE I SIMULATION PARAMETERS.\", \"page\": 5, \"index\": 1, \"width\": 529, \"height\": 525}, {\"url\": \"assets/figures/arxiv/2605.07547v1/fig-002.webp\", \"caption\": \"Fig. 1. HAF architecture: a slow, epoch-based placement layer and a fast, event-driven allocation layer over AI-RAN.\", \"page\": 3, \"index\": 2, \"width\": 515, \"height\": 379}]"
motivation: AI-RAN中实时RAN功能与异构AI服务在计算共享时存在时间尺度不匹配以及服务迁移带来的中断开销问题。
method: 提出一种分层智能体框架，利用LLM进行慢尺度部署，结合凸优化算法进行快尺度资源分配，并引入预测评论器过滤低效迁移。
result: "HAF实现了90.0%的整体SLO达成率，较基准提升20.5%，并将AI服务请求达成率从51%大幅提高至85.3%。"
conclusion: 该分层架构能有效平衡AI-RAN中的资源竞争与迁移成本，显著优化了边缘计算环境下的服务水平目标达成。
---

## 摘要
AI-RAN将AI服务和无线接入网（RAN）功能整合到网络边缘统一的GPU加速基础设施上。然而，实时RAN功能与高度异构的AI服务之间的计算共享需要协调不同时间尺度上的调度决策，且位置调整可能涉及跨节点的业务迁移，从而产生不可忽视的中断。本文提出了一种用于AI-RAN计算共享的层级代理框架（HAF），该框架结合了基于大语言模型（LLM）的代理（用于AI服务和RAN功能的慢时间尺度放置）以及一种闭式、截止日期感知的凸算法（用于快时间尺度的GPU/CPU分配）。该LLM代理还配备了一个预测性评论器（predictive critic），当迁移引起的服务中断超过预期的服务水平目标（SLO）收益时，该评论器会过滤掉这些迁移。实验结果表明，HAF实现了90.0%的整体SLO达成率，比最强基准提高了20.5%，并将AI服务请求达成率从51%提升至85.3%。进一步的评估显示，HAF在各种负载条件下均保持优势，且该评论器在多个开源LLM代理中一致地提高了SLO达成率。

## 速览
**TLDR**：本文针对AI-RAN架构中AI服务与无线接入网（RAN）功能共享计算资源时面临的时间尺度不匹配及迁移中断问题，提出了一种分层代理框架（HAF）。该框架结合了基于大语言模型（LLM）的慢速尺度放置决策与基于凸优化的快速尺度资源分配算法，并引入预测性评论器以优化迁移决策。实验表明，HAF显著提升了服务水平目标（SLO）的达成率，在复杂负载下表现优异，为AI与通信融合提供了高效的资源管理方案。 \
**Motivation**：旨在解决AI-RAN中实时RAN功能与异构AI服务在共享GPU资源时存在的时间尺度不一及服务迁移中断挑战。 \
**Method**：提出HAF框架，利用LLM代理进行慢速放置决策，结合凸优化算法进行快速资源分配，并使用预测性评论器过滤低效迁移。 \
**Result**：HAF实现了90.0%的整体SLO达成率，较最强基准提升20.5%，并将AI服务请求完成率从51%提高至85.3%。 \
**Conclusion**：分层代理架构能有效协调AI-RAN中的资源竞争，通过结合LLM的推理能力与数学优化，显著增强了边缘计算的可靠性与效率。

---

## Abstract
AI-RAN consolidates AI services and Radio Access Network (RAN) functions onto a unified, GPU-accelerated infrastructure at the network edge. However, compute sharing between real-time RAN functions and highly heterogeneous AI services requires coordination of scheduling decisions at mismatched timescales, and placement adaptation may require service migration across nodes with non-negligible interruptions. This paper proposes a hierarchical agentic framework (HAF) for compute sharing in AI-RAN that combines a large language model (LLM)-based agent for slow-timescale placement of AI services and RAN functions with a closed-form, deadline-aware convex algorithm for fast-timescale GPU/CPU allocation. The LLM agent is further equipped with a predictive critic that filters out migrations when the induced service interruption outweighs the expected service-level objective (SLO) benefit. Experimental results show that HAF reaches 90.0% overall SLO fulfillment, a 20.5% improvement over the strongest baseline, and raises AI service request fulfillment from 51% to 85.3%. Further evaluations show that HAF retains its advantage under diverse load conditions, while the critic consistently improves SLO fulfillment across multiple open-source LLM agents.

---

## 论文详细总结（自动生成）

这篇论文针对 6G 演进中的 AI-RAN（人工智能与无线接入网融合）架构，提出了一种创新的资源管理框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **背景**：AI-RAN 旨在将 AI 推理服务与 RAN（无线接入网）功能（如 DU、CU-UP）整合在边缘节点的统一 GPU 加速基础设施上。
*   **核心挑战**：
    *   **时间尺度不匹配**：RAN 功能要求毫秒级的实时响应，而 AI 服务（尤其是大模型）的部署调整和迁移通常在秒级。
    *   **迁移开销**：服务迁移涉及模型重新加载和协议栈初始化，会产生显著的中断，若调度不当，迁移带来的损失可能超过其收益。
    *   **资源竞争**：GPU、CPU 和内存资源在硬实时（RAN）与弹性（AI）任务之间存在复杂的耦合竞争。

### 2. 方法论：层级代理框架 (HAF)
论文提出了一个双层控制架构，将决策解耦：
*   **慢时间尺度放置层（Placement Layer）**：
    *   **LLM 智能体**：利用大语言模型（如 Qwen、DeepSeek）作为推理引擎，根据节点负载、VRAM 余量和任务优先级，生成候选的服务迁移方案。
    *   **预测评论器（Predictive Critic）**：一个轻量级的 MLP 网络，用于预测迁移后的 SLO（服务水平目标）达成率。它充当“过滤器”，如果预测显示迁移导致的中断损失大于性能收益，则否决该动作。
*   **快时间尺度分配层（Allocation Layer）**：
    *   **截止日期感知凸算法**：在确定的部署状态下，针对瞬时到达的请求，通过闭式凸优化公式动态分配 GPU/CPU 算力。
    *   **硬实时保障**：通过为 RAN 功能设置“算力底线（Capacity Floors）”，确保其在任何情况下优先满足截止日期要求。

### 3. 实验设计
*   **数据集与场景**：
    *   **AI 负载**：采用 Azure LLM 推理追踪数据集（包含不同长度的 Prompt 和 Response）。
    *   **RAN 负载**：根据 3GPP 标准合成的 URLLC（超可靠低延迟）和 eMBB（增强移动宽带）流量。
    *   **硬件环境**：模拟包含 6 个异构节点（GPU 密集型、CPU 密集型、平衡型）的边缘集群。
*   **对比方法（Benchmarks）**：
    *   **HAF-Static**：固定部署，仅使用 HAF 的分配层。
    *   **Round-Robin**：轮询调度。
    *   **Lyapunov 优化**：基于漂移加惩罚的传统控制理论方法。
    *   **博弈论（Game Theory）**：基于最佳响应的资源竞争模型。
    *   **CAORA (DRL)**：基于深度强化学习（SAC 算法）的现有先进基准。

### 4. 资源与算力
*   **算力使用情况**：论文**未明确说明**训练 MLP 评论器和运行 LLM 智能体所消耗的具体物理算力（如 GPU 型号、数量或训练时长）。
*   **模型选择**：实验中评估了多种开源 LLM 代理，包括 `qwen3:32b`、`qwen2.5:72b`、`deepseek-r1:70b` 等，这暗示了推理过程需要相当规模的 GPU 显存支持。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了**消融实验**，验证了不同 LLM 引擎以及“评论器”模块对性能的提升。
    *   进行了**负载敏感度分析**，在不同负载强度（$\rho = 0.75$ 到 $1.25$）下测试框架的鲁棒性。
*   **充分性评价**：实验设计较为全面，涵盖了从轻载到过载的多种场景，且对比了从传统启发式到现代强化学习的多种基准方法。使用真实世界的 LLM 推理追踪数据增加了实验的可信度。

### 6. 主要结论与发现
*   **性能飞跃**：HAF 实现了 **90.0%** 的整体 SLO 达成率，比最强的基准方法提升了 **20.5%**。
*   **AI 服务优化**：在保障 RAN 硬实时的前提下，将 AI 服务的请求达成率从 51% 提升至 **85.3%**。
*   **评论器的价值**：预测评论器能有效减少约 **44%** 的无效迁移，使 SLO 达成率额外提升 6.3%。
*   **容量极限**：当系统极度过载（$\rho = 1.25$）时，所有方法的性能趋于一致，表明此时系统受限于物理容量而非调度算法。

### 7. 优点
*   **架构解耦**：巧妙地解决了 AI-RAN 中毫秒级分配与秒级部署之间的矛盾。
*   **引入 Agentic AI**：利用 LLM 的逻辑推理能力处理复杂的部署决策，比传统的启发式规则更具灵活性。
*   **迁移感知**：通过预测评论器量化迁移成本，解决了动态环境中“频繁迁移导致性能下降”的痛点。

### 8. 不足与局限
*   **LLM 开销**：虽然 LLM 运行在慢尺度层，但其本身的推理延迟和显存占用在边缘节点上可能仍然过高，论文未深入讨论 LLM 代理本身的资源消耗。
*   **实时性风险**：如果网络环境变化极快，5 秒一次的慢尺度调整可能仍显滞后。
*   **泛化性验证**：实验主要基于模拟环境，尚未在真实的 AI-RAN 物理测试床上验证（如 NVIDIA Aerial 平台）。

（完）
