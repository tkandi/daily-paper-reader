---
title: "LBI: Parallel Scan Backpropagation via Latent Bounded Interfaces"
authors: "Shaun Christopher Lee, Sangeetha Abdu Jyothi"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.09204v1"
tags: ["query:mlsys"]
score: 8.5
evidence: 并行扫描反向传播以解决并行训练瓶颈
tldr: 本研究提出LBI算法，通过将层间通信限制在低维潜空间，解决了反向传播在深度上的串行瓶颈。该方法将并行扫描的计算复杂度从O(d^3)降至O(r^3)，在保持Mamba和Transformer等模型性能的同时，显著降低了跨设备通信开销，为大规模并行训练提供了高效的算法基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09204v1/fig-001.webp\", \"caption\": \"Figure 3: Spectral norm of interface Jacobians throughout Mamba-3 SISO LBI training at ranks r ∈ {16, 32, 64} with region size 2. Left: mean local spectral norm ∥Jk∥2 averaged across regions. Right: mean spectral norm of the suffix-composed interface Jacobian ∥Pk∥2 = ∥ ∏K−1\", \"page\": 18, \"index\": 1, \"width\": 813, \"height\": 284}, {\"url\": \"assets/figures/arxiv/2605.09204v1/fig-002.webp\", \"caption\": \"Figure 1: Smoothed training CE (solid lines) and periodic online validation CE (markers) on FineWebEdu across four model backends, averaged over three seeds. Training curves are smoothed with an exponential moving average for readability. Shaded regions indicate ±1 standard deviation across seeds for validation loss. Online validation uses a small held-out budget per evaluation, final quantitative comparisons are reported using post-hoc evaluation on the full held-out sample (Table 2).\", \"page\": 8, \"index\": 2, \"width\": 709, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2605.09204v1/fig-003.webp\", \"caption\": \"Table 2: Final validation CE across architectures and interface ranks. Rank r = – denotes the dense baseline (no interface), while finite r corresponds to bounded-interface (LBI) models. Backend N counts block parameters only and excludes token embeddings and output parameters; Interface N counts additional bounded-interface parameters; Total N counts all trainable parameters. All models reported are trained up to a fixed 20.48M tokens (Figure 1) to enable controlled comparisons of relative behavior. Reported CE are post-hoc evaluations of final checkpoints, using the same fixed held-out FineWeb-Edu sample of 524,288 tokens, with mean ± standard deviation across three seeds.\", \"page\": 8, \"index\": 3, \"width\": 660, \"height\": 362}, {\"url\": \"assets/figures/arxiv/2605.09204v1/fig-004.webp\", \"caption\": \"Table 6: Post-hoc validation CE across region sizes for Mamba-3 SISO and Transformer at interface rank r = 32 (same evaluation procedure as Table 2). Region size denotes the number of consecutive layers per region, yielding K = ⌈# layers/region size⌉ regions. When the division is uneven, the final region is truncated in the number of layers. Backend N is fixed within each architecture, and the interface N varies with region size due to the per-region encoder/decoder overhead. Entries report mean ± standard deviation across three seeds.\", \"page\": 17, \"index\": 4, \"width\": 823, \"height\": 210}, {\"url\": \"assets/figures/arxiv/2605.09204v1/fig-005.webp\", \"caption\": \"Figure 2: Smoothed training CE (solid lines) and periodic online validation CE (markers) on FineWebEdu for Mamba-3 SISO (left) and Transformer (right) at interface rank r = 32, varying the number of layers per region from 1 to 4. Training curves are smoothed with an exponential moving average for readability. Shaded regions indicate ±1 standard deviation across three seeds for validation loss. Final quantitative comparisons are reported in Table 6.\", \"page\": 17, \"index\": 5, \"width\": 783, \"height\": 261}]"
motivation: 传统反向传播的串行依赖限制了训练速度，而现有并行方案因高维雅可比矩阵计算过于昂贵。
method: 引入低维潜接口限制区域间通信，将伴随递归简化为对小规模矩阵的后缀扫描。
result: 实验表明16维接口即可保证模型精度，并大幅减少跨设备通信数据量。
conclusion: LBI为区域并行训练提供了可行方案，有效平衡了计算效率与模型表达能力。
---

## Abstract
Backpropagation is inherently sequential across depth, creating an $O(K)$-deep dependency chain that bottlenecks parallel training. While parallel-scan formulations theoretically reduce this depth to $O(\log K)$, they are computationally prohibitive for modern architectures due to the $O(d^3)$ cost of composing full-rank $d\times d$ Jacobians over the entire hidden state. We introduce Latent Bounded Interfaces (LBI), an algorithmic formulation that makes scan-based backpropagation tractable by restricting inter-region communication to a low-dimensional latent interface, $ m_k \in \mathbb{R}^{r}$, where $r \ll d$. This reduces the adjoint recursion to a suffix scan over $r \times r$ Jacobians, cutting per-combine cost from $O(d^3)$ to $O(r^3)$ while preserving exact gradients under the bounded-interface model. We demonstrate that LBI maintains model quality across four architectures (Mamba-2, Mamba-3, Transformer, and a Mamba--Transformer hybrid) at 47--61M block parameters. Interfaces of dimension $r=16$ suffice to preserve training quality within 0.16--0.35 cross entropy of dense baselines. The resulting framework provides an algorithmic foundation for region-parallel training, reducing cross-device backward communication to a single scan over $K$ fixed-size matrices, of approximately 56 KB for our experimental configurations.