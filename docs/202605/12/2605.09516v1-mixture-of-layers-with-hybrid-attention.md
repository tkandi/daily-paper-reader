---
title: Mixture of Layers with Hybrid Attention
title_zh: 具有混合注意力的混合层
authors: "Ivan Ternovtsii, Yurii Bilak"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09516v1"
tags: ["query:mlsys"]
score: 6.5
evidence: 用并行块取代单体层的新型Transformer架构
tldr: 本研究针对标准MoE模型层结构单体化的问题，提出了“层混合”（MoL）架构。MoL将传统的高维Transformer块替换为多个并行的低维薄块，并利用学习到的投影和top-k路由进行组合。为了解决稀疏路由导致的注意力覆盖范围缩小问题，研究引入了混合注意力机制，将一个共享的Softmax全局上下文块与路由块中的Gated DeltaNet线性注意力相结合，在降低计算开销的同时保持了强大的全局建模能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09516v1/fig-001.webp\", \"caption\": \"Table 6: Zero-shot accuracy (%, lm-eval-harness 0.4.7, bf16; acc_norm where benchmark prefers it; MMLU 5-shot, others 0-shot). Best per row in bold; MMLU rows are within noise of chance (∼25%) so we omit bolding there. Stderr per task is 0.005–0.014.\", \"page\": 7, \"index\": 1, \"width\": 740, \"height\": 192}, {\"url\": \"assets/figures/arxiv/2605.09516v1/fig-002.webp\", \"caption\": \"Table 14: Zero-shot accuracy with stderr (%). Stderr from lm-eval-harness 1000-iter bootstrap.\", \"page\": 14, \"index\": 2, \"width\": 698, \"height\": 297}, {\"url\": \"assets/figures/arxiv/2605.09516v1/fig-003.webp\", \"caption\": \"Table 12: Decode latency per token (ms, batch=1, RTX 3090, KV cache enabled). Dense scales linearly; MoL stays flat at ∼60ms, dominated by a per-block Python dispatch floor that masks the smaller lineargrowth term from MoL’s single shared softmax block.\", \"page\": 13, \"index\": 3, \"width\": 984, \"height\": 355}, {\"url\": \"assets/figures/arxiv/2605.09516v1/fig-004.webp\", \"caption\": \"Figure 1: Validation perplexity (log scale) vs training step for the three 1.3B-scale runs on FineWeb-Edu 20B tokens. Dense Softmax 1.3B (blue) leads from step 1; MoL Hybrid (red) stays ∼3 PPL above through training; Dense Softmax 0.7B (green) starts above MoL and crosses below at step 7K, then narrows the gap from −3.5 PPL to −0.49 PPL by step 20K. The Dense 1.3B vs MoL gap stabilizes at step 11K and does not close within budget.\", \"page\": 8, \"index\": 4, \"width\": 708, \"height\": 398}, {\"url\": \"assets/figures/arxiv/2605.09516v1/fig-005.webp\", \"caption\": \"Table 7: Prefill throughput (tokens/s, batch=1, single GPU; 5-run mean per cell after warmup, run-to-run variance <2% at fixed T on each GPU). MoL Hybrid value is the faster of batched_sparse and sparse at each T (both shown where measured; see protocol note). Best per row in bold marks the crossover between Dense and MoL Hybrid. The four measured GPUs do not show a clean monotonic shift in crossover position with HBM bandwidth or compute; we present empirical positions only.\", \"page\": 9, \"index\": 5, \"width\": 1050, \"height\": 495}, {\"url\": \"assets/figures/arxiv/2605.09516v1/fig-006.webp\", \"caption\": \"Table 2: Hybrid attention on WikiText-103. S=shared (all tokens), R=routed (sparse). †=1 seed; ±=3 seeds.\", \"page\": 5, \"index\": 6, \"width\": 682, \"height\": 300}]"
motivation: 传统的MoE模型仅在层内路由专家，而层结构本身仍是单体式的，限制了模型架构的稀疏化潜力。
method: 将全宽Transformer块替换为多个低维并行薄块，通过top-k路由进行动态组合，并引入混合注意力机制。
result: 通过结合共享的Softmax全局块和路由块中的线性注意力，有效解决了稀疏路由带来的注意力覆盖不足问题。
conclusion: MoL架构通过层级稀疏化和混合注意力设计，为构建高效且具备全局感知能力的Transformer模型提供了新方案。
---

## 摘要
标准的混合专家（MoE）Transformer 将 token 路由到每一层内的专家子网络，但层结构本身仍然是单体式的。我们引入了混合层（MoL），它将全宽 Transformer 块（d_model）替换为 K 个低维度的并行薄块（d_thin << d_model），这些块通过学习到的下/上投影连接，并通过 top-k 块路由进行组合。将稀疏块路由扩展到多个块会产生注意力覆盖问题，因为每个块看到的 token 较少。我们通过引入混合注意力来解决这个问题，它将一个用于全局上下文的共享 softmax 块与路由块中的 Gated DeltaNet 线性注意力相结合。

## Abstract
Standard Mixture-of-Experts (MoE) transformers route tokens to expert subnetworks within each layer, but the layer structure itself remains monolithic. We introduce Mixture of Layers (MoL), which replaces full-width transformer blocks (d_model) with K parallel thin blocks at reduced dimensionality (d_thin << d_model), connected via learned down/up projections and composed via top-k block routing. Scaling sparse block routing to many blocks creates an attention coverage problem, as each block sees fewer tokens. We address this by introducing hybrid attention, which pairs one shared softmax block for global context with Gated DeltaNet linear attention in routed blocks.