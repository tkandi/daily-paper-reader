---
title: "ChunkFlow: Communication-Aware Chunked Prefetching for Layerwise Offloading in Distributed Diffusion Transformer Inference"
title_zh: ChunkFlow：分布式扩散 Transformer 推理中面向逐层卸载的通信感知块状预取
authors: "Han Meng, Danny Willow Liu, Dong Li"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.11335v1"
tags: ["query:mlsys"]
score: 9.5
evidence: 分布式推理中预取与通信的协同调度
tldr: 针对分布式扩散Transformer（DiT）推理中层级卸载导致的PCIe带宽竞争问题，本文提出了ChunkFlow。它通过一阶分析模型预测预取延迟，并设计了通信感知的块粒度预取机制，在PCIe受限环境下协调预取与集体通信。实验表明，ChunkFlow在H100集群上相比SGLang实现了1.28倍加速，并显著降低了显存占用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11335v1/fig-001.webp\", \"caption\": \"Figure 1: PCIe contention on PCIe-based nodes during distributed DiT inference. (a) H2D prefetch and inter-GPU collectives converge at each GPU’s PCIe receive port (Rx) and contend for bandwidth. (b) Denoising step latency breakdown: the contention stall (21%) accounts for a large portion.\", \"page\": 2, \"index\": 1, \"width\": 820, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2605.11335v1/fig-002.webp\", \"caption\": \"Figure 3: Denoising step time (top) and GPU peak memory (bottom) across frame and batch sizes\", \"page\": 7, \"index\": 2, \"width\": 821, \"height\": 410}, {\"url\": \"assets/figures/arxiv/2605.11335v1/fig-003.webp\", \"caption\": \"Figure 2: Block-level timing under (a) no offload, (b) whole-layer layerwise offload, and (c) our communication-aware chunked prefetch, illustrated on a DiT block with Ulysses sequence parallelism. Substituting the compute and prefetch models above, the overlap condition can be written directly in terms of the block-level compute workload Fblock.\", \"page\": 5, \"index\": 3, \"width\": 821, \"height\": 336}]"
motivation: 在分布式DiT推理中，层级预取与GPU间集体通信在PCIe链路上存在带宽竞争，导致预取延迟无法被计算掩盖。
method: 提出ChunkFlow运行时，利用分析模型指导块粒度预取，使其能主动避让集体通信并灵活调节显存与预取量的平衡。
result: "在双H100 PCIe环境下，ChunkFlow比SGLang层级卸载快1.28倍，且在保持性能的同时减少了高达49%的峰值显存。"
conclusion: ChunkFlow通过协同调度预取与通信，有效解决了分布式推理中的带宽瓶颈，实现了显存节省与推理速度的优化平衡。
---

## 摘要
逐层卸载（Layerwise offloading）通过从主机内存预取即将执行的层来减少大型扩散 Transformer (DiT) 推理的 GPU 显存占用，但其有效性取决于能否将预取延迟隐藏在每层计算之后。当单 GPU 计算负载较小时，这一假设将不再成立。此外，在仅支持 PCIe 的节点上，预取与 GPU 间的集合通信（如 all-reduce 和 all-to-all）会在共享的 PCIe 路径上产生竞争，导致即使计算本可以隐藏延迟，预取延迟也会暴露出来。我们将逐层卸载重新定义为预取与通信之间的协同调度问题，并由一个预测预取何时能被计算隐藏的一阶分析模型指导。基于该模型，我们设计了 ChunkFlow，这是一个通信感知的、块粒度的卸载运行时，它能自适应地让位于集合通信，并平滑地在 GPU 显存与预取量之间进行权衡。在两块 H100 GPU 上通过 PCIe 运行 Ulysses 序列并行的三个代表性扩散 Transformer 实验表明，ChunkFlow 相比 SGLang 现有的逐层卸载实现了高达 1.28 倍的单步时间加速；在负载足够大时，相比不卸载的基准线，在单步时间几乎相同的情况下减少了高达 49% 的峰值 GPU 显存；并提供了一种可调的显存-延迟权衡，在小负载场景下实现了近乎零的单步时间开销。

## Abstract
Layerwise offloading reduces the GPU memory footprint of large diffusion transformer (DiT) inference by prefetching upcoming layers from host memory, but its effectiveness hinges on hiding prefetch latency behind per-layer computation. This assumption breaks down when the per-GPU compute workload is small. Moreover, on PCIe-only nodes, prefetch and inter-GPU collective communications such as all-reduce and all-to-all contend on the shared PCIe path, exposing prefetch latency even when compute would otherwise hide it. We revisit layerwise offloading as a co-scheduling problem between prefetch and communication, guided by a first-order analytical model that predicts when prefetch can be hidden by computation. Building on this model, we design ChunkFlow, a communication-aware, chunk-granular offloading runtime that adaptively yields to collective communication and smoothly trades GPU memory for prefetch volume. On three representative diffusion transformers running on two H100 GPUs over PCIe with Ulysses sequence parallelism, ChunkFlow delivers up to 1.28x step-time speedup over SGLang's existing layerwise offloading, reduces peak GPU memory by up to 49% over the no-offload baseline at near-identical step time once the workload is large enough, and exposes a tunable memory-latency tradeoff that recovers near-zero step-time overhead in the small-workload regime.

---

## 论文详细总结（自动生成）

以下是对论文《ChunkFlow: Communication-Aware Chunked Prefetching for Layerwise Offloading in Distributed Diffusion Transformer Inference》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着扩散 Transformer（DiT）模型参数量和处理序列长度（视频/高分辨率图像）的激增，GPU 显存成为推理的主要瓶颈。现有的**逐层卸载（Layerwise Offloading）**技术虽然能减少显存占用，但在分布式推理场景下存在两个致命缺陷：
*   **计算掩盖失效**：当单 GPU 的计算负载较小时，计算时间不足以掩盖从主机内存预取权重的时间。
*   **PCIe 带宽竞争**：在仅支持 PCIe 的多 GPU 节点上，主从机间的权重预取（H2D）与 GPU 间的集体通信（如 All-to-All）共享同一 PCIe 路径，导致严重的带宽争用，使预取延迟暴露在关键路径上，推理速度大幅下降。

### 2. 方法论：核心思想与关键技术
论文提出了 **ChunkFlow** 运行时系统，核心包含三个部分：
*   **一阶分析模型**：定义了“临界计算负载 $F^*$”。通过建模计算时间（基于峰值算力和利用率）与预取时间（基于 H2D 带宽和权重大小），预测预取能否被完全掩盖。当实际计算量 $F_{block} < F^*$ 时，预取延迟会暴露。
*   **通信感知块状预取（Communication-Aware Chunked Prefetching）**：
    *   将每一层的权重划分为固定大小的**块（Chunks）**。
    *   引入**暂停/恢复机制**：在计算流发起集体通信前设置标志位，预取流在块边界检查该标志。若有通信需求，预取流会主动让出 PCIe 带宽，待通信完成后再恢复。这解决了 PCIe 上的竞争问题。
*   **块粒度部分参数驻留（Partial Parameter Residency）**：
    *   针对计算量极小、无法掩盖预取的场景，允许在 GPU 显存中保留一定比例（如 20%-60%）的权重块，仅预取剩余部分。
    *   这提供了一个精细可调的“显存-延迟”权衡曲线。

### 3. 实验设计
*   **模型场景**：选取了三个代表性的开源 DiT 模型：**WanVideo (5B)**、**HunyuanVideo (13B)** 和 **FLUX.1-dev (12B)**。
*   **对比基准（Benchmark）**：
    1.  **No Offload**：权重全部驻留 GPU（代表性能上限，但显存需求最高）。
    2.  **Layerwise Offload**：SGLang 现有的逐层卸载机制（全层预取，无通信感知）。
*   **实验变量**：改变视频帧数（影响序列长度 $S$）、批处理大小（Batch Size $B$）、块大小（Chunk Size $C$）以及参数驻留比例。

### 4. 资源与算力
*   **硬件环境**：单节点配置 **2 块 NVIDIA H100 PCIe GPU**。
*   **互连方式**：通过 PCIe Gen5 x16 连接（非 NVLink），这是云端和本地廉价多 GPU 节点的典型配置。
*   **软件框架**：基于 SGLang 构建，使用 Ulysses 序列并行（Sequence Parallelism）。
*   **训练/推理时长**：文中未提及训练，推理实验针对 10 个去噪步进行测量。

### 5. 实验数量与充分性
*   **实验规模**：针对三个模型分别进行了帧数/批大小的扩展性实验、延迟分解实验、块大小消融实验以及显存-延迟权衡实验。
*   **充分性**：实验涵盖了从计算受限到带宽受限的多个区间，验证了分析模型的准确性。通过对延迟成分的细粒度拆解（计算、通信、预取停顿、运行时开销），客观地证明了性能提升的来源。

### 6 主要结论与发现
*   **性能提升**：ChunkFlow 相比 SGLang 的逐层卸载，在 WanVideo 和 HunyuanVideo 上分别实现了高达 **1.26x 和 1.28x** 的加速。
*   **显存节省**：在负载足够大且性能接近“不卸载”基准时，ChunkFlow 可减少 **30% 至 49%** 的峰值显存。
*   **模型验证**：实验结果高度符合一阶分析模型预测的临界点 $F^*$。
*   **权衡能力**：通过部分参数驻留，即使在极小负载下，只需保留 60% 的参数即可消除几乎所有预取开销，同时仍比不卸载方案节省约 7%-15% 的显存。

### 7. 优点
*   **针对性强**：敏锐捕捉到了 PCIe 节点上分布式通信与预取竞争这一被忽视的瓶颈。
*   **理论指导实践**：提出的 $F^*$ 模型简单有效，为系统优化提供了清晰的边界。
*   **粒度精细**：块状管理比传统的层级管理更灵活，支持更平滑的性能调优。
*   **低开销**：ChunkFlow 的运行时同步开销极低（小于总步时的 2%）。

### 8. 不足与局限
*   **硬件局限**：主要针对 PCIe 互连节点。在拥有高带宽 NVLink 的系统上，通信与 H2D 预取路径分离，该方案的边际收益会减小。
*   **应用范围**：附录中提到该方法在 LLM（大语言模型）推理（尤其是 Decode 阶段）中效果有限，因为 LLM 的计算强度远低于 DiT，且受 KV Cache 卸载的干扰更严重。
*   **自动化程度**：目前块大小和驻留比例可能仍需要根据具体硬件手动微调，缺乏完全自动化的动态配置机制。

（完）
