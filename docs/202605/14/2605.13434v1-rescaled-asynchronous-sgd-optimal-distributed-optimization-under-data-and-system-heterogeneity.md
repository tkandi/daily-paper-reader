---
title: "Rescaled Asynchronous SGD: Optimal Distributed Optimization under Data and System Heterogeneity"
title_zh: 重缩放异步随机梯度下降：数据与系统异构下的最优分布式优化
authors: "Ammar Mahran, Artavazd Maranjyan, Peter Richtárik"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13434v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 系统异构下分布式优化的异步SGD算法
tldr: 针对分布式学习中数据和系统异构性导致的异步随机梯度下降（ASGD）偏差问题，本文提出了Rescaled ASGD。该方法通过根据计算时间缩放各节点的步长，确保每个节点在周期内贡献相同的聚合学习率，从而消除对快速节点的偏好。理论证明其在非凸设置下收敛至全局最优，且时间复杂度达到下界，实验验证了其有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.13434v1/fig-001.webp\", \"caption\": \"Table 1: Comparison of worst-case wall-clock time complexities for parallel stochastic first-order methods in the fixed-computation time model with data and system heterogeneity to achieve εstationarity in (1). We denote the arithmetic mean and maximum, respectively, of the workers’ computation times by τA, τmax. Problem parameters include the initial optimality gap ∆ := F (x0)− F ∗ and global smoothness constant L (Assumption 2.3), the target stationarity ε > 0, and a bound on the stochastic gradient variance σ2 (Assumption 2.2). Asymptotic Optimality: time complexity matches the lower bound established by Tyurin and Richtárik [2023] in the leading term and achieves ε-stationarity for arbitrarily small ε. No Idle Workers: all workers remain busy and computational resources are fully utilized. No Memory Overhead: no gradients or model iterates need to be stored for later use.\", \"page\": 2, \"index\": 1, \"width\": 822, \"height\": 500}, {\"url\": \"assets/figures/arxiv/2605.13434v1/fig-002.webp\", \"caption\": \"Figure 1: Solid lines denote the median loss across five random seeds, with shaded regions indicating the minimum and maximum. Malenia SGD and Ringleader ASGD are slowed down under fluctuating computation times as the gradient gathering phase takes longer. Rescaled ASGD shows virtually identical performance in both settings.\", \"page\": 8, \"index\": 2, \"width\": 814, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2605.13434v1/fig-003.webp\", \"caption\": \"Figure 4: Delay-Adaptive ASGD is heavily biased towards the local objective of the faster worker. Rescaled ASGD converges to a small neighborhood around the minimizer of the equal-weighted average.\", \"page\": 32, \"index\": 3, \"width\": 633, \"height\": 434}, {\"url\": \"assets/figures/arxiv/2605.13434v1/fig-004.webp\", \"caption\": \"Figure 2: Cumulative stepsize taken over wall-clock time. The gathering phases dilate under fluctuating computation times, causing Malenia SGD and Ringleader ASGD to stall in the search space. Rescaled ASGD takes steps at an unimpeded rate of α/τmax.\", \"page\": 9, \"index\": 4, \"width\": 814, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2605.13434v1/fig-005.webp\", \"caption\": \"Figure 3: Rescaled ASGD targets the equal-weighted average F , Vanilla ASGD the frequencyweighted average F̃ .\", \"page\": 31, \"index\": 5, \"width\": 809, \"height\": 314}]"
motivation: 传统的异步SGD在数据异构时会偏向计算速度快的节点，导致模型无法收敛到正确的全局目标。
method: 提出Rescaled ASGD，通过将各节点的步长与其计算时间成比例缩放，使每个节点对全局更新的贡献保持均衡。
result: 理论证明该方法在非凸环境下收敛于正确目标的驻点，且其时间复杂度的领先项达到了已知下界。
conclusion: Rescaled ASGD无需复杂的缓冲或额外内存，仅通过步长调整即可在异构环境下实现最优的分布式优化。
---

## 摘要
异步随机梯度下降 (ASGD) 是分布式学习中利用异构计算资源的标准方法：服务器在梯度到达时立即更新模型，而不是强制快速节点等待慢速节点。原始 ASGD 以相同的权重应用每个到达的梯度。当本地数据分布异构时，这会产生问题：计算速度较快的节点贡献了更多的更新，我们在理论上证明了该方法会偏向于本地目标的频率加权平均值，而非期望的全局目标。现有的补救措施通常通过引入收集阶段、缓冲或额外内存来脱离简单的 ASGD 模板。我们证明这是不必要的。在保留标准 ASGD 机制的同时，我们通过按计算时间比例重缩放特定节点的步长来恢复正确的目标，从而使每个节点在一个周期内贡献相同的聚合学习率。在非凸设置下，在平滑性和有界异构性假设下，我们证明了所提出的方法 Rescaled ASGD 在固定计算模型中收敛到正确全局目标的驻点。其时间复杂度的首项与已知的下界相匹配，而陈旧性和数据异构性的影响仅出现在低阶项中。实验证实该方法收敛于正确的目标，并且与最先进的基准方法相比具有竞争力。

## Abstract
Asynchronous stochastic gradient descent (ASGD) is a standard way to exploit heterogeneous compute resources in distributed learning: instead of forcing fast workers to wait for slow ones, the server updates the model whenever a gradient arrives. Vanilla ASGD applies each arriving gradient with the same weight. When local data distributions are heterogeneous, this becomes problematic: faster workers contribute more updates, and we show theoretically that the method is biased toward a frequency-weighted average of the local objectives rather than the desired global objective. Existing remedies typically move away from the simple ASGD template by introducing gathering phases, buffering, or extra memory. We show that this is unnecessary. Keeping the standard ASGD mechanism, we recover the correct objective by rescaling worker-specific stepsizes in proportion to their computation times, so that each worker contributes the same aggregate learning rate over a cycle. In the non-convex setting, under smoothness and bounded heterogeneity assumptions, we prove that the resulting method, Rescaled ASGD, converges to stationary points of the correct global objective in the fixed-computation model. Its time complexity matches the known lower bound in the leading term, while the effects of staleness and data heterogeneity appear only in lower-order terms. Experiments confirm that the method converges to the correct objective and is competitive with state-of-the-art baselines.

---

## 论文详细总结（自动生成）

这篇论文由沙特阿卜杜拉国王科技大学（KAUST）的 Ammar Mahran、Artavazd Maranjyan 和 Peter Richtárik 撰写，探讨了在数据和系统双重异构环境下的分布式优化问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在分布式机器学习中，**异步随机梯度下降 (ASGD)** 是利用异构计算资源（即不同节点计算速度不同）的标准手段。然而，当各节点的数据分布不一致（数据异构）时，传统的 ASGD 会产生**目标不一致性 (Objective Inconsistency)** 问题：
*   **偏差风险**：计算速度快的节点（Fast Workers）会比慢速节点贡献更多的梯度更新。
*   **结果偏移**：这导致模型最终收敛到的是一个受更新频率加权的局部目标函数平均值，而不是预期的全局目标函数。
*   **现有方案的缺陷**：现有的修正方法通常需要引入同步等待（Gathering phases）、缓冲区（Buffering）或额外的内存开销，破坏了 ASGD 的简洁性和高效性。

### 2. 论文提出的方法论：Rescaled ASGD
论文的核心思想是：**无需改变 ASGD 的异步机制，仅通过调整各节点的步长（学习率）来抵消频率偏差。**

*   **核心思想**：将每个节点的步长 $\gamma_i$ 与其计算时间 $\tau_i$ 成比例缩放。如果节点 $i$ 的计算速度是节点 $j$ 的两倍，则将其步长减半。
*   **关键技术细节**：
    *   **步长重缩放公式**：$\gamma_i = \gamma \cdot \tau_i \cdot \frac{\tau_H}{n \tau_{max}}$，其中 $\tau_H$ 是计算时间的调和平均值。
    *   **周期性视角**：在理论分析中引入“周期（Cycle）”概念。在一个周期内，尽管快速节点更新次数多，但由于步长小，其对模型总位移的贡献与慢速节点（更新次数少但步长大）保持一致。
    *   **算法流程**：服务器维护全局模型，节点计算完梯度后立即发送给服务器；服务器根据发送节点的身份应用对应的重缩放步长更新模型，并立即将新模型发回该节点。
*   **理论保证**：在非凸设置下，证明了该方法能收敛至全局目标的驻点，且其时间复杂度的领先项达到了理论下界 $O(\frac{\Delta L \sigma^2}{n \epsilon^2} \tau_A)$，其中 $\tau_A$ 是计算时间的算术平均值。

### 3. 实验设计
*   **数据集**：MNIST 手写数字识别。
*   **场景设置**：
    *   **极端数据异构**：将 10 个类别的图片分别分配给 10 个节点（每个节点仅拥有一种标签的数据）。
    *   **系统异构**：设置不同的计算时间 $\tau_i \in \{1, 2, 4, 8, 16\}$。
    *   **时间模型**：对比了“固定计算时间”和“随机波动计算时间（指数分布）”两种情况。
*   **Benchmark 与对比方法**：
    *   **Malenia SGD**：具有异步收集阶段但同步更新的方法。
    *   **Ringleader ASGD**：使用梯度表（Memory-heavy）的异步方法。
    *   **Vanilla ASGD**：不进行步长缩放的标准异步 SGD。
    *   **Delay-Adaptive ASGD**：根据延迟调整步长的方法。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或数量。
*   **训练细节**：实验是在模拟环境下进行的，通过“模拟墙钟时间（Simulated Wall-Clock Time）”来衡量效率。实验设置了 30,000 个时间单位的预算，Batch Size 为 64。

### 5. 实验数量与充分性
*   **实验规模**：
    *   主实验对比了三种核心算法在固定和波动时间下的收敛曲线。
    *   提供了消融实验和概念验证实验（如验证 Vanilla ASGD 确实收敛到错误的目标）。
    *   每个实验运行了 5 个不同的随机种子并展示了中位数及最值区间。
*   **充分性评价**：对于一篇理论驱动的优化论文，其实验在验证“目标一致性”和“时间复杂度最优性”方面是**充分且客观**的。虽然没有在大规模 LLM 或 ImageNet 上测试，但其针对异构性问题的验证逻辑非常严密。

### 6. 论文的主要结论与发现
*   **消除偏差**：Rescaled ASGD 成功解决了数据异构下的目标偏移问题，能够收敛到正确的全局最优解。
*   **性能优越**：在计算时间波动的真实场景下，Rescaled ASGD 优于 Malenia 和 Ringleader，因为它没有同步等待环节，不会被“掉队者（Stragglers）”拖累。
*   **Vanilla ASGD 的本质**：证明了原始 ASGD 实际上是在优化一个以节点更新频率为权重的代理函数。
*   **复杂度达标**：理论和实验均证明其时间复杂度在领先项上是帕累托最优的。

### 7. 优点
*   **极简性**：不需要额外的内存（如梯度表）或复杂的逻辑，仅需一行步长调整代码即可实现。
*   **无等待**：完全异步，最大化了硬件利用率，没有任何节点闲置。
*   **理论完备**：不仅给出了收敛性证明，还给出了匹配下界的时间复杂度分析。

### 8. 不足与局限
*   **假设限制**：理论分析依赖于“谐波周期（Harmonic Periods）”假设（即计算时间互为倍数），虽然实验表明在非谐波情况下依然稳健，但理论上仍有局限。
*   **异构性度量**：依赖于有界异构性假设（Assumption 2.5），如果节点间数据差异极端巨大且梯度范数不受控，算法稳定性可能受影响。
*   **超参数敏感性**：虽然消除了频率偏差，但仍需针对全局步长 $\gamma$ 进行调优，且不同节点的步长差异可能导致某些节点更新步子过大，影响初期稳定性。

（完）
