---
title: Unleashing Scalable Context Parallelism for Foundation Models Pre-Training via FCP
title_zh: 通过 FCP 释放基础模型预训练的可扩展上下文并行能力
authors: "Yilong Zhao, Xiaonan Nie, Kan Zhu, Shuang Ma, Zhichao Lai, Hongxiang Hao, Yang Zhou, Baris Kasikci, Ion Stoica"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08524v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 基础模型预训练的可扩展上下文并行
tldr: 针对基础模型预训练中长短序列混合导致的计算效率低下和负载不均问题，本文提出了一种灵活的上下文并行范式FCP。FCP摒弃了传统的环形通信拓扑，采用块级粒度的序列分片和点对点通信，并结合装箱算法优化任务分配。实验表明，FCP在多达256个GPU上表现出优异的线性扩展性，显著提升了注意力机制的计算利用率，为大规模长文本训练提供了高效支持。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08524v1/fig-001.webp\", \"caption\": \"Figure 6. FCP System Overview.\", \"page\": 6, \"index\": 1, \"width\": 860, \"height\": 340}, {\"url\": \"assets/figures/arxiv/2605.08524v1/fig-002.webp\", \"caption\": \"Figure 13. Sensitivity test of per-GPU tokens on 128× GPU-X.\", \"page\": 10, \"index\": 2, \"width\": 479, \"height\": 282}, {\"url\": \"assets/figures/arxiv/2605.08524v1/fig-003.webp\", \"caption\": \"Figure 11. Weak-scaling of module-level attention MFU on realworld dataset. The number of tokens per GPU is fixed at 32K.\", \"page\": 10, \"index\": 3, \"width\": 479, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2605.08524v1/fig-004.webp\", \"caption\": \"Figure 8. Example of the congestion-free solver over three sequences with causal mask. Given the block assignments from block distributor, communication planner constructs a bipartite graph based on the data dependency across GPUs. For example, 1-st block from sequence B are transferred from GPU 2 to GPU 0 and 1, adding edges 2 → 1 and 2 → 0. The solver then calculates the maximal matching of the bipartite graph with minimum iterations, providing an optimal congestion-free communication ordering.\", \"page\": 8, \"index\": 4, \"width\": 901, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2605.08524v1/fig-005.webp\", \"caption\": \"Figure 15. (a) Trace distribution of the lognormal distribution and (b) weak-scaling of module-level attention MFU. The number of tokens per GPU is fixed at 32K.\", \"page\": 16, \"index\": 5, \"width\": 481, \"height\": 560}, {\"url\": \"assets/figures/arxiv/2605.08524v1/fig-006.webp\", \"caption\": \"Figure 16. (a) Trace distribution of the bimodal distribution and (b) weak-scaling of module-level attention MFU. The number of tokens per GPU is fixed at 32K.\", \"page\": 16, \"index\": 6, \"width\": 484, \"height\": 561}]"
motivation: 现有的上下文并行方法在处理变长序列时存在计算效率低、通信开销大及负载不均衡的问题。
method: 提出FCP范式，通过块级粒度的序列分片、点对点通信及装箱算法实现灵活的任务调度。
result: 在256个GPU上实现了近乎线性的扩展性，并将注意力机制的MFU提升了1.13至2.21倍。
conclusion: FCP通过打破固定拓扑限制，为基础模型的大规模长文本预训练提供了一种高效且可扩展的并行方案。
---

## 摘要
上下文并行（CP）已被广泛采用，以支持基础模型预训练中不断增长的上下文长度。然而，现有的设计无法处理训练数据集中序列长度的巨大差异，导致性能欠佳。这些方法通常会对短序列进行过度分片，导致计算效率低下和过多的通信，或者在没有适当装箱（bin-packing）的情况下分别处理长短序列，从而导致工作负载不平衡。在本文中，我们提出了 FCP，一种灵活的上下文并行范式，它在块级粒度上对序列进行分片和调度。FCP 不依赖于环形（ring）等僵化的通信拓扑，而是支持任意的点对点通信，允许在工作节点之间灵活放置序列块。通过对长短序列的块进行装箱，FCP 实现了高计算效率和平衡的工作负载分布。广泛的评估表明，FCP 在多达 256 个 NVIDIA GPU 上实现了近乎线性的可扩展性，注意力机制的算力利用率（MFU）提升了 1.13 倍至 2.21 倍。

## Abstract
Context parallelism (CP) has been widely adopted to support the growing context length in foundation model pretraining. However, existing designs fail to handle the large variation in sequence length from training datasets, resulting in suboptimal performance. These methods often over-shard short sequences, leading to compute inefficiency and excessive communication, or process long and short sequences separately without proper bin-packing, causing workload imbalance. In this paper, we propose FCP, a flexible context parallelism paradigm that shards and schedules sequences at block-level granularity. Instead of relying on rigid communication topologies such as ring, FCP enables arbitrary peer-to-peer communication, allowing flexible placement of sequence blocks across workers. By bin-packing blocks from both short and long sequences, FCP achieves both high compute efficiency and balanced workload distribution. Extensive evaluations show that FCP attains near-linear scalability on up to 256 NVIDIA GPUs, with 1.13x-2.21x improvement in the attention MFU.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **FCP (Flexible Context Parallelism)** 的新型上下文并行范式，旨在解决基础模型在处理变长序列预训练时的效率与负载均衡难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：随着基础模型上下文长度（如从 4K 扩展到 1M+）的增加，训练数据呈现出极高的**长度异构性**（长尾分布）。
*   **核心问题**：现有的上下文并行（CP）方案存在两大缺陷：
    1.  **计算低效**：如 Ring Attention 将所有序列均匀分片，导致短序列被过度分片，无法饱和 GPU 算力（Tensor Cores），且引入了不必要的通信开销。
    2.  **负载不均**：一些改进方案（如 ByteScale）将长短序列分开处理，但由于注意力计算量随长度呈平方级增长，简单的分组会导致某些 GPU 过载而其他 GPU 闲置。
*   **整体含义**：FCP 试图打破传统 CP 依赖固定环形拓扑的限制，通过灵活的块级调度实现计算效率与负载均衡的兼得。

### 2. 方法论：核心思想与关键技术
FCP 的核心思想是将序列视为**固定大小的块（Blocks）**，并将其视为独立的调度单元。
*   **块分发器 (Block Distributor)**：
    *   **固定块大小**：无论原始序列多长，均切分为固定大小（如 4K tokens）的块，以确保单块计算能饱和硬件。
    *   **装箱算法 (Bin-packing)**：采用改进的**最长处理时间 (LPT) 调度算法**，根据计算量和内存需求，将不同序列的块贪婪地分配给负载最低的 GPU。
*   **通信规划器 (Communication Planner)**：
    *   **二分图建模**：将 GPU 间的点对点（P2P）通信建模为二分图，通过**最大匹配算法**求解无拥塞的通信顺序。
    *   **块级流水线**：将“拉取-计算-推送”过程分解为块粒度，实现计算与通信的高度重叠。
*   **透明重排器 (Transparent Reshuffler)**：
    *   在注意力模块前后自动进行数据重排（Reshuffle），使其能无缝集成到 FSDP、TP 等现有并行框架中，无需修改数据加载或位置编码逻辑。

### 3. 实验设计
*   **数据集/场景**：
    *   **真实分布**：来自工业界内部的预训练追踪数据（呈现对数正态长尾分布，最大长度 512K）。
    *   **合成分布**：包括集中分布和双峰分布（Bimodal），用于验证泛化性。
*   **Benchmark 配置**：基于 Llama-3-70B 模型结构（8 KV heads, 64 QO heads）。
*   **对比方法**：
    *   **Ring Attention**（平衡优化型基准）。
    *   **ByteScale**（效率优化型基准）。
    *   **WLB-LLM**（自适应切换型基准）。
    *   **MagiAttention**（开源的并发研究工作）。

### 4. 资源与算力
*   **GPU 型号**：使用了两种匿名型号，**GPU-X**（对应 Hopper 架构级别）和 **GPU-Y**（对应 Blackwell 架构级别）。
*   **集群规模**：实验在多达 **256 个 GPU** 的集群上进行。
*   **网络环境**：配备了高性能互联网络（如 50 GB/s 的 InfiniBand 或 RoCE）。
*   **训练时长**：文中未明确给出总训练时长，主要关注算力利用率（MFU）和扩展性指标。

### 5. 实验数量与充分性
*   **实验组数**：
    *   **弱扩展性实验**：16 到 256 GPU 的性能曲线。
    *   **消融实验**：逐步验证了流水线、拥塞求解器、合并器和重排器的贡献（Table 2）。
    *   **敏感性分析**：测试了不同块大小（1K-8K）、每 GPU Token 数（16K-80K）以及不同 GPU 架构的影响。
*   **充分性与客观性**：实验设计非常充分，不仅涵盖了真实业务分布，还通过合成分布压力测试了极端情况。对比基准涵盖了目前最前沿的学术和工业界方案，评估指标（MFU、负载不均率）具有高度客观性。

### 6. 主要结论与发现
*   **性能提升**：FCP 在注意力机制的 MFU 上比基准提升了 **1.13x 至 2.21x**。
*   **扩展性**：在 256 个 GPU 规模下实现了**近乎线性**的扩展能力。
*   **负载均衡**：FCP 能将计算和通信的负载不均率控制在 **5% 以内**，而传统方案在长尾分布下可能高达 70%。
*   **硬件饱和度**：通过合理的块大小选择，FCP 在不同架构（Hopper/Blackwell）上均能保持 90% 以上的单机 FlashAttention 效率。

### 7. 优点
*   **灵活性极高**：打破了环形拓扑的“金字塔枷锁”，允许任意 P2P 通信，从根本上解决了异构长度序列的调度难题。
*   **数学严谨性**：利用二分图匹配理论解决了 P2P 通信中的网络拥塞问题，保证了通信效率。
*   **易用性**：透明重排器的设计使得该方法可以作为“插件”集成到现有大模型训练框架中。

### 8. 不足与局限
*   **网络拓扑依赖**：FCP 依赖于全连接或胖树（Fat-tree）等支持高效 P2P 通信的网络拓扑。在 Torus（如 TPU v3）等受限拓扑上，性能可能会受限。
*   **超参数微调**：虽然文中给出了指导原则，但最佳“块大小”仍需根据具体的硬件算力/带宽比进行微调。
*   **实现复杂度**：相比简单的 Ring Attention，FCP 的通信规划和块管理逻辑更为复杂，对底层算子（如 FlashAttention）的定制化要求较高。

（完）
