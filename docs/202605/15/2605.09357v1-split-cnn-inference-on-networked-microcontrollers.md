---
title: Split CNN Inference on Networked Microcontrollers
title_zh: 联网微控制器上的卷积神经网络拆分推理
authors: "Junyu Lu, Shashwath Suresh, Hao Liu, Qi Hong, Qing Wang"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09357v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 联网微控制器的拆分推理系统
tldr: 针对微控制器（MCU）内存受限导致无法运行大型CNN模型的问题，本文提出一种细粒度的分布式推理系统。通过在子层级别（卷积核和神经元维度）对预训练模型进行切分，并将参数和激活值分布到多个联网MCU上协作执行。实验证明该方法能有效降低单机内存峰值，使原本无法运行的模型在MCU集群上成功部署。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09357v1/fig-001.webp\", \"caption\": \"Fig. 2: The overall workflow of the proposed fine-grained split CNN inference across networked MCUs.\", \"page\": 3, \"index\": 1, \"width\": 671, \"height\": 235}]"
motivation: 单个微控制器的RAM资源极度有限，难以处理CNN推理过程中产生的大量中间激活值。
method: 采用子层粒度的切分策略，通过轻量级协调器在多个异构MCU间分配计算任务和内存负载。
result: 在8台MCU上的实验显示，该系统显著降低了单机内存占用，并保持了实用的端到端推理延迟。
conclusion: 细粒度切分推理打破了单机内存瓶颈，为在资源受限的物联网设备上部署复杂深度学习模型提供了可行方案。
---

## 摘要
在微控制器单元（MCU）上运行深度神经网络受到有限内存资源的严重制约。虽然 TinyML 技术减小了模型大小和计算量，但由于推理过程中由中间激活值主导的峰值随机存取存储器（RAM）占用过高，这些技术在实践中往往难以奏效。因此，许多模型在独立 MCU 上仍然无法运行。在这项工作中，我们为联网 MCU 提出了一种细粒度的拆分推理系统，实现了卷积神经网络（CNN）模型在多个设备上的协同推理。我们的核心见解是，打破内存瓶颈需要在子层粒度而非层边界处拆分推理。我们重新解释了预训练模型，以实现基于卷积核和神经元的分区，并将模型参数和中间激活值分布到多个 MCU 上。一个轻量级的资源感知协调器负责在具有异构资源的 MCU 设备之间编排推理过程。我们在真实测试平台上实现了所提系统，并使用代表性 CNN 模型 MobileNetV2 在多达 8 个 MCU 上进行了评估。实验结果表明，原本在单个 MCU 上无法运行的 CNN 模型可以在联网 MCU 上执行，在降低单个 MCU 峰值 RAM 占用的同时，保持了实用的端到端推理延迟。本工作的所有源代码均可在以下网址找到：https://github.com/shashsuresh/split-inference-on-MCUs。

## Abstract
Running deep neural networks on microcontroller units (MCUs) is severely constrained by limited memory resources. While TinyML techniques reduce model size and computation, they often fail in practice due to excessive peak Random Access Memory (RAM) usage during inference, dominated by intermediate activations. As a result, many models remain infeasible on standalone MCUs. In this work, we present a fine-grained split inference system for networked MCUs that enables collaborative inference of Convolutional Neural Networks (CNN) models across multiple devices. Our key insight is that breaking the memory bottleneck requires splitting inference at sub-layer granularity rather than at layer boundaries. We reinterpret pre-trained models to enable kernel-wise and neuron-wise partitioning, and distribute both model parameters and intermediate activations across multiple MCUs. A lightweight, resource-aware coordinator orchestrates the inference across MCU devices with heterogeneous resources. We implement the proposed system on a real testbed and evaluate it on up to 8 MCUs using MobileNetV2, a representative CNN model. Our experimental results show that CNN models infeasible on a single MCU can be executed across networked MCUs, reducing the per-MCU peak RAM usage while maintaining the practical end-to-end inference latency. All the source code of this work can be found here: https://github.com/shashsuresh/split-inference-on-MCUs.

---

## 论文详细总结（自动生成）

这是一份关于论文《Split CNN Inference on Networked Microcontrollers》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在微控制器（MCU）上部署深度学习模型（TinyML）时，主要的瓶颈并非 Flash 存储空间，而是**峰值 RAM 占用**。中间激活值和临时缓冲区往往超过了单个 MCU 仅有的几十到几百 KB 的内存限制，导致即使经过量化和剪枝的模型也无法运行。
*   **研究背景**：现有的拆分推理（Split Inference）多基于“层”粒度，但在 MCU 上，单层激活值就可能撑爆内存。本文提出利用廉价且联网的 MCU 集群，通过**细粒度（子层级）拆分**来打破单机内存瓶颈。

### 2. 方法论
该系统由一个**协调器（Coordinator）**和多个**工作节点（Workers/MCUs）**组成，核心技术包括：
*   **模型重解释（Model Reinterpretation）**：在离线阶段分析预训练模型，提取神经元级别的依赖关系、感受野和张量维度，为细粒度切分打下基础。
*   **细粒度切分策略**：
    *   **卷积层**：按卷积核（Kernel-wise）切分输出特征图，每个 MCU 只负责计算部分输出通道或空间区域。
    *   **全连接层**：按列（Column-wise）切分权重矩阵，每个 MCU 独立计算部分输出神经元。
*   **跨层激活映射**：协调器维护一个映射表，精确控制层 $i$ 的哪些输出需要路由到层 $i+1$ 的哪些工作节点，避免无效数据传输。
*   **资源感知的工作负载分配**：
    *   提出一个**评分机制（Rating-based Allocation）**，综合考虑 MCU 的时钟频率（计算力）、网络带宽和通信延迟。
    *   公式化建模执行时间，根据评分比例分配权重碎片，确保异构集群中的负载均衡，减少木桶效应。
*   **系统级优化**：应用层融合（如 Conv+BN+ReLU 融合）、8位量化以及通信与计算的流水线重叠。

### 3. 实验设计
*   **实验场景**：使用 8 台 Teensy 4.1 MCU（Cortex-M7, 600MHz, 1MB RAM）构建真实测试床，通过以太网交换机连接。
*   **测试模型**：MobileNetV2（输入分辨率 112x112x3），该模型在单台 Teensy 4.1 上因内存不足无法运行。
*   **对比基准（Benchmark）**：
    *   **均匀分配（Evenly）**：不考虑硬件差异，平均分配任务。
    *   **仅频率分配（Freq-only）**：仅根据 MCU 主频分配任务。
    *   **优化分配（Optimized）**：本文提出的资源感知分配方案。
*   **评估指标**：端到端延迟、通信开销、单机峰值内存占用。

### 4. 资源与算力
*   **硬件设备**：8 台 Teensy 4.1 开发板，PC 作为协调器。
*   **算力说明**：论文侧重于**推理阶段**的分布式执行，未涉及模型训练。推理时的计算量分布在 600MHz 的 Cortex-M7 内核上。
*   **训练时长**：文中未提及模型训练时长，因为使用的是预训练并量化的模型。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **真实测试**：在 3、5、8 台 MCU 上进行了端到端推理实验。
    *   **异构性测试**：设置了 8 组不同的频率和延迟组合（如 600/150/450 MHz 混合），验证分配算法的鲁棒性。
    *   **仿真实验**：为了验证扩展性，通过仿真模拟了多达 120 个 MCU 的内存下降趋势。
*   **充分性评价**：实验设计较为充分，涵盖了从内存可行性验证到异构负载均衡的多个维度，且通过真实硬件测试增强了结论的可信度。

### 6. 主要结论与发现
*   **内存可行性**：通过细粒度拆分，原本无法在单机运行的 MobileNetV2 成功在 MCU 集群上运行，单机峰值 RAM 显著降低。
*   **负载均衡效果**：在异构网络环境下，资源感知分配方案比均匀分配提升了约 18.9% 的性能。
*   **扩展性边际效应**：随着 MCU 数量增加，计算时间线性下降，但通信开销（数据交换）逐渐成为主导因素。仿真显示，在 10-20 个节点后，内存降低的收益开始趋于平缓。

### 7. 优点
*   **粒度极细**：突破了传统“层”拆分的局限，真正解决了 TinyML 的 RAM 瓶颈问题。
*   **异构支持**：评分机制很好地处理了现实中 MCU 性能不一、网络波动的复杂情况。
*   **实用性强**：在 bare-metal（裸机）环境下实现，不依赖重型操作系统，符合嵌入式实际。

### 8. 不足与局限
*   **通信瓶颈**：由于所有中间激活值都通过协调器转发，协调器可能成为通信中心节点和潜在瓶颈。
*   **能耗未评估**：虽然降低了内存压力，但频繁的网络通信会显著增加系统总能耗，论文未对此进行量化分析。
*   **模型局限**：目前主要针对 CNN 结构，对于 Transformer 等具有全局依赖性的新型模型，其细粒度切分和通信开销可能面临更大挑战。

（完）
