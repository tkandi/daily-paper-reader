---
title: "FastOmniTMAE: Parallel Clause Learning for Scalable and Hardware-Efficient Tsetlin Embeddings"
title_zh: FastOmniTMAE：面向可扩展且硬件高效的 Tsetlin 嵌入的并行子句学习
authors: "Ahmed K. Kadhim, Lei Jiao, Rishad Shafik, Ole-Christoffer Granmo, Mayur Kishor Shende"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06982v1"
tags: ["query:mlsys"]
score: 6.0
evidence: 针对逻辑学习嵌入模型的硬件高效并行训练过程。
tldr: 本研究针对逻辑学习范式Tsetlin Machine在词嵌入任务中训练缓慢的问题，提出了FastOmniTMAE。该方法通过将顺序训练重构为评估与更新两阶段并行过程，显著提升了训练效率。实验表明，FastOmniTMAE在保持嵌入质量的同时，训练速度提升达5倍，并成功在SoC-FPGA硬件上实现，证明了其在资源受限环境下的高效性与可扩展性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.06982v1/fig-001.webp\", \"caption\": \"Figure 3: SoC-based architecture of the FastOmniTMAE accelerator with PS–PL communication over AXI interfaces for efficient parallel training.\", \"page\": 6, \"index\": 1, \"width\": 714, \"height\": 339}, {\"url\": \"assets/figures/arxiv/2605.06982v1/fig-002.webp\", \"caption\": \"Figure 4: Classification performance of FastOmniTMAE and Omni TM-AE over 100 epochs using precision, recall, and F1-score.\", \"page\": 7, \"index\": 2, \"width\": 735, \"height\": 217}, {\"url\": \"assets/figures/arxiv/2605.06982v1/fig-003.webp\", \"caption\": \"Figure 2: (a on the left) Relationship between the class_sum and the update probability update_p. (b in the middle) Update probability update_p over epochs for different datasets and hyperparameters. (c on the right) Clause weights variation over epochs.\", \"page\": 4, \"index\": 3, \"width\": 819, \"height\": 204}, {\"url\": \"assets/figures/arxiv/2605.06982v1/fig-004.webp\", \"caption\": \"Figure 6: Distribution of TA states across clauses after training. The first half of the literal index range corresponds to original vocabulary features, while the second half corresponds to negated literals. The distribution shows that original literals are mostly pushed toward low states, whereas negated literals form a richer high-state distribution, indicating that absence-based information is important for Omni-style embedding construction.\", \"page\": 12, \"index\": 4, \"width\": 829, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2605.06982v1/fig-005.webp\", \"caption\": \"Table 2: FastOmniTMAE hardware benchmark on the RG65 44-token workload (352k examples total). Each row reports the mean with standard deviation over 5 runs.\", \"page\": 9, \"index\": 5, \"width\": 823, \"height\": 268}]"
motivation: 现有的Omni TM-AE逻辑嵌入模型训练过程存在顺序依赖，导致在大规模应用和硬件加速方面效率受限。
method: 提出一种两阶段并行训练架构，将子句学习分为评估和更新阶段，并针对SoC-FPGA平台设计了可重用的硬件加速器。
result: 在分类任务中实现高达5倍的训练加速，同时在相似度测量中保持了与原模型相当的性能，并在FPGA上展现了极低的资源占用。
conclusion: FastOmniTMAE为逻辑嵌入模型提供了一种高效、可扩展且硬件友好的并行训练方案。
---

## 摘要
自然语言处理（NLP）中的嵌入模型日益依赖于 BERT 等深度架构，而 Word2Vec 等较简单的模型虽然提供了高效的表示，但可解释性有限。Tsetlin 机（TM）提供了一种替代的基于逻辑的学习范式。Omni TM 自编码器（Omni TM-AE）通过利用单个子句层内的自动机状态分布，将该范式应用于静态嵌入，但其训练过程仍然缓慢。在这项工作中，我们提出了 FastOmniTMAE，这是对 Omni TM-AE 的重新构建，它将顺序训练依赖关系替换为两阶段并行过程：评估和更新。通过涵盖分类、相似度和聚类的单次运行多环境基准测试，FastOmniTMAE 在分类任务中实现了高达 5 倍的训练加速，同时在 Spearman 和 Kendall 相似度指标下保持了相当的嵌入质量。为了解决 TM 在传统 GPU 上训练效率有限的问题，我们进一步将 FastOmniTMAE 实现为 SoC-FPGA 平台上的可重用加速器。多硬件基准测试显示，FastOmniTMAE 在资源受限的 FPGA 上实现了 0.669 的相似度得分，在 UltraScale+ SoC 上实现了 0.696 的相似度得分，证明了在较小硬件占用下进行高效的基于逻辑的嵌入训练的可行性。

## Abstract
Embedding models in natural language processing (NLP) increasingly rely on deep architectures such as BERT, while simpler models such as Word2Vec provide efficient representations but limited interpretability. The Tsetlin Machine (TM) offers an alternative logic-based learning paradigm. Omni TM Autoencoder (Omni TM-AE) applies this paradigm to static embedding by exploiting automaton state distributions within a single clause layer, but its training process remains slow. In this work, we propose FastOmniTMAE, a reformulation of Omni TM-AE that replaces sequential training dependencies with a two-stage parallel process: evaluation and update. Using a Single-Run Multi-Environment Benchmark covering classification, similarity, and clustering, FastOmniTMAE achieves up to 5$\times$ faster training in classification while maintaining comparable embedding quality under both Spearman and Kendall similarity measures. To address the limited efficiency of TM training on conventional GPUs, we further implement FastOmniTMAE as a reusable accelerator on SoC-FPGA platforms. The Multi-Hardware Benchmark shows that FastOmniTMAE achieves similarity scores of 0.669 on a resource-constrained FPGA and 0.696 on an UltraScale+ SoC, demonstrating efficient logic-based embedding training with a small hardware footprint.