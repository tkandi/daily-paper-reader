---
title: "Dimensionality Reduction for Robust Federated Learning: A Theoretical Analysis and Convergence Guarantee"
title_zh: 鲁棒联邦学习的降维方法：理论分析与收敛性保证
authors: "Shiyuan Zuo, Jiashuo Li, Rongfei Fan, Han Hu, Jie Xu"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28335v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 提出降维方法加速联邦学习中的鲁棒聚合
tldr: 联邦学习中，拜占庭攻击威胁模型安全，现有鲁棒聚合方法在高维梯度处理时计算开销随模型增大而剧增。本文提出投影降维加速框架PDR，通过稀疏随机投影将梯度压缩至低维子空间，高效计算可靠性权重，将服务器计算复杂度降至理论下界O(Mp)。在标准假设下证明了非凸和强凸目标的收敛速率，且加速仅带来可控的误差膨胀。实验表明该方法使聚合时间效率提升数个量级，同时保持有竞争力的模型精度。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.28335v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1443, \"height\": 937, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28335v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1442, \"height\": 972, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28335v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 765, \"height\": 299, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28335v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1444, \"height\": 870, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28335v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1444, \"height\": 873, \"label\": \"Table\"}]"
motivation: 现代联邦学习模型规模快速增长，现有拜占庭鲁棒聚合方法带来的高维梯度计算开销成为训练瓶颈，亟需高效加速方案。
method: 提出投影降维(PDR)框架，利用稀疏随机投影将梯度映射到低维子空间以计算可靠性权重，从而加速各类基于距离的鲁棒聚合器。
result: PDR将服务器计算复杂度降至O(Mp)，在非凸和强凸条件下分别达到O(1/√T)和O(1/T)的最优收敛速率，加速因子仅引入可控的拜占庭误差放大。
conclusion: PDR能以微小精度代价实现鲁棒联邦学习的大幅加速，为高维场景下可扩展的拜占庭鲁棒训练提供了理论和实践支撑。
---

## 摘要
联邦学习（FL）允许多个客户端在不共享原始数据的情况下协作训练模型，但它极易受到拜占庭攻击。现有的鲁棒方法能够化解这些威胁，但在高维梯度聚合过程中会产生巨大的计算开销，这种开销随模型规模增长而急剧增加，且随着现代模型越来越大，逐渐成为训练成本的主要部分。为了解决这一计算瓶颈，我们提出了投影降维（PDR），这是一个针对基于向量级距离的鲁棒聚合器的通用加速框架，它通过稀疏随机投影将梯度压缩到一个极小的子空间中，从而高效计算可靠性权重并进行鲁棒聚合。该方法将服务器计算复杂度降低到最优的 $\mathcal{O}(Mp)$，其中 $M$ 是客户端数量，$p$ 是模型维度，达到了仅读取梯度所需的理论下界。我们在先前的拜占庭鲁棒联邦学习分析中的标准假设下建立了收敛性保证。利用子空间嵌入定理，我们证明 PDR 在非凸函数下达到了 $\mathcal{O}(1/\sqrt{T})$ 的最优收敛速率，在强凸函数下达到了 $\mathcal{O}(1/T)$ 的最优收敛速率，其中 $T$ 表示迭代次数。关键的是，我们从数学上证明，这种大幅加速几乎是无代价的，仅将固有的拜占庭误差底限放大了有限且可调的 $\frac{1+ε}{1-ε}$ 倍。在基准数据集上的实验结果证实，将 PDR 与现有聚合器结合可在时间效率上带来数量级的提升，同时保持极具竞争力的收敛性能。

## Abstract
Federated Learning (FL) enables multiple clients to collaboratively train models without sharing raw data, but it is highly vulnerable to Byzantine attacks. Existing robust approaches can neutralize these threats but incur substantial computational overhead during high-dimensional gradient aggregation, an overhead that scales poorly with model size and increasingly dominates the training cost as modern models grow larger. To address this computational bottleneck, we propose Projected Dimensionality Reduction (PDR), a universal acceleration framework for vector-level distance-based robust aggregators, which performs robust aggregation by compressing gradients into a drastically smaller subspace via sparse random projection to efficiently compute reliability weights. This approach reduces the server computational complexity to an optimal $ \mathcal{O}(Mp) $, where $ M $ is the number of clients and $ p $ is the model dimension, matching the theoretical lower bound required merely to read the gradients. We establish convergence guarantees under standard FL assumptions in prior Byzantine-robust FL analyses. By leveraging the Subspace Embedding Theorem, we show that PDR achieves optimal convergence rates of $ \mathcal{O}(1/\sqrt{T}) $ for non-convex functions and $ \mathcal{O}(1/T) $ for strongly convex functions, where $ T $ denotes the number of iterations. Crucially, we mathematically demonstrate that this massive acceleration comes almost for free, merely inflating the inherent Byzantine error floor by a bounded, tunable factor of $ \frac{1+ε}{1-ε} $. Experimental results on benchmark datasets confirm that integrating PDR with existing aggregators yields orders of magnitude speedups in time efficiency while maintaining highly competitive convergence performance.

---

## 论文详细总结（自动生成）

好的，请查收按照您要求的结构化论文总结。

---

### 论文核心问题与整体含义
*   **研究动机**：在联邦学习（FL）中，虽然拜占庭鲁棒聚合器（Byzantine-robust aggregators）能有效防御恶意客户端的攻击，但它们通常依赖高维梯度向量的距离计算（如欧氏距离、排序等），其计算复杂度随模型参数量 $p$ 的增长而急剧增加，逐渐成为训练的主要瓶颈。
*   **核心问题**：如何解决现有向量级距离型鲁棒聚合器在高维梯度场景下计算开销过大的问题，即在保持鲁棒性的同时，大幅降低服务器端的聚合计算成本。
*   **整体含义**：本文提出一种通用、可插拔的加速框架，通过降维技术从根本上缓解维数灾难，将鲁棒聚合的计算瓶颈从巨大的模型维度转移到一个小得多的投影维度，从而实现几乎无代价的大幅加速。

### 论文提出的方法论
*   **核心思想**：本文观察到，基于向量级距离的鲁棒聚合器的核心机制（例如筛选恶意更新）依赖于客户端更新向量之间的**相对距离关系**，而非确切的高维坐标。因此，可以利用随机投影在保留向量间距离的前提下，将高维梯度压缩至低维空间，在低维空间中高效地完成异常检测和权重计算。
*   **关键技术细节——投影降维（PDR）框架**：
    1.  **压缩**：服务器在每轮通信中，生成一个稀疏随机投影矩阵 $\mathbf{P} \in \mathbb{R}^{k \times p}$ ($k \ll p$)，将接收到的高维梯度 $\mathbf{g}^m$ 压缩为低维表示 $\tilde{\mathbf{g}}^m = \mathbf{P}\mathbf{g}^m$。该矩阵满足子空间嵌入定理（Subspace Embedding Theorem），能以高概率保留向量间的欧氏距离。
    2.  **聚合**：在低维空间 $\mathbb{R}^k$ 中，运行任意基于向量级距离的鲁棒聚合器（如 Krum, GM, Bulyan），计算出每个客户端的可靠性权重 $\tilde{\alpha}^m$。
    3.  **重构**：将计算出的权重 $\{\tilde{\alpha}^m\}$ 应用到原始的高维梯度 $\{\mathbf{g}^m\}$ 上，通过加权求和得到最终的全局更新 $\mathbf{g} = \sum_{m} \tilde{\alpha}^m \mathbf{g}^m$。
*   **理论保证**：
    *   **计算复杂度**：通过使用稀疏随机投影，该框架将服务器端的计算复杂度降至 $\mathcal{O}(Mp)$，这与仅读取所有梯度所需的理论下界相匹配。
    *   **收敛性**：在非凸和强凸两种标准假设下，该框架分别能达到 $\mathcal{O}(1/\sqrt{T})$ 和 $\mathcal{O}(1/T)$ 的最优收敛速率。
    *   **误差控制**：降维带来的加速代价仅为将固有的拜占庭误差底界放大了有限的 $\frac{1+ε}{1-ε}$ 倍（$ε ∈ (0,1)$），此因子可通过投影维度 $k$ 调节。

### 实验设计
*   **数据集与场景**：
    *   **数据集**：TinyImageNet、CIFAR-100、CIFAR-10。
    *   **模型**：MobileNetV3、VGG16、ResNet18。
    *   **数据分布**：为模拟非独立同分布场景，使用狄利克雷分布（Dirichlet distribution）以不同的浓度参数 $β \in \{0.2, 0.6\}$ 将数据划分给 $M=50$ 个客户端。
*   **拜占庭攻击模型**：模拟四种典型的攻击——高斯攻击（Gaussian）、符号翻转攻击（Sign-flip）、LIE攻击和FoE攻击。攻击者比例 $b$ 设置为 $\{0.1, 0.3\}$。
*   **基准方法（Baselines）**：将PDR框架与四种经典的鲁棒聚合器结合，形成 `PDR+Krum`、`PDR+Bulyan`、`PDR+Geometric Median`、`PDR+MCA`，并与它们未加速的原始版本进行对比。
*   **评估指标**：测试集上的Top-1准确率（Test Accuracy）和服务器端聚合过程的实际耗时（Wall Time）。

### 资源与算力
*   论文正文及附录中**未明确提及**实验所使用的具体硬件配置（如GPU型号、数量）以及完整的训练总时长。

### 实验数量与充分性
*   **实验数量**：实验覆盖了**3个数据集**、**3种模型**、2种非独立同分布程度（$β$）、2种攻击者比例（$b$）以及**4种攻击类型**，形成了较大规模的实验组合。此外，还包含了对投影维度 $k$ 和稀疏度 $s$ 的消融研究。
*   **充分性与公平性**：实验设计较为充分、客观且公平。它覆盖了多种具有挑战性的攻击和异构数据场景，对比了不同种类的聚合器，并从模型精度和计算效率两个维度进行评估。消融实验验证了方法对关键超参数的稳定性。

### 论文的主要结论与发现
1.  **效率大幅提升**：与原始鲁棒聚合器相比，PDR框架在服务器执行时间上能带来**数个数量级的加速**，成功解决了高维聚合的计算瓶颈。
2.  **精度高度保持**：虽然随机投影偶尔会导致微小的精度下降，但PDR增强的方法在各种攻击和数据异构性下都保持了**极具竞争力的测试准确率**。在某些情况下，投影甚至起到了隐式正则化作用，提升了精度。
3.  **鲁棒性完美继承**：PDR框架**严格继承了其底层聚合器的鲁棒属性**。当基础聚合器对某种攻击失效时，PDR版本同样不收敛；反之，基础聚合器有效时，PDR版本也能成功抵御攻击。
4.  **参数稳定性**：PDR的性能在合理的投影维度 $k$ 和稀疏度 $s$ 范围内保持稳定，无需繁琐的超参数调优。

### 优点
*   **通用性与解耦设计**：作为一个“即插即用”的框架，PDR可以与任何基于向量级距离的鲁棒聚合器无缝集成，将计算瓶颈从聚合器本身剥离。
*   **坚实的理论支撑**：论文提供了严密的理论分析，包括计算复杂度下界的达成、收敛速率的证明以及有界误差放大的量化，理论深度突出。
*   **显著的实用价值**：实验证明了其在真实数据集和攻击场景下的巨大加速潜力，对于推动大规模联邦学习系统的鲁棒性部署具有重要意义。

### 不足与局限
*   **适用范围的明确限制**：该方法不适用于坐标级（coordinate-level）聚合器（如Coordinate-wise Median或Trimmed Mean），因为随机投影会混合所有维度，破坏此类方法依赖的逐坐标独立异常信息。
*   **引入可控的精度代价**：尽管是可控的，但降维不可避免地为聚合结果引入了一个由因子 $\frac{1+ε}{1-ε}$ 量化的额外方差，导致理论上的收敛误差底界被轻微放大。
*   **缺乏与通信压缩方法的对比**：论文侧重于解决**计算瓶颈**，实验主要对比了原始聚合器，而未与可能同样减轻服务器负载的梯度通信压缩方法进行对比。

（完）
