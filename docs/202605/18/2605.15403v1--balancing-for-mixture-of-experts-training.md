---
title: $φ$-Balancing for Mixture-of-Experts Training
title_zh: $φ$-平衡用于混合专家模型训练
authors: "Lizhang Chen, Jonathan Li, Qi Wang, Runlong Liao, Shuozhe Li, Chen Liang, Ni Lao, Qiang Liu"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15403v1"
tags: ["query:mlsys"]
score: 8.5
evidence: MoE训练中专家负载均衡的原则性框架
tldr: 混合专家模型（MoE）的扩展性依赖于专家的负载均衡，但现有方法多基于小批量统计的启发式策略，存在噪声和偏差。本文提出φ-balancing框架，通过最小化期望路由分布的严格凸势函数，直接优化总体层面的专家平衡。利用凸对偶性，该方法通过镜像下降推导出一种简单的在线EMA路由调整算法。实验表明，该方法在预训练和微调中均优于现有基准，实现了更稳定、高效的专家利用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.15403v1/fig-001.webp\", \"caption\": \"Table 2. Ablation on mirror maps ϕ. We report validation loss and maximum global load-balance violation (MaxVioglobal), defined in Appendix A; lower is better.\", \"page\": 6, \"index\": 1, \"width\": 500, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2605.15403v1/fig-002.webp\", \"caption\": \"Figure 2. Pretraining scaling studies under controlled per-token compute. We evaluate routing stability and optimization across three orthogonal MoE scaling axes, while keeping the per-token computational cost (FLOPs) approximately constant within each study by adjusting expert size as needed. (Left) Active-parameter scaling: we train models with E = 16 experts and A = 2 active experts per token, varying the number of active parameters N ∈ {111M, 338M, 588M, 986M}. (Middle) Granularity scaling: for fixed model size M and activation ratio A/E, we vary the granularity factor G ∈ {2, 4, 8, 16, 32} by increasing the total number of experts from 16 to 256 while proportionally shrinking each expert, so per-token FLOPs remain constant. (Right) Expert-count scaling (activation ratio): we isolate the effect of A/E by holding the compute budget M , the number of activated experts A = 2, and the expert size (granularity) fixed, and varying the total number of experts E ∈ {8, 16, 32, 64, 128}.\", \"page\": 3, \"index\": 2, \"width\": 992, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2605.15403v1/fig-003.webp\", \"caption\": \"Table 8. Mixed benchmark. We combine 1,500 examples from the seven benchmarks used in per-benchmark finetuning, and combine them into a mixed finetuning dataset. Similar to per-benchmark finetuning, each example contains high quality chain-of-thought reasoning from a strong teacher model (OpenAI GPT-5.2). For benchmarks with less than total 1,500 examples, we select all of its training distribution. We finetune with LoRA rank r = 4 on one epoch with learning rate 2e-5 (approximately 500 steps). We finetune Deepseek-MoE-16B-Chat and Deepseek-V2-Lite-Chat models and show the accuracy of each benchmark in the evaluation set.\", \"page\": 18, \"index\": 3, \"width\": 1012, \"height\": 202}, {\"url\": \"assets/figures/arxiv/2605.15403v1/fig-004.webp\", \"caption\": \"Table 7. Summary of MoE scaling study configurations. All studies are conducted while keeping per-token FLOPs approximately constant.\", \"page\": 18, \"index\": 4, \"width\": 741, \"height\": 191}, {\"url\": \"assets/figures/arxiv/2605.15403v1/fig-005.webp\", \"caption\": \"Figure 8. Effect of entropy order α on validation performance. Validation loss and accuracy are reported after 20.5K training steps for Tsallis and Rényi entropy variants across different values of α. Both objectives exhibit a stable accuracy profile near α ≈ 0.9–1.0. Conversely, extreme values of α degrade validation loss and/or accuracy, indicating that moderate entropy orders yield the most robust performance. Model configuration: ∼588M active parameters per token, with a total memory footprint of ∼3.34B parameters.\", \"page\": 18, \"index\": 5, \"width\": 1004, \"height\": 206}, {\"url\": \"assets/figures/arxiv/2605.15403v1/fig-006.webp\", \"caption\": \"Table 5. Domain specialization. Accuracy across benchmarks (mean ± approx. std (half-width/1.96); higher is better). Avg is the unweighted mean over all benchmarks shown. Best per model/column is in bold.\", \"page\": 8, \"index\": 6, \"width\": 1025, \"height\": 265}, {\"url\": \"assets/figures/arxiv/2605.15403v1/fig-007.webp\", \"caption\": \"Figure 5. Performance comparison of ablation method combinations across three model architectures. The radar charts illustrate the evaluation of DeepSeek-MoE-Chat, DeepSeek-V2-Lite, and Moonlight-16B-A3B-Instruct on seven diverse benchmarks. Radial axes represent the corresponding benchmark scores, identified by the labels and the color-coded outer rim segments. The proposed method (Ours) demonstrates a consistent expansion of the performance capabilities compared to the ST-MoE baseline.\", \"page\": 8, \"index\": 7, \"width\": 1000, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2605.15403v1/fig-008.webp\", \"caption\": \"Figure 3. Pre-Training dynamics and expert utilization. We compare ϕ-balancing (red, solid) against ST-MoE (blue, dashed) over 10k steps. (Left) Validation Loss and Accuracy show that ϕ-balancing (negative entropy) achieves comparable or superior convergence. (Right) Gini coefficient and Expert Loading Analysis demonstrates significantly lower routing imbalance for ϕ-balancing. ϕ-balancing maintains tighter bounds between maximum and minimum expert load, staying closer to the perfect allocation line (green) compared to ST-MoE, which exhibits higher variance in expert capacity usage.\", \"page\": 4, \"index\": 8, \"width\": 1009, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2605.15403v1/fig-009.webp\", \"caption\": \"Figure 7. Hyperparameter sensitivity analysis. Heatmaps displaying Validation Accuracy (left) and Gini Coefficient (right) across varying Learning Rates (γ ∈ {1e-3, . . . , 4e-3}) and ϕ-balancing loss coefficient (α ∈ {0.001, . . . , 0.015}). While accuracy remains robust (peak 0.4136), increasing α drastically reduces the Gini coefficient.\", \"page\": 17, \"index\": 9, \"width\": 696, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2605.15403v1/fig-010.webp\", \"caption\": \"Figure 6. Domain specialization in routing. Routed-token ratio (fraction of tokens) assigned to each expert (IDs 0–7) for different data domains (Arxiv, Books, C4, Github, Stack, Wiki) at two representative layers (Layer 5 and Layer 11). Compared to ST-MoE, our router exhibits sharper domain-to-expert preferences (stronger specialization), albeit with mildly uneven expert loads.\", \"page\": 9, \"index\": 10, \"width\": 986, \"height\": 307}, {\"url\": \"assets/figures/arxiv/2605.15403v1/fig-011.webp\", \"caption\": \"Table 1. Summary of ϕ-balancing variants. The choice of the potential function ϕ determines the relationship between the accumulated expert usage state mt+1 and the auxiliary loss Laux. Here, summations are taken over the experts e, and q denotes the conjugate exponent such that 1 p + 1 q = 1. We follow the convention 0 log 0 := 0.\", \"page\": 5, \"index\": 11, \"width\": 1017, \"height\": 284}]"
motivation: 针对现有MoE负载均衡方法依赖小批量启发式统计导致的偏差和不稳定性问题。
method: 提出一种基于凸势函数最小化的φ-balancing框架，并利用镜像下降实现高效的在线EMA路由调整。
result: 在大规模预训练和微调实验中，该方法一致性地超越了Switch-style和无损基准。
conclusion: 该研究为MoE模型提供了一种理论严谨且开销极小的负载均衡新方案，增强了模型的扩展潜力。
---

## 摘要
混合专家（MoE）模型依赖于平衡的专家利用率，以充分实现其可扩展性。然而，现有的负载均衡方法大多是启发式的，且基于带有噪声的小批量（mini-batch）分配统计数据运行，从而引入了相对于总体层面（population-level）目标的偏差。我们提出了 $φ$-平衡（$φ$-balancing），这是一个有原则的框架，通过最小化期望路由分布的一个严格凸、对称且可微的势函数，直接针对总体层面的专家平衡。利用凸对偶性，我们推导出了一个等效的极大极小（min-max）公式，并通过镜像下降法获得了一个简单的在线算法，从而产生了一种开销极小的、基于指数移动平均（EMA）的高效路由调整方法。在大规模预训练和下游微调中，$φ$-平衡始终优于先前的 Switch 风格和无损（loss-free）基准模型，展示了更稳定且有效的专家利用率。

## 速览
**TLDR**：混合专家模型（MoE）的扩展性依赖于专家的负载均衡，但现有方法多基于小批量统计，存在启发式偏差。本文提出 φ-balancing 框架，通过最小化期望路由分布的凸势函数，直接优化总体层面的专家平衡。该方法利用凸对偶性推导出基于镜像下降的在线算法，通过指数移动平均（EMA）调整路由，计算开销极低。实验表明，该方法在大规模预训练和微调中均优于现有基准，实现了更稳定高效的专家利用。 \
**Motivation**：现有的 MoE 负载均衡方法主要依赖于带有噪声的小批量统计数据，导致相对于总体目标的优化偏差。 \
**Method**：提出 φ-balancing 框架，通过最小化期望路由分布的凸势函数，并利用镜像下降推导出高效的在线 EMA 路由调整算法。 \
**Result**：在多项大规模预训练和下游微调实验中，该方法在专家利用率和模型性能上均优于传统的 Switch-style 等基准。 \
**Conclusion**：φ-balancing 为 MoE 训练提供了一种理论完备且计算高效的平衡机制，显著提升了模型的稳定性和扩展性。

---

## Abstract
Mixture-of-Experts (MoE) models rely on balanced expert utilization to fully realize their scalability. However, existing load-balancing methods are largely heuristic and operate on noisy mini-batch assignment statistics, introducing bias relative to population-level objectives. We propose $φ$-balancing, a principled framework that directly targets population-level expert balance by minimizing a strictly convex, symmetric, and differentiable potential of the expected routing distribution. Using convex duality, we derive an equivalent min-max formulation and obtain a simple online algorithm via mirror descent, yielding an efficient EMA-based routing adjustment with negligible overhead. Across large-scale pretraining and downstream fine-tuning, $φ$-balancing consistently outperforms prior Switch-style and loss-free baselines, demonstrating more stable and effective expert utilization.

---

## 论文详细总结（自动生成）

这是一份关于论文《$\phi$-Balancing for Mixture-of-Experts Training》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：混合专家模型（MoE）在训练中面临“专家负载不均”的挑战。如果某些专家被过度调用而其他专家被闲置，模型的扩展性（Scalability）将大打折扣。
*   **研究动机**：现有的负载均衡方法（如 Switch Transformer 的辅助损失）大多是启发式的，且仅在“小批量（mini-batch）”层面计算统计数据。这种局部统计带有大量噪声，无法代表整个数据分布（总体层面）的真实需求，从而导致路由决策存在偏差和不稳定性。

### 2. 方法论：$\phi$-balancing 框架
*   **核心思想**：将负载均衡视为一个优化问题，目标是最小化总体路由分布的**严格凸势函数** $\phi$。通过这种方式，将“强制每批次均衡”转变为“追求全局期望均衡”。
*   **关键技术细节**：
    *   **凸对偶性（Convex Duality）**：利用对偶理论将原始的最小化问题转化为一个极大极小（min-max）问题，引入对偶变量来代表专家的“拥堵成本”。
    *   **在线镜像下降（Online Mirror Descent）**：为了避免直接计算全局分布，算法通过镜像下降推导出一种在线更新规则。
    *   **算法流程**：
        1.  维护一个专家负载的**指数移动平均（EMA）**，记为 $m$。
        2.  在每个训练步，根据当前批次的路由概率 $p$ 更新 EMA：$m \leftarrow (1-\eta)m + \eta p$。
        3.  计算辅助损失 $L_{aux}$，其形式为当前路由概率与势函数梯度的内积：$\sum p_e \cdot \nabla \phi(m)_e$。
    *   **势函数选择**：论文探讨了多种势函数（如欧几里得范数、$\ell_p$ 范数、负熵等），其中**负熵（Negative Entropy）**在实践中效果最好。

### 3. 实验设计
*   **数据集与场景**：
    *   **大规模预训练**：基于 C4 数据集，使用 MoE 增强的 Gemma 模型进行预训练。
    *   **下游微调**：在 DeepSeek-MoE-16B、DeepSeek-V2-Lite 和 Moonlight-16B 等开源大模型上进行 LoRA 微调。
*   **基准（Benchmarks）**：涵盖数学（GSM8K, MATH500）、代码（HumanEval）、综合逻辑（BBH, LiveBench, GPQA）和语言理解（GLUE）。
*   **对比方法**：主要对比了 **ST-MoE**（Switch Transformer 风格的辅助损失）和 **Loss-free balancing**（无损负载均衡，如 DeepSeek 早期方案）。

### 4. 资源与算力
*   **算力消耗**：论文明确指出，所有实验总计消耗了约 **40,000 个 NVIDIA H100 (80GB HBM3) GPU 小时**。
*   **开销评估**：该方法引入的计算开销极小（仅涉及简单的 EMA 更新和梯度计算），在实际训练中几乎可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **扩展性研究**：分别针对“激活参数量”（111M 到 986M）、“专家数量”（8 到 128）和“路由粒度”进行了三组正交的扩展性实验。
    *   **消融实验**：对势函数类型（Table 1 中的 9 种变体）、EMA 衰减率 $\eta$、全局批次大小等进行了详细消融。
*   **充分性评价**：实验设计非常充分且严谨。通过在不同架构（Gemma, DeepSeek, Moonlight）和不同任务（预训练 vs. 微调）上的验证，证明了该方法的普适性。

### 6. 主要结论与发现
*   **性能提升**：$\phi$-balancing 在所有测试的预训练规模和下游任务中均优于 ST-MoE 和无损基准。
*   **稳定性**：相比于基于批次频率的方法，$\phi$-balancing 显著降低了路由的不稳定性（Gini 系数更低），专家利用率更接近理想状态。
*   **专家专业化**：观察发现，该方法能促进专家产生更清晰的领域偏好（如某些专家专门处理代码，某些处理维基百科），而 ST-MoE 往往会抑制这种专业化。

### 7. 优点与亮点
*   **理论严谨**：将负载均衡从“启发式技巧”提升到了“凸优化理论”的高度，提供了清晰的数学解释。
*   **实现简单**：仅需几行代码修改即可集成到现有 MoE 框架中，且不增加推理负担。
*   **鲁棒性强**：对超参数（如批次大小）的敏感度较低，在小批次下依然能保持良好的全局均衡。

### 8. 不足与局限
*   **超参数依赖**：虽然鲁棒，但仍引入了 EMA 衰减率 $\eta$ 和损失权重 $\alpha$ 两个超参数，需要针对不同模型进行微调以获得最优效果。
*   **势函数选择**：虽然负熵表现优异，但论文尚未给出一个通用的理论框架来预测在特定任务下哪种势函数是“最优”的。
*   **应用限制**：实验主要集中在文本模态，尚未在多模态（如视觉-语言 MoE）或极其异构的硬件环境下进行验证。

（完）
