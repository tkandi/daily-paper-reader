---
title: Improving Generalization by Permutation Routing Across Model Copies
title_zh: 通过跨模型副本的置换路由提升泛化性能
authors: "Shuhei Kashiwamura, Timothee Leleu"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09256v1"
tags: ["query:mlsys"]
score: 7.0
evidence: 分布式训练中模型副本间的消息传输拓扑与路由
tldr: 本研究提出了一种基于M-覆盖变换的机器学习方法，旨在通过在多个模型副本间进行置换路由来提高泛化能力。与传统的参数平均或弹性SGD不同，该方法通过结构化混合核Q对计算路径进行重连，使局部损失在由不同副本参数组合而成的路由模型上评估。这种机制在感知机、委员会机和多层感知机上均适用，通过结构化的消息共享而非简单的副本合并，有效提升了模型的泛化性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09256v1/fig-001.webp\", \"caption\": \"Table 4. Main hyperparameters for the MNIST MLP experiments.\", \"page\": 11, \"index\": 1, \"width\": 508, \"height\": 468}, {\"url\": \"assets/figures/arxiv/2605.09256v1/fig-002.webp\", \"caption\": \"Figure 1. Workflow of the M -layer transform for learning. A base neural-network model with initial weights W is first interpreted as a factor graph and replicated into M cover copies. For a directed incidence a → i, source variables such as wj provide the computational context for the destination variable wi. The M -layer transform preserves the local factor a but permutes source cover indices according to ρa→i,j ∼ PQ, so that destination copy w (α) i receives context from routed source copies w(ρa→i,j(α))\", \"page\": 2, \"index\": 2, \"width\": 1017, \"height\": 210}, {\"url\": \"assets/figures/arxiv/2605.09256v1/fig-003.webp\", \"caption\": \"Table 1. Examples of the exact M -cover construction applied to progressively richer architectures. The index q denotes a source trainable variable and p denotes a destination trainable variable. The permutation ρq→p changes only the cover index of the source variable; it does not change its structural identity. When q = p, the destination copy is kept fixed, i.e. ρp→p = id. The data-pattern index µ is written as a subscript throughout. Bias terms in the MLP row are omitted for clarity.\", \"page\": 7, \"index\": 3, \"width\": 995, \"height\": 440}, {\"url\": \"assets/figures/arxiv/2605.09256v1/fig-004.webp\", \"caption\": \"Figure 2. Generality of the M -cover transform across learning architectures. (a,b) Generalization error of binary teacher–student perceptrons as a function of the loading factor α = P/N , where P is the number of training patterns and N is the input dimension. (c) Test-error distribution for committee machines trained on subsampled Fashion-MNIST using SGD, M -cover SGD, and replicated SGD. (d) Test-error distribution for MNIST MLPs comparing the single-cover baseline (M = 1) with structured M -cover training at M = 5. Across all four settings, structured error routing improves or shifts performance relative to the corresponding baselines.\", \"page\": 4, \"index\": 4, \"width\": 1017, \"height\": 823}, {\"url\": \"assets/figures/arxiv/2605.09256v1/fig-005.webp\", \"caption\": \"Figure 3. Effect of the routing kernel Q in the binary teacher–student perceptron. Generalization error is plotted against the Gaussian-ring width σ for different numbers of covers M . Panels show increasing routing shifts µ. Experiments use N = 1000 and fixed loading α = P/N . Error bars denote 95% CI over independent runs.\", \"page\": 5, \"index\": 5, \"width\": 1017, \"height\": 258}]"
motivation: 旨在探索一种不同于传统参数空间耦合或平均的机制，通过改变消息传输拓扑来提升模型的泛化能力。
method: 复制模型M次，并利用结构化混合核Q采样置换，将不同副本的参数重新组合成路由模型进行局部更新和消息分发。
result: 该框架成功应用于从离散感知机到微分神经网络的多种模型，实现了通过结构化消息共享提升泛化性能的目标。
conclusion: 实验证明，通过置换路由构建的计算路径拓扑能有效控制因子图结构，为提高机器学习模型的泛化能力提供了一种新颖且通用的机制。
---

## 摘要
我们介绍了 M-覆盖（或 M-层）变换在机器学习中的一种应用。该方法将模型复制 M 次，但并非像复制 SGD 或弹性 SGD 那样通过参数平均或显式吸引力来耦合副本，而是重新连接了计算局部学习消息的上下文。每个局部损失都在一个路由模型上进行评估，该模型的参数是根据从结构化混合核 Q 中采样的置换从不同副本中提取的。训练随后使用原始的局部更新规则，而产生的学习消息则通过这些路由计算路径在副本之间重新分配。因此，Q 定义了消息传输的拓扑结构，并控制了提升因子图的长环结构。我们为感知机、委员会机和多层感知机制定了这一构造，表明同样的原理适用于从离散模型到可微神经网络的各种模型。由此产生的框架提供了一种通过结构化消息共享而非副本崩溃或参数空间耦合来提高泛化能力的机制。

## Abstract
We introduce a use of the \(M\)-cover (or \(M\)-layer) transform for machine learning. The method replicates a model \(M\) times, but instead of coupling the copies through parameter averaging or an explicit attractive force, as in replicated SGD or Elastic SGD, it rewires the contexts in which local learning messages are computed. Each local loss is evaluated on a routed model whose parameters are drawn from different copies according to permutations sampled from a structured mixing kernel \(Q\). Training then uses the original local update rule, while the resulting learning messages are redistributed across the copies through these routed computational paths. Thus \(Q\) defines a topology for message transport and controls the long-loop structure of the lifted factor graph. We formulate this construction for perceptrons, committee machines, and multilayer perceptrons, showing that the same principle applies from discrete models to differentiable neural networks. The resulting framework provides a mechanism for improving generalization through structured message sharing rather than replica collapse or parameter-space coupling.