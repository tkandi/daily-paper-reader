---
title: "BatchWeave: A Consistent Object-Store-Native Data Plane for Large Foundation Model Training"
title_zh: BatchWeave：一种用于大型基础模型训练的一致性对象存储原生数据平面
authors: "Ting Sun, Junjie Zhang, Xiao Yan, Songxin Zhang, Zhuoyang Song, Jingyi Xi, Zunyao Mao, Bingyi Jing, Jiaxing Zhang, Zejian Xie"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.09994v2"
tags: ["query:mlsys"]
score: 10.0
evidence: 用于分布式大模型训练的原生对象存储训练数据平面
tldr: BatchWeave是一个专为大规模基础模型（LFM）训练设计的对象存储原生数据平面。针对现有数据加载器缺乏故障隔离或批处理语义表达能力不足的问题，它引入了事务性全局批处理（TGB）和去中心化自适应提交（DAC）算法。通过版本化清单和条件对象写入，BatchWeave实现了原子可见性、全局有序步进及端到端精确一次恢复，在提升吞吐量的同时确保了训练的一致性与可靠性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09994v2/fig-001.webp\", \"caption\": \"Figure 8: Overhead of exactly-once producer state persistence, measured against a dummy-metadata control. The per-commit cost (9.4%–74.9% at run start) declines over time because the fixed state size becomes negligible relative to growing commit I/O.\", \"page\": 11, \"index\": 1, \"width\": 474, \"height\": 424}, {\"url\": \"assets/figures/arxiv/2605.09994v2/fig-002.webp\", \"caption\": \"Figure 6: Producer ingestion throughput versus producer count across three payload sizes. BatchWeave is the only system that scales linearly with producer count; all baselines plateau or fail at high concurrency. Omitted Kafka points indicate no successful strict-TGB run at that configuration.\", \"page\": 11, \"index\": 2, \"width\": 1024, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2605.09994v2/fig-003.webp\", \"caption\": \"Figure 7: BatchWeave DAC-policy ablation with 32 producers over 5 hours. BatchWeave’s DAC policy is the only policy that sustains both high throughput and commit success as the manifest grows; all fixed-interval and heuristic policies degrade.\", \"page\": 11, \"index\": 3, \"width\": 476, \"height\": 409}, {\"url\": \"assets/figures/arxiv/2605.09994v2/fig-004.webp\", \"caption\": \"Figure 5: Per-step latency (ms, log) and end-to-end throughput (step/s) across three workloads. Kafka failed on the Qwen3-VL workloads (no usable strict-TGB run at 8 nodes); for GR00T it is shown over the longest measured prefix (9,694 steps), after which sustained queue-service backpressure caused per-step wait times to exceed an acceptable threshold. BatchWeave outperforms Local by 2.68–7.73× across all three workloads.\", \"page\": 10, \"index\": 4, \"width\": 1026, \"height\": 369}, {\"url\": \"assets/figures/arxiv/2605.09994v2/fig-005.webp\", \"caption\": \"Figure 3: Two structural limitations of message queues for LFM training. (a) Inconsistent Batch Order: concurrent producers commit shards out of order, causing DP ranks to consume shards from different batches at the same step and corrupting gradient aggregation. (b) Read Amplification: delivering a full batch as one message forces every rank to download all 𝐷 shards, consuming only 1/𝐷 .\", \"page\": 4, \"index\": 5, \"width\": 498, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2605.09994v2/fig-006.webp\", \"caption\": \"Figure 10: Consumer throughput, P95 latency, and read amplification across payload and world sizes. BatchWeave maintains near-1× read amplification at all scales because each rank issues a single range read targeting only its own data slice; dense-read and Kafka amplification grow linearly with world size. Omitted Kafka points indicate no successful strictTGB run.\", \"page\": 12, \"index\": 6, \"width\": 475, \"height\": 415}]"
motivation: 现有的分布式训练数据加载方案在故障隔离和批处理语义一致性方面存在缺陷，难以满足现代大模型训练的动态需求。
method: 提出BatchWeave系统，利用对象存储的版本化清单和条件写入实现事务性全局批处理，并采用去中心化自适应提交算法优化吞吐。
result: 在64个GPU的大规模多模态预训练和微调任务中，BatchWeave的吞吐量超过了同构数据加载器和Apache Kafka，且具有更低的读取延迟。
conclusion: BatchWeave通过将训练语义直接集成到存储层，为大规模分布式模型训练提供了一个高效、一致且具备故障隔离能力的数据平面。
---

## 摘要
现代大型基础模型（LFM）训练已将数据流水线从静态摄取层转变为必须随训练过程共同演进的动态组件。现有系统对此准备不足：同机部署的数据加载器（colocated dataloaders）缺乏故障隔离，而基于消息队列的解耦数据加载器则运行在记录/偏移量（record/offset）抽象之上，无法表达分布式训练所需的批次级语义。我们提出了 BatchWeave，一种用于分布式 LFM 训练的对象存储原生训练数据平面。BatchWeave 利用版本化清单（versioned manifests）和条件对象写入来协调批次的发布、恢复和生命周期管理。首先，它引入了事务性全局批次（Transactional Global Batch, TGB），该技术基于版本化清单的 ACID 存储语义，并将其扩展到训练特定的连贯性，包括原子化的全 rank 批次可见性、全局有序的步进序列、与检查点对齐的生命周期管理以及端到端的精确一次（exactly-once）恢复。其次，它通过提交协议持久化生产者状态，并将回收与分布式检查点状态绑定，直接在存储层实现恢复和保留。第三，其去中心化自适应提交（Decentralized Adaptive Commit, DAC）算法在清单增长时仍能维持稳定的摄取吞吐量，且无需生产者之间的通信。在 64 个 GPU 上进行的大规模多模态预训练和微调（SFT）工作负载评估表明，BatchWeave 在提供完整故障隔离的同时，吞吐量超过了同机部署的数据加载器，在摄取吞吐量上优于 Apache Kafka，且消费者读取延迟低于 Kafka。

## Abstract
Modern Large Foundation Model (LFM) training has transformed the data pipeline from a static ingestion layer into a dynamic component that must co-evolve with the training process. Existing systems are ill-equipped: colocated dataloaders offer no failure isolation, while message queue-based disaggregated dataloaders operate on a record/offset abstraction that cannot express the batch-level semantics required by distributed training. We present BatchWeave, an object-store-native training data plane for distributed LFM training. BatchWeave uses versioned manifests and conditional object writes to coordinate batch publication, recovery, and lifecycle management. First, it introduces the Transactional Global Batch (TGB), which builds on versioned-manifest ACID storage semantics and extends them with training-specific consistency, including atomic all-rank batch visibility, a globally ordered step sequence, checkpoint-aligned lifecycle management, and end-to-end exactly-once recovery. Second, it realizes recovery and retention directly in the storage layer, by durably persisting producer state through the commit protocol and tying reclamation to distributed checkpoint state. Third, its Decentralized Adaptive Commit (DAC) algorithm sustains stable ingestion throughput as the manifest grows, without any inter-producer communication. Evaluations on large-scale multimodal pre-training and SFT workloads using 64 GPUs show that BatchWeave outperforms colocated dataloader throughput while providing full failure isolation, outperforms Apache Kafka in ingestion throughput, and achieves lower consumer read latency than Kafka.

---

## 论文详细总结（自动生成）

### 论文总结：BatchWeave —— 面向大模型训练的一致性对象存储原生数据平面

#### 1. 核心问题与研究动机
现代大型基础模型（LFM）训练对数据流水线提出了三大挑战，而现有系统（如同机部署的 Dataloader 或基于消息队列的解耦方案）难以应对：
*   **数据量动态膨胀：** 多模态数据（如视频）在预处理后体积会膨胀数十至数千倍，静态资源分配难以处理这种不确定性。
*   **运行时批次确定：** 在线 Token 打包和模型相关的样本选择使得批次边界在运行时才确定，需要原子化的全局批次可见性。
*   **检查点对齐需求：** 训练过程需要频繁回滚或重放，要求数据平面具备持久化且与检查点一致的批次历史记录。
**研究动机：** 现有的同机方案缺乏故障隔离，而消息队列（如 Kafka）缺乏批次语义且存在严重的读取放大。BatchWeave 旨在利用对象存储的弹性与去中心化特性，构建一个具备事务一致性的原生数据平面。

#### 2. 方法论：核心思想与关键技术
BatchWeave 采用纯客户端库设计，核心思想是将对象存储作为共享底座，通过版本化清单（Manifest）管理数据可见性。
*   **事务性全局批次 (TGB)：** 将每个训练批次视为一等公民。TGB 由多个不可变对象组成，包含一个页脚索引（Footer Index），允许不同 Rank 通过 Range Read 仅读取其所需的数据分片，消除了读取放大。
*   **版本化清单 (Versioned Manifest)：** 借鉴 Lakehouse 架构，通过条件写入（Conditional Put）实现清单的原子更新。只有被清单记录的 TGB 对消费者可见，确保了全集群 Rank 看到的批次序列是全局一致且有序的。
*   **去中心化自适应提交 (DAC) 算法：** 
    *   **背景：** 随着清单增长，提交冲突和开销会增加。
    *   **逻辑：** 每个生产者独立运行 DAC，根据在线观测的提交耗时（$\tau_v$）和冲突率，动态计算最优的提交间隔（Gap）。
    *   **公式：** 通过泊松模型估算冲突概率，在满足冲突预算（$\epsilon$）和开销预算（$\delta$）的前提下，选择最小的 Gap 以保证数据新鲜度。
*   **检查点对齐的生命周期管理：** 引入全局水位线（Global Watermark），即所有 Rank 已完成检查点的最小进度。只有低于该水位线的数据才会被回收，确保了回滚时的安全性。

#### 3. 实验设计
*   **应用场景：** 
    1.  **GR00T：** 具身智能策略训练。
    2.  **HoloAssist：** 视频指令微调（SFT）。
    3.  **BEHAVIOR-1K：** 视觉-语言-动作（VLA）模型训练。
*   **对比基准 (Benchmarks)：**
    *   **Local：** 专家级优化的同机部署流水线（生产环境标准）。
    *   **Kafka：** 基于专用集群的严格 TGB 语义实现。
*   **微观测试：** 生产者扩展性测试（8-128 个生产者）、DAC 策略消融实验、消费者读取效率测试。

#### 4. 资源与算力
*   **GPU 资源：** 最大规模使用了 **64 块 NVIDIA H200 GPU**（8 个节点）。
*   **存储底座：** 百度对象存储（BOS）。
*   **计算节点：** 每个节点配备 64 核 CPU。
*   **训练时长：** 实验涵盖了从数小时的微观基准测试到长期的端到端训练任务。

#### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **端到端性能：** 在三个不同领域的大模型任务上验证了吞吐量。
*   **扩展性：** 测试了生产者数量从 8 增加到 128 时的吞吐变化。
*   **消融实验：** 对比了 DAC 与固定间隔、AIMD 等 5 种提交策略。
*   **一致性与恢复：** 验证了精确一次（Exactly-once）语义的开销及垃圾回收（GC）的有效性。
**评价：** 实验设计较为充分，涵盖了从系统吞吐到长时运行稳定性的各个方面，对比基准（Local 和 Kafka）具有代表性，结果客观反映了解耦架构的优势。

#### 6. 主要结论与发现
*   **吞吐量提升：** 在端到端任务中，BatchWeave 比优化的 Local 方案提升了 **2.68x 至 7.73x** 的吞吐量。
*   **延迟优化：** 相比 Kafka，BatchWeave 显著降低了消费者的读取延迟，且 P95 延迟非常稳定（<4s），避免了训练中的长尾阻塞。
*   **DAC 有效性：** 在清单持续增长的 5 小时测试中，只有 DAC 能维持 95% 以上的提交成功率和稳定的摄取带宽。
*   **存储节省：** 通过检查点驱动的 GC，存储占用可减少约 **72%**。

#### 7. 优点与亮点
*   **架构简洁：** 纯客户端实现，无需维护复杂的中间件服务器或调度器，降低了运维成本和故障域。
*   **解决读取放大：** 通过 TGB 索引实现精确的 Range Read，解决了分布式训练中常见的“全量下载、局部使用”的带宽浪费问题。
*   **原生支持动态性：** 完美适配 LFM 训练中预处理开销大、数据膨胀率不固定的特点。

#### 8. 不足与局限
*   **存储依赖：** 强依赖于对象存储的“条件写入”（Conditional Put/If-None-Match）特性，在某些不支持该特性的私有云存储上可能受限。
*   **小批次开销：** 虽然 DAC 有所缓解，但在极小批次、极高频率提交的极端场景下，清单元数据的更新开销仍可能成为瓶颈。
*   **冷启动延迟：** 论文未详细讨论在极大规模生产者同时启动时，清单初始争用的极端情况。

（完）
