---
title: Detecting Atypical Clients in Federated Learning via Representation-Level Divergence
title_zh: 通过表示层面散度检测联邦学习中的非典型客户端
authors: "Cristian Pérez-Corral, Jose I. Mestre, Alberto Fernández-Hernández, Manuel F. Dolz, Enrique S. Quitana-Ortí"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22266v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 提出轻量级几何信号，通过测量本地训练对激活诱导分割的改变来检测联邦学习中的非典型客户端，提升分布式训练鲁棒性。
tldr: 联邦学习中客户端数据异构常导致模型更新不稳定与聚合可靠性问题。本文提出一种轻量级几何信号，通过共享探测集测量本地训练对激活空间划分的改变，量化客户端与全局模型的功能偏差。该方法能有效识别引起非典型功能变化的客户端，区分稳定异构与显著偏离的个体。其结果为联邦学习系统提供了一种简单易用的客户端监控工具和风险感知聚合策略。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.22266v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1863, \"height\": 1064, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22266v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1867, \"height\": 883, \"label\": \"Figure\"}]"
motivation: 为解决联邦学习中客户端更新偏离预期（由于非IID、分布偏移或异常输入）导致的聚合不可靠问题。
method: 基于共享探测集评估本地训练对激活空间划分的改变，提出一种排列不变且可解释的客户端-全局功能偏离度量。
result: 该度量能有效识别非典型功能变化客户端，区分稳定异构与显著偏离的更新。
conclusion: 所提度量为联邦学习提供了一种轻量级客户端行为监控方法，有助于实现风险感知聚合。
---

## 摘要
联邦学习支持跨分布式客户端进行协作训练，但客户端数据的异构性常导致更新不稳定和全局性能下降。此外，在实际部署中，客户端更新可能偏离预期行为，这不仅源于良性的非独立同分布数据，也可能由分布偏移或异常输入引起，从而引发对聚合过程可靠性的关切。本文提出一种轻量级几何信号，用于量化客户端相对于全局模型的功能偏离。不同于比较模型参数或梯度，我们的方法通过在共享探测集上评估每个客户端的局部训练如何改变由激活诱导的输入空间划分。这产生了一种置换不变且可解释的客户端-全局散度度量，捕捉模型处理数据方式的差异。我们表明，该信号能有效识别引发非典型功能变化的客户端，将稳定但异构的客户端与那些更新显著偏离全局范式的客户端区分开来。因此，所提出的度量为监控客户端行为和实现联邦学习系统中风险感知的聚合策略提供了一个简单工具。

## Abstract
Federated learning enables collaborative training across distributed clients with heterogeneous data, but such heterogeneity often leads to unstable updates and degraded global performance. Moreover, in practical deployments, client updates may deviate from the expected behavior not only due to benign not i.i.d. distributions, but also due to distributional shifts or anomalous inputs, raising concerns about the reliability of the aggregation process. In this work, we propose a lightweight geometric signal to quantify the functional deviation of a client with respect to the global model. Instead of comparing model parameters or gradients, our approach measures how the local training of each client alters the activation-induced partition of the input space, evaluated on a shared probe set. This yields a permutation-invariant, interpretable metric of client--global divergence that captures differences in how data is processed by the model. We show that this signal effectively identifies clients that induce atypical functional changes, distinguishing stable yet heterogeneous clients from those whose updates significantly diverge from the global regime. As a result, the proposed metric provides a simple tool for monitoring client behavior and enabling risk-aware aggregation strategies in federated learning systems.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究背景**：联邦学习（Federated Learning, FL）允许多个客户端在本地数据不共享的情况下协作训练深度模型，但客户端数据往往高度异构（非独立同分布），导致本地更新不稳定、全局模型收敛困难。
- **核心问题**：实际部署中，客户端更新可能因良性数据异构、分布偏移或异常输入而偏离预期，影响聚合的可靠性和模型安全性。现有的参数或梯度比较方法难以反映模型内部功能行为的改变。
- **整体含义**：作者提出从**表示层面（representation-level）**的几何视角监控客户端行为，即通过分析神经网络激活模式对输入空间的划分，量化客户端与全局模型的功能散度，从而识别非典型客户端，实现风险感知的聚合。

### 2. 论文提出的方法论

- **核心思想**：神经网络（特别是ReLU网络）的激活模式将输入空间划分为线性区（region）。不同客户端训练后，即使参数差异不大，其对同一组探测样本的**激活分区方式**可能发生显著改变。利用这一性质，可衡量客户端模型与全局模型在几何结构上的差异。
- **关键技术细节**：
  - **激活模式**：对输入 \( x \)，在第 \( l \) 层的预激活向量 \( z_l(x;\theta) \) 生成二元激活模式 \( a_l(x;\theta) = \mathbf{1}\{z_l(x;\theta) > 0\} \)。
  - **激活亲和度矩阵**：固定一个共享探测集 \( P \)，对每层计算样本对之间的归一化汉明距离 \( K_\theta^l(i,j) = 1 - \frac{1}{n_l} H(a_l(x_i;\theta), a_l(x_j;\theta)) \)，得到一个 \( m\times m \) 的矩阵，反映样本在激活空间中的接近程度。
  - **层间几何散度**：比较两个模型在相同层上的亲和度矩阵的 Frobenius 范数 \( d_l(\theta,\theta') = \|K_\theta^l - K_{\theta'}^l\|_F \)。
  - **层级几何散度**：考虑到深层划分的差异仅在浅层划分相似时才有意义，引入衰减因子 \( \lambda_l = \exp(-\beta d_l(\theta,\theta')) \)，定义层级散度 \( D_{hier}(\theta,\theta') = d_1 + \sum_{l=2}^L \left(\prod_{r=1}^{l-1} \lambda_r\right) d_l \)。
  - **客户端散度**：在第 \( t \) 轮，客户端 \( c \) 的局部模型 \( \theta_c^{(t)} \) 与全局模型 \( \theta_g^{(t)} \) 之间的散度记为 \( D_c^{(t)} \)。
- **异常检测**：基于每轮客户散度计算稳健 z-分数，使用中位数和中位数绝对偏差（MAD）识别离群客户端。

### 3. 实验设计

- **数据集与模型**：
  - **MLP**（隐藏层维度128）在 **Fashion‑MNIST** 上训练。
  - **ResNet‑18** 在 **CIFAR‑10** 上训练。
- **联邦设置**：
  - 采用Dirichlet分割模拟不同程度的非独立同分布，浓度参数 \( \alpha \in \{10, 1, 0.1\} \)。
  - 每轮每个客户端本地训练1个epoch，使用SGD动量0.9。
  - 客户端数：10个（异常检测实验中）。
- **探测集**：每个场景随机选择128个独立同分布样本。
- **异常构造**：在 \( \alpha=1 \) 的背景下，选择1个客户端的本地数据施加扰动（高斯噪声、旋转、模糊），模拟分布偏移或数据损坏。
- **对比方法**：本文未与已有的联邦异常检测方法进行直接性能对比，而是重点验证所提度量的有效性，如散度随异构程度的变化、异常客户端的识别能力。

### 4. 资源与算力

- 文中**未明确说明**使用的GPU型号、数量、训练时长或具体算力消耗。仅提及软件环境（Python 3.10, PyTorch 2.6, Flower 1.25）和固定随机种子。故无法评估其计算开销。

### 5. 实验数量与充分性

- **实验组数**：
  - 一个验证性实验：在不同 \( \alpha \) 下可视化客户端散度的演变（仅MLP + Fashion‑MNIST）。
  - 一个异常检测实验：在两个模型-数据集对上分别展示异常客户端的z-分数变化。
- **消融研究**：矩阵比较时仅固定 \( \beta=1 \)，未分析超参数影响；未与基于梯度或参数的异常检测方法做定量比较；未在更多客户端数、不同扰动类型或对抗攻击下测试。
- **公平性与客观性**：实验设计较为简单，但直观地验证了核心主张。缺乏基准对比和系统的消融实验，**实验充分性有限**。

### 6. 论文的主要结论与发现

- 所提出的几何散度能有效反映数据异构程度：在近独立同分布时散度迅速稳定，强异构下散度升高且客户端之间持续分化。
- 通过对每轮散度分布的稳健z-分数，可以清晰地区分正常客户端与异常（如数据偏移）客户端，异常客户端的z-分数显著偏高且不收敛。
- 该几何信号在两种不同架构和数据集上表现一致，具有较好的跨任务鲁棒性。
- 总体而言，基于激活模式的功能偏离监测为联邦学习提供了一种轻量级、可解释的客户端信任度评估手段，可用于指导加权或过滤聚合。

### 7. 优点

- **新颖且结构化的视角**：从激活模式定义的输入空间几何划分出发，衡量客户端与全局模型的功能偏离，超越了传统的参数/梯度比较。
- **计算轻量**：只需对一个小型共享探测集执行前向传播并计算汉明距离，避免复杂梯度计算，适用于资源受限的联邦场景。
- **可解释性强**：度量直接反映模型内部表征的组织方式变化，便于理解客户端行为。
- **通用性好**：在MLP和CNN架构上均有效，且不依赖特定的联邦优化算法，易于集成到现有FL系统中。
- **稳健异常检测**：采用中位数和MAD构造z-分数，对离群值具有天然鲁棒性。

### 8. 不足与局限

- **实验覆盖不充分**：仅在小规模图像数据集和简单模型上验证，缺乏更大规模、更复杂任务（如语言模型）和更多客户端场景的评估。
- **缺乏对比基线**：未与其他异常检测或离群客户端识别方法（如基于梯度异常、损失异常等）进行定量比较，难以证明其优越性。
- **超参数敏感性未分析**：\( \beta \) 固定为1，探测集大小和构造方式的影响未讨论。
- **攻击形式单一**：异常模拟仅采用简单数据扰动，未考虑标签翻转、后门攻击等更复杂的恶意行为。
- **深层衰减因子依赖计算顺序**：层级散度的可微性和对模型深度的鲁棒性需进一步理论分析。
- **未在实际动态环境中验证**：未展示将该信号实时集成到联邦聚合过程中的训练曲线改善，缺乏端到端效益证明。

（完）
