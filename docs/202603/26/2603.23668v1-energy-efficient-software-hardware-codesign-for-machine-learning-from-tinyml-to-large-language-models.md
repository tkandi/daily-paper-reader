---
title: "Energy Efficient Software Hardware CoDesign for Machine Learning: From TinyML to Large Language Models"
title_zh: 面向机器学习的高能效软硬件协同设计：从 TinyML 到大语言模型
authors: "Mohammad Saleh Vahdatpour, Yanqing Zhang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23668v1"
tags: ["query:mlsys"]
score: 10.0
evidence: 从边缘到数据中心的AI基础设施软硬件协同设计
tldr: 本文综述了从微瓦级TinyML到大规模语言模型（LLM）的能效软硬件协同设计方法。针对数据传输和内存瓶颈，文章探讨了加速器架构、系统级优化及运行时自适应技术，分析了跨平台泛化和搜索空间等挑战，并提出了层次化分解视角，为构建低碳、高效的AI系统提供了实践指南。
selection_source: fresh_fetch
motivation: 随着机器学习规模从边缘扩展到云端，数据搬运和内存瓶颈已取代算力成为制约AI可持续发展的核心能效挑战。
method: 综述了涵盖ASIC/FPGA数据流、存内计算、量化、调度及系统分区等跨层级的软硬件协同设计策略。
result: 总结了当前设计中的关键权衡点，并指出了跨平台泛化能力不足、协同设计搜索空间巨大以及基准测试不一致等行业痛点。
conclusion: 提出了一种层次化分解优化框架，为开发能效与碳排放感知的机器学习系统提供了系统性的指导和演进路径。
---

## 摘要
机器学习在从毫瓦级 TinyML 设备到大语言模型的各类平台上的快速部署，使得能效成为可持续人工智能的主要约束。在这些尺度上，性能和能耗日益受到数据移动和存储系统行为的限制，而不仅仅取决于算术吞吐量。本文综述了涵盖从边缘推理与训练到数据中心级 LLM 服务的能效软硬件协同设计方法，包括加速器架构（如 ASIC/FPGA 数据流、存内处理/存内计算设计）以及系统级技术（如分区、量化、调度和运行时自适应）。我们提炼了常见的设计杠杆和权衡，并强调了反复出现的差距，包括有限的跨平台泛化能力、庞大且昂贵的协同设计搜索空间，以及不同工作负载和部署环境下的基准测试不一致性。最后，我们概述了一种分层分解视角，将优化策略映射到计算角色并支持增量自适应，为构建能源和碳感知的机器学习系统提供了实践指导。

## Abstract
The rapid deployment of machine learning across platforms from milliwatt-class TinyML devices to large language models has made energy efficiency a primary constraint for sustainable AI. Across these scales, performance and energy are increasingly limited by data movement and memory-system behavior rather than by arithmetic throughput alone. This work reviews energy efficient software hardware codesign methods spanning edge inference and training to datacenter-scale LLM serving, covering accelerator architectures (e.g., ASIC/FPGA dataflows, processing-/compute-in-memory designs) and system-level techniques (e.g., partitioning, quantization, scheduling, and runtime adaptation). We distill common design levers and trade-offs, and highlight recurring gaps including limited cross-platform generalization, large and costly co-design search spaces, and inconsistent benchmarking across workloads and deployment settings. Finally, we outline a hierarchical decomposition perspective that maps optimization strategies to computational roles and supports incremental adaptation, offering practical guidance for building energy and carbon aware ML systems.

---

## 论文详细总结（自动生成）

这是一篇关于机器学习（ML）能效优化的深度综述论文，涵盖了从微型机器学习（TinyML）到大语言模型（LLM）的全频谱。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：随着 AI 从毫瓦级的边缘设备普及到兆瓦级的数据中心，**能效**已成为可持续 AI 的首要约束。
*   **研究背景**：
    *   **存储墙（Memory Wall）**：数据在计算单元与存储器之间的移动（而非算术运算本身）成为了功耗和延迟的主要瓶颈。DRAM 访问的能耗比算术运算高出约 200 倍。
    *   **环境挑战**：训练单个 LLM 的碳排放量相当于数辆汽车全生命周期的排放，而推理阶段的排放已占到 LLM 生命周期总排放的一半以上。
    *   **研究意义**：传统的孤立优化（仅软件或仅硬件）已不足够，必须通过**软硬件协同设计（Co-Design）**来打破层级壁垒，实现系统级的能效提升。

### 2. 论文提出的方法论
论文系统性地梳理了跨层级的协同设计策略，并提出了一种**层次化分解（Hierarchical Decomposition）**的统一视角：
*   **核心思想**：将神经网络分为两层。底层负责稳定的特征提取，使用 FPGA 直接求解方程（无需反向传播）；顶层负责自适应推理，应用增量学习。
*   **关键技术细节**：
    *   **算法层**：量化（Quantization）、剪枝（Pruning）、知识蒸馏、N:M 稀疏模式。
    *   **架构层**：存内计算（CiM）以消除数据搬运；FPGA/ASIC 定制数据流（如权重驻留、零缓冲区设计）。
    *   **系统层**：动态电压频率调节（DVFS）、边缘-云端切分推理（Split Computing）、连续批处理（Continuous Batching）及 KV 缓存管理。
*   **算法流程**：通过层次化框架，底层 LLM 在 FPGA 上进行能效优化的表示提取，顶层 LLM 仅对任务相关的子网络进行选择性更新。

### 3. 实验设计与 Benchmark
由于本文是综述性质，它汇总并对比了多项代表性研究的实验结果：
*   **场景与数据集**：涵盖了 TinyML（如深度可分离卷积 DSC）、边缘-云端协作（如 VGG16、Vision Transformers）、以及大规模 LLM（如 LLaMA、Falcon、GPT）。
*   **对比方法（Baseline）**：主要对比了通用 GPU（如 Nvidia A40）、ARM CPU 以及传统的软件实现。
*   **Benchmark 指标**：能效比（TOPS/W）、加速比（Speedup）、功耗降低百分比（% pwr ↓）、服务质量（QoS）。

### 4. 资源与算力
论文引用了多种硬件平台的实验数据，但未进行单一的大规模训练实验：
*   **硬件型号**：Nvidia A40 GPU、树莓派 4 (RPi4)、FPGA (28nm/32nm/KV260)、Cerebras WSE-2（晶圆级系统）。
*   **算力说明**：文中提到 LLM 推理在 GPU 上通过 DVFS 可降低 30% 能耗；FPGA 加速器在处理 Transformer 时比 ARM 提升 7 倍性能。

### 5. 实验数量与充分性
*   **实验覆盖**：论文引用了从 2016 年到 2025 年的数十项代表性工作（见 Timeline 图表），涵盖了 ASIC、FPGA、CiM、模拟/数字混合等多种架构。
*   **充分性与客观性**：实验数据涵盖了从子毫瓦到兆瓦级的极端跨度，对比了不同任务（语言理解 vs 算术推理）下的表现。论文客观地指出，虽然 FPGA/CiM 在能效上有 16-59 倍的优势，但在绝对吞吐量上 GPU 仍具优势。

### 6. 主要结论与发现
*   **内存访问是元凶**：在所有尺度上，内存访问始终是能耗的主要来源。TinyML 的零缓冲区设计可减少 87% 的数据移动。
*   **协同设计的优势**：FPGA 和 CiM 架构在功耗受限的部署中表现远超 GPU。例如，HASTILY 架构在 CiM 中加速 Softmax，能效比 A40 GPU 高出 16-36 倍。
*   **非线性权衡**：能效并不随模型规模线性变化。在边缘-云端场景下，小模型（如 MobileNet）全边缘运行最省电，而大模型（如 ViT）则受益于切分推理。

### 7. 优点（亮点）
*   **跨尺度视角**：罕见地将 TinyML 与 LLM 放在统一的能效框架下讨论，识别出了共同的“存储墙”挑战。
*   **前瞻性建议**：提出了层次化分解的“绿色协同设计”视角，为未来构建可扩展且能效感知的 AI 系统提供了理论路径。
*   **多维度评估**：不仅关注能耗，还提到了水资源消耗、碳足迹等更广泛的可持续性指标。

### 8. 不足与局限
*   **泛化性挑战**：针对 CNN 优化的 TinyML 技术（如零缓冲区）难以直接迁移到具有不规则访问模式的 Transformer 架构。
*   **搜索空间巨大**：软硬件协同设计的参数空间（量化、数据流、并行度等）极其庞大，目前仍高度依赖专家经验，缺乏全自动化的工具链。
*   **基准测试不一致**：许多评估基于理想化的实验室环境（如稳定的网络），低估了现实部署中的重构开销和环境波动。
*   **安全风险**：论文指出，极端的能效优化（如低精度运算）可能会增加侧信道攻击和对抗性攻击的风险。

（完）
