---
title: "Swarm: Co-Activation Aware KVCache Offloading Across Multiple SSDs"
title_zh: Swarm：跨多块 SSD 的协同激活感知 KVCache 卸载
authors: "Tuowei Wang, Liyun Chu, Ruwen Fan, Ju Ren"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17803v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 大模型跨多SSD的KVCache卸载系统
tldr: 针对大语言模型推理中KV Cache带来的显存压力，本文提出Swarm系统。针对SSD卸载方案中带宽受限的问题，Swarm利用KV Cache共激活特性，即特定KV条目常被共同访问的规律，通过离线聚类和多SSD图布局策略实现并行I/O。运行时通过负载均衡检索和动态调整，显著提升了SSD带宽利用率并降低了I/O延迟。
selection_source: fresh_fetch
motivation: 传统的SSD卸载方案受限于单设备带宽和PCIe吞吐量，难以满足大规模LLM推理的高带宽需求。
method: 利用KV Cache共激活特性进行离线聚类，并通过多SSD图布局与选择性副本机制实现高效的并行I/O访问。
result: 实验结果显示，Swarm成功将I/O时间缩短了2.41倍，并将有效带宽利用率提升了2.72倍。
conclusion: Swarm证明了通过感知数据访问相关性并利用多SSD并行性，可以有效克服SSD在KV Cache卸载中的带宽瓶颈。
---

## 摘要
键值（KV）缓存已成为大语言模型（LLM）推理中内存消耗的主要来源。尽管将 KVCache 从 GPU 高带宽内存（HBM）卸载到 CPU DRAM 可以缓解设备内存压力，但对于大规模、持久性的工作负载，DRAM 的容量仍然受限且成本高昂。固态硬盘（SSD）提供了一种具有成本效益的替代方案，但由于 PCIe 吞吐量受限以及单设备带宽限制，朴素的基于 SSD 的分页机制在根本上受到带宽限制。在本文中，我们观察到真实工作负载中的 KVCache 激活表现出强且稳定的相关性。我们将这种现象称为 KVCache 协同激活（Co-Activation），即访问一个 KV 条目通常伴随着一组稳定且重复出现的其他 KV 条目。利用这一特性，我们提出了 Swarm，一个基于 SSD 的 KVCache 卸载系统，它将受带宽限制的单设备访问转换为跨多块 SSD 的并行 I/O。具体而言，Swarm 在线下对协同激活的 KV 条目进行聚类，并使用基于图的放置策略和选择性复制将生成的簇分布在多块 SSD 上，以最大化并行 I/O 带宽。在运行时，Swarm 执行负载均衡的簇检索，并动态调整聚类和缓存决策，以在不断变化的访问模式下维持高带宽利用率。评估结果表明，Swarm 将 I/O 时间缩短了 2.41 倍，并将有效带宽利用率提高了 2.72 倍。

## Abstract
The key-value (KV) cache has become the dominant contributor to memory consumption in large language model (LLM) inference. Although offloading KVCache from GPU high-bandwidth memory (HBM) to CPU DRAM alleviates device memory pressure, DRAM remains capacity-limited and costly for large, persistent workloads. Solid-state drives (SSDs) provide a cost-effective alternative, but naive SSD-based paging is fundamentally bandwidth-bound due to limited PCIe throughput and per-device bandwidth constraints.   In this paper, we observe that KVCache activations in real-world workloads exhibit strong and stable correlations. We term this phenomenon KVCache Co-Activation, where accessing a KV entry is often accompanied by a stable and recurring set of other KV entries. Leveraging this property, we present Swarm, an SSD-based KVCache offloading system that converts bandwidth-bound single-device access into parallel I/O across multiple SSDs. Specifically, Swarm clusters co-activated KV entries offline and distributes the resulting clusters across SSDs using graph-based placement with selective replication to maximize parallel I/O bandwidth. At runtime, Swarm performs load-balanced cluster retrieval and dynamically adapts clustering and caching decisions to sustain high bandwidth utilization under evolving access patterns. Evaluations show that Swarm reduces I/O time by 2.41x and improves effective bandwidth utilization by 2.72x.

---

## 论文详细总结（自动生成）

### Swarm: 跨多块 SSD 的协同激活感知 KVCache 卸载系统总结

#### 1. 核心问题与研究动机
*   **核心问题**：在大语言模型（LLM）推理中，KV Cache（键值缓存）随序列长度增长而急剧膨胀，成为显存瓶颈。现有的卸载（Offloading）方案中，GPU HBM 容量极小，CPU DRAM 成本高且容量有限，而 SSD 虽然容量大、成本低，但受限于 PCIe 接口和单设备性能，其**带宽不足**成为制约推理速度的致命瓶颈。
*   **研究动机**：作者观察到 LLM 推理中的 KV Cache 激活并非随机，而是存在**协同激活（Co-Activation）**现象——即某些 KV 条目总是倾向于被同时访问。如果能利用这种相关性，将协同激活的条目分布到多块 SSD 上并行读取，就能突破单盘带宽限制。

#### 2. 方法论与关键技术
Swarm 系统分为离线和在线两个阶段：
*   **离线阶段（Offline Phase）**：
    *   **协同激活聚类**：通过分析历史访问模式，构建 KV 条目间的相关性图。使用启发式算法选择“中心点（Medoid）”并进行簇扩张。特别地，Swarm 允许**选择性副本（Selective Replication）**，即一个 KV 条目可以属于多个簇，以处理复杂的重叠激活模式。
    *   **分层布局与放置**：将中心点索引、本地窗口条目和热点簇保留在 DRAM 中；其余簇采用 Round-robin（轮询）策略均匀分布在多块 SSD 上，确保检索任一簇时都能触发多盘并行 I/O。
*   **在线阶段（Online Phase）**：
    *   **负载均衡调度**：运行时根据查询预测激活的簇，合并条目并去重，根据 SSD 负载情况动态调度读取请求，最大化聚合带宽。
    *   **动态调整机制**：随着推理进行，新生成的 KV 条目会根据其与现有簇中心点的距离动态归类；同时基于“成本效益评分”动态更新 DRAM 缓存内容。
    *   **系统级优化**：利用 Linux `io_uring` 实现异步非阻塞 I/O，并设计了计算与 I/O 重叠的流水线预取机制。

#### 3. 实验设计
*   **数据集/场景**：WikiText（语言建模）、LongBench（长文本理解）、MMLU（多任务知识评估）、GSM8K（数学推理）。
*   **Benchmark 模型**：Qwen3 (14B/32B/235B MoE)、Llama3.1 (70B)、GPT-OSS (120B)。
*   **对比方法**：
    1.  **No Cluster**：朴素的 SSD 存储，无聚类索引。
    2.  **InfLLM**：基于固定块划分的上下文管理。
    3.  **PQCache**：基于乘积量化（K-means）的聚类方法。
*   **主要指标**：吞吐量（TPS）、I/O 带宽利用率、模型准确率、I/O 延迟。

#### 4. 资源与算力
*   **硬件配置**：
    *   **CPU**：Intel Xeon Gold 6530 (64核)。
    *   **GPU**：8块 NVIDIA H20 (每块 96GB HBM)。
    *   **内存**：1TB DDR5 DRAM。
    *   **存储**：最多 8 块 NVMe SSD（包括高性能 Samsung PM9A3 和低延迟 Intel Optane 900P）。
*   **实现**：使用 Python 和 C++ 编写，核心代码约 2000 余行。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 14B 到 235B 不同规模、不同架构（Dense 和 MoE）的模型，测试了从 16K 到 1M 的超长上下文。
*   **消融实验**：非常充分。分别验证了聚类算法（中心点/副本）、SSD 放置策略、DRAM 缓存策略、在线调度算法以及动态更新机制的有效性。
*   **敏感性分析**：针对 SSD 数量（1-8块）、SSD 类型、前缀长度、稀疏比例等变量进行了详尽测试。实验设计客观，对比基准具有代表性。

#### 6. 主要结论与发现
*   **性能提升**：Swarm 平均减少了 **2.41倍** 的 I/O 时间，有效带宽利用率提升了 **2.72倍**。
*   **带宽突破**：在 8 块 SSD 环境下，Swarm 达到了 **37.67 GB/s** 的聚合带宽，这一数值已接近 GPU HBM 与 CPU DRAM 之间的 PCIe 通信带宽。
*   **准确率保持**：相比于其他稀疏激活方案，Swarm 的聚类策略能更精准地捕捉重要条目，准确率最接近 Oracle（全量访问）水平。

#### 7. 优点
*   **视角独特**：敏锐捕捉到了 KV Cache 访问的“协同激活”这一物理特性，并将其转化为存储系统的并行性。
*   **软硬协同**：不仅有算法层面的聚类优化，还针对 Linux 内核 I/O 接口和多盘拓扑进行了深度系统编程。
*   **扩展性强**：性能随 SSD 数量增加表现出良好的线性扩展性，为低成本实现超长上下文推理提供了可行路径。

#### 8. 不足与局限
*   **离线依赖**：聚类依赖于离线 Profiling，如果实际推理任务的分布与预训练/测试分布差异极大，聚类效果可能受限。
*   **硬件门槛**：该系统优势建立在“多块 SSD”基础上，对于单盘设备（如普通 PC）提升有限。
*   **存储冗余**：为了提高并行度引入了副本机制，虽然文中称开销可控，但在存储极端受限的场景下仍需权衡。

（完）
