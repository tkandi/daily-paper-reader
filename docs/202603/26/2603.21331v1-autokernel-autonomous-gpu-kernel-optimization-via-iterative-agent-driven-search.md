---
title: "AutoKernel: Autonomous GPU Kernel Optimization via Iterative Agent-Driven Search"
title_zh: AutoKernel：基于迭代智能体驱动搜索的自主 GPU 算子优化
authors: "Jaber Jaber, Osama Jaber"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21331v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 针对PyTorch模型的自动GPU算子优化
tldr: AutoKernel是一个开源框架，旨在解决高性能GPU算子开发的高人力成本问题。它通过自主智能体循环，针对PyTorch模型进行性能剖析，并利用Amdahl定律识别瓶颈。该系统通过数百次迭代实验自动优化Triton或CUDA C++算子，并结合五阶段正确性验证确保可靠性。在H100上，其优化算子性能显著优于PyTorch原生及torch.compile，在多个核心算子上实现了数倍的加速。
selection_source: fresh_fetch
motivation: 编写高性能GPU算子是机器学习系统工程中极度耗费人力的任务。
method: 利用自主智能体驱动的迭代搜索循环，结合性能剖析、瓶颈排序及五阶段正确性验证体系自动优化算子实现。
result: 在H100上，AutoKernel在RMSNorm和Softmax等算子上分别实现了比PyTorch原生高5.29倍和2.82倍的加速，且性能优于torch.compile。
conclusion: AutoKernel通过自动化迭代优化流程，显著提升了GPU算子性能并降低了人工开发成本，具有极高的实用价值。
---

## 摘要
编写高性能 GPU 算子是机器学习系统工程中最耗费人力的任务之一。我们提出了 AutoKernel，这是一个开源框架，它将自主智能体循环应用于任意 PyTorch 模型的 GPU 算子优化。给定一个模型，AutoKernel 会对其进行性能分析以识别计算瓶颈，根据阿姆达尔定律的影响力对其进行排名，并在无需人工干预的情况下，通过数百次实验迭代优化 Triton 或 CUDA C++ 算子实现。一个包含冒烟测试、形状扫描、数值稳定性、确定性验证和边缘情况覆盖的五阶段正确性测试框架，确保在记录任何加速效果之前，每个候选算子都经过了验证。该系统包含超过 9,000 行 Python 代码、跨两个后端的 18 个初始算子实现、一套六级优化策略指南，并集成了 KernelBench 基准测试套件。AutoKernel 涵盖了现代 Transformer 架构中主流操作的九种算子类型。在 NVIDIA H100 上，我们的 Triton 算子在大多数测试配置中均优于 PyTorch eager 模式和 torch.compile (max-autotune)：在 RMSNorm 上比 eager 模式快 5.29 倍，在 softmax 上快 2.82 倍，在交叉熵上快 2.21 倍；同时分别比 torch.compile 快 2.83 倍、3.44 倍和 2.94 倍。在社区部署中，经 AutoKernel 优化的算子在 vectorsum_v2 B200 排行榜上获得了第一名。完整系统可在 https://github.com/RightNow-AI/autokernel 获取。

## Abstract
Writing high-performance GPU kernels is among the most labor-intensive tasks in machine learning systems engineering. We present AutoKernel, an open-source framework that applies an autonomous agent loop to GPU kernel optimization for arbitrary PyTorch models. Given a model, AutoKernel profiles it to identify computational bottlenecks, ranks them by Amdahl's law impact, and iteratively refines Triton or CUDA C++ kernel implementations through hundreds of experiments without human intervention. A five-stage correctness harness covering smoke tests, shape sweeps, numerical stability, determinism verification, and edge-case coverage ensures every candidate kernel is validated before any speedup is recorded. The system comprises over 9,000 lines of Python, 18 starter kernel implementations across two backends, a six-tier optimization playbook, and integration with the KernelBench benchmark suite. AutoKernel covers nine kernel types spanning the dominant operations in modern transformer architectures. On an NVIDIA H100, our Triton kernels outperform both PyTorch eager and torch.compile (max-autotune) on the majority of tested configurations: 5.29x over eager on RMSNorm, 2.82x on softmax, and 2.21x on cross-entropy, while beating torch.compile by 2.83x, 3.44x, and 2.94x respectively. In community deployment, an AutoKernel-optimized kernel achieved first place on the vectorsum_v2 B200 leaderboard. The full system is available at https://github.com/RightNow-AI/autokernel.

---

## 论文详细总结（自动生成）

### AutoKernel: 基于迭代智能体驱动搜索的自主 GPU 算子优化总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
编写高性能 GPU 算子（Kernel）是机器学习工程中最耗时且门槛极高的任务。尽管有 cuBLAS 等供应商库，但深度学习架构的快速创新（如 SwiGLU、RMSNorm、RoPE）往往领先于官方库的支持。现有的 LLM（大语言模型）在单次生成高性能算子方面表现不佳（如 KernelBench 评估显示其性能匹配率不足 20%）。
**核心问题**：如何利用 AI 智能体自动化这一过程，减少对人类专家的依赖，并针对特定模型和硬件实现超越通用编译器（如 `torch.compile`）的性能。

#### 2. 论文提出的方法论
AutoKernel 采用了一种**自主智能体驱动的迭代搜索循环**，其核心思想是模拟专家的“编写-测试-改进”流程。
*   **核心流程（三阶段）**：
    1.  **Phase A（性能剖析）**：分析 PyTorch 模型，识别耗时最长的算子，并根据**阿姆达尔定律（Amdahl's Law）**对优化优先级进行排序。
    2.  **Phase B（迭代优化循环）**：智能体修改算子代码 -> 运行五阶段正确性验证 -> 测量吞吐量 -> 根据结果决定保留（Keep）或回滚（Revert）。
    3.  **Phase C（端到端验证）**：验证优化后的算子在完整模型中的正确性和实际加速比。
*   **关键技术细节**：
    *   **六级优化策略指南（Playbook）**：为智能体提供结构化指令，涵盖分块大小调整、内存访问优化（合并加载、预取）、计算优化（算子融合）、高级技术（Split-K、持久化算子）等。
    *   **五阶段正确性测试框架**：包括冒烟测试、多形状扫描（10+配置）、数值稳定性（对抗输入）、确定性验证（位一致性）和边缘情况（非 2 的幂维度）。
    *   **双后端支持**：同时支持 Triton（快速迭代）和 CUDA C++（底层控制）。

#### 3. 实验设计
*   **硬件环境**：主要在 NVIDIA H100 80GB GPU 上进行测试。
*   **对比基准（Baselines）**：
    1.  **PyTorch Eager 模式**：使用官方 cuBLAS 或 ATen 算子。
    2.  **torch.compile (max-autotune)**：PyTorch 2.0 的官方编译器优化模式。
*   **测试场景**：涵盖 9 种核心算子类型（Matmul, Softmax, LayerNorm, RMSNorm, CrossEntropy 等），并集成了 **KernelBench**（250 个标准化问题）。
*   **社区验证**：在 B200 向量求和挑战赛中获得第一名，并在 FP4 Matmul 任务中对比 NVIDIA 的 CUTLASS 库。

#### 4. 资源与算力
*   **算力型号**：NVIDIA H100 80GB (HBM3)。
*   **时间成本**：单次迭代约需 **90 秒**（30s 验证、30s 基准测试、30s 智能体推理）。
*   **实验规模**：一次典型的“过夜运行”（约 10 小时）可完成 **300 到 400 次** 实验迭代。

#### 5. 实验数量与充分性
*   **实验规模**：论文对 7 种算子类型的 34 种生产环境配置进行了详细测试。
*   **充分性**：实验涵盖了从微小形状到超大形状（如 8192 维度）的全面扫描，并使用了三种数据类型（FP16, BF16, FP32）。
*   **客观性**：通过五阶段严苛的正确性验证（尤其是对抗输入和位一致性测试），确保了性能提升不是以牺牲精度或稳定性为代价的。

#### 6. 论文的主要结论与发现
*   **显著加速**：在 H100 上，AutoKernel 优化的 Triton 算子在 RMSNorm 上比 Eager 模式快 **5.29 倍**，比 `torch.compile` 快 **2.83 倍**。
*   **访存密集型算子获益最大**：Softmax 和 Cross-Entropy 等算子通过融合减少了 HBM 读写，达到了硬件峰值带宽的 80% 以上。
*   **超越专家库**：在特定任务（如 FP4 Matmul）中，智能体生成的 Triton 算子性能超过了手写的 CUTLASS 库（加速比 1.63x - 2.15x）。
*   **自动化价值**：将原本需要专家数周完成的调优工作缩短至数小时的自主运行。

#### 7. 优点
*   **实用性强**：直接针对完整的 PyTorch 模型进行剖析和优化，而非孤立的算子。
*   **可靠性高**：五阶段正确性验证体系非常严密，有效防止了 AI 生成代码中常见的边缘情况错误。
*   **架构简洁**：避免了复杂的多智能体协作，采用简单的“保留/回滚”循环，透明度高且易于调试。
*   **开源贡献**：提供了 9,000 多行代码和丰富的初始算子库。

#### 8. 不足与局限
*   **LLM 能力上限**：对于极度复杂的底层优化（如自定义 PTX 指令、复杂的软件流水线），智能体可能仍难以达到人类顶尖专家的水平。
*   **单卡限制**：目前主要针对单 GPU 算子优化，尚未涵盖分布式算子或多设备内存管理。
*   **搜索空间效率**：虽然有策略指南，但迭代搜索仍带有一定的随机性，对于某些计算密集型算子（如 Matmul），可能需要更多迭代才能逼近 cuBLAS 的性能。

（完）
