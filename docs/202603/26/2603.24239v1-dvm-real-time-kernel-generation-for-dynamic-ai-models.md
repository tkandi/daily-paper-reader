---
title: "DVM: Real-Time Kernel Generation for Dynamic AI Models"
title_zh: DVM：面向动态 AI 模型的实时算子生成
authors: "Jingzhi Fang, Xiong Gao, Renwei Zhang, Zichun Ye, Lei Chen, Jie Zhao, Chengnuo Huang, Hui Xu, Xuefeng Jin"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24239v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 动态AI模型的实时算子编译器
tldr: 针对AI模型中动态形状和控制流导致的编译效率问题，本文提出实时编译器DVM。DVM通过基于字节码虚拟机的运行时算子编译器，将算子程序在CPU上编码为字节码并在NPU上直接解码执行，结合符号推导和运行时算子融合技术，显著降低了编译开销并提升了模型执行效率，解决了传统编译器在动态模型上的性能瓶颈。
selection_source: fresh_fetch
motivation: 现有编译器在处理动态AI模型时，面临运行时编译耗时长或离线编译优化不足、内存占用大的矛盾。
method: 提出基于字节码虚拟机的实时编译器，通过CPU编码字节码、NPU解码执行，并结合符号推导实现动静结合的算子融合。
result: 实验表明，DVM在算子和模型效率上最高提升11.77倍，且最大编译速度比现有方案快5个数量级。
conclusion: DVM通过创新的虚拟机架构和融合策略，为动态AI模型的高效实时编译和执行提供了可行且高性能的解决方案。
---

## 摘要
动态性在 AI 计算中非常普遍，例如模型中的动态张量形状和动态控制流。由于编译时间较长，现有的运行时编译会损害模型效率，而离线编译器为了覆盖动态模型所有可能的执行实例，要么面临漫长的编译时间和巨大的设备内存占用，要么为了易用性而牺牲优化机会。在本文中，我们重新思考了动态模型运行时编译的可行性，并指出其成功的关键在于加速编译或隐藏编译开销。为此，我们提出了一种实时编译器 DVM。在 DVM 中，我们设计了一个基于字节码虚拟机的运行时算子编译器，以便根据输入为每个动态算子实例执行有效且高效的编译。具体而言，我们不是将程序编译为机器码，而是在 CPU 上将算子程序编码为字节码，并将字节码解码为虚拟指令，以便在 NPU 上直接执行。基于该运行时算子编译器，我们进一步提出了一种算子融合器，它在静态图上执行基于符号推导的融合，在动态图上执行运行时融合。支持基于模式（pattern-based）和基于堆叠（stacking-based）的融合，以增加融合机会。对算子、子图和模型的评估表明，与 TorchInductor、PyTorch-eager 和 MindSpore-graph-O0 相比，我们在算子/模型效率方面提升高达 11.77 倍，在最大编译时间方面快达 5 个数量级。

## Abstract
Dynamism is common in AI computation, e.g., the dynamic tensor shapes and the dynamic control flows in models. Due to the long compilation time, existing runtime compilation damages the model efficiency, while the offline compilers either suffer from the long compilation time and device memory footprint to cover all the possible execution instances of a dynamic model, or sacrifice optimization opportunities for usability. In this paper, we rethink the feasibility of runtime compilation for dynamic models and identify that the key for it to work is to speed up the compilation or hide the compilation overhead. To do this, we propose a real-time compiler, DVM. In DVM, we design a runtime operator compiler based on a bytecode virtual machine to perform effective and efficient compilation for each dynamic operator instance given its input. Specifically, instead of compiling programs into machine code, we encode the operator program into bytecode on the CPU and decode the bytecode into virtual instructions for direct execution on the NPU. Based on the runtime operator compiler, we further propose an operator fuser, which performs symbol-deduction-based fusion on static graphs and runtime fusion on dynamic graphs. Both pattern- and stacking-based fusion are supported to increase fusion opportunities. Evaluation on operators, subgraphs, and models shows that, compared with TorchInductor, PyTorch-eager and MindSpore-graph-O0, we are up to 11.77$\times$ better in terms of the operator/model efficiency and up to 5 orders of magnitude faster in terms of the maximum compilation time.

---

## 论文详细总结（自动生成）

以下是对论文《DVM: Real-Time Kernel Generation for Dynamic AI Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于 AI 模型中的**动态性问题**（如大语言模型中波动的序列长度导致的动态张量形状，以及条件分支导致的动态控制流）。
*   **现有痛点：** 传统的运行时编译（JIT）如 TorchInductor 编译开销巨大（有时比算子运行慢 5 个数量级）；而离线编译或预编译内核（如 Bucketing 或 Micro-kernels）在面对海量形状组合时，要么内存占用过高，要么因泛化性差而牺牲了优化机会。
*   **核心目标：** 重新思考运行时编译的可行性，通过**加速编译过程**和**隐藏编译开销**，实现动态模型的高效实时内核生成。

### 2. 方法论
DVM 提出了一种基于**字节码虚拟机（VM）**的实时编译器架构：
*   **运行时算子编译器：** 
    *   **字节码编码：** 在 CPU 上将算子计算逻辑编码为轻量级字节码（Bytecode），而非复杂的机器码。
    *   **形状平铺（Shape Tiling）：** 采用硬件对齐的轻量级平铺算法，根据当前实例的精确形状划分计算任务。
    *   **虚拟机执行：** 在 NPU 的 Scalar 单元上解码字节码，并分发给 Vector/Cube 单元执行预定义的虚拟指令函数。
*   **算子融合器（Operator Fuser）：**
    *   **静态图融合：** 基于符号推导（Symbolic Deduction）判断融合条件。
    *   **动态图流式融合：** 引入算子缓冲区实现“延迟执行”，在运行时根据实际执行路径和形状进行流式融合。
    *   **融合策略：** 支持模式融合（如向量-向量、矩阵-向量）和堆叠融合（空间/时间维度的任务堆叠）。
*   **开销隐藏：** 利用 NPU 硬件多单元并行的特性，将字节码解码（标量计算）与指令执行（向量/矩阵计算）流水线化，从而掩盖编译耗时。

### 3. 实验设计
*   **测试场景：** 
    *   **算子级：** 动态 `matmul`。
    *   **子图级：** `LayerNorm`、`addmm`、带有控制流的 `if-else-add`。
    *   **模型级：** BERT（训练）、MMoE（推理）、Qwen3-14B 和 Llama3.1-8B（微调）。
*   **Benchmark（对比方法）：** 
    *   **PyTorch-eager：** 无自动融合的算子下发模式。
    *   **TorchInductor-recompile：** 针对每个新形状重新触发完整编译。
    *   **TorchInductor-dynamic：** TorchInductor 的动态形状模式。
    *   **MindSpore-graph-O0：** MindSpore 的基础图模式。

### 4. 资源与算力
*   **硬件平台：** 主要使用华为昇腾（Ascend）NPU。
    *   具体型号包括 **Ascend 910B2** 和 **Ascend 910**。
    *   宿主机配备 4 台 64 核鲲鹏 920 CPU，2TB RAM。
*   **分布式规模：** Qwen3-14B 使用 8 卡模型并行；Llama3.1-8B 使用 8 卡（2 路数据并行 + 4 路模型并行）。
*   **软件环境：** openEuler 22.03, CANN 8.5.0, PyTorch 2.1/MindSpore 2.7/2.8。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从基础算子到主流大模型的完整测试链路。针对动态算子/子图，随机采样了 **60 组** 不同的形状进行测试。
*   **充分性：** 实验对比了工业界最先进的动态编译器实现（如 TorchInductor），并考虑了 L2 缓存清理以确保公平性。模型级实验包含了训练、推理和微调多种负载，具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升：** DVM 在算子和模型执行效率上比基准方法最高提升 **11.77 倍**。
*   **编译提速：** DVM 的编译时间（毫秒级）比 TorchInductor 快 **5 个数量级**。
*   **开销占比：** DVM 的编译开销极低，通常仅占总运行时间的 **0.3% 到 2.2%** 之间。
*   **动态性优势：** 在处理复杂的 `if-else` 分支和 LLM 变长序列时，DVM 能够实时生成最优内核，避免了传统 JIT 的频繁重编译抖动。

### 7. 优点
*   **架构创新：** 巧妙地将虚拟机架构引入算子编译，绕过了传统编译器后端（如 LLVM）生成机器码的漫长过程。
*   **极致轻量：** 字节码设计极大地降低了内存占用，无需缓存海量的二进制内核文件。
*   **动静结合：** 流式融合技术解决了动态控制流下难以进行跨分支优化的难题。
*   **硬件协同：** 深度利用了昇腾 NPU 的异步并行架构，实现了编译与执行的完美重叠。

### 8. 不足与局限
*   **硬件绑定：** 目前的设计深度依赖昇腾 NPU 的特定硬件特性（如标量/向量单元并行），在其他架构（如 GPU）上的通用性有待验证。
*   **融合深度：** 论文中提到的矩阵-向量融合目前主要支持简单的后处理算子，对于更复杂的算子链（如多矩阵运算融合）支持情况尚不明确。
*   **软件集成度：** 部分高级功能（如动态图融合器）在某些框架后端（如 torch-npu）中尚未完全自动化集成，仍需手动触发部分逻辑。

（完）
