Title: SVD Contextual Sparsity Predictors for Fast LLM Inference

URL Source: https://arxiv.org/pdf/2603.14110v1

Published Time: Tue, 17 Mar 2026 01:54:29 GMT

Number of Pages: 21

Markdown Content:
# SVD Contextual Sparsity Predictors for Fast LLM Inference 

Georgii Serbin 1 Kirill Koshkin 2 Zhongao Sun 1 Anastasiya Bistrigova 1 2 C.C. Korikov 1

## Abstract 

Contextual sparsity is one of the approaches used to reduce computational complexity in the inference process of large language models (LLMs). Existing techniques for efficient LLM inference acceleration based on contextual spar-sity with minimal accuracy degradation require training sparse pattern predictors. This paper presents a framework for accelerating inference of ReGLU-based feed-forward networks (FFNs) within LLMs. The proposed framework provides a fast, training-free method for building sparse pattern predictors using truncation-aware singular value decomposition (SVD) of the gate projection matrix, along with a threshold calibration algo-rithm, and inference executors supporting condi-tional computation on CUDA and CANN devices. Experiments on three sparse LLMs with an aver-age activation sparsity level of 90% in the FFNs demonstrate up to a 1.8 × reduction in end-to-end decoding time while maintaining less than 1% degradation in benchmark scores on tasks in-volving complex math and code generation. This work advances the deployment of LLMs on edge devices. 

## 1. Introduction 

The remarkable capabilities of LLMs have led to their widespread integration across diverse domains (Brown et al., 2020; Dong et al., 2024b). While on-device AI deployment offers advantages in privacy, latency, and cost compared to server-based solutions (Shankar, 2024), the substantial computational requirements of LLM inference present sig-nificant challenges for edge deployment. A key characteristic of ReGLU-based LLMs is dynamic (or contextual) sparsity – the phenomenon where only a small subset of neurons within FFN blocks are activated for any given input. This inherent property enables inference          

> 1Huawei Technologies Ltd. 2Moscow State Univer-sity, Russia. Correspondence to: C.C. Korikov <constan-tine.korikov@gmail.com >.
> Preprint. March 17, 2026.

acceleration by reducing both computational overhead and memory bandwidth requirements during generation (Liu et al., 2023). A prevalent approach for leveraging contextual sparsity in-volves employing lightweight, learnable sparsity predictors (Liu et al., 2023). These predictors generate input-specific sparse activation patterns prior to FFN forward passes. The sparse pattern identifies neuron indices that will produce positive dot products with the input vector. In ReGLU FFNs, computations for the remaining neurons can be safely skipped without affecting output correctness, as the activa-tion function nullifies negative values. Nevertheless, pre-diction errors may arise from false negative classifications, which compromise model accuracy. Effective predictors must balance high recall rates with substantial sparsity ra-tios while maintaining sufficient speed to reduce end-to-end (E2E) text generation latency. In this study, we present a simple yet effective technique leveraging low-rank singular value decomposition (SVD) to predict sparse patterns, thereby enabling significant in-ference acceleration in ReGLU-based LLMs. Our method-ology employs low-rank approximations of gate projection weight matrices in each layer as lightweight predictors of the FFN block sparsity mask during generation. Unlike ex-isting contextual sparsity prediction methods, our approach provides theoretical guarantees regarding prediction accu-racy. To enhance the accuracy of SVD-based predictors, we em-ploy a combination of techniques: • Calibrated bias . Direct application of SVD approxi-mations as predictors results in notable accuracy degra-dation. However, we observe that SVD predictors exhibit strong separation capabilities. Introducing neuron-specific thresholds – implemented as bias terms – effectively mitigates this issue. These biases are cal-ibrated offline using a custom greedy algorithm that does not require gradient computation. • Data whitening technique . Given the predictor speed constraints, the rank must be significantly smaller than the hidden dimension. To improve prediction accuracy under these conditions, we apply truncation-aware data whitening (Wang et al., 2025a;b; Yuan et al., 2025). 1    

> arXiv:2603.14110v1 [cs.LG] 14 Mar 2026 SVD Contextual Sparsity Predictors for Fast LLM Inference W
> Truncation-aware
> SVD Approximation Gate Projection Matrix Calibrated Bias
> Offline Stage
> Sparsity Prediction Input
> Activations
> Online Stage
> Predictor Custom Executors For Sparse FFN Output Activations
> Figure 1. Overview of our framework. During the offline stage, sparsity predictors are constructed using low-rank SVD approxi-mation with calibrated per-neuron thresholds. At inference time, SVD-based predictors precede each FFN block to exploit contex-tual sparsity and accelerate generation.

This technique reduces Frobenius norm error by in-corporating input activation statistics, requiring only a small calibration dataset. Implementation-level optimizations further enhance infer-ence speedup: • Sequential computation of gate and up projections .Gate and up projections are computed sequentially, en-abling further acceleration through early elimination of false positive predictions. After computing gate projections, falsely activated neurons are identified and excluded from subsequent up and down projec-tions, thereby increasing sparsity levels in the final FFN stages. • Inference executors . Our implementations support conditional computation on CUDA (NVIDIA Corpo-ration, 2024) and CANN (Huawei Technologies Co., Ltd., b) devices. For CUDA, we employ custom ker-nels rather than Triton-based abstractions (Tillet et al., 2019), prioritizing enhanced low-level control. On Huawei Ascend hardware (Liao et al., 2019), compara-ble performance gains are achieved exclusively through high-level PyTorch APIs, demonstrating effective uti-lization of the platform’s native software stack. Figure 1 illustrates our framework architecture. Our key contributions are summarized as follows: 1. We propose training-free SVD-based contextual spar-sity predictors for ReGLU-based LLM inference accel-eration; 2. We introduce calibrated bias terms that significantly im-prove prediction accuracy, with biases tuned on small datasets using a custom gradient-free algorithm; 3. We develop executors supporting sparse FFN inference on CUDA and CANN devices; 4. We achieve up to 1.8 × end-to-end inference speedup with minimal accuracy degradation. 

## 2. Related Work 

Sparsification is one of the most effective paradigms for ac-celerating LLM inference, typically categorized into static 

and dynamic sparsity methods. 

Static sparsity removes redundant parameters before de-ployment (Ashkboos et al., 2024), reducing computation and memory costs but often sacrificing accuracy at high sparsity levels. In contrast, dynamic sparsity – also known as dynamic pruning or contextual sparsity – adapts sparsity patterns at runtime based on input context, enabling flexible efficiency-accuracy trade-offs. 

Predictor-based dynamic sparsity methods, such as Deja Vu (Liu et al., 2023) and ShadowLLM (Akhauri et al., 2024), learn lightweight neural predictors to estimate layer-wise sparsity. ShadowLLM enhances this approach by incorpo-rating gradient information during training and utilizing a single predictor for all layers. 

Training-free dynamic sparsity eliminates learned predic-tors and instead derives sparsity from activation statistics or calibrated thresholds. GRIFFIN (Dong et al., 2024a) selects active “expert” neurons using prefill activations, while TDA (Ma et al., 2024) introduces offline-calibrated thresholds allowing adaptive activation per layer. Both maintain fixed sparsity patterns within a prompt. CLADA (Yang et al., 2025) updates sparsity during generation but requires re-computation of previous states, hindering KV-cache reuse. TEAL (Liu et al., 2024) exploits input sparsity, which differs from activation sparsity in that it arises from redundancy in the input representations rather than neuron activations, using calibrated thresholds. SparseInfer (Shin et al., 2025) proposes a calibrated solution for ReLU-fied LLMs using sign-bit comparisons, which lacks theoretical justifications. Dynamic sparsity can also underpin efficient on-device infer-ence frameworks. PowerInfer (Song et al., 2024b) acceler-ates LLMs on consumer GPUs by preloading frequently acti-vated neurons into GPU memory, minimizing CPU transfers. It achieves up to 11.69 × speedup, particularly in CPU/GPU hybrid settings, whereas our work targets homogeneous 2SVD Contextual Sparsity Predictors for Fast LLM Inference 

GPU inference with batch size one. The effectiveness of dynamic sparsity depends heavily on activation functions. ReLU activations naturally induce sparse activations, unlike smoother functions such as Swish or SiLU used in modern LLMs (Zhang et al., 2024). Recent studies show that replacing them with ReLU variants plus fine-tuning recovers substantial sparsity (Mirzadeh et al., 2023). ProSparse (Song et al., 2024a) achieved ∼90% spar-sity in LLaMA2-7B through progressive regularization and activation threshold shifting, while TurboSparse (Song et al., 2024c) used double ReLU (dReLU) activation function to sparsify models like Qwen2-7B and Mistral-7B. Beyond pruning, SVD offers complementary compression capabilities. ASVD (Yuan et al., 2025) leverages activation-aware transformations to attain 10–30% model compression, and SVD-LLM (Wang et al., 2025b) provides theoretical connections between discarded components and L2 loss. SparseLoRA (Khaki et al., 2025) integrates SVD-based es-timators into parameter-efficient fine-tuning, dynamically slicing weights by input relevance. However, sparse compu-tations are applied only to context tokens (the prefix tokens provided as input), while output tokens (the target tokens used for loss computation) remain densely processed to avoid performance degradation. Achieving accelerated and high-quality decoding of LLMs through contextual sparsity remains an open challenge not addressed by SparseLoRA. 

## 3. Preliminaries 

Consider a ReGLU-activated FFN block during decoding, defined as: FFN (x) = W down  ReLU (W gate x) ⊙ (W up x) , (1) where x ∈ Rd is the input activation vector, and 

W gate , W up ∈ RD×d, and W down ∈ Rd×D are the gate, up-projection, and down-projection matrices, respectively. Here, d denotes the hidden size and D the intermediate size. Let X ∈ Rd×N denote the hidden states of N calibration tokens, and let r denote the predictor rank. For brevity, we omit layer indices. Our objective is to obtain a predictor P that outputs a bi-nary mask m = P (x) ∈ { 0, 1}D over the D intermediate neurons. This mask identifies active neurons by selecting the corresponding rows of W gate and W up , together with their aligned columns of W down , yielding sparse computa-tion algebraically equivalent to the dense FFN due to ReLU sparsity. 

## 4. SVD-Based Sparsity Predictors 

This section introduces training-free SVD-based sparsity predictors (SVDP), designed for efficient inference in ReGLU-based LLMs. Our approach consists of two stages: an offline phase for predictor construction and an online phase for accelerated sparse inference. 

4.1. Training-free Predictor Building 

The predictors are constructed by approximating the gate projection matrix W gate using a low-rank SVD decompo-sition. This approach offers a theoretical upper bound on local error (see Section A.1). We propose predictors of the affine form 

P (x) = H(ABx + b), (2) where H : R → { 0, 1} is the Heaviside step function ap-plied element-wise. H(z) = 1 if z > 0 and 0 otherwise. The product AB approximates W gate , while b adjusts per-neuron decision thresholds. 4.1.1. D ATA -A WARE SVD-B ASED FACTORIZATION 

To improve the accuracy of the SVD approximation, we adopt a data whitening technique. This minimizes the Frobe-nius norm error: 

∥W X − Ur Σr V ⊤ 

> r

X∥F (3) on calibration data X, by incorporating input statistics through a whitening matrix S:

W = ( W S )S−1

= ( U ΣV ⊤)S−1 ≈ Ur Σr V ⊤ 

> r

S−1 = AB. (4) We follow Wang et al. (2025b) in choosing S as the Cholesky decomposition of XX ⊤, which outperforms ASVD in terms of perplexity at low ranks, as shown in Table 2 of Yuan et al. (2025). The predictor construction pipeline is described in Algo-rithm 1 and is performed independently for each layer. Throughout, we fix the predictor rank r across layers (see Section E.3 for a discussion on rank selection). 

Algorithm 1 Building SVD-predictors (Offline Phase) 

Input: W gate ∈ RD×d, X ∈ Rd×N , r ∈ N

Output: A ∈ RD×r, B ∈ Rr×d

S ∈ Rd×d ← Cholesky  XX ⊤

U, Σ, V ← SVD (W gate S)

A ∈ RD×r ← U:,:r Σ:r, :r

B ∈ Rr×d ← V:r, :S−1

return A, B 

4.1.2. T HRESHOLD CALIBRATION 

SVD-based sparsity predictors typically exhibit good sepa-ration between active and inactive neurons; however, naive 3SVD Contextual Sparsity Predictors for Fast LLM Inference 0.30 0.25 0.20 0.15 0.10 0.05 0.00 0.05 

> SVD prediction value
> 0.0
> 2.5
> 5.0
> 7.5
> 10.0
> 12.5
> 15.0
> 17.5
> Density
> Active neuron values
> Inactive neuron values

Figure 2. Distribution of SVD-based predictor outputs for a sample neuron. Although active and inactive states are well separated, zero thresholding causes significant false negatives for truly active neurons. 

thresholding (e.g., at zero) leads to substantial accuracy degradation. This effect arises from a shift in the predictor output distribution induced by truncation of singular value components, as illustrated in Figure 2. To compensate for this distribution shift, a bias term is introduced in the second layer of the SVD-based predictor. The central problem is to calibrate this bias so as to achieve a prescribed sparsity level while limiting the associated accuracy loss. Simple schemes that tune thresholds to match a target sparsity, recall, or accuracy on a calibration set were empirically found to yield suboptimal trade-offs. Instead, a fast greedy calibration algorithm, Algorithm 2, is employed that explicitly accounts for the heterogeneous impact of different neurons on model accuracy. From a computational standpoint, all neurons are equally valuable for acceleration, since pruning any neuron reduces the cost by the same amount, whereas their contributions to output accuracy differ substantially. Consequently, some neurons require high recall, whereas others can tolerate higher false-negative rates without noticeably affecting overall perfor-mance. Under certain conditions, our greedy algorithm is guaranteed to provide an optimal solution (see Section A.2). Let s ∈ (0 , 1] denote the desired sparsity ratio, controlling the speed-accuracy trade-off (see Section E.3). For each neuron i ∈ [D], initialize τi ← min   

> t:w(t)
> i>0

s(t) 

> i

. Then greedily increase sparsity by advancing the neuron with smallest incremental cost Pηj=1 wi, (ki+j) until 

1

DT 

> D

X

> i=1
> T

X

> t=1

1[s(t) 

> i

≤ τi] ≥ s. 

The penalty Ci(ki + η) − Ci(ki) proxies FFN output per-turbation across η samples. Fixed biases b = −τ ∈ RD are used during inference. The proposed predictor is lightweight and data-efficient, requiring only few thousands of tokens for reliable construc-tion and bias estimation. It is fast and memory-efficient, making it suitable for practical use in constrained settings. 

4.2. Runtime Execution 

We modify only the FFNs; the rest of the model remains unchanged. During inference, the prefill stage remains un-changed. At the decoding stage, we leverage CUDA and CANN executors for sparse FFN computation. Figure 3 illustrates the sparse inference pipeline, where speedup is determined by the token-dependent sparsity ratio. 

Sparsity Prediction Our predictor comprises two linear layers, requiring no specialized implementation. Deja Vu employs lookahead predictors to circumvent prediction la-tency. This is enabled via asynchronous computation, where predictor execution time is overlapped with data transfer from global memory or inter-process communication under tensor parallelism. However, such pipelining may be infea-sible on certain hardware platforms (see Section B). Given our focus on local inference for edge deployment scenarios, similar to PowerInfer engine, our predictors are inserted at the beginning of each FFN and sequentially generate sparsity patterns for their respective gate projections. 

Gate/up Execution Order A prevalent optimization for efficient deployment of LLMs with gated FFNs involves parallelized execution of the gate and up projections. While effective in dense computation scenarios, this approach presents non-trivial challenges for local inference systems leveraging sparse activations. We delineate two distinct computational methodologies to address this: • Parallel pipeline e.g., PowerInfer. A predictor first gen-erates a sparsity mask, which is concurrently applied to independently compute both gate and up projections. This maintains identical sparsity levels across both pro-jections during execution. • Sequential pipeline e.g., SparseInfer and our approach. The gate and up projections are computed sequentially, eliminating false positives: neurons predicted as ac-tive are revalidated after gate projection computation, further increasing sparsity in subsequent projections. 

Algorithmic Complexity In our case, realized sparsity equals or exceeds the predicted sparsity s by filtering erro-neous predictions. Thanks to sequential pipeline, we can use more lightweight predictors that provide relatively low sparsity at the gate projection stage without compromising the FFN’s performance. 4SVD Contextual Sparsity Predictors for Fast LLM Inference 

Algorithm 2 Greedy Bias Calibration (Offline Phase) 

Input: 

W gate , W up ∈ RD×d, W down ∈ Rd×D – weight matrices, 

D = {x(t) ∈ Rd}Tt=1 – calibration dataset, 

A, B – low-rank approximation from Algorithm 1, 

s ∈ [0; 1] – desired sparsity level, η – discrete step size. 

Output: b ∈ RD

w(t) 

> i

← ReLU  W gate  

> i, :

x(t) · W up  

> i, :

x(t) 2 W down :,i  

> 22

(Compute neurons’ importance) 

s(t) 

> i

← (ABx (t))i (Compute predictor’s outputs) 

for i = 1 to D do 

si, (1) ≤ · · · ≤ si, (T ) (Sort samples in nondecreasing order) 

Ci(k) = 

Pkj=1 wi, (j), k = 1 , . . . , T Ci(T ), T < k < T + η

+∞, k = T + η

(Compute cumulative costs with padding) 

ki ← min {k : wi, (k) > 0}, τi ← si, (ki) (Initialize pointers and thresholds) 

end for while 1

> DT

PDi=1 

PTt=1 1[s(t) 

> i

≤ τi] < s do 

i⋆ ← arg min i [Ci(k + η) − Ci(k)] (Greedy step) 

ki⋆ ← max( ki⋆ + η, T ), τi⋆ ← si, (ki⋆ ) (Update pointers and thresholds) 

end while 

b ← − τ

return b

A dense FFN requires 3dD multiplications (excluding element-wise product), whereas our sparse approach re-duces this to: 

r(d + D)

| {z }

> predictor

+ d(1 − s)D

| {z }

> gate

+ 2 d(1 − s′)D

| {z }

> up/down

(5) where s is the predicted sparsity and s′ the realized sparsity. For example, in ProSparse-LLaMA2-7B (Song et al., 2024a) (d = 4096 , D = 11008 , s = 0 .5, s′ = 0 .9, r = 256 ), opera-tions decrease by 3.8 ×. Note that hardware-level dynamic sparsity challenges prevent this from directly translating to wall-clock speedup. 

Hardware platforms CUDA and CANN devices are sup-ported for sparse FFN inference. See implementation details in Section B. 

## 5. Experimental Results 

5.1. Experimental Setup Hardware A single consumer-grade NVIDIA RTX 3090 GPU 1 at the MSU Laboratory of Computer Engineering 

> 1

All GPU-based experiments were conducted exclusively by authors affiliated with Moscow State University (Kirill Koshkin) without involvement of Huawei-affiliated researchers. Huawei-affiliated co-authors contributed solely to theoretical analysis, al-gorithm design and CANN performance evaluation. 

is used for both quality and performance evaluation. The inference is performed entirely on GPU. For evaluation on NPU, a single Ascend 310P3 (Huawei Technologies Co., Ltd., a) device is used. 

Software We use the vLLM framework (Kwon et al., 2023), preferring it to the Transformers framework (Wolf et al., 2020) for the experiments, as it better reflects real-world deployment scenarios. Notably, we do not utilize any server-specific features of vLLM, such as tensor parallelism. Inference is performed in half precision, while predictor construction in the offline stage uses double precision. 

Models We evaluate our solution across three open-source sparse 7B language models: ProSparse-LLaMA2-7B (Song et al., 2024a) and two instruction-tuned models – SparseQwen2-7B and TurboSparse-Mistral-Instruct (Song et al., 2024c). The instruction-tuned models incorporate double ReLU (dReLU) activation in their FFN layers: FFN (x) = W down  ReLU  W gate x⊙ReLU (W up x) (6) 

Quality Evaluation We evaluate model performance us-ing a diverse set of open-source benchmarks spanning rea-soning, coding, knowledge, and multilingual understand-ing: GSM8K (Cobbe et al., 2021) (math reasoning), Hu-manEval (Chen et al., 2021) (code generation), ARC-

5SVD Contextual Sparsity Predictors for Fast LLM Inference Input Activations          

> Predictor BA
> Sparsity Prediction Gate Matrix
> Input Activations
> Sparse Gate Sparse UpAndMul
> Up Matrix
> Sparse Down
> Down Matrix (transposed)
> Input Activations
> Output Activations
> Extra-weights Positive values Weights loaded into memory Negative values (falsely active neurons) Non-zero values

Figure 3. Sparse inference using SVD-based sparsity predictors. Neurons with positive predictor values are considered active during gate projection. Gate and up projections are executed sequentially to increase sparsity. 

E/ARC-C (Clark et al., 2018) (scientific and common-sense reasoning), TriviaQA (Joshi et al., 2017) (open-domain QA), MBPP (Austin et al., 2021) (small Python tasks), BBH (Suzgun et al., 2022) (complex reasoning), and 

CMMLU (Li et al., 2024) (Chinese multi-task understand-ing). We adopt the UltraEval framework (He et al., 2024), align-ing with prior ProSparse evaluations, as the model shows compatibility issues with lm-eval-harness (Gao et al., 2024). All benchmarks use free-form generation rather than per-plexity scoring to better reflect real-world LLM usage. 

Performance Evaluation Model performance is mea-sured using the PyTorch profiler (Ansel et al., 2024). For each domain, results are averaged across multiple samples. E2E latency includes the prefill stage and 200-token genera-tion. 

5.2. Performance Under Different Sparsity Ratios 

The desired sparsity hyperparameter s from Algorithm 2 influences the calibrated thresholds and can be used to trade off accuracy and acceleration. As Table 1 shows, lower thresholds lead to lower sparsity and lower acceleration. In contrast, higher thresholds provide more speedup but may degrade quality. Long generation tasks are the most sensitive, while for short answer tasks the sparsity can be significantly increased. With our approach we can achieve up to 1.8× E2E generation speedup while maintaining less than 1% degradation in benchmark scores on tasks involving complex math and code generation. See Section D for detailed evaluation results. 

5.3. Comparison With Other Methods 

We compare SVDP against training-free sparsity methods (GRIFFIN (Dong et al., 2024a)) and learnable predictors (PowerInfer (Song et al., 2024b), Deja Vu (Liu et al., 2023)). GRIFFIN relies on prompt-based statistical sparsity patterns, 

Table 1. Average accuracy when varying hyperparameter s for dif-ferent models. s implicitly sets sparsity, which directly translates to end-to-end speedup.                                            

> Model Method sAccuracy, % Speedup ProSparse-LLaMA2-7B Dense −38.92 1.00 ×
> SVDP 0.4 38.66 (-0.26) 1.61 ×
> 0.5 38.78 (-0.14) 1.64 ×
> 0.6 38.77 (-0.15) 1.67 ×
> 0.7 38.57 (-0.35) 1.71 ×
> TurboSparse-Mistral-Instruct Dense −54.30 1.00 ×
> SVDP 0.4 53.72 (-0.58) 1.67 ×
> 0.5 53.56 (-0.74) 1.73 ×
> 0.6 53.18 (-1.12) 1.79 ×
> 0.7 53.44 (-0.86) 1.84 ×
> SparseQwen2-7B Dense −65.84 1.00 ×
> SVDP 0.4 65.41 (-0.43) 1.78 ×
> 0.5 65.20 (-0.64) 1.81 ×
> 0.6 65.13 (-0.71) 1.85 ×
> 0.7 64.49 (-1.35) 1.86 ×0510 15 20 25 30
> Layer ID
> 0.82
> 0.84
> 0.86
> 0.88
> 0.90
> 0.92
> 0.94
> 0.96
> 0.98
> ROC AUC Score
> Deja Vu, rank 1024
> PowerInfer, rank 1024
> SVDP, rank 256
> SVDP, rank 1024

Figure 4. Layer-wise ROC AUC score of different predictors averaged across several sequence samples for ProSparse-LLaMA2-7B. AUROC score of 1 indicates perfect separation and AUROC score of 0.5 corresponds to random guessing. 

6SVD Contextual Sparsity Predictors for Fast LLM Inference 

Table 2. Comparison with other methods on ProSparse-LLaMA2-7B, evaluated in our framework ( r = 1024 parallel, r = 256 sequential). Method Predicted sparsity, % TriviaQA ARC-E ARC-C HumanEval MBPP GSM8K BBH CMMLU Average E2E acc, % acc, % acc, % pass@1, % pass@1, % acc, % acc, % acc, % Score Speedup Dense baseline − 62.94 75.42 53.58 17.68 22.59 11.45 35.85 31.84 38.92 1.00 ×

Trained predictors 

Deja Vu 20 59.47 74.87 53.41 3.66 2.98 3.03 34.49 30.97 32.86 1.38 ×

PowerInfer 80 61.10 74.54 52.99 15.85 21.15 10.69 35.29 31.80 37.93 1.63 ×

Training-free methods 

GRIFFIN 20 59.77 74.75 53.33 15.85 22.18 8.95 35.49 32.01 37.79 1.14 ×

GRIFFIN 50 35.74 72.77 51.96 12.20 18.17 4.47 35.32 31.88 32.81 1.46 ×

Ours 

SVDP 75 62.66 75.38 53.58 17.68 22.18 10.54 35.86 31.85 38.72 1.61 ×

SVDP (seq.) 50 62.74 75.34 53.67 17.68 22.07 11.22 35.58 31.94 38.78 1.64 ×

while PowerInfer and Deja Vu use trained predictors. For Deja Vu, we trained predictors following their official repository instructions. For PowerInfer, we used their rec-ommended pretrained predictors. 

Benchmarks Table 2 shows that our SVDP matches Pow-erInfer’s accuracy and sparsity while requiring less data and compute for construction. GRIFFIN struggles with stable performance at high sparsity ratios, particularly for long gen-erations. The sequential pipeline implementation reduces SVDP size by 4 × without compromising either accuracy or inference speed. 

ROC-AUC Analysis We also evaluate predictor separa-tion ability via layer-wise ROC-AUC scores on held-out test sequences (Figure 4). SVDP outperforms learnable base-lines on unseen data. Trained predictors may suffer from out-of-distribution sensitivity due to their data-dependent nature, while SVD predictors directly leverage the LLM’s internal weight structure. 

5.4. Ablation Study 

Table 3 shows ablation of SVD sparsity estimators, starting from SparseLoRA’s baseline (Khaki et al., 2025) (referred to as Naive SVD): • Data whitening: Incorporate input data statistics (Sec. 4.1.1). • Bias calibration: Add neuron-specific bias terms (Sec. 4.1.2). Bias calibration significantly improves accuracy by account-ing for distribution shift after low-rank approximation. Data whitening substantially enhances ROC-AUC separa-tion vs. Naive SVD on held-out test sequences (Figure 5), independent of bias calibration. 0 5 10 15 20 25 30 

> Layer ID
> 0.825
> 0.850
> 0.875
> 0.900
> 0.925
> 0.950
> 0.975
> ROC AUC Score
> Naive SVD, rank 1024
> Data-aware SVD, rank 1024
> Naive SVD, rank 256
> Data-aware SVD, rank 256

Figure 5. Layer-wise ROC-AUC: data-aware SVD vs. Naive SVD for ProSparse-LLaMA2-7B. 

5.5. Inference Executors 

Table 4 presents FFN performance of our CUDA and CANN executors across varying sparsity ratios. Here, we use Huawei’s Ascend 310P3 AI accelerator. Notably, we achieve comparable performance gains on the Ascend hard-ware using only high-level PyTorch APIs, eliminating the need for manual low-level instruction optimization. This result facilitates deeper exploration of contextual sparsity optimizations within the DaVinci architecture. It is worth noting that when the sparsity value approaches 90%, the L1 (Liao et al., 2019) cache hit rate reaches almost 100%, resulting in a significant speedup increase. 

## 6. Limitations 

• Our method targets sparse models where non-linear activation functions naturally produce significant zero-valued outputs. Extension to non-ReGLU-based ar-chitectures remains an important direction for future 7SVD Contextual Sparsity Predictors for Fast LLM Inference                                                                                                          

> Table 3. SVD sparsity estimator ablation study. †Averaged across all benchmarks (see Appendix D). Configuration ProSparseLLAMA2-7B TurboSparse-Mistral-Instruct SparseQwen2-7B GSM8K HumanEval Average †GSM8K HumanEval Average †GSM8K HumanEval Average †
> Dense baseline 11.45 17.68 38.92 61.71 34.15 54.30 77.1 64.02 65.84
> Parallel pipeline
> Naive SVD 6.60 10.98 34.08 60.20 24.39 51.52 74.00 53.66 62.05 + Data whitening 10.16 16.46 37.10 60.88 37.20 53.02 75.89 61.59 63.99 + Bias calibration 10.54 17.68 38.72 62.24 36.59 54.34 76.65 62.20 65.37
> Sequential pipeline
> Naive SVD 1.90 0.61 9.69 9.17 1.83 30.87 63.00 12.20 51.60 + Data whitening 6.90 8.54 31.00 48.60 29.27 46.49 72.93 57.32 60.80 + Bias calibration 11.22 17.68 38.78 61.41 33.54 53.56 76.80 62.80 65.22
> Table 4. FFN performance of our CUDA and CANN executors at different sparsity ratios. The speedups are compared to the corresponding dense implementations. Predictors latency is not included. For this experiment, sparsity ratio is fixed across all linear layers. LLM architecture Sparsity, % FFN Speedup CUDA CANN ProSparseLLAMA2-7B 20 1.30 ×0.76 ×
> 50 1.90 ×1.23 ×
> 80 3.34 ×3.38 ×
> 95 4.67 ×30.67 ×
> TurboSparse-Mistral-Instruct 20 1.29 ×0.76 ×
> 50 1.90 ×1.18 ×
> 80 3.44 ×3.10 ×
> 95 4.92 ×22.85 ×
> SparseQwen2-7B 20 1.33 ×0.81 ×
> 50 1.95 ×1.32 ×
> 80 3.47 ×3.41 ×
> 95 4.91 ×28.46 ×

research. See Section C for detailed discussion. • Batched processing inherently reduces sparsity levels, limiting achievable speedups and precluding the use of sparsity predictors during the prefill phase. 

## Conclusion 

This paper introduces a training-free framework for acceler-ating the inference of ReGLU-based large language models through contextual sparsity exploitation. The proposed so-lution comprises two stages: (1) an offline calibration phase where sparsity predictors are constructed using data-aware truncated singular value decomposition of gate projection matrices with calibrated bias parameters, and (2) an online inference phase enabling efficient sparse LLM execution on CUDA and CANN devices. Evaluation across three sparse language models demonstrates up to 1.8 × end-to-end decod-ing acceleration with minimal accuracy degradation across diverse benchmarks, including code and mathematical rea-soning tasks. 

## Acknowledgements 

We thank the MSU Laboratory of Computer Engineer-ing for conducting all GPU-based experiments. Huawei-affiliated authors declare no access to or involvement in GPU-based experiments due to U.S. export control regu-lations. No Huawei-affiliated researchers participated in GPU-dependent aspects of this work. 

## Impact Statement 

This paper presents an approach for accelerated inference of LLMs. While there are many potential societal conse-quences of our work, we do not believe any require specific highlighting here. 

## References 

Akhauri, Y., AbouElhamayed, A. F., Dotzel, J., Zhang, Z., Rush, A. M., Huda, S., and Abdelfattah, M. S. Shad-owllm: Predictor-based contextual sparsity for large lan-guage models, 2024. URL https://arxiv.org/ abs/2406.16635 .Ansel, J., Yang, E., He, H., Gimelshein, N., Jain, A., Voz-nesensky, M., Bao, B., Bell, P., Berard, D., Burovski, E., Chauhan, G., Chourdia, A., Constable, W., Desmaison, A., DeVito, Z., Ellison, E., Feng, W., Gong, J., Gschwind, M., Hirsh, B., Huang, S., Kalambarkar, K., Kirsch, L., Lazos, M., Lezcano, M., Liang, Y., Liang, J., Lu, Y., Luk, C., Maher, B., Pan, Y., Puhrsch, C., Reso, M., Saroufim, M., Siraichi, M. Y., Suk, H., Suo, M., Tillet, P., Wang, E., Wang, X., Wen, W., Zhang, S., Zhao, X., Zhou, K., Zou, R., Mathews, A., Chanan, G., Wu, P., and Chintala, S. PyTorch 2: Faster Machine Learning Through Dynamic Python Bytecode Transformation and Graph Compila-tion. In 29th ACM International Conference on Architec-tural Support for Programming Languages and Operat-

8SVD Contextual Sparsity Predictors for Fast LLM Inference 

ing Systems, Volume 2 (ASPLOS ’24) . ACM, April 2024. doi: 10.1145/3620665.3640366. URL https://docs. pytorch.org/assets/pytorch2-2.pdf .Ashkboos, S., Croci, M., Gennari do Nascimento, M., Hoe-fler, T., and Hensman, J. Slicegpt: Compress large lan-guage models by deleting rows and columns. In Kim, B., Yue, Y., Chaudhuri, S., Fragkiadaki, K., Khan, M., and Sun, Y. (eds.), International Conference on Representa-tion Learning , volume 2024, pp. 11682–11701, 2024. Austin, J., Odena, A., Nye, M., Bosma, M., Michalewski, H., Dohan, D., Jiang, E., Cai, C., Terry, M., Le, Q., and Sutton, C. Program synthesis with large language mod-els, 2021. URL https://arxiv.org/abs/2108. 07732 .Brown, T. B., Mann, B., Ryder, N., Subbiah, M., Kaplan, J., Dhariwal, P., Neelakantan, A., Shyam, P., Sastry, G., Askell, A., Agarwal, S., Herbert-Voss, A., Krueger, G., Henighan, T., Child, R., Ramesh, A., Ziegler, D. M., Wu, J., Winter, C., Hesse, C., Chen, M., Sigler, E., Litwin, M., Gray, S., Chess, B., Clark, J., Berner, C., McCandlish, S., Radford, A., Sutskever, I., and Amodei, D. Lan-guage models are few-shot learners. In Proceedings of the 34th International Conference on Neural Informa-tion Processing Systems , NIPS ’20, Red Hook, NY, USA, 2020. Curran Associates Inc. ISBN 9781713829546. Chen, M., Tworek, J., Jun, H., Yuan, Q., de Oliveira Pinto, H. P., Kaplan, J., Edwards, H., Burda, Y., Joseph, N., Brockman, G., Ray, A., Puri, R., Krueger, G., Petrov, M., Khlaaf, H., Sastry, G., Mishkin, P., Chan, B., Gray, S., Ryder, N., Pavlov, M., Power, A., Kaiser, L., Bavar-ian, M., Winter, C., Tillet, P., Such, F. P., Cummings, D., Plappert, M., Chantzis, F., Barnes, E., Herbert-Voss, A., Guss, W. H., Nichol, A., Paino, A., Tezak, N., Tang, J., Babuschkin, I., Balaji, S., Jain, S., Saun-ders, W., Hesse, C., Carr, A. N., Leike, J., Achiam, J., Misra, V., Morikawa, E., Radford, A., Knight, M., Brundage, M., Murati, M., Mayer, K., Welinder, P., Mc-Grew, B., Amodei, D., McCandlish, S., Sutskever, I., and Zaremba, W. Evaluating large language models trained on code, 2021. URL https://arxiv.org/abs/ 2107.03374 .Clark, P., Cowhey, I., Etzioni, O., Khot, T., Sabharwal, A., Schoenick, C., and Tafjord, O. Think you have solved question answering? try arc, the ai2 reasoning challenge, 2018. URL https://arxiv.org/abs/ 1803.05457 .Cobbe, K., Kosaraju, V., Bavarian, M., Chen, M., Jun, H., Kaiser, L., Plappert, M., Tworek, J., Hilton, J., Nakano, R., Hesse, C., and Schulman, J. Training verifiers to solve math word problems, 2021. URL https://arxiv. org/abs/2110.14168 .Dong, H., Chen, B., and Chi, Y. Prompt-prompted adaptive structured pruning for efficient LLM generation. In First Conference on Language Modeling , 2024a. URL https: //openreview.net/forum?id=4aqq9xTtih .Dong, X., Zhang, X., Bu, W., Zhang, D., and Cao, F. A survey of llm-based agents: Theories, technologies, ap-plications and suggestions. In 2024 3rd International Conference on Artificial Intelligence, Internet of Things and Cloud Computing Technology (AIoTC) , pp. 407–413, 2024b. doi: 10.1109/AIoTC63215.2024.10748304. Gao, L., Tow, J., Abbasi, B., Biderman, S., Black, S., DiPofi, A., Foster, C., Golding, L., Hsu, J., Le Noac’h, A., Li, H., McDonell, K., Muennighoff, N., Ociepa, C., Phang, J., Reynolds, L., Schoelkopf, H., Skowron, A., Sutawika, L., Tang, E., Thite, A., Wang, B., Wang, K., and Zou, A. The language model evaluation harness, 07 2024. URL 

https://zenodo.org/records/12608602 .He, C., Luo, R., Hu, S., Zhao, Y., Zhou, J., Wu, H., Zhang, J., Han, X., Liu, Z., and Sun, M. Ultraeval: A lightweight platform for flexible and comprehensive evaluation for llms, 2024. Huawei Technologies Co., Ltd. Atlas 200 AI Accelerator Module User Guide. https://support.huawei. com/enterprise/en/doc/EDOC1100223189/ 968ec576/basic-specifications , a. Official product page of the Ascend 310P3 chip. Huawei Technologies Co., Ltd. Ascend CANN. https:// www.hiascend.com/zh/cann , b. Official product page of CANN for Ascend AI chip series. Joshi, M., Choi, E., Weld, D. S., and Zettlemoyer, L. Triviaqa: A large scale distantly supervised challenge dataset for reading comprehension, 2017. URL https: //arxiv.org/abs/1705.03551 .Khaki, S., Li, X., Guo, J., Zhu, L., Plataniotis, K. N., Yazdanbakhsh, A., Keutzer, K., Han, S., and Liu, Z. SparseloRA: Accelerating LLM fine-tuning with con-textual sparsity. In Forty-second International Con-ference on Machine Learning , 2025. URL https: //openreview.net/forum?id=z83rodY0Pw .Kwon, W., Li, Z., Zhuang, S., Sheng, Y., Zheng, L., Yu, C. H., Gonzalez, J. E., Zhang, H., and Stoica, I. Efficient memory management for large language model serving with pagedattention. In Proceedings of the ACM SIGOPS 29th Symposium on Operating Systems Principles , 2023. Li, H., Zhang, Y., Koto, F., Yang, Y., Zhao, H., Gong, Y., Duan, N., and Baldwin, T. Cmmlu: Measuring massive multitask language understanding in chinese, 2024. URL 

https://arxiv.org/abs/2306.09212 .9SVD Contextual Sparsity Predictors for Fast LLM Inference 

Liao, H., Tu, J., Xia, J., and Zhou, X. Davinci: A scalable architecture for neural network computing. In 2019 IEEE Hot Chips 31 Symposium (HCS) , pp. 1–44, 2019. doi: 10.1109/HOTCHIPS.2019.8875654. Liu, J., Ponnusamy, P., Cai, T., Guo, H., Kim, Y., and Athiwaratkun, B. Training-free activation sparsity in large language models, 2024. URL https://arxiv. org/abs/2408.14690 .Liu, Z., Wang, J., Dao, T., Zhou, T., Yuan, B., Song, Z., Shrivastava, A., Zhang, C., Tian, Y., Re, C., et al. Deja vu: Contextual sparsity for efficient llms at inference time. In International Conference on Machine Learning , pp. 22137–22176. PMLR, 2023. Ma, C., Huang, M., Zhang, Y., Wang, C., Wang, Y., Yu, L., Liu, C., and Lin, W. First activations matter: Training-free methods for dynamic activation in large language models, 2024. URL https://arxiv.org/abs/ 2408.11393 .Mirzadeh, I., Alizadeh, K., Mehta, S., Mundo, C. C. D., Tuzel, O., Samei, G., Rastegari, M., and Farajtabar, M. Relu strikes back: Exploiting activation sparsity in large language models, 2023. URL https://arxiv.org/ abs/2310.04564 .NVIDIA Corporation. CUDA C Programming Guide , 12.4 edition, July 2024. URL https://docs.nvidia. com/cuda/cuda-c-programming-guide/ index.html . Version 12.4 of the CUDA Toolkit. Shankar, V. Edge ai: A comprehensive survey of tech-nologies, applications, and challenges. In 2024 1st In-ternational Conference on Advanced Computing and Emerging Technologies (ACET) , pp. 1–6, 2024. doi: 10.1109/ACET61898.2024.10730112. Shin, J., Yang, H., and Yi, Y. Sparseinfer: Training-free prediction of activation sparsity for fast llm inference. In 

2025 Design, Automation & Test in Europe Conference (DATE) , pp. 1–7, 2025. doi: 10.23919/DATE64628.2025. 10992997. Song, C., Han, X., Zhang, Z., Hu, S., Shi, X., Li, K., Chen, C., Liu, Z., Li, G., Yang, T., and Sun, M. ProSparse: Introducing and enhancing intrinsic activation sparsity within large language models. arXiv preprint arXiv:2402.13516 , 2024a. URL https://arxiv. org/pdf/2402.13516.pdf .Song, Y., Mi, Z., Xie, H., and Chen, H. Powerinfer: Fast large language model serving with a consumer-grade gpu, 2024b. URL https://arxiv.org/abs/ 2312.12456 .Song, Y., Xie, H., Zhang, Z., Wen, B., Ma, L., Mi, Z., and Chen, H. Turbo sparse: Achieving llm sota performance with minimal activated parameters, 2024c. URL https: //arxiv.org/abs/2406.05955 .Suzgun, M., Scales, N., Sch ¨arli, N., Gehrmann, S., Tay, Y., Chung, H. W., Chowdhery, A., Le, Q. V., Chi, E. H., Zhou, D., and Wei, J. Challenging big-bench tasks and whether chain-of-thought can solve them, 2022. URL 

https://arxiv.org/abs/2210.09261 .Tillet, P., Kung, H. T., and Cox, D. Triton: an inter-mediate language and compiler for tiled neural net-work computations. In Proceedings of the 3rd ACM SIGPLAN International Workshop on Machine Learn-ing and Programming Languages , MAPL 2019, pp. 10–19, New York, NY, USA, 2019. Association for Com-puting Machinery. ISBN 9781450367196. doi: 10. 1145/3315508.3329973. URL https://doi.org/ 10.1145/3315508.3329973 .Wang, X., Alam, S., Wan, Z., Shen, H., and Zhang, M. SVD-LLM v2: Optimizing singular value truncation for large language model compression. In Chiruzzo, L., Ritter, A., and Wang, L. (eds.), Proceedings of the 2025 Conference of the Nations of the Americas Chap-ter of the Association for Computational Linguistics: Human Language Technologies (Volume 1: Long Pa-pers) , pp. 4287–4296, Albuquerque, New Mexico, April 2025a. Association for Computational Linguistics. ISBN 979-8-89176-189-6. doi: 10.18653/v1/2025.naacl-long. 217. URL https://aclanthology.org/2025. naacl-long.217/ .Wang, X., Zheng, Y., Wan, Z., and Zhang, M. SVD-LLM: Truncation-aware singular value decomposition for large language model compression. In Interna-tional Conference on Learning Representations (ICLR) ,2025b. URL https://openreview.net/forum? id=LNYIUouhdt .Wolf, T., Debut, L., Sanh, V., Chaumond, J., Delangue, C., Moi, A., Cistac, P., Rault, T., Louf, R., Funtowicz, M., Davison, J., Shleifer, S., von Platen, P., Ma, C., Jer-nite, Y., Plu, J., Xu, C., Scao, T. L., Gugger, S., Drame, M., Lhoest, Q., and Rush, A. M. Transformers: State-of-the-art natural language processing. In Proceedings of the 2020 Conference on Empirical Methods in Natu-ral Language Processing: System Demonstrations , pp. 38–45, Online, October 2020. Association for Compu-tational Linguistics. URL https://www.aclweb. org/anthology/2020.emnlp-demos.6 .Yang, Y., Wang, Y., Ma, C., Yu, L., Chersoni, E., and Huang, C.-R. Sparse brains are also adaptive brains: Cognitive-load-aware dynamic activation for llms, 2025. URL https://arxiv.org/abs/2502.19078 .10 SVD Contextual Sparsity Predictors for Fast LLM Inference 

Yuan, Z., Shang, Y., Song, Y., Yang, D., Wu, Q., Yan, Y., and Sun, G. Asvd: Activation-aware singular value decom-position for compressing large language models, 2025. URL https://arxiv.org/abs/2312.05821 .Zhang, Z., Song, Y., Yu, G., Han, X., Lin, Y., Xiao, C., Song, C., Liu, Z., Mi, Z., and Sun, M. Relu2 wins: Discovering efficient activation functions for sparse llms. ArXiv , abs/2402.03804, 2024. URL https://api.semanticscholar. org/CorpusID:267499856 .11 SVD Contextual Sparsity Predictors for Fast LLM Inference 

## A. Theoretical Analysis 

A.1. Predictor error bounds 

Let m, d > 0 and 0 < r ≤ min( m, d ). Consider a weight matrix W ∈ Rm×d and its rank-r approximation Wr ∈ Rm×d

(e.g., truncated SVD), with bias vector b ∈ Rm. For any input x ∈ Rd, x̸ = 0 , we have: 

A(x) := ReLU( W x ), (7) 

gb(x) := 1[Wr x + b > 0] , (8) 

Bb(x) := A(x) ⊙ gb(x). (9) Throughout, we use the following notation: 

δi ∈ Rm, i ∈ [m], denotes the i-th standard basis vector (0 , . . . , 1|{z} 

> i

, . . . , 0) ⊤,t+ := max( t, 0) , the ReLU function (i.e., t+ = 0 if t < 0 and t+ = t if t ≥ 0).

We aim to derive deterministic bounds on the relative gating error 

Eb(x) := ∥A(x) − Bb(x)∥2

∥x∥2

in terms of the spectral error of Wr and the bias b.

Exact error representation and monotonicity Definition A.1 (Residual operator) . Define the residual matrix and residual vector 

∆W := W − Wr , e(x) := ∆ W x. 

Proposition A.2 (Exact representation of the gating error) . For every x ∈ Rd,

A(x) − Bb(x) = ReLU( W x ) ⊙ 1[Wr x + b ≤ 0] , (10) 

and therefore 

∥A(x) − Bb(x)∥22 =

> m

X

> i=1

(( W x )i)2+ 1[( Wr x + b)i ≤ 0] . (11) 

Proof. The claim follows immediately from the componentwise definition of Bb(x).

Proposition A.3 (Monotonicity w.r.t. biases) . Let b, b ′ ∈ Rm and suppose b′ ≤ b componentwise. Then for all x ∈ Rd,

∥A(x) − Bb′ (x)∥2 ≥ ∥ A(x) − Bb(x)∥2 .

Proof. If b′ ≤ b, then 1[Wr x + b ≤ 0] ≤ 1[Wr x + b′ ≤ 0] componentwise. Using the identity (10) and the fact that 

ReLU( W x ) ≥ 0 componentwise, the ℓ2 norm can only increase when we replace b by the smaller bias b′, which activates the mask on a superset of coordinates. 

Remark A.4 . Using thresholds τ = −b clarifies the trade-off: increasing τ increases sparsity (more gates are off) but never decreases the error; equivalently, increasing b reduces sparsity and never increases the error. 12 SVD Contextual Sparsity Predictors for Fast LLM Inference 

A deterministic bound via the shifted residual Lemma A.5 (Residual-based upper bound) . For every x ∈ Rd and b ∈ Rm,

∥A(x) − Bb(x)∥2 ≤ (∆ W x − b)+ 2 ≤ ∥ ∆W x − b∥2 . (12) 

Proof. Let z = W x , zr = Wr x, and e := z − zr = ∆ W x . Define the gated index set 

I := {i ∈ [m] : ( zr + b)i ≤ 0}.

By definition, 

∥A(x) − Bb(x)∥22 = X

> i∈I

(zi)2+ .

For i ∈ I we have zr,i ≤ − bi, hence zi = zr,i + ei ≤ ei − bi. By monotonicity of t 7 → (t)+, (zi)+ ≤ (ei − bi)+. Therefore 

X

> i∈I

(zi)2+ ≤

> m

X

> i=1

(ei − bi)2+ = (e − b)+ 

> 22

,

which gives the first inequality. The second inequality follows from (u)+ 2 ≤ ∥ u∥2.

Worst-case bounds Due to the previous result, the analysis of the nonlinear gating error reduces to a deterministic quantity depending only on the residual ∆W x and the bias b. We therefore can study the worst-case value 

sup 

> ∥e∥2≤R

(e − b)+ 2 ,

which characterizes the maximal gating error at residual energy R.

Uniform bias Theorem A.6 (Worst-case bound for uniform bias) . Let b = β1 with β ∈ R. For all R ≥ 0,

sup 

> ∥e∥2≤R

(e − β1)+ 2 =

((R − β)+ , β ≥ 0,R − β√m, β < 0.

Proof. 

Case 1: β ≥ 0. Write y := ( e − β1)+ ∈ Rm 

> ≥0

and t := ∥y∥2.Let S := {i : yi > 0} (if t = 0 there is nothing to prove, otherwise R > β ). For i ∈ S we have ei = β + yi, hence 

∥e∥22 ≥ X

> i∈S

(β + yi)2 = β2|S| + 2 β X

> i∈S

yi + X

> i∈S

y2 

> i

.

Since y ≥ 0, we have P 

> i∈S

yi = ∥y∥1 ≥ ∥ y∥2 = t, and also |S| ≥ 1. Therefore 

∥e∥22 ≥ β2 + 2 βt + t2 = ( t + β)2,

so ∥e∥2 ≥ t + β. Under ∥e∥2 ≤ R this yields t ≤ R − β, i.e., (e − β1)+ 2 ≤ (R − β)+.Equality holds by taking 

R ≤ β : e = 0 ,R > β : e = Rδ 1.

Case 2: β < 0. Write β = −c with c > 0. Then for any e,

(e − β1)+ 2 = (e + c1)+ 2 ≤ ∥ e + c1∥2 ≤ ∥ e∥2 + c ∥1∥2 ≤ R + c√m = R − β√m. 

Equality holds for e = R√m 1.13 SVD Contextual Sparsity Predictors for Fast LLM Inference 

General bias vector Theorem A.7 (Worst-case bound for general bias) . Let b ∈ Rm and R ≥ 0. Then 

sup 

> ∥e∥2≤R

(e − b)+ 2 =



max i∈[m](R − bi)+, b ≥ 0 componentwise ,R + ∥b∥2 , b < 0 componentwise .

Proof. 

Case 1: b < 0.

∥(e − b)+∥2 ≤ ∥ e − b∥2 ≤ R + ∥b∥2 .

Tightness: e = R

> ∥b∥2

(−b) gives exactly R + ∥b∥2.

Case 2: b ≥ 0. For i⋆ = arg min i bi, let bc = bi⋆ 1. Then (e − b)+ ≤ (e − bc)+ componentwise, so 

∥(e − b)+∥2 ≤ ∥ (e − bc)+∥2

> (A.6)

≤ (R − bi⋆ )+ = max  

> i

(R − bi)+.

Tightness: e = Rδ i⋆ for i⋆ = arg max i(R − bi)+ achieves equality. 

Remark A.8 . Theorem A.7 identifies two regimes in which the worst-case residual can be characterized exactly: a fully negative bias, where the bound grows linearly with ∥b−∥2, and a fully nonnegative bias, where the optimum concentrates on a single coordinate. 

Corollary A.9 (Main deterministic bound) . Let b ∈ Rm and R ≥ 0 be such that ∥∆W x ∥2 ≤ R. Then for all x ∈ Rd,

Eb(x) = ∥A(x) − Bb(x)∥2

∥x∥2

≤



max i∈[m](R − bi)+

∥x∥2

, b ≥ 0 componentwise ,R + ∥b∥2

∥x∥2

, b < 0 componentwise .

Corollary A.10 (Truncated SVD) . By Eckart–Young–Mirsky, if Wr is the rank-r truncated SVD of W gate , then ∥∆W ∥2 =

σr+1 (W gate ) = σr+1 , so for all x ∈ Rd,

Eb(x) ≤



max 

> i∈[m]



σr+1 − bi

∥x∥2



> +

, b ≥ 0 componentwise ,σr+1 + ∥b∥2

∥x∥2

, b < 0 componentwise .

Remark A.11 . While rather loose in practice due to data distribution, this bound cleanly separates approximation error σr+1 

from gating bias b, revealing bias addition’s influence on residuals. 

A.2. Greedy threshold selection Greedy threshold calibration under a fixed sparsity budget We study a data-dependent calibration rule for component-wise thresholds τ ∈ Rm under a fixed drop budget K (equivalently, fixed sparsity). Throughout, the gate is 

gτ (x) = 1[ABx > τ ],

where AB denotes any rank-r approximation of W gate ∈ Rm×d; the specific choice used in our experiments is given in Section 4.1.1. 

Proxy scores and damage weights Let D = {x(t)}Tt=1 be a dataset and write [m] = {1, . . . , m }, [T ] = {1, . . . , T }. For each coordinate i ∈ [m] and sample t ∈ [T ] define the proxy score 

s(t) 

> i

:= ( ABx (t))i,

14 SVD Contextual Sparsity Predictors for Fast LLM Inference 

and define the marginal damage weight 

w(t) 

> i

:= ReLU 



W gate  

> i, :

x(t)

· W up  

> i, :

x(t) 2

W down :,i  

> 22

(≥ 0) . (13) Given thresholds τ , the empirical output error incurred by dropping (i, t ) whenever s(t) 

> i

≤ τi is 

L(τ ) = 

> T

X

> t=1
> m

X

> i=1

w(t) 

> i

1[s(t) 

> i

≤ τi]. (14) 

Remark A.12 (Origin of the weight (13)) . Consider the standard gated FFN decomposition 

z(x) = ReLU( W gate x) ⊙ (W up x), h(x) = W down z(x).

Then h(x) decomposes into rank-one terms: 

h(x) = 

> m

X

> i=1

zi(x) W down :,i , zi(x) = ReLU( W gate  

> i, :

x) · W up  

> i, :

x. 

Dropping the i-th hidden coordinate removes exactly the contribution vi(x) = zi(x)W down :,i , hence 

∥vi(x)∥22 = ReLU  W gate  

> i, :

x · W up  

> i, :

x 2 W down :,i  

> 22

,

which matches (13) for x = x(t).

Prefix formulation and fixed drop budget Fix i ∈ [m]. Sort samples in nondecreasing order of proxy scores: 

si, (1) ≤ si, (2) ≤ · · · ≤ si, (T ),

and let wi, (j) denote the corresponding permuted weights. Define 

Ci(k) := 

> k

X

> j=1

wi, (j), Ci(0) = 0 , ∆i(k) := Ci(k) − Ci(k − 1) = wi, (k).

For each coordinate i, setting τi so exactly ki samples satisfy s(t) 

> i

≤ τi incurs cost Ci(ki). The total loss is thus 

L(τ ) = 

> m

X

> i=1

Ci(ki), ki := # {t : s(t) 

> i

≤ τi} ∈ { 0, . . . , T }.

With fixed drop budget K ∈ { 0, 1, . . . , mT }, threshold selection reduces to 

min   

> ki∈{ 0,...,T }Pmi=1 ki=Km

X

> i=1

Ci(ki). (15) 

Greedy optimality Definition A.13 (Global optimality) . A feasible solution k⋆ = ( k⋆ 

> 1

, . . . , k ⋆m) with P 

> i

k⋆i = K is globally optimal for (15) if 

> m

X

> i=1

Ci(k⋆i ) ≤

> m

X

> i=1

Ci(ki) for all feasible k = ( k1, . . . , k m).

Assumption A.14 (Monotone marginals within each coordinate) . For every i ∈ [m], the marginal increments are nonde-creasing: 

∆i(1) ≤ ∆i(2) ≤ · · · ≤ ∆i(T ).

15 SVD Contextual Sparsity Predictors for Fast LLM Inference 

Theorem A.15 (Greedy is globally optimal) . Initialize ki = 0 for all i. For ℓ = 1 , 2, . . . , K (drop budget) choose 

iℓ ∈ arg min   

> i:ki<T

∆i(ki + 1) , and update kiℓ ← kiℓ + 1 .

Under Theorem A.14 the resulting k is globally optimal for (15) .Moreover, it can be realized by thresholds τ via 

τi ∈ [ si, (ki), s i, (ki+1) ),

with conventions si, (0) := −∞ and si, (T +1) := + ∞.Proof. The problem is equivalent to selecting exactly K increments ∆i(j) while respecting prefix constraints: selecting 

∆i(j) requires all ∆i(1) , . . . , ∆i(j − 1) .The greedy algorithm selects the smallest available “next increment” ∆i(ki + 1) at each step. Let G be greedy’s sequence of K increments, and O any optimal sequence. Suppose G̸ = O. Let a = ∆ p(kp + 1) be the first (chronologically) greedy increment not in O. Since |G| = |O| = K, there exists b = ∆ q (j) ∈ O \ G.Choose b as the last increment of coordinate q in O (maximal j). Removing b preserves q’s prefix constraint. Adding a

extends p’s prefix, also preserving feasibility. By greedy choice, a ≤ ∆q (kq + 1) where kq counts greedy’s selections in q up to that point. Since b / ∈ G but O includes it, 

j ≥ kq + 1 . By Theorem A.14 (nondecreasing increments), ∆q (kq + 1) ≤ ∆q (j) = b.Thus a ≤ b, so swapping b ↔ a preserves feasibility and does not increase cost. Repeating such exchanges transforms O

into G, proving optimality. The threshold realization follows from the definition of ki.

Remark A.16 (Role of rank-r approximation and empirical validation) . Theorem A.15 requires monotone marginal increments after sorting by proxy scores (Theorem A.14). This is not guaranteed , since proxy scores s(t) 

> i

= ( ABx (t))i

depend only on rank-r approximation AB ≈ W gate , while damage, defined above, also involves W up and W down .If AB approximates W gate well on the input data distribution , then (ABx )i ≈ (W gate x)i, so proxy sorting should align with true damage ordering. In practice, we perform greedy selection over non-overlapping η-step sums to mitigate data noise: 

S(m) 

> i

:= 

> mη

X

> j=( m−1) η+1

wi, (j), m = 1 , . . . , ⌊T /η ⌋.

We validate their monotonicity using normalized Kendall tau distance between group sums and their position indices: 

τ K 

> i

:= 1 − Kd

 {S(m) 

> i

}Mm=1 , {m}Mm=1 



M (M − 1) ,

where Kd(τ1, τ 2) = # (m1, m 2) : m1 < m 2, [τ1(m1) ≶ τ1(m2) ∧ τ2(m1) ≷ τ2(m2)] counts discordant pairs. The use of Kendall tau distance is justified by the fact that it directly quantifies ordinal consistency required by Theorem A.14: for all m1 < m 2, we expect S(m1) 

> i

≤ S(m2) 

> i

. Formally, τ K 

> i

measures the fraction of concordant pairs while properly accounting for ties – the precise condition for greedy optimality in Theorem A.15. On sample data used to calibrate biases, Figure 6 shows stable Kendall τ K 

> i

in [0 .9, 1] , confirming proxy sorting ABx ≈ W gate x and the suitability of our greedy approach. 

Remark A.17 (Knapsack reformulation and practical optimality) . Problem (15) is a 0–1 knapsack problem with mT items 

{∆i(j)}mi=1 Tj=1 , capacity K, and prefix constraints that selection of ∆i(j) requires prior selection of ∆i(1) , . . . , ∆i(j − 1) .While standard dynamic programming requires O(mT K ) time, our greedy algorithm runs in O(( K + m) log m) via priority queue and is exactly optimal under Theorem A.14, yielding superior efficiency. 16 SVD Contextual Sparsity Predictors for Fast LLM Inference 0 5 10 15 20 25 30 

> Layer ID
> 0.70
> 0.75
> 0.80
> 0.85
> 0.90
> 0.95
> 1.00
> Normalized value
> Kendall distance
> Sparsity ratio

Figure 6. Mean Kendall τ K 

> i

across neurons for proxy-sorted η-step sums from ProSparse-LLaMA2-7B. The x-axis shows layer number. For clarity, layer-wise sparsity ratios are depicted. The shaded regions denote the 10th to 90th percentile interval, illustrating the central 80% of the data distribution. 

Algorithm 3 Sparse FFN Forward Pass on CUDA (Decoding Phase) 

Input: W gate , W up ∈ RD×d, W down ∈ Rd×D , x ∈ Rd, A ∈ RD×r, B ∈ Rr×d, b ∈ RD

Output: x ∈ Rd

m ← ABx + b (Predict sparsity pattern) 

m ← SPARSE GATE KERNEL (W gate , x, m )

x ← SPARSE UPAND MUL KERNEL (W up , x, m )

x ← SPARSE DOWN KERNEL (W down , x )

return x

## B. Sparse FFN Inference Implementation 

Unlike Deja Vu’s (Liu et al., 2023) asynchronous pipeline, which leverages Ampere’s cuda::memcpy async operations (NVIDIA Corporation, 2024), our approach targets consumer-grade GPUs with compute capability 7.0 for broader applica-bility. Predictors are inserted at the beginning of each FFN and sequentially generate sparsity patterns for their respective gate projections. This design ensures minimal integration overhead with existing frameworks. 

Custom CUDA Kernels for Sparse FFN We implement three CUDA kernels (Algorithm 3). The predictor’s output 

m ∈ RD highlights active rows of W gate . Throughout gate and up projections each CUDA block processes one neuron, first verifying its activity via m. Gate outputs are stored back into m for reuse. Key optimizations: • Memory : Column-major storage of W down for coalesced memory access. • Precision : Full precision register operations; half precision for memory reads and writes. • Determinism : Avoid atomic additions via shared-memory partial sums in down projection. Our kernel design diverges from existing ProSparse (Song et al., 2024a) and Deja Vu (Liu et al., 2023) implementations. Both prior approaches rely on atomic addition operations within the down projection phase, introducing non-determinism and potential precision instability during floating-point accumulation. To address these limitations, our algorithm employs partial sum reduction implemented via shared memory. This technique enforces a predefined summation order and utilizes a hierarchical reduction strategy. Consequently, our approach guarantees deterministic outputs while simultaneously achieving enhanced numerical stability through reduced computational error propagation. We postpone kernel fusion due to diminishing returns, prioritizing maintainability. 17 SVD Contextual Sparsity Predictors for Fast LLM Inference 

Algorithm 4 Sparse FFN Forward Pass on CANN (Decoding Phase) 

Input: W gate , W up ∈ RD×d, W down ∈ Rd×D , x ∈ Rd, A ∈ RD×r, B ∈ Rr×d, b ∈ RD

Output: x ∈ Rd

m ← ABx + b > 0 (Predict sparsity pattern) 

p ← NONZERO (m) (Obtain indices) 

g ← SELROWS (W gate , p ) (Select rows of W gate indexed by p)

m1 ← MATMUL (g, x )

u ← SELROWS (W up , p ) (Select rows of W up indexed by p)

m2 ← MATMUL (u, x )

m3 ← RELU (m1) · m2

d ← SELCOLS (W down , p ) (Select columns of W down indexed by p)

x ← MATMUL (d, m 3)

return x

NPU Solution for Sparse FFN We implement our NPU solution for sparse FFN, detailed in Algorithm 4, leveraging high-level PyTorch instructions. The predictor’s output m ∈ RD is used to construct smaller dense submatrices by retrieving the corresponding rows or columns. This approach enables highly efficient execution by utilizing the dedicated matrix multiplication accelerator unit (cube) inherent to the CANN NPU architecture (Liao et al., 2019). W down matrix is stored in column-major format. 

## C. Extension to Non-ReLU Activations 

The solution can in principle be applied to LLMs with non-ReLU activation functions, such as SiLU. However, the straightforward utilization may lead to substantial accuracy degradation on long generation tasks. The quality evaluation results for Qwen2-7B-Instruct model with SiLU activation function are presented at Table 5. Potential modifications to the current solution may include transformations to the intermediate activations to mitigate errors that arise. 

Table 5. Qwen2-7B-Instruct accuracy results, 25% average predicted sparsity 

Method TriviaQA ARC-C ARC-E Humaneval MBPP GSM8K BBH CMMLU Average acc, % acc, % acc, % pass@1, % pass@1, % acc, % acc, % acc, % Dense mode 60.96 86.43 93.81 60.98 44.46 36.85 47.81 79.58 63.86 SVDP 51.48 86.43 93.81 49.39 36.55 37.83 47.61 79.58 60.34 

## D. Detailed accuracy results 

Table 6, Table 7, Table 8 provide detailed evaluation results for three sparse LLMs equipped with our SVD-predictors. Table 9, Table 11, Table 10 provide a detailed ablation study of SVD sparsity estimators for three sparse LLMs. 

Table 6. ProSparse-LLaMA2-7B evaluation results, r = 256 

Method s TriviaQA ARC-E ARC-C Humaneval MBPP GSM8K BBH CMMLU Average E2E acc, % acc, % acc, % pass@1, % pass@1, % acc, % acc, % acc, % Score Speedup Dense mode - 62.94 75.42 53.58 17.68 22.59 11.45 35.85 31.84 38.92 1.00 ×

SVDP 0.4 62.38 75.29 53.67 17.68 22.38 10.61 35.46 31.81 38.66 1.61 ×

0.5 62.74 75.34 53.67 17.68 22.07 11.22 35.58 31.94 38.78 1.64 ×

0.6 62.90 75.29 53.75 18.29 21.97 10.54 35.54 31.91 38.77 1.67 ×

0.7 62.04 75.42 53.58 17.07 21.87 11.22 35.43 31.90 38.57 1.71 ×

18 SVD Contextual Sparsity Predictors for Fast LLM Inference                                                                                                                                                     

> Table 7. TurboSparse-Mistral-Instruct evaluation results, r= 352
> Method sTriviaQA ARC-E ARC-C Humaneval MBPP GSM8K BBH CMMLU Average E2E acc, % acc, % acc, % pass@1, % pass@1, % acc, % acc, % acc, % Score Speedup Dense mode -48.58 87.21 71.67 34.15 42.20 61.71 42.35 46.52 54.30 1.00 ×
> SVDP 0.4 45.46 87.08 71.67 32.93 42.09 61.49 42.46 46.55 53.72 1.67 ×
> 0.5 43.26 87.04 71.76 33.54 42.71 61.41 42.32 46.47 53.56 1.73 ×
> 0.6 40.63 87.08 72.10 33.54 42.09 61.41 42.19 46.43 53.18 1.79 ×
> 0.7 39.21 87.21 72.10 37.20 42.09 60.96 42.33 46.45 53.44 1.84 ×
> Table 8. SparseQwen2-7B evaluation results, r= 512
> Method sTriviaQA ARC-E ARC-C Humaneval MBPP GSM8K BBH CMMLU Average E2E acc, % acc, % acc, % pass@1, % pass@1, % acc, % acc, % acc, % Score Speedup Dense mode -50.76 91.92 83.87 64.02 46.3 77.1 48.42 64.35 65.84 1.00 ×
> SVDP 0.4 50.87 91.92 83.87 62.80 44.56 76.50 48.40 64.35 65.41 1.78 ×
> 0.5 49.96 91.92 83.87 62.80 43.53 76.80 48.40 64.35 65.20 1.81 ×
> 0.6 48.82 91.92 83.87 63.41 43.53 76.80 48.34 64.35 65.13 1.85 ×
> 0.7 47.42 91.92 83.87 62.20 41.99 75.82 48.36 64.35 64.49 1.86 ×

## E. Technical details 

E.1. LLMs hyperparameter values 

We provide hyperparameter values for the tested models in Table 12, as they influence E2E latency. Specifically, all evaluated optimization techniques modify only the FFN portion of inference, leaving the attention block unchanged. Thus, a higher intermediate size increases the FFN’s contribution to E2E latency. Similarly, the use of Grouped Query Attention reduces the attention contribution to total inference time. 

E.2. Predictor Building 

For calibration in total we use 112 few-shot samples ( ≈20000 tokens) from the train splits of the GSM8K, ARC-E, ARC-C, OpenBookQA, QAsper, CodeXGLUE datasets. 

E.3. Predictor’s rank 

One can trade off between the predicted sparsity ratio and predictor size. While a larger predictor allows for more sparse computations within gate projection, it requires more compute for prediction. In turn, the s hyperparameter can be used to calibrate the predicted sparsity ratio. Figure 7 shows how the separation capability of SVD predictors depends on their size. Due to this insight, higher acceleration can be achieved with relatively small predictors. Moreover, small predictors consume less additional memory. Similarly to r, we set s to be constant across all layers. Empirically, we found that setting the rank to approximately 2% of the intermediate size D works well. 19 SVD Contextual Sparsity Predictors for Fast LLM Inference 

Table 9. SVD sparsity estimator ablation study for ProSparseLLAMA2-7B. TriviaQA ARC-E ARC-C Humaneval MBPP GSM8K BBH CMMLU Average Dense baseline 62.94 75.42 53.58 17.68 22.59 11.45 35.85 31.84 38.92 

Parallel pipeline 

Naive SVD 58.19 69.91 49.32 10.98 13.35 6.60 33.98 30.28 34.08 + Data whitening 53.85 75.34 53.75 16.46 19.92 10.16 35.46 31.83 37.10 + Bias calibration 62.66 75.38 53.58 17.68 22.18 10.54 35.86 31.85 38.72 

Sequential pipeline 

Naive SVD 34.53 3.58 4.35 0.61 0.21 1.90 11.12 21.23 9.69 + Data whitening 27.58 75.21 53.33 8.54 11.40 6.90 33.71 31.34 31.00 + Bias calibration 62.74 75.34 53.67 17.68 22.07 11.22 35.58 31.94 38.78 

Table 10. SVD sparsity estimator ablation study for SparseQwen2-7B. TriviaQA ARC-E ARC-C Humaneval MBPP GSM8K BBH CMMLU Average Dense baseline 50.76 91.92 83.87 64.02 46.30 77.10 48.42 64.35 65.84 

Parallel pipeline 

Naive SVD 46.24 91.92 83.87 53.66 34.39 74.00 48.00 64.35 62.05 + Data whitening 42.92 91.92 83.87 61.59 43.02 75.89 48.37 64.35 63.99 + Bias calibration 50.20 91.92 83.87 62.20 45.28 76.65 48.45 64.35 65.37 

Sequential pipeline 

Naive SVD 40.03 91.92 83.87 12.20 9.03 63.00 48.43 64.35 51.60 + Data whitening 30.00 91.92 83.87 57.32 37.78 72.93 48.22 64.35 60.80 + Bias calibration 49.96 91.92 83.87 62.80 43.63 76.80 48.40 64.35 65.22 

Table 11. SVD sparsity estimator ablation study for TurboSparse-Mistral-Instruct. TriviaQA ARC-E ARC-C Humaneval MBPP GSM8K BBH CMMLU Average Dense baseline 48.58 87.21 71.67 34.15 42.20 61.71 42.35 46.52 54.30 

Parallel pipeline 

Naive SVD 41.40 87.04 71.76 24.39 38.91 60.20 42.13 46.34 51.52 + Data whitening 37.98 86.57 71.25 37.20 41.79 60.88 42.27 46.18 53.02 + Bias calibration 46.21 87.12 71.76 36.59 41.89 62.24 42.44 46.45 54.34 

Sequential pipeline 

Naive SVD 35.29 71.09 57.85 1.83 8.73 9.17 37.72 25.24 30.87 + Data whitening 18.09 87.12 72.27 29.27 28.44 48.60 41.87 46.22 46.49 + Bias calibration 43.26 87.04 71.76 33.54 42.71 61.41 42.32 46.47 53.56 

Table 12. LLMs hyperparameter values. 

hyperparameters Model hidden size intermediate size n attention heads n KV heads ProSparse-LLaMA2-7B 4096 11008 32 32 TurboSparse-Mistral-Instruct 4096 14336 32 8SparseQwen2-7B 3584 18944 28 420 SVD Contextual Sparsity Predictors for Fast LLM Inference 

Figure 7. ROC AUC scores of SVD-predictors at different layers averaged across several sequence samples for ProSparse-LLaMA2-7B. The brighter curves represent deeper layers. For most of the layers a quality metric reaches a plateau at relatively small rank. 

21