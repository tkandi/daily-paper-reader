---
title: "NCCL EP: Towards a Unified Expert Parallel Communication API for NCCL"
title_zh: NCCL EP：迈向统一的 NCCL 专家并行通信 API
authors: "Amos Goldman, Nimrod Boker, Maayan Sheraizin, Nimrod Admoni, Artem Polyakov, Subhadeep Bhattacharya, Fan Yu, Kai Sun, Georgios Theodorakis, Hsin-Chun Yin, Peter-Jan Gootzen, Aamir Shafi, Assaf Ravid, Salvatore Di Girolamo, Manjunath Gorentla Venkata, Gil Bloch"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13606v1"
tags: ["query:mlsys"]
score: 10.0
evidence: 针对MoE扩展的专家并行通信库
tldr: 随着MoE架构在大型语言模型中的普及，高效的专家并行通信变得至关重要。本文提出NCCL EP，这是一个完全基于NCCL Device API构建的MoE通信库。它提供了统一的Dispatch和Combine原语，支持针对推理解码的低延迟（LL）模式和针对训练及预填充的高吞吐（HT）模式。通过利用GPU发起的RDMA和拓扑感知优化，NCCL EP在H100集群上实现了卓越的性能，并成功集成至vLLM，为NVIDIA平台提供了原生的专家并行支持。
selection_source: fresh_fetch
motivation: 现有的MoE通信库虽展示了GPU发起RDMA的优势，但缺乏与NCCL深度集成且统一的专家并行通信标准。
method: 基于NCCL Device API开发了支持LL和HT两种模式的通信原语，分别针对小批量低延迟和大批量高吞吐场景进行拓扑感知优化。
result: 在H100集群上的评估显示，NCCL EP具有极具竞争力的内核性能，并能有效提升vLLM端到端的推理效率。
conclusion: NCCL EP通过在NCCL内部原生构建MoE通信，为当前及未来的NVIDIA硬件平台提供了一条受官方支持且高效的专家并行路径。
---

## 摘要
混合专家（MoE）架构已成为扩展大语言模型的关键，推动了 DeepEP、Hybrid-EP 等专门的设备端发起通信库的发展。这些库展示了 GPU 发起的 RDMA 在 MoE 分发（dispatch）和合并（combine）操作中的性能优势。本文介绍了 NCCL EP（专家并行），这是一个完全基于 NCCL Device API 从底层构建的 MoE 通信库。NCCL EP 提供了统一的 ncclEpDispatch 和 ncclEpCombine 原语，并提供 C 和 Python 接口，支持用于推理解码的低延迟（LL）模式和用于训练及推理预填充的高吞吐（HT）模式。LL 模式针对小批量（1-128 个 token），采用直接的全对全 RDMA+NVLink 网格连接，并使用双缓冲通信来重叠分发和合并阶段。HT 模式针对大批量（4096+ 个 token），采用分层通信，在进行节点间 RDMA 传输之前先在 NVLink 域内聚合 token。两种模式在节点内和节点间通信中均利用了 Device API，充分发挥了其拓扑感知和优化的 GPU 发起实现的优势。我们在基于 H100 的集群上针对多节点配置评估了 NCCL EP，展示了具有竞争力的 LL 内核性能，并提供了与 vLLM 集成的端到端结果。通过在 NCCL 内部原生构建 MoE 通信，NCCL EP 为当前和新兴的 NVIDIA 平台上的专家并行提供了一条受支持的路径。

## Abstract
Mixture-of-Experts (MoE) architectures have become essential for scaling large language models, driving the development of specialized device-initiated communication libraries such as DeepEP, Hybrid-EP, and others. These libraries demonstrate the performance benefits of GPU-initiated RDMA for MoE dispatch and combine operations.   This paper presents NCCL EP (Expert Parallelism), a ground-up MoE communication library built entirely on NCCL's Device API. NCCL EP provides unified ncclEpDispatch and ncclEpCombine primitives with both C and Python interfaces, supporting Low-Latency (LL) mode for inference decoding and High-Throughput (HT) mode for training and inference prefill. LL targets small batch sizes (1-128 tokens) using direct all-to-all RDMA+NVLink mesh connectivity with double-buffered communication for overlapping dispatch and combine phases. HT targets large batches (4096+ tokens) using hierarchical communication that aggregates tokens within NVLink domains before inter-node RDMA transmission. Both modes leverage Device API for both intra- and inter-node communications, taking advantage of its topology awareness and optimized GPU-initiated implementation.   We evaluate NCCL EP on an H100-based cluster across multi-node configurations, demonstrating competitive LL kernel performance and presenting end-to-end results with vLLM integration. By building MoE communication natively within NCCL, NCCL EP provides a supported path for expert parallelism on current and emerging NVIDIA platforms.

---

## 论文详细总结（自动生成）

### 论文总结：NCCL EP —— 迈向统一的 NCCL 专家并行通信 API

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：混合专家（MoE）架构（如 DeepSeek-V3、Mixtral）已成为扩展大语言模型（LLM）的关键。MoE 引入了独特的通信模式：**分发（Dispatch）**和**合并（Combine）**，这本质上是动态且不规则的全对全（All-to-All）交换。
*   **核心问题**：
    *   **生态碎片化**：现有的高性能 MoE 通信库（如 DeepEP、Hybrid-EP、pplx-kernels）通常独立于 NCCL 运行，使用不同的 API 和通信后端（如 NVSHMEM），导致集成复杂且难以维护。
    *   **性能瓶颈**：传统的 CPU 编排通信在处理 MoE 的细粒度、动态路由时延迟较高。
    *   **缺乏统一性**：推理（低延迟）和训练（高吞吐）往往需要完全不同的库和接口。
*   **研究目的**：开发 **NCCL EP**，一个完全集成在 NCCL 生态中、基于 GPU 设备端 API（Device API）构建的统一专家并行通信库。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：利用 NCCL 2.28 引入的 **Device API**，允许 GPU 内核直接发起 RDMA 操作（GIN）和 NVLink 访问（LSA），从而消除 CPU 参与路径，降低延迟并提高灵活性。
*   **关键技术细节**：
    *   **统一 API 设计**：提供 `ncclEpDispatch` 和 `ncclEpCombine` 原语，通过 `ncclEpGroup_t`（长期资源）和 `ncclEpHandle_t`（单次转发状态）管理资源。
    *   **双模式支持**：
        *   **低延迟（LL）模式**：针对推理解码（1-128 tokens）。采用全对全网格连接和双缓冲技术，实现通信与计算的重叠。
        *   **高吞吐（HT）模式**：针对训练和预填充（4096+ tokens）。采用分层通信，先通过 NVLink 在节点内聚合，再通过 RDMA 进行节点间传输。
    *   **内存布局优化**：在 LL 模式中，通过在消息头中包含路由信息，将通信缓冲区的空间复杂度从 $O(E \cdot B \cdot P)$ 降低到 $O(N \cdot B \cdot P)$（其中 $E$ 为专家数，$N$ 为 GPU 数，$B$ 为批量大小），实现了约一个数量级的内存占用减少。
    *   **硬件加速**：利用 NVIDIA 的张量内存加速器（TMA）进行高效的数据搬运。

#### 3. 实验设计
*   **实验场景**：
    1.  **微基准测试（Microbenchmarks）**：评估 LL 模式下 Dispatch 和 Combine 内核的吞吐量。
    2.  **端到端推理测试**：将 NCCL EP 集成到 **vLLM** 框架中，测试实际服务指标。
*   **对比方法（Baseline）**：主要对比对象为 **DeepEP**（目前最先进的 MoE 通信库）。
*   **测试模型**：Qwen3-30B-A3B（一个 30B 参数的 MoE 模型）。
*   **评估指标**：吞吐量（tokens/s）、首字延迟（TTFT）、间隔延迟（ITL）、每 token 延迟（TPOT）。

#### 4. 资源与算力
*   **硬件平台**：NVIDIA EOS 集群。
*   **GPU 配置**：使用 **H100-80GB HBM3** GPU。
*   **集群规模**：实验涵盖了 1 节点（8 GPU）到 8 节点（64 GPU）的配置。
*   **网络**：8×400 Gbit/s InfiniBand 互连，第四代 NVLink（900 GB/s）。
*   **训练时长**：文中未提及具体训练时长，主要侧重于通信内核性能和推理服务评估。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   LL 内核测试在 1、2、4、8 节点规模下进行，每组实验多次运行并取均值。
    *   vLLM 端到端测试在 1、2、4 节点下进行，每种配置运行 4 次，并使用 IQR 滤波排除异常值。
*   **充分性评价**：
    *   **LL 模式评估较为充分**，涵盖了内核级和框架级的对比。
    *   **HT 模式评估尚不完整**，文中明确指出 HT 模式的训练性能评估和优化仍在进行中，因此缺乏 HT 模式的详细实验数据。
    *   **客观性**：实验在相同的硬件和底层传输层（NCCL GIN）上对比了 NCCL EP 和 DeepEP，确保了公平性。

#### 6. 主要结论与发现
*   **内核性能**：NCCL EP 的 LL Dispatch 内核在多节点规模下达到或超过了 DeepEP 的性能；Combine 内核在 8 节点时与 DeepEP 持平，但在小规模下略有落后（5-12%）。
*   **端到端推理**：在 vLLM 集成中，NCCL EP 的吞吐量和延迟指标比使用 NVSHMEM 后端的 DeepEP 慢约 7-10%。
*   **内存优势**：NCCL EP 显著降低了通信所需的显存占用，这对于显存受限的推理场景至关重要。
*   **生态价值**：成功证明了在 NCCL 内部原生构建 MoE 通信是可行的，且能保持极具竞争力的性能。

#### 7. 优点
*   **原生集成**：作为 NCCL 的扩展，继承了拓扑感知、故障恢复和现有的网络插件架构，无需额外的通信栈。
*   **API 简洁统一**：通过单一接口透明切换 LL 和 HT 模式，降低了框架开发者的适配难度。
*   **内存效率**：提出的内存优化方案解决了大规模 MoE 模型通信缓冲区占用过大的痛点。
*   **跨平台潜力**：基于 C API 构建，易于集成到 TensorRT-LLM、vLLM 等多种运行时环境。

#### 8. 不足与局限
*   **HT 模式数据缺失**：论文未提供 HT 模式（训练场景）的实验结果，其在大规模训练中的表现尚待验证。
*   **性能差距**：在端到端推理中，相比于高度优化的专用库（如基于 NVSHMEM 的 DeepEP），仍存在约 10% 的性能差距。
*   **实验覆盖面**：目前仅在 H100 平台上进行了评估，未展示在其他架构（如 Blackwell 或旧款 A100）上的表现。
*   **状态阶段**：目前仍标记为“实验性 API”，社区反馈和生产环境的长期稳定性有待观察。

（完）
