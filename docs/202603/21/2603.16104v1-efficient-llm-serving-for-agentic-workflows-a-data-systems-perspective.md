---
title: "Efficient LLM Serving for Agentic Workflows: A Data Systems Perspective"
title_zh: 面向智能体工作流的高效 LLM 服务：一种数据系统视角
authors: "Noppanat Wadlom, Junyi Shen, Yao Lu"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16104v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 工作流感知推理框架与缓存感知调度
tldr: 随着智能体工作流成为主流AI负载，其由多个相互依赖的LLM调用组成，存在大量冗余。现有系统如vLLM仅优化单次推理，忽视了跨调用依赖。本文提出Helium框架，从数据系统视角将工作流建模为查询计划，通过主动缓存和缓存感知调度最大化Prompt和KV状态的复用。实验表明，Helium在多种负载下比现有SOTA系统提速达1.56倍，证明了端到端工作流优化的必要性。
selection_source: fresh_fetch
motivation: 现有的LLM推理系统主要针对单次调用进行优化，无法有效处理智能体工作流中复杂的跨调用依赖和数据冗余问题。
method: 提出Helium框架，将智能体工作流视为查询计划，并引入主动缓存与缓存感知调度技术来优化KV状态和Prompt的复用。
result: 在多种工作流负载下，Helium相比于最先进的智能体服务系统实现了高达1.56倍的端到端加速。
conclusion: 采用数据系统视角对LLM工作流进行端到端优化，是实现高效、可扩展智能体服务的关键。
---

## 摘要
智能体工作流由一系列相互依赖的大语言模型（LLM）调用组成，已成为现代 AI 系统中的主流负载。由于推测性执行和并行探索，这些工作流在重叠的提示词和中间结果中表现出大量的冗余。现有的 LLM 服务系统（如 vLLM）专注于优化单个推理调用，而忽略了跨调用的依赖关系，导致效率显著低下。本文从数据系统的视角重新思考了 LLM 和智能体服务，并引入了 Helium。这是一个感知工作流的服务框架，它将智能体负载建模为查询计划，并将 LLM 调用视为一等算子。Helium 集成了主动缓存和感知缓存的调度，以最大限度地提高提示词、KV 状态和工作流之间的复用。通过这些技术，Helium 将经典的查询优化原则与 LLM 服务相结合，在各种负载下相比最先进的智能体服务系统实现了高达 1.56 倍的加速。我们的结果表明，跨工作流的端到端优化对于构建可扩展且高效的基于 LLM 的智能体至关重要。

## Abstract
Agentic workflows are composed of sequences of interdependent Large Language Model (LLM) calls, and they have become a dominant workload in modern AI systems. These workflows exhibit extensive redundancy from overlapping prompts and intermediate results due to speculative and parallel exploration. Existing LLM serving systems, such as vLLM, focus on optimizing individual inference calls and overlook cross-call dependencies, leading to significant inefficiencies. This paper rethinks LLM and agent serving from a data systems perspective and introduces Helium, a workflow-aware serving framework that models agentic workloads as query plans and treats LLM invocations as first-class operators. Helium integrates proactive caching and cache-aware scheduling to maximize reuse across prompts, KV states, and workflows. Through these techniques, Helium bridges classic query optimization principles with LLM serving, achieving up to 1.56x speedup over state-of-the-art agent serving systems on various workloads. Our results demonstrate that end-to-end optimization across workflows is essential for scalable and efficient LLM-based agents.

---

## 论文详细总结（自动生成）

以下是对论文《Efficient LLM Serving for Agentic Workflows: A Data Systems Perspective》的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：现代 AI 应用正从单次 LLM 调用转向复杂的“智能体工作流”（Agentic Workflows），这些工作流由多个相互依赖的 LLM 调用组成。
*   **研究背景**：
    *   **冗余性高**：工作流中存在大量重叠的提示词（Prompts）和中间结果。
    *   **现有系统不足**：现有的推理引擎（如 vLLM）仅优化单次调用，缺乏对全局工作流结构的感知；而编排框架（如 LangGraph）将 LLM 视为黑盒，无法进行底层推理优化（如 KV 缓存复用）。
    *   **视角转换**：论文提出从**数据系统（数据库）**的视角出发，将智能体工作流视为查询计划，将 LLM 调用视为一等算子（First-class Operators），从而实现端到端的全局优化。

### 2. 方法论与核心技术
Helium 框架引入了以下关键技术：
*   **逻辑计划优化（Logical Plan Optimization）**：
    *   **算子剪枝与公共子图消除（CSE）**：识别并合并工作流中重复的计算节点，删除不影响输出的无效分支。
    *   **提示词缓存（Prompt Cache）**：对于确定性算子，若输入相同则直接通过 `CacheFetch` 获取结果，跳过昂贵的推理过程。
*   **模板化基数树（Templated Radix Tree, TRT）**：
    *   一种新型数据结构，用于建模提示词的结构（静态文本+占位符）及其依赖关系。它能识别跨算子、跨查询的共享前缀。
*   **主动缓存管理（Proactive Caching）**：
    *   不同于传统的被动/机会主义缓存，Helium 在编译阶段识别静态前缀，并在执行前预热（Pre-warm）GPU 中的 KV 缓存。
*   **缓存感知调度算法（Cache-Aware Scheduling）**：
    *   **形式化建模**：将调度建模为最小化“令牌步长”（Token Step，类似 Makespan）的优化问题。
    *   **贪心策略**：基于 TRT 进行深度优先搜索（DFS）风格的调度，优先安排具有长共享前缀的任务在同一 Worker 上运行，以最大化 KV 缓存命中率。

### 3. 实验设计
*   **数据集与场景**：
    *   **微基准测试**：涵盖 Map-Reduce、多智能体辩论（Debate）、反思（Reflection）、迭代改进、并行链等 5 种原始模式。
    *   **数据集**：MMLU（通用理解）、TAT-QA（金融问答）、Amazon Reviews（摘要生成）。
    *   **端到端场景**：构建了一个复杂的“金融交易（Trading）”工作流，包含 19 个智能体和 88 个 LLM 算子。
*   **对比方法（Baselines）**：
    *   **vLLM**：原生推理引擎（顺序执行）。
    *   **OpWise**：类似 Spark/Dask 的算子级批处理。
    *   **LangGraph / AgentScope**：主流智能体编排框架。
    *   **Parrot / KVFlow**：最先进的感知前缀或工作流的 LLM 服务系统。

### 4. 资源与算力
*   **硬件环境**：
    *   **CPU**：AMD EPYC 9554 (64核)。
    *   **GPU**：**2 张 NVIDIA H100 NVL (94GB)**。
*   **模型**：Qwen3-8B、14B、32B 以及 Llama-3.1-8B。
*   **软件基础**：基于 vLLM v0.16.0 进行开发。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽测试，包括 7 种原始工作流组合、1 个大规模端到端案例研究。
*   **消融实验**：分别验证了 KV 缓存、计划剪枝、提示词缓存和调度算法对性能的贡献。
*   **敏感性分析**：测试了不同 Batch Size（8 到 80）、并行分支数、辩论轮数、Worker 数量以及内存限制下的表现。
*   **客观性**：通过 MILP（混合整数线性规划）求解理论最优解，证明了其调度算法的接近最优性（差距仅 0.9%）。实验设计严谨，对比了多种 SOTA 系统，具有较高的说服力。

### 6. 主要结论与发现
*   **性能提升**：Helium 在原始工作流上比 SOTA 系统快 **1.56 倍**，在复杂金融工作流上实现 **1.34 倍** 加速。
*   **缓存效率**：通过全局调度，KV 缓存命中率显著提升（如在 Qwen3-8B 上比 LangGraph 高出约 22%）。
*   **规模效应**：随着 Batch Size 增加或模型变大，Helium 的优化收益更加明显，证明了其在处理大规模负载时的优越性。

### 7. 优点与亮点
*   **理论与工程结合**：将经典的数据库查询优化理论（Volcano 模型、CSE 等）成功移植到 LLM 推理领域。
*   **主动性**：改变了现有系统“看到请求再匹配缓存”的被动模式，通过静态分析实现主动预热。
*   **高效调度**：TRT 结构巧妙地平衡了任务并行度与缓存局部性。

### 8. 不足与局限
*   **动态控制流限制**：目前的 DAG 模型对包含复杂循环（Loops）或运行时动态分支的工作流支持有限。
*   **外部 API 干扰**：若智能体频繁调用外部工具（如搜索、数据库），其不可预测的延迟会干扰成本模型的准确性。
*   **同质模型假设**：实验主要基于同一基础模型的不同角色，未深入探讨跨异构模型（如混合使用 GPT-4 和 Llama）的复杂优化。

（完）
