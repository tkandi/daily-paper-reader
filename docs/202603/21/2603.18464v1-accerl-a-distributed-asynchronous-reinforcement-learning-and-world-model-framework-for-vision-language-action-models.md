---
title: "AcceRL: A Distributed Asynchronous Reinforcement Learning and World Model Framework for Vision-Language-Action Models"
title_zh: AcceRL：一种用于视觉-语言-动作模型的分布式异步强化学习与世界模型框架
authors: "Chengxuan Lu, Shukuan Wang, Yanjie Li, Wei Liu, Shiji Jin, Fuyuan Qian, Peiming Li, Baigui Sun, Yang Liu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18464v1"
tags: ["query:mlsys"]
score: 10.0
evidence: 用于大规模模型的分布式异步强化学习框架
tldr: AcceRL是一个针对大规模视觉-语言-动作（VLA）模型设计的分布式异步强化学习框架。它通过物理隔离训练、推理和采样过程，消除了同步障碍，并首次将可训练的世界模型集成到异步流水线中以生成虚拟经验。该框架在LIBERO基准测试中达到了SOTA水平，显著提升了硬件利用率、吞吐量和样本效率，为复杂控制任务提供了高效的训练方案。
selection_source: fresh_fetch
motivation: 旨在解决大规模VLA模型在强化学习训练中面临的计算效率瓶颈和数据获取难题。
method: 采用完全异步且解耦的架构物理隔离核心环节，并引入即插即用的世界模型生成虚拟经验。
result: 在LIBERO基准上取得SOTA性能，实现吞吐量的超线性扩展及卓越的硬件利用率。
conclusion: AcceRL证明了异步解耦架构与世界模型结合能显著提升大规模VLA模型的训练效率与稳定性。
---

## 摘要
大规模视觉-语言-动作（VLA）模型的强化学习（RL）在计算效率和数据获取方面面临重大挑战。我们提出了 AcceRL，这是一个完全异步且解耦的强化学习框架，旨在通过物理隔离训练、推理和采样过程来消除同步障碍。至关重要的是，AcceRL 首次将即插即用、可训练的世界模型集成到分布式异步强化学习流水线中，以生成虚拟经验。在 LIBERO 基准测试上的实验表明，AcceRL 达到了当前最先进（SOTA）的性能。在系统层面，它在吞吐量上表现出超线性扩展，并具有极高的硬件利用率。在算法层面，世界模型增强变体在复杂控制任务中提供了前所未有的样本效率和稳健的训练稳定性。

## Abstract
Reinforcement learning (RL) for large-scale Vision-Language-Action (VLA) models faces significant challenges in computational efficiency and data acquisition. We propose AcceRL, a fully asynchronous and decoupled RL framework designed to eliminate synchronization barriers by physically isolating training, inference, and rollouts. Crucially, AcceRL is the first to integrate a plug-and-play, trainable world model into a distributed asynchronous RL pipeline to generate virtual experiences. Experiments on the LIBERO benchmark demonstrate that AcceRL achieves state-of-the-art (SOTA) performance. Systematically, it exhibits super-linear scaling in throughput and highly efficient hardware utilization. Algorithmically, the world-model-augmented variant delivers unprecedented sample efficiency and robust training stability in complex control tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《AcceRL: A Distributed Asynchronous Reinforcement Learning and World Model Framework for Vision-Language-Action Models》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大规模视觉-语言-动作（VLA）模型在强化学习（RL）训练中面临严重的**计算效率瓶颈**和**数据获取难题**。
*   **研究背景**：
    *   **同步障碍**：传统 RL 框架采用同步机制（收集数据-更新参数），导致高性能 GPU 在等待缓慢的物理模拟器或长尾轨迹结束时处于闲置状态。
    *   **数据饥渴**：VLA 模型参数量巨大，仅依赖物理模拟器采样速度太慢，难以满足训练需求。
    *   **策略滞后**：在异步架构中，采样策略与训练策略不一致会导致算法不稳定。

### 2. 方法论
AcceRL 提出了一个完全异步且解耦的架构，核心思想是将训练、推理和采样在物理上隔离。
*   **宏观异步（训练与采样解耦）**：训练器（Trainer）从分布式重放池采样，而采样器（Rollout Workers）持续与环境交互并推送数据，互不阻塞。利用 ZeRO-2 优化内存。
*   **微观异步（交互与推理解耦）**：采用“推理即服务”模式，物理模拟在 CPU 上运行，动作推理集中在 GPU 推理池，通过动态批处理（Dynamic Batching）最大化 GPU 利用率。
*   **可训练世界模型（World Model）**：首次在异步流水线中集成基于扩散模型（Diffusion-based）的世界模型。通过“在想象中学习”，利用合成经验替代部分物理交互，将样本效率提升了 200 倍。
*   **算法优化细节**：
    *   **GIPO 算法**：使用高斯重要性采样策略优化，通过软约束代替 PPO 的硬截断，缓解异步带来的策略滞后问题。
    *   **价值重计算与全局优势归一化**：在训练前重新评估旧轨迹的价值，确保梯度信号的准确性。
    *   **词表瘦身与 Token 级优化**：裁剪 VLA 的语言头，仅保留动作 Token，并进行 Token 级别的策略梯度计算以增强数值稳定性。

### 3. 实验设计
*   **数据集/场景**：使用 **LIBERO** 机器人操作基准测试，涵盖四个子集：LIBERO-Spatial（空间）、LIBERO-Object（物体）、LIBERO-Goal（目标）和 LIBERO-Long（长程任务）。
*   **对比方法**：
    *   **OpenVLA-OFT**（基于模仿学习的基准）。
    *   **SimpleVLA-RL** 和 **RLinf-VLA**（最新的 VLA 强化学习框架）。
*   **评估指标**：任务成功率、系统吞吐量（SPS，每秒样本数）、硬件利用率。

### 4. 资源与算力
*   **GPU 型号**：实验中提到了使用 **NVIDIA H200** GPU。
*   **规模**：
    *   **训练端**：扩展至 7 个 Trainer GPU，实现了超线性加速（达到 104.22 SPS）。
    *   **采样端**：扩展至 64 个 Rollout Workers，表现出近乎线性的吞吐量增长。
*   **模型规模**：基于 7B 参数量的 VLA 模型（如 OpenVLA）。

### 5. 实验数量与充分性
*   **实验覆盖**：涵盖了 LIBERO 的四大类复杂任务，并进行了大规模的扩展性测试（从 1 个 GPU 到多个 GPU 的吞吐量对比）。
*   **消融实验**：针对“价值重计算”机制和“GIPO 算法 vs PPO”进行了专门的对比实验，验证了各组件对稳定性的贡献。
*   **充分性评价**：实验设计较为全面，不仅关注算法性能（成功率），还深入探讨了分布式系统的效率问题，数据对比客观。

### 6. 主要结论与发现
*   **性能领先**：AcceRL 在 LIBERO 所有类别中均达到 SOTA，尤其在长程任务（LIBERO-Long）中表现出极强的鲁棒性（成功率 99.1%）。
*   **超线性扩展**：得益于 ZeRO 优化减少了单卡内存压力并增加了微批次大小，系统吞吐量随 GPU 增加呈现超线性增长。
*   **世界模型威力**：引入世界模型后，仅需 10,000 次真实环境交互即可达到传统方法需要数百万次交互才能达到的性能，效率提升 20,000%。

### 7. 优点
*   **系统架构创新**：彻底解决了 VLA 训练中的 GPU 闲置问题，实现了极高的硬件利用率（>94%）。
*   **即插即用**：世界模型模块化设计，可根据需要开启，极大地缓解了具身智能数据获取难的问题。
*   **算法稳定性**：通过 GIPO 和 Token 级优化，解决了大规模生成式模型在强化学习中的梯度爆炸和不收敛问题。

### 8. 不足与局限
*   **任务范围**：实验主要集中在 LIBERO 模拟环境，尚未展示在真实物理机器人上的迁移效果。
*   **模型支持**：论文提到目前该框架尚未支持大规模语言模型（LLM）的后训练对齐（Alignment），主要侧重于动作控制。
*   **计算开销**：虽然世界模型减少了环境交互，但训练和运行扩散模型本身需要额外的计算资源。

（完）
