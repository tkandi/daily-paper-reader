---
title: Ruyi2.5 Technical Report
title_zh: Ruyi2.5 技术报告
authors: "Huan Song, Shuyu Tian, Qingfei Zhao, Wenhao Hong, Jiang Liu, Ting Long, Jiawei Shao, Xuelong Li"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17311v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 用于大规模多模态模型协同训练的统一流水线与框架
tldr: Ruyi2.5是基于AI Flow框架的多模态家族模型，延续了“一次训练，多次部署”的范式，通过共享骨干架构在统一流水线中协同训练不同规模的模型。该研究推出了隐私保护摄像服务系统Ruyi2.5-Camera，采用边缘去标识化与云端推理的两阶段架构，并提出BPPO算法显著提升强化学习微调效率。实验证明其在通用基准上比肩Qwen3-VL，且在隐私监控任务中表现更优。
selection_source: fresh_fetch
motivation: 旨在解决多模态模型在不同规模部署时的语义一致性问题，并提升隐私保护场景下的监控效率与强化学习训练速度。
method: 采用共享骨干架构的协同训练流水线，结合信息瓶颈引导的特征映射实现隐私脱敏，并提出二元前缀策略优化（BPPO）加速微调。
result: Ruyi2.5在通用多模态基准上达到Qwen3-VL水平，而Ruyi2.5-Camera在隐私受限的监控任务中显著超越了Qwen3-VL。
conclusion: Ruyi2.5通过统一的训练架构和创新的优化算法，成功实现了高性能、高效率且具备隐私保护能力的多模态模型部署。
---

## 摘要
我们介绍了 Ruyi2.5，这是一个基于 AI Flow 框架构建的多模态家族模型。Ruyi2.5 将 Ruyi2 的“一次训练，多次部署”范式扩展到多模态领域，构建了一个共享骨干网络架构，在单一统一流水线中协同训练不同规模的模型，确保了所有部署层级间的语义一致性。基于 Ruyi2.5，我们开发了 Ruyi2.5-Camera 模型作为一种隐私保护摄像头服务系统，该系统将 Ruyi2.5-Camera 实例化为一个两阶段识别流水线：边缘模型在源头应用信息瓶颈引导的不可逆特征映射对原始帧进行去标识化处理，而云端模型则执行深度行为推理。为了加速强化学习微调，我们进一步提出了二值前缀策略优化（Binary Prefix Policy Optimization, BPPO），该方法通过二值响应选择减少样本冗余，并将梯度更新集中在响应前缀上，相比 GRPO 实现了 2 到 3 倍的训练加速。实验表明，Ruyi2.5 在通用多模态基准测试中与 Qwen3-VL 表现相当，而 Ruyi2.5-Camera 在隐私受限的监控任务中显著优于 Qwen3-VL。

## Abstract
We present Ruyi2.5, a multimodal familial model built on the AI Flow framework. Extending Ruyi2's "Train Once, Deploy Many" paradigm to the multimodal domain, Ruyi2.5 constructs a shared-backbone architecture that co-trains models of varying scales within a single unified pipeline, ensuring semantic consistency across all deployment tiers. Built upon Ruyi2.5, Ruyi2.5-Camera model is developed as a privacy-preserving camera service system, which instantiates Ruyi2.5-Camera into a two-stage recognition pipeline: an edge model applies information-bottleneck-guided irreversible feature mapping to de-identify raw frames at the source, while a cloud model performs deep behavior reasoning. To accelerate reinforcement learning fine-tuning, we further propose Binary Prefix Policy Optimization (BPPO), which reduces sample redundancy via binary response selection and focuses gradient updates on response prefixes, achieving a 2 to 3 times training speedup over GRPO. Experiments show Ruyi2.5 matches Qwen3-VL on the general multimodal benchmarks, while Ruyi2.5-Camera substantially outperforms Qwen3-VL on privacy-constrained surveillance tasks.

---

## 论文详细总结（自动生成）

以下是对《Ruyi2.5 Technical Report》的结构化总结：

### 1. 核心问题与研究背景
*   **核心矛盾**：在智能视频分析（如跌倒检测、打斗识别）中，**隐私保护**与**语义理解**之间存在天然冲突。传统方法（如模糊处理、骨干提取）往往在脱敏的同时丢失了关键语义，导致识别精度下降。
*   **部署挑战**：多模态大模型（MLLM）在不同规模（边缘端 vs 云端）部署时，通常需要独立训练，这不仅耗费巨大算力，还难以保证不同规模模型间的语义一致性。
*   **训练效率**：现有的强化学习微调（RLVR）方法（如 GRPO）在处理长链推理时，存在样本冗余度高、计算成本昂贵的问题。

### 2. 方法论
Ruyi2.5 基于 **AI Flow** 理论框架，提出了以下核心技术：
*   **共享骨干家族模型架构（Shared-Backbone Familial Model）**：
    *   采用“一次训练，多次部署”范式。通过参数继承和灵活分支，在单一流水线中同时训练不同规模（如 1.9B 和 8B）的模型。
    *   确保了边缘端（轻量级）和云端（全量级）模型处于同一语义空间，实现无缝的任务路由和协同。
*   **Ruyi2.5-Camera 隐私保护流水线**：
    *   **边缘端**：利用信息瓶颈（Information Bottleneck）理论，对原始图像进行非线性映射和随机噪声注入，生成不可逆的抽象特征向量（去标识化），从数学上防止原始图像被还原。
    *   **云端**：大模型直接对脱敏后的特征进行深度行为推理，处理复杂的时空动态和多人交互。
*   **二值前缀策略优化（BPPO）算法**：
    *   **样本筛选**：在每个 Prompt 生成的响应组中，仅保留一个正样本和一个负样本进行梯度更新，消除组内冗余。
    *   **前缀约束**：仅对响应的前 $n$ 个 Token 进行梯度回传。研究发现早期 Token 决定了推理轨迹，此举在不损失质量的前提下实现了 2-3 倍的训练加速。

### 3. 实验设计
*   **数据集与场景**：
    *   **通用能力**：涵盖视觉问答（VQA）、STEM 推理、文档理解（OCR、图表、PDF）、代码生成等。
    *   **特定场景**：构建了针对隐私敏感环境（洗手间、更衣室、医疗设施）的行为识别基准，包含跌倒、吸烟、打斗等风险行为。
*   **对比基准（Benchmark）**：
    *   通用多模态基准测试。
    *   隐私受限的监控任务基准。
*   **对比方法**：主要对比对象为同规模的 **Qwen3-VL** 系列模型。

### 4. 资源与算力
*   **算力说明**：报告中**未明确给出**具体的 GPU 型号（如 H100/A100）、集群规模或具体的训练总时长。
*   **效率提升**：文中强调了 BPPO 算法相比 GRPO 实现了 2-3 倍的训练加速，且通过家族模型协同训练减少了重复训练不同规模模型的总计算开销。

### 5. 实验数量与充分性
*   **实验规模**：对比了 1.9B 和 8B 两个量级的模型。
*   **充分性评价**：
    *   **通用性验证**：在标准多模态任务上证明了 Ruyi2.5 达到了当前 SOTA 模型（Qwen3-VL）的水平。
    *   **垂直领域验证**：在隐私监控任务上，Ruyi2.5-Camera-8B 的 F1-Score 达到 93.67，显著超过 Qwen3-VL-8B（82.77），证明了其在特定约束下的优越性。
    *   **消融/效率验证**：对 BPPO 算法的加速效果进行了量化对比。实验设计较为客观，涵盖了从基础感知到深度推理的多个维度。

### 6. 主要结论与发现
*   **语义一致性**：家族模型架构成功实现了不同规模模型间的语义对齐，支持边缘-云端的动态任务切换。
*   **隐私与性能兼得**：通过信息瓶颈引导的特征映射，可以在不传输原始像素的情况下，保持极高的行为识别准确率。
*   **训练优化**：BPPO 证明了在强化学习中，关注“判别性样本”和“推理前缀”是提升大规模多模态模型训练效率的有效途径。

### 7. 优点
*   **架构创新**：将“家族模型”概念引入多模态领域，解决了多尺度部署的协同问题。
*   **隐私方案完备**：不仅是视觉上的脱敏，而是从信息论角度实现了特征级的不可逆保护。
*   **训练高效**：BPPO 算法针对长链推理的优化具有很强的实用价值，显著降低了 RL 成本。

### 8. 不足与局限
*   **硬件细节缺失**：缺乏具体的算力消耗和超参数配置，不利于社区复现。
*   **隐私攻击测试不足**：虽然理论上基于信息瓶颈不可逆，但文中未展示针对该特征映射的对抗性攻击或模型逆向攻击的具体实验数据。
*   **应用限制**：边缘端仍需具备一定的计算能力（运行 1.9B 模型的单层 Transformer 或特征映射），对于极低功耗的嵌入式传感器可能仍有压力。

（完）
