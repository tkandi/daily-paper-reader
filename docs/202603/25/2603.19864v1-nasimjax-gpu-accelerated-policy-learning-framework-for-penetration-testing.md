---
title: "NASimJax: GPU-Accelerated Policy Learning Framework for Penetration Testing"
title_zh: NASimJax：用于渗透测试的 GPU 加速策略学习框架
authors: "Raphael Simon, José Carrasquel, Wim Mees, Pieter Libin"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19864v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 基于 JAX 的 GPU 加速训练框架
tldr: 本研究针对渗透测试强化学习训练慢、难扩展的问题，推出基于JAX的高性能模拟器NASimJax，实现比原版快100倍的吞吐量。通过将渗透测试建模为上下文POMDP并引入多样化网络生成算法，研究深入探讨了零样本泛化。提出了两阶段动作分解（2SAS）策略，显著提升了大规模网络下的表现，为自动化渗透测试提供了高效、可扩展的研究平台。
selection_source: fresh_fetch
motivation: 现有的渗透测试模拟器运行缓慢，限制了强化学习在复杂、大规模网络场景下的训练效率和泛化能力。
method: 开发了基于JAX的NASimJax框架实现硬件加速，并提出两阶段动作分解（2SAS）及基于上下文POMDP的场景生成流水线。
result: 实验证明NASimJax能有效处理40个主机规模的网络，且2SAS在处理线性增长的动作空间时表现优于传统的动作掩码。
conclusion: NASimJax为RL驱动的渗透测试提供了快速且现实的平台，揭示了拓扑稀疏性和优先级采样对策略泛化的重要影响。
---

## 摘要
渗透测试是模拟网络攻击以识别漏洞的实践，是一项复杂的序列决策任务，本质上具有部分可观测性，并具有大规模动作空间的特点。在该领域训练强化学习（RL）策略面临一个根本性的瓶颈：现有的模拟器在真实网络场景的大规模训练中速度过慢，导致策略无法泛化。我们提出了 NASimJax，这是网络攻击模拟器（NASim）的一个完全基于 JAX 的重新实现，其环境吞吐量比原始模拟器高出多达 100 倍。通过在硬件加速器上运行整个训练流水线，NASimJax 使得在固定计算预算下对以前无法实现的大型网络进行实验成为可能。我们将自动化渗透测试建模为上下文部分可观测马尔可夫决策过程（Contextual POMDP），并引入了一个网络生成流水线，该流水线能够产生结构多样且保证可解的场景。这些共同为研究零样本策略泛化提供了原则性基础。我们利用该框架研究了多达 40 个主机的网络中的动作空间扩展和泛化问题。我们发现，优先级别重放（Prioritized Level Replay）比领域随机化（Domain Randomization）能更好地处理密集训练分布，特别是在大规模情况下；此外，在较稀疏的拓扑结构上进行训练会产生一种隐式课程，从而提高分布外泛化能力，即使是在比训练时更密集的拓扑结构上也是如此。为了处理线性增长的动作空间，我们提出了一种两阶段动作分解（2SAS），在大规模情况下其性能显著优于扁平动作掩码（flat action masking）。最后，我们识别出一种由优先级别重放的回合重置行为与 2SAS 的信用分配结构之间的相互作用引起的失效模式。因此，NASimJax 为推进基于强化学习的渗透测试提供了一个快速、灵活且真实的平台。

## Abstract
Penetration testing, the practice of simulating cyberattacks to identify vulnerabilities, is a complex sequential decision-making task that is inherently partially observable and features large action spaces. Training reinforcement learning (RL) policies for this domain faces a fundamental bottleneck: existing simulators are too slow to train on realistic network scenarios at scale, resulting in policies that fail to generalize. We present NASimJax, a complete JAX-based reimplementation of the Network Attack Simulator (NASim), achieving up to 100x higher environment throughput than the original simulator. By running the entire training pipeline on hardware accelerators, NASimJax enables experimentation on larger networks under fixed compute budgets that were previously infeasible. We formulate automated penetration testing as a Contextual POMDP and introduce a network generation pipeline that produces structurally diverse and guaranteed-solvable scenarios. Together, these provide a principled basis for studying zero-shot policy generalization. We use the framework to investigate action-space scaling and generalization across networks of up to 40 hosts. We find that Prioritized Level Replay better handles dense training distributions than Domain Randomization, particularly at larger scales, and that training on sparser topologies yields an implicit curriculum that improves out-of-distribution generalization, even on topologies denser than those seen during training. To handle linearly growing action spaces, we propose a two-stage action decomposition (2SAS) that substantially outperforms flat action masking at scale. Finally, we identify a failure mode arising from the interaction between Prioritized Level Replay's episode-reset behaviour and 2SAS's credit assignment structure. NASimJax thus provides a fast, flexible, and realistic platform for advancing RL-based penetration testing.

---

## 论文详细总结（自动生成）

### NASimJax: GPU 加速的渗透测试策略学习框架论文总结

#### 1. 核心问题与整体含义
*   **研究动机**：自动化渗透测试（Penetration Testing）是识别网络漏洞的关键，但将其建模为强化学习（RL）任务面临三大挑战：**部分可观测性**、**巨大的动作空间**以及**模拟器性能瓶颈**。
*   **核心问题**：现有的渗透测试模拟器（如 NASim）多基于 CPU 运行，速度极慢，导致在大规模、复杂网络场景下训练 RL 代理变得不可行，且难以进行充分的超参数搜索，最终导致模型泛化能力差。
*   **整体含义**：本文推出了 **NASimJax**，一个基于 JAX 的高性能、可扩展框架，旨在通过 GPU 加速和算法创新（如动作空间分解），解决大规模网络攻击模拟中的训练效率与泛化难题。

#### 2. 方法论
*   **核心思想**：利用 JAX 的即时编译（JIT）和向量化（vmap）能力，将整个环境模拟和 RL 训练流水线移至 GPU/TPU，消除 CPU-GPU 通信开销。
*   **关键技术细节**：
    *   **上下文 POMDP 建模**：将渗透测试形式化为 Contextual POMDP，其中每个“上下文”代表一个具体的网络实例，支持研究零样本（Zero-shot）策略迁移。
    *   **网络生成流水线**：设计了新的随机生成算法，通过控制拓扑密度（$t_d$）、服务密度等参数生成结构多样且**保证可解**的网络。
    *   **两阶段动作选择（2SAS）**：针对随主机数量线性增长的动作空间，将决策分解为“选择主机”和“在该主机上选择动作”两个阶段。这显著降低了每一层的决策复杂度。
    *   **奖励缩放**：根据网络中敏感主机的理论最大奖励进行归一化，确保不同规模网络间的学习信号具有可比性。
    *   **算法集成**：实现了 PPO 算法，并结合了优先级别重放（PLR）和领域随机化（DR）等无监督环境设计方法。

#### 3. 实验设计
*   **场景设置**：构建了 16、26 和 40 个主机规模的网络场景。
*   **Benchmark 与对比方法**：
    *   **性能对比**：对比原始 Python 版 NASim。
    *   **动作空间处理**：对比“扁平动作掩码（Flat Masking）”与本文提出的“2SAS”。
    *   **泛化性实验**：对比领域随机化（DR）、优先级别重放（PLR）及其变体 PLR$^\perp$。
*   **评估指标**：吞吐量（Steps/s）、解决率（Solve Rate）、零样本策略迁移表现。

#### 4. 资源与算力
*   **硬件环境**：Intel Xeon Gold 6230R CPU @ 2.10GHz，**NVIDIA RTX A4000 GPU**。
*   **训练预算**：
    *   16 主机网络：1 亿步（100M steps）。
    *   26 主机网络：5 亿步（500M steps）。
    *   40 主机网络：10 亿步（1B steps）。
*   **性能表现**：NASimJax 达到了最高 **160 万步/秒** 的吞吐量，比原始 NASim 快约 **100 倍**。

#### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的超参数搜索（每个算法 250 次贝叶斯采样试验），并在最终评估中使用 5 个独立随机种子以确保结果的鲁棒性。
*   **充分性评价**：实验设计较为充分。涵盖了从性能基准到算法消融（2SAS vs Masking），再到复杂的分布外（OOD）泛化测试。通过改变拓扑密度（$t_d$）来测试策略在不同复杂程度网络间的迁移能力，体现了实验的客观性与深度。

#### 6. 主要结论与发现
*   **加速效果显著**：GPU 加速使得在固定计算预算下训练更大规模的网络成为可能。
*   **2SAS 的优越性**：在 40 主机的大规模场景下，2SAS 的解决率（42%）远高于传统掩码方法（14%），证明了动作分解在处理大规模动作空间时的必要性。
*   **隐式课程效应**：在稀疏拓扑（低密度）上训练的策略，在密集拓扑（高密度）上表现出更好的泛化性，这表明稀疏网络提供了一种自然的学习路径。
*   **PLR 的鲁棒性**：相比 DR，PLR 能更好地处理密集的训练分布，通过重放机制构建有效的学习课程。
*   **失效模式识别**：发现 PLR 的回合重置机制与 2SAS 的信用分配在超大规模网络中存在冲突，导致训练崩溃，为未来改进指明了方向。

#### 7. 优点
*   **极致性能**：100 倍的加速彻底改变了该领域的研究节奏。
*   **架构优雅**：基于 JAX 的函数式编程设计，易于集成到现代深度学习生态中。
*   **解决实际痛点**：2SAS 有效缓解了渗透测试中动作空间爆炸的问题。
*   **科学的生成机制**：保证了生成场景的可解性，避免了无效训练。

#### 8. 不足与局限
*   **仿真抽象度**：虽然比前代更强，但仍属于抽象模拟，与包含真实虚拟机、复杂流量和防御者动态行为的完全仿真环境（Emulation）相比，仍存在“仿真到现实”的差距。
*   **特定失效**：在 40 主机规模下，PLR 与 2SAS 的结合出现了不稳定性，显示出算法在高维状态-动作空间下的脆弱性。
*   **防御者建模**：目前主要侧重于攻击方，对动态防御者（如主动响应、蜜罐）的建模相对较少。

（完）
