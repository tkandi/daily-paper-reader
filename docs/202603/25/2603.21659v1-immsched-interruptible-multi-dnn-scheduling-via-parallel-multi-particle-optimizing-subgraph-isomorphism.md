---
title: "IMMSched: Interruptible Multi-DNN Scheduling via Parallel Multi-Particle Optimizing Subgraph Isomorphism"
title_zh: IMMSched：通过并行多粒子优化子图同构实现的可中断多 DNN 调度
authors: "Boran Zhao, Hetian Liu, Zihang Yuan, Yanbin Hu, Wenzhe Zhao, Tian Xia, Pengju Ren"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21659v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 边缘加速器上的可中断多DNN任务调度
tldr: 针对边缘加速器上不可预测的多DNN任务调度难题，本文提出IMMSched框架。该框架将调度建模为NP难的子图同构问题，通过结合多粒子优化与Ullmann算法，并利用概率连续松弛方案实现并行化，消除了传统方法的串行依赖。配合硬件层面的量化调度和全局控制器，IMMSched显著降低了调度延迟与功耗，实现了不可预测任务的实时可中断调度。
selection_source: fresh_fetch
motivation: 现有调度框架多依赖CPU离线计算且难以应对不可预测的任务到达，无法满足边缘端实时可中断调度的需求。
method: 提出一种结合多粒子优化与Ullmann算法的并行子图同构方法，并配合硬件量化调度方案实现高效探索。
result: 实验结果显示，IMMSched在调度延迟和能耗上实现了数量级的降低，支持复杂DNN任务的实时执行。
conclusion: IMMSched通过算法并行化与硬件协同设计，有效解决了边缘加速器在处理动态多任务负载时的效率与实时性瓶颈。
---

## 摘要
对具有不可预测任务到达时间的多 DNN 工作负载的需求日益增长，凸显了在边缘加速器上进行可中断调度的必要性。然而，现有的抢占式框架通常假设任务到达时间已知，并依赖基于 CPU 的离线调度，这会产生沉重的运行时开销，且难以处理不可预测的任务到达。更糟糕的是，先前的研究表明，多 DNN 调度需要在有限时间内解决大型有向无环图上的 NP 难子图同构问题，这极具挑战性。为了解决这一问题，我们提出了 IMMSched，这是一种并行子图同构方法，它将多粒子优化与基于概率连续松弛方案的 Ullmann 算法相结合，消除了先前工作中的串行数据依赖。最后，硬件架构中的量化调度方案和全局控制器进一步结合了多粒子结果，以进行共识引导的探索。评估结果表明，IMMSched 在调度延迟和功耗方面实现了数量级的降低，从而能够在边缘加速器上实时执行不可预测的 DNN 任务。

## Abstract
The growing demand for multi-DNN workloads with unpredictable task arrival times has highlighted the need for interruptible scheduling on edge accelerators. However, existing preemptive frameworks typically assume known task arrival times and rely on CPU-based offline scheduling, which incurs heavy runtime overhead and struggles to handle unpredictable task arrivals. Even worse, prior studies have shown that multi-DNN scheduling requires solving an NP-hard subgraph isomorphism problem on large directed acyclic graphs within limited time, which is extremely challenging. To tackle this, we propose IMMSched, a parallel subgraph isomorphism method that combines Multi-Particle Optimization with the Ullmann algorithm based on a probabilistic continuous-relaxation scheme, eliminating the serial data dependencies of previous works. Finally, a quantized scheduling scheme and a global controller in the hardware architecture further combine multi-particle results for consensus-guided exploration. Evaluations demonstrate that IMMSched achieves orders-of-magnitude reductions in scheduling latency and energy consumption, enabling real-time execution of unpredictable DNN tasks on edge accelerators.

---

## 论文详细总结（自动生成）

以下是对论文《IMMSched: Interruptible Multi-DNN Scheduling via Parallel Multi-Particle Optimizing Subgraph Isomorphism》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在边缘加速器上，如何高效处理具有**不可预测到达时间**的多DNN任务实时调度。
*   **研究背景**：
    *   现有的抢占式调度框架（如PREMA, Planaria等）大多假设任务到达时间已知，且依赖CPU进行离线串行调度，导致运行时开销巨大，无法应对突发任务。
    *   先进的调度模式（如Tile Spatial Scheduling, TSS）将调度建模为**子图同构（Subgraph Isomorphism）**问题。由于该问题是NP-hard，在大型有向无环图（DAG）上求解极其耗时，成为实时调度的瓶颈。
    *   边缘设备资源受限，无法负担专门的硬件调度器，且传统算法的串行依赖限制了并行加速的可能性。

### 2. 方法论
IMMSched 提出了一种软硬件协同的设计方案，核心思想是将调度算法并行化并复用DNN加速器硬件进行计算：
*   **并行子图匹配算法**：结合了**粒子群优化（PSO）**与经典的**Ullmann算法**。PSO负责全局探索，Ullmann负责可行性验证。通过将Ullmann的串行步骤分解为可并行的矩阵运算，消除了数据依赖。
*   **概率连续松弛方案**：针对子图匹配的离散组合搜索空间，引入连续松弛矩阵 $S$（表示映射概率），将离散问题转化为连续优化问题，解决了PSO在离散空间搜索不稳定的问题。
*   **硬件复用与量化调度**：
    *   利用加速器原有的定点运算单元（MAC）执行调度算法，将松弛矩阵 $S$ 量化为8位整数，避免浮点运算开销。
    *   **硬件改进**：在加速器中加入轻量级的全局控制器和树状累加器（支持索引输出），用于融合多粒子搜索结果并引导共识探索。
*   **自适应抢占策略**：基于“单核抢占率”策略，优先抢占执行时间余量（Slack）最大的低优先级任务，确保高优先级任务实时执行的同时减少对原任务的影响。

### 3. 实验设计
*   **工作负载（Workloads）**：分为三类复杂度：
    *   **Simple**：MobileNetV2, ResNet50, UNet（常用于AR/VR）。
    *   **Middle**：EfficientNet, NASNet, PNASNet（神经架构搜索模型）。
    *   **Complex**：DeepSeek-7B, Qwen-7B, Llama-3-8B（大语言模型）。
*   **实验场景**：涵盖边缘端（Edge, 64 MACs）和云端（Cloud, 128 MACs）两种配置。
*   **对比方法（Baselines）**：
    *   基于层级时间调度（LTS）的方法：PREMA, CD-MSA, Planaria, MoCA。
    *   基于空间切片调度（TSS）的方法：IsoSched。
*   **评价指标**：加速比（Speedup）、延迟受限吞吐量（LBT）、能效比（Energy Efficiency）。

### 4. 资源与算力
*   **硬件建模**：使用 Verilog 实现引擎和片上网络（NoC），通过 Synopsys Design Compiler 在 **FreePDK 45nm** 工艺下进行综合。
*   **模拟工具**：使用 CACTI-P 建模 SRAM 功耗和面积，使用 McPAT 1.3 建模 NoC 功耗。
*   **算力规格**：边缘端配置为 128x128 引擎阵列，主频 700MHz。
*   **训练/运行**：论文未提及具体的训练时长，因为其核心是推理阶段的动态调度算法，而非模型训练。

### 5. 实验数量与充分性
*   **实验规模**：针对 3 种复杂度等级的 9 个主流模型进行了测试，并在边缘和云端两种硬件规格下进行了交叉验证。
*   **充分性**：实验涵盖了从传统卷积网络到最新大语言模型（LLM）的广泛谱系，对比了当前最先进的 LTS 和 TSS 调度基准。
*   **客观性**：通过泊松分布模拟任务到达，符合真实开放场景的随机性；指标评价全面（性能、吞吐、能效）。

### 6. 主要结论与发现
*   **性能提升**：相比于 LTS 类方法，IMMSched 在加速比上平均提升了 **27.9x 至 81.4x**；相比于同为 TSS 类的 IsoSched，提升了 **1.6x**。
*   **吞吐量**：在延迟受限的吞吐量（LBT）上，IMMSched 比 IsoSched 提升了 **3.4x**，主要归功于并行 PSO 算法和连续松弛机制带来的快速收敛。
*   **能效**：由于减少了 DRAM 访问并复用了硬件单元，能效比相比 LTS 方法提升了三个数量级（最高 **2722x**），比 IsoSched 提升了 **3.43x**。
*   **实时性**：证明了在边缘端利用 NPU 自身算力进行在线调度是可行的，且能显著降低调度延迟。

### 7. 优点
*   **软硬协同创新**：不只是改进算法，而是思考如何利用 NPU 现有的定点计算能力来解决复杂的调度算法难题。
*   **解决 NP-hard 瓶颈**：通过连续松弛和多粒子并行化，将原本难以在线处理的子图同构问题带入了实时处理范畴。
*   **前瞻性场景**：针对自动驾驶、人机交互等“不可预测任务”场景，具有很强的实际应用价值。

### 8. 不足与局限
*   **硬件依赖性**：该方法高度依赖于具有多引擎、片上互联的 TSS 架构加速器，对于传统的单核或简单脉动阵列加速器适配性较差。
*   **模拟环境局限**：所有结论均基于硬件描述语言模拟和功耗建模工具，缺乏在真实硅片（Silicon）上的实测数据。
*   **量化损失风险**：虽然 8-bit 量化能加速调度，但在极大规模 DAG 图下，量化精度是否会影响子图匹配的成功率（Feasibility）尚需更深入的消融实验支撑。

（完）
