---
title: Characterizing CPU-Induced Slowdowns in Multi-GPU LLM Inference
title_zh: 表征多 GPU 大语言模型推理中由 CPU 引起的性能下降
authors: "Euijun Chung, Yuxiao Jia, Aaron Jezghani, Hyesoon Kim"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22774v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 多GPU LLM推理基础设施的性能分析
tldr: 本研究系统分析了多GPU大语言模型推理中被忽视的CPU瓶颈问题。研究发现，受限的CPU分配会导致内核启动延迟和通信停滞，使GPU无法充分发挥性能。通过增加CPU核心数，可以在不增加GPU成本的前提下，将首字延迟（TTFT）降低1.36至5.40倍，显著提升推理服务的稳定性和响应速度。
selection_source: fresh_fetch
motivation: 在多GPU推理场景中，CPU往往无法及时调度任务，导致GPU资源因等待指令而严重闲置。
method: 通过对现代LLM推理负载进行系统化实验，量化分析了CPU资源对内核启动、通信及分词等关键环节的影响。
result: 充足的CPU配置能有效消除控制端瓶颈，使TTFT延迟降低1.36-5.40倍，并显著减少高负载下的超时现象。
conclusion: CPU配置是多GPU推理性能的关键决定因素，增加CPU核心数是提升LLM服务效率的高性价比手段。
---

## 摘要
大规模机器学习工作负载日益依赖多 GPU 系统，但其性能往往受限于一个被忽视的组件：CPU。通过对现代大语言模型（LLM）推理和推理服务工作负载的详细研究，我们发现多 GPU 性能下降通常并非因为 GPU 饱和，而是因为 CPU 无法使 GPU 保持繁忙状态。在 CPU 分配受限的情况下，系统表现出诸如内核启动延迟、通信停滞以及分词延迟增加等症状，即使在 GPU 资源充足时也会导致严重的 GPU 利用率不足。本研究对多 GPU LLM 推理中 CPU 引起的性能下降进行了系统分析。我们表明，即使在采用进程级隔离和 CUDA Graphs 等现代 GPU 端优化的推理服务栈中，这些瓶颈依然存在。由于额外 CPU 核心的边际成本相对于 GPU 实例定价而言较小，我们的评估表明，增加 CPU 核心数量可以以极小的额外成本显著提高性能和稳定性。在中等服务负载下，我们观察到 CPU 资源匮乏的配置经常出现超时，而提供充足的 CPU 资源可恢复响应能力，并在各种配置下将首字延迟（TTFT）降低 1.36 至 5.40 倍，且无需额外的 GPU。本研究表明，CPU 资源配置是多 GPU LLM 推理配置中的关键因素，有助于防止控制侧瓶颈。

## Abstract
Large-scale machine learning workloads increasingly rely on multi-GPU systems, yet their performance is often limited by an overlooked component: the CPU. Through a detailed study of modern large language model (LLM) inference and serving workloads, we find that multi-GPU performance frequently degrades not because GPUs are saturated, but because CPUs fail to keep the GPUs busy. Under limited CPU allocations, systems exhibit symptoms such as delayed kernel launch, stalled communication, and increased tokenization latency, leading to severe GPU underutilization even when ample GPU resources are available. This work presents a systematic analysis of CPU-induced slowdowns in multi-GPU LLM inference. We show that these bottlenecks persist even in serving stacks that employ process-level separation and modern GPU-side optimizations such as CUDA Graphs. Since the marginal cost of additional CPU cores is small relative to GPU instance pricing, our evaluation indicates that increasing the number of CPU cores can substantially improve performance and stability at minimal additional cost. Under moderate serving load, we observe that CPU-starved configurations frequently time out, while providing adequate CPU resources restores responsiveness and reduces time-to-first-token (TTFT) latency by 1.36-5.40x across configurations, all without requiring additional GPUs. This work shows that CPU provisioning is a crucial factor in multi-GPU LLM inference configuration, helping prevent control-side bottlenecks.

---

## 论文详细总结（自动生成）

### 论文总结：多 GPU LLM 推理中由 CPU 引起的性能下降表征分析

#### 1. 核心问题与整体含义（研究动机和背景）
在现代大规模机器学习（ML）工作负载中，业界普遍认为 GPU 是唯一的性能瓶颈。然而，本论文指出，在多 GPU 大语言模型（LLM）推理场景下，**CPU 往往成为被忽视的性能杀手**。
*   **研究动机**：在共享集群或云环境中，用户通常根据 GPU 数量静态分配 CPU 资源（例如 4 个 GPU 配 8 个 CPU 核心）。这种不平衡的配置会导致 CPU 无法及时处理分词（Tokenization）、内核启动（Kernel Launch）和进程间同步，从而使昂贵的 GPU 资源处于闲置状态。
*   **核心问题**：论文旨在系统性地表征 CPU 资源受限如何导致多 GPU 推理性能下降，并揭示其背后的底层机制。

#### 2. 论文提出的方法论
论文通过系统性的实验分析和底层机制剖析，提出了以下分析框架：
*   **CPU 任务分类**：将 CPU 在 LLM 推理中的职责分为三类：
    1.  **输入处理（分词）**：多线程执行的计算密集型任务。
    2.  **请求调度与管理**：处理 HTTP 请求和批处理逻辑。
    3.  **内核启动与同步**：负责向 GPU 发送指令并协调多卡间的通信（如 NCCL）。
*   **“攻击者-受害者”实验模型**：为了模拟真实推理服务环境，设计了实验模型。其中“受害者”是目标推理请求，“攻击者”是并发产生的背景请求，通过改变攻击者的请求频率（RPS）和序列长度，观察 CPU 争用对受害者请求延迟的影响。
*   **底层瓶颈识别**：识别出两个关键瓶颈：
    *   **同步停滞**：CPU 调度延迟导致多卡通信内核无法同时启动，引发 GPU 忙等。
    *   **共享内存广播竞争**：在多进程架构中，1-写-N-读的共享内存队列在 CPU 受限时会产生严重的轮询延迟。

#### 3. 实验设计
*   **模型与框架**：使用 vLLM (v0.11.1) 推理引擎，测试了 Llama 3.1 8B 和 Qwen 2.5 14B 模型。
*   **场景模拟**：
    *   **离线分析**：分析不同序列长度下分词延迟占首字延迟（TTFT）的比例。
    *   **在线服务模拟**：在不同 CPU 核心分配（从“GPU数+1”到“GPU数×8”）下，测量不同 RPS 负载对 TTFT 的影响。
*   **Benchmark 与对比**：
    *   对比基准是不同 CPU 核心数配置下的性能表现。
    *   分析了 4.65 百万条来自真实研究和教学集群的作业调度日志，以验证 CPU 分配不足的普遍性。

#### 4. 资源与算力
论文在三种高性能异构系统上进行了评估：
*   **H100 系统**：8×H100 GPU (Hopper)，Intel Xeon Platinum 8480CL (64核)，NVLink 4.0。
*   **H200 系统**：8×H200 GPU (Hopper)，Intel Xeon Platinum 8480CL (64核)，NVLink 4.0。
*   **Blackwell 系统**：8×RTX Pro 6000 (Blackwell)，双 Intel Xeon 6737P (64核)，PCIe 5.0。
*   **注**：实验中禁用了超线程（SMT），以确保 CPU 核心性能表征的准确性。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从单卡到 8 卡的多种配置，测试了不同模型、不同请求速率（8 RPS, 16 RPS）以及极长序列（最高 114k tokens）。
*   **充分性**：实验设计非常充分。不仅有宏观的端到端延迟测量，还有微观的 CPU/GPU 利用率追踪、内核启动时序分析以及大规模集群日志的统计分析。
*   **客观性**：通过在不同架构（Hopper, Blackwell）和不同互连技术（NVLink, PCIe）上重复实验，证明了 CPU 瓶颈的普遍存在，而非特定硬件的偶发问题。

#### 6. 主要结论与发现
*   **显著的性能提升**：在不增加 GPU 的情况下，仅通过增加 CPU 核心数，可将 TTFT 延迟降低 **1.36–5.40 倍**。
*   **分词开销巨大**：在长序列推理中，CPU 端的分词延迟可占到 TTFT 的 50% 以上。
*   **CPU 饥饿导致 GPU 闲置**：当 CPU 核心不足时，内核启动延迟会从微秒级飙升至毫秒级，导致 GPU 在等待指令时严重欠载。
*   **配置建议**：为了避免控制侧瓶颈，多 GPU 系统中每个 GPU 至少应配备 4-8 个物理 CPU 核心。
*   **成本效益**：由于 CPU 核心的成本远低于 GPU 实例，增加 CPU 分配是提升 LLM 推理性价比的最有效手段。

#### 7. 优点
*   **视角独特**：挑战了“GPU 是唯一瓶颈”的固有认知，填补了多 GPU 推理中 CPU 资源配置研究的空白。
*   **实战导向**：结合了真实云服务定价和集群日志分析，具有极强的工程指导意义。
*   **深度剖析**：不仅展示了性能下降，还深入到 Linux 共享内存机制和 NCCL 同步机制层面解释了原因。

#### 8. 不足与局限
*   **框架局限性**：主要基于 vLLM 框架进行评估，虽然提及了 SGLang 等类似架构，但未对 TensorRT-LLM 等其他闭源或不同架构的引擎进行深度对比。
*   **硬件覆盖**：实验主要集中在 NVIDIA GPU 和 Intel CPU 上，未探讨 AMD GPU (ROCm) 或 ARM 架构 CPU（如 Grace Hopper）在相同场景下的表现。
*   **训练负载缺失**：研究重点完全在于推理，虽然文中提到训练负载对 CPU 敏感度较低，但未给出详细的对比数据。

（完）
