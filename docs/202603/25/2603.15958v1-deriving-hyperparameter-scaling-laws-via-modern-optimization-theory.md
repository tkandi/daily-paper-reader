---
title: Deriving Hyperparameter Scaling Laws via Modern Optimization Theory
title_zh: 通过现代优化理论推导超参数缩放法则
authors: "Egor Shulgin, Dimitri von Rütte, Tianyue H. Zhang, Niccolò Ajroldi, Bernhard Schölkopf, Antonio Orvieto"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15958v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 大规模训练优化的超参数缩放法则
tldr: 本研究利用现代优化理论中的线性最小化算子（LMO）收敛界，推导了超参数随训练时长和批大小变化的缩放法则。不同于侧重模型规模的现有方法，本文为normalized SGD、signSGD和Muon等优化器提供了学习率、动量和批大小的闭式幂律调度方案，从理论上统一了多项经验观察，并揭示了动量与批大小缩放之间的关键相互作用。
selection_source: fresh_fetch
motivation: 现有超参数迁移研究多侧重于模型规模，而缺乏针对训练时长和批大小变化的统一理论框架和缩放准则。
method: 通过最小化基于LMO框架的现代一阶优化器收敛界，推导出超参数作为迭代次数或Token预算函数的闭式幂律公式。
result: 研究成功复现了文献中关于学习率和批大小缩放的多数经验观察，并推导出适用于不同优化器的统一超参数调度方案。
conclusion: 该理论框架为理解现代优化器的超参数缩放提供了原则性视角，并指出了动量与批大小缩放策略在实现最优性能中的协同作用。
---

## 摘要
超参数迁移已成为现代大规模训练方案的重要组成部分。现有方法（如 muP）主要关注模型规模之间的迁移，而跨批次大小和训练周期的迁移通常依赖于经验缩放规则，这些规则受时间尺度保持、二次代理和连续时间近似等见解的启发。我们通过基于线性最小化算子（LMO）方法的最新收敛界限，研究了现代一阶优化器的超参数缩放法则；该框架涵盖了归一化 SGD、signSGD（Adam 的近似）以及 Muon。通过将近期文献中的界限作为代理并在不同调优方案下对其进行最小化，我们得出了学习率、动量和批次大小作为迭代次数或 token 预算函数的闭式幂律调度方案。在保持模型规模不变的情况下，我们的分析在一个统一且有原则的视角下复现了文献中的大部分见解和观察结果，并为未来的研究指明了清晰的方向。我们的结果特别关注了动量与批次大小缩放之间的相互作用，表明可以通过多种缩放策略实现最佳性能。

## Abstract
Hyperparameter transfer has become an important component of modern large-scale training recipes. Existing methods, such as muP, primarily focus on transfer between model sizes, with transfer across batch sizes and training horizons often relying on empirical scaling rules informed by insights from timescale preservation, quadratic proxies, and continuous-time approximations. We study hyperparameter scaling laws for modern first-order optimizers through the lens of recent convergence bounds for methods based on the Linear Minimization Oracle (LMO), a framework that includes normalized SGD, signSGD (approximating Adam), and Muon. Treating bounds in recent literature as a proxy and minimizing them across different tuning regimes yields closed-form power-law schedules for learning rate, momentum, and batch size as functions of the iteration or token budget. Our analysis, holding model size fixed, recovers most insights and observations from the literature under a unified and principled perspective, with clear directions open for future research. Our results draw particular attention to the interaction between momentum and batch-size scaling, suggesting that optimal performance may be achieved with several scaling strategies.

---

## 论文详细总结（自动生成）

这篇论文通过现代优化理论，为大规模深度学习训练中的超参数（HP）选择提供了理论支撑。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在大规模模型训练中，如何随计算预算（Token 数量）和批大小（Batch Size）的变化，科学地调整学习率、动量和批大小。
*   **研究动机**：目前的超参数迁移方法（如 $\mu$P）主要解决模型宽度/深度的迁移，而跨训练时长和批大小的迁移仍依赖经验法则或简单的二次近似。随着训练成本激增，亟需一个统一的理论框架来预测不同优化器（如 Adam、Muon）的最优超参数缩放路径。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将现代非凸优化理论中的**收敛上界（Convergence Bounds）**作为性能代理（Proxy），通过对该上界关于超参数求导并取极小值，推导出闭式的幂律缩放公式。
*   **关键技术框架**：
    *   **LMO 框架**：研究基于线性最小化算子（Linear Minimization Oracle）的优化器，涵盖了归一化 SGD、signSGD（Adam 的近似）和 Muon。
    *   **代理目标函数**：利用 Kovalev (2025) 提出的非凸收敛界，该界包含了确定性优化项、动量“预热”项、噪声地板项和光滑性误差项。
    *   **优化变量**：在固定 Token 预算 $T = bK$（批大小 $\times$ 迭代次数）的情况下，对学习率 $\eta$、动量参数 $\alpha$（即 $1-\beta$）和批大小 $b$ 进行联合或受限优化。
*   **推导出的缩放法则（部分）**：
    *   **固定动量时**：最优学习率 $\eta^* \propto T^{-1/4}$，最优批大小 $b^* \propto T^{1/2}$。
    *   **联合调优时**：$b^* \propto T^{1/6}$，$\eta^* \propto T^{-7/12}$，$\alpha^* \propto T^{-1/3}$。

### 3. 实验设计
*   **数值验证**：在极广的参数范围（Token 预算跨越 20 个数量级，$10^2$ 到 $10^{22}$）内，通过数值计算验证理论推导的幂律指数是否与代理目标的最小值一致。
*   **实证支持**：
    *   **场景**：语言模型预训练。
    *   **模型**：160M 参数的 Transformer (PlainLM)。
    *   **数据集**：SlimPajama（训练量达 5B Tokens）。
    *   **对比方案**：在不同的批大小下进行网格搜索寻找最优学习率，观察其随训练时长变化的趋势。
*   **文献对比**：将推导出的指数与 DeepSeek、StepLaw、Muon 等已有经验研究中的观测值进行对比。

### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的 GPU 型号和总算力消耗（如“使用了 128 张 A100”）。
*   **实验规模**：实证部分使用了 160M 的中小型模型，这表明该研究侧重于理论验证而非刷新 SOTA 记录。数值验证部分主要涉及公式计算，算力需求极低。

### 5. 实验数量与充分性
*   **实验数量**：数值验证覆盖了极其广泛的超参数空间，具有很强的统计说服力。实证部分虽然模型较小，但通过 5B Tokens 的训练展示了清晰的学习率偏移趋势。
*   **充分性与公平性**：实验设计较为客观。作者承认了理论假设（如独立同分布噪声）与实际训练的偏差，并进行了敏感性分析（附录 C），讨论了不同噪声模型对指数的影响，体现了学术严谨性。

### 6 主要结论与发现
*   **统一视角**：成功复现了文献中“学习率随批大小平方根缩放”的观察，并解释了为何在固定批大小时学习率应随时间衰减。
*   **Token 最优批大小**：发现对于 LMO 类优化器（如 Adam/Muon），存在一个随 Token 预算增长的非平凡最优批大小（$b^* \propto T^{1/2}$ 或 $T^{1/6}$），而普通 SGD 在该理论下不具备此特性。
*   **动量的关键作用**：当硬件限制了最大批大小时，通过调整动量（随预算增加而增加）可以补偿批大小的不足，维持最优收敛率。
*   **解决矛盾**：解释了为何不同文献中关于“学习率随预算增加是上升还是下降”存在矛盾——这取决于批大小是否同步增长以及调优协议。

### 7. 优点
*   **理论深度**：跳出了简单的二次模型，利用现代非凸优化界限，使理论更贴近 Adam 和 Muon 等现代优化器的实际表现。
*   **实用性强**：提供了清晰的“预算迁移规则表”（Table 2），指导开发者如何将小规模短时间的调优结果外推到大规模长时间训练。
*   **解释力**：能够从数学上解释多种经验缩放法则的来源。

### 8. 不足与局限
*   **泛化性假设**：分析假设优化效率与下游泛化性能强相关，未考虑统计泛化界限（Generalization Gap）。
*   **模型规模耦合**：主要分析固定模型规模下的超参数缩放，未深入探讨超参数与模型参数量 $N$ 的复杂交互。
*   **简化假设**：理论推导基于常数学习率，未完全建模 Warmup 和复杂的 LR Schedule（如余弦退火）的动态过程。
*   **实证规模**：缺乏在百亿或千亿级参数模型上的直接大规模验证。

（完）
