---
title: Accelerating Byzantine-Robust Distributed Learning with Compressed Communication via Double Momentum and Variance Reduction
title_zh: 通过双重动量和方差缩减加速具有压缩通信的拜占庭鲁棒分布式学习
authors: "Yanghao Li, Changxin Liu, Yuhao Yi"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15144v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 通信高效的随机分布式学习算法
tldr: 本研究针对分布式学习中的拜占庭容错与通信开销问题，提出了Byz-DM21算法。该算法通过创新的双动量梯度估计器结合误差反馈技术，在无需大批量数据的情况下实现了高效的通信压缩与鲁棒性。此外，通过引入局部方差削减技术，进一步提出了Byz-VR-DM21算法，显著提升了收敛速度。理论证明与实验结果均验证了该方法在处理拜占庭攻击时的有效性与高效性。
selection_source: fresh_fetch
motivation: 旨在解决分布式学习中拜占庭鲁棒性算法面临的高通信开销及对大批量数据的依赖问题。
method: 提出一种基于双动量机制和误差反馈的梯度估计器，并结合局部方差削减技术以加速收敛。
result: 理论证明Byz-DM21和Byz-VR-DM21分别达到O(ε⁻⁴)和O(ε⁻³)的收敛率，且在数值实验中表现优异。
conclusion: 该研究为构建兼具通信效率、鲁棒性及快速收敛性能的分布式学习系统提供了有效的算法框架。
---

## 摘要
在协作式和分布式学习中，拜占庭鲁棒性是优化算法的一个重要方面。此类分布式算法通常伴随着大量参数的传输，因此通信压缩对于有效的解决方案至关重要。在本文中，我们提出了 Byz-DM21，这是一种新型的拜占庭鲁棒且通信高效的随机分布式学习算法。我们的核心创新是一种基于双重动量机制的新型梯度估计器，它集成了误差反馈技术的最新进展。利用该估计器，我们设计了标准算法和加速算法，在保持对拜占庭工作节点鲁棒性的同时，消除了对大批量（batch size）的需求。我们证明了 Byz-DM21 算法具有更小的邻域范围，并在 $\mathcal{O}(\varepsilon^{-4})$ 次迭代中收敛至 $\varepsilon$-驻点。为了进一步提高效率，我们引入了一个名为 Byz-VR-DM21 的分布式变体，它在每个节点中加入了局部方差缩减，以逐步消除随机近似带来的方差。我们证明了 Byz-VR-DM21 能够在 $\mathcal{O}(\varepsilon^{-3})$ 次迭代中收敛至 $\varepsilon$-驻点。此外，我们将结果扩展到了函数满足 Polyak-Łojasiewicz 条件的情况。最后，数值实验证明了所提方法的有效性。

## Abstract
In collaborative and distributed learning, Byzantine robustness reflects a major facet of optimization algorithms. Such distributed algorithms are often accompanied by transmitting a large number of parameters, so communication compression is essential for an effective solution. In this paper, we propose Byz-DM21, a novel Byzantine-robust and communication-efficient stochastic distributed learning algorithm. Our key innovation is a novel gradient estimator based on a double-momentum mechanism, integrating recent advancements in error feedback techniques. Using this estimator, we design both standard and accelerated algorithms that eliminate the need for large batch sizes while maintaining robustness against Byzantine workers. We prove that the Byz-DM21 algorithm has a smaller neighborhood size and converges to $\varepsilon$-stationary points in $\mathcal{O}(\varepsilon^{-4})$ iterations. To further enhance efficiency, we introduce a distributed variant called Byz-VR-DM21, which incorporates local variance reduction at each node to progressively eliminate variance from random approximations. We show that Byz-VR-DM21 provably converges to $\varepsilon$-stationary points in $\mathcal{O}(\varepsilon^{-3 })$ iterations. Additionally, we extend our results to the case where the functions satisfy the Polyak-Łojasiewicz condition. Finally, numerical experiments demonstrate the effectiveness of the proposed method.

---

## 论文详细总结（自动生成）

以下是对论文《Accelerating Byzantine-Robust Distributed Learning with Compressed Communication via Double Momentum and Variance Reduction》的结构化深入总结：

### 1. 论文的核心问题与整体含义
在分布式学习（Distributed Learning）中，系统面临两大核心挑战：
*   **拜占庭鲁棒性（Byzantine Robustness）：** 部分工作节点可能因硬件故障或恶意攻击（拜占庭节点）向服务器发送错误信息，导致模型不收敛或被操纵。
*   **通信开销（Communication Overhead）：** 高维模型参数的频繁传输成为性能瓶颈，需要通过通信压缩（如量化、稀疏化）来降低带宽需求。

**研究动机：** 现有的鲁棒算法往往依赖大批量（Large Batch）训练或无法有效结合有偏压缩技术（Biased Compression）。本文旨在提出一种既能抵御拜占庭攻击、又能实现高效通信压缩，且无需大批量数据的快速收敛算法。

### 2. 论文提出的方法论
论文提出了两种核心算法：**Byz-DM21** 及其加速版本 **Byz-VR-DM21**。

*   **核心思想：**
    *   **双重动量机制（Double Momentum）：** 引入两个动量估计器（$v$ 和 $u$）。第一个动量平滑梯度噪声，第二个动量进一步优化更新方向。理论证明，双动量估计器的方差在渐进情况下仅为单动量的一半。
    *   **误差反馈（Error Feedback, EF21）：** 结合 EF21 技术处理有偏压缩（如 Top-k 稀疏化）带来的压缩误差，确保压缩后的更新仍能引导模型收敛。
    *   **鲁棒聚合（Robust Aggregation）：** 在服务器端使用 $(B, \kappa)$-鲁棒聚合器（如 RFA、坐标中位数 CM、修剪平均值 CWTM），并配合“最近邻混合”（NNM）预处理步骤来过滤异常值。
*   **Byz-VR-DM21 的改进：** 在双重动量的基础上引入了类似 STORM 的**局部方差缩减（Variance Reduction）**技术。通过利用连续两次迭代间的梯度差值来修正当前梯度估计，从而将收敛率从 $\mathcal{O}(\varepsilon^{-4})$ 提升至 $\mathcal{O}(\varepsilon^{-3})$。

### 3. 实验设计
*   **数据集与场景：**
    *   **二分类任务：** 使用 LibSVM 中的 `a9a` 和 `w8a` 数据集进行逻辑回归。
    *   **图像分类任务：** 使用 `CIFAR-10`（ResNet-20 模型）和 `FEMNIST`（CNN 模型）。
*   **攻击模型（Adversarial Attacks）：** 测试了四种主流攻击：符号翻转（SF）、标签翻转（LF）、内积操纵（IPM）以及“一点点就够”（ALIE）攻击。
*   **对比方法（Baselines）：** 包括 BR-DIANA、Byz-VR-MARINA、Byz-EF21-SGDM 以及 Byz-DASHA-PAGE。
*   **压缩算子：** 针对所提方法使用 Top-k 压缩，针对部分基准方法使用 Rand-k 压缩，以符合各自的理论框架。

### 4. 资源与算力
*   **硬件环境：** 论文明确指出了实验设备。
    *   **CPU：** AMD Ryzen Threadripper PRO 5975WX (32核)。
    *   **GPU：** NVIDIA GeForce RTX 4090。
*   **软件环境：** PyTorch 2.0.1, CUDA 11.7。
*   **训练细节：** 实验运行了 40 到 100 个 Epoch，或固定为 5000 次迭代。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 4 个不同类型的数据集上进行了测试，涵盖了凸优化（逻辑回归）和非凸优化（深度神经网络）。
*   **充分性：** 实验覆盖了多种聚合规则（RFA, CM, CWTM）和多种攻击场景，并进行了不同拜占庭节点比例（1/20 到 8/20）的灵敏度分析。
*   **客观性：** 所有实验均使用 3 个随机种子运行并报告了均值和标准差，确保了结果的可重复性和公平性。

### 6. 论文的主要结论与发现
*   **收敛速度提升：** Byz-DM21 在非凸设置下达到 $\mathcal{O}(\varepsilon^{-4})$ 收敛率，而 Byz-VR-DM21 达到 $\mathcal{O}(\varepsilon^{-3})$，在理论上优于现有的同类鲁棒算法。
*   **方差削减效果：** 实验观察到 Byz-VR-DM21 在收敛后能保持极低的训练方差，证明了双动量与方差缩减结合的有效性。
*   **精度优势：** 在异构数据（Heterogeneous Setting）下，所提算法收敛到的“邻域范围”（Neighborhood size）更小，即最终模型的误差更低。
*   **PŁ 条件下的表现：** 在满足 Polyak-Łojasiewicz 条件的函数上，算法展现出了更快的线性收敛特性。

### 7. 优点（亮点）
*   **Batch-free 特性：** 与 Byz-VR-MARINA 等需要定期计算全梯度或依赖大批量的算法不同，Byz-DM21 仅依赖随机梯度，更适合大规模在线分布式场景。
*   **理论与实践结合：** 提供了严谨的收敛性证明，且在实际深度学习任务（CIFAR-10）中表现出比基准方法更强的抗干扰能力。
*   **通信效率：** 成功将有偏压缩与拜占庭鲁棒性结合，显著降低了每轮通信的数据量。

### 8. 不足与局限
*   **异构性瓶颈：** 尽管邻域更小，但在数据高度异构时，算法仍无法完全消除由于拜占庭攻击和数据差异导致的固定误差（Error Floor），这是 BRDL 领域的普遍难题。
*   **超参数敏感性：** 引入了双重动量和方差缩减，增加了动量系数 $\eta$ 等超参数的调优成本。
*   **计算开销：** 虽然通信减少了，但每个工作节点需要维护额外的动量状态和执行 NNM 预处理，增加了本地计算负担。

（完）
