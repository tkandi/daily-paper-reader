---
title: "Decentralized Orchestration Architecture for Fluid Computing: A Secure Distributed AI Use Case"
title_zh: 流体计算的去中心化编排架构：一个安全分布式人工智能用例
authors: "Diego Cajaraville-Aboy, Ana Fernández-Vilas, Rebeca P. Díaz-Redondo, Manuel Fernández-Veiga, Pablo Picallo-López"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12001v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 分布式AI的编排架构
tldr: 针对跨边缘、雾和云的多域异构资源管理挑战，本文提出了一种流式计算的去中心化编排架构。该架构支持多域间的去中心化协调与租户意图驱动的部署，同时保持各域自治。通过在分布式联邦学习（DFL）场景中引入基于SDN的异常检测机制FU-HST，有效提升了系统抵御拜占庭攻击的安全性，并通过仿真验证了其在性能和开销方面的优势。
selection_source: fresh_fetch
motivation: 现有的流式计算方案多为中心化，难以应对跨多个管理域的异构资源管理及安全挑战。
method: 提出一种去中心化的多域编排架构，并结合SDN技术开发了FU-HST异常检测机制以增强分布式联邦学习的安全性。
result: 仿真实验表明，该架构能有效检测多域环境下的异常行为，并在保证联邦学习性能的同时控制计算与通信开销。
conclusion: 该去中心化编排架构为跨域流式计算提供了灵活且安全的部署方案，特别适用于复杂的分布式AI应用场景。
---

## 摘要
分布式人工智能（AI）和物联网（IoT）应用越来越多地在跨越终端设备、边缘/雾基础设施和云平台的异构资源上执行，且这些资源通常属于不同的管理域。流体计算（Fluid Computing）已成为一种极具前景的范式，通过将计算连续体中的海量资源视为统一的织物（fabric），从而增强资源管理，并实现由应用需求驱动的最优服务无关部署。然而，现有解决方案在很大程度上仍是中心化的，且通常没有明确解决多域考量。本文提出了一种适用于流体计算环境的通用多域编排架构。编排平面实现了各域之间的去中心化协调，这些域在保持本地自治的同时，共同实现租户基于意图的部署请求，确保端到端的放置与执行。为此，该架构将域侧控制服务提升为一等能力，以支持运行时的应用级增强。作为一个代表性用例，我们考虑了拜占庭威胁下的多域去中心化联邦学习（DFL）部署。我们利用域侧能力，通过引入 FU-HST（一种支持 SDN 的多域异常检测机制）来增强拜占庭安全性，该机制与拜占庭鲁棒聚合相辅相成。我们通过在单域和多域设置下的仿真验证了该方法，评估了异常检测、DFL 性能以及计算/通信开销。

## Abstract
Distributed AI and IoT applications increasingly execute across heterogeneous resources spanning end devices, edge/fog infrastructure, and cloud platforms, often under different administrative domains. Fluid Computing has emerged as a promising paradigm for enhancing massive resource management across the computing continuum by treating such resources as a unified fabric, enabling optimal service-agnostic deployments driven by application requirements. However, existing solutions remain largely centralized and often do not explicitly address multi-domain considerations. This paper proposes an agnostic multi-domain orchestration architecture for fluid computing environments. The orchestration plane enables decentralized coordination among domains that maintain local autonomy while jointly realizing intent-based deployment requests from tenants, ensuring end-to-end placement and execution. To this end, the architecture elevates domain-side control services as first-class capabilities to support application-level enhancement at runtime. As a representative use case, we consider a multi-domain Decentralized Federated Learning (DFL) deployment under Byzantine threats. We leverage domain-side capabilities to enhance Byzantine security by introducing FU-HST, an SDN-enabled multi-domain anomaly detection mechanism that complements Byzantine-robust aggregation. We validate the approach via simulation in single- and multi-domain settings, evaluating anomaly detection, DFL performance, and computation/communication overhead.

---

## 论文详细总结（自动生成）

### 论文总结：流体计算的去中心化编排架构与安全分布式 AI 用例

#### 1. 论文的核心问题与整体含义
*   **研究背景**：随着物联网（IoT）和分布式 AI 的发展，计算任务需要在终端、边缘、雾和云等异构资源（即“计算连续体”）上灵活执行。
*   **核心问题**：现有的“流体计算”（Fluid Computing）方案大多依赖中心化编排，难以应对跨多个行政管理域（Multi-domain）的资源调度。此外，在分布式 AI（如去中心化联邦学习 DFL）中，如何利用网络基础设施（如 SDN）在多域环境下主动识别并拦截恶意攻击（拜占庭威胁）仍缺乏有效架构支持。
*   **整体含义**：本文旨在构建一个去中心化的编排框架，使不同管理域在保持自治的同时能够协同工作，并证明了通过域侧 SDN 服务可以显著增强分布式应用的安全性和运行性能。

#### 2. 论文提出的方法论
*   **去中心化编排架构**：
    *   **租户层（Tenant Layer）**：租户通过意图（Intent）定义应用需求，而不必关心底层硬件细节。
    *   **编排平面（Orchestration Plane）**：每个域拥有独立的**域服务编排器（DSO）**和**多域协调代理（MDCA）**。DSO 负责域内资源调度，MDCA 负责跨域的意图分解与状态同步。
    *   **控制服务**：将执行控制、SDN 控制和 QoS 控制提升为“一等能力”，允许基础设施主动参与应用优化。
*   **核心算法：FU-HST（反馈更新半空间树）**：
    *   **核心思想**：在 SDN 控制器中运行一种在线流式异常检测算法，通过收集节点间的互评“告警信号”来识别恶意参与者。
    *   **关键技术细节**：
        1.  **特征合成**：提取邻居评价的均值、标准差及历史反馈。
        2.  **分数稳定（滞后阈值）**：引入类似电子学中施密特触发器的双阈值机制（$\tau^+$ 和 $\tau^-$），防止因噪声导致的频繁误判。
        3.  **双重决策规则**：结合当前异常分数和历史反馈状态决定是否封禁节点。
        4.  **安全更新**：仅利用被判定为“安全”的样本更新检测模型，防止模型被攻击者“带偏”。

#### 3. 实验设计
*   **应用场景**：多域环境下的去中心化联邦学习（DFL），面临模型毒化等拜占庭攻击。
*   **数据集**：经典的 **MNIST** 手写数字识别数据集（假设为 IID 分布）。
*   **Benchmark 与对比方法**：
    *   **对比算法**：原始 HST（半空间树）、SAD（标准绝对偏差）、iLOF（增量局部离群因子）。
    *   **攻击模型**：噪声攻击（Noise）、符号翻转（Sign-Flipping）、内积操纵（IPM-100）。
    *   **评估指标**：异常检测的 F1 分数、准确率、误封率（FBR），以及 DFL 模型的分类准确率和系统开销。

#### 4. 资源与算力
*   **软件环境**：使用 Python 编写的 `DecentralizedFedSim` 模拟器，基于 PyTorch 库。
*   **硬件资源**：论文提到实验得到了加利西亚超级计算中心（CESGA）的支持。
*   **明确性说明**：文中**未明确说明**具体的 GPU 型号、显存大小、节点数量或具体的训练总时长。实验主要通过仿真模拟完成。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   **扩展性实验**：测试了 20 到 100 个节点的单域场景。
    *   **威胁分析**：测试了不同数量（1-4个）恶意节点的场景。
    *   **多域场景**：设计了 8 组（S1-S8）涵盖不同域数量（1-3个）、不同连接率（$p_2$）和不同恶意节点分布的复杂实验。
*   **充分性评价**：实验设计较为**充分且客观**。作者不仅关注了检测算法本身的准确性，还深入分析了封禁动作对 DFL 最终收敛性能的影响，并进行了详细的计算与通信开销分析。通过网格搜索优化了超参数，确保了对比的公平性。

#### 6. 论文的主要结论与发现
*   **检测性能**：FU-HST 在各种攻击下均表现出比基准方法更稳定的 F1 分数，尤其在节点规模扩大时，其误封率（FBR）保持在极低水平。
*   **应用效果**：在强力攻击（如 IPM-100）导致 DFL 性能崩溃的场景下，FU-HST 能够成功识别恶意节点并恢复模型准确率（例如从 0.101 恢复至 0.677）。
*   **系统开销**：SDN 异常检测带来的计算开销仅占 DFL 进程的 **<0.05%**，通信开销仅占 **~0.01%**，证明了该方案在实际部署中的可行性。

#### 7. 优点
*   **架构前瞻性**：提出了一种符合 6G 和边缘计算趋势的去中心化、意图驱动编排架构。
*   **跨层协同**：打破了“网络仅作为透明管道”的传统观念，利用 SDN 编程能力为上层 AI 应用提供运行时安全增强。
*   **算法鲁棒性**：FU-HST 针对流数据的非平稳特性进行了优化，引入反馈和滞后机制，有效解决了在线检测中的“震荡”问题。

#### 8. 不足与局限
*   **数据集单一**：仅使用了 MNIST 数据集，该数据集特征较为简单，未能验证在更复杂任务（如 ImageNet 或 NLP 任务）上的表现。
*   **数据分布假设**：实验假设数据为 IID（独立同分布），但在实际联邦学习中，Non-IID（非独立同分布）是常态，这会极大增加区分“数据差异”与“恶意攻击”的难度。
*   **多域协调细节**：虽然提出了 MDCA 代理，但对于跨域协作中的利益博弈、隐私保护下的信息披露程度等深层治理问题讨论较少。

（完）
