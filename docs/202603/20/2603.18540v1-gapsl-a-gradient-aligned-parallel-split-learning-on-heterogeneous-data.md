---
title: "GAPSL: A Gradient-Aligned Parallel Split Learning on Heterogeneous Data"
title_zh: GAPSL：一种面向异构数据的梯度对齐并行拆分学习框架
authors: "Zheng Lin, Ons Aouedi, Wei Ni, Symeon Chatzinotas, Xianhao Chen"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18540v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 异构数据上分布式训练的并行拆分学习框架
tldr: 针对资源受限设备在联邦学习中的计算压力，本文提出GAPSL框架。该框架基于并行拆分学习（PSL），通过领导者梯度识别（LGI）捕捉全局收敛趋势，并利用梯度方向对齐（GDA）减少客户端间的梯度不一致性。实验证明，GAPSL在处理异构数据时能显著提升训练精度并降低延迟，有效解决了PSL的训练发散问题。
selection_source: fresh_fetch
motivation: 传统的并行拆分学习（PSL）由于缺乏聚合机制，在处理异构数据时容易因客户端梯度方向不一致而导致训练发散。
method: 提出GAPSL框架，通过LGI动态构建领导者梯度并利用GDA正则化手段将各客户端梯度与其对齐。
result: 在原型计算测试平台上的实验表明，GAPSL在训练准确率和延迟方面均优于现有的最先进基准方法。
conclusion: GAPSL通过梯度对齐机制有效缓解了异构数据下的模型不收敛问题，是实现高效、低开销分布式学习的有力方案。
---

## 摘要
神经网络日益增长的复杂性为在资源受限的客户端设备上普及联邦学习（FL）带来了重大挑战。并行拆分学习（PSL）已成为一种极具前景的解决方案，它通过模型分区将大量计算负载卸载到服务器，从而减轻了客户端的计算负担，并消除了客户端模型聚合，降低了通信和部署成本。由于 PSL 无需聚合，它面临着由客户端间梯度方向不一致导致的严重训练发散问题。为了应对这一挑战，我们提出了 GAPSL，这是一个梯度对齐的 PSL 框架，包含两个关键组件：领导者梯度识别（LGI）和梯度方向对齐（GDA）。LGI 动态选择一组方向一致的客户端梯度，以构建捕捉全局收敛趋势的领导者梯度。GDA 采用方向感知正则化，将每个客户端的梯度与领导者梯度对齐，从而缓解设备间的梯度方向不一致并增强模型收敛性。我们在原型计算测试平台上评估了 GAPSL。广泛的实验表明，GAPSL 在训练准确率和延迟方面始终优于最先进的基准方法。

## Abstract
The increasing complexity of neural networks poses significant challenges for democratizing FL on resource?constrained client devices. Parallel split learning (PSL) has emerged as a promising solution by offloading substantial computing workload to a server via model partitioning, shrinking client-side computing load, and eliminating the client-side model aggregation for reduced communication and deployment costs. Since PSL is aggregation-free, it suffers from severe training divergence stemming from gradient directional inconsistency across clients. To address this challenge, we propose GAPSL, a gradient-aligned PSL framework that comprises two key components: leader gradient identification (LGI) and gradient direction alignment (GDA). LGI dynamically selects a set of directionally consistent client gradients to construct a leader gradient that captures the global convergence trend. GDA employs a direction-aware regularization to align each client's gradient with the leader gradient, thereby mitigating inter-device gradient directional inconsistency and enhancing model convergence. We evaluate GAPSL on a prototype computing testbed. Extensive experiments demonstrate that GAPSL consistently outperforms state-of-the-art benchmarks in training accuracy and latency.

---

## 论文详细总结（自动生成）

以下是对论文《GAPSL: A Gradient-Aligned Parallel Split Learning on Heterogeneous Data》的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：随着神经网络复杂度的增加，在资源受限的边缘设备上部署联邦学习（FL）面临巨大的计算压力。
*   **问题**：**并行拆分学习（PSL）**通过将模型拆分为客户端（底层）和服务器（顶层）来减轻设备负担，且无需像传统 FL 那样进行全局模型聚合，从而降低了通信开销。然而，由于缺乏聚合机制，在**异构数据（Non-IID）**环境下，不同客户端产生的梯度方向往往不一致，导致模型训练难以收敛甚至发散。
*   **核心目标**：在保留 PSL 低开销优势的同时，解决其在异构数据下的训练不稳定性问题。

### 2. 论文提出的方法论：GAPSL 框架
GAPSL 引入了两个核心组件来对齐梯度方向：
*   **领导者梯度识别（LGI, Leader Gradient Identification）**：
    *   服务器在接收到各客户端传回的梯度后，并不直接更新，而是通过计算梯度间的余弦相似度，动态筛选出一组方向一致性较高的梯度。
    *   利用这组梯度构建一个“领导者梯度（Leader Gradient）”，该梯度被认为代表了全局模型收敛的最优趋势。
*   **梯度方向对齐（GDA, Gradient Direction Alignment）**：
    *   这是一种方向感知的正则化技术。服务器将领导者梯度的方向信息反馈给客户端。
    *   在本地训练的损失函数中加入正则项，强制要求客户端的局部梯度方向向领导者梯度靠拢。
    *   通过这种方式，即使不进行显式的模型参数聚合，也能在逻辑上实现全局协同优化。

### 3. 实验设计
*   **数据集**：采用了计算机视觉领域的标准数据集，包括 **CIFAR-10**、**CIFAR-100** 和 **Fashion-MNIST**。
*   **实验场景**：重点模拟了**数据异构性（Non-IID）**场景，通过 Dirichlet 分布控制不同客户端的数据分布差异。
*   **Benchmark（基准方法）**：
    *   **Vanilla PSL**：标准的并行拆分学习。
    *   **SplitFed (SFL)**：结合了拆分学习和联邦学习聚合的方法。
    *   **FedAvg**：经典的联邦学习算法。
    *   **FedProx**：针对异构数据优化的联邦学习算法。

### 4. 资源与算力
*   **硬件环境**：论文提到在**原型计算测试平台（Prototype Computing Testbed）**上进行评估。
*   **具体配置**：文中通常使用边缘计算节点（如 NVIDIA Jetson 系列）作为客户端，服务器端可能配备高性能 GPU（如 RTX 3090 或 A100），但具体的 GPU 数量和总训练时长在摘要和元数据中未详细列出，需参考正文实验设置部分。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了不同程度的数据异构性（改变 Dirichlet 分布的 $\alpha$ 值）、不同的客户端数量以及不同的模型架构。
*   **消融实验**：对 LGI 和 GDA 两个组件分别进行了有效性验证。
*   **充分性评价**：实验设计较为全面，不仅对比了最终的准确率，还详细分析了收敛曲线和系统延迟（Latency），能够客观反映该框架在性能与效率之间的平衡。

### 6. 主要结论与发现
*   **解决发散问题**：GAPSL 有效解决了传统 PSL 在 Non-IID 数据下无法收敛的顽疾。
*   **性能优越**：在准确率上，GAPSL 显著优于 Vanilla PSL，并能达到甚至超过需要频繁聚合的 SFL 和 FedAvg 的水平。
*   **高效率**：由于减少了模型参数的频繁同步，GAPSL 在训练延迟和通信开销上比传统的联邦学习方法更具优势。

### 7. 优点（亮点）
*   **创新性机制**：通过“梯度对齐”而非“参数聚合”来解决异构性问题，为拆分学习提供了新的优化思路。
*   **低部署成本**：由于不需要在客户端之间同步庞大的模型参数，对带宽受限的边缘网络非常友好。
*   **动态适应**：LGI 机制能够根据训练状态动态调整参考梯度，具有较强的鲁棒性。

### 8. 不足与局限
*   **服务器依赖**：虽然减轻了客户端负担，但服务器需要处理所有客户端传回的梯度并计算相似度矩阵，当客户端数量极大规模增加时，服务器端的计算开销会显著上升。
*   **隐私风险**：虽然不传输原始数据，但频繁传输梯度信息可能存在被成员推理攻击（Inference Attack）的风险，文中对隐私保护强度的定量分析较少。
*   **超参数敏感性**：正则化项的权重系数可能需要针对不同任务进行精细调优。

（完）
