---
title: "cuGenOpt: A GPU-Accelerated General-Purpose Metaheuristic Framework for Combinatorial Optimization"
title_zh: cuGenOpt：一种用于组合优化的 GPU 加速通用元启发式框架
authors: Yuyang Liu
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19163v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 用于资源管理和调度优化的GPU加速框架
tldr: cuGenOpt是一个GPU加速的通用元启发式框架，旨在解决组合优化问题中通用性、性能和易用性之间的矛盾。它采用“单块演化单解”的CUDA架构，支持多种编码方式和自适应算子选择。通过JIT编译提供Python接口，并集成LLM建模助手。实验表明，该框架在性能上远超通用MIP求解器，且在多种复杂问题上达到了与专用求解器相当的精度。
selection_source: fresh_fetch
motivation: 现有的组合优化求解方法在通用性、计算性能和用户易用性之间难以兼得。
method: 提出一种基于CUDA的“单块演化单解”架构，结合统一编码抽象、自适应算子选择及LLM辅助的Python建模接口。
result: 在多代GPU上，cuGenOpt的性能比通用MIP求解器高出数个数量级，并在TSP等复杂问题上表现出极高的求解质量。
conclusion: cuGenOpt通过硬件加速与智能化建模的结合，为大规模组合优化问题提供了一个高效、通用且易用的求解平台。
---

## 摘要
组合优化问题广泛存在于物流、调度和资源分配中，然而现有方法在通用性、性能和易用性之间面临着根本性的权衡。我们提出了 cuGenOpt，这是一个 GPU 加速的通用元启发式框架，同时解决了这三个维度的问题。在引擎层面，cuGenOpt 采用了“一个线程块演化一个解”的 CUDA 架构，具有统一的编码抽象（排列、二进制、整数）、两级自适应算子选择机制以及硬件感知的资源管理。在可扩展性层面，用户定义的算子注册接口允许领域专家注入特定问题的 CUDA 搜索算子。在易用性层面，即时编译（JIT）流水线将框架封装为纯 Python API，且基于大语言模型（LLM）的建模助手可将自然语言问题描述转换为可执行的求解器代码。在三种 GPU 架构（T4、V100、A800）上针对五个主题套件进行的实验表明，cuGenOpt 的性能比通用混合整数规划（MIP）求解器高出数个数量级，在规模高达 n=150 的实例上达到了与专用求解器相当的质量，并在 30 秒内实现了 TSP-442 上 4.73% 的差距（gap）。涵盖五种编码变体的十二种问题类型被求解至最优。框架级优化累计将 pcb442 的差距从 36% 降低到 4.73%，并将 VRPTW 的吞吐量提升了 75-81%。代码：https://github.com/L-yang-yang/cugenopt

## Abstract
Combinatorial optimization problems arise in logistics, scheduling, and resource allocation, yet existing approaches face a fundamental trade-off among generality, performance, and usability. We present cuGenOpt, a GPU-accelerated general-purpose metaheuristic framework that addresses all three dimensions simultaneously.   At the engine level, cuGenOpt adopts a "one block evolves one solution" CUDA architecture with a unified encoding abstraction (permutation, binary, integer), a two-level adaptive operator selection mechanism, and hardware-aware resource management. At the extensibility level, a user-defined operator registration interface allows domain experts to inject problem-specific CUDA search operators. At the usability level, a JIT compilation pipeline exposes the framework as a pure-Python API, and an LLM-based modeling assistant converts natural-language problem descriptions into executable solver code.   Experiments across five thematic suites on three GPU architectures (T4, V100, A800) show that cuGenOpt outperforms general MIP solvers by orders of magnitude, achieves competitive quality against specialized solvers on instances up to n=150, and attains 4.73% gap on TSP-442 within 30s. Twelve problem types spanning five encoding variants are solved to optimality. Framework-level optimizations cumulatively reduce pcb442 gap from 36% to 4.73% and boost VRPTW throughput by 75-81%.   Code: https://github.com/L-yang-yang/cugenopt

---

## 论文详细总结（自动生成）

### cuGenOpt: GPU 加速的通用组合优化元启发式框架论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
组合优化问题（如 TSP、VRP、调度等）在物流和资源分配中至关重要，但现有求解方法存在“不可能三角”：
*   **精确算法（如 MIP）**：虽然通用且保证最优，但在处理大规模问题时扩展性极差。
*   **专用求解器（如 OR-Tools, NVIDIA cuOpt）**：性能极强但适用范围窄，难以处理自定义约束或非线性目标。
*   **元启发式算法（CPU 版）**：通用性好，但搜索吞吐量低，在大规模实例上收敛缓慢。
**cuGenOpt 的动机**是打破这一权衡，通过 GPU 并行计算提供一个**通用、高性能且易用**的元启发式框架。

#### 2. 方法论：核心思想与关键技术
*   **核心架构**：采用“**一个线程块演化一个解**”（one block evolves one solution）的 CUDA 并行模式。每个线程块独立负责一个种群个体的演化，块内线程并行采样候选移动，通过块内规约选择最优移动。
*   **统一编码抽象**：支持排列（Permutation）、二进制（Binary）和整数（Integer）三种通用编码，涵盖了绝大多数组合优化问题。
*   **两级自适应算子选择 (AOS)**：
    *   **L1 层**：基于问题规模（小/中/大）预设算子权重。
    *   **L3 层**：运行期间利用指数移动平均（EMA）根据算子的改进贡献动态调整权重。
*   **硬件感知资源管理**：
    *   **共享内存自动扩展**：根据硬件能力（如 A800 的 164KB）自动将问题数据载入片上内存。
    *   **L2 缓存感知种群缩放**：根据 L2 缓存大小自动调整并行解的数量，防止缓存抖动（Cache Thrashing）。
*   **易用性工具链**：
    *   **JIT 编译流水线**：用户只需提供 Python 接口或 CUDA 代码片段，框架自动完成编译和执行。
    *   **LLM 建模助手**：利用大模型将自然语言描述直接转化为可执行的 GPU 求解代码。

#### 3. 实验设计
*   **数据集/场景**：
    *   **TSP**：TSPLIB（eil51 到 pcb442）。
    *   **VRP/VRPTW**：Augerat 实例和 Solomon 实例。
    *   **其他**：QAPLIB (QAP)、OR-Library (JSP)、Pisinger (Knapsack)。
*   **Benchmark 与对比方法**：
    *   **通用 MIP 求解器**：SCIP、CBC。
    *   **专用求解器**：Google OR-Tools Routing（基于 GLS 算法）。
    *   **硬件对比**：在 T4、V100、A800 三代 GPU 上进行横向对比。

#### 4. 资源与算力
论文详细记录了所使用的算力资源：
*   **单卡测试**：NVIDIA Tesla T4 (40 SMs)、V100 (80 SMs)、A800 (108 SMs)。
*   **多卡测试**：使用 2 × V100S GPU 验证并行扩展性。
*   **桌面端验证**：使用 RTX 3080 Ti 测试用户自定义算子的有效性。
*   **时长**：实验通常设定 30 秒或 60 秒的时间限制，或固定的迭代次数（如 5000 代）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个主题实验套件，包括基准对比、扩展性分析、通用性验证（12 种问题类型）、消融实验和自定义算子测试。
*   **充分性**：每组实验均运行 5 个随机种子并取平均值，确保了结果的统计客观性。
*   **消融实验**：逐项验证了启发式初始化、AOS 频率、问题配置文件权重和种群自适应对最终 Gap 的贡献，实验设计非常严谨。

#### 6. 主要结论与发现
*   **性能压制**：在相同时间内，cuGenOpt 的求解质量比通用 MIP 求解器（SCIP/CBC）高出数个数量级。
*   **竞争力**：在 $n \le 150$ 的规模下，cuGenOpt 的表现优于或等同于高度优化的专用求解器 OR-Tools。
*   **硬件增益**：通过共享内存扩展，VRPTW 的吞吐量提升了 75-81%；在 A800 上，TSP-442 的 Gap 在 30 秒内降至 4.73%。
*   **通用性**：成功将 12 种不同编码的问题求解至最优，证明了框架的广泛适用性。

#### 7. 优点
*   **极低的准入门槛**：通过 JIT 和 LLM 助手，非 CUDA 专家也能利用 GPU 算力解决复杂优化问题。
*   **卓越的硬件适配**：能够根据不同型号 GPU 的 L2 缓存和共享内存特性自动优化执行参数。
*   **灵活的可扩展性**：允许用户注入特定领域的 CUDA 算子，并将其纳入自适应权重竞争系统。

#### 8. 不足与局限
*   **超大规模瓶颈**：当问题规模 $n > 1200$ 时，CUDA Graph 可能会出现兼容性问题，且由于工作集超出 L2 缓存，性能增益会受限。
*   **VRP 规模限制**：受限于 CUDA 内核参数传递机制，VRP 的解结构大小被限制在约 80KB 以内，限制了车辆数和客户数的极端组合。
*   **多 GPU 通信**：目前的多 GPU 模式较为简单（独立运行取最优），缺乏更深层的解交换机制。
*   **LLM 局限**：对于极其复杂的约束描述，LLM 生成的代码仍可能需要人工微调。

（完）
