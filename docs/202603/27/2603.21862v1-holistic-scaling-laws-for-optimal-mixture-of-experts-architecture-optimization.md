---
title: Holistic Scaling Laws for Optimal Mixture-of-Experts Architecture Optimization
title_zh: 针对最优混合专家模型架构优化的全面缩放法则
authors: "Weilin Wan, Jingtao Han, Weizhong Zhang, Cheng Jin"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21862v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 混合专家模型（MoE）的架构优化与缩放法则
tldr: 本研究针对混合专家模型（MoE）架构设计的复杂性，提出了一个全局缩放法则框架。研究指出单凭每令牌FLOPs不足以衡量MoE性能，进而建立了结合FLOPs、激活参数和总参数的三重约束。通过代数约束将16维搜索空间简化为两个低维阶段，并在广泛的计算规模下验证了该框架。该成果能为任意计算预算提供最优MoE架构配置，并揭示了随规模扩大而增加的架构设计灵活性。
selection_source: fresh_fetch
motivation: 现有的MoE缩放研究因设计空间巨大且实验预算有限，难以在全局范围内提供精确的架构优化指导。
method: 提出一种将16维搜索空间简化为两阶段低维优化的框架，并引入FLOPs、激活参数和总参数的三重约束。
result: 在跨越六个数量级计算量的数百个模型上验证了该框架，成功将计算预算映射到完整的最优MoE架构。
conclusion: 研究发现近优配置范围随规模扩大而拓宽，为在基础设施约束下平衡缩放法则建议提供了量化灵活性。
---

## 摘要
大语言模型的缩放法则主导着宏观资源分配，但由于设计空间存在巨大的组合可能性，将其转化为精确的混合专家模型（MoE）架构配置仍是一个悬而未决的问题。现有的 MoE 缩放研究受限于实验预算，要么在缩放公式中增加额外的 MoE 变量，从而面临拟合不可靠的风险；要么固定所有非 MoE 因素，从而忽略了全局交互作用。我们提出了一个可复用的全面 MoE 架构优化框架，以填补这一空白。我们首先证明，仅凭每 token 的 FLOPs 并不是衡量 MoE 模型的充分公平指标，因为不同层类型的计算密度差异可能会在不增加成比例计算成本的情况下导致参数膨胀；为此，我们建立了由每 token FLOPs、激活参数量和总参数量组成的联合约束三元组。随后，我们通过代数约束和隐藏层维度的秩保持特性，将 16 维架构搜索空间简化为两个连续的低维阶段。经过跨越六个计算量级、数百个 MoE 模型的验证，我们的框架得出了稳健的缩放法则，能够将任何计算预算映射到完整的、最优的 MoE 架构。一个关键发现是，近乎最优的配置带随规模扩大而变宽，这为从业者提供了量化的灵活性，以便在缩放法则建议与基础设施限制之间取得平衡。

## Abstract
Scaling laws for Large Language Models govern macroscopic resource allocation, yet translating them into precise Mixture-of-Experts (MoE) architectural configurations remains an open problem due to the combinatorially vast design space. Existing MoE scaling studies are constrained by experimental budgets to either augment scaling formulas with extra MoE variables, risking unreliable fits, or fix all non-MoE factors, ignoring global interactions. We propose a reusable framework for holistic MoE architectural optimization that bridges this gap. We first show that FLOPs per token alone is an inadequate fairness metric for MoE models because differing computational densities across layer types can inflate parameters without proportional compute cost, and establish a joint constraint triad of FLOPs per token, active parameters, and total parameters. We then reduce the 16-dimensional architectural search space to two sequential low-dimensional phases through algebraic constraints and a rank-preserving property of the hidden dimension. Validated across hundreds of MoE models spanning six orders of magnitude in compute, our framework yields robust scaling laws that map any compute budget to a complete, optimal MoE architecture. A key finding is that the near-optimal configuration band widens with scale, giving practitioners quantitative flexibility to balance scaling law recommendations against infrastructure constraints.

---

## 论文详细总结（自动生成）

### 针对最优混合专家模型（MoE）架构优化的全面缩放法则总结

#### 1. 核心问题与整体含义（研究动机和背景）
混合专家模型（MoE）通过稀疏激活机制，实现了在保持计算成本（FLOPs）不变的情况下大幅增加模型参数量。然而，MoE 的架构设计空间极其复杂，涉及专家数量、路由稀疏度、层数、隐藏维度等多个维度的组合。
*   **研究动机**：现有的缩放法则（Scaling Laws）在处理 MoE 时存在局限：要么直接引入过多变量导致拟合困难，要么固定非 MoE 参数（如 Attention 层）导致无法捕捉全局最优配置。
*   **核心问题**：如何在给定的计算预算下，系统性地确定 MoE 模型的最优全局架构配置（而非仅仅是局部 MoE 参数）？

#### 2. 论文提出的方法论
论文提出了一个“全面 MoE 架构优化框架”，核心思想是通过数学解耦和维度压缩来攻克“维度灾难”。
*   **三重约束准则**：研究发现仅用“每令牌 FLOPs ($M$)”衡量 MoE 是不公平的，因为可以通过调整 Attention 和 FFN 的比例在不增加计算量的情况下膨胀参数。因此，必须同时约束 **$M$（计算量）、$N_a$（激活参数量）和 $N$（总参数量）**。
*   **维度缩减技术**：将 16 维的架构搜索空间简化为 4 个核心自由度（$M, N_a, N, d$）。
*   **两阶段搜索策略**：
    1.  **利用秩保持特性（Rank-preserving property）**：发现隐藏维度 $d$ 的改变不影响不同配置间的相对性能排名。
    2.  **第一阶段**：使用 $d$ 的中值作为代理，在 $O(n^3)$ 复杂度下搜索最优的 $(M, N_a, N)$ 组合。
    3.  **第二阶段**：在确定宏观参数后，在 $O(n^2)$ 复杂度下精细化搜索最优隐藏维度 $d$。
*   **数学公式**：建立了计算预算 $C$ 与最优参数 $D$（数据量）、$M$、$N_a$、$N$ 及 $d$ 之间的幂律映射关系。

#### 3. 实验设计
*   **数据集**：使用高质量的过滤语料库，涵盖通用文本、代码、数学和多语言内容。
*   **实验场景**：在 6 个计算规模等级（从 $10^{18}$ 到 $3 \times 10^{20}$ FLOPs）下进行系统性实验。
*   **对比对象**：通过改变 $M/N_a$（激活计算密度）和 $N/N_a$（参数膨胀率）等比例，对比了数百种不同的 MoE 架构变体。
*   **Benchmark**：以预训练阶段的交叉熵损失（Cross-entropy Loss）作为核心评价指标。

#### 4. 资源与算力
*   **框架**：基于 Megatron-LM 分布式训练框架。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号（如 H100 或 A100）及具体卡数。
*   **训练规模**：实验覆盖了从 $10^{18}$ 到 $3 \times 10^{20}$ FLOPs 的总算力范围，这在学术研究中属于中大规模。

#### 5. 实验数量与充分性
*   **实验规模**：论文训练了**超过 670 个**不同的 MoE 模型。
*   **充分性**：实验设计非常详尽，不仅涵盖了多个算力数量级，还通过坐标变换（如 $M/N_a$ 比例）确保了搜索空间的均匀覆盖。
*   **客观性**：通过重新拟合 Chinchilla 风格的 $(C, M, D)$ 曲线，确保了实验基准与自身基础设施的一致性，避免了直接套用他人公式带来的偏差。

#### 6. 主要结论与发现
*   **最优配置公式**：给出了从计算预算 $C$ 直接推导最优架构参数（层数、维度、专家配置）的完整闭式解。
*   **近优带（Near-optimal band）拓宽**：发现随着计算规模的增大，性能接近最优的架构参数范围会变宽。这意味着**模型越大，对架构微调的容错度越高**。
*   **工程灵活性**：上述发现为工程实践提供了量化依据，即在大规模模型中，可以为了硬件效率（如对齐内存）而稍微偏离理论最优架构，且性能损失极小。

#### 7. 优点
*   **系统性强**：首次将 MoE 的局部参数与 Transformer 的全局架构参数放在统一框架下研究。
*   **方法论优雅**：通过数学手段将不可做的 $O(n^{16})$ 搜索降维至可行的 $O(n^3)$，具有很高的学术参考价值。
*   **工程指导价值高**：提出的“近优带”概念直接回答了工程实现中“性能”与“硬件友好性”如何权衡的问题。

#### 8. 不足与局限
*   **稀疏配置固定**：实验中固定了专家总数（288）和激活专家数（8），虽然这符合当前主流设计，但未探讨不同稀疏度配置下的缩放行为。
*   **下游任务验证缺失**：所有结论均基于预训练 Loss，尚未验证这些架构优化是否能等比例转化为下游任务（如推理、对话）的胜率提升。
*   **迭代闭环不完整**：拟合 $(C, M, D)$ 时使用了启发式架构，理想情况下应使用搜索出的最优架构再次迭代拟合，但受限于资源未进行此闭环。

（完）
