---
title: Bandwidth-Aware and Cost-Efficient Pipeline Parallel Scheduling in Geo-Distributed LLM Training
title_zh: 地理分布式LLM训练中带宽感知且成本高效流水线并行调度
authors: "Han Zhang, Jianchun Liu, Hongli Xu"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25375v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 地理分布式大模型训练的流水线并行调度
tldr: "跨地域LLM训练中，管道并行调度面临带宽异构与电价波动挑战，现有方法无法兼顾延迟与成本且易现队头阻塞。BACE-Pipe通过动态优先级、带宽感知路径规划和成本最小化分配实现高效调度。模拟实验显示，相比基线，平均作业完成时间降低27.9%-64.7%，总电费降低12.6%-30.6%。该框架缓解队头阻塞，提升资源利用率，实现成本与效率联合优化。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25375v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 693, \"height\": 490, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25375v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 718, \"height\": 827, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25375v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 921, \"height\": 513, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25375v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 929, \"height\": 365, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25375v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 927, \"height\": 371, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25375v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 926, \"height\": 364, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25375v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 922, \"height\": 507, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.25375v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 759, \"height\": 157, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25375v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 921, \"height\": 979, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25375v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 871, \"height\": 524, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25375v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1527, \"height\": 332, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25375v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1564, \"height\": 418, \"label\": \"Table\"}]"
motivation: 现有调度器在多租户环境下无法同时优化作业完成时间和电力成本，且易受HoL阻塞影响。
method: 提出BACE-Pipe框架，结合动态作业优先级、带宽感知路径规划和成本最小化GPU分配。
result: "相比基线，平均JCT降低27.9%-64.7%，总电费降低12.6%-30.6%。"
conclusion: BACE-Pipe有效缓解HoL阻塞，提高资源利用率，实现跨地域LLM训练的成本与效率双优。
---

## 摘要
大语言模型（LLMs）的快速发展使得地理分布式训练变得必要，因为单个云区域的GPU稀缺。在这种跨区域环境下，流水线并行（PP）通信效率高，但在异构的区域间带宽和区域电价下，调度流水线并行仍然具有挑战性。现有的调度器要么以延迟为先，导致高电力成本，要么以成本为先，依赖于僵化的资源分配，延长了作业完成时间（JCT）。它们也无法有效优化多租户环境中的执行顺序，长时间运行且带宽密集的作业可能导致队头阻塞（HoL）并降低整体性能。为此，我们提出了BACE-Pipe，一个面向跨地理分布集群LLM训练的带宽感知且成本高效的流水线调度框架。BACE-Pipe首先引入了一种动态作业优先级机制，通过联合考虑作业特性（如计算时间）和实时网络利用率来优化执行顺序。然后，它采用带宽感知的路径查找器，识别满足通信约束的可行跨区域流水线路径，从而防止通信导致流水线停滞。在所有可行路径中，成本最小化分配器通过优先将资源分配到电价较低的区域，确定最优的GPU放置策略。因此，BACE-Pipe减轻了队头阻塞，提高了资源利用率，并同时减少了作业完成时间和总电力成本。大量仿真表明，与最先进的基线相比，BACE-Pipe将平均JCT降低了27.9%至64.7%，总电力成本降低了12.6%至30.6%。

## Abstract
The rapid evolution of large language models (LLMs) has made geographically distributed training necessary due to GPU scarcity within a single cloud region. In such cross-region settings, Pipeline Parallelism (PP) is communication-efficient, yet scheduling PP remains challenging under heterogeneous inter-region bandwidth and regional electricity prices. Existing schedulers are either delay-first, incurring high electricity cost, or cost-first, relying on rigid resource allocation that prolongs Job Completion Time (JCT). They are also ineffective at optimizing execution order in multi-tenant environments, where long-running and bandwidth-intensive jobs can cause head-of-line (HoL) blocking and degrade overall performance. To this end, we propose BACE-Pipe, a bandwidth-aware and cost-efficient pipeline scheduling framework for LLM training across geo-distributed clusters. BACE-Pipe first introduces a dynamic job prioritization mechanism that optimizes execution order by jointly considering job characteristics (e.g., computation time) and real-time network utilization. It then employs a bandwidth-aware pathfinder to identify feasible cross-region pipeline paths that satisfy communication constraints, thereby preventing communication from stalling the pipeline. Among all feasible paths, a cost-minimizing allocator determines the optimal GPU placement strategy by preferentially assigning resources to regions with lower electricity prices. Consequently, BACE-Pipe mitigates HoL blocking, improves resource utilization, and simultaneously reduces both JCT and total electricity cost. Extensive simulations show that BACE-Pipe reduces average JCT by 27.9%--64.7% and total electricity cost by 12.6%--30.6% compared with state-of-the-art baselines.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题：** 大语言模型（LLM）训练需要大量 GPU，但单个云区域 GPU 稀缺，不得不在多地建立地理分布式集群。在这种跨区域场景下，管道并行（PP）虽然通信量较小，但调度面临两大挑战：
    - 区域间带宽异构严重，部分链路通信延迟远超计算时间，导致流水线气泡、作业完成时间（JCT）急剧延长。
    - 各区域电价差异显著，不同放置策略会造成巨大的电力成本差异。
- **现有方法局限：**
    - **延迟优先（Delay-First）调度器**（如 Crux、CASSINI）：试图降低 JCT，但忽略电价，在跨区域环境中成本高昂。
    - **成本优先（Cost-First）调度器**（如 TanGo）：利用区域电价差，但采用固定资源分配，无法动态利用残余 GPU 并适配 PP 的通信敏感特性。
    - **均不优化作业执行顺序：** 固定的先来先服务（FCFS）顺序下，长耗时高带宽的大作业容易造成队头阻塞（HoL），拖慢后续小作业，降低整体集群效率。
- **研究动机：** 需要一种能**同时优化平均JCT和总电力成本**，并能**通过智能排序缓解HoL阻塞**的跨区域流水线调度框架。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **整体架构：** BACE-Pipe 分为控制平面与数据平面。控制平面通过状态收集器、作业解析器和作业调度器进行集中式调度，决策流程包含三个阶段。
- **阶段一：动态作业优先级（Dynamic Job Prioritization）**
    - **计算强度** \(I_j\)：以单 GPU 理论执行时间归一化，数值越小代表作业越短。
    - **带宽敏感性** \(D_j\)：以作业所需最小带宽归一化，值越大代表越容易造成网络拥塞。
    - **自适应权重** \(\alpha\)：定义为当前被占用带宽与总带宽容量之比，实时反映网络拥塞程度。
    - **优先级分数公式：** \( \text{Priority}_j = (1-\alpha) \cdot (1-I_j) + \alpha \cdot (1-D_j) \)。网络空闲时偏向短作业减少 HoL，网络繁忙时偏向低带宽作业避免拥塞恶化。
- **阶段二：带宽感知多区域路径查找器（Bandwidth-Aware Pathfinder）**
    - 首先为每个作业确定**最优 GPU 数** \(K^*\)：选择使单次迭代时间最小的 GPU 数量。
    - **单区域放置检查：** 若某区域 GPU 容量足够，则选择电价最低的区域整体放置，避免跨区通信。
    - **多区域路径构造：** 类似 Prim 算法的贪心扩张：
        - 从每个区域出发，迭代添加与当前路径末端带宽最高的区域，累积 GPU 容量。
        - **关键约束：** 每添加一个区域，检查瓶颈带宽 \(b_{\min}\)，只有当通信时间不超过当前 GPU 数量下的计算时间时，才允许扩展（公式：\(A / b_{\min} \le t_{\text{comp}}(g') \)），确保通信不成为流水线瓶颈。
        - 对所有起始区域尝试后，选择聚合 GPU 数最多（最接近 \(K^*\)）的路径；若存在多个，选平均电费最低的。
- **阶段三：成本最小化分配器（Cost-Min Allocator）**
    - 在选定的路径 P 和目标 GPU 数 g 下，具体分配各区域 GPU 数量。
    - **步骤：** 为路径上每个区域至少分配 1 块 GPU（保证流水线连通性），剩余 GPU 按电价升序逐个分配，优先填满低价区域，直到达到总数量或容量上限。

### 3. 实验设计：使用场景、Benchmark 与对比方法
- **仿真环境：** 模拟 6 个全球主要区域（如爱尔兰、纽约、法兰克福、东京、新加坡、悉尼），GPU 容量从 {16, 32, 64, 128} 中随机选取，电价取自 GlobalPetrolPrices 真实商业电价，区域间带宽参照 AWS G4 实例采样 (25-100 Gbps)。
- **工作负载：** 8 个 LLM 训练作业，参数量从 14B 到 101B（如 Qwen2.5-14B, Llama-3.1-70B, FLM-101B 等），数据集在 Alpaca-52k（小）、WikiText-103（中）、OpenWebText（大）中随机分配，保证多样性。
- **对比方法：**
    - **单区域基线：** LCF（选最低电价区域放置）、LDF（选最大 GPU 容量区域放置）。
    - **跨区域扩展基线：** CR-LCF（按电价排序聚合 GPU）、CR-LDF（从最大容量区域出发按最高带宽贪心扩展）。
- **评估指标：** 平均作业完成时间（Average JCT）和总电力成本（Total Electricity Cost）。

### 4. 资源与算力
- 文中未进行实际物理训练，**所有实验均为仿真（simulations）**。配置中提及 GPU 型号为 Nvidia A6000，用以设定单个区域 GPU 数量范围，但未报告实际训练使用的算力、集群规模或训练时长。因此，**无法直接评估该方法在真实硬件上的开销与性能**。

### 5. 实验数量与充分性
- 实验设计较为全面，主要包括：
    - **端到端性能对比：** 1 组（图 4），对比 BACE-Pipe 与四个基线在默认配置下的 JCT 与成本。
    - **灵敏度分析：** 2 个维度（带宽缩放 0.3x/0.9x/1.5x，GPU 容量缩放 0.5x/0.75x/1.25x），共 2×3 种设置，每种均报告 JCT 与成本。
    - **工作负载强度：** 作业数量从 8 增至 24（4 个梯度），评估不同负载压力下的性能变化。
    - **消融研究：** 移除优先级、路径查找器、成本分配器三个组件，分别测试对 JCT 与成本的影响。
- **评估充分性：** 多维度参数扫描和组件级消融实验确保了结果的可靠性和归因清晰度。对比方法覆盖了工业界实践和直接扩展的跨区域策略，对比公平。但实验完全基于仿真，缺乏真实系统验证。

### 6. 论文的主要结论与发现
- BACE-Pipe 在所有场景下均取得最低平均 JCT 和总电力成本。相比基线，JCT 降低 27.9%–64.7%，成本降低 12.6%–30.6%。
- **揭示“跨区域悖论”**：简单跨区域扩展的 CR-LCF/CR-LDF 在部分场景下 JCT 甚至差于单区域方法，原因是缺乏智能排序导致 HoL 阻塞加剧。
- **优先级机制至关重要**：尤其在资源紧张或带宽充裕时，其吞吐量导向的排序能显著缓解资源垄断。
- **成本最小化分配器** 不仅能降电费，还能通过将负载集中到低价区域、减少资源碎片化，间接改善后续作业的调度效率。
- 面对极端工作负载时，各方法性能趋于收敛，但 BACE-Pipe 始终维持最优。

### 7. 优点：方法或实验设计上的亮点
- **多目标联合优化框架：** 将执行顺序、路径选择、GPU 分配拆解为三个阶段依次求解，有效降低问题复杂度，同时兼顾 JCT 和成本。
- **自适应优先级公式：** 通过实时网络利用率 α 动态平衡计算强度与带宽敏感性，设计精巧且对 HoL 阻塞有针对性。
- **瓶颈感知的路径构造：** 将通信与计算时间之比作为硬约束，确保跨区流水线不会因链路过慢而徒增空泡，这是 PP 调度的关键。
- **消融实验清晰：** 量化了每个模块的独立贡献，证明路径查找器是性能基础，优先级是降低 HoL 的主因，成本分配器兼具经济与碎片化控制双重收益。
- **跨区域悖论的发现：** 指出了不加排序的跨区聚合可能导致整体性能恶化，为后续研究提供了有价值的洞察。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **纯仿真环境，缺乏真实系统验证：** 未部署于实际的跨云/多区域分布式集群，无法反映真实网络抖动、节点故障、动态带宽竞争等影响，实用性待验证。
- **电价模型静态：** 文中假定电价固定且已知，未考虑现货市场波动、长期合同等复杂定价，限制了在经济模型更复杂环境下的直接应用。
- **工作负载模型简化：** 仅使用 PP 并行策略，未考虑数据并行、专家并行等混合策略，且假定每个作业的 PP 配置固定，无法动态调整并行度。
- **对比方法有限：** 基线仅包含 FCFS 加贪心放置，未与现代异构感知调度器（如 HetPipe、MiCS 等）或更先进的排序策略（如 SJF 变体）进行横向比较。
- **调度开销未评估：** 没有分析搜索路径、计算优先级的时间开销，在超大规模集群中，搜索所有起始区域可能成为调度瓶颈。
- **仅模拟 8 个作业、6 个区域：** 规模较小，对数百节点、数百作业的真实云环境推广性不足。

（完）
