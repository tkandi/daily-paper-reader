---
title: "MUFFLe: Efficient Model Update Compression via Generalized Deduplication for Federated Learning"
title_zh: MUFFLe：通过广义去重实现联邦学习中的高效模型更新压缩
authors: "Xiaobo Zhao, Daniel E. Lucani"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14354v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 通过广义去重实现联邦学习的通信高效模型更新压缩
tldr: "联邦学习上行通信开销大，制约边缘部署。MUFFLe将广义去重融入FedAvg，对更新向量近似重复检测，产生固定速率可变计数压缩，消除冗余。IID MNIST 20客户端实验中，仅38 MB通信达92.93%准确率，比无压缩(310 MB)节省87.7%，优于量化(75 MB)和稀疏化(86 MB)。初步验证广义去重用于高效联邦学习的有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.14354v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 659, \"height\": 547, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14354v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 842, \"height\": 419, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14354v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 914, \"height\": 382, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.14354v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 842, \"height\": 218, \"label\": \"Table\"}]"
motivation: 联邦学习模型更新频繁上传，通信开销巨大，成为边缘场景部署的关键瓶颈，亟需高效压缩方法。
method: 提出MUFFLe，将广义去重集成到FedAvg，对更新向量进行近似重复模式去重，产生固定速率、可变计数的压缩表示。
result: "IID MNIST下20客户端，MUFFLe用38 MB通信达92.93%准确率，优于量化(75 MB)和稀疏化(86 MB)。"
conclusion: 广义去重可显著降低联邦学习通信量，验证了其在通信高效联邦学习中的有效性，为低开销系统设计提供新思路。
---

## 摘要
联邦学习非常适用于边缘环境，但通常受限于传输模型更新的上行链路成本。这篇进行中论文介绍了MUFFLe，一种通信高效的更新压缩方案，它将广义去重（GD）集成到FedAvg流程中。MUFFLe对更新向量中的重复模式进行去重，形成一种固定速率、可变计数的压缩方案。在具有20个客户端的IID MNIST数据集上的初步实验表明，MUFFLe以38 MB的累计上行通信量达到了92.93%的目标准确率，而8位量化需要75 MB，Top-k稀疏化需要86 MB，未压缩的FedAvg需要310 MB。这些结果证明了将GD应用于通信高效联邦学习的可行性。

## Abstract
Federated learning is well suited to edge environments but is often limited by the uplink cost of transmitting model updates. This Work-in-Progress paper presents MUFFLe, a communication-efficient update compression scheme that integrates generalized deduplication (GD) into the FedAvg pipeline. MUFFLe deduplicates repeated patterns across the update vector, yielding a fixed-rate, variable-count compression scheme. Preliminary experiments on IID MNIST with 20 clients show that MUFFLe reaches the target accuracy of $92.93\%$ with 38~MB cumulative uplink communication, compared with 75~MB for 8-bit quantization, 86~MB for Top-$k$ sparsification, and 310~MB for uncompressed FedAvg. These results demonstrate the feasibility of applying GD to communication-efficient federated learning.

---

## 论文详细总结（自动生成）

# MUFFLe 论文结构化总结

## 1. 论文的核心问题与整体含义
- **核心问题**：联邦学习（FL）在边缘环境中，客户端向服务器上传模型更新的上行通信成本极高，成为系统瓶颈。
- **整体含义**：提出一种新的压缩思路——利用更新向量内部的重复模式进行广义去重（GD），而不是传统的量化或稀疏化。通过消除更新中的冗余，在不牺牲太多模型精度的前提下大幅降低通信量。
- **研究动机**：现有压缩方法（如量化、Top‑k 稀疏化）虽然有效，但要么均匀降低精度，要么只传输部分元素，没有充分挖掘更新向量中跨元素的结构性重复。MUFFLe 试图填补这一空白，证明 GD 在 FL 压缩中的可行性。

## 2. 论文提出的方法论
- **核心思想**：将联邦平均（FedAvg）中的本地更新向量转换为定长二进制符号，利用广义去重提取重复出现的“基”（base）图案，仅传输不重复的基和每个元素对应的基编号，舍弃残差，实现有损压缩。
- **关键技术细节**：
  - **预处理**：先将浮点更新转换为无符号整数表示（沿用 GreedyGD 的预处理步骤），找出所有元素共有的恒定比特位，保留预设的 `g` 个非恒定最高有效位（MSB）作为基。
  - **广义去重（GD）**：更新元素共享同一 `g`‑bit 基图案的，映射到同一个基，形成字典；每个元素用基 ID 替换。
  - **有损近似**：丢弃偏差（最低有效位），不再存储，实现有损压缩。
  - **压缩码流**：服务器收到 `nb` 个唯一基（每个 `g` bit）和 `d` 个基 ID（每个需 `⌈log₂(nb)⌉` bit）。总比特数 = `nb·g + d·⌈log₂(nb)⌉`，恒定比特仅存一次，开销忽略。
  - **重建**：服务器根据基 ID 将每个元素映射回对应基图案，再逆转换为近似的更新向量。
- **算法流程**（文字描述）：
  1. 计算本地更新 `Δw`，转为无符号整数表示；
  2. 确定恒定比特，截取 `g` 个非恒定 MSB 作为基；
  3. 对基序列去重，生成 `nb` 个唯一基及其 ID 分配；
  4. 传输字典（唯一基）和长度 `d` 的 ID 序列；
  5. 服务器根据 ID 重建近似更新，参与聚合。

## 3. 实验设计
- **数据集与场景**：
  - **MNIST**，独立同分布（IID）划分，训练集均匀分配给 **20 个客户端**。
  - 每轮随机选取 **20%（m=4）** 个客户端参与。
- **模型**：轻量级多层感知机（MLP）：Flatten → 128 单元 ReLU → 10 类 Softmax。
- **训练配置**：SGD，学习率 0.01，本地训练 1 个 epoch。固定随机种子。
- **对比方法（Benchmark）**：
  - **未压缩 FedAvg**（32‑bit 全精度传输）。
  - **8‑bit 量化**：每个参数用 8 位有符号整数传输，附带 32 位缩放因子，总成本 `d×8+32` bit。
  - **Top‑k 稀疏化**：k 的大小匹配 8‑bit 量化的通信预算，传输 k 对（32‑bit 值 + 索引 bit）。
- **评价指标**：达到目标精度 **92.93%**（未压缩基线在 200 轮时的测试精度）所需的 **累计上行通信量**（MB）。
- **预处理**：更新值先四舍五入到小数点后四位，再进行 MUFFLe 预处理。

## 4. 资源与算力
- 论文**未明确说明**使用的 GPU 型号、数量、训练时长或任何硬件信息。
- 仅提到固定随机种子以保证可复现性，未涉及计算资源消耗分析。

## 5. 实验数量与充分性
- **实验数量**：
  - 仅包含 **一个数据集**（MNIST）、**一种数据分布**（IID）、**一种模型**（小 MLP）、**一组对比方法**和 **一个目标精度下的通信成本对比**。
  - 提供了一张表格和两幅图，分别展示通信‑精度曲线和 MUFFLe 的节省倍数。
- **充分性与公平性**：
  - 实验规模较小，属于 Work‑in‑Progress 的概念验证，**不够充分**，无法全面评估方法的泛化能力。
  - 对比方法的选择和参数（8‑bit 量化、匹配预算的 Top‑k）设定相对公平，但未对 MUFFLe 的关键参数（如保留的比特数 `g`）进行调优或消融研究。
  - 没有误差反馈或补偿机制，后续可能影响公平比较；实验也未涵盖非 IID、更大模型等现实场景。

## 6. 论文的主要结论与发现
- MUFFLe 在 IID MNIST 上仅用 **38 MB** 累计上行通信量就达到 92.93% 的目标准确率，相较未压缩 FedAvg（310 MB）实现了 **8.3 倍** 的通信量降低。
- 相比 8‑bit 量化（75 MB，4.1 倍）和 Top‑k 稀疏化（86 MB，3.6 倍），MUFFLe 仍保有超过 2 倍的额外增益。
- 结果初步证明了 **广义去重** 用于联邦更新压缩的可行性和有效性，其通过利用更新内部的重复模式可以显著降低传输成本，并且该方法与量化、稀疏化正交，可组合使用。

## 7. 优点
- **创新性强**：将广义去重从数据压缩领域引入 FL 更新压缩，开辟了新方向。
- **压缩机制独特**：不是降低精度或丢弃部分元素，而是识别并消除跨元素的冗余，实现固定比特率但可变数量的压缩。
- **压缩效率突出**：在初步实验中展现了对基线方法的大幅度通信节省，增益明显。
- **兼容性**：方法可以与现有量化、稀疏化策略正交组合，具备良好的扩展潜力。

## 8. 不足与局限
- **实验局限性**：
  - 仅在 **MNIST 的 IID 设置** 和小模型上验证，无法反映非 IID、复杂任务下的性能。
  - 缺少对关键参数 `g` 的消融分析，不清楚最优比特保留量如何影响精度‑通信权衡。
  - 没有误差反馈或累积校正机制，收敛稳定性未经验证。
- **方法论细节缺失**：
  - 预处理（浮点转整数）的具体实现和潜在精度损失未详述，四舍五入可能人为降低难度。
  - 编码和解码的计算开销、字典传输的额外时延未讨论，实际系统中的可行性存疑。
- **对比维度有限**：未与其他先进的压缩方法（如向量量化、随机量化、误差补偿的 Top‑k 等）比较，仅与两种基本方法对比。
- **应用限制**：依赖于更新向量中存在大量重复模式，当模型结构复杂或梯度分布分散时，增益可能急剧下降。

（完）
