---
title: Clipping Makes Distributed and Federated Asynchronous SGD Robust to Stragglers
title_zh: 梯度裁剪使分布式和联邦异步随机梯度下降对滞后节点具有鲁棒性
authors: "Samuel Erickson, Mikael Johansson"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13287v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 异步SGD中梯度裁剪消除最大延迟依赖
tldr: 异步并行训练中慢节点导致的更新延迟严重影响模型收敛。本文证明梯度裁剪可消除最大延迟对算法复杂度的影响，从而提升对慢节点的鲁棒性。采用sub-Weibull噪声模型刻画深度学习中的重尾梯度噪声，首次在异步优化中同时给出期望和高概率收敛保证。该结果为实践中梯度裁剪的稳定效应提供了理论支撑。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13287v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 809, \"height\": 354, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13287v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 851, \"height\": 1065, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13287v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 849, \"height\": 1073, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13287v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 850, \"height\": 1065, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.13287v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1088, \"height\": 637, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13287v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 847, \"height\": 548, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13287v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 853, \"height\": 462, \"label\": \"Table\"}]"
motivation: 异步SGD受慢节点引起的更新延迟影响，收敛变差，而实践中梯度裁剪能稳定训练，但缺乏理论解释。
method: 采用sub-Weibull噪声模型建模重尾梯度噪声，分析带裁剪的异步SGD收敛性，并消除最大延迟依赖。
result: 证明梯度裁剪使异步SGD的oracle复杂度不再依赖最大延迟，并给出期望和高概率收敛保证。
conclusion: 理论上解释了梯度裁剪在异步训练中的抗滞后效应，为分布式深度学习的稳定训练提供保障。
---

## 摘要
在现代机器学习中，训练的并行化是扩大规模的重要策略。异步随机梯度下降（ASGD）通过避免等待慢速工作节点来最大化硬件利用率。然而，在使用恒定步长时，由于更新延迟较大，ASGD的收敛性仍会受到慢速节点的负面影响。同时，在深度学习模型的异步训练中，经验性地观察到梯度裁剪能够“稳定”训练。在本文中，我们为这一行为提供了理论依据，证明裁剪消除了预言复杂度对最大延迟的依赖。我们采用梯度噪声的次威布尔模型，该模型将次高斯和次指数分布推广到更重尾的分布，这源于深度学习中的经验观察。我们证明了期望收敛，并在异步优化中首次证明了高概率收敛。

## Abstract
In modern machine learning, parallelization of training is an important strategy for increasing scale. Asynchronous stochastic gradient descent (ASGD), which maximizes the utilization of available hardware by avoiding waiting for slow workers. However, with constant step sizes, the convergence of ASGD is nonetheless affected negatively by slow workers due to large delays in updates. At the same time, it has been empirically observed in asynchronous training of deep learning models that gradient clipping "stabilizes" training. In this work, we provide a theoretical justification for this behavior, as we show that clipping removes the dependence of the maximum delay in the oracle complexity. We employ a sub-Weibull model of gradient noise which generalizes sub-Gaussian and sub-exponential distributions to more heavy-tailed distributions, motivated by empirical observations in deep learning. We show convergence in expectation, and the first time in asynchronous optimization, convergence with high probability.

---

## 论文详细总结（自动生成）

好的，以下是基于该论文的结构化总结。

### 1. 论文的核心问题与整体含义

*   **研究动机**：在分布式或联邦学习的异步随机梯度下降（AsyncSGD）中，计算慢的节点（“滞后节点”）会导致梯度更新延迟，严重影响模型的收敛速度和稳定性，甚至导致训练失败。实践中发现梯度裁剪能“稳定”异步训练，但缺乏理论解释。
*   **核心问题**：梯度裁剪能否从根本上消除异步SGD对最大延迟的依赖，从而在理论上和实践中都使算法对滞后节点具有鲁棒性？
*   **整体含义**：本文首次从理论上证明，梯度裁剪不仅能解释其在深度学习异步训练中的稳定化效应，还能在不偏向快速节点（即不引入偏差）的情况下，使异步SGD的收敛复杂度不再依赖于最大延迟。这为在数据异构的联邦学习等场景中使用AsyncSGD提供了重要的理论支撑。

### 2. 论文提出的方法论

*   **核心思想**：利用梯度裁剪操作固有的有界性（`||clipped_gradient|| ≤ c`），在经典AsyncSGD的“扰动迭代分析”框架中，将虚拟序列与真实序列的误差从依赖于梯度总和的累积量，约束为一个仅与裁剪半径 `c` 和并发数 `τC` 相关的常量 `ηcτC`。这从根本上切断了延迟分析链条中对最大延迟 `τmax` 的依赖。
*   **关键技术细节**：
    *   **梯度噪声模型**：采用**次威布尔（sub-Weibull）分布** `subW(θ, σ)` 来建模梯度噪声。该分布比传统的高斯或次指数分布更重尾，能更好地拟合并描述深度学习训练中真实观察到的梯度噪声特性。参数 `θ` 控制尾部厚度。
    *   **算法流程**：
        1.  **同构设置（算法1）**：服务器维护一个大小为 `τC` 的活跃工作节点集合。每当一个工作节点完成其裁剪后梯度 `clipc(∇F)` 的计算，服务器立即用它更新全局模型，并选择一个新的空闲节点加入活跃集合开始计算。此流程不因节点快慢而产生偏向。
        2.  **异构设置（算法2）**：为避免偏向快速节点而引入优化偏差，当有节点完成计算后，服务器随机从**所有** `n` 个节点中抽取一个来接替任务。这可能导致一个节点同时处理多个请求（梯度排队），但保证了更新的无偏性。
    *   **理论分析要点**：通过构造虚拟序列 `x̃t`，其更新只依赖于最新的梯度（`x̃_{t+1} = x̃_t - η * g_t(x_t)`），证明了真实迭代序列 `xt` 与 `x̃t` 的距离被 `ηcτC` 所限制。以此为基础，分别推导了**期望收敛**和**高概率收敛**的界。在期望分析中，利用次威布尔噪声的矩性质控制裁剪引入的偏差；在高概率分析中，利用Freedman鞅不等式处理由异步和裁剪带来的鞅差序列。

### 3. 实验设计

*   **数据集与场景**：
    *   **同构设置（数据共享内存）**：CIFAR-10（训练ResNet-18模型）和Shakespeare（训练LSTM用于下一词预测）。
    *   **异构设置（联邦学习）**：CIFAR-10用狄利克雷分布（Dirichlet, α=0.5）划分标签以制造数据异构性，训练简单的两层CNN模型。
*   **性能指标**：达到特定测试准确率或困惑度所需的**模拟墙上时钟时间**。
*   **对比基准方法**：
    *   **Vanilla ASGD**：使用恒定步长的标准异步SGD。
    *   **Delay-adaptive ASGD**：Koloskova et al. (2022)提出的在线步长自适应方法。
    *   **Ringleader ASGD**：Maranjyan et al. (2025)等人提出的通过丢弃过于陈旧梯度来达到最优时间复杂度的最新方法。
*   **滞后节点模拟**：使用16个工作节点，其中一半节点计算速度为1个时间单位，另一半为 `D` 个时间单位（实验中取 `D=4, 8`）。

### 4. 资源与算力

*   论文未明确提及实验所使用的具体**GPU型号、数量或总训练时长**。仅说明了使用模拟的墙上时钟时间作为性能衡量标准，且每个实验设置使用3个不同的随机种子进行平均。

### 5. 实验数量与充分性

*   **实验组数**：实验覆盖了**两个数据集（CIFAR-10, Shakespeare）** 在**两种设置（同构，异构）** 和**两种延迟因子（D=4, D=8）** 下的情况，共计8组主要对比实验。
*   **充分性与客观性**：实验设计较为全面，覆盖了视觉和语言任务、同步和异步数据分布。对比方法包括了当前最优的几种异步算法。每个实验都通过超参数扫描（步长、裁剪半径等）报告了达到目标指标的最优墙上时间，确保了比较的公平性。但所有实验均为模拟环境，未在真实的分布式物理集群上进行验证。

### 6. 论文的主要结论与发现

*   **理论结论**：该论文严格证明了，在平滑非凸优化中，使用梯度裁剪的异步SGD（Clipped ASGD）的收敛复杂度（达到ε精度所需的迭代次数）**与最大延迟 `τmax` 无关**。这无论在数据同构还是异构下均成立，并且首次给出了异步优化算法的高概率收敛保证。
*   **实验发现**：
    *   在同构设置下，Clipped ASGD在墙上时钟时间上均显著优于Vanilla和Delay-adaptive ASGD，最高加速可达**2.1倍**。
    *   在异构设置下，Clipped ASGD同样持续超越基线方法，证实了其在联邦学习场景中对滞后的鲁棒性。
    *   随着延迟增大，Vanilla ASGD需要更精细地调小步长才能稳定，而Clipped ASGD表现更稳定，验证了理论预测。

### 7. 优点

*   **理论创新与深度**：首次在理论上揭示了梯度裁剪在异步训练中的根本性作用，即消除对最大延迟的依赖。该发现为普遍的经验实践提供了有力解释。
*   **分析工具先进**：采用更具表达力的次威布尔噪声模型，比常规的高斯或矩界假设更贴合深度学习实际，并在此框架下同时完成了期望和高概率两种收敛分析。
*   **结果普适性好**：理论结果涵盖了同构和更具挑战性的数据异构场景（联邦学习），证明其方法不会像某些延迟自适应方案那样引入优化偏差。
*   **实验验证全面**：在多个标准数据集、不同架构和延迟设置下，与多个先进基线进行了比较，结果有力支持了理论发现。

### 8. 不足与局限

*   **模拟实验而非真实系统**：所有的实验都在模拟的延迟环境下进行，未在真实的分布式系统或异构硬件集群上验证算法的墙上时钟加速效果。
*   **异构设置的实用性**：算法2为保证无偏性采取的随机调度策略可能导致节点积压任务，其在真实大规模联邦学习系统中的效率和调度复杂度未被深入讨论。
*   **超参数敏感性**：虽然裁剪提升了鲁棒性，但最优的裁剪半径 `c` 仍需通过扫描来确定，且文中未提供自适应调整 `c` 的方案。
*   **假设范围**：理论分析依赖于标准的平滑性假设。论文也指出，在更弱但更现实的条件如 `(L0, L1)`-平滑性下，裁剪的效果可能更为显著，这有待未来探索。

（完）
