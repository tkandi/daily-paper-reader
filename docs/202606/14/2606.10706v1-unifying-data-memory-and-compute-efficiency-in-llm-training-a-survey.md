---
title: "Unifying Data, Memory, and Compute Efficiency in LLM training: A Survey"
title_zh: 统一大型语言模型训练中的数据、内存与计算效率：综述
authors: "Vanessa Schmidt, Huy Hoang Nguyen, Cédric Jung, Shirin Salehi, Anke Schmeink"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10706v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 综述从数据、内存和计算瓶颈角度组织大语言模型训练效率技术
tldr: 随着大语言模型训练和部署越来越受资源限制，效率优化需从孤立技术转向系统约束。本综述从数据效率、内存效率和计算预算三大约束出发，梳理了数据选择、内存联合优化和计算感知分配等方法。关键发现：不同任务与预算下最优数据子集差异显著；GPU内存是微调主要瓶颈，需权重、优化器状态和激活内存协同缩减；计算预算应动态分配，边际收益不足时停止或重定向。该工作为资源受限下的模型训练与部署提供了统一决策框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10706v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1770, \"height\": 293, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10706v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 892, \"height\": 407, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10706v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 920, \"height\": 1203, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10706v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 890, \"height\": 785, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10706v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 889, \"height\": 1044, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10706v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1846, \"height\": 717, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10706v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1568, \"height\": 1124, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10706v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1649, \"height\": 219, \"label\": \"Table\"}]"
motivation: 资源约束日益决定LLM训练与部署，需超越孤立技术，从系统约束视角整合效率研究。
method: 以数据、内存和计算三大约束为中心，综述数据选择、内存联合优化及计算感知分配等进展。
result: 不同任务和预算下最优数据子集不同；GPU内存为微调主要瓶颈，需联合缩减；计算预算应动态分配，边际收益不足时停止。
conclusion: 效率优化应遵循资源条件决策原则，联合考虑数据、内存与计算约束，为LLM训练部署提供统一框架。
---

## 摘要
资源约束日益决定哪些内容可以在大型语言模型（LLM）中训练、微调和部署，然而效率通常通过孤立的技术来研究，而不是作为一个相互作用的限制系统。本综述采用以约束为中心的视角，围绕三个耦合的瓶颈组织最新进展：数据效率（训练什么）、内存效率（如何适应训练）以及计算预算意识（何时何地消耗FLOPs）。在数据维度上，我们回顾了最大化每个标记学习效果的筛选与剪枝方法，涵盖了基于学习动态的可扩展代理信号、基于梯度和影响力的评分，以及难度感知和课程式策略。我们强调新证据表明，在不同的场景下，不同的“好数据”概念占主导地位，这意味着最优子集取决于任务目标和资源预算，而非普遍适用。在系统方面，我们表明GPU内存而非原始计算通常是微调中的主要瓶颈，有效的扩展需要联合减少权重存储、优化器状态和激活内存，而不是孤立地优化任何单一组件。除了内存，我们将训练和推理视为受计算控制的过程，其中优化、数据选择和解码必须明确考虑有限的FLOPs预算。我们回顾了计算最优分配和停止规则的证据，即当边际性能增益低于预算相关阈值时，应停止或重新分配计算。这些结果共同将计算感知的数据选择、缩放定律和自适应推理统一在资源条件决策的共同原则下。

## Abstract
Resource constraints increasingly determine what can be trained, fine-tuned, and deployed in large language models (LLMs), yet efficiency is often studied through isolated techniques rather than as an interacting system of limits. This survey adopts a constraint-centric perspective and organizes recent progress around three coupled bottlenecks: data efficiency (what to train on), memory efficiency (how to fit training), and compute budget awareness (when and where to spend FLOPs). On the data axis, we review selection and pruning methods that maximize learning per token, ranging from scalable proxy signals based on learning dynamics to gradient- and influence-based scoring, as well as difficulty-aware and curriculum-style strategies. We highlight emerging evidence that different notions of good data dominate in different regimes, implying that optimal subsets depend on the task objective and resource budget rather than being universal. On the systems side, we show that GPU memory, not raw compute, is often the dominant bottleneck in fine-tuning, and that effective scaling requires jointly reducing weight storage, optimizer states, and activation memory rather than optimizing any single component in isolation. Beyond memory, we frame training and inference as compute-governed processes in which optimization, data selection, and decoding must explicitly account for finite FLOP budgets. We review evidence for compute-optimal allocation and stopping rules, where computation should be halted or reallocated once marginal performance gains fall below a budget-dependent threshold. Together, these results unify compute-aware data selection, scaling laws, and adaptive inference under a common principle of resource-conditioned decision-making.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义
- **研究背景**：大型语言模型（LLM）的训练和部署面临数据、内存和计算三重资源瓶颈，而现有效率研究往往将这三者视为孤立维度，忽略了它们之间的耦合与制约。
- **核心问题**：如何系统性地在有限资源（VRAM、FLOPs、时间）下，同时优化“训练什么数据”（数据效率）、“如何把训练塞进硬件”（内存效率）以及“何时停止或重新分配计算”（计算预算），避免单点优化导致瓶颈转移。
- **整体含义**：本文提出一种**约束中心的统一视角**，把数据选择、内存管理和计算分配看作一个耦合的资源条件决策问题，以边际效用（每单位资源的性能增益）作为统一的衡量标准，为边缘设备、学术实验室等资源受限场景提供了原理性的设计指南。

## 2. 论文提出的方法论
本文作为综述，并没有提出全新的算法，而是**以分析框架组织已有方法**，核心方法论包括：
- **资源受限生命周期架构**（Figure 1）：将效率优化流程分解为三个相互反馈的阶段——  
  - **数据效率（基础）**：“训练什么”，从噪声过滤到基于梯度影响力、课程学习的动态数据筛选。  
  - **内存效率（约束）**：“如何装下”，分解内存占用量为权重（Mθ）、优化器状态（MO）、激活内存（MA），并针对每一项设计压缩策略。  
  - **计算预算（调控器）**：“何时停止”，引入计算调控器（compute governor）概念，将训练与推理决策统一为**边际增益（Gt = –ΔL/ΔFLOPs）**与剩余预算的比较。
- **计算调控器形式化**（Section V）：将系统决策抽象为策略 π(St, Bt)→at，其中 St 是当前损失动态和策略状态，Bt 是剩余FLOPs/能量/时间预算，at∈{继续,重分配,停止}。当边际增益低于阈值时触发重分配或停止，从而把 scaling laws、数据选择、自适应性推理统一到同一个原则下。
- **关键技术细节（综合已有工作）**：  
  - 数据效率维度：从早期的梯度范数（GraNd, EL2N）、学习轨迹（SmallToLarge）、Adam影响力（LESS）、在线贪心近似（GREATS），到平衡能力（BIDS）、难度感知（DART）和计算约束选数（CADS, compute‑constrained data selection）。  
  - 内存效率维度：用核心集替代大批次（CoLM）、块坐标下降更新（HiFT, BAdam）、随机子空间与低秩零阶估计（SubZero, LOZO, AdaZeta）、直接量化训练（DQT）以及冻结量化基座加适配器（QLoRA, PEQA, QST）。  
  - 计算预算维度：计算最优 scaling laws（Kaplan 参数重、Chinchilla 数据重、数据受限重复训练），KV‑cache 分页管理（vLLM）、专家混合（MoE）、逐层/逐token路由（Duo‑LLM）以及投机解码（speculative decoding）等。

## 3. 实验设计（综述覆盖的已有工作）
- **数据集**：综述总结了大量公开基准，包括通用语言建模（C4、The Pile）、数学推理（MATH、GSM8K）、指令跟随（Alpaca、MathInstruct）、常识问答（MMLU）以及长文本任务等。
- **任务场景**：涵盖预训练、监督微调（SFT）、指令微调（IFT）、参数高效微调（PEFT）、RLHF 以及推理部署。
- **对比方法**：每个子领域内部进行了横向对比，例如数据选择中比较 LESS、GREATS、STAFF、S2L、BIDS；内存方法中比较 LoRA、QLoRA、BAdam、HiFT、Addax、DQT 等；计算预算中比较不同 scaling laws（Kaplan vs. Chinchilla vs. 数据受限扩展），以及推理阶段的 MoE 路由、投机解码等。
- **评价指标**：除了常用的困惑度（PPL）、准确率、BLEU，还关注内存占用（VRAM GB）、FLOPs/Token、吞吐量（tokens/s）和训练时长等系统指标。

## 4. 资源与算力
- 本综述本身**不涉及额外的算力投入**，所有结论均基于对现有文献的整理与归纳。
- 但综述引用的工作中，典型实验使用了 **A100‑40/80GB、24 GB消费级GPU（如RTX 3090/4090）**，并在部分场景下展示了在单卡上全参微调 7B‑13B 模型的可能性（如 HiFT、BAdam）。文中未给出统一的算力总览，但反复强调“GPU内存而非纯计算是瓶颈”。

## 5. 实验数量与充分性
- 综述覆盖了约 **80+ 篇**相关文献，跨数据、内存、计算三大维度，每个维度又被细分为多个子方向（如数据效率下分4个分支，内存效率分4个杠杆），覆盖面广。
- 由于是综述，它本身没有进行新的对照实验，但通过对比不同独立工作的结果，分析了方法之间的相对优势和隐含的耦合效应（如数据选择可能增加内存选值开销，块更新可能减慢收敛）。这种跨工作分析有助于揭示单篇论文难以观察的折衷，但从实验标准看，并非由统一基准上运行所有方法而得，因此存在**异质性偏差**（不同模型、数据集、超参）。
- 总体上，综述引用的研究数量充足，对三大瓶颈的梳理较为系统，能够支撑其统一视角的主张。

## 6. 主要结论与发现
- **数据效率**：从静态噪声过滤转向动态边际效用最大化。*最优数据子集依赖于任务目标和计算预算*，而不是通用的；高成本的梯度影响力选择需在足够高的训练‑选择模型规模比（约5‑10倍）下才值得投资。
- **内存效率**：GPU内存是微调的主要瓶颈，并且**需要联合压缩参数、优化器状态和激活内存**，而不是单独优化某一项。块更新、量化训练、核心集选取等方法尽管有效，但各自引入“时间换空间”、噪声等代价，单一方法的收益在被其他内存项限制后会递减。
- **计算预算**：计算应被视为有限资源，训练和推理过程都应遵循**边际收益阈值停止规则**。Scaling laws 给出了计算‑最优的训练规模与时长比例；推理阶段的路由、投机解码和 KV 缓存压缩同样是预算感知的决策问题。
- **系统级耦合**：优化数据往往增加内存/计算开销，内存压缩可能损害数据适应度或增加总训练时间，因此真正的效率必须从资源条件决策的原理出发，采用“计算调控器”统一协调。

## 7. 优点
- **视角创新**：提出“资源受限生命周期”和“计算调控器”的统一框架，把分散在各个子领域的技术联系到一起，尤其点出了“静态到动态”的数据选择鸿沟。
- **结构清晰**：围绕“训练什么‑如何装下‑何时停止”的逻辑主线组织内容，对每一个记忆分量和瓶颈给出明确分类（Figure 4、Table I）。
- **强调耦合与折衷**：明确指出单个维度的优化往往会转移瓶颈，并汇总了跨瓶颈影响表（Table II），提醒研究人员避免单点思维。
- **前沿覆盖**：包含2024‑2025年最新工作（如 DQT、HiFT、BAdam、STAFF、GREATS、CADS 等），对边缘部署场景的论述也具有现实意义。

## 8. 不足与局限
- **综述性质限制实验客观性**：所有结论基于文献结果的交叉比较，而非统一受控实验，各方法之间不可直接数值比较，可能存在选择性报告偏倚。
- **对硬件和系统实现的考量较浅**：文中虽提到量化需要硬件友好内核，但对具体硬件实现细节（如 Tensor Core 利用、内存带宽分析）讨论较少，未深入对比不同优化器在实际部署中的吞吐量。
- **计算调控器缺少量化实例**：提出的 π(St, Bt) 概念具有启发性，但没有给出一个完整的、可运行的案例，可能会降低在工程中的直接可指导性。
- **部分领域覆盖有限**：例如分布式训练（ZeRO、FSDP）的详细分析被一带而过，对多GPU下的内存‑计算权衡讨论不足；推理部分的 system‑aware 调度仅点到为止。
- **风险**：综述强调的“动态选择”和“联合压缩”路径仍处于早期阶段，可能存在尚不成熟的噪声累积或稳定性问题（文中也提及），实际落地风险较高。

（完）
