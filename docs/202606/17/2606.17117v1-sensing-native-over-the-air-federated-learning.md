---
title: Sensing-Native Over-the-Air Federated Learning
title_zh: 感知原生的空中联邦学习
authors: "Peiyuan Huang, Shijian Gao, Jia Yan, Georgios B. Giannakis"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.17117v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 利用梯度统计量实现零开销分布式感知的空中联邦学习
tldr: 现有通感算一体化空中联邦学习因感知模块独享资源，导致通信与计算资源竞争，损害模型训练性能。本文提出感知原生空中联邦学习，零开销利用本地梯度信号自相关特性实现多目标距离估计，并通过梯度统计量上传实现分布式协作定位。设计了基于匹配滤波的鲁棒距离估计和三边定位方法，分析了模型聚合误差与噪声统计对收敛的影响，并优化功率分配与波束赋形。实验表明，所提框架在学习与感知性能上均显著超越现有基准，为一体化系统设计开辟新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17117v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 777, \"height\": 575, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17117v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 762, \"height\": 570, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17117v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 567, \"height\": 425, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17117v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 563, \"height\": 422, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17117v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 569, \"height\": 430, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17117v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 575, \"height\": 450, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17117v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 566, \"height\": 428, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17117v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 571, \"height\": 429, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.17117v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 663, \"height\": 217, \"label\": \"Table\"}]"
motivation: 现有通感算一体化空中联邦学习需为感知模块分配专用资源，与通信计算竞争，降低联邦学习性能。
method: 提出感知原生空中联邦学习，利用梯度信号自相关进行匹配滤波距离估计，通过梯度统计量传输实现三边协作定位，并联合优化功率分配与波束赋形。
result: 仿真显示，较代表性基准，所提框架在学习精度和定位精度上均实现更优性能。
conclusion: 通过挖掘梯度内在感知能力，实现零开销分布式定位，联合优化可平衡学习与感知需求，为通感算一体化提供新范式。
---

## 摘要
空中联邦学习（FL）利用多址信道的叠加特性，实现通信高效的分布式模型训练。现有的集成感知、通信与计算（ISCC）的空中FL系统通常需要为感知模块分配专用资源，不可避免地因资源竞争而损害FL性能。本文提出了一种感知原生的空中FL框架，在每轮模型聚合中零开销地发掘内置的分布式无线感知能力。具体而言，具有良好自相关特性的高维局部梯度信号被同时用于目标距离估计，而空中FL本身所需的梯度统计量则充当现成的网关，将本地感知结果传送到边缘服务器进行协同定位。为对抗设备间干扰、信道衰落和通信噪声，我们提出了一种基于高效匹配滤波距离估计的鲁棒三边测量目标定位方法。然后，通过显式刻画不完美模型聚合和噪声梯度统计量传输对感知原生空中FL收敛性的影响，我们设计了一种统计感知的通信-学习联合优化方法。我们首先推导了分配给局部梯度及其统计量的闭式最优功率预算，并在此基础上提出了一种高效的连续凸近似方法用于接收波束成形优化。仿真结果表明，与代表性基线相比，所提出的框架同时实现了卓越的学习与感知性能。

## Abstract
Over-the-air federated learning (FL) leverages the superposition property of multiple-access channels to enable communication-efficient distributed model training. Existing integrated sensing, communication, and computation (ISCC)-enabled over-the-air FL systems typically require dedicated resources for the sensing module, inevitably compromising FL performance due to resource competition. In this paper, we propose a sensing-native over-the-air FL framework that explores built-in distributed wireless sensing capability with zero overhead per model aggregation. Specifically, the high-dimensional local gradient signals possessing favorable autocorrelation property are concurrently leveraged for target distance estimation, while the gradient statistics already required for over-the-air FL serve as a ready-made gateway to deliver locally-sensed results to the edge server for cooperative localization. To combat inter-device interference, channel fading, and communication noise, we put forth a robust trilateration-based target positioning method building upon an efficient matched-filtering-based distance estimation. Then, by explicitly characterizing the impact of imperfect model aggregation and noisy gradient-statistics transmission on the sensing-native over-the-air FL convergence, we develop a statistics-aware communication-learning co-design approach. We first derive the closed-form optimal power budgets allocated to local gradients and their statistics, based on which an efficient successive convex approximation method is proposed for receiver beamforming optimization. Simulation results show that the proposed framework simultaneously achieves superior learning and sensing performance compared to representative baselines.

---

## 论文详细总结（自动生成）

好的，根据您提供的论文内容，以下是该论文的结构化、深入且客观的中文总结。

### 1. 研究的核心问题与整体含义

*   **核心问题**：现有的集成感知、通信与计算（ISCC）空中联邦学习（FL）系统，需要为无线感知模块分配专用的时频或功率资源。这导致了感知与 FL 任务之间的**资源竞争**，不可避免地损害 FL 的模型训练性能。
*   **研究动机**：空中 FL 本身具备实现分布式感知的潜力。其一，高维度的本地梯度信号具有良好的自相关特性，类似于雷达波形；其二，FL 过程中本就需要传输梯度统计量（均值、方差）来辅助模型聚合，这些信号可以作为“现成的”信息载体。然而，这一内置的感知能力在现有工作中被严重忽视。
*   **整体含义**：本文提出了一个**感知原生（Sensing-Native）空中 FL 框架**。其核心思想是“零开销”地发掘空中 FL 过程中内在的无线感知功能，即**完全复用**本地梯度信号及其统计量传输过程，同时完成模型聚合与协同目标定位，从而从根本上消除感知与 FL 之间的性能权衡，实现两者的共赢。

### 2. 方法论

论文提出的方法论包含零开销感知方法和统计感知的通信-学习联合优化两部分。

*   **核心思想**：
    *   **零开销感知**：每个无线设备（WD）利用自身发射、并经目标反射回来的局部梯度信号，通过匹配滤波估计目标距离。然后，将本地距离估计值“嵌入”到原本就要上传给基站（BS）的梯度均值信号的虚部中，实现感知信息的无额外开销上传。
    *   **联合优化**：显式建模了由于噪声统计量传输导致的模型聚合误差，并以此为一个新的误差项，联合优化发射功率预算分配和接收波束成形，以最大化 FL 的收敛性能。

*   **关键技术细节**：
    *   **双功能信号设计**：本地梯度信号 `x1` 被同时用于空中计算和目标感知；梯度标准差 `x2` 用于传输方差信息；梯度均值信号 `x3` 被设计为复数信号，其实部传输均值，**虚部传输本地距离估计值**，从而实现感知数据与学习统计量的融合传输。
    *   **本地距离估计**：
        *   利用本地梯度信号 `x1` 的良好自相关特性 `(公式 17)`，对回波信号 `(公式 18)` 进行匹配滤波 `(公式 19)`。
        *   由于不同设备梯度的统计正交性，干扰项可被近似消除，匹配滤波输出的峰值位置 `(公式 21)` 即对应目标反射造成的时延，进而估算出距离。为提升鲁棒性，采用历史多轮“多数投票”机制 `(公式 22)` 确定最终上传的距离估计值。
    *   **协同目标定位**：
        *   基站（BS）从解调后的梯度均值信号中提取各 WD 的距离估计值 `(公式 14)`。
        *   提出**基于密度聚类的鲁棒三边定位法**：对所有 WD 的任意三组合进行最小二乘定位 `(公式 23)`，生成候选位置集合。然后使用 DBSCAN 算法对这些候选位置进行聚类，剔除异常点，选取最大簇的几何中心作为最终的目标位置 `(公式 24)`。
    *   **收敛性分析与问题建模**：
        *   **关键洞察**：论文首次刻画了噪声统计量传输与空中模型聚合误差之间的**耦合效应** `(引理1, 公式 30)`。聚合误差被分解为三个部分：`E1`（统计量噪声）、`E2`（梯度聚合噪声）、`E3`（耦合噪声）。
        *   基于此，推导了 FL 的收敛界 `(定理1, 公式 34)`，并将其作为目标函数，构建了联合优化接收波束成形向量 `f` 和功率预算 `PG, Pμ, Pν` 的非凸问题 `(P1)`。
    *   **优化求解**：
        *   **功率分配**：在给定波束成形向量 `f` 的情况下，推导出了功率预算 `Pμ`（用于梯度均值传输）的唯一最优解的判断方程 `(命题1, 公式 36)`，并给出了 `PG`（用于梯度传输）和 `Pν`（用于梯度方差传输）的闭式最优解 `(命题2, 公式 37-39)`。
        *   **波束成形**：给定功率分配后，将非凸的波束成形优化问题转化为一系列可解的凸问题（二阶锥规划，SOCP），并采用连续凸近似（SCA）方法迭代求解 `(命题4, 公式 P4)`。

### 3. 实验设计

*   **数据集与场景**：
    *   **学习任务**：使用 **CIFAR-10** 数据集训练一个卷积神经网络（CNN），该网络参数总量为 `D = 83,594`。数据集被平均分配给 `M = 20` 个 WD。
    *   **仿真场景**：模拟一个 `100m × 100m` 的毫米波（mmWave）通信区域，BS 配备 `N = 32` 根天线，位于 `(0, 0, 10)`。载频为 26 GHz。信道模型采用多径几何模型。
*   **基准方法（Benchmarks）**：
    1.  **Error-free aggregation**：理想的无噪声、无衰落的模型聚合（性能上界）。
    2.  **Random beamforming**：使用最优功率分配，但波束赋形向量随机。
    3.  **Uniform power budget allocation**：波束赋形优化，但功率预算均分。
    4.  **Uniform power allocation + Random beamforming**：功率均分且波束随机。
    5.  **Power-splitting-based ISCC**：设备将功率一分为二，分别用于传输梯度信号和专用感知波形。
    6.  **User-splitting-based ISCC**：将设备分为两组，一组负责 FL，一组负责感知。
*   **对比维度**：实验对比了不同方法在多种**信噪比（SNR）**、不同**设备分布密度（dmid）**、不同**感知增益**下的学习性能（测试准确率）和感知性能（定位覆盖率、定位误差）。

### 4. 资源与算力

论文**未明确说明**实验所使用的具体硬件资源（如 GPU 型号、数量）或模型训练的总时长。这一信息在文中缺失。

### 5. 实验数量与充分性

论文设计了较为全面的实验，以验证其框架在不同维度的有效性，实验设计具有充分性和客观性。
*   **学习性能实验**：验证了在不同信噪比（10 dBm 和 -10 dBm）、不同优化算法（梯度下降和随机梯度下降）下的测试准确率，并与多个基准进行对比（图 3, 4, 5, 6）。
*   **感知性能实验**：评估了在不同感知增益、不同设备分布密度（dmid = 5m, 20m）、不同设备数量（10, 20, 30）下的**定位误差分布**（图 7）和**定位覆盖率**（表 1，图 8）。
*   **消融实验**：对感知方法本身进行了消融研究，对比了无“本地多数投票”和无“密度聚类”的简化版方法，证明了这两个模块的重要性（图 8）。
*   **公平性**：所有对比方法均在相同的仿真设置下进行评估，结果报告了 30 次独立实验的平均值，保证了结果的统计稳定性和比较的公平性。

### 6. 主要结论与发现

1.  **性能优势显著**：所提出的感知原生框架在学习性能和感知性能上均显著优于各种基准方案，特别是在低信噪比和资源受限场景下优势更为明显。
    *   **学习上**，它能以接近理想聚合（Error-free）的性能运行，远超功率均分或 ISCC 对比方案（图 3）。
    *   **感知上**，在高感知增益下，其定位覆盖率比无聚类和无投票的简化版分别高出 38.0% 和 41.5%，而传统的 ISCC 方案覆盖率则极低（图 8）。
2.  **打破资源权衡**：与需要资源竞争的 ISCC 方案不同，感知原生设计通过“零开销”复用，**完全释放了 ISCC 的全部资源用于提升 FL 性能**，同时实现了高精度的感知，证明了感知与学习之间可以不是零和博弈。
3.  **鲁棒感知方法有效**：“本地多数投票”和“基于密度聚类的三边定位”对克服设备间干扰和通信噪声、提升定位鲁棒性至关重要。
4.  **联合优化的必要性**：功率预算的优化分配至关重要，尤其在低信噪比下。论文发现，当某个设备信道条件极差时，为其梯度和统计量分配近似相等的功率是最优策略，这表明统计量传输与梯度传输同等重要。

### 7. 优点

*   **创新性强**：提出的“感知原生”理念是一个新颖且优雅的解决方案，通过巧妙复用，从根本上解决了现有 ISCC 系统中的资源竞争难题。
*   **理论扎实**：对噪声统计量导致的 FL 收敛性偏差进行了严格的数学刻画，并据此推导了闭式或半闭式的资源分配最优解，理论深度足够。
*   **设计完整**：从信号设计、本地距离估计、协同定位到通信-学习联合优化，形成了一套完整的端到端解决方案。
*   **实验设计周密**：考虑了信噪比、设备分布、感知增益、优化器等多种因素，并通过消融实验验证了各模块的有效性，结论具有说服力。

### 8. 不足与局限

*   **硬件与算力未报告**：“资源与算力”部分的缺失，使得其他研究者难以精确评估提出方案的实际部署成本和能耗。
*   **假设条件较强**：
    *   **梯度独立假设**：目标的协同定位依赖于不同设备间梯度信号的统计独立性假设。作者虽提出了“多轮投票”来缓解这一问题，但在非独立同分布（Non-IID）数据场景下的鲁棒性仍需验证。
    *   **单目标假设**：论文假设只存在一个被动的感知目标，对于多目标场景的适用性未作探讨。
*   **仿真场景的局限性**：实验基于仿真的毫米波系统和 CIFAR-10 数据集，虽然具有代表性，但与真实物理世界的复杂电磁环境和更大规模、更复杂的深度学习任务仍有差距。
*   **优化问题的静态性**：提出的联合优化方案可能基于每轮或每隔几轮回传的信道信息，算法的动态适应性和信令开销未在文中详细评估。

（完）
