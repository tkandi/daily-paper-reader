---
title: "KANLib -- An Modular, Extensible and Fast Kolmogorov-Arnold Network Implementation"
title_zh: KANLib —— 一个模块化、可扩展且快速的Kolmogorov-Arnold网络实现
authors: "Julian Hoever, Gregor Schiele"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.17927v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 为KAN架构提供了一个模块化、可扩展且高效的软件框架，类似于深度学习框架设计
tldr: Kolmogorov-Arnold网络虽具解释性与表达力，但高计算成本与框架支持缺陷阻碍实用研究。本文提出KANLib，统一PyKAN、EfficientKAN、FastKAN等实现，提供模块化、高效架构，支持多种基函数与网格调整，兼容PyTorch。在加州房价基准测试中，KANLib精确重现参考行为，计算效率具竞争力，且架构变体几乎不影响性能。这为可扩展KAN的后续研究打造了坚实框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17927v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 906, \"height\": 453, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.17927v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1215, \"height\": 224, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17927v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1418, \"height\": 322, \"label\": \"Table\"}]"
motivation: 解决KANs高计算成本与现有框架特征支持不一致的问题。
method: KANLib整合多种KAN实现，提供模块化架构、双基函数、自适应网格缩放与扩展，兼容PyTorch。
result: 在加州房价数据集上，KANLib重现参考实现行为，效率接近最优，架构变体对预测性能影响轻微。
conclusion: 为可扩展KAN架构的未来研究提供了灵活、高效的实验平台与坚实基础。
---

## 摘要
Kolmogorov-Arnold网络（KAN）近期作为传统多层感知器的一种有前景的替代方案而出现，其用可学习的单变量函数替代了线性权重。尽管在可解释性和表达能力方面具有理论优势，但由于计算成本高且现有框架之间功能支持不一致，KAN的实际研究仍然困难重重。本文介绍KANLib，一个模块化、可扩展且计算高效的框架，用于开发和评估KAN架构。KANLib将现有实现（包括PyKAN、EfficientKAN和FastKAN）的核心概念统一到一致的软件架构中，强调灵活性、功能对等和高性能。该框架支持两种基函数类型、自适应网格缩放、网格扩展和细粒度架构定制，同时保持与标准PyTorch工作流的兼容性。在加州房价基准上的实验评估表明，KANLib再现了既定参考KAN实现的预测行为，同时实现了具有竞争力的计算效率。此外，该框架能够探索超出标准KAN形式的架构变体，而仅对预测性能产生轻微影响。总体而言，KANLib为未来可伸缩且可扩展的KAN架构研究提供了坚实的基础。

## Abstract
Kolmogorov-Arnold Networks (KANs) have recently emerged as a promising alternative to traditional multilayer perceptrons by replacing linear weights with learnable univariate functions. Despite their theoretical advantages in interpretability and expressiveness, practical research of KANs remains difficult due to high computational costs and inconsistent feature support across existing frameworks. This paper introduces KANLib, a modular, extensible, and computationally efficient framework for developing and evaluating KAN architectures. KANLib unifies core concepts from existing implementations, including PyKAN, EfficientKAN, and FastKAN, within a consistent software architecture that emphasizes flexibility, feature parity, and high performance. The framework supports two basis function types, adaptive grid rescaling, grid extension, and fine-grained architectural customization while maintaining compatibility with standard PyTorch workflows. Experimental evaluation on the California Housing benchmark demonstrates that KANLib reproduces the predictive behavior of established reference KAN implementations while achieving competitive computational efficiency. Furthermore, the framework enables the exploration of architectural variations beyond standard KAN formulations with only minor impacts on predictive performance. Overall, KANLib provides a robust foundation for future research on scalable and extensible KAN architectures.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- Kolmogorov-Arnold网络（KAN）用可学习的单变量样条函数替代MLP中的线性权重，在可解释性和表达能力上具有理论优势。
- 实际研究中，KAN面临两大障碍：计算成本高昂，以及现有框架（如PyKAN、EfficientKAN、FastKAN）之间功能支持零散且不一致。
- **研究动机**：亟需一个统一、高效、模块化的KAN实现框架，以降低实验门槛并加速架构探索。

### 2. 方法论：KANLib框架的核心思想与关键技术
- **统一架构**：将PyKAN、EfficientKAN、FastKAN的核心创新整合到一致的软件设计中，强调模块化、可扩展性与高性能。
- **基函数双支持**：
  - **B样条层**：采用与PyKAN一致的残差分支结构（SiLU激活）与样条加权组合 `φ(x) = w_b·b(x) + w_s·spline(x)`，同时融入EfficientKAN的高效基函数求值与线性层合并技术。
  - **高斯径向基函数（GRBF）层**：用 `exp(-||x - x_i||²/(2σ²))` 替代B样条，避免递归计算，并设计为兼容网格操作。
- **网格操作支持**：
  - 自适应网格缩放：针对B样条和GRBF等距网格动态调整网格边界与分布。
  - 网格扩展：在训练中增加样条分辨率而不丢失已学习信息，两种基函数均支持。
- **细粒度架构定制**：允许独立禁用残差分支、样条权重，或同时禁用两者（Plain模式）；可选层归一化将输入映射到基函数有效区间。
- **PyTorch兼容性**：所有层均可作为标准PyTorch模块，方便构建混合模型或MLP型KAN。

### 3. 实验设计
- **数据集**：加州房价数据集（California Housing），20,640样本，8个数值特征，预测房价中位数，用于回归任务。
- **对比基准**：4个参考框架（PyKAN、EfficientKAN、FastKAN）与KANLib的两个变体（B样条、GRBF）。
- **评估指标**：验证集RMSE、R²分数、可训练参数量、CPU推理时间（1000样本平均）。
- **配置**：统一采用两层KAN、隐藏维度30、样条阶数3、网格点数10、网格范围[-1,1]；禁用自适应网格缩放、网格扩展和层归一化，以孤立实现差异。训练300 epoch，Adam优化器，学习率1e-3，批次大小1024，MSE损失，20次随机初始化取平均与标准差。

### 4. 资源与算力
- 文中明确提到推理时间测量使用 **AMD Ryzen 9 9950X CPU**。
- 未提及GPU型号、数量或训练时长，仅报告了推理耗时。训练算力资源未明确说明，这是实验描述的一个缺失。

### 5. 实验数量与充分性
- **实验组数**：
  - 主对比实验：5个框架（FastKAN、KANLib-GRBF、PyKAN、EfficientKAN、KANLib-B-spline），每组20次重复，共100次训练。
  - 消融实验：KANLib在B样条和GRBF两种基函数下，分别测试4种架构配置（Default、无残差、无样条权重、Plain），每组20次重复，共160次训练。
- **充分性**：实验覆盖了主流参考实现与框架自身变体，随机重复次数充足，使用标准回归基准，比较公平。但仅限于单个小型表格数据集，未涉及分类、图像或时序任务，且未对比近期其他KAN变体（如FourierKAN、fKAN等），泛化证据有限。

### 6. 主要结论与发现
- **预测一致性**：KANLib的两种基函数均能精确复现对应参考实现的行为，RMSE差异小于1.7%，且绝对值最低（B样条RMSE 0.5376，R² 0.7852）。
- **计算效率**：
  - B样条：KANLib与EfficientKAN推理耗时相近（~142 ms），比PyKAN快约32.7%。
  - GRBF：KANLib比PyKAN快62%，但比FastKAN（约41 ms）仍慢近一倍，主要因支持网格缩放引入了额外开销。
- **架构弹性**：移除残差分支或样条权重对预测性能影响轻微（RMSE波动约0.6%~3.3%），而在B样条下推理时间最多可缩短10%，在GRBF下最多缩短16.8%，证明KANLib能灵活探索轻量KAN结构。

### 7. 优点
- **高度模块化与统一接口**：在同一框架内同时支持B样条与GRBF，实现功能对等，极大简化对比实验。
- **细粒度的架构控制**：允许按需组合残差、样条权重、层归一化，便于研究各组件贡献。
- **兼顾效率与功能**：继承EfficientKAN等优化，同时保留网格扩展、自适应缩放等高级特性。
- **开箱即用的 PyTorch 集成**：可直接插入任意PyTorch模型，降低使用门槛。
- 代码开源，为KAN研究提供了可复现的公共基础。

### 8. 不足与局限
- **实验局限**：仅在一个小规模回归数据集上测试，缺乏图像、文本、时间序列等任务的评估，外部效度不足。
- **GRBF推理效率差距**：尽管支持网格缩放，但当前实现比FastKAN慢约96%，说明GRBF内核优化不彻底，可能影响大规模应用。
- **对比范围偏窄**：未与FourierKAN、fKAN、WavKAN等非样条基函数实现比较，也未与卷积KAN等其他架构对比。
- **缺乏训练速度对比**：仅给出推理时间，未报告每个epoch训练耗时或总训练时间。
- **网格缩放仅支持等距网格**：对于GRBF的非等距网格限制，可能削弱自适应缩放的实际收益。
- **未涉及可解释性评估**：尽管KAN以可解释性著称，但论文未量化或展示框架在此方面的优势。

（完）
