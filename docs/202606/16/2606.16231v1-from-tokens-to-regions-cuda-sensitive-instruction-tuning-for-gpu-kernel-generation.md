---
title: "From Tokens to Regions: CUDA-Sensitive Instruction Tuning for GPU Kernel Generation"
title_zh: 从令牌到区域：面向GPU内核生成的CUDA敏感指令微调
authors: "Wentao Chen, Jiace Zhu, Xing Zhe Chai, Zeng Qu, Qiaoling Xiao, Liucheng Duan, An Zou"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16231v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 提出CUDA敏感指令微调，改进基于LLM的GPU内核生成，是AI基础设施的关键任务。
tldr: 大语言模型生成CUDA内核时难以遵守隐含执行约束，现有方法或依赖昂贵的强化学习管道，或采用未显式建模代码敏感性的监督微调。本文从令牌置信度视角揭示CUDA敏感性分布：多数敏感令牌预测置信度高，少数低置信令牌构成执行关键区域。这些发现表明需同时利用高置信敏感令牌并保留低置信敏感区域。据此提出CuSeT方法，遵循“从令牌到区域”，结合自适应令牌掩码与区域感知重加权进行低成本微调。实验显示CuSeT在多个模型上提升功能正确性，优于多种SFT变体，以更低推理成本达到前沿水平。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16231v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 720, \"height\": 533, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16231v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1451, \"height\": 1195, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16231v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1372, \"height\": 981, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16231v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1439, \"height\": 431, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16231v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1470, \"height\": 784, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16231v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1476, \"height\": 840, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.16231v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1396, \"height\": 373, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16231v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1269, \"height\": 903, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16231v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1438, \"height\": 615, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16231v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1106, \"height\": 372, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16231v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1432, \"height\": 320, \"label\": \"Table\"}]"
motivation: LLM生成CUDA内核时忽略代码令牌与区域的执行敏感性，现有SFT未显式建模CUDA敏感模式，导致正确率低。
method: 提出CuSeT，通过令牌置信度分析发现敏感模式，结合自适应令牌掩码和区域感知样本重加权，实现低成本后训练。
result: CuSeT在多个模型家族和规模上持续提升功能正确性，超过标准SFT与高级变体，以更低推理成本媲美前沿内核生成模型。
conclusion: 揭示CUDA内核生成中从令牌到区域的敏感性规律，提出高效CuSeT方法，显著提升正确性并降低推理开销。
---

## 摘要
高性能CUDA内核对于可扩展的AI系统至关重要，但大型语言模型（LLMs）仍因严格且隐式的执行约束而难以生成正确的内核。现有基于LLM的方法要么依赖昂贵的代理式或强化学习（RL）流水线，要么采用监督微调（SFT）目标，这些目标未能显式建模CUDA敏感性，即与执行约束紧密耦合的代码令牌或区域。本文从令牌置信度模式的角度研究CUDA敏感性，发现CUDA敏感性同时体现在令牌和区域两个层面：多数CUDA敏感令牌被高置信度预测，而少数低置信度子集则形成对应执行关键结构的区域。这些发现表明，有效的CUDA内核生成应同时利用高置信度的CUDA敏感令牌并保留低置信度的CUDA敏感区域。基于此，我们提出**CUDA敏感指令微调（CuSeT）**，这是一种在简单SFT框架下的低成本后训练方法。CuSeT遵循“从令牌到区域”的原则，结合了*自适应令牌级掩码*与*区域感知样本重加权*。实验显示，CuSeT在多个模型家族与规模上持续提升功能正确性，优于标准SFT及先进SFT变体，并在推理成本显著更低的情况下，取得与前沿CUDA内核生成模型相比具有竞争力的性能。

## Abstract
High-performance CUDA kernels are essential for scalable AI systems, while Large Language Models (LLMs) still struggle to generate correct kernels due to strict and implicit execution constraints. Existing LLM-based approaches either rely on costly agentic or reinforcement-learning (RL) pipelines, or adopt supervised fine-tuning (SFT) objectives that fail to explicitly model CUDA sensitivity, namely code tokens or regions tightly coupled with execution constraints. In this work, we investigate CUDA sensitivity from the perspective of token confidence patterns, showing that CUDA sensitivity appears at both token and region levels, where most CUDA-sensitive tokens are predicted with high confidence, while a smaller low-confidence subset forms regions corresponding to execution-critical structures. These findings suggest that effective CUDA kernel generation should both leverage high-confidence CUDA-sensitive tokens and preserve low-confidence CUDA-sensitive regions. Building on these insights, we propose \textbf{\underline{CU}DA-\underline{Se}nsitive Instruction \underline{T}uning (CuSeT)}, a low-cost post-training method within a simple SFT framework. CuSeT follows the principle of ``from tokens to regions'' by combining \emph{adaptive token-level masking} with \emph{region-aware sample reweighting}. Experiments show that CuSeT consistently improves functional correctness across multiple model families and scales, outperforming standard SFT and advanced SFT variants, while achieving competitive performance against frontier CUDA kernel generation models with substantially lower inference cost.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容撰写的详细中文总结。

### 论文的核心问题与整体含义

*   **核心问题**：大型语言模型在自动生成高性能GPU内核时，常常因无法满足CUDA编程中严苛且隐式的执行约束（如内存访问模式、线程索引同步等）而失败。现有方法要么依赖成本高昂的代理式或强化学习管道，要么采用标准的监督微调，但标准SFT对所有目标令牌施加统一的监督，未能显式建模代码中与执行约束高度相关的**CUDA敏感性**。
*   **整体含义**：本文旨在揭示CUDA代码生成中模型预测置信度的内在规律，并提出一种低成本、高效的SFT方法，通过强化对“CUDA敏感”令牌和区域的监督，显著提升LLM生成内核的功能正确性。

### 论文提出的方法论

论文的核心方法是**CUDA敏感指令微调（CuSeT）**，其核心思想是“从令牌到区域”，包含两个互补的关键技术：

*   **核心思想与发现**：
    *   **CUDA敏感性**：指代码令牌或区域的正确性与CUDA执行约束紧密耦合的性质。
    *   **令牌置信度分析**：
        *   **令牌级**：多数CUDA敏感令牌被模型以高置信度预测，可作为可靠的监督信号；而CUDA中性令牌更分散，低置信度部分噪声较多。
        *   **区域级**：少数低置信度的CUDA敏感令牌并非随机分布，而是聚集成区域，这些区域恰好对应共享内存分配、启动配置等执行关键结构。
    *   **结论**：有效的监督应同时利用高置信度的敏感令牌，并保留低置信度的敏感区域，以学习困难的CUDA模式。

*   **关键技术细节**：
    1.  **自适应令牌级掩码**：
        *   **置信度掩码**：根据模型对每个目标令牌的预测概率（置信度）\( c_{n,t} \) 生成一个二值掩码 \( M^{\text{conf}}_{n,t} \)，设定阈值 \( \tau \)，只对高置信度令牌进行监督，过滤低置信度的噪声令牌。
        *   **CUDA掩码**：为保留对低置信度但位于执行关键位置（如内核体、启动配置）的令牌的监督，引入一个CUDA掩码 \( M^{\text{CUDA}}_{n,t} \)，为这些位置的令牌赋予一个基础权重 \( \lambda \)。
        *   **混合掩码**：将上述两个掩码相加 \( (M^{\text{hybrid}}_{n,t} = M^{\text{conf}}_{n,t} + M^{\text{CUDA}}_{n,t}) \)，实现既保留高置信令牌，又保护低置信关键令牌，同时抑制低置信中性令牌的监督效果。
    
    2.  **区域感知样本重加权**：
        *   使用一个大小为 \( l \) 的滑动窗口在令牌序列上滑动，计算每个窗口内的平均令牌置信度 \( C_{n,t} \)。
        *   为每个训练样本找到其内部置信度最低的窗口，该窗口被视作包含执行关键结构的“最困难区域”。
        *   样本的最终权重 \( w_n \) 由该最低窗口置信度决定 \( (w_n = 1 - \min_{t} C_{n,t}) \)，置信度越低，样本权重越高，从而让模型更专注于学习包含复杂CUDA结构的样本。

*   **训练目标**：最终的损失函数将令牌级混合掩码与样本级权重相结合 \( L_{\text{CuSeT}} \)，实现从令牌到区域的分层CUDA敏感性监督。

### 实验设计

*   **数据集/场景**：
    *   **训练数据**：从GitHub、HuggingFace、基准数据集（如PyTorch模块）等来源收集CUDA内核，并通过LLM生成测试用例，经GPU上编译和功能正确性验证后，筛选出**6,278个**高质量提示-响应对。
    *   **评估基准**：使用 **CUDA Bench Level 1** 基准来评估内核生成效果。

*   **对比方法**：
    *   **SFT变体**：标准SFT，以及多种改进的SFT方法（DFT, EAFT, ASFT, ProFit）。
    *   **前沿模型**：包括通用和代码大模型（GLM-5.1, DeepSeek-V3.2-Thinking, Qwen3-Coder-Next）以及CUDA专用模型（Kevin, KernelCoder）。

*   **评估指标**：
    *   **编译成功率**
    *   **功能正确性**：使用 **Pass@k** (k=1, 3) 评估生成正确内核的概率。

### 资源与算力

*   **训练硬件**：所有微调实验均在 **8块 NVIDIA RTX 3090 Ti GPU** 上进行。
*   **训练详情**：论文未明确提及时长，但提及使用LoRA技术微调，超参数为秩 \( r=8 \)，\( \alpha=16 \)，学习率 \( 1\times 10^{-4} \)，训练3个epoch。这表明训练的成本和资源需求相对较低。

### 实验数量与充分性

*   **实验组数**：论文进行了多组实验，包括：
    *   **主实验**：在3个不同模型家族（Qwen2.5-Coder, DeepSeek-Coder, Seed-Coder）上，对比1个Vanilla基线和5种SFT方法。
    *   **与前沿模型对比实验**：将CuSeT微调的5个不同规模模型与5个前沿模型对比。
    *   **可扩展性实验**：在1个4B和1个32B模型上验证。
    *   **消融实验**：包含1个基线+4个组件组合的对比，以及针对3个关键超参数的敏感性分析（每种超参数取4-5个值）。
*   **充分性与客观性**：实验设计**比较充分和客观**。
    *   **多模型验证**：覆盖了多个模型家族和规模，证明了方法的泛化性。
    *   **多基线对比**：对比了标准SFT、多种同类改进SFT，甚至对比了参数量远超自己的前沿大模型，层次丰富。
    *   **详细消融**：通过严谨的消融实验证明了方法中各组件的有效性，并对关键超参数进行了敏感性分析，增强了结论的可信度。

### 论文的主要结论与发现

1.  提出并验证了**CUDA敏感性**概念，揭示了其在模型预测中表现为令牌级高/低置信度分布差异和区域级低置信结构聚集的现象。
2.  基于此提出的**CuSeT方法**，能通过低成本的后训练，显著且稳定地提升LLM生成CUDA内核的**功能正确性**。
3.  CuSeT性能**超越**了标准SFT和多种先进的SFT变体。
4.  在**推理成本显著更低**（平均生成令牌数少）的情况下，CuSeT微调的小模型可以取得与大规模前沿模型（如685B的DeepSeek-V3.2-Thinking）和CUDA专用模型相比**较具竞争力**的性能，实现了性能与效率的较优平衡。

### 优点

*   **问题洞察深入**：从模型内部置信度出发，原创性地揭示了CUDA代码生成的“令牌-区域”双层敏感性规律，为后续研究提供了新视角。
*   **方法透明高效**：CuSeT完全在SFT框架内，通过分析模型自身的预测和代码结构实现，避免了复杂的强化学习或代理系统，训练和推理成本都很低。
*   **实验扎实全面**：在多个模型、多种基线上进行了大量实验验证，并结合消融研究和超参数分析，结论可信度高。
*   **效果与效率兼顾**：不仅提升了正确性，还通过降低推理长度实现了高效生成，与实际应用结合紧密。

### 不足与局限

*   **数据集依赖**：训练和评估数据主要基于现有公开库和基准，其覆盖的CUDA内核类型和复杂性可能有限，模型在真实世界、更广泛或更复杂的优化任务上的泛化能力有待进一步检验。
*   **基准局限性**：仅在CUDA Bench Level 1上评估，该基准可能侧重于特定类型的任务或未能充分反映极高性能优化场景下的挑战。
*   **对比基线**：与前沿模型的对比中，部分模型（如GLM-5.1）可能未针对CUDA生成进行专门优化，直接比较其原始表现可能不完全公平，但这种对比依然能说明效率优势。
*   **领域偏差风险**：CUDA掩码的定义依赖于“内核体/启动配置”等先验知识判断，尽管在CUDA中很通用，但在其他领域编程语言中可能不具备这种明确的代码位置敏感性，限制了方法的直接迁移。

（完）
