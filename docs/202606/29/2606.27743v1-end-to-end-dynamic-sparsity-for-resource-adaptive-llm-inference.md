---
title: End-to-End Dynamic Sparsity for Resource-Adaptive LLM Inference
title_zh: 面向资源自适应LLM推理的端到端动态稀疏化
authors: "Yuhang Chen, Jinhao Duan, Ruichen Zhang, Mingfu Liang, Xiaohan Wei, Yunchen Pu, Fei Tian, Chonglin Sun, Parish Aggarwal, Frank Shyu, Luke Simon, Sandeep Pandey, Tianlong Chen, Xi Liu"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.27743v1"
tags: ["query:mlsys"]
score: 9.0
evidence: LLM推理动态稀疏化以适应云资源波动
tldr: "当前LLM推理部署于静态资源假设，难以适应云环境动态波动。L2A框架将推理构建为预算与输入感知的约束分配问题，通过轻量门控网络联合优化性能与资源成本。在Llama-3-8B和Qwen-3-4B上，单一模型在34%层稀疏下GSM8K精度仅降0.6%，且零样本迁移稳定。该方法首次实现端到端资源自适应，无需多模型调优即可覆盖计算-精度前沿。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27743v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 767, \"height\": 773, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27743v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1648, \"height\": 563, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27743v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1714, \"height\": 413, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27743v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1529, \"height\": 498, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27743v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1562, \"height\": 224, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27743v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1721, \"height\": 369, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27743v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1721, \"height\": 362, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27743v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1716, \"height\": 430, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27743v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1724, \"height\": 324, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27743v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1721, \"height\": 367, \"label\": \"Table\"}]"
motivation: 真实云基础设施资源动态波动，静态LLM推理或崩溃或浪费计算，缺乏灵活性。
method: 提出L2A框架，将推理形式化为预算约束分配问题，通过轻量门控网络联合优化，实现层跳过、头修剪和推理令牌缩减。
result: "在Llama-3-8B和Qwen-3-4B上，L2A模型达34%层稀疏时GSM8K精度仅降0.6%，分布外零样本表现同样稳定，优于需单独调优的静态基线。"
conclusion: 单一L2A模型可覆盖计算-精度帕累托前沿，有效适应动态资源，为资源自适应LLM推理提供端到端解决方案。
---

## 摘要
大型语言模型（LLM）的推理通常基于静态资源假设部署，模型执行固定的计算图而不考虑运行时环境。然而，现实世界的云基础设施具有固有的动态性，表现为可用性波动（例如，竞价实例抢占）和分层服务质量要求。在这种不稳定的环境中，静态模型缺乏灵活性：它们在资源受限时可能崩溃，或在冗余操作上浪费计算资源。为了弥补这一差距，我们提出了学习分配（L2A）框架，这是一种端到端的资源自适应推理方法。与以往仅以输入难度为条件的方法不同，我们将推理问题形式化为一个以输入和运行时资源预算本身为条件的约束分配问题。我们引入了集成到LLM中的轻量级、预算条件化和输入感知的门控网络。这些门控通过统一的优化目标进行训练，该目标同时优化任务性能、逻辑一致性和资源成本，沿着三个反映现实世界动态表现的维度：层跳过以应对内存和深度压力，头剪枝以应对吞吐量争用，以及推理令牌减少以收紧延迟。这使得模型能够学习超越单纯输入难度的预算感知策略：它根据实时资源动态自适应地配置其计算足迹，在资源允许时最大化推理深度，同时在预算紧张时强制执行严格的节约。单个L2A模型在Llama-3-8B和Qwen-3-4B上描绘了完整的计算-准确率帕累托前沿：在高达34%的实际层稀疏度下，它在GSM8K上保持在稠密基线的0.6%以内，并在分布外任务的零样本推理中保持相同差距，而每个静态或启发式基线都需要单独调优的模型，并且在可比推理时间下仍然下降5-10%。

## Abstract
Large Language Models (LLMs) inference is typically deployed under a static resource assumption, where models execute a fixed computational graph regardless of the runtime environment. However, real-world cloud infrastructure is inherently dynamic, characterized by fluctuating availability (e.g., spot instance preemption) and tiered Quality-of-Service requirements. In such volatile settings, static models are inflexible: they either crash under resource constraints or waste compute on redundant operations. To bridge this gap, we propose Learning to Allocate (L2A), an end-to-end framework for resource-adaptive inference. Unlike prior methods that condition only on input difficulty, we formulate inference as a constrained allocation problem conditioned on both the input and the runtime resource budget itself. We introduce lightweight, budget-conditioned and input-aware gating networks integrated into the LLM. These gates are trained via a unified objective that jointly optimizes task performance, logical consistency, and resource costs along three axes matching how real-world dynamics manifest: layer skipping for memory and depth pressure, head pruning for throughput contention, and reasoning-token reduction for latency tightening. This lets the model learn a budget-aware policy beyond input difficulty alone: it adaptively configures its computational footprint with respect to real-time resource dynamics, maximizing reasoning depth when resources permit while enforcing strict frugality when budgets tighten. A single L2A model traces the entire compute-accuracy Pareto frontier on Llama-3-8B and Qwen-3-4B: at up to 34% realized layer sparsity, it stays within 0.6% of the dense baseline on GSM8K, with the same gap holding zero-shot on out-of-distribution tasks, while every static or heuristic baseline requires a separately tuned model and still drops by 5-10% at comparable inference time.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **核心矛盾**：大语言模型推理通常基于“静态资源假设”，即模型执行固定的全量计算图，但真实云环境（如竞价实例抢占、多租户争抢、服务等级区分）中GPU内存、延迟和吞吐量是波动的。
- **研究动机**：在资源突然收紧（如实例被回收前2分钟）或存在差异化QoS需求（如付费/免费用户）时，静态模型要么因显存溢出崩溃，要么在简单任务上浪费算力，缺乏弹性退化能力。
- **整体含义**：提出将推理重新定义为“以输入特征和实时资源预算为双重条件的约束分配问题”，让模型学会根据当前可用算力动态调整计算量，从而在紧张时极力节省资源，宽裕时保留深度推理能力。

### 2. 方法论
- **核心框架**：**L2A（Learning to Allocate）**，一种端到端的资源自适应推理框架。
- **关键技术细节**：
  - **预算条件化门控网络**：向冻结的LLM中注入三种轻量门控MLP：
    1. **层跳过门**（αₗ）：基于当前隐藏状态和预算嵌入，决定是否执行第l层。
    2. **头剪枝门**（βₗ,ₕ）：决定是否抑制第l层第h个注意力头。
    3. **推理令牌缩减门**（τₜ）：在生成阶段判断是否强制输出`</think><answer>`边界标记，提前结束推理段以降低延迟。
  - **预算信号**：将归一化标量预算b∈[0,1]映射为嵌入e(b)，在训练时从分布D中采样，推理时由剩余时间、显存水位、队列长度等实时系统信号校准得到。
  - **训练方式**：联合优化LoRA参数（适应稀疏路径）和所有门控参数，采用统一损失：
    ```
    L(b) = L_ce + γ·L_kd + λ·(C(b) − B(b)) + λ_T·R_tok
    ```
    - `L_ce`：下一代词交叉熵，保证生成质量。
    - `L_kd`：冻结稠密教师的KL蒸馏，防止门控过于激进时逻辑崩坏。
    - `C(b) − B(b)`：期望计算量（基于层/头激活值）与目标预算的偏差惩罚，驱动稀疏化。
    - `R_tok`：推理段预期长度惩罚，推动门控提前结束思考阶段。
  - **推理时**：αₗ、βₗ,ₕ、τₜ均通过阈值（如0.5）二值化为硬决策，层被物理跳过，头被丢弃，推理段提前终止，实现实际加速与内存节省。

### 3. 实验设计
- **基座模型**：Llama-3-8B、Qwen-3-4B。
- **数据集/场景**：
  - 分布内（ID）训练/校准：OpenWebText（通用语言建模）、GSM8K（数学推理）、MMLU（知识问答）、Alpaca-Eval（指令跟随）。
  - 分布外（OOD）泛化测试：HumanEval（代码生成）、Big-Bench Hard（符号推理）。
- **对比方法**：
  - 原始稠密模型。
  - 静态剪枝：均匀层跳过（Uniform pruning）、基于离线校准的Oracle静态剪枝（Oracle Static Pruning）。
  - 自适应启发式：AdaSkip（基于熵的早期退出）、FlexiDepth（可学习适配器+辅助跳过损失）。
- **评价指标**：OpenWebText困惑度，各下游任务准确率/得分/Pass@1，实现的平均层/头稀疏度，峰值GPU内存，总推理耗时。

### 4. 资源与算力
- 论文未明确说明训练所用的GPU型号、卡数、训练时长以及推理吞吐等具体硬件细节，仅提及使用相同评估管线保证公平性。

### 5. 实验数量与充分性
- **实验数量**：包含6张表格（主对比、固定预算对比、跨域泛化、消融实验）及4张诊断图，覆盖两大模型家族、6个以上基准、两类资源约束模式。
- **充分性评估**：
  - 通过对比静态、Oracle静态、现行动态启发式等多种基线，多维度验证了方法优势。
  - 设置了固定预算评测（Table 4）以验证动态分配比均匀压缩更有效。
  - 进行了Super-matrix跨域泛化实验（Table 5），系统性检验策略是否过拟合单一数据域。
  - 消融实验（Table 6）逐一剔除KD、预算损失、令牌长度损失、头门控，证明了每个组件的必要性。
  - 训练收敛、门控稳定性、预算响应等均有可视化诊断（Figure 3、4）。
- **客观公平性**：所有方法在同批次、同解码、同硬件条件下评估，且各基线都调至与L2A相当的平均时间/稀疏度进行比较。

### 6. 主要结论与发现
- **性能-效率帕累托最优**：在Llama-3-8B上，L2A可实现34%层稀疏，而GSM8K准确率仅比稠密基线降低约0.6%（35.9% vs 36.5%）；在Qwen-3-4B上同样保持极小性能差距，远优于静态剪枝（下降5-10%）和现有自适应方法。
- **动态预算适应能力**：模型能实时感知预算变化并相应调整层激活率，而静态模型在相同平均计算量下对困难样本会欠计算，导致推理退化。
- **跨域泛化性**：在混合数据上训练的门控策略可迁移至未见的OOD任务（代码生成、符号推理），而仅在单一域训练的策略会出现过度剪枝或剪枝不足的极端行为。
- **组件贡献**：KD损失对防止推理崩坏至关重要；不带预算损失则模型退化为稠密；联合层+头+推理段控制比纯层跳过实现更优的精度-延迟平衡。

### 7. 优点
- **端到端自适应**：首次将实时资源预算作为模型推理的条件变量，单一模型即可覆盖从极度节省到满血的连续谱系，无需为每个预算级别单独训练模型。
- **多维控制粒度**：并行控制深度（层）、宽度（头）和推理长度，映射了内存、吞吐、延迟三类实际瓶颈，解耦设计使加速更贴近硬件特性。
- **鲁棒性与泛化**：通过联合优化与蒸馏，在极具挑战的推理任务和域迁移上维持稳定性，未出现突然崩溃或过度保守。
- **部署友好**：推理阶段仅需简单的阈值硬决策，并在通用Transformer架构上实现，利于集成到现有推理引擎。

### 8. 不足与局限
- **硬件实现依赖性**：头剪枝的实际加速收益高度依赖底层库对稀疏MHA的优化，当前报告的速度提升主要来源为层跳过和推理缩减。
- **校准精度要求**：推理时预算校准依赖于对稠密模型延迟/显存的合理估计，估计偏差可能导致预算设置过于保守或激进。
- **格式依赖**：推理段缩减依托于`<think>/<answer>`标签结构，强制规定生成范式，可能不适用于所有对话或代码生成场景。
- **实验范围**：未报告训练计算开销和门控网络本身的显存/时延开销，也未在更大规模（>8B）或不同架构（非LLaMA/Qwen）上验证。
- **安全临界性**：自动根据预算降低计算量可能导致简单任务被误判而过度压缩，或困难任务因高压预算被提前截断，论文未深入分析错误案例与安全边界。

（完）
