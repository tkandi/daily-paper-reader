---
title: "FedMTFI: Feature Importance Based Optimized Multi Teacher Knowledge Distillation in Heterogeneous Federated Learning Environment"
title_zh: FedMTFI：异构联邦学习环境中基于特征重要性的优化多教师知识蒸馏
authors: "Nazmus Shakib Shadin, Aaron Cummings, Xinyue Zhang, Bobin Deng"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.01607v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 结合多教师知识蒸馏与特征重要性以提升异构联邦学习性能
tldr: 联邦学习在实际应用中常面临客户端数据非独立同分布和硬件资源差异大的问题，导致模型性能下降。为解决这一挑战，本文提出FedMTFI，一种融合特征重要性的多教师知识蒸馏框架。该方法首先基于硬件相似性和模型类型对客户端聚类，各客户端在本地数据上训练个性化模型，服务器聚合得到多个原型教师。然后，通过多教师蒸馏将知识迁移到全局学生模型，并利用SHAP值对输入特征的重要性进行加权，强化关键特征的学习。实验结果表明，FedMTFI在非独立同分布数据下比传统联邦学习算法取得了更高的准确率，同时增强了模型的可解释性，为异构联邦学习的有效实现提供了新的思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01607v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1855, \"height\": 471, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01607v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 861, \"height\": 611, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01607v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 861, \"height\": 612, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01607v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 908, \"height\": 555, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01607v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 907, \"height\": 554, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.01607v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 951, \"height\": 1604, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01607v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 850, \"height\": 262, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01607v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 847, \"height\": 263, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01607v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 960, \"height\": 186, \"label\": \"Table\"}]"
motivation: 在异构联邦学习中，设备能力和数据分布的差异导致传统方法难以保证全局模型性能。
method: 提出FedMTFI，通过聚类、多教师知识蒸馏和SHAP赋予的特征重要性，训练泛化性强且可解释的全局模型。
result: 在非IID数据上实验表明，FedMTFI准确率优于传统联邦学习算法，并提升模型可解释性。
conclusion: FedMTFI显著提升了非IID数据下联邦学习的准确率和可解释性，为异构环境中的隐私保护协同学习提供了有效且实用的方法。
---

## 摘要
联邦学习（FL）是一种去中心化方法，能够在保护原始数据的前提下协作训练模型。它不传输敏感数据，而仅共享模型权重，将个人数据保留在本地并确保安全。然而，在现实场景中，设备持有的数据往往分布不均，且设备在计算能力和内存容量方面大多存在差异。这些差异使得联邦学习难以在整个系统中保持稳定的性能。为解决这些问题，我们提出 FedMTFI，这是一种新颖的架构，结合了多教师知识蒸馏（MTKD）和特征重要性，以改进异构环境下的联邦学习过程。在 FedMTFI 中，客户端根据相似的硬件和模型类型进行聚类。每个集群在非独立同分布（non-IID）数据上训练特定的模型。在集群内部，每个客户端仅使用本地私有数据更新该模型。服务器随后使用 FedAvg 聚合每个集群中本地训练的模型，形成多个原型模型。然后，这些原型模型作为教师模型，通过 MTKD 训练一个全局泛化的学生模型。FedMTFI 更独特之处在于整合了 Shapley 值（SHAP），在蒸馏过程中强调重要特征，从而提高了准确性和可解释性。实验结果表明，FedMTFI 比传统联邦学习算法具有更高的准确率，并且在非独立同分布数据条件下表现更为有效。

## Abstract
Federated learning (FL) is a decentralized approach that enables collaborative model training without exposing raw data. Instead of transferring sensitive data, it allows devices to share only model weights, keeping personal data locally and secure. However, in real world settings, the data held by devices is often not evenly distributed and devices mostly differ in computing power and memory capacity. These differences make FL harder to maintain consistent performance across the system. To address these issues, we propose FedMTFI, a novel architecture that combines multi-teacher knowledge distillation (MTKD) with feature importance to improve the FL process in heterogeneous environments. In FedMTFI, clients are clustered based on similar hardware and model types. Each cluster trains a specific model on not independently and identically distributed (non-IID) data. Within a cluster, every client updates that model using only its own local private data. The server then aggregates the locally trained models in each cluster using FedAvg to form multiple prototype models. Then these prototypes serve as teacher models to train a global generalized student model using MTKD. What makes FedMTFI more unique is the integration of Shapley values (SHAP) to emphasize important features during distillation, which enhances both accuracy and interpretability. Experimental results show that FedMTFI achieves higher accuracy than traditional FL algorithms and performs more effectively under non-IID data conditions.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

-   **研究背景**：联邦学习 (FL) 作为一种隐私保护的去中心化训练范式，允许设备仅共享模型更新而非原始数据。然而，在实际部署中，面临严峻的“异构性”挑战。
-   **核心问题**：
    -   **设备异构**：不同的客户端设备（如树莓派、智能手机、笔记本电脑）计算能力、内存和电池寿命差异巨大，难以用统一的全局模型。
    -   **数据异构**：客户端持有的本地数据通常是**非独立同分布**的，这种统计上的偏差会严重损害传统聚合算法（如 FedAvg）训练出的全局模型性能。
-   **整体含义**：论文提出 FedMTFI 架构，旨在正面应对而非消除异构性。其核心思想是通过**聚类化聚合、多教师知识蒸馏与特征重要性加权**三者结合，在异构环境中训练出一个高性能、强泛化且具备一定可解释性的全局模型。

### 2. 论文提出的方法论

-   **核心思想**：
    -   **分组异构**：将硬件和模型结构相似的客户端聚成一类，每类内部使用同质化模型。
    -   **教师蒸馏**：服务器将各类聚合后的模型作为“多教师”，通过知识蒸馏技术，将它们的知识迁移到一个统一的“学生模型”中。
    -   **特征加权的知识迁移**：利用 Shapley 值量化每个输入特征的重要性，并以此权重调整蒸馏损失，让学生模型在训练中更关注关键特征。
-   **关键技术细节与工作流程**：
    1.  **客户端操作**：
        -   客户端基于 Dirichlet 分布（由参数 \(\alpha\) 控制非 IID 程度）获取私有的非 IID 数据。
        -   每个客户端在本地使用交叉熵损失进行常规的监督训练，并将更新后的模型权重和评估指标发送回服务器。（见算法 1）
    2.  **服务器端操作**（核心环节，见算法 2）：
        -   **聚类聚合**：服务器将来自同一集群的客户端模型使用 **FedAvg** 算法进行聚合，生成多个“原型教师模型” \(P_1, P_2, ..., P_k\)。
        -   **计算特征重要性**：为每个教师模型 \(\theta^{(k)}_T\) 在一个公开数据集上，采用**梯度近似方法**计算 **SHAP** 值，得到特征重要性向量 \(\phi^{(k)}\)。然后聚合所有教师的 SHAP 值，生成一个平均的综合特征重要性 \(\bar{\phi}\)。
        -   **多教师知识蒸馏 (MTKD)**：初始化一个学生模型 \(\theta_s\)。对于每个批次的公开数据：
            -   计算所有教师模型的输出 logits 并取平均，得到集成后的教师预测 \(\bar{z}_T\)。
            -   计算学生模型对应的输出 logits \(z_S\)。
            -   使用温度系数 \(T\) 将两者转换为软目标概率 \(\tilde{y}_T\) 和 \(\tilde{y}_S\)。
            -   构成综合损失函数：
                1.  蒸馏损失：\(L_{KD} = D_{KL}(\tilde{y}_T \parallel \tilde{y}_S)\)
                2.  学生损失：\(L_{CE} = CrossEntropy(z_S, y)\)
                3.  总损失：\(L_{total} = (1-\alpha)L_{CE} + \alpha T^2 L_{KD}\)
        -   **特征重要性加权**：将聚合的特征重要性标量 \(\bar{\phi}\) 与总损失结合，得到加权最终损失 \(L_{weighted} = \bar{\phi} \cdot L_{total}\)。这意味着特征越重要的批次，对模型更新的影响越大。
        -   在公开数据上使用 \(L_{weighted}\) 对学生模型 \(\theta_s\) 进行多轮完整训练，得到最终的泛化全局模型。

### 3. 实验设计

-   **数据集与场景**：
    -   **客户端本地数据**：使用 MNIST 数据集，通过不同参数 \(\alpha\) (0.1, 0.3, 0.5, 1.0, 10.0) 的 Dirichlet 分布进行分割，模拟不同程度的非 IID 场景。
    -   **服务器端公开数据**：使用 FMNIST 和 CIFAR-10 数据集，用于蒸馏训练和评估最终全局学生模型的泛化能力。
-   **基准模型与对比方法**：
    -   **中心化学习**：作为性能理论上限。
    -   **传统 FL 基线**：**FedAvg** 和 **FedProx**（增强了对系统异构的鲁棒性）。
    -   **知识蒸馏基线**：**FedKDShap**（将 Shapley 值直接用于单教师知识蒸馏）。
-   **客户端模型配置**：为模拟设备算力异构，设计了四种模型架构：
    -   *SimpleCNN* (轻量级, 0.8M 参数)
    -   *ResNetLike* (均衡型, 1.5M 参数)
    -   *MobileNetLike* (移动端优化, 1.2M 参数)
    -   *ResNet18Like* (高性能, 2.1M 参数)
-   **学生模型**：采用一个更轻量的 *StudentCNN* (0.3M 参数) 作为目标全局模型。

### 4. 资源与算力

-   **文中明确说明**：论文未提及具体的 GPU 型号、数量或总训练时长。
-   **提及的优化**：
    -   SHAP 值计算由于在服务器端后处理阶段进行，**不占用客户端的通信与计算资源**。
    -   为解决精确 Shapley 值计算 \(O(2^{|F|})\) 的指数级复杂度，论文采用了**梯度近似法**，将复杂度降至 \(O(B \cdot N)\)。
    -   实际计算中选取一个较小的背景样本集（100 个实例），并为每类代表样本（共 10 个）计算 SHAP 值，声称开销相较于总训练时间可忽略不计。

### 5. 实验数量与充分性

-   **主要实验**：
    -   **收敛性分析**：在 MNIST 上测试了不同集群（3, 4 个）与不同客户端数量（5, 10, 100）组合下的全局损失和精度变化曲线。
    -   **泛化性能测试**：在完全未见过的 FMNIST 和 CIFAR-10 数据集上评估最终学生模型的准确率。
    -   **数据异构敏感性**：测试了不同 Dirichlet 参数 \(\alpha\) 对客户端模型性能的影响。
-   **对比实验**：将 FedMTFI 与 3 种联邦学习基线（FedAvg, FedProx, FedKDShap）及中心化学习上限在 CIFAR-10 和 FMNIST 上进行了性能对比。
-   **消融实验**：针对 SHAP 特征加权这一核心贡献，进行了有/无特征加权的消融研究（在 3 集群、10 客户端配置下）。
-   **实验充分性评估**：实验设计较为全面，覆盖了收敛性、泛化性、异构性影响、多基线比较和自身模块的有效性验证。对比基准包含了经典方法和同类型知识蒸馏方法，结果客观，论证较为严谨。

### 6. 论文的主要结论与发现

-   **性能优越性**：FedMTFI 在所有基准测试中均优于传统的 FedAvg、FedProx 和基于蒸馏的 FedKDShap 算法。在 CIFAR-10 上达到 64.48% 准确率（远超 FedAvg 的 56.15%），在 FMNIST 上达到 87.28%（接近中心化学习的 88.91%），显著缩小了联邦学习与中心化学习之间的性能差距。
-   **异构鲁棒性**：即使客户端数据呈高度非 IID 状态 (\(\alpha=0.1\))，FedMTFI 仍能保持稳健的性能（客户机精度达 91.24%）。
-   **有效性验证**：消融实验证实，集成 SHAP 特征重要性加权能为模型带来稳定的正向增益（CIFAR-10 提升 2.67%，FMNIST 提升 1.88%），验证了该模块的有效性。

### 7. 优点

-   **架构新颖**: 创新性地将多教师知识蒸馏与 SHAP 特征重要性相结合，用于解决 FL 异构问题，提供了一种新范式。
-   **拥抱而非消除异构**: 通过聚类策略为不同能力的设备分配合适的专属模型，再通过蒸馏统一知识，避免“一刀切”，更贴近现实场景。
-   **兼顾性能与可解释性**: 引入 SHAP 不仅为了提升性能，通过分析特征重要性也增强了全局模型的可解释性，这在 FL 中是一个有价值的尝试。
-   **实验设计扎实**：使用了公共数据集（FMNIST, CIFAR-10）评估学生模型泛化能力，而非仅评估参与训练的客户数据，评估方式更真实、可靠。

### 8. 不足与局限

-   **计算开销与成本**：
    -   服务器端需要维护一个公开数据集，且需要进行完整的 MTKD 训练以及 SHAP 值计算，对服务器算力要求较高。
    -   SHAP 值的计算虽采用近似法，但在高维特征任务（如大规模图像）中，其计算开销仍可能成为瓶颈，论文未对该开销进行量化对比。
-   **实验范围局限**：
    -   实验仅在相对简单的基准数据集（MNIST, FMNIST, CIFAR-10）上进行，未在更大规模、更复杂的数据集上验证。
    -   对比的 FL 基线方法较经典，未与更新、更强的算法（如文中未来工作中提及的 SCAFFOLD, FedDF）进行比较。
-   **系统实现细节**：对于客户端模型分簇的具体标准（如何界定“算力相似”）、跨簇复杂度的通信同步开销等实际部署问题，未做深入讨论。

（完）
