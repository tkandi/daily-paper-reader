---
title: "TL++: Accuracy and Privacy Preserving Traversal Learning for Distributed Intelligent Systems"
title_zh: TL++：面向分布式智能系统的精度与隐私保护遍历学习
authors: "Erdenebileg Batbaatar, Young Yoon"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25627v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 提出一种分布式遍历学习框架，实现与集中式小批量梯度等价的效果
tldr: "分布式训练中，联邦学习面临数据异构挑战且需频繁交换完整模型，分割学习减少通信但破坏集中式梯度行为并泄露激活/梯度。TL++提出遍历学习框架，通过显式同步构建虚拟批次恢复集中式小批量梯度，基础模式仅交换切割层激活与梯度，安全模式基于秘密共享保护中间张量。在CIFAR-10上，TL++基础和安全模式分别达91.41%和90.93%，超最强非TL++基线12个百分点以上，且通信减少13.1倍；在BioGPT/PubMedQA同样占优。TL++实现近集中训练精度、低通信开销及激活级隐私保护，适用于跨数据孤岛协作。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25627v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1429, \"height\": 988, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25627v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1338, \"height\": 1148, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25627v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1346, \"height\": 762, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25627v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1193, \"height\": 630, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25627v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1449, \"height\": 955, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25627v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 837, \"height\": 617, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25627v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1397, \"height\": 424, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25627v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1136, \"height\": 224, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25627v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 689, \"height\": 457, \"label\": \"Table\"}]"
motivation: 分布式训练需保护数据隐私，但现有联邦学习受数据异构影响且通信成本高，分割学习梯度行为不准确并泄露中间数据。
method: TL++框架通过虚拟批次显式同步恢复集中式梯度，基础模式交换切割层激活/梯度，安全模式秘密共享中间张量。
result: "CIFAR-10上TL++精度最高达91.41%，超基线12个百分点以上，通信减少13.1倍；PubMedQA结果同样领先。"
conclusion: TL++接近集中训练性能，大幅减少通信并实现激活级秘密共享隐私保护。
---

## 摘要
分布式智能系统越来越需要在集中化原始数据的情况下跨数据孤岛进行训练。联邦学习保持数据本地化，但在异构分区下可能表现不佳，且需重复交换完整模型。分割学习通过切割层激活减少通信，但标准协议通常无法恢复集中式小批量梯度行为，并可能以明文形式暴露激活和梯度。我们提出 TL++，一种双模式遍历学习框架，该框架在节点间构建虚拟批次，以在显式同步假设下恢复集中式小批量梯度行为。基本模式交换切割层激活和梯度，而非完整模型。安全模式将每个切割层激活和梯度在编排器与一个非共谋辅助方之间进行秘密共享，防止任一服务器观察明文切割层张量。此保护仅限于半诚实两服务器设置；标签及损失相关输出仍对编排器可见。在本文评估的轻量级安全路径中，精确性要求线性或仿射服务器路径，而非线性操作需要非线性 MPC 或近似。我们形式化 TL++，分析通信与计算成本，并在 CIFAR-10 和 BioGPT/PubMedQA 上使用全量微调和 LoRA，将其与联邦学习和分割学习基线进行比较。在 CIFAR-10 上，TL++ 基本切割 1 与精确安全切割 3 分别取得 91.41%（标准差 0.19）和 90.93%（标准差 0.17）的准确率，超过最强非 TL++ 基线逾 12 个百分点。TL++ 基本切割 1 相比全模型同步，每步通信减少 13.1 倍。PubMedQA 结果同样有利于 TL++。总体而言，TL++ 在接近集中训练性能的同时，降低了通信开销，并提供了激活级别的秘密共享保护。

## Abstract
Distributed intelligent systems increasingly need to train across data silos without centralizing raw data. Federated learning keeps data local but can suffer under heterogeneous partitions and requires repeated full-model exchange. Split learning reduces communication through cut-layer activations, but standard protocols generally do not recover centralized mini-batch gradient behavior and may expose activations and gradients in plaintext. We present TL++, a two-mode traversal-learning framework that constructs virtual batches across nodes to recover centralized mini-batch gradient behavior under explicit synchronization assumptions. Base mode exchanges cut-layer activations and gradients rather than full models. Secure mode secret-shares each cut-layer activation and gradient between an orchestrator and a non-colluding helper, preventing either server from observing plaintext cut-layer tensors. This protection is limited to a semi-honest two-server setting; labels and loss-related outputs remain visible to the orchestrator. In the lightweight secure path evaluated here, exactness requires a linear or affine server path, while nonlinear operations require nonlinear MPC or approximation. We formalize TL++, analyze communication and computation costs, and evaluate it against federated and split-learning baselines on CIFAR-10 and BioGPT/PubMedQA using full fine-tuning and LoRA. On CIFAR-10, TL++ base cut 1 and exact secure cut 3 achieve accuracies of 91.41% (SD 0.19) and 90.93% (SD 0.17), respectively, exceeding the strongest measured non-TL++ baseline by more than 12 percentage points. TL++ base cut 1 also reduces per-step communication by 13.1-fold relative to full-model synchronization. PubMedQA results similarly favor TL++. Overall, TL++ approaches centralized-training performance while reducing communication and providing activation-level secret sharing.

---

## 论文详细总结（自动生成）

### 1. 论文核心问题与整体含义
- **研究动机**：分布式智能系统（如临床监测、自动驾驶等）常面临数据“孤岛”，集中式训练既涉及隐私和带宽成本，又与数据管辖法规（如GDPR、HIPAA）相冲突。
- **核心矛盾**：现有分布式学习范式难以同时满足三个关键需求：
  - **集中式梯度等价性**：在异构（非独立同分布）数据下的梯度更新与集中式小批量训练一致；
  - **通信效率**：避免反复传输完整模型参数；
  - **中间计算安全性**：保护切割层激活与梯度，抵御来自服务器端的推断攻击。
- **论文定位**：提出现有联邦学习（FL）、分割学习（SL）及其变体均无法同时满足上述三个要求（见表1），进而设计**TL++**——一种双模式遍历学习框架，通过在节点间构造虚拟批次实现集中式梯度行为，并可选地引入秘密共享以保护中间张量。

### 2. 方法论
- **核心思想**：遍历学习（Traversal Learning, TL）通过**显式构造跨节点的虚拟批次**（virtual batch），使得每一步梯度更新在数学上等价于对全体集中数据做小批量随机梯度下降（SGD），同时仅交换切割层激活和梯度，从而同时满足梯度等价与通信效率。
- **双模式设计**：
  - **基础模式**：节点将切割层激活以明文发送给编排器（orchestrator），编排器完成前向/反向传播后回传切割层梯度。此模式适用于可信环境。
  - **安全模式**：引入一个**非共谋辅助服务器**（helper server），节点对切割层激活进行加法秘密共享（additive secret sharing）：
    \[
    a^{(i)}_1 = r,\quad a^{(i)}_2 = a^{(i)} - r
    \]
    两份额分别发给编排器和辅助方，使得任一单独服务器无法获取明文中间张量。
- **关键技术细节**：
  - **虚拟批次构建**：编排器收集各节点样本数，基于全局索引构造每轮epoch的无放回随机批次，并分解为节点子索引；节点仅加载对应本地样本，从而形成等价于集中式采样的虚拟批次。
  - **梯度等价证明**（命题1）：在相同模型参数和批次索引下，基础模式的服务器端梯度与集中式小批量SGD完全相同。
  - **安全模式精确条件**：当**共享份额独立经过的服务器路径为线性/仿射**时（如切割3仅含最终全连接层），加法秘密共享能够保持函数与梯度的代数精确性（命题2）；若路径包含非线性层（如ReLU、池化等），直接独立计算份额会产生近似误差，需额外的安全非线性协议。
  - **可选噪声增强**：可向激活份额或梯度份额注入高斯噪声，以增加隐私保护强度（非差分隐私审计），可调节σ参数。
  - **LoRA集成**：在语言模型微调中，仅训练低秩适配矩阵，进一步降低同步参数量，但不改变线性/仿射路径的安全条件。

### 3. 实验设计
- **数据集与场景**：
  - **CIFAR-10**：使用定制VGG风格CNN（3个卷积块+分类器），考察2至10类的类别数扩展，模拟标签分布非IID。
  - **BioGPT/PubMedQA**：生物医学问答任务，分别用全量微调和LoRA适配，评估遍历学习在NLP模型上的泛化性。
- **对比基线**：FedAvg、FedProx、SCAFFOLD（FL类）；标准Split Learning（SL）；SplitFed；并包含集中式训练作为上限。
- **评估指标**：
  - **效用**：5次独立运行的平均测试准确率±标准差。
  - **通信成本**：基于张量负载的分析模型，包括序列化开销、帧头、单向延迟等，但不包含实际包级运行时测量。
- **切割层设置**：cut 1（首块池化后，16384维）、cut 2（次块池化后，8192维）、cut 3（首全连接后dropout前，512维）。安全模式下仅cut 3满足线性/仿射精确条件。

### 4. 资源与算力
- 论文**未明确报告实际使用的GPU型号、数量或训练时长**。
- 在通信与计算分析模型中，假设节点算力100 GFLOPs/s、服务器算力1000 GFLOPs/s、链路带宽100/200 Mb/s等，但这些都是理论建模参数，并非真实硬件资源记录。因此，无法从中提取具体消耗的算力规模。

### 5. 实验数量与充分性
- **实验数量**：
  - CIFAR-10上共进行了**9种方法（含集中训练）×3种切割层×2个模式（基础/安全）** 的对比，并在每一类数任务（2至10类）上重复5次，累计数百次独立训练。
  - PubMedQA上对比了6种方法，分全量微调和LoRA两种设置。
  - 还进行了切割层敏感性分析、通信成本解析以及多维度对比汇总。
- **充分性与公平性**：
  - 实验覆盖了图像分类和语言理解两类任务，包含不同模型规模和参数效率策略，设计较为全面。
  - 所有分布式方法在相同非IID分片、批次大小和优化器设置下评估，确保了公平性。
  - 但对比以描述性统计为主（均值±标准差），未提供不同方法间的配对统计检验；安全模式的近似切割（cut1, cut2）被明确标注为近似并解释原因，避免了误导性结论。
  - 未见严格的超参数调优横向对比，可能对基线性能存在一定影响。

### 6. 主要结论与发现
- **精度接近集中式**：CIFAR-10全10类任务上，TL++基础cut 1和精确安全cut 3分别达到91.41%和90.93%，仅比集中式训练低约0.6和1.1个百分点；而最强的传统基线仅约79%，落后逾12个百分点。PubMedQA上趋势相同。
- **通信开销显著降低**：基础cut 1每步通信负载仅为FedAvg的1/13.1（约20.9 MB vs 273 MB）。安全cut 1和cut 2仍有4.2~3.4倍降低，但安全cut 3因为需要传输节点模型梯度份额导致负载约为560 MB，接近FL水平。
- **安全与效用权衡**：精确安全模式需要切割点之后为线性/仿射路径（在本CNN架构中为cut 3），此时秘密共享不引入近似误差，精度保持稳定；若切割点包含非线性层，独立份额计算会带来近似损失。
- **即时发现指导**：在信任环境中推荐基础cut 1；在半诚实非共谋的安全环境中推荐安全cut 3；若需降低安全模式通信成本，需结合参数高效更新或压缩技术。

### 7. 优点
- **创新性整合**：首次将遍历学习的虚拟批次机制与加法秘密共享结合，在单一框架下实现集中式梯度等价、通信效率和激活级隐私。
- **严格的理论保证**：明确定义“梯度等价”为数学推导而非近似，并给出线性/仿射路径下的精确性证明，以及非线性情况下的近似性质说明，逻辑清晰。
- **可配置的双模架构**：基础模式和安全模式共用同一拓扑，部署者可根据信任假设灵活选择。
- **详尽的通信成本分析**：通过解析模型揭示不同切割点、模式下的负载构成和瓶颈，为实际部署提供指导。
- **代码公开**：提供了CIFAR-10和BioGPT两个案例的完整实现，便于复现。

### 8. 不足与局限
- **安全模式的适用性限制**：
  - 仅在半诚实、非共谋的两服务器威胁模型下有效；无法防御恶意服务器、共谋、或节点侧的攻击。
  - 需切割点后服务器路径为线性/仿射才能达到精确梯度等价，限制了网络架构的灵活性，深层非线性网络需要额外的安全非线性协议。
  - 标签和损失依赖的输出信号在编排器端可见，未保护标签隐私。
- **通信模型过于理想**：仅基于理论负载和时间建模，缺乏真实网络环境下的包级运行时验证（如GPU利用率、内存压力、延迟抖动等）。
- **实验覆盖面有限**：
  - 仅在CIFAR-10和PubMedQA小品项上验证，未涉及大型Transformer、生成任务、推荐系统或更复杂的医疗数据集。
  - 仅探索了LoRA形式的参数高效微调，未与其他梯度压缩或本地多步更新方法结合。
- **隐私增益未经严格审计**：可选噪声添加虽可调，但未进行正式的差分隐私（ϵ，δ）核算，也未开展针对真实梯度泄露攻击的抵抗性实验。
- **比较基准可能欠优化**：基线方法的性能显著低于TL++，其中或许包含超参数、正则化手段未充分调优的因素，可能高估了TL++的相对优势。

（完）
