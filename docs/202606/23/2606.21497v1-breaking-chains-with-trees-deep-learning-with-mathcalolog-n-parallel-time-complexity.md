---
title: "Breaking chains with trees: Deep learning with $\\mathcal{O}(\\log N)$ parallel time complexity"
title_zh: "用树结构打破链式依赖：具有 $\\mathcal{O}(\\log N)$ 并行时间复杂度的深度学习"
authors: "Neeraj Mohan Sushma, Aditya Nagarsekar, Cabrel Teguemne Fokam, Robin Schiewer, Amit Kumar Pal, Anand Subramoney, David Kappel"
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21497v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 提出分层块局部学习，实现并行深度学习的对数时间复杂度
tldr: 深度网络训练依赖串行反向传播，导致层间锁定与权重传输问题，限制并行扩展。提出的分层块局部学习（HBLL）通过层次化分解网络，利用变分原理的局部目标训练各块，无需端到端反向传播，首次实现O(log N)并行时间复杂度。在视觉和语言建模任务上验证，性能具竞争力，并可扩展至递归结构，为大规模并行学习提供新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21497v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1436, \"height\": 535, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21497v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1450, \"height\": 524, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21497v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1406, \"height\": 846, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21497v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1450, \"height\": 718, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21497v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1037, \"height\": 510, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21497v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 748, \"height\": 328, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21497v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1153, \"height\": 221, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21497v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1153, \"height\": 221, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21497v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1012, \"height\": 221, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21497v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1366, \"height\": 1204, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21497v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1277, \"height\": 292, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21497v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1338, \"height\": 291, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21497v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1278, \"height\": 291, \"label\": \"Table\"}]"
motivation: 反向传播的层间锁定和权重传输问题制约并行训练效率与可扩展性。
method: HBLL将网络层次分解为块，用局部学习目标替代端到端反向传播，实现O(log N)并行时间。
result: 在视觉和语言任务上取得有竞争力结果，并成功应用于递归序列架构。
conclusion: HBLL首次突破O(log N)并行深度训练，为高效可扩展学习开辟新方向。
---

## 摘要
现代深度神经网络架构通过反向传播进行训练，这要求误差在参数更新之前逐层顺序传播。这带来了两个限制：锁定现象，即逐层更新严格相互依赖，无法并行进行；以及权重传输问题，即精确梯度计算需要对称的前向和后向通路。这些约束限制了并行性，增加了内存和通信开销，并对可扩展学习构成了挑战。在本文中，我们提出了分层块局部学习（HBLL）框架，该框架将深度神经网络分解为层次化连接的块，利用变分原理导出的局部学习目标进行训练，消除了全端到端反向传播的需求，同时保持了网络间的有效信息传播。HBLL 是首个能够以 $\mathcal{O}(\log N)$ 并行时间复杂度训练深度神经网络的算法，其中 $N$ 是网络层数。我们证明 HBLL 隐式定义了一族子网络，对应于不同的层次路径，从而支持使用不同有效层数的灵活推理。我们在一系列具有挑战性的视觉和语言建模任务上评估了 HBLL，取得了有竞争力的性能。我们还将 HBLL 扩展到循环序列架构，应用于那些原本依赖时间反向传播的设置。

## Abstract
Modern deep neural network architectures are trained via backpropagation, which requires errors to be sequentially propagated through all layers before parameters can be updated. This introduces two limitations: locking, where layer-wise updates are strictly interdependent and cannot proceed in parallel, and the weight transport problem, which requires symmetric forward and backward pathways for exact gradient computation. These constraints restrict parallelism, increase memory and communication overhead, and pose challenges for scalable learning. In this work, we propose Hierarchical Block-Local Learning (HBLL), a framework that decomposes deep neural networks into hierarchically linked blocks trained using local learning objectives derived from variational principles, eliminating the need for full end-to-end backpropagation while maintaining effective information propagation across the network. HBLL is the first algorithm that is able to train deep neural networks in $\mathcal{O}(\log N)$ parallel time complexity, where $N$ is the number of network layers. We show that HBLL implicitly defines a family of subnetworks corresponding to different hierarchical paths, enabling flexible inference with different effective numbers of layers. We evaluate HBLL on a set of challenging vision and language modeling tasks, achieving competitive performance. We also extend HBLL to recurrent sequence architectures, applying to settings that otherwise rely on backpropagation through time.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究背景**：深度神经网络训练普遍依赖反向传播（BP），但其存在两个固有限制：
  - **锁定问题**：层与层的参数更新必须串行进行，无法并行化。
  - **权重传输问题**：精确梯度计算要求前向与反向通路完全对称，增加内存和通信开销。
- **整体含义**：这些限制制约了大规模模型训练的并行效率和可扩展性。论文试图从根本上突破这一瓶颈，提出一种无需端到端反向传播的训练框架，使得训练过程可以在$\mathcal{O}(\log N)$的并行时间复杂度内完成（$N$为网络层数），同时保持具有竞争力的性能。

### 2. 方法论：核心思想与关键技术细节
- **核心思想**：
  - 将深度网络视为一阶马尔可夫链，通过引入变分后验分布，用树结构将其分解为层次化连接的块。
  - 每个块使用局部学习目标进行训练，无需全局梯度传播，从而消除层间依赖，实现高度并行化。
- **关键技术细节**：
  - **分层块局部学习（HBLL）**：网络被划分为$N=2^D$个块，构建深度为$D$的二叉树。每个块对应条件分布$q(z_n \mid \text{pa}(n))$，其中父节点$\text{pa}(n)$来自树结构中的左右边界表示。
  - **变分框架**：通过最小化负对数似然与KL散度之和（即变分自由能$\mathcal{F}$），导出局部目标：
    $$\mathcal{F} \approx \frac{1}{M}\sum_{m=1}^M \left[\sum_{n=1}^{N-1} \log \frac{q(z_n^{(m)}\mid \text{pa}(n))}{p(z_n^{(m)}\mid z_{n-1}^{(m)})} - \log p(y\mid z_N^{(m)})\right]$$
    其中$z_n$是中间表示，$p$是前向马尔可夫转移，$q$是层次化后验近似。
  - **实现细节**：假设条件分布属于指数族（如线性高斯），使用重参数化技巧生成$z_n$样本，并通过泰勒近似进行贝叶斯反演以计算后验均值。算法1给出了训练的并行化执行流程：从根块开始，逐层向下生成中间激活，同一深度的所有块可以完全并行计算。
  - **扩展到RNN（HBLL-RNN）**：将时间序列划分为层次化树，每个节点将其左侧的输入块折叠为状态，传递给子节点。推理时仅使用最细粒度的因果循环单元。为缓解训练-推理分布不匹配，引入并行固定点一致性校正阶段。
- **并行时间复杂度**：传统BP为$\mathcal{O}(N)$，HBLL通过树结构将计算路径缩短为$\mathcal{O}(\log N)$，T∞（并行计算复杂度）达到对数级别。

### 3. 实验设计
- **数据集与任务**：
  - 图像分类：MNIST（MLP）、CIFAR-10、CIFAR-100（Vision Transformer）。
  - 语言建模：WikiText-103（GPT-2风格Transformer）。
  - 序列建模：顺序MNIST和随机置换顺序MNIST（RNN, GRU/Elman单元）。
- **对比方法**：
  - 主要基线：标准端到端反向传播（BP），使用与HBLL相同深度和参数量的模型。
  - 附加基线：对比前向-前向（CFF+M）算法，在视觉任务中作为局部学习的参考。
  - 所有对比均保持架构、优化器、训练设置一致，以确保公平性。
- **评估指标**：
  - 分类准确率（MNIST, CIFAR-10/100），CIFAR-100使用top-5准确率。
  - 语言模型困惑度（PPL）。
  - 鲁棒性测试：CIFAR-10下的缺失数据与标签噪声。
  - 子网络分析：从同一HBLL模型抽取不同层次路径，评估不同有效层数的性能。

### 4. 资源与算力
- 论文仅提及使用GCS超级计算机JUWELS，未披露具体GPU型号、数量、训练时长或能耗数据。
- **透明度评价**：没有给出任何关于算力开销的量化信息，无法评估HBLL的实际硬件利用效率和绝对训练耗时。

### 5. 实验数量与充分性
- **实验组数**：
  - MNIST：3种深度（D=2,3,4） × 3种隐藏单元（50,100,500），共计9组对比。
  - CIFAR-10：ViT架构，深度3，全路径与短路径，并额外进行缺失数据与标签噪声的鲁棒性实验（4种噪声设置）。
  - CIFAR-100：ViT架构，深度3，全路径与短路径。
  - WikiText-103：深度3，全路径与短路径。
  - 循环序列：2种单元（GRU、Elman） × 2种任务（顺序/置换顺序MNIST） × 多种深度（最长到D=8），并验证一致性校正步数K的影响。
  - 子网络路径分析：多个任务上评估了从同一HBLL模型采样的不同子网络（1,2,3,7个Transformer块）的性能。
- **充分性与公平性**：
  - 实验覆盖了前馈、Transformer和循环结构，任务难度从小规模到中等规模语言模型，较为多元。
  - 对比基线严格匹配模型容量和训练配置，但CFF+M结果部分引用自前人工作，可能存在轻微的环境差异。
  - 未进行大规模消融研究（如块大小、噪声尺度、采样数的影响），也未在ImageNet等更大基准上验证。

### 6. 主要结论与发现
- HBLL首次实现了$\mathcal{O}(\log N)$并行时间复杂度的深度网络训练，从理论上打破了反向传播的串行限制。
- 在MNIST上，HBLL在深层（如16层）仍可成功训练，而BP在无残差连接时退化至随机水平。
- 在CIFAR-10/100的ViT分类上，HBLL性能接近BP（差距约1.5%），且通过短路径可获得更紧凑的子网络。
- HBLL在标签噪声和缺失数据下表现出比BP更高的鲁棒性。
- 语言建模上，HBLL的困惑度（22.22）与BP（18.17）存在可接受差距，且短路径性能更优。
- 循环序列建模中，HBLL-RNN在多种深度下与时间反向传播（BPTT）性能相当甚至略优，且随着深度增加训练加速比显著（D=8时达3.6倍）。

### 7. 优点
- **理论基础坚实**：从概率图模型和变分推断导出，提供了清晰的数学框架。
- **并行可扩展性**：首次达到对数级并行复杂度，为大规模分布式训练提供了新范式。
- **内置子网络寻址**：同一训练模型可依据计算预算灵活选择不同深度的推理路径，无需重新训练。
- **方法通用性**：成功应用于MLP、ViT、GPT风格Transformer及RNN，展现了跨架构的泛化能力。
- **抗噪性**：在部分场景下表现出优于BP的鲁棒性，可能源于分层局部目标的隐式正则化。

### 8. 不足与局限
- **性能差距**：在标准视觉和语言任务上，HBLL的最佳结果仍略低于等效BP模型，尤其在大规模语言建模上。
- **计算开销**：尽管并行路径短，但每个块涉及额外的局部损失计算和反传，绝对计算量可能高于BP，且未提供具体显存/时间对比。
- **深层稳定性**：在CIFAR-10的全路径（7个Transformer块）上HBLL略低于BP，更深的网络性能趋势未知。
- **实验规模有限**：未在ImageNet、更大语言模型（如1B参数以上）或更复杂的网络模块（如注意力以外的MLP-Mixer）上验证。
- **超参数敏感度**：噪声尺度、块划分方式、一致性校正步数等可能需要仔细调节，但论文未充分探讨。
- **理论假设**：指数族分布和线性高斯假设可能限制表达能力，尽管实验显示其仍有效。

### 9. 讨论与未来可探索方向
- **缩放至更大规模**：当前实验仅覆盖中等规模的分类与语言建模任务，未在参数规模超过千万级的模型上检验 HBLL 的并行增益与性能保持情况。将此框架扩展到数十亿参数的语言模型或高分辨率图像任务（如 ImageNet-22K）中，评估其通信/计算比的真实优势，是关键下一步。
- **块划分策略与自动化**：树结构与块边界目前采用固定的均匀分割，未见针对网络宽度、模块功能差异的自适应划分。未来可研究根据层间信息流或模块重要性动态构建分层后验，进一步减少局部目标引入的近似误差。
- **跨模态与复杂架构的泛化**：论文虽涵盖前馈、Transformer 与 RNN，但未涉及图神经网络、扩散模型或强化学习智能体等。验证 HBLL 在时序预测、多模态对齐等需要复杂交互的任务上的有效性，将拓宽其应用范围。
- **理论近似界的紧致性**：当前变分目标基于一阶马尔可夫假设和线性高斯/指数族近似，虽实践有效，但其导致的偏差与最优传输之间的定量关系尚未清晰。推导更紧致的误差界，或设计自适应选择噪声尺度的机制，可帮助指导超参数选择。
- **混合训练策略**：HBLL 的短路径子网络可以视为一种隐式深度集成，而端到端反向传播在最终精度上仍占优。或许可以探索“用 HBLL 热启动 + 局部微调”等混合范式，兼顾早期训练的并行可扩展性与后期精度的精细调整。
- **硬件协同设计与系统实现**：理论上的对数并行时间需要实际映射至异构硬件集群，块间的数据传输、显存驻留策略以及局部梯度计算的异步调度，仍需具体系统支撑。构建针对分层块局部学习的专用通信原语与编译器优化，有望最大化加速收益。

（完）
