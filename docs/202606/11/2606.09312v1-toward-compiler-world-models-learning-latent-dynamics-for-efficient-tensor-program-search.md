---
title: "Toward Compiler World Models: Learning Latent Dynamics for Efficient Tensor Program Search"
title_zh: 走向编译器世界模型：学习潜在动态以高效搜索张量程序
authors: "Haolin Pan, Lianghong Huang, Xvlin Zhou, Mingjie Xing, Yanjun Wu"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09312v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 基于世界模型的张量程序调度优化评估器
tldr: "张量程序优化对机器学习系统至关重要，但其搜索空间庞大，现有自动调度器将候选程序视为静态快照，忽略调度轨迹，导致对动作依赖和表面变化不敏感。我们提出世界模型启发的评估器，将调度评估建模为动作条件的潜在动态，在连续潜在空间中滚动动作，避免AST变换和重复编码。在TVM AutoScheduler上，64次试验内GPU/CPU代表性子图延迟分别提升1.37倍和1.54倍；用10%测量次数即达Ansor-10K的2.2%精度内；全模型推理加速4.61/3.67倍。该方法为张量程序搜索提供了高效方案，大幅减少测量开销，提升编译性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09312v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 785, \"height\": 921, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09312v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 878, \"height\": 698, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09312v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1748, \"height\": 893, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09312v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 892, \"height\": 501, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09312v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 901, \"height\": 768, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09312v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1845, \"height\": 521, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09312v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 917, \"height\": 976, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09312v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 925, \"height\": 415, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09312v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 924, \"height\": 620, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09312v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 877, \"height\": 376, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09312v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 790, \"height\": 897, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09312v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 695, \"height\": 1001, \"label\": \"Table\"}]"
motivation: 现有调度器将候选程序当作静态快照，忽略调度轨迹，对动作依赖和表面代码变化不敏感，搜索效率受限。
method: 受世界模型启发，将调度评估建模为动作条件潜在动态，在连续潜在空间滚动动作，结合动作与硬件特征排序。
result: "64次试验GPU/CPU延迟分别提升1.37/1.54倍，10%测量次数匹配Ansor-10K的2.2%内，全模型推理加速4.61/3.67倍。"
conclusion: 通过潜在动态建模高效评估张量程序，大幅减少测量成本，显著提升编译性能。
---

## 摘要
张量程序优化对现代机器学习系统至关重要，但其搜索空间巨大。现有的自动调度器通过学习的成本模型降低测量成本，但它们通常将每个候选方案作为静态代码快照评估，忽略了产生该方案的调度轨迹。这使得它们对动作依赖性不敏感，且容易受到表面代码变化的影响。我们提出一种受世界模型启发的评估器，将调度评估建模为在程序状态上的动作条件潜在动态。从初始程序开始，它利用轻量级转换模型在连续潜在空间中展开调度动作，避免了昂贵的AST变异和重复代码编码。最终的动态表示与动作和硬件特征结合以对候选方案排序。在TVM AutoScheduler中实现后，我们的方法在相同的64次试验预算下，将代表性子图延迟在GPU上相比Ansor提高了1.37倍，在CPU上提高了1.54倍。它还能在减少10倍测量次数的情况下，以2.2%的几何平均误差匹配Ansor-10K，并且相比PyTorch/PyTorch-opt(cuDNN)，将全模型推理的几何平均加速了4.61倍/3.67倍。

## Abstract
Tensor program optimization is essential for modern machine learning systems, but its search space is enormous. Existing auto-schedulers reduce measurement cost with learned cost models, yet they usually evaluate each candidate as a static code snapshot, ignoring the schedule trajectory that produced it. This makes them insensitive to action dependencies and vulnerable to superficial code variations. We propose a \emph{world-model-inspired} evaluator that models schedule evaluation as action-conditioned latent dynamics over program states. Starting from the initial program, it rolls out scheduling actions in a continuous latent space with a lightweight transition model, avoiding expensive AST mutation and repeated code encoding. The final dynamic representation is combined with action and hardware features to rank candidates. Implemented in TVM AutoScheduler, our method improves representative-subgraph latency over Ansor by 1.37$\times$ on GPU and 1.54$\times$ on CPU under the same 64-trial budget. It also matches Ansor-10K within 2.2% geometric mean using 10$\times$ fewer measurements, and accelerates full-model inference over PyTorch/PyTorch-opt(cuDNN) by 4.61$\times$/3.67$\times$ geometric mean.

---

## 论文详细总结（自动生成）

好的，请查收基于所提供论文内容的结构化中文总结。

### 1. 论文的核心问题与整体含义

*   **核心问题**：现代深度学习编译器中，张量程序的优化（即自动调度）是一个搜索代价极高的问题。现有的学习型成本模型（用于评估候选程序）通常存在一个根本性缺陷：它们将每个候选程序视为**静态的代码快照**进行评估。
*   **研究动机与背景**：
    *   **静态视角的局限性**：这种静态看法忽略了产生该程序的**逐步变换过程（调度轨迹）**。这导致两个严重后果：
        1.  **上下文缺失**：无法理解后续的优化决策如何依赖于早期决策建立的上下文。
        2.  **语法噪声敏感**：容易被结构不同但硬件性能完全相同的表面代码变化所误导（例如，因循环边界不对齐而自动生成的`loop peeling`代码）。
    *   **整体含义**：本文提出应从根本上改变评估视角，不再将候选程序视为最终产物，而是将其建模为一个从初始程序状态开始，在调度动作驱动下**动态演化的结果**。论文旨在通过引入“编译器世界模型”框架，更本质地捕捉调度变换的语义，从而提高搜索效率和最终程序性能。

### 2. 论文提出的方法论

*   **核心思想**：受强化学习中的“世界模型”（World Model）启发，将编译器优化过程建模为在连续潜在空间中的**动作条件状态转换**。
    *   **环境**：编译器 + 目标硬件
    *   **状态**：张量中间表示（TensorIR）程序
    *   **动作**：调度原语（如循环分块、融合、向量化）
*   **关键技术细节与流程**：
    1.  **问题形式化**：将候选评估分解为三个函数：
        *   **状态编码器** $E(\cdot)$: 将TensorIR程序状态 $s$ 映射为稠密向量 $z$。
        *   **多步状态转换模型** $F(\cdot)$: 从初始状态表示 $z_0$ 开始，给定一个调度动作序列 $a_{1:T}$，预测最终状态的表示 $\hat{z}_T$。
        *   **排序模型** $G(\cdot)$: 输入预测的终态表示 $\hat{z}_T$、结构化动作特征 $\phi(a_{1:T})$ 和硬件特征 $h$，输出一个用于**相对排序**的标量分数 $y$。
    2.  **TensorIR 状态编码器**：使用基于 CodeBERT 的模型并通过对比学习进行训练，目的是在表示空间中，将语义相近的程序状态拉近，将无关的状态推远。
    3.  **动作条件多步状态转换**：
        *   **状态演进模拟**：通过公式 $\hat{z}_t = F(z_{t-1}, a_t)$ 在潜在空间中**逐步**“展开”调度动作。这避免了在搜索过程中实际执行昂贵的AST变异。
        *   **转换模型实现**：采用受知识图谱嵌入启发的轻量级几何转换模型（TransH），将每个调度动作视为在特定超平面上的一个平移操作。训练时，通过多步展开，联合最小化状态匹配损失和边际排序损失。
    4.  **基于排序的候选评估**：使用XGBoost实现 $G(\cdot)$，它是一个 LambdaRank 模型。它不直接回归绝对执行时间，而是以**任务（workload）** 为单位进行分组，学习将性能更好的候选程序排在前面。这更符合搜索过程中需要相对比较的实际需求。

### 3. 实验设计

*   **数据集/场景（Benchmark）**：
    *   **模型级**：7个代表性神经网络模型，覆盖CNN（ResNet-50, MobileNetV2）、Transformer（BERT-base, GPT-2, OPT-1.3B, GPT-Neo-2.7B）和3D视觉（ResNet18-3D）。
    *   **子图级**：从上述模型中提取了103个子图，并从中挑选出22个有代表性的算子类型（如各类卷积、矩阵乘法、层归一化、Softmax等）进行精细化分析。
*   **对比方法（Baselines）**：
    *   **主要对比**：TVM原生的自动调度器 **Ansor**，在相同和不同的试验/时间预算下进行对比。
    *   **学习型成本模型对比**：TenSet（基于静态快照的评估器）。
    *   **框架级推理性能对比**：PyTorch、启用cuDNN的PyTorch-opt、TensorRT。
    *   **消融实验**：对比移除关键组件（如移除转换模型、仅用初始/最终状态、移除动作特征、替换排序目标为回归）后的性能变化。

### 4. 资源与算力

*   **硬件平台**：实验在一台配备 **Intel Xeon Gold 6430 CPU** 和 **NVIDIA GeForce RTX 4090 GPU** 的机器上进行。
*   **训练数据规模**：论文详细说明了训练各组件（状态编码器、转换模型、排序模型）所使用的样本量，从 **1万到50万** 条轨迹/样本不等。这从侧面反映了所需的训练算力。
*   **训练时长**：论文**未明确提及**训练三个模型组件（CodeBERT编码器、TransH转换器、XGBoost排序器）所需的GPU卡数量以及确切的总训练时长。这是一个信息缺失。

### 5. 实验数量与充分性

*   **实验数量**：实验设计较为系统和全面，大致包括：
    1.  **7个模型的端到端推理性能测试**（1组实验）。
    2.  **模型级加权延迟的搜索性能对比**（GPU和CPU两个平台，不同时间和试验预算下约3组实验）。
    3.  **22个代表性算子子图的精细化分析**（GPU和CPU两个平台，不同预算下与**Ansor**对比约2组实验，以及与**TenSet**的对比1组）。
    4.  **样本效率对比**（与使用高达10240次试验的超大预算Ansor对比，1组实验）。
    5.  **消融实验**（针对5个关键变体进行64次试验对比，1组实验）。
*   **充分性与公平性**：实验涵盖了多种主流模型、算子类型和硬件平台，对比了SOTA编译器及深度学习框架，消融实验设计合理。所有搜索性能对比均在**相同的TVM AutoScheduler框架和调优预算**下进行，确保了公平性。总体来看，实验设计是**充分且客观的**。

### 6. 论文的主要结论与发现

*   **搜索效率显著提升**：在相同的64次试验预算下，提出的方法在GPU和CPU的代表性算子上的几何平均延迟分别比 Ansor **降低了1.37倍和1.54倍**。在模型层面也观察到显著提速。
*   **极高的样本效率**：该方法仅用**1/10的测量次数**（1k次 vs. 10k次），就在几何平均延迟上匹配甚至略优于Ansor，展示了用更少硬件测量达到同等或更优性能的巨大潜力。
*   **显著加速模型推理**：端到端模型推理性能相比PyTorch和优化后的PyTorch有**4.61倍/3.67倍**的几何平均加速，证明了其实际应用价值。
*   **核心组件的有效性**：消融实验证明，**动作条件状态转换模型**是性能提升的最关键因素，其次是结构化动作特征和基于排序的训练目标。

### 7. 优点

*   **视角新颖**：首次将世界模型的动态观念引入张量程序编译评估，从根本思路上超越了静态快照评估的范式。
*   **方法设计巧妙**：通过在连续潜在空间中模拟调度动作，既解决了静态模型对语法噪声的敏感性，又规避了在搜索循环中执行昂贵的编译器中间状态变换，做到了轻量与高效。
*   **工程实现扎实**：在TVM AutoScheduler中实现了完整的端到端流程，包括数据集构建、模型训练和在线集成，验证了方法的可行性。
*   **实验令人信服**：在多种硬件、模型和预算下，相比SOTA方法取得了稳健且显著的优势，尤其是在低测量预算下的高样本效率是其核心亮点。
*   **问题分解清晰**：将复杂的评估问题拆解为状态编码、状态转换和候选排序三个相对独立的阶段，每个阶段的模型选择和训练目标都与其子任务良好匹配。

### 8. 不足与局限

*   **评估器而非生成器**：该方法仅改进候选**评估**，不直接生成或扩大搜索空间。其性能上限受限于底层调度器（如Ansor）能提出的候选方案质量。
*   **长轨迹误差累积**：对于特别长或复杂的调度序列，多步潜在状态展开可能导致预测误差累积，影响终态表示和排序的可靠性。
*   **排序而非回归的局限**：模型专为**任务内候选排序**设计，不能直接预测绝对执行时间，因此无法用于跨任务的性能比较或需要精确耗时估计的场景。
*   **跨任务泛化未验证**：实验主要在同一组7个模型上评估，论文未探讨该评估器在遇到训练时未见过的、结构差异巨大的全新任务时的泛化能力。
*   **硬件与模型覆盖范围**：实验仅在单一的Intel CPU和NVIDIA GPU上进行，且未覆盖当前主流的大语言模型（LLM），在这些更复杂场景下的表现有待验证。

（完）
