---
title: "ANEForge: Python for direct computation on the Apple Neural Engine"
title_zh: ANEForge：在 Apple 神经引擎上直接计算的 Python 包
authors: Spencer H. Bryngelson
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.17090v1"
tags: ["query:mlsys"]
score: 9.0
evidence: Python包，无需CoreML直接编程苹果神经引擎，将惰性张量图编译为单一ANE程序
tldr: Apple Neural Engine (ANE) 作为固定功能硬件加速器广泛部署于 Apple 设备，但开发者只能通过 CoreML 间接调度，难以确保模型在 ANE 上运行并直接挖掘性能。ANEForge 是一个 Python 包，编译惰性张量图为直接 ANE 程序，提供 58 个融合算子和 19 个原生桥接算子，绕过 CoreML 通过守护进程与内核驱动直接调度引擎。它支持 0.33ms ResNet-18 前向推理，并实现训练的前向、反向传播和优化器更新，支持 int8/int4 权重流和融合注意力，状态可驻留。这为开发者提供了底层控制 ANE 的途径，已在 ResNet-18、ViT、句子编码器、Stable Diffusion U-Net 等模型上验证。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17090v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1133, \"height\": 494, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17090v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1382, \"height\": 555, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17090v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1397, \"height\": 249, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17090v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1651, \"height\": 325, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17090v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1643, \"height\": 387, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17090v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1645, \"height\": 245, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.17090v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1617, \"height\": 406, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17090v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1650, \"height\": 588, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17090v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1263, \"height\": 273, \"label\": \"Table\"}]"
motivation: 开发者仅能通过 CoreML 间接使用 ANE，无法直接控制或保证模型在 ANE 上执行，限制了性能挖掘。
method: 将惰性张量图编译为直接 ANE 程序，提供 58 个融合算子和 19 个原生桥接算子，绕过 CoreML 通过守护进程和内核驱动直接调度。
result: ResNet-18 前向推理 0.33ms，单次调用 90μs；支持训练前向/反向/优化器更新、int8/int4 量化、融合注意力及状态驻留。
conclusion: 提供了直接控制 ANE 的 Python 接口，实现极低延迟推理与完整训练，并在多个模型上验证了正确性与性能。
---

## 摘要
ANEForge 是一个 Python 包，可直接编程 Apple 神经引擎 (ANE)，这是每台近期 Apple 设备上的固定功能神经加速器，无需借助 CoreML。在生产环境中，该引擎只能通过 CoreML 访问，后者将其视为一种调度选项：没有配置强制使用 ANE，模型可能默默地在 CPU 或 GPU 上运行。ANEForge 将惰性张量图编译为单个 ANE 程序，该图由 58 个融合算子和 19 个原生桥接算子构建而成。程序通过 Apple 内部框架相同的 ANE 守护进程和内核驱动堆栈进行调度。除推理外，该包还可访问引擎的原生融合注意力机制，流式处理 int8、int4 和稀疏权重，跨步骤保持解码器和优化器状态驻留，并在引擎上执行训练的前向传播、反向传播和优化器更新。一个小型融合程序在约 90 微秒内完成调用，接近引擎每次程序调度 70 微秒的最低开销；预训练的 ResNet-18 前向传播端到端运行时间为 0.33 毫秒。ResNet-18、一个句子编码器和一个 Vision Transformer 在框架参考下实现了端到端运行，Stable Diffusion U-Net 也验证了其前向传播。ANEForge 的目标平台为 macOS 14 及更高版本的 Apple Silicon。每个发布版本都针对所记录的 macOS 和 ANE 编译器版本进行了验证。

## Abstract
ANEForge is a Python package that programs the Apple Neural Engine (ANE), the fixed-function neural accelerator on every recent Apple device, directly and without CoreML. In production the engine is reachable only through CoreML, which treats it as a scheduling option: no configuration requires the ANE, and a model can silently run on the CPU or GPU instead. ANEForge compiles a lazy tensor graph, built from 58 fused operators and 19 native bridge operators, into a single ANE program. The program is dispatched through the same ANE daemon and kernel-driver stack as Apple's internal framework. Beyond inference, the package reaches the engine's native fused attention, streams int8, int4, and sparse weights, keeps decoder and optimizer state resident across steps, and runs the forward pass, backward pass, and optimizer update of training on the engine. A small fused program completes a call in about 90us, near the engine's 70us per-program dispatch floor, and a pretrained ResNet-18 forward runs end-to-end in 0.33ms. ResNet-18, a sentence encoder, and a Vision Transformer run end-to-end against framework references, and a Stable Diffusion U-Net validates its forward pass. ANEForge targets Apple Silicon under macOS 14 and later. Each release is verified against a recorded macOS and ANE-compiler version.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **核心问题**：Apple神经引擎（ANE）虽然广泛部署在iPhone、iPad和Mac上，但开发者只能通过CoreML间接使用，无法保证特定计算一定在ANE上执行；CoreML将ANE仅作为一种调度选项，可能静默回退到CPU/GPU，导致性能不可控和无法精准分析硬件能力。
- **整体含义**：ANEForge提供了一种绕过CoreML、直接编程ANE的Python接口，将计算图编译为单一ANE程序并通过私有框架栈直接调度，使研究者可以固定计算单元、挖掘硬件潜力，并支持训练、量化推理、融合注意力等高级特性。

## 2. 方法论

- **核心思路**：构建惰性张量图 → 编译为单个ANE程序 → 通过ANE守护进程和内核驱动（与CoreML内部相同的`e5rt`接口）直接下发执行，无CoreML依赖。
- **技术细节**：
  - **算子表面**：
    - 融合路由（fused route）：58个算子（卷积、矩阵乘、激活、规范化、注意力组合等），可将整个图编译为一个程序。
    - 原生桥接路由（bridge route）：19个底层硬件原生算子（如融合注意力、argmax、topk等），以子图切割方式运行，绕过公开工具链。
    - 支持`af.sdpa`直接调用ANE专用融合注意力层（CoreML从不发射该层）。
  - **编译与调度**：
    - 图由`af.compile`降级为Apple模型中间语言（MIL），打包权重为二进制blob，生成可重复调用的程序句柄。
    - 通过Objective-C++ shim调用私有框架`e5rt`，利用`aned`守护进程编译、签名程序，下发到ANE内核驱动；不需要系统完整性保护关闭或特殊权限。
  - **权重压缩**：支持int8、int4查找表、非结构化稀疏权重流式处理，权重占用减少约4倍（int4），压缩精度可门控回退。
  - **在引擎驻留状态**：通过缓冲区别名实现键值缓存和优化器状态的跨步驻留，零拷贝，仅主机喂入新输入。
  - **在引擎训练**：自动微分模块生成反向计算图，前向、反向及优化器更新均在ANE上编译为一个程序执行，支持常见算子梯度。
  - **优化器**：基于代价模型的自动调优（`af.tune`），可重写注意力块为融合程序，测量并缓存最优实现。
- **算法流程（文字描述）**：
  - 用户用`af.input`等构建张量图 → `af.compile`进行图融合与权重打包 → 返回可调用对象 → 调用对象触发通过`e5rt`到ANE的调度 → 结果返回。

## 3. 实验设计

- **验证模型与场景**：
  - 预训练模型端到端推理：
    - ResNet-18（torchvision权重，49个算子融合为1个程序）
    - Vision Transformer (ViT-B/16，299个算子)
    - MiniLM句子编码器
    - Stable Diffusion U-Net（前向验证，316个算子）
    - VAE解码器
  - 在引擎训练：卷积网络（conv + GroupNorm）在CIFAR-10上训练，对比PyTorch基线。
  - 数值方法：固定迭代的Krylov求解器、FFT在ANE上运行（作为静态数据流图）。
  - 注意力块自动调优：将QKV投影及注意力折叠为单一融合程序，测量加速比。
- **对比基准（Benchmark）**：
  - 对于每个模型，输出与参考框架（torchvision、transformers、diffusers）的余弦相似度、相对误差或top-k匹配。
  - 性能对比：ResNet-18在ANE上的延迟 vs. CPU（PyTorch float32）、GPU（torch-MPS float32）；调用开销对比ANE程序最低调度开销。
  - 功率测量：使用`powermetrics`采集ANE rail功耗。
- **性能数据**：
  - 小融合程序调用约90微秒（ANE调度下限约70微秒）。
  - ResNet-18 在ANE上0.33毫秒，功耗4.5瓦；GPU需2.0毫秒，CPU需6.0毫秒。
  - 注意力融合改写取得3.7–5.3倍加速。

## 4. 资源与算力

- **未明确提及使用任何训练GPU**。工作全部在ANE硬件上执行，测试设备为：
  - Apple M5 Pro（macOS 26.5，ANECompiler 3520.4.1）
  - Apple M1 Max（验证算子语料库通过）
- **未提供训练时长、批大小等详细算力消耗**，性能数据仅涉及单次推理延迟与瞬时功耗，而非大规模训练的资源消耗。

## 5. 实验数量与充分性

- **实验数量**：
  - 验证5种预训练模型的端到端推理正确性（表格3所列）。
  - 在CIFAR-10上进行一次在引擎训练，与PyTorch比较准确率。
  - 注意力重写加速比测试。
  - 数值方法正确性（与NumPy双精度比较至半精度误差）。
  - 权重压缩功能展示。
  - 算子语料库测试作为回归门（覆盖所有发布算子）。
- **充分性与公平性**：
  - 正确性验证均基于外部参考实现，结果可复现（脚本在仓库中），公平性较好。
  - 性能对比虽然展示了ANE的巨大优势，但对比的CPU/GPU配置可能未优化到最佳（MPS可能未充分利用），且仅测了一款M5 Pro机型，缺乏更全面的硬件覆盖。
  - 训练实验仅一组简单卷积网，未展示更复杂模型的全链路训练，训练相关消融实验缺失。

## 6. 主要结论与发现

- 实现了Python直接访问ANE、无需CoreML的完整通路，调用开销接近硬件极限。
- 同款模型在ANE上运行速度远超CPU/GPU，且功耗更低。
- 支持在引擎端完成训练全流程（前向+反向+优化器更新），以及权重压缩、融合注意力、状态驻留等高级特性。
- 图融合为单一程序可显著降低延迟，自动调优能进一步提升性能。
- 通过机器检查的硬件能力注册表，确立了引擎可接受的算子全集。

## 7. 优点

- **精确控制计算位置**：彻底避免CoreML静默回退，确保程序在ANE上执行。
- **全栈透明**：直接调用私有API，揭露了ANE的底层能力（原生融合注意力、压缩权重流等）。
- **覆盖训练能力**：反向图和优化器均在引擎完成，支持隐私保护端侧学习。
- **低开销**：调度延迟接近硬件下限（90微秒对70微秒）。
- **模型覆盖广**：验证了CNN、ViT、Transformer、扩散模型等。
- **工具链完备**：包含算子正确性检查、自动调优、性能功率测量工具。
- **无特殊系统修改**：不关SIP、不需要特殊权限，仅利用公开符号解析。

## 8. 不足与局限

- **依赖私有API**：映射自未公开框架，随系统更新可能失效，每个版本绑定特定macOS和ANE编译器版本，迁移成本高。
- **数值精度限制**：ANE计算通路基于半精度（float16），不适用于需要高精度的场景；例如完整Stable Diffusion采样回路因半步误差无法收敛。
- **硬件单一性**：仅支持Apple Silicon Mac，且测试机器有限，通用性受限。
- **单程序调度**：引擎呈现单硬件通道，不支持并发多程序，吞吐只能靠图融合实现。
- **算子表面受限**：仅提供ANE硬件可接受的算子集合，非通用张量库，超出范围的图编译失败。
- **训练实验不充分**：仅一个简单CNN训练示例，缺乏对大型模型训练的验证与性能分析。
- **部署限制**：因链接私有框架，无法通过App Store分发。

（完）
