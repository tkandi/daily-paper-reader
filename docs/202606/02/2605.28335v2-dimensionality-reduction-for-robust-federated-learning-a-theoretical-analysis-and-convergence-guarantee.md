---
title: "Dimensionality Reduction for Robust Federated Learning: A Theoretical Analysis and Convergence Guarantee"
title_zh: 面向鲁棒联邦学习的降维方法：理论分析与收敛性保证
authors: "Shiyuan Zuo, Jiashuo Li, Rongfei Fan, Han Hu, Jie Xu"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28335v2"
tags: ["query:mlsys"]
score: 9.0
evidence: 提出联邦学习中用于鲁棒聚合的降维方法并提供理论保证
tldr: "PDR通过生成稀疏随机投影矩阵，将每个客户端的高维梯度映射到低维子空间，在该子空间中执行现有的距离基鲁棒聚合算法。理论分析证明了在特定条件下PDR能够保持与原始高维聚合相同的收敛速率。大量实验表明，将梯度降维至原维度的1%后，鲁棒聚合的计算效率提升近两个数量级，且几乎不影响模型最终精度，为大规模联邦学习部署清除了计算瓶颈。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.28335v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1443, \"height\": 937, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28335v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1442, \"height\": 972, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28335v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 765, \"height\": 299, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28335v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1444, \"height\": 870, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28335v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1444, \"height\": 873, \"label\": \"Table\"}]"
motivation: 提出联邦学习中用于鲁棒聚合的降维方法并提供理论保证。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
联邦学习允许多个客户端在不共享原始数据的情况下协同训练模型，但其极易受到拜占庭攻击。现有鲁棒方法能够抵御这些威胁，但在高维梯度聚合过程中会产生巨大的计算开销，该开销随模型规模增加而急剧上升，随着现代模型越来越大，逐渐成为训练成本的主要部分。为解决这一计算瓶颈，我们提出了投影降维（PDR），这是一个面向基于向量距离的鲁棒聚合器的通用加速框架。该框架通过稀疏随机投影将梯度压缩至极小的子空间中，从而高效计算可靠性权重，实现鲁棒聚合。该方法将服务器的计算复杂度降低至最优的 $\mathcal{O}(Mp)$，其中 $M$ 为客户端数量，$p$ 为模型维度，仅达到读取梯度所需的理论下限。我们在先前拜占庭鲁棒联邦学习分析的标准假设下，建立了收敛性保证。借助子空间嵌入定理，我们证明 PDR 对非凸函数达到 $\mathcal{O}(1/\sqrt{T})$ 的最优收敛速率，对强凸函数达到 $\mathcal{O}(1/T)$ 的最优收敛速率，其中 $T$ 表示迭代次数。关键的是，我们从数学上证明了这种大幅加速几乎零代价，仅使固有的拜占庭误差底线扩大一个受限且可调的因子 $\frac{1+ε}{1-ε}$。在基准数据集上的实验结果证实，将 PDR 与现有聚合器结合，在时间效率上可带来数量级的加速，同时保持极具竞争力的收敛性能。

## Abstract
Federated Learning (FL) enables multiple clients to collaboratively train models without sharing raw data, but it is highly vulnerable to Byzantine attacks. Existing robust approaches can neutralize these threats but incur substantial computational overhead during high-dimensional gradient aggregation, an overhead that scales poorly with model size and increasingly dominates the training cost as modern models grow larger. To address this computational bottleneck, we propose Projected Dimensionality Reduction (PDR), a universal acceleration framework for vector-level distance-based robust aggregators, which performs robust aggregation by compressing gradients into a drastically smaller subspace via sparse random projection to efficiently compute reliability weights. This approach reduces the server computational complexity to an optimal $ \mathcal{O}(Mp) $, where $ M $ is the number of clients and $ p $ is the model dimension, matching the theoretical lower bound required merely to read the gradients. We establish convergence guarantees under standard FL assumptions in prior Byzantine-robust FL analyses. By leveraging the Subspace Embedding Theorem, we show that PDR achieves optimal convergence rates of $ \mathcal{O}(1/\sqrt{T}) $ for non-convex functions and $ \mathcal{O}(1/T) $ for strongly convex functions, where $ T $ denotes the number of iterations. Crucially, we mathematically demonstrate that this massive acceleration comes almost for free, merely inflating the inherent Byzantine error floor by a bounded, tunable factor of $ \frac{1+ε}{1-ε} $. Experimental results on benchmark datasets confirm that integrating PDR with existing aggregators yields orders of magnitude speedups in time efficiency while maintaining highly competitive convergence performance.

---

## 论文详细总结（自动生成）

好的，以下是基于所提供论文内容生成的结构化中文总结。

### 1. 论文的核心问题与整体含义

- **核心问题**：联邦学习（FL）虽然保护数据隐私，但极易受到拜占庭攻击。现有拜占庭鲁棒聚合器（如Krum、Bulyan、几何中位数等）在抵御攻击时，通常需要在高维梯度向量间计算成对距离等操作，其计算复杂度随模型参数量**p**的超线性增长，成为大规模模型训练的主要性能瓶颈。
- **整体含义**：本文旨在解决这一维度灾难导致的计算瓶颈，提出一种通用的、即插即用的降维加速框架，在不牺牲鲁棒性和几乎不影响收敛性能的前提下，将服务器端的鲁棒聚合时间大幅缩短，使其达到理论最优下限。

### 2. 论文提出的方法论

- **核心思想（PDR框架）**：将鲁棒聚合的计算从百万/十亿维的原始梯度空间**Rp**，转移到一个维度极低（**k**，如 **k=4096**）的子空间中进行。该框架专为**基于向量级别距离的鲁棒聚合器**设计（如Krum，几何中位数，Bulyan等），不适用于坐标级聚合器（如坐标中位数）。
- **关键技术细节**：
    - **稀疏随机投影**：服务器在每轮生成一个稀疏随机投影矩阵 **Pt ∈ Rk×p**（如Achlioptas矩阵），将客户端上传的高维梯度**gtm**压缩为低维表示 **˜gtm = Pt gtm**。
    - **低维鲁棒聚合**：在低维子空间中，使用原有的鲁棒聚合器（RAgg）基于向量距离计算每个客户端的可靠性权重 **˜αtm**。
    - **高维还原**：将得到的低维权重 **˜αtm** 直接应用于原始高维梯度 **gtm** 进行加权求和，得到最终的聚合梯度 **gt = Σ ˜αtm gtm**。
- **理论保证**：
    - 利用**子空间嵌入定理（Subspace Embedding Theorem）** 证明了低维空间中的向量距离能以**1±ε**的误差逼近原始距离，从而保证权重分配的可靠性。
    - 将服务器端总复杂度从例如Krum的**O(M2p)**或几何中位数的**O(I·Mp)**，降至统一的**O(Mp + CRAgg(M,k))** ，由于**k«p**，该复杂度趋近于读取数据所需的理论下限**O(Mp)**。
    - 数学上证明，这种加速仅会使最终的拜占庭误差项扩大一个有限且可控的因子 **(1+ϵ)/(1-ϵ)**。

### 3. 实验设计

- **数据集与场景**：在 **TinyImageNet**、**CIFAR-100** 和 **CIFAR-10** 上进行评估。为模拟联邦环境，使用狄利克雷分布（参数**β∈{0.2, 0.6}**）将数据划分为非独立同分布的50个客户端子集。
- **模型**：采用 **MobileNetV3**、**VGG16** 和 **ResNet18** 三种不同规模的模型进行实验。
- **攻击方法（Benchmark）**：测试了四种典型的拜占庭威胁模型：**高斯攻击 (Gaussian Attack)**、**符号翻转攻击 (Sign-flip Attack)**、**LIE攻击** 和 **FoE攻击 (Fall of Empires Attack)**。
- **对比方法（Baselines）**：将PDR框架与四种经典的鲁棒聚合器结合，形成 **PDR+Krum**、**PDR+Bulyan**、**PDR+几何中位数** 和 **PDR+MCA**，并与它们未加速的原始版本进行比较。
- **评估指标**：主要考察**测试集准确率（Test Accuracy）** 和服务器端聚合所消耗的**实际时间（Wall Time）**。

### 4. 资源与算力

- 论文中未明确提及实验所使用的具体GPU型号、数量或总训练时长。

### 5. 实验数量与充分性

- **实验数量**：实验覆盖了**3个数据集**（含不同异构度β）、**4种攻击**、**2种恶意比例b**（0.1和0.3）、**4种baseline聚合器**，组合较多。此外，论文还包含了对PDR中关键参数（子空间维度**k**和稀疏度**s**）的消融实验。
- **充分性与公平性**：实验设计较为全面，评估了方法在不同数据、模型、攻击和异构性下的表现，与原始方法的对比直接且公平。消融实验验证了方法对参数不敏感的优点，增强了结论的可靠性。

### 6. 论文的主要结论与发现

- **超大幅加速**：PDR框架为所有测试的基于距离的鲁棒聚合器带来了**数量级的服务器端耗时加速**。
- **保持竞争性精度**：在实现巨大加速的同时，PDR保持了与原始baseline高度可比的模型准确率。在某些情况下，投影甚至起到了隐式正则化器的作用，过滤了对抗性噪声，从而获得了略高的准确率。
- **鲁棒性无损**：PDR完美继承了底层聚合器的鲁棒性特性。当原始baseline能成功防御攻击时，融合了PDR的方法同样可以；当原始baseline因攻击率过高而发散时，PDR方法也会无法收敛，未引入新的脆弱性。
- **参数鲁棒性**：方法性能对投影维度**k**和稀疏参数**s**的选择不敏感，避免了繁琐的参数微调。

### 7. 优点

- **问题导向明确**：准确切中了联邦学习鲁棒性领域中一个极为实际且迫切的“计算瓶颈”问题。
- **方法巧妙且通用**：提出的PDR框架是一个“即插即用”的加速器，理论支撑坚实，通用性强，能够赋能一系列现有的距离基聚合器。
- **理论与实验结合的创新**：不仅有严谨的子空间嵌入理论作为保证，还通过大量的实验在多个维度验证了其有效性和效率，理论分析与实证结果吻合度高。
- **计算效率的极致优化**：将聚合复杂度压至理论下限**O(Mp)**，这对于推动鲁棒联邦学习在大模型时代的应用具有重大价值。

### 8. 不足与局限

- **算力信息缺失**：论文未报告实验所使用的硬件规格和具体算力开销，这可能影响其他研究者对其效率增益绝对值的评估和复现。
- **适用范围的限定**：框架不适用于坐标级别的鲁棒聚合方法（如坐标中位数，修剪均值），这是一个明确的限制。
- **理论误差的必然性**：随机投影机制不可避免地引入了一个倍率为 **(1+ϵ)/(1-ϵ)** 的误差放大量。在极端的精度要求下，这个微小的精度代价需要被考量，不过论文已通过受控参数ε来平衡。
- **对自适应攻击的讨论有限**：虽然提及矩阵可在每轮重生以对抗自适应攻击，但缺乏对该类更强大攻击的针对性防御分析和实验验证。

（完）
