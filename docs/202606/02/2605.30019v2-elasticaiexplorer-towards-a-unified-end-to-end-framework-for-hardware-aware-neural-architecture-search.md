---
title: "elasticAI.explorer: Towards a Unified End-to-End Framework for Hardware-Aware Neural Architecture Search"
title_zh: "elasticAI.explorer: 面向硬件感知神经架构搜索的统一端到端框架"
authors: "Natalie Maman, Florian Hettstedt, Andreas Erbslöh, Gregor Schiele"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.30019v2"
tags: ["query:mlsys"]
score: 8.0
evidence: 基于YAML搜索空间定义的硬件感知NAS可扩展框架
tldr: 现有硬件感知神经架构搜索（NAS）框架常将搜索空间、模型实现与部署流程紧密耦合，难以扩展到新硬件平台。本文提出elasticAI.explorer，一个基于Optuna的可扩展框架，通过YAML定义搜索空间并动态生成模型，支持分层搜索，并统一优化与部署接口。框架集成硬件特定代码生成、Docker交叉编译与自动基准测试，实现硬件在环NAS，同时提供FLOPs、参数量与延迟评估器。该框架旨在降低嵌入式AI部署工程开销，加速异构加速器平台的硬件感知NAS研究。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.30019v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1371, \"height\": 607, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30019v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 924, \"height\": 447, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30019v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 918, \"height\": 681, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30019v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 938, \"height\": 541, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.30019v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 936, \"height\": 607, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30019v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1884, \"height\": 379, \"label\": \"Table\"}]"
motivation: 现有NAS框架耦合搜索空间、模型实现和部署，阻碍向新硬件扩展和自定义算子的加入。
method: 基于Optuna构建，用YAML动态生成可执行模型，集成硬件特定代码生成、Docker交叉编译和自动基准测试。
result: 实现了硬件在环NAS工作流，并提供可扩展的FLOPs、参数量、延迟评估，降低部署开销。
conclusion: elasticAI.explorer为硬件感知NAS提供了统一、可扩展的端到端框架，促进异构加速器平台研究。
---

## 摘要
神经架构搜索（NAS）已成为在任务特定和硬件特定约束下自动设计神经网络的重要方法。然而，许多现有的NAS框架紧密耦合了搜索空间定义、模型实现和部署流水线，导致难以扩展到新的硬件平台和自定义算子。本文中，我们提出elasticAI.explorer，一个基于Optuna构建的、可扩展的Python硬件感知NAS框架。该框架引入了一种基于YAML的搜索空间规范，在采样过程中动态转换为可执行的神经网络模型。该方法支持逐层、基于单元和分层搜索空间，同时保持统一的优化和部署接口。除架构生成外，该框架还集成了硬件特定代码生成、基于Docker的交叉编译工具链以及设备端基准测试二进制文件的自动创建，从而支持硬件在环的NAS工作流。系统进一步提供了可扩展的评估器，用于FLOPs、参数数量和延迟估计。elasticAI.explorer旨在减少嵌入式AI部署的工程开销，并加速面向异构加速器平台的硬件感知NAS研究。

## Abstract
Neural Architecture Search (NAS) has become an important approach for automatically designing neural networks under task-specific and hardware-specific constraints. However, many existing NAS frameworks tightly couple search space definitions, model implementations, and deployment pipelines, making extension to new hardware platforms and custom operators difficult. In this paper, we present the elasticAI.explorer, an extensible Python framework for hardware-aware NAS built on top of Optuna. The framework introduces a YAML-based search space specification that dynamically translates into executable neural network models during sampling. The approach supports layer-wise, cell-based, and hierarchical search spaces while maintaining a unified interface for optimization and deployment. Beyond architecture generation, the framework integrates hardware-specific code generation, Docker-based cross-compilation toolchains, and automated creation of on-device benchmarking binaries, enabling hardware-in-the-loop NAS workflows. The system further provides extensible evaluators for FLOPs, parameter count, and latency estimation. The elasticAI.explorer aims to reduce the engineering overhead of embedded AI deployment and accelerate research on hardware-aware NAS for heterogeneous accelerator platforms

---

## 论文详细总结（自动生成）

好的，以下是根据提供的论文内容生成的结构化中文总结。

---

## 1. 研究动机与核心问题

- **背景**：神经架构搜索（NAS）在嵌入式深度学习中，需同时平衡任务精度与硬件约束（如延迟、内存、能耗）。
- **现有痛点**：多数NAS框架将搜索空间定义、模型实现与部署流水线紧密耦合，导致：
  - 扩展到新硬件平台或自定义算子时工程浩大。
  - 难以在统一实验设置下对不同NAS范式或目标平台进行横向比较。
- **核心目标**：构建一个可扩展、端到端的硬件感知NAS框架，降低嵌入式AI部署的工程开销，加速异构加速器上的NAS研究。

## 2. 方法论与关键技术细节

### 2.1 总体架构
- 基于 **Optuna** 的超参优化引擎构建，框架分为 **NAS工具包** 与 **硬件感知部署流水线** 两部分。
- 工具包负责搜索空间翻译、候选采样、模型构建与评估；部署流水线负责目标硬件代码生成、交叉编译与基准测试。

### 2.2 声明式搜索空间规范（YAML DSL）
- 采用 **YAML 格式** 定义搜索空间，将架构描述从代码实现中解耦。
- 核心抽象为 **块序列（block sequence）**，每个块包含可选的算子候选集（`op_candidates`）、参数及重复规则。
- 支持三种主流NAS搜索范式（图2）：
  - **逐层（layer-wise）**：使用 `vary_all` 重复模式，每层独立采样。
  - **基于单元（cell-based）**：通过 `repeat_ops` / `repeat_params` / `repeat_block` 实现结构复用，并借助复合操作（`composites`）堆叠复杂单元。
  - **分层（hierarchical）**：通过嵌套复合操作和多种重复机制组合实现。
- 全局默认参数（`default_op_params`）减少冗余定义。
- **预处理器设计空间**：支持滤波、降采样、窗口化、归一化等信号处理算子，可与架构参数联合优化，实现端到端传感器数据处理流水线。

### 2.3 动态模型构建
- 在优化过程中，将采样到的参数动态翻译为可执行的 PyTorch 模型。
- **ModelBuilder** 自动推断张量形状，并在不兼容层之间插入适配模块，无需手动处理形状变换。
- 通过 **LayerBuilder 接口** 和 `@register_layer` 装饰器实现算子热插拔，新算子仅需实现 `build_layer` 和 `get_last_layer` 方法即可集成到搜索空间。

### 2.4 评估 API
- 提供模块化的 **性能评估器**（如分类精度）与 **成本评估器**（参数量、FLOPs、延迟等）。
- 评估结果可作为 Optuna 的目标、软约束或硬约束：
  - **硬约束** 在评估初期执行，可提前中止不满足资源约束的架构。
  - **软约束** 和 **目标值** 通过加权和标量化（支持自定义聚合函数），统一搜索准则。
- 实测延迟可由硬件部署流水线提供，实现 **硬件在环** 成本评估。

### 2.5 硬件感知部署流水线
- 包含 **生成器（Generator）** 系统，将模型实例翻译为目标平台代码（如 TorchScript、LiteRT、FPGA 专用格式）。
- 支持 **Docker 交叉编译**，降低环境配置复杂度。
- 目标平台示例：树莓派 4/5、树莓派 Pico、elasticAI.ENV5 FPGA。
- 生成器可提供 **反射 API**，确保搜索过程中仅使用目标硬件支持的操作实现。
- 两种工作模式：
  1. **离线模式**：将搜索到的最佳模型生成硬件可执行文件并部署评估。
  2. **硬件在环模式**：搜索过程中实时编译候选架构并在硬件上运行，将实测延迟等指标反馈给优化器。

## 3. 实验设计

- **论文未报告任何实验**。文中没有提供数据集、基准测试、对比方法或性能数据。
- 仅从框架设计角度描述了组件功能，未涉及具体应用场景或与现有框架（如 NNI、AW-NAS、HANNAH）的量化比较。

## 4. 资源与算力

- **文中未提及任何 GPU 型号、数量或训练时长**。由于缺乏实验部分，算力需求无从得知。

## 5. 实验数量与充分性

- **实验数量为零**。论文属于纯系统框架介绍，缺失实验验证。因此无法评估实验的充分性、客观性或公平性。

## 6. 主要结论与发现

- **结论**：elasticAI.explorer 通过 YAML 声明式搜索空间、动态模型生成和模块化硬件后端，为硬件感知 NAS 提供了一体化工具箱，能够降低嵌入式 AI 部署的工程门槛。
- **贡献声明**：
  1. 提供一个端到端的硬件感知 NAS 框架。
  2. 引入可扩展的声明式搜索空间规范。
  3. 实现从搜索到真实设备基准测试的完整硬件在环流程。
- 未来工作将拓展对量化/剪枝感知 NAS 的支持，加强 FPGA 与定制 SoC 后端，并探索超网与 one-shot NAS 方法。

## 7. 优点

- **高度解耦**：搜索空间与模型实现分离，扩展新算子或硬件平台只需实现接口，无需修改核心引擎。
- **统一的多范式支持**：同一套 YAML 语法可描述逐层、单元与分层搜索空间，便于复用与比较。
- **硬件在环评估**：直接使用真实硬件延迟反馈，而非依赖粗略的解析估算，提升搜索结果的实际部署可信度。
- **模块化与工程友好**：Docker 交叉编译、自动适配模块插入、评估器插件机制，显著减少重复工程工作。
- **预处理联合搜索**：能同时优化信号处理与模型结构，适合连续传感器数据流应用。

## 8. 不足与局限

- **缺乏实验验证**：全文未提供任何实验结果（精度、延迟、搜索效率、与 baselines 的比较），无法判断框架的实际效能或开销。
- **概念性较强**：目前呈现的主要是系统设计与接口定义，具体实现细节（如适配模块生成策略、交叉编译工具链稳定性）未被验证。
- **对比缺失**：未与相关框架（如 NNI、AW-NAS、HANNAH、Archai）进行功能或性能对比，说服力不足。
- **平台覆盖描述有限**：虽列举了部分硬件，但未说明支持的操作算子范围、底层优化程度或量化部署能力。
- **搜索算法依赖性**：框架强依赖 Optuna 的采样算法，自身未贡献新的搜索策略。
- **标量化策略简单**：默认加权和聚合多目标，可能掩盖帕累托前沿中的复杂权衡。

---

（完）
