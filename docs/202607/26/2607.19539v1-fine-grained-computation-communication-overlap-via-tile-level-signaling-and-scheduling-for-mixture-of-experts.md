---
title: Fine-grained Computation-Communication Overlap via Tile-level Signaling and Scheduling for Mixture-of-Experts
title_zh: 基于瓦片级信令与调度的细粒度计算-通信重叠在混合专家中的应用
authors: "Minyu Cui, Anna Wingkvist, Morgan Ericsson"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.19539v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 针对GPU上MoE的细粒度计算-通信重叠调度
tldr: 混合专家（MoE）架构扩展大语言模型容量时依赖分布式全对全通信，传统顺序执行导致通信延迟暴露，GPU利用率低。本文提出细粒度重叠方法，通过tile级信令调度，将专家计算与回传通信重叠：持久计算核覆盖所有本地专家并优先产出远程tile，持久通信核驻留少量专用SM即时传输就绪tile。在4×A100上，端到端最高加速2.64倍，MoE层加速2.74倍。方法无需修改底层算子，多配置下提升性能，为MoE高效部署提供实用方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.19539v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 842, \"height\": 218, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19539v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 801, \"height\": 1044, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19539v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 788, \"height\": 339, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19539v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 814, \"height\": 406, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19539v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 863, \"height\": 430, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19539v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 880, \"height\": 905, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19539v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 879, \"height\": 900, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19539v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1799, \"height\": 471, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19539v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1802, \"height\": 446, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.19539v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 890, \"height\": 199, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19539v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 873, \"height\": 491, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19539v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 690, \"height\": 258, \"label\": \"Table\"}]"
motivation: MoE模型分布式执行中，全对全通信顺序执行，通信延迟暴露在关键路径上，显著降低GPU利用率，亟需高效的重叠方法。
method: 设计持久化计算核与通信核：计算核优先产出远程tile，通信核在少量SM上监听就绪信号并发起分段传输。
result: 在4×A100上评测三个MoE模型，对比四个先进系统，端到端加速最高2.64倍，MoE层加速2.74倍，多种配置下一致提升。
conclusion: 该方法非侵入式，无需修改底层算子，可显著提升多GPU上MoE模型的执行效率，为扩展大语言模型提供高效部署方案。
---

## 摘要
混合专家（Mixture-of-Experts, MoE）架构能在不成比例增加计算成本的情况下提升模型容量，已成为将大语言模型扩展至万亿参数规模的关键构建模块。这些 MoE 模型的高效部署依赖于跨多个 GPU 的分布式执行，其中每个 MoE 层包含两次全对全通信：将令牌分发至专家进程，以及将专家输出返回至源进程。传统的 MoE 实现在专家计算完成后再启动返回全对全通信，导致通信延迟暴露在关键路径上，降低了 GPU 利用率。我们提出了一种细粒度方法，通过瓦片级信令与调度将专家计算与第二次全对全通信重叠。我们的生产者-消费者协同设计结合了：（1）一个持久化的每进程计算内核（生产者），覆盖进程中所有本地专家，以消除重复的内核启动开销，并优先处理对远程关键的瓦片；（2）一个持久化的通信内核（消费者），运行在专门划分的一小部分流式多处理器（SM）上，在瓦片就绪时发出分段粒度的传输。我们的协同设计避免了对底层计算算子或通信原语的侵入式修改，使其能够实际用于提升多 GPU 系统上分布式 MoE 的执行效率。在 4 卡 A100 GPU 平台上，针对三个 MoE 模型与四种最先进的 MoE 系统进行评估，我们的方法实现了高达 2.64 倍的端到端加速和 2.74 倍的 MoE 层加速。与传统的无重叠基线相比，我们的方法在不同 GEMM 形状、路由模式以及广泛的生产者/消费者 SM 划分下，均能持续提升算子级和 MoE 层级的性能，同时保持正确性。

## Abstract
Mixture-of-Experts (MoE) architectures increase model capacity without proportionally increasing computation cost and have become a key building block for scaling large language models (LLMs) to trillion-parameter regimes. Efficient deployment of these MoE models relies on distributed execution across multiple GPUs, where each MoE layer involves two all-to-all communications: dispatching tokens to expert ranks and returning the expert outputs to their source ranks. Conventional MoE implementations launch this return all-to-all after expert compute completes, exposing communication latency on the critical path and reducing GPU utilization. We present a fine-grained approach that overlaps expert compute with the second all-to-all via tile-level signaling and scheduling. Our producer-consumer co-design combines: (1) a persistent per-rank computation kernel (producer) that covers all local experts on the rank to eliminate repeated kernel launch overhead and prioritizes remote-critical tiles, and (2) a persistent communication kernel (consumer) on a small dedicated partition of streaming multiprocessors (SMs) that issues segment-granular transfers as tiles become ready. Our co-design avoids intrusive changes to the underlying computation operators or communication primitives, making it practical for improving distributed MoE execution efficiency on multi-GPU systems. On a 4-A100 GPU platform, evaluated on three MoE models against four state-of-the-art MoE systems, our approach achieves up to 2.64x end-to-end speedup and 2.74x MoE-layer speedup. Compared with a conventional non-overlap baseline, our approach consistently improves both operator- and MoE-layer-level performance across varying GEMM shapes, router modes, and a broad range of producer/consumer SM partitions, while preserving correctness.

---

## 论文详细总结（自动生成）

好的，以下是对给定论文《Fine-grained Computation-Communication Overlap via Tile-level Signaling and Scheduling for Mixture-of-Experts》的详细中文总结。

### 1. 论文的核心问题与整体含义
*   **研究背景与动机**：Mixture-of-Experts (MoE) 架构已成为将大语言模型 (LLM) 扩展到万亿参数的关键技术，它通过每次仅激活部分专家来增加模型容量，同时控制计算成本。但在多 GPU 分布式环境中，MoE 层的两次全对全 (all-to-all) 通信（分发和收集）成为严重瓶颈，尤其是收集（返回）通信。传统实现中，必须先完成所有专家的计算，再启动收集通信，导致通信延迟完全暴露在关键路径上，GPU 长时间空闲，利用率低下。
*   **核心问题**：如何在 MoE 的“返回路径”上，将计算专家的 GEMM 操作与收集全对全通信进行**细粒度重叠**，从而隐藏通信延迟，同时避免对底层计算和通信库进行侵入式修改。
*   **整体含义**：论文提出了一种**生产者-消费者协同设计**，通过**瓦片级信令与调度**，将计算和通信作为并发的持久化内核执行，实现了非侵入式的高效重叠，显著提升了分布式 MoE 推理/前向传播的性能。

### 2. 论文提出的方法论
*   **核心思想**：让计算内核（生产者）和通信内核（消费者）在不相交的 SM（流式多处理器）分区上持久运行。生产者每完成一个输出瓦片 (tile) 的计算就立即通过信号通知，消费者轮询信号，一旦一个传输分段（多个瓦片）内的数据就绪，便立即发起远程写入，无需等待整个计算完成。
*   **关键技术细节**：
    *   **远程所有者对齐的行布局 (Remote-owner-aligned Row Layout)**：对每个本地专家的输入（令牌）按目标进程 (rank) 重新排序，使远程目标的行在前且按进程分组，本地的行在最后，并在组边界用零行对齐到瓦片高度 (`tile_M`)。这确保了每个输出瓦片只包含发往**同一个**目标进程的数据，是实现细粒度传输的前提。
    *   **远程优先的瓦片调度 (Remote-first Tile Schedule)**：构建一个统一的瓦片工作列表，优先调度所有远程瓦片，再调度本地瓦片；拥有更多远程行的专家被优先调度。这最大化了计算与通信的重叠窗口。
    *   **持久化的进程级 GEMM 内核 (Rank-wide Persistent GEMM Kernel)**：一个 CUDA 内核覆盖进程上的所有本地专家，通过统一的工作列表和动态切换专家参数（矩阵指针和尺寸），消除了为每个专家重复启动内核的开销。内核在写完每个瓦片后，通过 epilogue 发出一个轻量级的设备端完成信号。
    *   **持久化的通信内核 (Persistent Communication Kernel)**：运行在一小部分专用 SM 上，轮询就绪信号。当构成一个传输分段（通常是多个连续的包含完整行宽的“行带”，如下图所示）的所有瓦片就绪时，通过 NVSHMEM 非阻塞 `put` 操作，一次性将整个分段的数据写入目标进程的缓冲区。分段大小可配置（首尾分段为1个行带，中间分段可为多个行带），以在传输启动延迟和带宽利用率间取得平衡。
    *   **生产者-消费者协同调度**：两个内核以不同优先级运行在独立 CUDA 流上（消费者优先级更高），并通过 CUDA persistent kernel 机制占用固定的、不相交的 SM 集合，避免了 SM 级别的资源竞争。
*   **算法流程**：算法1描述了在每进程上构建远程所有对齐行布局和远程优先瓦片调度的过程。

### 3. 实验设计
*   **数据集/场景**：3个不同配置的 MoE 模型（M-GPT, M-BERT, M-Trans-xl），它们由标准的 Transformer 块替换部分或全部 FFN 层为 MoE 层构建，模型参数详见表1。
*   **基准测试目标 (Benchmark)**：主要目标是降低**端到端前向延迟**和**MoE 层延迟**，以及提升**吞吐量**。同时进行了**算子级（专家GEMM+收集通信）延迟**的分析。
*   **对比方法**：
    *   **与四个最先进的 MoE 系统对比**：FasterMoE, Megatron-CUTLASS, Megatron-TE (Transformer Engine), Tutel。
    *   **与传统的无重叠基线对比**：使用 PyTorch + cuBLAS + NCCL 的顺序执行实现。在此基础上集成论文的方法进行消融实验，以 pure 加速比衡量重叠机制本身的效果。在此基线对比中，进行了广泛的参数扫描，包括3种路由分布（balanced, moderate_skew, stress_skew）、多种专家数量（`E`）、两种 GEMM 形状、两种分段合并粒度（`mgb`）和12种生产者/消费者 SM 划分（`cCTA`）。

### 4. 资源与算力
*   **硬件平台**：单节点配备 **4块 NVIDIA A100 (40GB) GPU**，通过 NVLink 互联。
*   **软件环境**：CUDA 12.1，PyTorch 2.6.0，CUTLASS 3.9，NVSHMEM 3.6.5，NCCL 2.29.3。
*   **算力需求说明**：论文明确指出实验设定为每 GPU 一个进程 (`rank`)，专注于**推理/前向传播**。没有提及训练时长或大规模集群实验。

### 5. 实验数量与充分性
*   **实验组数**：实验非常全面，大致包括：
    *   **3个模型** × **5个系统** 的端到端和层性能对比。
    *   **1种单层微基准** × **5种专家数** (`E=4,8,16,32,64`) 的层延迟和重叠率对比。
    *   **与顺序基线的详细对比**：至少 **2 (GEMM形状) × 5 (E) × 3 (路由模式) × 2 (mgb) × 12 (cCTA)** = 720 个主实验配置，每个配置均测量了算子级、层级延迟和吞吐量。
    *   专门针对 **SM 资源争用**的分析，在图9中展示了所有 `cCTA` 配置下的层延迟。
    *   **正确性验证**：共进行了 **1440 次独立检查**，覆盖全部配置。
*   **充分性评估**：实验设计**非常充分、客观且公平**。它不仅与多个成熟 SoA 系统对比，还通过消融实验严格证明了所提重叠机制的独立价值。参数扫描全面，覆盖了影响性能的关键因素（计算量、通信量、负载均衡、SM 资源分配），并且对结论（如性能优势、资源争用影响）进行了深入分析。

### 6. 论文的主要结论与发现
*   **性能大幅提升**：相比四种 SoA MoE 系统，该方法在多数模型和配置上实现了更低的延迟，最高可达 **2.64 倍端到端加速**和 **2.74 倍 MoE 层加速**。
*   **有效的重叠**：与顺序执行基线相比，算子级加速最高可达 **3.01 倍**，MoE 层加速最高 **1.78 倍**。重叠率（隐藏的通信时间比例）始终很高，在许多情况下接近 100%。
*   **性能随专家数扩展**：随着每进程专家数 `E` 增加，通信量变大，该方法的性能优势更加明显。
*   **资源分区至关重要**：SM 分区对性能高度敏感。为消费者分配过少 SM（如2个）会导致性能急剧倒退，甚至慢于基线；存在一个适中的 SM 分配范围（如10-20个）能提供最佳性能。
*   **方法正确可靠**：在所有测试配置下，输出结果与基线一致（差异在 FP16 舍入误差范围内），证明了方法的正确性。

### 7. 优点
*   **非侵入式设计**：没有修改底层的 GEMM（如 CUTLASS）或通信库（如 NVSHMEM），仅通过包装和调度实现，工程实现上更简单、通用。
*   **细粒度高效重叠**：通过瓦片级信号和分段传输，实现了比传统分块流水线方法更精细的重叠粒度，同时避免了单瓦片传输导致的带宽浪费。
*   **最大化重叠窗口**：远程所有者对齐布局和远程优先调度确保通信数据尽早、连续地就绪，为消费者提供了最长的传输时间窗口。
*   **消除启动开销**：使用持久化内核覆盖所有专家，显著减少了内核启动的耗时。
*   **全面的实验评估**：覆盖了多种模型、实现、工作负载参数和资源分配情况，结论坚实，并提供了关于资源争用的宝贵洞察。

### 8. 不足与局限
*   **仅支持前向传播**：当前的设计和评估仅限于推理。将方法扩展到训练的反向传播过程，需要处理更复杂的梯度通信，是明确的未来工作。
*   **小规模验证环境**：实验仅在单节点的4 GPU上完成，未在更大规模（如跨节点）集群上验证该方法的可扩展性和网络拓扑适应性。
*   **存在计算开销**：远程所有者对齐布局中必要的零行填充会引入少量无效计算，在极端负载不均衡（skew）场景下，该开销的相对占比可能增大。
*   **缺乏自适应机制**：最优的 SM 分区和分段合并粒度 (`mgb`) 高度依赖于工作负载，论文并未提供自动化的选择策略，需要手动或经验调优。
*   **小模型效果不突出**：在隐藏层维度最小的 M-Trans-xl 模型上，该方法性能略有不足，说明对于计算量小、通信量也小的工作负载，细粒度重叠的固定开销可能无法被有效摊平。

（完）
