---
title: Transition-Aware Backend Dispatch for Edge LLM Inference
title_zh: 面向边缘LLM推理的转换感知后端调度
authors: "Alaaddin Goktug Ayar, Martin Margala"
date: 2026-07-19
pdf: "https://arxiv.org/pdf/2607.17415v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 面向边缘Transformer的PyTorch执行后端调度优化
tldr: "边缘LLM推理中，不同执行后端因算子形状差异导致显著性能波动，静态分配难以优化，而逐算子独立选择会引入高昂的设备切换开销。为此，本文提出过渡感知后端调度方法，通过结合当前算子特征和历史后端选择，在保留形状特定优势的同时避免频繁切换。在Jetson平台上对多种Transformer模型的评估显示，该方法相比最佳静态策略平均降低延迟17.4%、能耗14.4%及能量延迟乘积28.5%，并有效减少后端切换。该工作验证了形状和过渡上下文对边缘端推理调度的重要性，为高效部署提供了新思路。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17415v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1841, \"height\": 874, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17415v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 908, \"height\": 465, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17415v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 848, \"height\": 492, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17415v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 914, \"height\": 480, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17415v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 969, \"height\": 331, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17415v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 890, \"height\": 468, \"label\": \"Table\"}]"
motivation: 边缘LLM推理受限于静态后端分配无法利用形状依赖的性能差异，而独立逐算子选择又引入昂贵的设备与框架切换成本。
method: 提出过渡感知后端调度，结合当前算子特征与先前选择的后端，动态指派算子执行后端，以减少切换并保留形状特定优势，通过轨迹回放评估。
result: "在7个Transformer模型、四个算子类上，过渡感知调度相比最佳静态策略平均延迟、能耗、能量延迟乘积分别降低17.4%、14.4%、28.5%，并减少切换。"
conclusion: 结合算子形状和过渡上下文的选择性后端调度能有效提升边缘Transformer推理的效率，多数模型全面受益。
---

## 摘要
高效的大语言模型（LLM）在边缘平台上的推理不仅受模型大小限制，还受不同执行后端之间形状依赖的性能差异影响。静态后端分配无法利用这种差异，而独立的逐算子选择可能会引入代价高昂的设备和框架切换。本文提出了一种面向边缘Transformer推理的转换感知后端调度方法。该方法将当前算子特征与先前选择的后端相结合，以保留有益的特定形状选择，同时避免不必要的转换。从七个Transformer模型的完整模型推理运行中收集有序追踪，并在NVIDIA Jetson平台上对PyTorch eager CPU、PyTorch eager CUDA和ONNX Runtime CPU四种常见算子类进行基准测试。通过基于测量的追踪重放，利用观察到的算子开销和从实际后端切换测得的转换开销，对调度策略进行评估。支持的算子动态选择，而调度范围外的算子保持静态分配。在9,584个有序算子实例和278个精确形状组中，相对于最佳静态策略，转换感知调度平均将重放延迟、能耗和能量延迟积分别降低了17.4%、14.4%和28.5%。与算子本地选择相比，它还减少了切换。留一模型评估表明，七个留出模型中的六个在所有三个目标上均有改善，且所有七个模型在能耗上均有改善。这些结果表明，结合算子形状和后端转换上下文可以改善边缘Transformer工作负载的选择性后端调度。

## Abstract
Efficient large language model (LLM) inference on edge platforms is limited not only by model size, but also by shape-dependent performance differences across execution backends. Static backend assignment cannot exploit this variation, while independent per-operator selection can introduce costly device and framework switches. This paper presents a transition-aware backend dispatch approach for edge transformer inference. The approach combines current operator features with the previously selected backend to preserve beneficial shape-specific choices while avoiding unnecessary transitions. Ordered traces are collected from full-model inference runs of seven transformer models, and four common operator classes are benchmarked across PyTorch eager CPU, PyTorch eager CUDA, and ONNX Runtime CPU on an NVIDIA Jetson platform. The dispatch policies are evaluated through measurement-backed trace replay using observed operator costs and transition costs measured from actual backend switches. Supported operators are selected dynamically, while operators outside the dispatch scope retain a static assignment. Across 9,584 ordered operator instances and 278 exact shape groups, transition-aware dispatch reduces replayed latency, energy, and energy-delay product relative to the best static policy by 17.4%, 14.4%, and 28.5% on average, respectively. It also reduces switching relative to operator-local selection. Leave-one-model-out evaluation improves all three objectives for six of seven held-out models and improves energy for all seven. These results demonstrate that incorporating operator shape and backend-transition context can improve selective backend dispatch for edge transformer workloads.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
*   **核心问题**：在边缘设备上进行大语言模型（LLM）推理时，不同的执行后端（如PyTorch CPU、PyTorch CUDA、ONNX Runtime CPU）对同一计算操作（算子）的性能会因**张量形状（shape）**的不同而存在显著差异。传统的静态后端分配无法利用这种形状依赖性，而逐算子独立选择最优后端又会引入频繁的设备（CPU/GPU）和软件框架切换开销，反而可能降低整体性能。
*   **整体含义**：论文旨在解决边缘异构计算环境下，**如何在利用形状-后端最优匹配的同时，避免过度切换带来的开销**，从而提升Transformer模型在边缘端的推理效率（延迟、能耗、能效积）。

### 2. 论文提出的方法论
该方法的核心是**转换感知的后端调度**，它是一种选择性、动态的算子后端分配策略。

*   **核心思想**：调度决策不仅基于当前算子的特征（形状、类型等）和各个后端的预估成本，**还显式地将前一个算子的后端选择作为状态输入**。通过权衡切换成本与潜在收益，避免那些切换代价超过其局部收益的后端变更。
*   **关键技术流程**：
    1.  **Full-Model Inference Trace Collection（全模型推理追踪收集）**：在Jetson平台上运行7个Transformer模型，收集推理过程中所有算子的有序执行序列，并记录每个算子的上下文和张量形状特征。
    2.  **Exact-Shape Grouping（精确形状分组）**：将追踪到的9584个算子实例，根据其操作类型、张量维度、数据类型等完全相同的条件，去重为278个“唯一形状组”，避免重复测量。
    3.  **Multi-Backend Benchmarking（多后端基准测试）**：在NVIDIA Jetson物理平台上，对每个唯一形状组在三种后端下进行精确测量，记录延迟、功耗、能耗、能量延迟积（EDP）和正确性。
    4.  **Selector Training（选择器训练）**：
        *   **算子本地选择器**：仅基于当前算子特征，训练一个分类器（如随机森林、直方图梯度提升树）来预测成本最低的后端。
        *   **转换感知选择器**：引入前一个后端状态 \(b_{i-1}\) 作为额外特征。其成本函数变为 \(C_i(b_i) = C_{op i}(b_i) + C_{trans i}(b_{i-1}, b_i)\)，其中 \(C_{trans}\) 是从实际测量中得到的后端间切换成本（延迟和能耗）。选择器学习在给定先前后端时，哪个候选后端总成本最低。
    5.  **Transition-Aware Trace Replay（转换感知追踪重放）**：在原始模型的有序算子序列上，用训练好的选择器**贪婪地、因果地**为每个算子动态指派后端。总成本通过累加测量表中的算子成本和实际切换成本来计算，以此评估调度策略，而非在集成运行时中执行。
*   **模型范围**：调度仅作用于支持的算子类型（Linear, MLP Activation, RMSNorm, LayerNorm），范围外的算子保持静态分配。

### 3. 实验设计
*   **数据集 / 场景**：
    *   **模型**：使用7个不同规模的Transformer模型生成推理追踪，包括：Qwen2.5-0.5B, Qwen3-0.6B, TinyLlama-1.1B, SmolLM2-135M, SmolLM2-360M, OLMo2-1B, ModernBERT-base。
    *   **推理阶段**：覆盖了预填充（Prefill，序列长度16/32/64/128）和解码（Decode）阶段。
*   **Benchmark（基准测试平台）**：
    *   **硬件**：NVIDIA Jetson Orin Nano（25W模式）。
    *   **软件后端**：PyTorch eager CPU, PyTorch eager CUDA, ONNX Runtime CPU。
    *   **评价指标**：重放后的总延迟（ms）、总能耗（J）、能量延迟积（EDP, J·s），以及后端、设备、框架的切换次数。
*   **对比方法**：
    *   **静态后端分配**：为整个追踪固定分配单一最佳后端。
    *   **算子本地选择**：独立为每个算子选择成本最低的后端，不考虑切换。
    *   **转换感知调度（本文方法）**：基于历史上下文的选择策略。

### 4. 资源与算力
*   **使用算力**：论文明确指出，所有基准测试（算子成本、转换成本）均在单台**NVIDIA Jetson Orin Nano**物理设备上实测完成。实验不涉及大规模GPU集群训练深度学习模型，而是进行算子级的测量和轻量级分类器（如HistGBM）训练。
*   **未说明部分**：文中未提及选择器模型的具体训练时长，也未使用云端GPU算力。

### 5. 实验数量与充分性
*   **实验规模与组数**：
    *   涵盖**7个不同模型**的追踪，包含 **9,584** 个有序算子实例，去重后为 **278** 个唯一形状组，完成了 **834** 次“形状-后端”测量。
    *   **主实验**：在7个模型追踪上，分别对延迟、能耗、EDP三个优化目标，对比了三种调度策略（静态、算子本地、转换感知）的性能和切换次数。
    *   **消融实验**：通过对比“算子本地”和“转换感知”选择器，**剥离出转换感知的独立贡献**，证明了其主要作用是减少切换，对成本指标有小幅额外增益。
    *   **泛化性实验**：执行了**留一模型（LOMO）评估**，将每个模型单独作为测试集，其余模型用于训练，测试策略的跨模型迁移能力。
*   **充分性与公平性**：
    *   **比较充分**：实验覆盖了多种模型、算子类型、推理阶段和优化目标，并设计了消融和泛化性实验，论证较为全面。
    *   **比较客观公平**：评估基于物理设备实测的成本，并通过“追踪重放”保证在所有策略下算子顺序一致，排除运行时环境干扰。通过GroupKFold按形状组划分验证集，防止数据泄露。

### 6. 论文的主要结论与发现
*   **主要性能提升**：相比最佳静态后端策略，转换感知调度在重放评估中平均降低了**17.4%的延迟、14.4%的能耗和28.5%的能量延迟积（EDP）**。
*   **转换感知的价值**：性能提升主要源自于形状感知的算子级后端特化，而转换感知机制的主要贡献在于**显著减少了后端、设备与框架的切换次数**（分别降低最高14.9%、19.7%、17.6%），同时对延迟、能耗、EDP有少量额外增益。
*   **跨模型泛化能力**：留一模型评估显示，该策略在大多数（6/7）模型上仍能带来正面收益，特别是能耗在所有模型上均有改善，表明学习到的“形状-后端-转换成本”关系具有不错的迁移性。仅在结构差异较大的编码器模型（ModernBERT）上显示出局限性。

### 7. 优点
*   **问题建模符合直觉**：论文精准地指出了边缘推理中“形状-后端适配”和“切换开销”这一对矛盾，并提出结合历史状态的决策方案，逻辑清晰。
*   **评估方法可靠**：采用基于物理设备实测的“追踪重放”评估法，避免了复杂集成运行时带来的不确定性，能够纯粹地衡量调度策略本身的潜力，方法论严谨。
*   **实验设计全面**：涵盖了多个模型、指标和优化目标，并包含消融和泛化性实验，研究结果说服力强。
*   **实用性强**：方法基于现有的框架和库（PyTorch, ONNX Runtime），不涉及底层内核修改，易于部署。

### 8. 不足与局限
*   **非真实运行时评估**：所有结果均来自“追踪重放”，**未在集成的异构运行时系统中进行端到端验证**。实际系统中的框架开销、内存管理、动态时钟频率等因素可能影响最终收益。
*   **算子覆盖范围有限**：当前调度范围仅限四种operator class，未覆盖注意力机制、KV缓存操作、量化算子等Transformer推理的关键部分，可能错失更多优化机会。
*   **平台和后端单一**：仅在NVIDIA Jetson Orin Nano单一平台和三种eager模式后端上验证，未涉及更广泛的边缘硬件（如手机NPU/DSP）或更高效的推理引擎（如TensorRT, ONNX Runtime CUDA）。
*   **选择器决策方式**：使用贪心（causal）而非全局序列最优的方式做决策，意味着理论上可能存在更优的全局后端分配方案。
*   **泛化性边界**：现代BERT（编码器架构）上的负面迁移结果表明，该方法的跨架构泛化能力尚有限，可能需要在更多样化的模型上进行训练。

（完）
