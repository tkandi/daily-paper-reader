---
title: Entropy-Regularized Probabilistic Gates for Sparse Model Discovery in Scarce-Data Federated Learning
title_zh: 基于熵正则化概率门控的稀缺数据联邦学习中的稀疏模型发现
authors: "Krishna Harsha Kovelakuntla Huthasana, Alireza Olama, Andreas Lundell"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2607.00275v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 联邦学习中基于熵正则化概率门的稀疏模型发现
tldr: 联邦学习在数据异构下训练稀疏模型对小样本高维场景易过拟合，且缺乏对参数空间不确定性的探索。本文研究熵正则化的概率门以维持不确定性，防止过早锁定稀疏结构，增强探索。在合成和真实数据上，该方法测试性能和稀疏恢复精度均优于Fed-IHT与剪枝FedAvg。为稀缺数据下联邦稀疏模型发现提供了新视角，强调保持训练不确定性的重要性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00275v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1166, \"height\": 851, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00275v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1638, \"height\": 974, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00275v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1535, \"height\": 1050, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00275v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1529, \"height\": 464, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.00275v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1167, \"height\": 328, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00275v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1647, \"height\": 260, \"label\": \"Table\"}]"
motivation: 联邦学习在小样本高维下训练稀疏模型易过拟合，且现有剪枝方法缺乏对参数空间不确定性的探索。
method: 在概率门稀疏框架中引入熵正则化，通过最大化门分布熵保持不确定性，避免过早锁定稀疏支持。
result: 在合成和真实基准上，该方法在测试性能及稀疏模式恢复准确度上一致优于Fed-IHT和剪枝后FedAvg。
conclusion: 熵正则化概率门为稀缺数据联邦稀疏学习提供了更鲁棒的策略，强调在稀疏训练中维持不确定性的重要性。
---

## 摘要
联邦学习是一种分布式机器学习范式，允许多个客户端在不共享数据的情况下进行协作。在数据异构和部分客户端参与的条件下，联邦学习面临挑战。学习稀疏模型有助于提高联邦学习中的通信和计算效率，但在小样本高维情形（d >> N）中尤为困难，因为优化可能产生无法泛化到未见测试数据的参数配置。虽然基于幅度的剪枝未考虑参数空间中的不确定性探索，但采用概率门控和L0约束的公式化方法允许在训练过程中从竞争的稀疏配置中进行采样。在本工作中，我们研究了门控分布的熵正则化作为一种机制，通过防止过早锁定稀疏结构，在稀疏联邦优化中保持不确定性。我们考察了其在数据异构、客户端参与异构和稀疏性下的影响。在合成和真实基准上的实验表明，与联邦迭代硬阈值（Fed-IHT）和密集联邦平均训练后剪枝相比，该方法在测试数据的统计性能和稀疏恢复精度上均有一致的提升。

## Abstract
Federated Learning (FL) is a distributed machine learning (ML) paradigm with collaboration among multiple clients without sharing data. FL is challenging under data heterogeneity and partial client participation. Learning sparse models is useful for communication and computational efficiency in FL, but it is especially difficult in the small-sample high-dimensional regime (d >> N) where optimization can yield parameter configurations that fail to generalize to unseen test data. While magnitude-based pruning doesn't account for uncertainty exploration in the parameter space, a formulation with probabilistic gates and an L0 constraint allows sampling from competing sparse configurations during training. In this work, we study entropy regularization of gate distributions as a mechanism to maintain uncertainty in sparse federated optimization by preventing early commitment to sparse support. We examine its impact under data heterogeneity, client participation heterogeneity, and sparsity. Experiments on synthetic and real-world benchmarks show consistent improvements over federated iterative hard thresholding (Fed-IHT) and pruning after dense federated averaging (FedAvg) training, both in statistical performance on test data and in sparsity recovery accuracy.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **问题背景**：联邦学习（FL）是一种隐私保护型分布式机器学习范式，其面临数据异构、部分客户端参与等挑战。在实际应用中，发现并训练稀疏模型可降低通信与计算开销，但在“总样本量小、参数维度高”（*d* ≫ *N*）的稀缺数据场景下，传统稀疏优化极易陷入泛化性差的局部解。
- **核心问题**：现有稀疏 FL 方法（如迭代硬阈值、训练后剪枝）大多依赖参数幅值，缺乏对稀疏支持（哪些参数应为非零）的不确定性探索，容易过早锁定不理想的稀疏配置，在小样本高维下表现尤为脆弱。
- **整体含义**：本文提出将“熵正则化”作用于概率门控分布，以主动维持稀疏结构的不确定性，防止过早收敛，从而在通信效率与稀疏恢复精度之间取得更好平衡，为稀缺数据联邦学习中的稀疏模型发现提供了新思路。

### 2. 论文提出的方法论：核心思想、关键技术细节、算法流程
- **核心思想**：在已有**L0 稠密约束的概率门控**稀疏 FL 框架上，加入**门分布与先验分布的 KL 散度（相对熵）**作为正则项。该正则化鼓励门分布保持较大熵，促进在训练中探索更多可能的稀疏子网络，避免过早锁定某个固定的稀疏支持。
- **关键技术细节**：
  - **模型参数化**：使用 **Hard Concrete 分布**近似伯努利门控 *z*，有效参数为 *θ* = *θ̃* ⊙ *z*。门参数 *φ* = log *α* 可导，支持重参数化梯度。
  - **优化目标**：构建含熵正则的拉格朗日函数（公式 4）：
    *L̂*(*θ̃*, *φ*, *λ*) = Σ 客户端损失 + *λ* (期望活跃门数 - 目标密度) + *T* · Σ KL(*q*(*z*ⱼ|*φ*)||*p*(*z*ⱼ|*φ*ⁱⁿⁱᵗ))
    其中 *T* 为熵惩罚温度，可恒定或衰减；*p*(*z*) 为初始化的先验 Hard Concrete 分布。
  - **算法流程 (E‑FLoPS)**：在 FedAvg 框架下，每一轮：
    1. 每个客户端本地 SGD 更新 *θ̃*, *φ*（包含 KL 和 L0 约束项的梯度），及 *λ* 的梯度上升（满足约束后重置为 0）。
    2. 服务器按客户端样本量加权聚合 *θ̃* 和 *z*，由 *z* 恢复 *φ*。
    3. 可选的通信压缩：在设定轮数后，只保留门值前 *m* (对应目标密度) 的参数，其余用平均值代替，序列化传输。
- **特点**：概率门不依赖数据，训练后可直接采样；利用 KL 散度闭合形式计算稳定。

### 3. 实验设计：数据集 / 场景、基准方法、对比方法
- **数据集与场景**：
  - **合成线性回归**：*d* = 1000，不同 *N*/|*θ*| 比率（0.24～2.0），Toeplitz 协方差（ρ=0.2），SNR=20，真实稀疏度 95%。数据按属性偏移（仿射变换）和客户端样本量异构（Dirichlet 分区）分配到多个客户端，每轮 60% 客户端随机参与。
  - **MNIST 图像分类**：CNN（44,426 参数），数据下采样至 1184 训练样本（*N*/|*θ*|≈0.02），目标稀疏度 97.5%，标注异构（Dirichlet），60% 客户端随机参与。
  - **Golub 白血病基因表达分类**：72 样本、3571 特征（*N*/|*θ*|≈0.02），训练二分类 softmax 分类器，目标稀疏度 99.9%。5 客户端，全参与，仅利用自然的数据分割异构。
- **基准方法**：
  - **FedAvg (密集训练后剪枝)**：训练中无稀疏约束，每轮取 top‑m 参数评估。
  - **Fed‑IHT**：每轮本地更新后进行迭代硬阈值剪枝。
  - **集中训练**（近似上界）。
- **对比指标**：测试 R²/准确率、稀疏恢复准确度 *A*(*θ*)、通信数据量、推理 MACs 减少。

### 4. 资源与算力
- 硬件：**Apple MacBook（M4 Pro 芯片，12 核 CPU，24 GB 统一内存）**，未提及 GPU。
- 算力规模较小，所有实验均在本地 MacBook 完成。文中未报告训练时长，但从设备规格可推断属于轻量级实验，适合原型验证，未涉及大规模 GPU 集群。

### 5. 实验数量与充分性
- **合成实验**：覆盖 7 个不同 *N*/*d* 比率，每个实验重复 **30 次**；并对 *η*_θ̃ 和 *η*_φ 进行了学习率敏感性分析。
- **真实数据实验**：
  - MNIST：1 种设定，展示精度曲线与通信/MACs 压缩。
  - 白血病：分别固定数据分割种子、运行种子各 **30 次**重复，还进行了不同数据分割的 30 次实验，并用配对 t 检验验证统计显著性。同时对比了不同方法选择的 top 基因及生物学意义。
- 实验从异质性（属性偏移、样本量异构、标签异构）、模型类型（线性、CNN、softmax）、指标（性能、恢复率、通信、计算量）多方面进行了评估，**较为充分、客观且公平**，尤其在极低数据比条件下进行了稳健性验证。

### 6. 论文的主要结论与发现
- **性能与稀疏恢复均提升**：在所有 *N*/*d* 比率和任务上，E‑FLoPS 的测试 R²/准确率均优于 Fed‑IHT 和 FedAvg 剪枝，并在多数比率下实现更高的稀疏支持恢复精度（尤其在极低比率时仍保持优势）。
- **不确定性探索有效**：熵正则化使优化在早期不轻易锁死稀疏结构，最终收敛到更优的泛化配置，在白血病数据上选出的基因与已知癌症扩散机制相符。
- **通信效率可比：** E‑FLoPS 的通信开销（2×ρ|θ|）虽略高于 Fed‑IHT（ρ|θ|），但远低于密集 FedAvg，是通信高效的稀疏训练方案；同时推理 MACs 显著减少。
- **学习率鲁棒性**：E‑FLoPS 在较宽的学习率范围内性能稳定。

### 7. 优点：方法或实验设计上的亮点
- **方法论创新**：首次将门分布熵正则化引入 FL 的 L0 稀疏学习中，利用不确定性探索缓解小样本高维下过早收敛，思想直观且可微分，易于集成。
- **理论与实践结合**：公式清晰，给出 KL 闭合形式，算法流程集成在 FedAvg 中，通信压缩策略简单实用。
- **实验全面**：涵盖合成与真实数据，充分考虑 FL 中多种异构性（属性、样本量、标签），并在极端样本比（N/|θ|≈0.02）下验证；统计检验和基因生物学验证增强了结论可信度。
- **评估维度多**：同时考量统计性能、稀疏恢复精度、通信成本、推理计算量，体现了稀疏模型的综合价值。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **实验规模受限**：模型参数量仅数万级，均在小规模样本上测试；未在更大规模（如 ResNet、大语言模型）或更复杂联邦场景下验证，泛化到工业级系统存疑。
- **对比方法偏少**：仅与 Fed‑IHT 和 FedAvg 剪枝比较，未纳入其他不确定性正则化或 Bayesian 稀疏学习方法（如 VarBayes 等），也未与更先进的 FL 稀疏方法（如稀疏通信压缩、结构化剪枝）对比。
- **温度 T 的选择**：文中提到 T 初始化为 1/|θ|，并可按经验调整，但缺乏对 T 调度策略的系统消融实验，其敏感性未充分揭示。
- **仅针对非结构化稀疏**：门控方式实现的是元素级稀疏，未探索结构化稀疏，后者可能对硬件加速更友好。
- **隐私保护未深化**：虽强调 FL 不共享数据，但概率门通信是否会泄露额外信息（如门分布）缺乏分析。
- **理论分析不足**：未给出熵正则化对收敛速度或泛化误差的理论保证，主要靠实验观察。

（完）
