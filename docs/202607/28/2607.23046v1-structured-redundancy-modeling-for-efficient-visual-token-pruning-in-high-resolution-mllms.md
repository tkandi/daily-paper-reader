---
title: Structured Redundancy Modeling for Efficient Visual Token Pruning in High-Resolution MLLMs
title_zh: 面向高分辨率MLLMs的高效视觉令牌剪枝的结构化冗余建模
authors: "Jouwon Song, Woohyeong Kim, Kyeongbo Kong"
date: 2026-07-25
pdf: "https://arxiv.org/pdf/2607.23046v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 高效视觉token剪枝以降低高分辨率多模态大模型延迟
tldr: 高分辨率多模态大模型因视觉token爆炸引发延迟瓶颈，现有迭代剪枝方法的选择开销导致理论加速无法兑现。本文提出Single-Forward Pruner，通过结构化冗余建模将重要性选择嵌入单次前向传播，利用语义岭杠杆和方向掩蔽消除冗余。在大幅减少选择时间至2.5ms的同时，保持竞争性能，成功将token缩减转化为实际推理加速。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23046v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1095, \"height\": 805, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23046v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1179, \"height\": 685, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23046v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1035, \"height\": 593, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23046v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1195, \"height\": 666, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23046v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1198, \"height\": 644, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23046v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1191, \"height\": 592, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23046v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 787, \"height\": 236, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23046v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1213, \"height\": 212, \"label\": \"Table\"}]"
motivation: 高分辨率MLLMs的视觉token激增导致延迟瓶颈，现有迭代剪枝的选择开销阻碍实际加速。
method: 提出SFPruner，通过单次前向的语义岭杠杆和方向掩蔽，将冗余控制嵌入评分空间，实现非迭代的冗余感知剪枝。
result: 在Qwen2.5-VL上，选择时间从112.4ms降至2.5ms（512 tokens），大幅提速并保持竞争性能。
conclusion: 结构化冗余建模有效将理论token缩减转化为实际加速，为非迭代视觉剪枝提供了高效方案。
---

## 摘要
近期的高分辨率多模态大语言模型（MLLMs）在每个输入中生成数千个视觉令牌，导致视觉令牌爆炸，带来严重的延迟瓶颈。尽管令牌剪枝能够缓解这一问题，但最先进的子集优化方法通常依赖于迭代式构建子集，以联合捕获视觉多样性和指令相关性。随着视觉令牌数量的增加，这种顺序依赖性引入了显著的筛选开销，严重限制了理论FLOPs降低转化为实际推理加速。为解决这一局限，我们提出了单次前向剪枝器（SFPruner），一种视觉令牌剪枝的结构化重构方法，将冗余控制直接嵌入评分空间，从而绕过了迭代组合优化的需要。我们的非迭代框架通过两个互补机制在单次前向传递中实现了冗余感知的重要性选择。首先，为在协方差层面衰减冗余，我们引入了语义引导的岭杠杆率方案。通过融合指令相关性和视觉显著性，该机制抑制了主导协方差方向并减轻了表示偏差。其次，基于排名的方向性掩码通过非对称相似性竞争解决残余重叠，其中得分较高的令牌通过并行张量操作显式地抑制冗余的低分替代令牌。广泛的评估表明，我们的方法保持了稳定的筛选成本，在Qwen2.5-VL的512个令牌时，将令牌筛选过程减少了最多110毫秒，从112.4毫秒降至仅2.5毫秒。这种结构效率成功地将理论令牌缩减转化为实际的推理加速，同时在激进压缩下保持了与最先进技术相竞争的性能。

## Abstract
Recent high-resolution Multimodal Large Language Models (MLLMs) generate thousands of visual tokens per input, leading to a visual token explosion that introduces severe latency bottlenecks. While token pruning mitigates this issue, state-of-the-art subset-optimization methods typically rely on iterative subset construction to jointly capture visual diversity and instruction relevance. As visual token counts scale, this sequential dependency introduces significant selection overhead, severely limiting the translation of theoretical FLOPs reductions into actual wall-clock speedups. To address this limitation, we propose Single-Forward Pruner (SFPruner), a structural reformulation of visual token pruning that embeds redundancy control directly into the scoring space, bypassing the need for iterative combinatorial optimization. Our non-iterative framework achieves redundancy-aware importance selection in a single forward pass through two complementary mechanisms. First, to attenuate redundancy at the covariance level, we introduce a semantics-guided ridge leverage scheme. By integrating instruction relevance and visual saliency, this mechanism suppresses dominant covariance directions and mitigates representation bias. Second, ranking-based directional masking resolves residual overlap through asymmetric similarity competition, where higher-scoring tokens explicitly suppress redundant lower-scoring alternatives via parallel tensor operations. Extensive evaluations demonstrate that our approach maintains stable selection costs, reducing the token selection process by up to 110 ms, from 112.4 ms to just 2.5 ms at 512 tokens in Qwen2.5-VL. This structural efficiency successfully translates theoretical token reductions into tangible inference speedups while preserving highly competitive performance against state-of-the-art techniques under aggressive compression.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：当前高分辨率多模态大语言模型（MLLMs）在处理单张图像时往往产生数千个视觉token，导致自注意力计算的开销呈平方级增长，形成严重的推理延迟瓶颈。
- **现有方法的困境**：视觉token剪枝技术被广泛用于缓解该问题，但主流方案分为两类：
  - **启发式方法**（如基于注意力或相似度排序）虽然速度快，但无法有效消除空间冗余，在激进剪枝下性能退化严重。
  - **子集优化方法**（如DPP、多样性最大化等）通过迭代式搜索同时考虑重要性和多样性，能够更好地保留准确率，但其**顺序依赖的迭代选择过程**本身会带来显著的计算开销，且开销随token数量线性增长，导致**理论FLOPs降低难以转化为实际推理加速**。
- **整体含义**：论文旨在打破这一“速度-性能”折衷，提出一种**结构化冗余建模**方法，将冗余控制直接嵌入到token评分空间中，从而在**单次前向传播**中完成冗余感知的重要性选择，彻底消除迭代依赖，使token缩减能够稳定地转化为端到端的实际加速。

### 2. 论文提出的方法论
核心思想是**Single-Forward Pruner (SFPruner)**，这是一个非迭代的视觉token剪枝框架，由三个紧密衔接的步骤构成，全部通过并行张量操作实现。
- **步骤1：语义引导 − 建立基础重要性**
  - 融合**指令相关性**（文本-视觉token余弦相似度，经温度缩放归一化）与**视觉显著性**（从视觉编码器自注意力中提取的全局token对各token的注意力权重）。
  - 通过线性融合 `S_guide = α * S_rel + (1-α) * S_attn` 并做最小-最大归一化，得到每个视觉token的指令感知基础分数 `S̃_guide`。对于无独立文本编码器的模型（如Qwen2.5-VL），仅使用视觉显著性。
- **步骤2：协方差层面的冗余衰减 − 语义引导的岭杠杆得分 (SG-RLS)**
  - 构建视觉token的特征协方差矩阵 `C = V^T V`。
  - 计算每个token的岭杠杆得分 `ℓ_i = v_i (C + λI)^{-1} v_i^T`。该得分会**自动抑制主导协方差方向**（即普遍存在的冗余模式），给予结构独特token更高分。
  - 将结构得分与语义引导分数逐元素相乘：`S_SG-RLS,i = ℓ_i · S̃_guide,i`。此乘法起到“软与门”作用，确保只有**结构独特且语义相关**的token才能获得高分。
- **步骤3：成对层面的冗余抑制 − 基于排名的方向性掩码**
  - 计算token间的余弦相似度矩阵 `C_sim`。
  - 依据SG-RLS得分构建方向性掩码：`M_ij = I(S_SG-RLS,j > S_SG-RLS,i)`，即只允许高分token抑制低分token（非对称竞争）。
  - 对每个token计算**方向性抑制惩罚** `P_i = 1 - max_j (M_ij · C_sim,ij)`，若存在高分且高度相似的token则惩罚增大。
  - 最终得分 `S_final,i = S_SG-RLS,i · P_i`，然后直接通过单次top-k操作选取最终子集，全程无迭代。

### 3. 实验设计
- **模型架构**：覆盖三种代表性MLLM家族，验证通用性：
  - **LLaVA-NeXT-7B**：高分辨率多图像块（AnyRes）设置。
  - **Qwen2.5-VL-7B**：单序列稠密编码高分辨率设置。
  - **LLaVA-Video-7B**：多帧视频处理设置。
- **基准与数据集**：
  - **图像理解**：VQAv2, GQA, TextVQA, MME, ScienceQA (SQA), AI2D, HBench, MMB, MMB_CN, POPE, MMStar 等。
  - **视频推理**：Video-MME, MLVU, LongVideoBench。
- **对比方法**：
  - **启发式方法**：SparseVLM, PruMerge/PruMerge+, VisionZip, VisPruner, VisionSelector (部分为学习型)。
  - **子集优化方法**：DivPrune, CDPruner, D²Pruner。
- **评测指标**：绝对任务精度、相对于未剪枝基线的性能保留率（Rel.%）、以及关键的选择延迟（毫秒，隔离剪枝模块执行时间）。

### 4. 资源与算力
- 文中明确说明**所有延迟测量均在单张NVIDIA RTX 4090 GPU上进行**，并使用同步CUDA计时以确保一致分析。
- 由于SFPruner是完全**无需训练**的剪枝方法，因此未涉及任何训练算力（如GPU数量、训练时长等）。论文使用的计算资源仅用于推理评估和性能剖析。

### 5. 实验数量与充分性
- **实验组数概览**：
  - 核心对比实验：分别在三个模型上，以多个token保留率（如640、320、160 tokens；40%、20% tokens等）对比了七种以上现有方法，总计生成**3张主要结果表格**（Table 1-3）。
  - 极端可扩展性测试：在Qwen2.5-VL上构造9216个token的超高分辨率场景，对比分析FLOPs、显存、剪枝耗时等（Table 4）。
  - 微架构剖析：对多个token数量级（最高至16384）进行端到端延迟与峰值显存分析（Figure 3）。
  - 消融实验：在LLaVA-NeXT-7B上系统比较了SG-RLS与方向性掩码的作用，以及不同选择策略（Top-K、序贯搜索、方向性掩码）的组合效果（Table 5）。
- **充分性与公平性**：实验涵盖图像、视频任务，从主流到激进压缩率，全面对比了同期最新的启发式与优化方法。所有对比均在同一基线模型和统一保留预算下进行，延迟测量方法公开透明。消融实验有力证明了各模块的贡献，整体实验设计充分、客观、公平。

### 6. 论文的主要结论与发现
- SFPruner成功将**冗余感知的重要性选择实现为非迭代的单次前向过程**，完全消除了传统子集优化方法中随token数增长的选择延迟。
- 该方法展现出**极高的结构效率**：在Qwen2.5-VL上选择512个token的时间从CDPruner的112.4ms骤降至2.5ms；即使在9216个token的极端场景下，剪枝开销仅28ms，远低于优化方法的500+ms。
- 性能保留高度竞争：在LLaVA-NeXT上保留约22%的token即达到**100%的原始性能**；在Qwen2.5-VL上保留40%的token时保留99.0%相对性能；在视频任务上也保持最优或次优性能，同时提供显著加速。
- 整体结论是，通过结构化的协方差衰减与方向性掩码，冗余感知token剪枝无需迭代构建，能够将理论token缩减稳定、直接地转化为实际推理加速，为高分辨率多模态推理提供了一种可扩展的高效方案。

### 7. 优点（方法或实验设计亮点）
- **根本性解决迭代瓶颈**：非迭代设计使剪枝开销保持恒定，不受输出token数量影响，首次在优化级冗余控制下实现与启发式方法可比的选择速度。
- **双层次冗余建模**：协方差层面的岭杠杆得分处理全局表示偏差，成对层面的方向性掩码处理局部token重叠，两者互补且均通过并行矩阵运算完成。
- **无需训练且架构泛化**：方法完全即插即用，不依赖额外训练，并覆盖了多patch、单序列、多帧视频等多种MLLM架构。
- **实验全面且剖析深入**：不仅提供与SOTA的精度对比，更通过极端压力测试和微架构剖析，真实反映了方法在“将理论FLOPs减少转化为端到端加速”上的显著优势，回应了领域内的一大痛点。

### 8. 不足与局限
- **协方差求逆的计算依赖**：虽然利用Woodbury恒等式将矩阵求逆的维度从N×N降至D×D（特征维度），但当视觉特征维度D本身极高时，该步仍可能带来计算开销，文中未讨论对此的进一步提升。
- **语义引导的依赖**：对于缺少明确全局token或文本编码器的非典型架构，需要构造代理显著性信号，这可能影响方法的直接迁移有效性（如文中对Qwen2.5-VL已做适配，但未探索更多变体）。
- **实验覆盖的局限性**：测评主要集中于基准测试（图像问答、视频推理），未在开放式多轮对话或更复杂的交互场景（如具身智能）中验证；此外，对比方法中包含学习型选择器，但未深入分析SFPruner与学习型方法的长期性能差异（如面对分布外数据时的鲁棒性）。
- **压缩极限未探索**：激进压缩下的性能保留虽然优秀，但论文未进一步分析当保留率极低（如低于5%）时，方法是否仍优于启发式合并等替代策略。

（完）
