---
title: Stable FP4 Training via Transposition-Invariant Block Quantization
title_zh: 基于转置不变分块量化的稳定FP4训练
authors: "Mehdi Rahimifar, Amin Darabi, Mehran Taghian Jazi, Xing Huang, Yao Wang, Zhijun Tu, Yufei Cui, Yunke Peng, Hongliang Li"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24953v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 稳定的FP4训练框架，通过降低精度提升LLM训练效率
tldr: "将训练精度进一步降低到4位浮点(FP4)时，现有微缩放方法因张量转置导致前后向缩放不一致，引发优化不稳定。为此提出转置不变的2D块FP4量化框架，强制前向与反向传播共享统一缩放因子，并配合截断自由缩放与随机舍入控制误差。在多达7B参数的稠密LLM和30B MoE模型上实现稳定端到端FP4训练，困惑度及下游任务精度下降均小于1.3%，验证了缩放一致性是规模化FP4训练的充分条件。该工作为超低精度高效训练提供了简单有效的实践路径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24953v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1508, \"height\": 1012, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24953v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1460, \"height\": 914, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24953v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1458, \"height\": 714, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24953v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1690, \"height\": 1217, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24953v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 936, \"height\": 603, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24953v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1532, \"height\": 426, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24953v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1020, \"height\": 504, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24953v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1124, \"height\": 504, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24953v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1147, \"height\": 266, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24953v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1676, \"height\": 1403, \"label\": \"Table\"}]"
motivation: 现有1D块量化中，张量转置使前向和反向传播对相同数据分配不同缩放因子，导致有偏且不稳定的梯度更新。
method: 提出2D块FP4量化实现转置不变的缩放一致性，并结合截断自由缩放与随机舍入，注意力投影采用MXFP8构成混合精度框架。
result: "在7B稠密模型和30B MoE上训练100B tokens，稳定匹配BF16性能，困惑度与下游精度退化均小于1.3%。"
conclusion: 强制前后向缩放一致足以支撑实用化FP4训练，为低精度高效LLM训练开辟了简单可行路径。
---

## 摘要
降低训练精度是提升大语言模型训练效率的关键手段，但将精度从FP8进一步推至4位浮点（FP4）仍因优化过程中的不稳定性而充满挑战。我们揭示了现有微缩放方法中不稳定性的一个根本来源：张量转置引起的缩放不一致。在传统的1D分块量化中，前向和反向传播在转置后会给相同的值分配不同的缩放因子，导致梯度更新有偏且不稳定。为解决这一问题，我们提出了一种基于2D分块FP4量化的低精度训练框架，该框架强制实现转置不变的缩放，并保持前向与反向计算之间的一致性。我们进一步结合无截断缩放和随机舍入来控制量化误差并维持无偏梯度。为处理注意力机制的敏感性，我们对查询和键投影采用MXFP8量化，形成实用的混合精度设计。我们在多达7B参数的稠密大语言模型和一个30B的混合专家模型上评估了方法，训练数据量高达100B词元。在所有设置下，我们的方法实现了稳定的端到端FP4训练，并紧密匹配BF16性能，困惑度和下游准确率的退化小于1.3%。这些结果表明，强制前向-反向缩放一致性足以实现大规模实用的FP4训练，为更高效的大语言模型训练提供了一条简单有效的途径。

## Abstract
Reducing training precision is a key lever for improving the e ciency of large language model (LLM) training, but pushing beyond FP8 to 4-bit oating point (FP4) remains challenging due to instability during optimization. We identify a fundamental source of this instability in existing microscaling approaches: scale inconsistency induced by tensor transposition. In conventional 1D block quantization, forward and backward passes assign di erent scaling factors to the same values after transposition, leading to biased and unstable gradient updates. To address this issue, we propose a low-precision training framework based on 2D block FP4 quantization, which enforces transposition-invariant scaling and preserves consistency between forward and backward computations. We further combine this with truncation-free scaling and stochastic rounding to control quantization error and maintain unbiased gradients. To handle the sensitivity of attention mechanisms, we adopt MXFP8 quantization for query and key projections, yielding a practical mixed-precision design. We evaluate our method on dense LLMs up to 7B parameters and a 30B Mixture-of-Experts model, trained on up to 100B tokens. Across all settings, our approach achieves stable end-to-end FP4 training and closely matches BF16 performance, with less than 1.3% degradation in perplexity and downstream accuracy. These results demonstrate that enforcing forwardbackward scaling consistency is su cient to enable practical FP4 training at scale, providing a simple and e ective pathway toward more e cient LLM training.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究背景**：大语言模型训练的计算与能耗成本急剧上升，降低数值精度是提升训练效率的核心手段。目前 FP8 训练已具备实用性，但进一步降到 4 位浮点（FP4）依然面临优化不稳定的严峻挑战。
- **核心问题**：论文指出，现有微缩放（microscaling）方法（如 MXFP4、NVFP4）存在一个根本缺陷：**1D 分块量化在张量转置后会破坏缩放一致性**。前向传播与反向传播对同一组值分配不同的缩放因子，导致梯度估计有偏，从而破坏了训练稳定性。
- **整体含义**：要将 FP4 训练推向实用，不能只关注量化误差的大小，而必须从根本上消除前向与反向计算之间的缩放不一致。论文的目标是提供一种能保证转置不变性的量化框架，实现稳定、可扩展的 FP4 训练。

### 2. 方法论

- **核心思想**：**利用 2D 正方形块代替 1D 块来实现转置不变的缩放**。2D 方块在转置后仍保持自身的结构，因此同一组值的块最大值不变，所分配的缩放因子也自然保持不变，从而强制前后向缩放一致。
- **关键技术细节**：
    - **2D 块 FP4 量化**：对线性层的权重和梯度采用 `32×32` 的 2D 方块进行分组量化；对激活值保留 1D 布局，以避免混淆不同通道的语义信息。
    - **无截断缩放（Truncation-Free Scaling）**：尺度因子计算为 `S = 2^⌈log2(2M/(Qp−Qn))⌉`，确保块内所有值都不超出 FP4 的表示范围，彻底消除溢出和截断失真。
    - **随机舍入（Stochastic Rounding）**：在反向传播中对梯度使用随机舍入（`E[round(x)] = x`），以保证梯度估计在统计上无偏。
    - **混合精度注意力**：查询（Q）和键（K）的投影对精度极度敏感，因此单独采用 MXFP8 量化，其他所有线性层（MLP、V/O 投影等）仍使用 2D-FP4。这种设计在保持稳定性的同时，仅对约 12.5% 的参数使用更高精度。
    - **梯度传播**：使用直通估计器（STE），将量化操作的导数近似为单位映射。

### 3. 实验设计

- **模型与场景**：选择了三类代表模型：
    - 稠密模型：OLMo‑1B、OLMo‑7B。
    - 混合专家模型：Qwen 30B MoE。
- **训练设置**：所有模型均从头训练，统一使用 100B 词元的语料（具体语料集文中未赘述），序列长度 2K，优化器为 AdamW。
- **对比基准**：
    - **BF16**：全精度训练基线。
    - **2D‑FP4**：本文所提的纯 2D 块 FP4 训练。
    - **2D‑FP4 + MXFP8**：加入混合精度注意力的变体。
- **评估基准**：
    - **语言建模**：在 Wikitext、Pile、C4 上的困惑度（perplexity）。
    - **下游推理**：SciQ、COPA、ARC‑Easy、HellaSwag 的准确率。
- **消融实验**：分别剔除无截断缩放和随机舍入，观察训练过程的变化；对比不同 2D 块大小（8×8 到 64×64）对最终损失和相对吞吐量的影响。

### 4. 资源与算力

- 论文明确指出，由于当前硬件缺乏对 FP4 张量核的原生支持，所有实验均通过**软件模拟**微缩放行为来执行。
- 因此，实验的重点是**优化稳定性与模型质量**，而非实测硬件加速比。
- **文中未提供任何关于所用 GPU 型号、数量或具体训练时长的信息**。

### 5. 实验数量与充分性

- **实验组数**：在 3 种模型规模上，每类至少包含 BF16、2D‑FP4、2D‑FP4+MXFP8 三种配置，共计不少于 9 个完整的端到端训练流程。外加块大小和舍入/缩放的消融实验，实验量相对充足。
- **充分性**：实验覆盖了稠密与稀疏两种主流架构，模型参数跨度从 1B 到 30B，并同时考察语言建模和下游推理两大维度的性能，结论具有较强的泛化基础。
- **客观性与公平性**：所有对比均采用完全相同的优化超参数、训练词元预算和评估流程，基线（BF16）对比清晰，无混淆因素。

### 6. 主要结论与发现

- **稳定性与精度**：2D 块 FP4 量化可实现稳定的端到端训练，最终模型在困惑度和下游任务精度上相对于 BF16 基线的退化均小于 **1.3%**，且性能差距随模型规模增大而进一步缩小。
- **核心归因**：FP4 训练失败的关键原因是**张量转置造成的缩放不一致**，而非单纯的量化误差；强制前后向缩放一致性足以解锁实用化的 FP4 训练。
- **组件作用**：无截断缩放与随机舍入是保证稳定收敛的必要互补手段，缺失任一环节都会导致溢出或偏差引起的训练劣化。
- **混合精度策略**：对 Q/K 投影使用 MXFP8 是稳定注意力的有效辅助措施，但主要增益来自 2D 块结构。

### 7. 优点

- **问题洞察深刻**：首次明确指出“转置导致的缩放不一致”是现有一维微缩放 FP4 训练不稳定的根本原因，并给出了清晰的理论分析与可视化证明。
- **方法简洁有效**：仅将 1D 块改为方形的 2D 块，结合已知的舍入和缩放技巧，即实现了大幅度的稳定性提升，改动成本低，易于集成到现有训练框架。
- **实验扎实全面**：验证跨越稠密、MoE 模型，从 1B 到 30B 参数，消融实验清晰揭示了各组件的必要性与块大小的权衡。
- **实用效率分析**：提供了理论内存和吞吐量分析，表明该方法可使线性层权重内存减少约 72%，理论吞吐提升约 3.56 倍，具备明确的效率潜力。

### 8. 不足与局限

- **硬件模拟而非实测**：实验基于软件模拟，缺乏在真实 FP4 加速器上的性能数据，实际达到的速度与能耗收益有待下一代硬件验证。
- **规模与训练长度有限**：最大只测试到 30B 模型和 100B 词元，对于进一步扩大至百亿、千亿参数的超大规模长周期训练，是否仍能保持同等稳定性尚待探索。
- **2D 块引入的设计取舍**：2D 块需要存储额外的缩放因子，其最优块尺寸依赖于具体架构和负载，可能需额外调优；论文固定使用 32×32，未提供自适应方案。
- **激活值未完全一致**：激活仍使用 1D 块量化，虽然权衡了语义相关性，但未能享受转置不变性的全部益处，可能在某些极端场景下成为瓶颈。
- **注意力未完全 FP4**：Q/K 投影仍需借助 MXFP8 保证稳定，未能实现全 FP4 训练，限制了部分效率上限。

（完）
