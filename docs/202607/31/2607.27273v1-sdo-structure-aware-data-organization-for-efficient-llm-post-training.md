---
title: "SDO: Structure-Aware Data Organization for Efficient LLM Post-Training"
title_zh: "SDO: 面向高效LLM后训练的结构感知数据组织"
authors: "Jinliang Gao, Ning Yang, Hai Wang, Baili Xiao, Pin Lyu"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.27273v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 面向高效LLM后训练的数据组织框架，采用曝光驱动的反馈机制
tldr: 大型语言模型后训练中，数据组织常被视为静态预处理，无法响应优化过程中样本曝光需求的变化，导致样本曝光趋同而优化效率低下。SDO引入曝光驱动的结构感知数据组织，在每个epoch内基于KNN邻域遍历构建局部一致性批次，跨epoch则记录样本参与并降低过曝光样本采样概率，保持长期覆盖。实验表明，SDO在SFT、DPO、GRPO等任务上加速收敛，早期-中期收益最大，梯度方向更一致，不同问题类型准确率更均衡。该框架无需模型预热，即插即用，不永久排除样本，为高效后训练提供了新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.27273v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 857, \"height\": 427, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27273v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1808, \"height\": 933, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27273v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1826, \"height\": 518, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27273v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 867, \"height\": 449, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27273v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 872, \"height\": 357, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.27273v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1847, \"height\": 349, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27273v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 883, \"height\": 316, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27273v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 884, \"height\": 297, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27273v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 880, \"height\": 259, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27273v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 882, \"height\": 233, \"label\": \"Table\"}]"
motivation: 现有数据组织作为静态预处理步骤，无法响应优化中样本曝光需求变化，导致冗余与欠优化，制约后训练效率。
method: SDO框架利用冻结外部嵌入，在epoch内基于KNN构建局部一致性批次，跨epoch根据历史曝光概率调度样本，实现结构感知的数据组织。
result: 实验表明，SDO在SFT、DPO和GRPO等任务上加速收敛，最大增益在早中期，梯度更一致且问题类型准确率更均衡。
conclusion: SDO即插即用，无需模型预热，通过结构感知的数据组织有效提升后训练效率与覆盖均衡性。
---

## 摘要
大型语言模型的后训练成本高昂，现有效率改进主要集中于选择信息丰富的样本或设计训练调度。然而，数据组织本身通常被视为静态预处理步骤：基于嵌入的分组方法在训练前构建固定分区，无法适应优化过程中不断变化的样本曝光。因此，尽管优化需求不同，所有样本获得相似的曝光，导致某些样本冗余更新，而其他样本则优化不足。为解决此问题，我们提出SDO（结构感知数据组织），一个即插即用的数据组织框架，具备曝光驱动的反馈机制，根据表示空间结构组织小批量组成和样本曝光。SDO在冻结的外部嵌入上逐轮次操作，避免模型预热训练开销：在每个轮次内，局部感知批处理通过KNN邻域遍历形成连贯的小批量；跨轮次，曝光均衡调度记录每个样本的参与情况，并减少过曝光样本的采样概率，以保持长期覆盖。在SFT、DPO和GRPO上，SDO加速收敛，最大的收益出现在早期至中期阶段，产生更连贯的梯度和跨问题类型更均衡的准确性，且不会永久排除训练样本。

## Abstract
Post-training of large language models is expensive, and existing efficiency improvements mainly focus on selecting informative samples or designing training schedules. However, data organization itself is usually treated as a static preprocessing step: embedding-based grouping methods construct fixed partitions before training and cannot adapt to the evolving sample exposure during optimization. As a result, all samples receive similar exposure despite their different optimization needs, leading to redundant updates for some samples while leaving others under-optimized. To address this problem, we propose SDO (Structure-Aware Data Organization), a plug-and-play data organization framework with an exposure-driven feedback mechanism that organizes mini-batch composition and sample exposure according to representation-space structure. SDO operates epoch by epoch on frozen external embeddings, avoiding model warm-up training overhead: within each epoch, locality-aware batching forms coherent mini-batches via KNN neighborhood traversal; across epochs, exposure-balanced scheduling records per-sample participation and reduces the sampling probability of over-exposed samples to preserve long-term coverage. Across SFT, DPO, and GRPO, SDO accelerates convergence, with the largest gains observed in the early-to-mid phase, producing more coherent gradients and more balanced accuracy across question types without permanently excluding training samples.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

大型语言模型的后训练（如监督微调 SFT、人类偏好对齐 DPO、强化学习训练 GRPO）成本高昂。现有效率改进工作大多聚焦于**样本级选择**（挑选信息量大的样本）或**训练调度设计**，而**数据组织（data organization）** 本身长期被当作一次性静态预处理步骤：基于嵌入的分组方法在训练前构建好固定分区，之后不再随训练进程调整。

由此产生一个关键问题：随着优化推进，不同样本对进一步优化的需求是动态变化的，但静态组织使所有样本获得相似的曝光次数和曝光顺序，导致部分样本被过度更新（冗余计算），而另一些样本则优化不足。这种无视表征空间结构且缺乏反馈的数据组织方式，限制了后训练的效率与覆盖均衡性。因此，论文提出 **SDO（Structure‑Aware Data Organization）**，一种结构感知的、具备曝光驱动反馈机制的数据组织框架，旨在让小批量的构成和样本的曝光过程动态适应表征空间结构与训练进展。

---

### 2. 论文提出的方法论

#### 核心思想
将数据组织从静态预处理转变为 **逐轮次（epoch‑by‑epoch） 动态反馈过程**，在冻结的外部嵌入空间上运作：
- **轮内（within‑epoch）**：基于嵌入空间的局部结构形成连贯小批量，减少批次内部的分布差异。
- **跨轮（across‑epoch）**：根据历史曝光频率动态调整采样概率，抑制过曝光样本，保持长期覆盖的均衡性。

整个框架即插即用，无需模型预热（warm‑up），不永久排除任何样本。

#### 关键技术细节
- **嵌入来源**：使用一个冻结的外部嵌入模型为所有训练样本生成表示，整个训练过程中不再更新。
- **局部感知批处理（Locality‑Aware Batching）**：在每个 epoch 开始时，以 KNN 邻域遍历的方式组织小批量。具体而言，在嵌入空间中为每个样本寻找 K 个最近邻，然后按照邻域连通性构建批次，使得同批次内的样本在表示上具有高度局部一致性。这有助于减少批次梯度的方差，使优化方向更连贯。
- **曝光均衡调度（Exposure‑Balanced Scheduling）**：跨 epoch 记录每个样本的累计参与次数（曝光量）。在下一 epoch 开始组织批次时，对曝光过多的样本降低其被采样的概率（例如按曝光次数的倒数或类似衰减方式），从而有意识地将训练资源向曝光不足的样本倾斜，避免某些样本在多个 epoch 中被反复使用而其他样本始终被忽略。
- **算法流程概览**：
  1. 利用冻结外部嵌入对所有数据编码，得到固定表示向量。
  2. 在每个 epoch 初，根据当前所有样本的累计曝光状态与嵌入进行 KNN 构图，并据此规划出局部连贯的小批量序列。
  3. 执行一个 epoch 的训练，更新模型参数，同时累积各样本的曝光计数。
  4. 下一 epoch 重复步骤 2‑3，直到训练结束。

该方法不依赖模型自身参数，因此不需要预先对模型做预热训练以获取可靠嵌入，可以直接从第一个 epoch 就应用 SDO。

---

### 3. 实验设计

论文在三种主要的后训练范型上验证了 SDO：
- **任务场景**：监督微调（SFT）、直接偏好优化（DPO）、组相对策略优化（GRPO）。
- **对比方法**：虽然摘要未列出具体对比基线，但根据上下文，应包含常规的静态分批方式（随机批量、基于嵌入的固定分区等方法）。
- **评估基准**：实验考察了收敛速度、梯度一致性、不同问题类型的准确率均衡性等指标。具体数据集名称在摘要中未给出，但可以推断包含多种问答或指令微调数据集，并按问题类型划分以评估均衡性。

---

### 4. 资源与算力

论文的元数据中 **未提供任何关于 GPU 型号、数量、训练时长等算力信息**。故无法给出具体算力规模。

---

### 5. 实验数量与充分性

已披露的实验覆盖了 **SFT、DPO、GRPO** 三种任务范型，并分析了收敛加速效果、梯度一致性、各类问题准确率均衡性等多个维度。从摘要和元数据看，还涉及“早期至中期阶段收益最大”的观察，暗示实验可能按训练阶段划分进行了分析。但关于消融研究（如不同 K 值、曝光衰减策略等）、不同数据集规模、不同模型尺度的对比，以及统计显著性等信息均未出现。因此，仅凭现有信息，**无法判断实验是否足够充分、是否排除了各种混杂因素**。在客观性和公平性上，需看到完整论文才能进一步评价。

---

### 6. 论文的主要结论与发现

- SDO 在 SFT、DPO 和 GRPO 上均能 **加速收敛**，且**最大收益出现在训练早‑中期**，说明结构感知的数据组织能更早地推动模型进入优化的高效阶段。
- 采用 SDO 后，梯度更新方向 **更加连贯一致**，表明批次内部的局部一致性有助于降低梯度噪声。
- 在按问题类型细分的准确率上，SDO 带来了 **更均衡的表现**，说明曝光均衡调度有效抑制了某些样本类型被过度训练而另一些被忽视的问题。
- 该框架具有 **即插即用** 特性，无需模型预热，也 **不会永久排除任何训练样本**，保持了数据的完整利用。

---

### 7. 优点：方法或实验设计上的亮点

- **动态反馈机制**：将数据组织从静态设计升级为依赖训练进程的自适应过程，创新性地解决了曝光不均与冗余计算的问题。
- **双层结构化组织**：轮内的局部感知与跨轮的曝光均衡相结合，既保证了批次的内在一致性，又维护了样本覆盖的长期公平性。
- **解耦于模型参数**：利用冻结外部嵌入，避免了对模型自身状态的依赖，实现了零预热、即插即用的易用性。
- **多范式验证**：在 SFT、DPO、GRPO 三种差异较大的后训练范型上都显示了有效性，增强了方法的通用性。

---

### 8. 不足与局限

- **算力开销未知**：需额外计算冻结外部嵌入的向量，这对于超大规模数据可能引入额外开销，论文未给出成本分析。
- **嵌入模型的依赖性**：外部嵌入的质量和与下游任务的匹配程度会影响局部感知分组的有效性，对不同领域可能需要更换嵌入模型。
- **实验细节缺失**：摘要未提及使用的具体数据集、模型规模、对比基线的详细种类及超参数设置，无法评估结论的稳健性。
- **缺乏消融分析**：未见对各组成部分（例如 KNN 邻域大小、曝光衰减函数的敏感度）的独立贡献分析。
- **应用边界**：论文仅在 SFT、DPO、GRPO 上测试，对于其他后训练任务（如持续的预训练或 RLHF 的其他变体）的适用性尚不明确。
- **长期影响的未知性**：虽然强调不永久排除样本，但曝光压制策略是否会导致某些困难样本在后期训练中被过度延迟，从而影响最终性能的上限，未作探讨。

（完）
