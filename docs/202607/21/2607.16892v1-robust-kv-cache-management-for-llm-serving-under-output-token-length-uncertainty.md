---
title: Robust KV Cache Management for LLM Serving under Output Token Length Uncertainty
title_zh: 输出令牌长度不确定性下LLM服务的鲁棒KV缓存管理
authors: "Jiaming Cheng, Duong The Do, Duong Tung Nguyen"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16892v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 在GPU集群上联合优化LLM服务的GPU并行配置、KV缓存预留和请求路由
tldr: "在基于GPU集群的LLM推理服务中，KV缓存内存是主要瓶颈，由于输出token长度未知，请求到达时需预留缓存，预留过少引发抢占与重计算，过多则浪费内存、降低吞吐。为解决这一内存效率与抢占风险的权衡，本文提出鲁棒KV缓存管理框架，联合优化GPU并行度、每类请求的缓存预留、异构服务组间的请求路由以及共享前缀缓存，并考虑延迟SLO约束。为应对输出长度不确定和负载分布变化，引入Wasserstein分布鲁棒优化，并设计可扩展的块坐标下降算法求解混合整数问题。分析揭示出关键的分位数结构，可自动适应不同抢占和内存成本场景。在BurstGPT等真实负载追踪上的评估显示，相比固定分位数预留方案，成本最高降低56%，同时P99延迟、有效吞吐和SLO违规率保持竞争力。该方法无需手动调参，显著提升了LLM服务的鲁棒性与资源效率。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16892v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 1121, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16892v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 921, \"height\": 376, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16892v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 921, \"height\": 374, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16892v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 921, \"height\": 375, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16892v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 920, \"height\": 376, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16892v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 709, \"height\": 518, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16892v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 921, \"height\": 336, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16892v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 911, \"height\": 315, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16892v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 795, \"height\": 385, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16892v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 842, \"height\": 444, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16892v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1136, \"height\": 227, \"label\": \"Table\"}]"
motivation: LLM服务中输出长度不确定导致KV缓存预留的抢占风险与内存浪费权衡，固定分位数方法缺乏鲁棒性。
method: 联合优化GPU并行、预留量、路由及前缀缓存，并通过Wasserstein DRO与块坐标下降算法求解混合整数问题。
result: "在真实负载上成本较固定分位数基线最高降56%，P99延迟和SLO违规率保持低水平。"
conclusion: 通过分位数结构自动适应不同成本机制，实现无手动调参的高效鲁棒KV缓存管理。
---

## 摘要
在部署于GPU集群的现代LLM服务系统中，KV缓存内存是一个主要瓶颈。一个根本挑战是，KV缓存必须在请求到达时预留，而输出令牌长度直到生成完成才知晓。预留不足会触发抢占——强制终止并重新计算请求，带来显著开销——而过度预留则浪费内存并降低吞吐量。这造成了内存效率与抢占风险之间的核心权衡。我们提出了一个用于LLM服务的鲁棒KV缓存管理框架，该框架联合优化GPU并行配置、每请求类别的KV缓存预留、跨异构服务组的请求路由，以及共享提示的前缀缓存。该框架纳入延迟SLO约束，并捕捉内存分配、吞吐量和排队延迟之间的相互作用。为应对输出令牌长度不确定性和工作负载分布偏移，我们提出了一个Wasserstein分布鲁棒优化（DRO）形式，并针对产生的混合整数问题开发了可扩展的块坐标下降算法。我们的分析揭示了一个关键的分位数结构，该结构能自动使预留分位数适应不同的抢占和内存成本模式，无需手动调优。基于生产环境LLM工作负载（包括BurstGPT、Azure和ShareGPT追踪数据）的追踪驱动评估显示，与固定分位数预留基线相比，成本降低高达56%，同时在多种运行状态下保持有竞争力的P99延迟、有效吞吐量和SLO违规率。

## Abstract
KV cache memory is a primary bottleneck in modern LLM serving systems deployed on GPU clusters. A fundamental challenge is that the KV cache must be reserved upon request arrival, while the output token length remains unknown until generation completes. Under-reservation triggers preemption -- forcing termination and recomputation of requests and incurring significant overhead -- whereas over-reservation wastes memory and reduces throughput. This creates a central trade-off between memory efficiency and preemption risk. We present a robust KV cache management framework for LLM serving that jointly optimizes GPU parallelism configuration, KV cache reservation per request class, request routing across heterogeneous serving groups, and prefix caching for shared prompts. The framework incorporates latency SLO constraints and captures the interaction between memory allocation, throughput, and queueing delay. To address output token length uncertainty and workload distribution shift, we develop a Wasserstein distributionally robust optimization (DRO) formulation together with a scalable block coordinate descent algorithm for the resulting mixed-integer problem. Our analysis reveals a critical fractile structure that automatically adapts reservation quantiles to different preemption and memory cost regimes without manual tuning. Trace-driven evaluation on production LLM workloads, including BurstGPT, Azure, and ShareGPT traces, demonstrates up to 56\% lower cost than fixed-quantile reservation baselines while maintaining competitive P99 latency, goodput, and SLO violation rates across diverse operating regimes.

---

## 论文详细总结（自动生成）

### 1. 研究动机与背景
- **核心问题**：在GPU集群上运行的大语言模型（LLM）推理服务中，KV缓存内存管理是关键瓶颈。每个请求到达时必须预先分配KV缓存空间，但其输出的令牌长度在生成结束前是未知的。
- **根本权衡**：预留不足会引发抢占（请求终止并重算），带来显著的延迟开销；预留过度则浪费宝贵的高带宽内存，降低并发吞吐量。这种内存效率与抢占风险之间的权衡，因工作负载的异质性、分布漂移和系统异构性而更加复杂。
- **现有方法局限**：当前系统主要依赖运行时机制（如PagedAttention、连续批处理）或固定的分位数预留策略（如P90、P95、P99），这些方法无法根据不同成本结构和负载变化自动调整，且各决策（并行配置、预留、路由）被相互独立处理，缺乏联合优化。

### 2. 方法论
- **核心思想**：将LLM推理服务中的KV缓存管理形式化为一个联合优化问题，在输出长度不确定性和延迟SLO约束下，协同决定四个控制变量：GPU并行配置、每类请求的KV缓存预留量、请求路由和共享前缀缓存。
- **关键技术细节**：
    - **系统模型**：定义请求类 `i`，其输出长度 `ξ_i` 为随机变量。成本由抢占成本（`cp`）和浪费成本（`cw`）的期望组成，其比值 `ρ = cp/cw` 决定最优权衡。
    - **分布鲁棒优化（DRO）形式**：为应对输出长度的分布不确定性，构建基于1-Wasserstein距离的模糊集 `P_i^ε`，将最坏情况期望成本纳入目标函数。
    - **可处理重构**：利用Kantorovich对偶性，将无限维的DRO问题转化为等价的有限维线性规划，通过引入对偶变量 `γ_i` 和辅助变量 `θ_{i,n}` 得到可解形式。
    - **关键分位数结构**：理论分析证明，在固定路由下，最优预留量 `r_i^*` 满足 `r_i^* = L_i^{in} + F_i^{-1}(ρ/(ρ+1))`，其中 `q^* = ρ/(ρ+1)` 被称为**临界分位数**。这意味着系统能自动根据成本比选取最优预留分位数，无需手工设定。
    - **BCD-DRO算法**：为高效求解产生的混合整数规划，设计了块坐标下降算法。算法循环执行三个步骤：交替求解连续变量的线性规划、基于闭式规则更新二进制前缀缓存变量、枚举有限的整数配置变量，直至目标函数收敛。

### 3. 实验设计
- **数据集与场景**：
    - **BurstGPT**：来自Azure OpenAI服务的生产负载，输出长度差异大（平均125，P99=1586）。
    - **Azure LLM 2024**：Azure生产环境负载，包含两类：Code（短输出）和Conversation（长输出）。
    - **ShareGPT**：众包的ChatGPT对话数据，代表交互式聊天场景。
- **对比基准（Baselines）**：
    - 固定分位数策略：**Max**、**Mean**、**P90**、**P95**、**P99**。
    - **样本平均近似（SAA）**：对应 `ε=0` 的DRO，即仅基于经验分布优化。
    - 长度预测方法：**LP+1σ** 和 **LP+2σ**，基于历史长度均值和标准差进行预留。
- **评估指标**：总成本（抢占和浪费成本、GPU/SLO惩罚等）、P99延迟、有效吞吐量（Goodput）和SLO违规率。

### 4. 资源与算力
- 论文模拟了**8张NVIDIA A100-80GB GPU**组成集群，用于服务一个**70B参数LLM**，并配置了4种不同的并行策略供优化器选择。
- 算法的**求解本身不涉及模型训练或推理**，其离线求解时间根据问题规模从0.07秒到约258秒不等（详见原论文Table III），可在实际生产中以5-10分钟为周期进行重优化。文本中未提供训练或推理时的GPU算力总时长。

### 5. 实验数量与充分性
- 论文进行了**多维度、较充分的实验评估**，主要包括：
    - **主实验**：在多个成本比（`ρ`从2到100）下对比DRO与所有基线方法的成本与延迟（表II、表IV）。
    - **理论验证**：展示了DRO实际选定的分位数与理论临界分位数的完美匹配（图2b）。
    - **算法可扩展性**：对比了直接求解混合整数规划与BCD-DRO算法在不同问题规模下的求解时间（表III）。
    - **敏感性分析**：测试了请求到达率放大系数（图3a）和Wasserstein半径ε（图3b）对成本和开销的影响。
    - **鲁棒性评估**：通过引入模拟的分布偏移，对比静态优化与滚动时域适应下的性能，并分析了DRO优势随偏移大小和成本比的变化（图5）。
    - **消融实验**：逐一剥离路由决策、分布鲁棒性（退化为SAA）和前缀缓存三个组件，评估其对成本和延迟的贡献（表V及图6）。
- 实验设计客观，对比了多种主流方法与变体，并且通过消融研究和多参数扫描充分验证了方法各组成部分的有效性。

### 6. 主要结论与发现
- **跨成本比适应性**：没有任何单一固定分位数基线在所有成本比下最优。DRO自动根据成本比调整预留分位数，在多种场景下总成本相比最优固定基线**最高降低56%**（如实时交互场景）。
- **临界分位数定理**：最优预留策略呈现鲜明的分位数结构，分位数由 `ρ/(ρ+1)` 决定，为无需手动调参提供了理论保证。
- **鲁棒性与算法有效性**：Wasserstein DRO能有效应对工作负载分布偏移，性能显著优于SAA。提出的BCD-DRO算法能在生产级问题规模下求解，将本为静态决策的KV缓存管理转化为可周期性适应的在线策略。
- **联合优化价值**：路由决策保证了异构内存需求下的可行性，前缀缓存在高并发下显著降低排队延迟，统一框架能协同管理这些效应。

### 7. 优点
- **理论坚实，洞察深刻**：将经典报童模型的临界分位数思想引入LLM服务的KV缓存管理，揭示了成本比与最优预留间的解析关系，理论优雅且实用。
- **方法集成，决策协同**：首次在单一框架中联合优化并行配置、预留、路由与缓存，捕捉了它们之间的耦合关系，避免了单点决策的次优性。
- **面向生产，鲁棒实用**：采用Wasserstein DRO应对不确定性，提供显式的鲁棒性保证。BCD-DRO算法可规模化求解，支持周期性重优化，且仅需设置两个有直观意义的参数（成本比`ρ`和鲁棒半径`ε`）。
- **实证充分，验证全面**：使用了三个不同分布特性的生产环境负载追踪数据，覆盖广泛的操作域和多种基线方法，通过丰富的实验（消融、敏感性、鲁棒性）有力验证了方法的各项优势。

### 8. 不足与局限
- **模型与假设的理想化**：排队模型采用M/G/1假设（泊松到达），且假定请求类内输出长度分布具有同质性，可能与高度复杂和快速变化的实际生产负载存在偏差。
- **参数敏感性**：成本比`ρ`和Wasserstein半径`ε`对性能有直接影响，论文虽讨论了其影响但依赖操作者设定；若在未知成本结构或剧烈变化的环境下，如何动态自适应设置这些参数仍是一个挑战。
- **模拟评估而非实际系统部署**：实验均在基于追踪数据的模拟器上完成，虽然模拟了关键系统特性，但尚未在真实GPU集群和如vLLM等生产级服务框架中进行端到端部署验证。
- **问题规模局限**：虽然BCD-DRO已展示出对中等规模问题的可扩展性，但文中最大实验实例（15类请求，2000个历史样本）能否平滑扩展至更庞大的集群和更细粒度的请求分类仍有待观察。
- **未与其他运行时机制深度结合**：方法作为控制面决策，与数据面的KV缓存驱逐方法正交，但实验未评估与例如H2O、SnapKV等内存缩减技术结合后的综合效果及对生成质量的影响。

（完）
