---
title: A Hardware-oriented Approach for Efficient Bayesian Inference Computation and Deployment
title_zh: 面向硬件的贝叶斯推理高效计算与部署方法
authors: "Nikola Pižurica, Matteo Risso, Nikola Milović, Alessio Burrello, Igor Jovančević, Conor Heins, Miguel de Prado"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17855v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 面向硬件的贝叶斯推理加速方法，针对嵌入式GPU
tldr: 为解决贝叶斯推理在边缘设备上计算成本高的问题，本文提出面向商用嵌入式GPU的硬件加速方法。核心是通过两种内存融合策略重构张量收缩操作，并引入稀疏表示与张量聚类压缩内存。在Jetson Orin AGX上针对POMDP的测试显示，典型加速2-2.5倍、最高5倍，输出数值完全一致。该方法为概率模型在资源受限场景的高效部署提供了实用路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17855v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 829, \"height\": 438, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17855v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 543, \"height\": 318, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17855v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1684, \"height\": 528, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17855v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 883, \"height\": 1225, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17855v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 904, \"height\": 465, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17855v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 907, \"height\": 452, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17855v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 917, \"height\": 525, \"label\": \"Table\"}]"
motivation: 贝叶斯推理计算成本高，难以在资源受限的边缘设备上高效部署，需硬件导向的加速方案。
method: 通过融合策略重构变分消息传递的张量收缩内存布局，结合稀疏表示和张量聚类，并采用自调谐器选择最优变体。
result: 在NVIDIA Jetson Orin AGX上对770个POMDP配置的基准测试中，实现最高5倍加速，典型加速2-2.5倍，且输出数值等同。
conclusion: 该方法显著加速了离散贝叶斯推理，支持在商用嵌入式GPU上实时部署概率模型，具有良好的实用性和可扩展性。
---

## 摘要
贝叶斯推理为不确定性下的推理提供了原则性基础，但其计算成本阻碍了在资源受限的边缘设备上的部署。本文提出了一种面向硬件的方法，用于在商用现成的嵌入式GPU上加速离散贝叶斯推理。我们发现，广泛的变分消息传递算法的延迟主要受张量收缩支配。我们的方法通过两种互补的合并策略重组这些操作的内存布局，生成更适合高效GPU执行的紧凑、规则形状的原语。随后引入可选稀疏数组表示和张量聚类方案以减少内存占用。我们实例化了该方法，并为隐马尔可夫模型（HMM）生成了三种消息传递算法的优化变体，即变分滤波、变分消息传递和边缘消息传递。此外，我们辅以基于机器学习的自动调谐器，自动为给定的生成模型规格选择性能最佳的算法变体。在NVIDIA Jetson Orin AGX上对770个随机采样的现实部分可观察马尔可夫决策过程（POMDP）配置进行基准测试，我们的实现实现了高达5倍的加速，典型增益为2-2.5倍，同时输出与基线实现数值相同的结果。

## Abstract
Bayesian inference provides a principled foundation for reasoning under uncertainty, but its computational cost hinders deployment on resource-constrained edge devices. In this paper, we present a hardware-oriented methodology for accelerating discrete Bayesian inference on commercial off-the-shelf embedded GPUs. We identify that the latency of a broad class of variational message-passing algorithms is dominated by tensor contractions. Our approach restructures the memory layout of these operations using two complementary merging strategies that produce compact, regularly-shaped primitives better suited for efficient GPU execution. We then introduce optional sparse array representations and a tensor-clustering scheme to reduce the memory footprint. We instantiate the methodology and produce optimized variants of three message-passing algorithms for Hidden Markov Models (HMMs), namely variational filtering, variational message passing, and marginal message passing. Furthermore, we complement this with a machine-learning-based autotuner that automatically selects the best-performing algorithmic variant for a given generative model specification. Benchmarked on an NVIDIA Jetson Orin AGX across 770 randomly sampled realistic Partially Observable Markov Decision Process (POMDP) configurations, our implementations achieve speedups of up to 5x, with typical gains of 2-2.5x, while producing numerically identical outputs to the baseline implementations.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **问题**：贝叶斯推理（特别是变分推理）在离散状态空间模型中进行消息传递时，其主要计算负载来自大量的张量收缩（tensor contractions）。这导致其在资源受限、有低延迟要求的边缘设备（如嵌入式 GPU）上难以高效部署，限制了不确定性推理在机器人、自动驾驶等真实场景中的应用。
*   **整体含义**：本文提出了一种**硬件导向**的优化方法论，通过重组计算原语的内存布局，将循环中不规则、小规模的张量收缩合并为紧凑、规整的操作，从而在不损失任何数值精度的前提下，大幅提升离散贝叶斯推理在商用嵌入式 GPU 上的执行速度。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

核心思想是识别出变分消息传递算法（如变分滤波、VMP、MMP）的延迟瓶颈在于**张量收缩**，并针对这一共性原语进行优化。主要技术包括：

*   **两种互补的内存合并策略**：
    *   **轴对齐填充与合并**：将不同形状的输入张量用零填充到最大公共形状，并在批量维度上连接成一个规整的张量进行运算，最后再拆分结果。此法简单但可能因填充导致内存爆炸。
    *   **块对角合并**：适用于一方操作数为1维的模式（如计算对数似然）。将高维输入扁平化并排列成一个大型块对角矩阵，将1维输入连接成向量，将循环的收缩操作转化为一次标准的矩阵-向量乘法。
*   **内存压缩技术**：
    *   **BCOO 稀疏表示**：利用 JAX 的批量坐标格式表示高度稀疏的合并数组，减少内存占用。
    *   **张量聚类**：不将所有收缩合并为一次，而是按形状相似性将输入数组分组（如按秩聚类或类似凝聚聚类的策略），在每组内进行合并。这平衡了运算的规整性与零填充的浪费。
*   **混合与端到端实现**：其优化实现分为“混合方法”（仅将部分步骤合并，其余部分使用原始循环实现）和“端到端方法”（在整个似然消息计算期间保持合并表示）两大类，共衍生出9种不同的算法变体（见表1）。
*   **基于机器学习的自动调谐器**：将选择最优算法变体建模为分类问题。给定一个 POMDP 规格（状态因子数、观察模态数、维度、依赖关系等），通过训练好的 XGBoost 分类器自动预测最快的变体，避免部署时对所有变体进行耗时的基准测试。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

*   **场景与数据集**：使用随机生成的 **770 个部分可观察马尔可夫决策过程（POMDP）配置**。配置参数（隐藏状态因子数 F、观察模态数 M、各因子/模态的维度、A-依赖列表）通过一个三阶段的生成器产生，包括粗粒度选择、细粒度维度抽样（均匀和偏斜两种模式）以及依赖列表生成，以确保覆盖异构且现实的形状和结构。
*   **基准对比**：以 `pymdp` 库的**原始实现作为基线**，对比了本文提出的 **9 种优化变体**（轴对齐混合、稀疏混合、聚类混合、块对角混合、稀疏块对角混合、聚类块对角混合、轴对齐端到端、稀疏端到端、聚类端到端），以及**自动调谐器选择的结果**。
*   **评估平台**：**NVIDIA Jetson Orin AGX** 嵌入式 GPU。
*   **评估指标**：**总推理延迟**，并以**加速比**（基线延迟除以优化后延迟）的形式呈现。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点。

*   **硬件平台**：所有推理基准测试均在单个 **NVIDIA Jetson Orin AGX** 嵌入式 GPU 上完成。
*   **训练算力**：用于自动调谐器的 XGBoost 和随机森林模型训练是在同一实验环境下进行的，但**论文未明确提及模型训练所需的 GPU 型号、数量或具体训练时长**。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平

*   **实验规模庞大**：在 **770 个** 具有高度差异性的随机生成 POMDP 配置上，对 **3 种算法（FPI, VMP, MMP）** 的 **9 个优化变体和 1 个基线** 进行基准测试，累计进行了数千组延迟测量。
*   **消融与对比实验充分**：通过对比不同合并策略（轴对齐 vs. 块对角）、不同内存优化方法（无优化、BCOO稀疏、聚类）以及不同实现方法（混合 vs. 端到端）的加速比分布，系统性地分析了各设计组件的作用和适用场景。
*   **客观公平性**：所有方法在同一套预先计算并固化到磁盘的 POMDP 配置与参数上进行，确保了平台和随机种子一致。优化变体与基线实现产生**数值完全相同**的输出，因此无需进行精度比较，延迟对比直接且公平。自动调谐器的评估采用标准的5折交叉验证，并使用“遗憾值”（regret）作为评估指标，避免了测试信息泄露。

### 6. 论文的主要结论与发现

*   **显著加速**：在 NVIDIA Jetson Orin AGX 上，优化后的变体实现了比基线**最高 5 倍**的加速，**典型加速比在 2 到 2.5 倍**之间，且所有输出数值完全一致。
*   **方法普适有效**：该加速效果在变分滤波（FPI）、变分消息传递（VMP）和边缘消息传递（MMP）三种不同算法上均得到验证，尤其是对计算量更大的 VMP 和 MMP 算法，加速效果尤为突出。
*   **策略性能差异**：
    *   块对角合并策略的鲁棒性最好，在各种配置下几乎都产生加速。
    *   张量聚类技术不仅能显著减少内存占用（约 9 倍），也是提升推理速度、减少性能波动的最有效手段。
    *   BCOO稀疏表示虽能大幅压缩内存（约 14 倍），但由于 JAX 的稀疏操作支持尚不成熟，当前并未带来显著的延迟收益。
    *   端到端方法虽然可能达到更高的峰值加速，但平均性能因引入过多冗余计算而不如混合方法。
*   **自动调谐的价值**：XGBoost 自动调谐器的性能几乎能追踪所有变体的性能上限，避免了耗时的人工基准测试（4 到 25 分钟），在几分之一秒内即可做出准确预测，且几乎不会选到比基线差的变体。

### 7. 优点：方法或实验设计上有哪些亮点

*   **算法无关的原语级优化**：不针对单个算法，而是从共享的底层计算原语（张量收缩）入手，方案具有广泛的适用性和扩展性。
*   **无损加速**：优化过程是纯工程性的内存布局重组，数学上等价于原始操作，不引入任何额外的近似或量化误差，对安全性要求高的应用至关重要。
*   **开销前置**：耗时的合并与聚类操作仅在模型初始化时执行一次，对推理阶段的延迟无任何额外开销。
*   **全面的实验评估**：实验设计系统且客观，大量的随机配置确保了统计显著性，而不是依赖于少数几个特定示例，结论可靠。
*   **实用的自动调优**：引入的自动调谐器解决了实际部署中如何为特定模型选择最优变体的难题，弥合了理论与落地之间的鸿沟。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

*   **模型结构局限**：当前方法仅支持简单的**过渡依赖（B-dependencies）**，即一个隐藏状态在下一时刻只依赖于自身的上一个值，并不支持更复杂的任意过渡依赖结构。
*   **平台与生态局限**：
    *   实验**仅在 NVIDIA Jetson Orin AGX 这一种平台上进行**，结论在其他嵌入式 GPU（如高通 Adreno）或边缘 TPU/NPU 上的泛化能力未知。
    *   BCOO 稀疏数组方案的加速潜力严重受制于 JAX 当前不成熟的稀疏操作后端，随着后端的完善，相关加速效果可能会显现。
*   **端到端方法的效能损失**：端到端方法因统一填充到全局最大维度而引入了大量冗余的零填充计算，导致其平均性能反而不如需要中途拆分的混合方法。
*   **内存与延迟的权衡**：虽然论文论证了内存增长在可接受范围内（通常小于 1GB），但合并策略不可避免地会增加参数数量，在极端内存受限的场景下可能成为瓶颈。

（完）
