---
title: "WSqD: A Horizon-Free Learning Rate Schedule for Large Model Training"
title_zh: WSqD：一种大模型训练的无视界学习率调度
authors: "Jianhao Ma, Yuxin Chen"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10959v1"
tags: ["query:mlsys"]
score: 8.0
evidence: 提出无训练时长依赖的学习率调度方案，优化大模型训练
tldr: 标准学习率调度绑定固定训练长度，难以适应训练时间延长。WSqD用平移反平方根基替代WSD恒定阶段，仅需冷却开始时刻的horizon。理论证明在凸优化中达到O(1/√T)最优收敛，实验在SlimPajama预训练中可重用单一峰值学习率，性能匹配或超越精细调优的WSD。该调度简化了大规模训练扩展，避免重复调参。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10959v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1428, \"height\": 353, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10959v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1698, \"height\": 541, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10959v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1702, \"height\": 543, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10959v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1708, \"height\": 1086, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10959v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1022, \"height\": 586, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10959v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1697, \"height\": 538, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10959v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1698, \"height\": 700, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10959v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1694, \"height\": 700, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10959v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1190, \"height\": 680, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10959v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1191, \"height\": 678, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10959v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 1190, \"height\": 679, \"label\": \"Figure\"}]"
motivation: 标准学习率调度绑定固定训练步数，限制训练扩展；WSD虽支持续训，但峰值需按初始horizon调整，扩展后次优。
method: 通过平移反平方根基替换WSD恒定阶段并保留线性冷却，实现horizon无关调度。
result: 理论证明达到O(1/√T)最优最后迭代收敛；实验在SlimPajama语言模型预训练中重用单一峰值学习率，性能匹配或超越精细调优的WSD。
conclusion: WSqD提供了一种horizon无关的学习率调度，避免了因训练扩展导致的峰值重调，适用于大规模灵活训练扩展。
---

## 摘要
标准的学习率调度（如余弦退火）与固定的训练视界相关联，限制了它们适应事后视界扩展的能力。预热-稳定-衰减（WSD）通过在短暂的线性冷却之前维持一个长的恒定速率阶段部分解决了这个问题，允许从衰减前检查点恢复训练。然而，其峰值学习率仍然基于原始训练视界进行调整，并且在训练扩展时可能变得次优。受随机凸优化的启发，我们提出了WSqD（平方根底数预热与线性衰减），一种用偏移的平方根倒数底数替换WSD的恒定稳定阶段，同时保留最终线性冷却的学习率调度。在随机凸设置中，WSqD可证明达到极小极大最优的$O(1/\sqrt{T})$最终迭代收敛速率。重要的是，其基础学习率调度与视界无关，仅需要训练视界来确定何时开始最终冷却。在经验上，使用SlimPajama语料库进行语言模型预训练时，WSqD在多个训练视界中重用单一峰值学习率的情况下，匹配或优于精心调整的WSD和其他基线。

## Abstract
Standard learning rate schedules such as cosine annealing are tied to a fixed training horizon, limiting their ability to accommodate post hoc horizon extension. Warmup-stable-decay (WSD) partially addresses this issue by maintaining a long constant-rate phase before a short linear cooldown, allowing training to resume from a pre-decay checkpoint. However, its peak learning rate is still tuned based on the original training horizon and can become suboptimal when training is extended. Motivated by stochastic convex optimization, we propose WSqD (Warmup with Square-root base and linear Decay), a learning rate schedule that replaces WSD's constant stable phase with a shifted inverse-square-root base while retaining the final linear cooldown. In the stochastic convex setting, WSqD provably attains the minimax-optimal $O(1/\sqrt{T})$ last-iterate convergence rate. Importantly, its base learning rate schedule is horizon-independent, and the training horizon is needed only to determine when to begin the final cooldown. Empirically, on language-model pretraining using the SlimPajama corpus, WSqD matches or outperforms carefully tuned WSD and other baselines across multiple training horizons while reusing a single peak learning rate.

---

## 论文详细总结（自动生成）

好的，以下是对该论文的结构化深入总结。

### 1. 论文的核心问题与整体含义

-   **核心问题**：现代大语言模型（LLM）训练日趋迭代化，研究者常在初始训练计划后继续扩展训练（例如，基于缩放定律增加计算量、领域适应或多阶段训练）。标准的固定视界学习率调度（如余弦退火）与预定义的总训练步数紧密绑定，难以自然且高效地适应这种事后扩展。
-   **现有方法的局限**：
    -   **余弦退火**：训练一旦超过预定视界，学习率已衰减至近零，继续训练效率极低，而重新预热又会导致损失尖峰（稳定性缺口）。
    -   **预热-稳定-衰减（WSD）**：通过保持一长段恒定学习率（稳定阶段）支持从检查点续训，但其最优峰值学习率仍取决于初始训练视界。当训练大幅延长时，原先调好的峰值会变得次优，需要重新调参，并非真正意义上的“无视界”调度。
-   **整体含义**：本文旨在设计一种结构上就与训练视界无关的学习率调度，其核心参数（峰值学习率）无需因训练延长而重新调整，从而简化大规模、灵活的训练流程。

### 2. 论文提出的方法论

-   **核心思想**：从随机凸优化的第一性原理出发，提出**WSqD（平方根底数预热与线性衰减）**调度。其基础阶段采用与视界无关的经典`1/√t`型调度，并在训练末期附加一个短暂的线性冷却阶段，以将“随时续训”的灵活性与优良的最终收敛性能结合起来。
-   **关键技术细节与公式**：
    -   WSqD的完整调度（预热阶段后，`t`为迭代次数）定义如下：
        -   基础阶段（`1 ≤ t ≤ (1-α)T`）：`η_t = c₀ / √(t + T₀)`
        -   衰减阶段（`(1-α)T < t ≤ T`）：`η_t = [c₀ / √((1-α)T + T₀)] * (T - t) / (αT)`
    -   **符号解释**：
        -   `c₀`: 控制整体学习率尺度的参数，其最优值在理论上是视界无关的。
        -   `T₀`: 偏移参数，用于防止训练初期学习率过大。任何固定的`T₀`在训练视界足够大时均是可行的。
        -   `α`: 衰减分数，表示最终线性冷却阶段占总训练步数的比例。
        -   `T`: 最终的计划训练视界。
    -   **方法论特点**：
        -   **解耦设计**：基础阶段（`c₀/√(t+T₀)`）完全不依赖总视界`T`，因此任何一个衰减前检查点都可作为续训锚点。冷却阶段（线性衰减）通过消除最终迭代的额外对数开销，恢复了理论上的最优`O(1/√T)`收敛速率。
        -   **理论保证**：在标准随机凸优化框架下，证明了WSqD达到极小极大最优的`O(1/√T)`最终迭代收敛速率。更重要的是，速率最优的尺度参数`c₀`与视界无关，这意味着可在小规模试点训练中调优`c₀`并直接复用于更长的训练。

### 3. 实验设计

-   **数据集与场景**：主要实验在**SlimPajama**语料库上进行语言模型预训练，消融实验也使用了**OpenWebText2**语料库以验证跨数据集的泛化能力。核心场景是**训练续训**，即在初始训练视界结束后，决定继续训练更长时间。
-   **模型**：一个拥有**2.13亿参数**的LLaMA风格Transformer模型（24层，12个注意力头，嵌入维度768）。
-   **优化器**：主要用于验证损失实验的是**AdamW**（`β1=0.9, β2=0.95`，权重衰减0.1，梯度裁剪1.0）。
-   **对比方法（Baselines）**：
    -   **WSD**：作为主要对比对象。使用与WSqD相同的峰值学习率（`η_max`）和衰减分数`α`。
    -   **两阶段调优WSD（Two-stage tuned WSD）**：一个更强的基线。允许在初始训练结束后，为续训阶段重新搜索一个新的最优峰值学习率。
    -   **幂律调度（Power Schedule）**：Shen等人（2024）提出的视界无关调度，形式为`η_t = min{η_max, βat^{-0.51}}`，其指数通过大规模实验拟合得出。
-   **续训协议**：对于WSqD、WSD和幂律调度，采用单一续训轨迹。基础/峰值学习率在一个短的**试运行（pilot run）**中选定后，便在同一轨迹上复用于所有后续的延长视界，期间**不再重新调优**。两阶段调优WSD是唯一允许在每次延长时重新调优学习率的基线。

### 4. 资源与算力

-   文中明确提到实验使用**单个设备（a single device）**进行训练，但**未指明所用GPU的具体型号**。
-   训练设置方面，有效批次大小为200个序列，通过单个设备上微批次大小为50、梯度累积4步来实现。
-   整体而言，由于计算资源限制，本文的实验规模和探索范围有限，作者将此明确归结为一种局限性。

### 5. 实验数量与充分性

-   **实验组数**：论文包含了多组实验来验证其主张，较为全面：
    1.  **主续训对比**：在4个不同训练视界（15k, 30k, 45k, 60k步）下对比WSqD与WSD。在5000步和10000步试运行选参两种设定下分别测试。
    2.  **最优学习率视界依赖性分析**：对WSD和WSqD，在不同视界下分别扫参，观察各自最优峰值学习率漂移情况。
    3.  **超参数（T₀）敏感性分析**：研究了WSqD的偏移参数`T₀`对最终性能的影响。
    4.  **与更强基线对比**：将WSqD与两阶段调优WSD和幂律调度进行对比。
    5.  **稳健性检验**：通过3个不同随机种子重复主实验，验证结果的可复现性。
    6.  **跨数据集与跨模型规模实验**：在OpenWebText2数据集上复现主实验，并在一个更小的1.24亿参数模型上进行实验。
-   **实验充分性与公平性**：实验设计周密，对比公平。续训协议严格约束了基线方法的调参机会；种子、模型规模和数据集的交叉验证增强了结论的可靠性。然而，所有实验仅限于较小规模的模型和有限的训练步数，相对于当今前沿的大模型训练标准，其充分性受到根本性约束。

### 6. 论文的主要结论与发现

-   **经验性能**：在“单一峰值学习率、复用至更长视界”的续训场景下，WSqD在所有测试视界下都**匹配或优于精心调优的WSD**，且优势随着视界延长而增大。它能以显著更少的计算量（最高节省约10%）达到与WSD相同的验证损失。
-   **视界无关性验证**：与WSD的最优峰值学习率随视界增大而漂移不同，WSqD的最优峰值学习率在不同视界下保持**极其稳定**。这验证了其核心设计目标：可在短跑中调参，直接应用于长跑。
-   **理论支撑**：为WSqD在随机凸优化下提供了严谨的收敛分析，证明了`O(1/√T)`的最优最终迭代速率，并为“基础阶段视界无关+末期冷却”这一设计提供了理论依据。

### 7. 优点

-   **方法设计巧妙**：将经典的`1/√t`调度与末期冷却相结合，巧妙地在“随时续训的灵活性”和“最优最终性能”之间取得了平衡，设计有清晰的理论直觉。
-   **理论实践紧密结合**：从随机凸优化理论推导出调度形式，并为其优越性提供了理论保障，与纯经验性的调度设计形成对比。
-   **工程实用性高**：解决了大模型训练中一个非常实际的痛点：无需为训练扩展重新进行昂贵的学习率扫描，简化了训练流程。
-   **实验验证严谨**：不仅与主流基线对比，还设置了更严格的调优基线（两阶段WSD），并对方法的稳健性（种子）、泛化性（数据集、模型规模）和超参敏感性（`T₀`）进行了全面的实验分析，结论令人信服。

### 8. 不足与局限

-   **模型规模与算力有限**：最大的不足。所有实验均在2.13亿参数的小模型上进行，训练步数仅数万步，使用的也是单个设备。这是否能直接推广至数十亿、数千亿参数的大模型大规模训练尚存疑，而作者也明确指出这是受限于计算资源。
-   **数据集覆盖有限**：仅在一个文本预训练语料库（SlimPajama）上进行了主实验，并在另一个（OpenWebText2）上验证，缺乏在代码、数学、多模态等更广泛数据类型下的测试。
-   **优化器适配性**：主实验仅基于AdamW优化器。虽然论文在附录中非正式地探讨了与Muon优化器的镜像下降联系，但WSqD在其他流行优化器下的有效性和理论适用性仍是开放问题。
-   **理论假设过于理想化**：理论分析基于强假设的随机凸优化模型，与现在LLM训练所用的非凸、自适应动量方法有较大差距。理论更多地是提供一种动机和直觉，而非对实用场景的严密解释。
-   **超参数`T₀`仍需选择**：虽然实验表明当`T₀`足够大时性能不敏感，并给出了`T₀ = T_{pilot}`的推荐默认值，但它仍然是一个需要设定的超参数，并未完全消除所有手动配置。

（完）
