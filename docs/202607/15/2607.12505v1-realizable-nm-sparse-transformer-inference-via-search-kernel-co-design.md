---
title: "Realizable N:M Sparse Transformer Inference via Search-Kernel Co-Design"
title_zh: "通过搜索-内核协同设计实现可落地的N:M稀疏Transformer推理"
authors: "Yiming Liu, Wenqi Lou, Zhiguang Wang, Zhiwei Ke, Fengrui Zuo, Chao Wang, Xuehai Zhou"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12505v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 协同设计稀疏 CUDA 核和稀疏配置，实现 ViT 在 GPU 上的高效推理，获得实际加速
tldr: "M稀疏理论加速但在GPU上难以实现。本文提出软硬协同框架：硬件设计MD-SpMM内核，将稀疏GEMM重构为微密集Tensor Core对齐数据流并自适应并行；软件用三阶段搜索进行延迟感知的逐层稀疏分配，避免过早收敛。在ViT/Swin上实现超2.2倍加速且精度相当，同延迟下精度更优。代码开源，为N:M稀疏实用推理提供方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12505v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 605, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12505v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 578, \"height\": 391, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12505v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1253, \"height\": 408, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12505v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1251, \"height\": 475, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12505v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1221, \"height\": 447, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12505v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1227, \"height\": 893, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12505v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1227, \"height\": 739, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12505v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1250, \"height\": 427, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12505v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1249, \"height\": 258, \"label\": \"Table\"}]"
motivation: M稀疏虽有理论计算节省，但在现代GPU上因执行不规整难以转化为实际速度提升。
method: 提出软硬协同框架，硬件设计MD-SpMM内核将稀疏GEMM重组织为微密集Tensor Core数据流，软件用三阶段搜索逐层分配稀疏度。
result: 在ViT/Swin等模型及GPU平台上实现超2.2倍延迟加速，精度相当，且同延迟下精度更优。
conclusion: M稀疏在真实GPU上的高效Transformer推理提供了实用化方案，代码已开源。
---

## 摘要
视觉Transformer(ViT)虽然精度高，但推理延迟也高。半结构化N:M稀疏性可以降低算术成本，但在现代GPU上，其理论节省往往无法转化为成比例的端到端加速。这种不匹配的原因在于，部署延迟不仅取决于算术量的减少，还取决于稀疏性下的执行规律性和硬件调度。因此，实现实际加速需要稀疏执行与稀疏配置的协同设计。为此，我们提出了一种面向N:M稀疏ViT推理的软硬件协同设计框架。在硬件方面，我们设计了MD-SpMM，这是一种N:M稀疏CUDA内核，它将稀疏GEMM重组为微密集、对齐Tensor Core的数据流，并利用推理感知的自适应并行性来维持利用率。在软件方面，我们在明确的端到端延迟预算下执行逐层稀疏性搜索，采用带有约束松弛的三阶段启发式搜索，以避免早熟收敛并实现部署感知的稀疏性分配。在多个ViT/Swin模型和GPU平台上的实验表明，该框架实现了超过2.2倍的延迟加速，同时保持可比精度，并在相同延迟约束下提供更优的精度。源代码公开于https://github.com/liuganhuo/realizable-nm-sparse-transformer。

## Abstract
Vision Transformers (ViTs) achieve strong accuracy but incur high inference latency. Semi-structured N:M sparsity can reduce arithmetic cost, yet its theoretical savings often fail to translate into proportional end-to-end speedups on modern GPUs. This mismatch arises because deployment latency depends not only on arithmetic reduction but also on execution regularity and hardware scheduling under sparsity. Achieving practical acceleration, therefore, requires coordinated design across sparse execution and sparsity configuration. To this end, we propose a hardware-software co-design framework for N:M sparse ViT inference. On the hardware side, we design MD-SpMM, an N:M sparse CUDA kernel that reorganizes sparse GEMM into micro-dense, Tensor-Core-aligned dataflow and uses inference-aware adaptive parallelism to sustain utilization. On the software side, we perform layer-wise sparsity search under explicit end-to-end latency budgets using a three-stage heuristic search with constraint relaxation to avoid premature convergence and enable deployment-aware sparsity allocation. Experiments on multiple ViT/Swin models and GPU platforms show that the framework achieves over 2.2x latency speedup while maintaining comparable accuracy and delivering superior accuracy under the same latency constraint. The source code is publicly available at https://github.com/liuganhuo/realizable-nm-sparse-transformer.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
*   **背景与动机**：视觉Transformer（ViT）精度高但推理延迟大。半结构化N:M稀疏（如2:4）可削减算术量，理论上能加速，但在现代GPU（尤其是Tensor Core和INT8推理）上，理论节省往往无法转化为成比例的端到端加速。
*   **核心问题**：这种不匹配源于部署延迟不仅取决于算术减少，还取决于稀疏执行下的**规律性、内存行为和硬件调度**。因此，单纯降低FLOPs或提高全局稀疏度，不等于在真实硬件上获得实际加速。
*   **整体含义**：论文提出“可实现的加速”概念，认为需要同时满足**执行可实现性**（稀疏算子在硬件上高效稳定执行）和**配置可实现性**（逐层稀疏分配需在明确延迟约束下进行）。现有方法只关注单一方面，因此需要一种**软硬件协同设计**框架，将两者耦合起来，将理论稀疏收益转化为真实的端到端延迟改善。

### 2. 方法论
论文从硬件内核和软件搜索两个层面协同设计。
*   **硬件侧：MD-SpMM执行可实现内核**
    *   **核心思想**：将不规则的稀疏计算重构为**对齐Tensor Core的、规则的微密集数据流**，并引入**自适应并行**以维持高利用率。
    *   **关键技术**：
        1.  **权重打包**：设计一种解码友好的打包表示，联合存储非零值和位置信息，减少独立的索引加载和解码开销。
        2.  **微密集执行**：将N:M窗口中的N个非零值直接物化为与Tensor Core MMA指令兼容的固定大小密集瓦片。这样Tensor Core始终处理规则密集碎片，稀疏的不规则性被约束在轻量级的加载与解码阶段，实现“N:M稀疏→微密集瓦片→Tensor Core数据流”的映射。
        3.  **自适应并行**：采用Split-K并行策略，沿归约维度K分割，多个线程块并行计算部分和，以提升SM占用率。通过一种规则驱动的策略自动选择**splitK**：优先确保足够块数使SM满载，同时限制splitK取值范围以控制归约和额外写回开销，避免在小K场景下性能反降。
*   **软件侧：延迟接地的逐层稀疏搜索**
    *   **核心思想**：在端到端延迟预算约束下搜索最优逐层N:M配置，使搜索目标与实际部署行为对齐。
    *   **延迟建模**：基于测量，为每种算子和稀疏度构建**算子级稀疏-延迟查找表(LUT)**。再通过少量全模型实测，用非负最小二乘法拟合一个**一阶线性模型**来预测端到端延迟，预测误差小于5%，提供常数时间的可行性检查。
    *   **搜索算法**：设计一种**可行区域感知的(1+λ)进化搜索**，通过三阶段子代生成克服延迟约束导致搜索空间碎片化、易陷入局部可行岛的问题：
        1.  **超越**：随机挑选若干层，故意向更密集方向移动（如4:8→5:8），暂时违反延迟约束，打破可行边界附近的搜索停滞。
        2.  **修复**：随机采样层逐步增加稀疏性，直至满足延迟约束，避免贪婪修复造成的方向性偏差。
        3.  **精炼**：再沿可行边界，逐步尝试密集化并只接受不降低适应度（继承自超网的类准确率）的更新。
        最终，通过代数迭代和子代竞争，选出延迟约束下适应度最高的配置。

### 3. 实验设计
*   **数据集与场景**：图像分类任务，使用ImageNet-1K。为加速搜索，在ImageNet-300子集（300类）上进行搜索，验证其趋势一致性。
*   **Benchmark与对比方法**：
    *   **内核评估**：在A100、RTX 4060、RTX 4090 GPU上，测量多形状、多稀疏度（50%, 75%, 87.5%）下的矩阵乘法延迟。对比基线：**cuBLAS**（密集）和**nmSPARSE**（N:M稀疏）。
    *   **搜索验证**：在DeiT-Small、DeiT-Base、Swin-Small模型上，设定1.5x、2.0x、2.5x目标加速。与层间稀疏搜索方法**Evo-Press**对比，均在相同超网和延迟预测器下进行。
    *   **整体评估**：在A100上进行INT8推理，对比不同**搜索策略**（均匀、EvoPress、本法）与不同**执行后端**（nmSPARSE、MD-SpMM）的组合效果，分析准确率-加速比平衡。

### 4. 资源与算力
*   **搜索阶段**：完整搜索流程（包含超网训练和200代进化搜索）在**三块NVIDIA A100 GPU**上运行，总计耗时约**10小时**。
*   内核评估和端到端推理测试未明确记录总体时长，但报告了单次延迟测量。
*   代码公开，便于复现。

### 5. 实验数量与充分性
实验设计较为全面且客观。
*   **内核实验**：覆盖3种GPU、大量矩阵形状和稀疏度组合，并针对自适应并行策略进行了消融分析（Top-1命中率、最差性能等），对比充分。
*   **搜索实验**：在3种模型、3个目标加速档下，与代表性搜索方法进行了轨迹和最终质量对比，证明了算法在碎片化可行空间中的稳定性。
*   **整体协同消融**：通过组合不同搜索与内核策略，直接验证了“协同设计”的必要性：单独改进任一方都无法达到最优。
*   **公平性**：使用了标准基准和公开的强基线（cuBLAS, nmSPARSE），并将自己的搜索方法嵌入到现有的一次性搜索框架中，与EvoPress公平比较。

### 6. 主要结论与发现
*   **N:M稀疏的实用性在于协同**：将N:M稀疏转化为实际加速，必须联合优化**稀疏执行**和**稀疏分配**。
*   **MD-SpMM高效可靠**：通过微密集执行和自适应并行，平均达到cuBLAS的**2.0倍**加速、nmSPARSE的**1.6-4.0倍**加速，在不同平台和形状下稳定。
*   **延迟接地搜索更优**：三阶段搜索策略能有效探索碎片化的可行空间，找到更高精度且满足延迟的层间稀疏配置。
*   **端到端收益**：整体框架在DeiT等模型上实现了**超过2.2倍**的延迟加速，同时精度提升可达**+1.4%** Top-1，显著优于传统策略组合。

### 7. 优点
*   **问题定义清晰，视角务实**：明确将“理论节省≠实际加速”归因于执行和配置的双重可实现性，针对真实部署。
*   **硬件设计巧妙**：通过**微密集瓦片**将稀疏运算完美融入Tensor Core数据流，并用解码友好的打包和自适应并行化解不规则性，设计思路清晰且效果显著。
*   **搜索策略针对性强**：三阶段进化搜索（超越-修复-精炼）专门应对延迟约束带来的搜索空间碎片化问题，具有创新性。
*   **评估体系完整**：涵盖内核微基准、搜索算法对比和端到端协同消融，层层递进验证观点，开源代码增强了可信度。

### 8. 不足与局限
*   **任务与模型覆盖有限**：仅在ViT系列模型和图像分类任务上验证，未扩展至语言模型（如BERT）或其他任务，泛化性待考。
*   **硬件平台依赖**：针对NVIDIA Tensor Core GPU优化，且主要在A100/40系上测试，迁移到其他架构可能需要重新设计或调优。
*   **搜索成本仍较高**：虽然使用了图像子集，但完整的超网训练与搜索仍需3块A100和10小时，对资源受限用户不够友好。
*   **稀疏格式限定**：仅支持N:M半结构化稀疏，无法直接应用于其他结构化或非结构化稀疏模式。
*   **延迟模型简化**：线性累加延迟模型虽在文中场景下误差小，但对于存在复杂图融合或并行优化的场景可能精度下降。

（完）
