---
title: "Memory-Efficient Activation Checkpointing with Sliding Window and Hirschberg's Algorithm for 0/1 Knapsack Solving in PyTorch"
title_zh: PyTorch 中基于滑动窗口与 Hirschberg 算法求解 0/1 背包的内存高效激活检查点
authors: Jędrzej Maczan
date: 2026-08-09
pdf: "https://arxiv.org/pdf/2608.08740v1"
tags: ["query:mlsys"]
score: 8.0
evidence: PyTorch中内存高效的激活检查点求解器
tldr: "在PyTorch的激活检查点优化中，需要求解0/1背包问题来决定哪些中间张量存储、哪些重计算，而默认的dp_knapsack求解器分配完整动态规划表，峰值内存高达O(nW)，导致操作数n=100时在64GB内存机器上直接崩溃。本文提出的dp_knapsack_sliding_hirschberg方法，同时应用滑动窗口技巧与Hirschberg算法，将峰值内存需求从O(nW)降至O(W)级别，且不失最优性。实验成功处理了n=2000的大型实例，而dp_knapsack在n=100即失败，可计算问题规模扩大20倍，并且运行时速度一致提升25-28%。该实现已被合并进PyTorch 2.10版本，为大规模神经网络训练提供了更高效的内存管理方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.08740v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 795, \"height\": 801, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.08740v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1431, \"height\": 474, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.08740v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1178, \"height\": 255, \"label\": \"Table\"}]"
motivation: PyTorch默认的dp_knapsack激活检查点求解器需要O(nW)内存，在n=100时即导致64GB内存崩溃，限制了大规模模型训练。
method: 本文结合滑动窗口技巧与Hirschberg算法，提出dp_knapsack_sliding_hirschberg，将峰值内存从O(nW)降至O(W)且保持最优解。
result: "实验成功求解n=2000的实例，可计算规模较dp_knapsack扩大20倍，并获得25-28%的运行时加速。"
conclusion: 该实现已合并入PyTorch 2.10，为神经网络激活检查点提供了高效且可扩展的精确求解方案。
---

## 摘要
激活检查点通过在给定内存预算下选择要存储和要重计算的中间张量，来最小化神经网络运行时间。PyTorch 将此问题作为 0/1 背包问题求解：联合前向-反向计算图中的运算被视为物品，具有内存成本（重量）和运行时间节省（价值）。默认求解器 dp_knapsack 分配一个形状为 $(n+1) × (W+1)$ 的完整动态规划表，其中 $n$ 是运算数量，$W$ 是量化内存预算。该方法资源消耗大，在 64 GB 内存的机器上 $n = 100$ 时会崩溃。本文提出 dp_knapsack_sliding_hirschberg，它结合滑动窗口技巧和 Hirschberg 算法，将峰值内存从 $O(nW)$ 降至 $O(W)$，同时保持精确最优解。实验表明，在 $n = 2000$ 时能够成功执行背包求解，而 dp_knapsack 在 $n = 100$ 时失败，可计算问题规模提升了 20 倍。此外，基准测试显示相较于 dp_knapsack 一致地获得 25-28% 的运行时加速。该实现已合入 PyTorch，并在 2.10 版本中发布。

## Abstract
Activation checkpointing minimizes the runtime of neural networks under a given memory budget, by selecting which intermediate tensors to store and which to recompute. PyTorch solves this as a 0/1 knapsack problem, where operations from a joint forward-backward computation graph are items with a memory cost (weight) and a runtime saving (value). The default solver, dp_knapsack, allocates a full dynamic programming (DP) table of shape $(n+1) \times (W+1)$, where $n$ is the number of operations and $W$ is the quantized memory budget. This method is resource-hungry and crashes at $n = 100$ items on a machine with 64 GB RAM.   In this paper, we introduce dp_knapsack_sliding_hirschberg, which combines the sliding window trick and Hirschberg's algorithm to reduce peak memory from $O(nW)$ to $O(W)$ while preserving the exact optimal solution. Our experiments show successful knapsack execution at $n = 2000$, where dp_knapsack fails at $n = 100$, a 20$\times$ increase in computable problem size. In addition, our benchmarks show a consistent 25-28\% runtime speedup over dp_knapsack.   The implementation is merged into PyTorch and released in version 2.10.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究背景**：在深度学习训练中，激活检查点（activation checkpointing）通过决定哪些中间张量存储、哪些重计算，来在给定内存预算下最小化运行时间。
- **核心问题**：PyTorch 将这一决策建模为 **0/1 背包问题**：联合前向-反向计算图中的运算为物品，内存成本为重量，运行时间节省为价值。默认求解器 `dp_knapsack` 使用完整动态规划表，内存复杂度为 \(O(nW)\)，其中 \(n\) 是运算数，\(W\) 是量化内存预算。
- **现实瓶颈**：该求解器在 \(n=100\)、\(W \approx 3.8 \times 10^8\) 时，DP 表约需 304 GB 内存，在 64 GB RAM 的机器上直接 OOM 崩溃，严重限制了大规模模型或长序列图的训练。
- **整体含义**：本文提出一种内存高效且保持精确最优的求解器，使 PyTorch 激活检查点能够扩展到更大规模计算图，并已合入 PyTorch 2.10。

## 2. 论文提出的方法论

- **核心思想**：
  - **滑动窗口技巧**：计算最优值时只需 DP 表的两行，将内存从 \(O(nW)\) 降至 \(O(W)\)。
  - **Hirschberg 分治算法**：在不存储完整 DP 表的情况下，通过分治策略恢复精确的最优物品选择，弥补滑动窗口无法回溯的缺陷。

- **算法流程（`dp_knapsack_sliding_hirschberg`）**：
  1. 使用显式 **LIFO 栈** 代替递归，避免大规模 \(n\) 时栈溢出。
  2. 每个栈帧保存索引范围 \([ℓ, r)\) 和当前容量 \(c\)。
  3. 基本情况：若 \(r - ℓ = 1\)，判断该物品是否保存（重量可容纳且价值为正）或重计算。
  4. 一般情况：取中点 \(m = ℓ + \lfloor (r-ℓ)/2 \rfloor\)，分别对左半 \([ℓ, m)\) 和右半 \([m, r)\) 用滑动窗口 DP 计算容量 profile \(P_1, P_2\)。
  5. 找到最优容量分割 \(c^* = \arg\max_{0 \le k \le c} (P_1[k] + P_2[c-k])\)。
  6. 将两个子问题 \((m, r, c-c^*)\) 和 \((ℓ, m, c^*)\) 压入栈。
  7. 最终得到保存列表与可重计算列表并排序输出。

- **复杂度**：
  - **空间**：\(O(W + n)\)，即四个长度为 \(W+1\) 的 DP 缓冲区、\(O(\log n)\) 栈帧和输出列表。
  - **时间**：\(O(nW \log n)\)，比基线 \(O(nW)\) 多一个 \(\log n\) 因子，但实际运行更快。

## 3. 实验设计

- **数据集/场景**：使用**合成实例**，模拟真实激活内存规划场景；容量 \(W\) 随 \(n\) 变化，从 \(n=1\) 时的 \(1.4 \times 10^8\) 增长至 \(n=100\) 时的 \(3.8 \times 10^8\)。
- **对比方法**：
  - `greedy_knapsack`：贪婪启发式，最快但可能次优。
  - `ilp_knapsack`：基于 SciPy 的整数线性规划求解器，精确但依赖外部库。
  - `dp_knapsack`：PyTorch 默认基线，完整 DP 表。
  - `dp_knapsack_sliding_hirschberg`（**本文方法**）。
- **评价指标**：
  - 运行时（每个实例 1000 次运行取平均）。
  - 最优性差距（与最优解比较）。
  - 可计算最大问题规模（是否 OOM）。

## 4. 资源与算力

- **硬件**：一台 **64 GB RAM** 的机器，CPU 为 **AMD Ryzen 7 9800X3D**，操作系统 Ubuntu 24.04.2。
- **算力规模**：仅使用单机 CPU，**未提及任何 GPU**，也未涉及多机或集群训练。
- **运行次数**：每个配置平均 1000 次运行；最大案例 \(n=2000\) 时报告峰值内存 58.4 GB（接近物理内存上限）。
- **明确说明**：论文未给出总实验耗时或能耗，也未使用 GPU。

## 5. 实验数量与充分性

- **实验组数**：
  - 主要基准测试在 \(n = 5, 10, 20, 50, 100\) 五档上进行，对比四种求解器。
  - 额外展示了 \(n=2000\) 的可扩展性案例。
  - 最优性差距测试覆盖多组实例，`greedy_knapsack` 差距从 0% 到 7.4% 不等。
- **充分性**：
  - **优点**：同机同条件下重复 1000 次，对比公平；覆盖了从极小到中大规模，并比较了启发式、ILP 和 DP 基线。
  - **不足**：全部为**合成实例**，未在真实 `torch.compile` 图或实际模型训练任务上验证；未设置只使用滑动窗口或只使用 Hirschberg 的消融实验；未测试不同内存预算分布或真实图结构下的鲁棒性。
- **客观性**：实验条件一致，且明确说明 `ilp_knapsack` 更快但依赖 SciPy，`greedy` 更快但非最优；本文方法定位在精确性与大规模可计算性之间，结论较为客观。

## 6. 论文的主要结论与发现

- **内存大幅降低**：峰值内存从 \(O(nW)\) 降至 \(O(W)\)，DP 表在 \(n=100\) 时从约 304 GB 缩至约 6 GB。
- **可扩展性提升 20 倍**：`dp_knapsack_sliding_hirschberg` 成功求解 \(n=2000\)，而 `dp_knapsack` 在 \(n=100\) 即 OOM。
- **意外加速**：尽管渐近时间多一个 \(\log n\) 因子，实际运行速度比 `dp_knapsack` 快 **25–28%**，归因于固定缓冲区和更优的内存访问模式。
- **保持最优性**：与 `dp_knapsack` 和 `ilp_knapsack` 一样，始终得到精确最优解；而 `greedy_knapsack` 可能低于最优解最多 7.4%。
- **生产落地**：实现已合并进 PyTorch 2.10。
- **使用建议**：有 SciPy 可用且追求速度时用 `ilp_knapsack`；仅需近似解时用 `greedy_knapsack`；否则在大规模或内存受限图上使用 `dp_knapsack_sliding_hirschberg`。

## 7. 优点

- **算法创新且实用**：将经典滑动窗口和 Hirschberg 分治算法成功组合并适配到真实生产系统，解决了明确的内存瓶颈。
- **保持精确性**：没有牺牲最优解，仅优化内存使用，对内存规划这类需要精确决策的场景至关重要。
- **实际加速**：不仅纯降低内存，还因为内存访问模式改善而获得 25–28% 的运行时提升。
- **工程贡献**：已合入 PyTorch 2.10，对机器学习社区有直接价值。
- **实验可复现性**：使用标准硬件、明确参数、多次重复测量，结果可信。

## 8. 不足与局限

- **时间复杂度的理论代价**：\(O(nW \log n)\) 渐近上劣于基线的 \(O(nW)\)，在某些极端输入下理论时间可能更差。
- **实验覆盖有限**：
  - 仅在**合成实例**上验证，未在真实模型或 `torch.compile` 捕获的图中测试。
  - 缺乏**消融实验**（例如单独使用滑动窗口或单独使用 Hirschberg 的效果对比）。
  - 未评估在**GPU 显存受限**或分布式训练场景下的表现。
- **未涉及真实工作负载多样性**：真实计算图的物品权重/价值分布可能差异很大，合成数据未必全面反映。
- **\(n=2000\) 峰值 58.4 GB** 仍接近 64 GB 物理内存，更大规模仍可能受限；空间复杂度 \(O(W+n)\) 中输出列表 \(O(n)\) 在极端 \(n\) 下不可忽略。
- **依赖外部库对比**：实验中的 `ilp_knapsack` 速度更快但依赖 SciPy，本文未说明在无 SciPy 环境下如何权衡。
- **论文中的一处表述矛盾**：第 4 节末尾推荐中疑似出现“ilp_knapsack when exact solutions don’t matter”的文字，但 ILP 实际是精确求解器，可能是笔误，读者需注意。

（完）
