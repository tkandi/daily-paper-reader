Title: Two-level domain-decomposition AdaGrad method for scalable training of graph neural networks

URL Source: https://arxiv.org/pdf/2608.22575v1

Published Time: Tue, 25 Aug 2026 01:20:49 GMT

Number of Pages: 26

Markdown Content:
# Two-level domain-decomposition AdaGrad method for scalable training of graph neural networks 

## Laurynas Varnas 1,2 , Julien Herrmann 1,6 , Alexander Heinlein 4,Serge Gratton 1,2,3 , Alena Kopaničáková 1,2,3,5* 

> 1

Institut de Recherche en Informatique de Toulouse, Toulouse, France. 

> 2

Artificial and Natural Intelligence Toulouse Institute, Toulouse, France. 

> 3

Toulouse-INP (ENSEEIHT), Toulouse, France. 

> 4

Delft Institute of Applied Mathematics, Delft University of Technology, Delft, Netherlands. 

> 5

Department of Computer Science, University of Geneva, Geneva, Switzerland. 

> 6

Centre National de la Recherche Scientifique, France. *Corresponding author(s). E-mail(s): Alena.Kopanicakova@unige.ch; Contributing authors: laurynas.varnas@toulouse-inp.fr; julien.herrmann@irit.fr; A.Heinlein@tudelft.nl; serge.gratton@toulouse-inp.fr; 

Abstract 

Graph neural networks (GNNs) have emerged as a powerful framework for learning from graph-structured data. However, their efficient training remains challenging, particularly in distributed computing environments. This challenge arises from the use of message passing, which couples all graph nodes, lead-ing to expensive optimization steps, high memory requirements, and substantial communication overhead. To alleviate these limitations, we propose a novel domain-decomposition (DD) variant of AG2m , an AdaGrad method enhanced with second-order curvature information and momentum, denoted by DD-AG2m . The proposed DD-AG2m alternates between AG2m optimization on the original (global) graph and AG2m optimization on the partitioned graphs. To incorporate global information at reduced cost, we further introduce a two-level variant ( 2DD-AG2m ) that performs global optimization steps on a coarse graph obtained by randomly subsampling nodes within each subdomain. Numerical 

1arXiv:2608.22575v1 [math.NA] 23 Aug 2026 experiments spanning graph classification, node-level regression, and spatiotem-poral forecasting tasks demonstrate that the proposed DD methods reduce the computational cost required to achieve the same predictive performance by a factor of 4–8. Moreover, for the fixed computational cost, they improve the predictive performance of GNNs by up to 22% compared with the baseline  

> AG2m .
> Keywords: GNN, training, AdaGrad, domain-decomposition, multilevel optimization

# 1 Introduction 

Graph Neural Networks (GNNs) have emerged as a powerful class of neural archi-tectures for learning from graph-structured data by combining entity attributes with relational information encoded by the graph topology [1–3]. Following the early recur-rent formulation of Scarselli et al. [1], several architectural principles have been introduced, including spectral graph convolutions [4, 5], inductive neighborhood aggre-gation [6], attention-weighted aggregation [7], and higher-order or substructure-aware models designed to improve expressivity [8, 9]. Many of these architectures can be expressed within the message-passing (MP) framework [10], in which the graph topol-ogy plays a dual role: it describes the relational structure of the data and determines the computational dependencies through which information is propagated. GNNs have been applied to a wide range of problems in which the relations between entities carry information that cannot be captured from their attributes alone, such as molecular property prediction and atomistic modeling [10, 11], social and recom-mender systems [6], and knowledge-graph reasoning [12, 13]. Of particular interest are scientific applications in which the graph represents the discretization of a physical or spatiotemporal system. For instance, GNNs have been used as graph-based surrogate models for physical simulation [14], mesh-based numerical simulation [15], computa-tional fluid dynamics [16], and traffic forecasting [17]. Moreover, weather forecasting provides a prominent large-scale spatiotemporal application, with GraphCast [18] and GenCast [19] employing mesh-based GNNs for deterministic and probabilistic global forecasting, respectively. In such applications, increasing the spatial resolution pro-duces larger and denser graphs, and therefore raises the computational and memory costs of both MP and backpropagation. Despite the success of GNNs accros wide range of applications, training GNNs efficiently at scale remains a significant challenge. In contrast to dense feed-forward networks, MP layers combine sparse neighborhood aggregation with dense transforma-tions of node or edge representations. Irregular graph topologies and non-uniform node degrees therefore induce indirect memory accesses and workload imbalance, which can limit accelerator utilization and complicate parallel execution [20, 21]. When training is distributed across multiple devices, MP dependencies additionally require exchang-ing node features and gradients between workers. The irregularity and volume of these exchanges hinder communication–computation overlap, increase synchronization costs, and may ultimately dominate the training time [22, 23]. 2These memory and computational demands have motivated a variety of methods that operate on reduced computation graphs. Node- and layer-wise sampling restrict the neighborhoods explored to a set of target nodes [6, 24], whereas subgraph-based methods construct mini-batches from connected graph regions, either through graph partitioning, as in Cluster-GCN [25], or subgraph sampling with normalized stochas-tic estimators, as in GraphSAINT [26]. Distributed GNN systems additionally address graph placement, remote feature access, workload balancing, and inter-device com-munication [22, 23, 27]. These approaches primarily reorganize or approximate the forward and backward computations. Closer to our setting are methods based on local training over graph partitions. For example, LLCG [28] trains independent copies of a GNN on graph partitions, applies periodic parameter averaging, and uses global server corrections to compensate for the dependencies omitted during local training. Its analysis shows that periodic averaging alone may retain a residual error when cross-partition dependencies are ignored. Randomized partitioning has alternatively been proposed to improve model aggregation by reducing the discrepancy between the data distributions, losses, and gradients observed by the local trainers [29]. Other approaches preserve part of the missing graph information directly during MP; for instance, DIGEST [30] periodically synchronizes stale representations of remote neighbors. Meanwhile, the numerical analysis community has extensively developed domain-decomposition (DD) methods [31–33] for solving, in parallel, large-scale systems of equations arising from the discretization of partial differential equations (PDEs). The idea behind these methods is to decompose the original problem into smaller subprob-lems that can be solved concurrently, while coordinating the information exchange among them in order to recover a globally coherent solution. Owing to this design, DD methods provide a natural framework for parallelization, and they rank among the most scalable solution strategies in scientific computing available to date. The interplay between DD and machine learning has recently attracted signif-icant attention; see [34, 35] for a comprehensive overview. In this work, we focus on approaches for accelerating and parallelizing neural network training. In this spirit, several methods based on decomposing either the network parameters or the training data have been proposed and led to model-parallel [36, 37] or data-parallel training [38], respectively. Moreover, authors of [39, 40] introduced a recursive bound-constrained AdaGrad framework, which extends AdaGrad-type methods to the multilevel and DD settings while accounting for stochastic gradient estimates. Complementarily, [41] proposed a multiscale approach to GNN training based on a hierarchy of coarsened graphs. Motivated by these developments, we propose two DD-based variants of Ada-Grad for the parallel training of GNNs. The proposed methods extend the recursive bound-constrained AdaGrad framework of [40] to the training of GNNs, with the decomposition induced directly by the structure of the input graph. The central idea is to partition the input graph into subgraphs and to perform independent local opti-mization steps on the resulting subgraphs using temporary copies of a shared global model. The local corrections are subsequently aggregated to update the parameters of the global model, in the spirit of additive Schwarz methods [32]. To incorporate 3global information at a reduced cost, we further introduce a two-level variant, which performs global optimization steps using a coarse graph, obtained by per-subdomain random node subsampling. In contrast to conventional distributed GNN training methods, which primarily focus on computational scalability, the proposed approach treats graph partitioning as a nonlinear preconditioning mechanism for the Ada-Grad optimizer, thereby enhancing convergence speed while also exposing additional parallelism. The remainder of the paper is organized as follows. In Section 2, we review GNNs and formulate the associated training problem. Section 3 introduces the proposed 

DD-AG2m and 2DD-AG2m algorithms, which constitute the main contribution of this work. In Section 4, we describe the benchmark problems, together with the algo-rithmic and implementation details. Section 5 demonstrates the convergence and the algorithmic scalability properties of the proposed algorithms. Finally, in Section 6, we summarize our findings and discuss possible future extensions. 

# 2 Graph Neural Networks (GNN) 

GNNs are neural architectures designed to process data whose underlying structure is represented by a graph [2, 3]. 

## 2.1 The Graph Neural Network Model 

The input data of a GNN are represented by an attributed graph. Formally, a graph is defined as a pair G = ( V, E), where V denotes the set of nodes and E ⊂ V × V denotes the set of edges connecting pairs of nodes. For any node v ∈ V, the neighborhood 

N (v) is defined as the set of nodes sharing an edge with v, i.e., 

N (v) = {u ∈ V : ( u, v ) ∈ E or (v, u ) ∈ E} .

The connectivity of G is encoded by the (possibly weighted) adjacency matrix 

A ∈ R|V|×| V|, whose entry Auv is nonzero if and only if u and v are connected. Each node v carries a feature vector xv ∈ Rd, encoding node-specific attributes (e.g., physical quantities, sensor measurements, or pixel intensities, depending on the application), with d being the number of features per node. These vectors are col-lected row-wise into the node feature matrix X ∈ R|V|× d. When available, each edge 

(u, v ) ∈ E also carries a feature vector euv .A GNN is a function fθ (·), parameterized by the parameters θ ∈ Rn, which maps an input graph G to a prediction fθ (G). The output may be associated with the complete graph, individual nodes, or node-time pairs. For instance, these settings correspond to graph classification, node regression, and spatiotemporal forecasting, respectively, all of which are considered in the numerical experiments of this work, see Section 5. 

## 2.2 Message-passing (MP) neural networks 

Many widely used local GNN architectures can be formulated within the message-passing (MP) framework [1, 10], which provides a flexible way to model dependencies 4h1                

> h2
> h3
> h4
> h6
> h5
> h7
> h8h9e12
> e14
> e13
> e16 e39
> e65
> e67
> e68
> e78
> a1= AGG θ{Mθ(h1,h2,h3,h4,h6,e12 ,e13 ,e14 ,e16 }
> ︸︷︷ ︸
> hu
> ︸︷︷ ︸
> evu

Fig. 1 : Illustration of MP aggregation for node v1 with feature vector h1. The sketch is adapted from [1]. between nodes in graph-structured data. In this framework, each node updates its representation by aggregating information from its local neighborhood, with the graph structure defining the corresponding computational dependencies. The initial hidden features of node v are given as h(0)  

> v

= xv . At each layer ℓ =0, . . . , L − 1, node representations are updated as 

a(ℓ) 

> v

= AGG θ,u ∈N (v)

n

M (ℓ)

> θ



h(ℓ) 

> v

, h(ℓ) 

> u

, euv 

o 

,

h(ℓ+1)  

> v

= U (ℓ)

> θ



h(ℓ) 

> v

, a(ℓ)

> v



,

where h(ℓ) 

> v

is the hidden state of node v at layer ℓ. The functions M (ℓ) 

> θ

and U (ℓ) 

> θ

,parametrized by θ, are the learnable message and differentiable update functions, respectively. Moreover, AGG θ denotes a permutation-invariant aggregation operator, such as element-wise sum, mean, maximum, or attention-weighted sum. Following the MP formulation of [10], the message function determines the information passed from a neighboring node to v based on the representations of the two nodes and, when present, the features of their connecting edge. The aggregation then combines these messages to form the input to the node update. Since each layer propagates information by at most one hop, the representation h(L) 

> v

obtained after L layers depends only on the information contained in the L-hop neighborhood of v, as depicted in Figure 1. The node representations obtained after L layers of MP, given by h(L) 

> v

, are mapped to task-specific predictions through a readout function Rθ . This defines the overall parameterized map 

fθ (G) = Rθ

n 

h(L) 

> v

: v ∈ V

o 

.

The specific form of Rθ depends on the task at hand. For node-level tasks, Rθ maps each final representation h(L) 

> v

independently to a node-wise prediction. For graph-level 5tasks, the representations {h(L) 

> v

: v ∈ V} are first combined through a permutation-invariant pooling operator, such as sum or mean, before being mapped to a single graph-level prediction. Spatiotemporal architectures may additionally include recur-rent or temporal-processing layers, but their graph component still follows the local MP structure described above. Note that, in the above description of MP, the vector θ ∈ Rn denotes the collection of all learnable parameters of the message, update, aggregation, and readout functions. The parameters of the message and update functions are shared across all nodes in the graph. Because the aggregation operator is invariant to the ordering of the incoming messages and the same functions are applied at every node, the resulting node representations are equivariant to permutations of the nodes. Furthermore, the MP framework imposes no restriction on the size of a node’s neighborhood. The MP formulation includes many commonly used architectures, for instance Graph Convolutional Networks [5], GraphSAGE [6], and Graph Attention Networks [7] mainly differ in their definitions of M (ℓ) 

> θ

, U (ℓ) 

> θ

, and AGG θ .

## 2.3 GNN training 

We consider a dataset D = (G(j), y(j)) Nj=1 of N samples, where G(j) is an attributed input graph, and y(j) contains its supervised target. Given a task-specific loss function that compares the prediction fθ (G(j)) with the corresponding target y(j), training consists of minimizing the following empirical risk: 

min  

> θ∈Rn

L(θ) := 1

N

> N

X

> j=1

loss 



fθ (G(j)), y(j)

. (1) The cost of solving (1) depends on the number of samples, as well as the size and the topology of the input graphs. Each MP layer combines sparse neighborhood aggregation with dense transformations of node or edge representations. Non-uniform node degrees consequently induce irregular workloads and memory-access patterns, which may reduce accelerator utilization and complicate parallel execution [20, 21]. These costs are further increased by gradient-based optimization, since back-propagation requires storing or recomputing intermediate node representations and MP dependencies across GNN layers. Moreover, increasing the number of MP lay-ers enlarges the receptive field of each node at the cost of increasing the amount of intermediate information involved in both the forward and backward computa-tions. The resulting memory footprint therefore scales as O(L |V| d′), growing with the network depth L, the size of the active graph |V|, and the width d′ of the hidden representations [42]. Furthermore, when a training sample is a very large graph, the memory required for full-graph processing may exceed the available device memory. Sampling, clustering, and graph partitioning address this limitation by restricting computations to smaller subgraphs [6, 25, 26]. This enables independent and potentially parallel processing of different subgraphs. However, restricting MP to subgraphs changes the loss function and its gradient. In contrast, the DD-based algorithms proposed in this work use 6Algorithm 1 AG2m (K, L, θ0, w0, m0, adjust )

Input: K ∈ N; L : Rn → R; θ0 ∈ Rn; w0 ∈ Rn

> ≥0

; m0 ∈ Rn; adjust ∈ { true, f alse }

Constants: β ∈ [0 , 1)  

> 1:

for k = 1 to K do  

> 2:

gk ≈ ∇θ L(θk−1) ▷ Get (stochastic) gradient estimate  

> 3:

wk,i =

q

w 2 

> k−1,i

+ g 2 

> k,i

▷ Compute AdaGrad weights  

> 4:

if k = 1 and adjust then  

> 5:

wk,i = max  wk,i , w0,i 

 ▷ Adjust weights based on the full-space  

> 6:

end if  

> 7:

∆k,i = |gk,i |

wk,i 

▷ Evaluate TR-radius  

> 8:

sSk,i = min  max  −gk,i , −∆k,i 

, ∆k,i 

 ▷ Compute CP step  

> 9:

Bk ≈ ∇2 

> θ

L(θk−1) ▷ Get Hessian approximation  

> 10:

sQk = γksSk , where ▷ Get second-order informed step-size 

γk =



min 1, −⟨ gk, sSk ⟩⟨sSk , BksSk ⟩

!

, if ⟨sSk , BksSk ⟩ > 0,

1, otherwise (2)  

> 11:

mk = βmk−1 + (1 − β)sQk ▷ Evaluate momentum  

> 12:

mk,i = min  max  mk,i , −∆k,i 

, ∆k,i 

 ▷ Enforce TR bound  

> 13:

θk = θk−1 + mk ▷ Update parameters  

> 14:

end for  

> 15:

return θK , wK , mK

subgraph and subsampled computations solely to construct local corrections, which are subsequently coordinated through global optimization steps to ensure consistency with the original full-graph training problem. 

# 3 Two-level DD-based AdaGrad for GNNs 

In this work, we minimize (1) using AdaGrad like algorithms from the family of 

objective-function-free optimization (OFFO) methods [39, 40, 43]. OFFO methods rely solely on (approximate) gradient information and, when available, (approxi-mate) second-order information, without requiring objective function evaluations. This property makes them robust to noise introduced by subsampling. 

## 3.1 Single-level curvature-informed AdaGrad with momentum 

We begin our presentation of the proposed algorithms by first discussing the single-level variant of AdaGrad, enhanced with curvature information [43] and momentum, which we abbreviate as AG2m . This algorithm constitutes the core computational engine of the proposed DD-AG2m and 2DD-AG2m methods. In contrast to the AdaGrad variant considered in [43], we also incorporate a momentum term. As outlined in Algorithm 1, at the k-th iteration, AG2m first evaluates the gradient or its subsampled approximation, i.e., gk ≈ ∇ L(θk−1). The accumulated squared 7gradients are then used to compute the coordinate-wise AdaGrad weights wk. These weights are then used to define the trust-region (TR) radius ∆k,i = |gk,i |/wk,i , where the subscript i denotes the i-th component of a vector. Following the terminology of TR methods [44], the vector ∆k defines a coordinate-wise, adaptive bound on the step size. Thus, if the accumulated weights increase, the corresponding admissible step sizes decrease, in turn reducing the magnitude of the admissible updates along coordinates that repeatedly exhibit large gradients. The Boolean flag adjust indicates whether the initial AdaGrad weights must be adjusted using the previously accumulated global (full-space) weights. As we will see later, this adjustment is performed whenever AG2m 

is invoked on a coarse or subgraph problem, ensuring that the size of their first AG2m 

step is bounded by the global weights. Once ∆k has been computed, AG2m first determines the step sSk obtained by trun-cating the negative gradient direction to the TR. The quality of this search direction is then improved by scaling it with a step-size γk ∈ R, computed from (approximate) second-order information. Specifically, γk is computed such that γksSk minimizes the local quadratic model ⟨gk, s⟩ + 12 ⟨s, Bks⟩ along the direction sSk , where Bk is either Hessian ∇2L(θk−1) or its symmetric approximation. Note that if curvature is negative or the resulting step size is larger than one, the direction sSk is kept. Finally, the step sQk = γksSk is incorporated into the momentum as 

mk = βmk−1 + (1 − β)sQk . This momentum is subsequently bounded to ensure that the resulting update satisfies −∆k,i ≤ mk,i ≤ ∆k,i . The vector mk is then used to update the GNN parameters, yielding the new iterate θk. The adopted momentum scheme follows the same algorithmic structure as in [45, 46]. Moreover, it falls within the class of methods analyzed recently in [47], where the global convergence rate is established for a broad family of adaptive first-order methods with momentum. 

## 3.2 DD-based AdaGrad for GNNs 

Training GNNs on large-scale graphs is computationally demanding, as each param-eter update requires MP over the entire graph. To improve scalability and exploit parallelism, we adopt a DD strategy, well known from solving large-scale systems associated with discretized PDEs [32]. In particular, we propose DD-AG2m , a DD-based variant of the AG2m [39, 40] method, specifically designed for training GNNs. The proposed DD-AG2m method partitions the input graph into multiple subgraphs, enabling optimization steps to be performed independently and in parallel on each subgraph. The resulting subgraph updates are subsequently synchronized to produce an update for the GNN parameters. Following the DD terminology, throughout the description of the DD-AG2m 

algorithm we refer to computations on subgraphs as subdomain computations and computations on the original graph as global computations . Superscripts are used to distinguish quantities associated with the global graph or subgraphs. Moreover, we use double subscripts to denote the outer and inner iterations of the DD-AG2m algorithm. For instance, θpk,K denotes the model parameters associated with the p-th subdomain after the k-th outer ( DD-AG2m ) iteration and the K-th inner ( AG2m ) iteration. 8V1

> V2
> V3

Fig. 2 : An example of non-overlapping graph partitioning. The original graph (left) and a partition of the node set V into P = 3 disjoint subsets V1, V2, V3 (right). 

3.2.1 Global and subdomain minimization problems 

The DD-AG2m is associated with the minimization of global and subgraph loss functions. The global loss function LG is defined using original graphs, i.e., LG := L.The subdomain losses are defined on the partitioned graphs. Thus, given a graph 

G, we partition the node set V into P disjoint subsets {V1, . . . , VP } such that 

Vi ∩ Vj = ∅ (i̸ = j),

> P

[

> p=1

Vp = V.

Each partition induces a subgraph Gp = ( Vp, Ep), where Ep contains only edges whose endpoints both belong to Vp, i.e., all inter-partition edges are removed 1. An illustration of the non-overlapping graph partitioning is provided in Figure 2. Each subgraph is associated with a subdomain loss, given as 

Lp(θp) := 1

N

> N

X

> j=1

loss 



fθp (G(p,j )), y(p,j )

, p = 1 , . . . , P, (3) where the upper script (p, j ) denotes p-th partition of j-th sample. All subdomain losses {Lp}Pp=1 are defined using the same GNN architecture as the global problem (1). Consequently, each subdomain maintains its own parameter vector θp ∈ Rn, which has the same structure as the global parameter vector θ, since only the graph is decomposed, while the model parameters are not. 

3.2.2 DD-AG2m algorithm 

Having defined subdomain losses (3), we now describe the DD-AG2m method, out-lined in Algorithm 2. Starting from the current iterate θk−1,0, each iteration of 

DD-AG2m consists of three stages. First, AG2m is applied to the global loss LG := L

for KG inner steps, producing the iterate θk−1,K G . This global step operates on the entire graph G and therefore ensures global information propagation. 

> 1Although our formulation assumes non-overlapping partitions, the proposed framework can be naturally extended to overlapping graph partitions.

9Algorithm 2 DD-AG2m (LG, {Lp}Pp=1 , θ0,0, w0,0, m0)

Input: LG : Rn → R; {Lp}Pp=1 , where each Lp : Rn → R; θ0,0 ∈ Rn, w0,0 ∈ Rn

> ≥0

,

m0 ∈ Rn

Constants: P, K, K G, {Kp}Pp=1 ∈ N 

> 1:

for k = 1 to K do 

▷ Perform KG AG2m steps using the fine graph  

> 2:

θk−1,K G , wk−1,K G , mk−1,K G = AG2m (KG, LG, θk−1,0, wk−1,0, mk−1,0, f alse ) 

> 3: 4:

for p = 1 to P in parallel do  

> 5:

▷ Perform Kp AG2m steps using p-th subgraph  

> 6:

θpk,K G+Kp , . . . = AG2m (Kp, Lp, θk−1,K G , wk−1,K G , mk−1,K G , true ) 

> 7:

spk = θpk,K G+Kp − θk−1,K G ▷ Evaluate subdomain correction  

> 8:

end for  

> 9:

θk, 0 = θk−1,K G + PPp=1 1 

> P

spk ▷ Update global GNN with DD corrections  

> 10:

end for  

> 11:

return θK, 0

Afterward, the DD step is performed on the partitioned graphs by applying AG2m 

to minimize each subdomain loss Lp for Kp inner steps. The subdomain optimization process is initialized using a copy of the global parameters, obtained after minimiza-tion of the global loss, i.e., θk−1,K G . Moreover, the AdaGrad weights are initialized by utilizing the weights wk,K G , computed after the global optimization step. In particu-lar, the initial subdomain weights are constructed by taking the maximum between the weights constructed during the first subdomain step and the global AdaGrad weights (i.e., AG2m is invoked with adjust = true ). This ensures that the first subdomain correction satisfies |s1,i | ≤ ∆1,i ≤ | g1,i |/w0,i , for each component i, thereby bound-ing its magnitude using the corresponding global AdaGrad weights, see [40]. Since the subdomain minimization problems are independent of each other, this DD phase can be carried out in parallel. After the DD phase is completed, the obtained correction is computed as 

spk = θpk,K G+Kp − θk−1,K G , for p = 1 , . . . , P . These corrections are then reconciled into a single global update, denoted by sDD k ∈ Rn. Here, we construct sDD k ∈ Rn

by means of averaging, i.e., sDD k = 1

> P

PPp=1 spk. Afterwards, the parameters of global GNN, θk−1,K G , are updated as θk, 0 = θk−1,K G + sDD k . This requires only a single communication step, consisting of a global reduction of all corrections across the sub-domains. In the end, we point out that more sophisticated aggregation strategies have recently been proposed in [48] and could be incorporated into the proposed DD framework in future work. The algorithm then proceeds to the next outer iteration. 

## 3.3 Two-level DD-based AdaGrad for GNNs 

The DD-AG2m algorithm exploits subgraph computations to accelerate AG2m ’s con-vergence while enabling parallelism. Its main computational bottleneck is the KG

AG2m steps performed on the global graph G, which are inherently serial. To reduce 10 V1 V2 V3 V1 V2 V3

> Random subsampling

Fig. 3 : Sketch of the per-subdomain random node subsampling to create the coarse graph. The original graph (left) and the coarse graph GC , formed by the selected nodes together with the edges inherited between them (right). this cost, we replace several global optimization steps with optimization steps on a coarsened graph. This follows the standard two-level DD approaches [49], in which a coarse level efficiently transfers global information at reduced cost. 

3.3.1 Coarse (subsampled graph) minimization problem 

To define the coarse minimization problem, we first construct the coarse graph. Fol-lowing [41], we express the coarsening process through a transfer operator that maps the original graph data onto a coarse representation. Concretely, let R ∈ { 0, 1}nc×| V|

denote a binary restriction (subsampling) operator, whose rows are the indicator vectors of the nc retained coarse-level nodes. The retained nodes are selected by ran-dom subsampling with coarsening factor cf , performed independently within each subdomain, while ensuring that at least one node is retained from every subdomain. Thus, for every subdomain Vp, a random subset of nodes is kept and the remain-ing nodes are dismissed. The operator R then restricts the global graph node set to the coarse node set as XC = R X . This defines the coarse graph GC = ( VC , EC )

with node features XC . The edges are thus directly inherited from G. Using standard Galerkin projection, well-known from multigrid, the adjacency matrix AC is con-structed as AC = R A R ⊤. Moreover, for node-level tasks, the labels are restricted analogously, yC = R y . Note, performing the random node subsampling per subdo-main ensures that the coarsening respects the partitioning. At the same time, the retained inter-subdomain edges keep GC globally connected and therefore reflective of the global structure of graph G. An illustration of the employed coarsening strategy is given in Figure 3. The coarse-level loss function LC : Rn → R is defined as 

LC (θC ) := 1

N

> N

X

> j=1

loss( fθC (G(C,j )), y(C,j )), (4) where the superscript (C, j ) denotes j-th coarse sample. Similarly to subdomain losses, 

LC is evaluated using the GNN with the same architecture as the global problem (1). Thus, the transfer operator R acts only on the graph data, i.e., the node features, adjacency matrix, and labels. The parameters of global GNN, θ, are not coarsened, 11 Algorithm 3 2DD-AG2m (LG, LC , {Lp}Pp=1 , θ0,0, w0,0, m0)

Input: LG := L : Rn → R; LC : Rn → R; {Lp}Pp=1 , where each Lp : Rn → R; θ0,0 ∈ Rn,

w0,0 ∈ Rn

> ≥0

, m0 ∈ Rn

Constants: P, K, K G, K C , {Kp}Pp=1 ∈ N 

> 1:

for k = 1 to K do 

▷ Perform KG AG2m steps using the original graph  

> 2:

θk−1,K G , wk−1,K G , mk−1,K G = AG2m (KG, LG, θk−1,0, wk−1,0, mk−1,0, f alse )

▷ Perform KC AG2m steps using the coarse graph  

> 3:

θk−1,K G+KC , _, _ = AG2m (KC , LC , θk−1,K G , wk−1,K G , mk−1,K G , true )

▷ Perform KG AG2m steps using the original graph  

> 4:

θk−1, 2KG+KC , wk−1, 2KG+KC , mk−1, 2KG+KC =

AG2m (KG, LG, θk−1,K G+KC , wk−1,K G+KC , mk−1,K G , f alse ) 

> 5:

for p = 1 to P in parallel do 

▷ Perform Kp subdomain AG2m steps using p-th subgraph  

> 6:

θpk, 2KG+KC +Kp , _, _ =

AG2m (Kp, Lp, θk−1, 2KG+KC , wk−1, 2KG+KC , mk−1, 2KG+KC , true ) 

> 7:

spk = θpk, 2KG+KC +Kp − θk−1, 2KG+KC ▷ Evaluate subdomain correction  

> 8:

end for  

> 9:

θk, 0 = θk−1, 2KG+KC + PPp=1 1 

> P

spk ▷ Update global GNN with DD corrections  

> 10:

end for  

> 11:

return θK, 0

thus θC ∈ Rn. This is possible as the weight matrices act on the feature dimension rather than on the node dimension, so that the same MP operations can be applied on the global and the coarse graphs [41]. We note that [41] considers additional coarsening strategies that could be explored within the proposed DD framework in future work. 

## 3.4 2DD-AG2m algorithm 

Algorithm 3 summarizes the proposed two-level DD-AG2m algorithm, denoted as 

2DD-AG2m . As we can see, 2DD-AG2m follows the same structure as DD-AG2m .However, the global steps are alternating, not just with subdomain steps, but also with coarse steps. Here, we point out that the convergence of the 2DD-AG2m is controlled by the full-space AdaGrad weights. To this end, AG2m is invoked with 

adjust = true during both the coarse and the subdomain minimization, so that the initial step sizes during coarse and subdomain minimization are restricted by the global AdaGrad weights. We also highlight that both DD-AG2m and 2DD-AG2m use momentum during the subdomain and coarse optimization. Following [46], the corresponding momentum variables are initialized from the global momentum, allowing the subdomain/coarse optimizations to benefit from the optimization history associated with the global objective. However, the subdomain and coarse momentums are not propagated back. 12 Table 1 : Summary of the benchmark problems and the associated experimental setup.                           

> CIFAR10 AirfRANS METR-LA Learning task Graph classification Node regression Spatiotemporal regression GNN architecture GCN [5] GraphSAGE [16] DCRNN [17] Number of layers 442
> Layer width 146 64 32
> Loss function Cross-entropy MSE Masked MAE Graph partitioner Spectral [50] Morton ordering [51] Multilevel Graclus [52] Nodes per graph 85 –150 32 ,000 207 1
> Batch size 32 164

This prevents optimization histories specific to the coarse/subdomain objectives from being mixed with the momentum associated with the global objective. 

# 4 Numerical examples and implementation details 

In this section, we describe the benchmark problems utilized to evaluate the proposed 

DD-AG2m and 2DD-AG2m algorithms, together with their algorithmic configu-ration and implementation details. The selected benchmark problems cover diverse learning tasks, GNN architectures, and graph partitioning strategies, enabling an assessment of the convergence behavior of the proposed DD-based methods across a broad range of settings. Table 1 summarizes the characteristics of all benchmark problems and the corresponding algorithmic setup. 

## 4.1 Benchmark problems 

We consider the following three benchmark problems: 

4.1.1 Super-pixel CIFAR10 

Our first benchmark problem concerns graph classification on the CIFAR10 super-pixel dataset. This dataset is derived from the CIFAR10 image dataset [53], which contains 32 × 32 RGB images from ten object categories. In particular, we use the preprocessed super-pixel graphs from the benchmark suite of [54], constructed using the Simple Linear Iterative Clustering (SLIC) algorithm for image segmentation [55]. SLIC partitions each image into spatially compact regions of pixels with similar color characteristics, which define the graph nodes whose features consist of the mean RGB intensity of the super-pixel and its two-dimensional centroid coordinates. Edges con-nect each super-pixel to its eight nearest neighbors and are weighted using a Gaussian kernel, resulting in graphs with approximately 85 –150 nodes; see Figure 4. We use the standard split of 45 ,000 / 5,000 / 10 ,000 graphs for training, validation, and testing, respectively.   

> 1For METR-LA, all samples share a single static graph with 207 nodes, whereas for CIFAR10 and AirfRANS each sample has its own graph.

13 Fig. 4: Example of a sample from CIFAR10 and its corresponding super-pixel graph [54]: the original image (left), the super-pixel nodes (middle left), the k-nearest-neighbor graph (middle right), and the resulting graph (right). Pressure divided by specific mass [m 2/s 2] ux [m/s] 

> −1500
> −1000
> −500 0500
> −10 010 20 30 40 50 60

Fig. 5 : Example of flow fields around a NACA airfoil from the AirfRANS dataset: the pressure divided by specific mass (left) and the velocity field in the x-direction (right). Graph classification is performed using a graph convolutional network (GCN) [5], with the architecture specified in [54], which comprises four graph convolutional layers with a hidden width of 146 , batch normalization, residual connections, and mean readout. The GCN is trained with the cross-entropy loss. We partition the graphs using spectral partitioning [50], for which the adjacency matrix is converted to a dense representation. 

4.1.2 AirfRANS 

Next, we consider a physics-based regression benchmark in computational fluid dynamics, based on the AirfRANS dataset [16]. It contains high-fidelity solutions of the two-dimensional incompressible steady-state Reynolds-averaged Navier–Stokes (RANS) equations over airfoils from the NACA 4- and 5-digit families. Each sample represents the steady turbulent flow around a single airfoil in the subsonic regime for Reynolds numbers in [2 × 10 6, 6 × 10 6] and angles of attack in [−5◦, 15 ◦]. Each node is described by five input features, i.e., the two free-stream velocity components, the distance to the airfoil surface, and the two components of the surface normal (set to zero at interior nodes). The regression targets are the two local velocity components, the reduced pressure (i.e., the pressure divided by the fluid density), and the turbulent kinematic viscosity. Representative pressure and velocity fields are shown in Figure 5. 14 Following [16], we consider the scarce-data setting with 200 training and 200 test-ing simulations. Each simulation is provided as an unstructured point cloud without explicit connectivity. As in the baseline setup, we uniformly subsample each simu-lation to 32 ,000 nodes and construct a sparse geometric graph by connecting nodes whose normalized Euclidean distance is below 0.05 .We employ the baseline GraphSAGE architecture of [16], comprising four SAGE convolution layers of width 64 , preceded by an MLP encoder and followed by an MLP decoder. The network is trained using the mean squared error (MSE) loss. Graph partitioning is performed using a Morton (Z-order) space-filling curve [51], i.e., the normalized point coordinates are converted to Morton codes, sorted, and divided into contiguous segments of equal size. We choose this partitioning because it relies solely on the spatial coordinates and is therefore independent of the graph connectivity. To construct the coarse-level dataset, we randomly sample 32 ,000 /c f nodes from the original point cloud and connect them using a radius graph with radius 0.1.

4.1.3 METR-LA 

Finally, we evaluate the proposed optimization algorithms on the METR-LA traffic forecasting dataset [17]. This dataset comprises traffic speed measurements from 207 loop detectors deployed on the highway network of Los Angeles County, collected over four months at five-minute intervals. The learning task is formulated as a node-level spatiotemporal regression problem, in which future traffic conditions are predicted from historical observations on a fixed road network graph. Each sensor corresponds to a graph node, with traffic measurements represented as node features. In particular, each node is associated with two input features per time step, i.e., the traffic speed and a time-of-day encoding. We use the graph provided with the dataset, whose edge weights are obtained by applying a thresholded Gaussian kernel to the pairwise road-network distances between connected sensors (Figure 6). We use the preprocessed METR-LA dataset provided by PyTorch Geometric Tem-poral [56, 57]. Following the standard forecasting setup of [17], each input sample contains the previous 12 time steps (corresponding to one hour of observations) and the model predicts the following 12 time steps. The traffic measurements are nor-malized using the training-set statistics. The resulting 34 ,249 sliding-window samples are split into 20 ,550 / 6,850 / 6,849 samples for training, validation, and testing, respectively. We model the traffic dynamics using the diffusion convolutional recurrent neu-ral network (DCRNN) [17], which combines diffusion convolutions with a GRU-based sequence-to-sequence model. The employed GNN consists of two DCRNN layers, map-ping the two input features to 32 hidden channels and preserving this width in the second layer, followed by a linear output layer. Training and evaluation use the masked mean absolute error (MAE) of [17], which ignores missing measurements. The road-network graph is partitioned using the multilevel Graclus algorithm [52], which operates directly on the sparse graph and incorporates edge weights throughout the coarsening process, thereby preserving the road-network connectivity. Our imple-mentation of the multilevel Graclus algorithm uses the graph matching and coarsening routines provided by PyTorch Geometric [58]. The initial partitioning of the coarsest 15 Fig. 6 : Example of a sample from the METR-LA dataset [17]: the sensor locations (left) and the corresponding graph partitioned into 5 subgraphs (right). graph and the refinement during uncoarsening follow the procedures of [52]. Follow-ing the heuristic used in [52], we coarsen the graph until the coarsest level contains approximately 5P nodes, where P denotes the target number of partitions. The parti-tion labels are then propagated back to the original graph through iterative refinement during uncoarsening. 

## 4.2 Algorithmic and implementation details 

The GNN models and the proposed training algorithms are implemented in Python 3.12 and rely on PyTorch 2.8 [59], PyTorch Geometric [58], and PyTorch Geometric Temporal [56, 57]. All GNN weights are initialized using Xavier initializa-tion [60], while all biases are initialized to zero. For batch normalization layers, the scale parameters are initialized to one and the biases to zero. Unless stated otherwise, both DD-AG2m and 2DD-AG2m use a momentum parameter of β = 0.9. The coordinate-wise AdaGrad weights are initialized as 

w0,i = 0 .01 for all i = 1 , . . . , n . The computation of γk in (2) requires the evaluation of the term ⟨sSk , BksSk ⟩. Rather than explicitly assembling the Hessian Bk, we compute the required Hessian-vector products using PyTorch automatic differentiation. Graph partitioning is performed once before training, and the resulting partitions are used throughout all DD iterations, since graph partitioning is computationally expensive and the resulting decomposition is deterministic. In contrast, the coarse graph is regenerated at each epoch via per-subdomain random node subsampling with a prescribed coarsening factor (cf ), defined as the fraction of nodes retained in each subdomain. This is due to the fact that the coarse graph construction is relatively inexpensive. Moreover, performing it at every epoch allows for different random coarse representations to be used throughout training, rather than relying on a single fixed subsampling. The remaining algorithmic parameters, namely the number of outer iterations ( K), the numbers of global ( KG), coarse ( KC ), and subdomain steps ( Kp), as well as the number of partitions P , are specified separately, as they vary across the experiments. 16 0 2 4 6

> ·10 4
> 0.30.40.5

C    

> acc val
> 00.511.52
> ·10 5
> 10 −1
> 10 −0.5

CLval  AG2 AG2m Fig. 7 : Convergence of AG2 and AG2m on the CIFAR10 (left) and AirfRANS (right) benchmark problems. The results are reported in terms of the validation accuracy (acc val ) for CIFAR10 and the validation loss ( Lval ) for AirfRANS. All experiments were performed on the Jean Zay supercomputer, using GPU nodes equipped with an NVIDIA V100 GPU ( 32 GB of memory) and a 12-core Intel Cascade Lake 6226 CPU. 

# 5 Numerical results 

In this section, we evaluate the proposed algorithms on the previously described bench-mark problems. To compare the training algorithms under consideration, we report the estimated parallel computational cost C, expressed in units of global-graph opti-mization steps. Since the cost of a AG2m optimization step is proportional to the size of the graph on which the MP is performed, a global optimization step on the full graph has a unit cost. A subdomain optimization step, executed concurrently on all P partitions, has a cost of 1/P , as each partition contains approximately 1/P of the nodes of the full graph. Moreover, an optimization step performed on a coarse graph, constructed with the coarsening factor cf , has a cost of 1/cf . As a consequence, the estimated computational cost accumulated over K outer iterations is given as 

C := 

> K

X

> k=1



KG + Kp

P + KC

cf



, (5) where KG, Kp, and KC denote the numbers of global, subdomain, and coarse-level optimization steps performed within each outer iteration. All reported numerical results and plots are averaged over four random seeds. 

## 5.1 Convergence properties of AG2m 

We begin by investigating the impact of the momentum on the convergence of the single-level algorithm. To this aim, we compare AG2 , i.e., the momentum-free curvature-informed AdaGrad [43], with its momentum-enhanced variant AG2m . As we can see from Figure 7, incorporating the momentum improves the convergence. In particular, on CIFAR10, AG2m converges faster than AG2 during the initial phase of the training, while reaching a comparable final validation accuracy. On AirfRANS, 17 0 2 4 6

> ·10 4
> 0.40.50.6

C   

> acc val
> 0246
> ·10 4

C   

> 0246
> ·10 4

C        

> AG2m KG= 1 KG=|D|/2KG=|D|

Fig. 8 : Convergence of AG2m and DD-AG2m with varying KG for the CIFAR10. 

DD-AG2m is executed with P = {2, 3, 5} (from left to right), and Kp = |D|.0 0.5 1

> ·10 5
> 0.40.45 0.50.55 0.6

C    

> acc val
> 02468
> ·10 4
> 0.10.15 0.2

CLval   

> 123
> ·10 4
> 0.21 0.22

CLval         

> AG2m Kp= 100 Kp=|D|Kp= 2 · | D|

Fig. 9 : Convergence of AG2m and DD-AG2m (P = 2 , KG = |D|) with varying Kp

for the CIFAR10 (left), for AirfRANS (middle) and for METR-LA (right). 

AG2m attains a lower validation loss than AG2 for a comparable computational cost. Based on these results, we employ momentum for all subsequent experiments and considered algorithms. 

## 5.2 Convergence properties of DD-AG2m 

Next, we investigate the convergence properties of the DD-AG2m algorithm. In par-ticular, we study the sensitivity of DD-AG2m with respect to the number of global steps KG, the number of subdomain steps Kp, and the number of partitions P .We start by varying the number of global steps KG, while keeping the number of subdomain steps fixed to Kp = 352 , which corresponds to one epoch, i.e., the full pass through the dataset. Figure 8 reports the results obtained for the CIFAR10 bench-mark problem with P ∈ { 2, 3, 5}. As we can see, using a small number of global steps (KG = 1 ) accelerates the initial phase of the training, since the computational cost of an outer iteration is dominated by the inexpensive subdomain steps. However, increas-ing the number of global steps ( KG ∈ { 176 , 352 }) yields a higher final validation accuracy, with this improvement becoming more pronounced as the number of parti-tions increases. Notably, for all considered configurations, DD-AG2m surpasses the 

AG2m baseline in terms of the validation accuracy reached for a given computational cost C.18 0 2 4 6

> ·10 4
> 0.40.45 0.50.55 0.6

C    

> acc val
> 02468
> ·10 4
> 0.10.15 0.2

CLval    

> 0123
> ·10 4
> 0.21 0.22

CLval       

> AG2m P= 2 P= 3 P= 5

Fig. 10 : Convergence of AG2m and DD-AG2m (KG = Kp = |D|) with increasing number of partitions P for the CIFAR10 (left), AirfRANS (middle), and METR-LA (right). 0 0.5 1

> ·10 5
> 0.40.50.6

C  

> acc val
> 00.51
> ·10 5

C  

> 00.51
> ·10 5

C     

> AG2m KC=|D|KC= 2 |D|

Fig. 11 : Convergence of AG2m and 2DD-AG2m (KG = 10 , c f = 2 ) with varying 

KC for the CIFAR10, P = 2 (left), P = 3 (middle), and P = 5 (right). We continue by studying the effect of the number of subdomain steps Kp. Figure 9 reports the results obtained for all considered datasets with P = 2 , KG = |D|, and 

Kp ∈ { 100 , |D|, 2|D|} . As we can see, using Kp = 2 |D| provides the best or comparable performance across all datasets. Based on our experimental experience, substantially increasing the number of subdomain steps Kp beyond 2|D| may lead to overfitting of the subgraph models, potentially reducing the benefit of the subsequent global aggregation phase. Finally, we examine the algorithmic scalability of DD-AG2m with respect to an increasing number of partitions P . As we can see from Figure 10, for all three bench-mark problems, DD-AG2m gives rise to a more accurate GNN model than AG2m for a given computational cost C. We also observe that the convergence of DD-AG2m 

does not deteriorate with an increasing P , which is essential for the parallel scalability of the proposed approach. 

## 5.3 Convergence properties of 2DD-AG2m 

At the end, we investigate the convergence properties of 2DD-AG2m with respect to the number of coarse steps KC and the utilized coarsening factor cf . The performed experiments utilize KG = 10 .19 0 2 4 6       

> ·10 4
> 0.10.15 0.20.25
> Lval
> 0246
> ·10 4
> 00.511.52
> ·10 4
> 0.21 0.22

CLval     

> 00.511.52
> ·10 4

C   

> AG2m
> P= 2
> P= 3
> P= 5

Fig. 12 : Convergence of AG2m and 2DD-AG2m (KG = 10 , K C = Kp = |D|) on the AirfRANS (top) and METR-LA (bottom), with the coarsening factors cf = 2 (left) and cf = 4 (right) for different numbers of graph partitions P .We start by investigating the effect of the number of coarse steps KC on the conver-gence behavior of 2DD-AG2m . The coarse optimization phase replaces a substantial number of expensive full-graph optimization steps with cheaper optimization steps on the coarsened graph. Figure 11 reports the results obtained for the CIFAR10 bench-mark problem. As observed, introducing the coarse optimization phase improves the convergence compared to the AG2m baseline, leading to higher final validation accu-racy. Moreover, utilizing KC = |D| and KC = 2 |D| yields comparable performance, which indicates that the number of coarse steps can be reduced without significantly affecting the final solution, thereby improving the parallel scalability. We next study the behavior of 2DD-AG2m when varying the number of par-titions P and the coarsening factor cf . Figure 12 reports the convergence results obtained for the AirfRANS and METR-LA benchmark problems. For both datasets, increasing the coarsening factor from cf = 2 to cf = 4 generally improves the con-vergence, with the difference becoming more significant as the number of partitions increases. This effect is particularly evident for AirfRANS, where combining a larger coarsening factor with a higher number of partitions leads to the largest performance and accuracy improvements. 

## 5.4 Comparison between AG2m , DD-AG2m and 2DD-AG2m 

Finally, we compare the convergence of AG2m , DD-AG2m , and 2DD-AG2m on all three benchmark problems. Figure 13 reports the results obtained with P = 5 par-titions. As we can see, both DD-based methods consistently outperform the AG2m 

baseline, producing more accurate GNN models for a given computational cost C.Furthermore, 2DD-AG2m converges faster than DD-AG2m during the early stages of training, and on CIFAR10 it also reaches a notably higher final validation accu-racy. These results highlight the benefits of incorporating the coarse-level steps, which 20 0 0.5 1

> ·10 5
> 0.40.50.6

C    

> acc val
> 02468
> ·10 4
> 0.10.15 0.2

CLval   

> 0.511.5
> ·10 4
> 0.21 0.22 0.23

CLval   

> AG2m DD-AG2m 2DD-AG2m

Fig. 13 : Convergence of AG2m , DD-AG2m (KG = Kp = |D|, P = 5 ) and 2DD-AG2m (KG = 10 , K C = Kp = |D|, P = 5 ) on the CIFAR10 (left, cf = 2 ), AirfRANS (middle, cf = 4 ), and METR-LA (right, cf = 4 ). enable the exchange of global information at a fraction of the cost of the full-graph optimization steps. 

# 6 Conclusion 

In this work, we proposed DD-AG2m and 2DD-AG2m , two DD-based algorithms for the scalable training of GNNs. Both algorithms are built upon AG2m , a curvature-informed AdaGrad method with momentum [43]. They combine optimization steps on graph partitions with optimization steps performed either on the full graph ( DD-AG2m ) or on a coarse graph obtained by random subsampling within each subdomain (2DD-AG2m ). To evaluate the proposed methods, we conducted numerical experi-ments on graph classification, node-level regression, and spatiotemporal forecasting tasks using different GNN architectures and graph partitioning strategies. Our results demonstrate that DD-AG2m and 2DD-AG2m require up to 4–8× fewer optimiza-tion steps than AG2m to achieve the same predictive performance. At the same time, 

DD-AG2m and 2DD-AG2m improve the predictive performance of GNNs by up to 

22% for the same number of optimization steps. Moreover, the performance remained stable as the number of graph partitions increased, demonstrating the algorithmic scalability of the proposed algorithms. We foresee several extensions of the presented work. In particular, we plan to develop a distributed-memory implementation to translate the observed reduction in computational cost into a wall-clock speedup. We also aim to investigate the use of overlapping subdomains, alternative coarsening strategies, and multilevel extensions with more than two levels. Finally, we plan to design asynchronous variants, including their practical implementation and analysis of their convergence properties. 

Acknowledgements. Authors thank A. Kssim for many insightful discussions and for providing the initial implementation of AG2m algorithm. This work benefited from ANITI (Grant No. ANR-23-IACL-0002) and the DAIMOS project (Grant No. ANR-25-EXNU-0002). The numerical results were carried out using HPC resources from GENCI-IDRIS (Grant No. AD011015766R1). 21 References 

[1] Scarselli, F., et al. : The graph neural network model. IEEE Transactions on Neu-ral Networks 20 (1), 61–80 (2009) https://doi.org/10.1109/TNN.2008.2005605 [2] Battaglia, P.W., et al. : Relational inductive biases, deep learning, and graph networks. arXiv (2018) https://doi.org/10.48550/arXiv.1806.01261 [3] Wu, Z., et al. : A comprehensive survey on graph neural networks. IEEE trans-actions on neural networks and learning systems 32 (1), 4–24 (2020) https: //doi.org/10.1109/TNNLS.2020.2978386 [4] Defferrard, M., et al. : Convolutional neural networks on graphs with fast localized spectral filtering. In: Neural Information Processing Systems, vol. 29, pp. 3844– 3852 (2016). https://doi.org/10.48550/arXiv.1606.09375 [5] Kipf, T.N., Welling, M.: Semi-Supervised Classification with Graph Convolu-tional Networks. arXiv (2017). https://doi.org/10.48550/arXiv.1609.02907 [6] Hamilton, W.L., Ying, R., Leskovec, J.: Inductive representation learning on large graphs. In: Adv. Neural Inf. Process. Syst. (NeurIPS), vol. 30, pp. 1024–1034 (2017). https://doi.org/10.5555/3294771.3294869 [7] Veličković, P., et al. : Graph attention networks. In: Int. Conf. Learn. Represent. (ICLR) (2018). https://doi.org/10.48550/arXiv.1710.10903 [8] Morris, C., et al. : Weisfeiler and Leman go neural: Higher-order graph neural networks. In: Proc. AAAI Conf. Artif. Intell., vol. 33, pp. 4602–4609 (2019). https://doi.org/10.1609/aaai.v33i01.33014602 [9] Bouritsas, G., et al. : Improving graph neural network expressivity via subgraph isomorphism counting. IEEE Trans. Pattern Anal. Mach. Intell. 45 (1), 657–668 (2023) https://doi.org/10.1109/TPAMI.2022.3154319 [10] Gilmer, J., et al. : Neural message passing for quantum chemistry. In: Proc. 34th Int. Conf. Mach. Learn. (ICML), pp. 1263–1272 (2017). https://doi.org/10. 48550/arXiv.1704.01212 [11] Schütt, K.T., et al. : Equivariant message passing for the prediction of tenso-rial properties and molecular spectra. In: International Conference on Machine Learning, pp. 9377–9388 (2021). https://doi.org/10.48550/arXiv.2102.03150 [12] Schlichtkrull, M., et al. : Modeling relational data with graph convolutional networks. In: The Semantic Web (ESWC 2018). Lect. Notes Comput. Sci., vol. 10843, pp. 593–607. Springer, Cham (2018). https://doi.org/10.1007/ 978-3-319-93417-4_38 [13] Chami, I., et al. : Low-dimensional hyperbolic knowledge graph embeddings. In: 22 Proc. 58th Annu. Meet. Assoc. Comput. Linguist. (ACL), pp. 6901–6914 (2020). https://doi.org/10.18653/v1/2020.acl-main.617 [14] Sanchez-Gonzalez, A., et al. : Learning to simulate complex physics with graph networks. In: International Conference on Machine Learning, pp. 8459–8468 (2020). https://doi.org/10.48550/arXiv.2002.09405 [15] Pfaff, T., et al. : Learning mesh-based simulation with graph networks. In: Int. Conf. Learn. Represent. (ICLR) (2021). https://doi.org/10.48550/arXiv.2010. 03409 [16] Bonnet, F., et al. : AirfRANS: High fidelity computational fluid dynamics dataset for approximating Reynolds-averaged Navier-Stokes solutions, vol. 35, pp. 23463– 23478 (2022). https://doi.org/10.48550/arXiv.2212.07564 [17] Li, Y., et al. : Diffusion convolutional recurrent neural network: Data-driven traffic forecasting. In: Int. Conf. Learn. Represent. (ICLR) (2018). https://doi.org/10. 48550/arXiv.1707.01926 [18] Lam, R., et al. : Learning skillful medium-range global weather forecasting. Science 382 (6677), 1416–1421 (2023) https://doi.org/10.1126/science.adi2336 [19] Price, I., et al. : Probabilistic weather forecasting with machine learning. Nature 

637 (8044), 84–90 (2025) https://doi.org/10.1038/s41586-024-08252-9 [20] Jia, Z., et al. : Redundancy-free computation for graph neural networks. In: Proc. 26th ACM SIGKDD Int. Conf. Knowl. Discov. Data Min., pp. 997–1005 (2020). https://doi.org/10.1145/3394486.3403142 [21] Wang, Y., et al. : GNNAdvisor: An adaptive and efficient runtime system for 

GN N acceleration on GP U s . In: 15th USENIX Symposium on Operating Systems Design and Implementation (OSDI 21), pp. 515–531 (2021) [22] Wan, C., et al.: Bns-gcn: Efficient full-graph training of graph convolutional net-works with partition-parallelism and random boundary node sampling 4, 673–693 (2022) [23] Shao, Y., et al. : Distributed graph neural network training: A survey. ACM Computing Surveys 56 (8), 1–39 (2024) https://doi.org/10.1145/3648358 [24] Zou, D., et al. : Layer-dependent importance sampling for training deep and large graph convolutional networks. In: Adv. Neural Inf. Process. Syst. (NeurIPS), vol. 32, pp. 11247–11256 (2019). https://doi.org/10.48550/arXiv.1911.01860 [25] Chiang, W.-L., et al. : Cluster-GCN: An efficient algorithm for training deep and large graph convolutional networks. In: Proc. 25th ACM SIGKDD, pp. 257–266 (2019). https://doi.org/10.1145/3292500.3330925 23 [26] Zeng, H., et al. : GraphSAINT: Graph sampling based inductive learning method. In: Int. Conf. Learn. Represent. (ICLR) (2020). https://doi.org/10.48550/arXiv. 1907.04931 [27] Wan, C., et al.: Pipegcn: Efficient full-graph training of graph convolutional net-works with pipelined feature communication (2022) https://doi.org/10.48550/ arXiv.2203.10428 [28] Ramezani, M., et al.: Learn locally, correct globally: A distributed algorithm for training graph neural networks (2022) https://doi.org/10.48550/arXiv.2111. 08202 [29] Zhu, J., et al. : Simplifying distributed neural network training on massive graphs: Randomized partitions improve model aggregation. ACM Trans. Knowl. Discov. Data 19 (1), 1–26 (2025) https://doi.org/10.1145/3701563 [30] Chai, Z., Bai, G., Zhao, L., Cheng, Y.: Distributed graph neural network training with periodic historical embedding synchronization. arXiv preprint arXiv:2206.00057, 1–20 (2022) [31] Quarteroni, A., Valli, A.: Domain Decomposition Methods for Partial Differential Equations. Oxford University Press, Oxford (1999) [32] Toselli, A., Widlund, O.B.: Domain Decomposition Methods – Algorithms and Theory. Springer Series in Computational Mathematics, vol. 34. Springer, Berlin (2005). https://doi.org/10.1007/b137868 [33] Saad, Y.: Iterative Methods for Sparse Linear Systems, 2nd edn. SIAM, Philadel-phia (2003). https://doi.org/10.1137/1.9780898718003 [34] Klawonn, A., Lanser, M., Weber, J.: Machine learning and domain decomposition methods – a survey. Computational Science and Engineering 1(1), 2 (2024) https: //doi.org/10.1007/s44207-024-00003-y [35] Heinlein, A., Klawonn, A., Lanser, M., Weber, J.: Combining Machine Learn-ing and Domain Decomposition Methods – A Review. https://doi.org/10.1007/ s44207-024-00003-y [36] Kopaničáková, A., et al. : Enhancing training of physics-informed neural networks using domain-decomposition based preconditioning strategies. SIAM Journal on Scientific Computing 46 (5), 46–67 (2024) https://doi.org/10.1137/23M1583375 [37] Lee, Y., Kopaničáková, A., Karniadakis, G.E.: Two-level overlapping additive schwarz preconditioner for training scientific machine learning applications. Com-puter Methods in Applied Mechanics and Engineering 448 , 118400 (2026) https: //doi.org/10.1016/j.cma.2025.118400 24 [38] A. Cruz Alegría, S., Trotti, K., Kopaničáková, A., Krause, R.: Data-parallel neural network training via nonlinearly preconditioned trust-region method (2023) [39] Gratton, S., Kopaničáková, A., Toint, P.L.: Multilevel objective-function-free optimization with an application to neural networks training. SIAM Journal on Optimization 33 (4), 2772–2800 (2023) https://doi.org/10.1137/23M1553455 [40] Gratton, S., Kopaničáková, A., Toint, P.L.: Recursive bound-constrained Ada-Grad with applications to multilevel and domain decomposition minimization. arXiv (2025) https://doi.org/10.48550/arXiv.2507.11513 [41] Gal, E., et al. : Towards efficient training of graph neural networks: A multiscale approach. arXiv (2025) https://doi.org/10.48550/arXiv.2503.19666 [42] Li, G., et al. : Training graph neural networks with 1000 layers. In: International Conference on Machine Learning, pp. 6437–6449 (2021) [43] Gratton, S., Jerad, S., Toint, P.L.: Complexity and performance for two classes of noise-tolerant first-order algorithms. Optimization Methods and Software 40 (6), 1473–1499 (2025) https://doi.org/10.1080/10556788.2025.2532736 [44] Conn, A.R., Gould, N.I.M., Toint, P.L.: Trust-Region Methods. MPS-SIAM Series on Optimization. SIAM, Philadelphia (2000). https://doi.org/10.1137/1. 9780898719857 [45] Erway, J.B., et al. : Trust-region algorithms for training responses: Machine learn-ing methods using indefinite Hessian approximations. Optimization Methods and Software 35 (3), 460–487 (2020) https://doi.org/10.1080/10556788.2019.1624747 [46] Kopaničáková, A., Krause, R.: Globally convergent multilevel training of deep residual networks. SIAM Journal on Scientific Computing 45 (3), 254–280 (2023) https://doi.org/10.1137/21M1434076 [47] Gratton, S., Toint, P.L.: A unified convergence theory for adaptive first-order methods in the nonconvex case, including AdaNorm, full and diagonal AdaGrad, Shampoo and Muon. arXiv (2026) https://doi.org/10.48550/arXiv.2604.17423 [48] Salvadó-Benasco, M., et al. : Multi-preconditioned LBFGS for training finite-basis PINNs. arXiv (2026) https://doi.org/10.48550/arXiv.2601.08709 [49] Dolean, V., Jolivet, P., Nataf, F.: An Introduction to Domain Decomposition Methods: Algorithms, Theory, and Parallel Implementation. SIAM, Philadelphia (2015). https://doi.org/10.1137/1.9781611974065 [50] Von Luxburg, U.: A tutorial on spectral clustering. Stat. Comput. 17 (4), 395–416 (2007) https://doi.org/10.1007/s11222-007-9033-z 25 [51] Morton, G.M.: A computer oriented geodetic data base and a new technique in file sequencing. Technical report, IBM Ltd. (1966) [52] Dhillon, I.S., Guan, Y., Kulis, B.: Weighted graph cuts without eigenvectors: A multilevel approach. IEEE Transactions on Pattern Analysis and Machine Intel-ligence 29 (11), 1944–1957 (2007) https://doi.org/10.1109/TPAMI.2007.1115 [53] Krizhevsky, A.: Learning multiple layers of features from tiny images. Technical report, University of Toronto (2009) [54] Dwivedi, V.P., et al. : Benchmarking graph neural networks. J. Mach. Learn. Res. 

24 (43), 1–48 (2023) https://doi.org/10.48550/arXiv.2003.00982 [55] Knyazev, B., Taylor, G.W., Amer, M.: Understanding attention and generaliza-tion in graph neural networks, vol. 32, pp. 4202–4212 (2019). https://doi.org/10. 48550/arXiv.1905.02850 [56] Rozemberczki, B., et al. : PyTorch Geometric Temporal: Spatiotemporal signal processing with neural machine learning models. In: Proc. 30th ACM CIKM, pp. 4564–4573 (2021). https://doi.org/10.1145/3459637.3482014 [57] Ockerman, S., et al.: PGT-I: Scaling spatiotemporal GNNs with memory-efficient distributed training. arXiv (2025). https://doi.org/10.48550/arXiv.2507.11683 [58] Fey, M., Lenssen, J.E.: Fast Graph Representation Learning with PyTorch Geometric. ArXiv (2019) https://doi.org/10.48550/arXiv.1903.02428 [59] Paszke, A., et al. : PyTorch: An imperative style, high-performance deep learning library. In: Adv. Neural Inf. Process. Syst. (NeurIPS), vol. 32, pp. 8026–8037 (2019). https://doi.org/10.5555/3454287.3455008 [60] Glorot, X., Bengio, Y.: Understanding the difficulty of training deep feedforward neural networks. In: Proc. 13th AISTATS, pp. 249–256 (2010) 26