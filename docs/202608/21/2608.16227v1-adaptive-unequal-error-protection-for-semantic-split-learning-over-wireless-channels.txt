Title: Adaptive Unequal Error Protection for Semantic Split Learning over Wireless Channels

URL Source: https://arxiv.org/pdf/2608.16227v1

Published Time: Tue, 18 Aug 2026 01:44:26 GMT

Number of Pages: 5

Markdown Content:
> 1

# Adaptive Unequal Error Protection for Semantic Split Learning over Wireless Channels 

Vukan Ninkovic, Member, IEEE, Dejan Vukobratovic, Senior Member, IEEE, Dragisa Miskovic, Member, IEEE, 

Chao Wang, Member, IEEE 

Abstract —We propose a task-aware semantic split learning (SL) framework for wireless edge–cloud inference, in which the reliability of transmitted latent representations is dynamically adapted to their relevance for the downstream task. An au-toencoder (AE)-based physical (PHY) layer enables end-to-end learning of the communication interface, while unequal error protection (UEP) is realized via mutual information (MI)-driven prioritization of latent components during training. The gradient of the estimated MI with respect to each latent component serves as a sensitivity-based proxy for task relevance, providing a fully learning-driven prioritization that adapts to both the data distribution and the downstream task. We further show that this prioritization translates into measurable physical-layer effects: MI-guided UEP assigns significantly higher transmit power to the most task-critical latent components compared to the equal error protection (EEP) baseline. Experiments on real-world IoT sensing data demonstrate consistent gains over equal and fixed-UEP baselines across SNR regimes. Additional analysis confirms ranking stability, estimator robustness and generalization across datasets and task types, indicating broad applicability of the proposed framework. 

Index Terms —Semantic communications, split learning, au-toencoders, unequal error protection, mutual information. 

I. I NTRODUCTION 

Wireless IoT inference systems increasingly rely on trans-mitting intermediate representations rather than raw data, re-quiring communication mechanisms that prioritize information relevant for the downstream task under strict bandwidth and reliability constraints [1]. Semantic communication improves efficiency and robustness under such constraints by focusing on task-relevant representations [2]–[4]. Split learning (SL) distributes intelligence across edge de-vices and the cloud [5]. In SL, a lightweight model at the edge extracts intermediate features from raw data and sends them to the server for further inference. This reduces edge computation while establishing a semantic interface whose reliability directly impacts task performance. At the physical (PHY) layer, autoencoder (AE)–based de-signs replace conventional modulation and coding with end-

V. Ninkovic is with the University of Novi Sad, Serbia, and the Institute for Artificial Intelligence Research and Development of Serbia (e-mail: ninkovic@uns.ac.rs); D. Vukobratovic is with the University of Novi Sad, Serbia; D. Miskovic is with the Institute for Artificial Intelligence Research and Development of Serbia; C. Wang is with the College of Electronic and Information Engineering, Tongji University, Shanghai, China. This work is supported by the Serbian Ministry of Science, Technolog-ical Development and Innovation (Serbia–China Cooperation Project No. 00101957 2025 13440 003 000 620 021) and Intergovernmental International Science and Technology Innovation Cooperation of National Key Research & Development Program of China under Grant 2024YFE0197400. 

to-end trainable neural transceivers that adapt to noise and fad-ing [6]. Such designs naturally enable unequal error protection (UEP), allowing different reliability levels to be assigned to different parts of the transmitted representation [7], [8]. Recent work has explored semantic communication with AE-based PHY design and distributed learning [8], [9], in-cluding information bottleneck approaches [10], [11]. Recent semantic UEP schemes prioritize tokens or features by se-mantic importance [12]–[14], but do not derive UEP from the task relevance of individual latent components at the split-learning interface. In contrast, our approach uses the downstream task to identify the latent components that require stronger protection, enabling task-aware reliability allocation directly at the semantic split. The key novelty lies in reformulating UEP at the SL inter-face as a task-driven information allocation problem . Rather than relying on predefined protection classes or reconstruction-based criteria, protection levels are determined dynamically based on the contribution of each latent component to the prediction task, quantified via its mutual information (MI) with the target variable. The contributions are: (1) We develop a semantic SL framework in which intermediate representations are transmitted over a fading wireless channel using an AE-based PHY layer; (2) We propose a task-aware ranking crite-rion that dynamically prioritizes latent components based on MI sensitivity during end-to-end training; (3) We demonstrate that the learned prioritization induces PHY-layer reliability differentiation, allocating higher transmit power to task-critical components and improving inference performance over equal error protection (EEP) and fixed-UEP baselines. II. B ACKGROUND & S YSTEM FRAMEWORK 

A. Theoretical Background 

SL for edge–cloud inference: SL partitions a neural net-work F between an edge device and a central server [5]. Let 

X ∈ RN be the edge input and T (X) the task output. The overall model F is decomposed as F = fS ◦ fE (Fig. 1), where fE : RN → Rn and fS : Rn → R. The edge device computes S = ( s1, .., s n) = fE(X), S ∈ Rn, and the server produces an estimate ˆT = fS( ˜S), as illustrated in Fig. 1. By separating feature extraction from final inference, SL reduces communication overhead: the centralized baseline requires transmitting the full input of dimension N , whereas the proposed framework transmits a compressed latent repre-sentation of dimension n ≪ N (see Section IV-B). arXiv:2608.16227v1 [cs.IT] 17 Aug 2026 2

Semantic communication paradigm: In contrast to con-ventional communication systems, the receiver aims to gener-ate a task-relevant output ˆT ≈ T (X) directly from the received representation, without requiring faithful reconstruction of the original input [3], [4]. This objective can be formalized through the Information Bottleneck (IB) principle [15], which seeks a representation Z = (z1, . . . , z K ), Z ∈ RK that preserves task-relevant information about T while minimizing redundancy with the input: 

min I(X; Z) s.t. I(Z; T (X)) ≥ ϵ, (1) where I(·; ·) denotes MI and ϵ sets the required task fidelity. The key observation that motivates this work is that minimiz-ing reconstruction error does not guarantee preservation of the sufficient statistics of X for predicting T [16]: a representation may achieve high reconstruction fidelity while discarding fea-tures critical for the downstream task, leading to the divergence between reconstruction MSE and task-level accuracy reported in Section IV-B. Consequently, unlike IB/variational-IB meth-ods [15], which determine what information to retain in Z,the proposed framework addresses how transmission reliability should be allocated across the learned latent representation. 

AE-Based PHY design with learning-driven UEP: AE-based approaches replace conventional modulation and coding with an end-to-end trainable system [6]. Here, UEP can be captured by a weighted reconstruction objective applied in the latent space [7]: 

LUEP = λ X

> k∈K imp

∥ˆzk −zk∥2 +(1 −λ) X

> k / ∈K imp

∥ˆzk −zk∥2, (2) where Kimp ⊂ { 1, . . . , K } denotes task-critical latent compo-nents and λ ∈ (0 .5, 1) controls the prioritization strength. Prior AE-based UEP methods rely on predefined or reconstruction-driven criteria to determine Kimp [7], [16], without explicitly considering the downstream task [14]. In contrast, the pro-posed framework determines Kimp dynamically according to task relevance. Thus, in this work, we preserve the weighted-loss realization itself, but the contribution lies in the task-driven criterion for selecting the protected latent components, which is equally applicable to alternative UEP mechanisms. 

B. System Framework for Semantic Split Learning 

We consider a unified semantic split learning framework for edge–cloud inference that integrates SL, semantic communi-cation, and AE-based PHY design, as illustrated in Fig. 1. At the edge device, input X ∈ RN is processed by a semantic encoder fsem , extracting Z = fsem (X). A channel encoder fch 

then produces transmit signal S = fch (Z), so fE = fch ◦fsem .At the server, ˜S = C(S), where C represents the physical communication channel, is processed by channel decoder gch 

to yield ˆZ = gch ( ˜S), and passed to semantic decoder gsem 

for the task estimate ˆT = gsem ( ˆZ), i.e., fS = gsem ◦ gch . The overall end-to-end system is: 

ˆT = gsem (gch (C(fch (fsem (X))))) . (3) This separation enables explicit allocation of communication resources to task-relevant components, forming the basis for the proposed task-aware UEP. 

Fig. 1: SL-aided semantic communication architecture. III. P ROPOSED SEMANTIC SPLIT LEARNING FRAMEWORK WITH LEARNING -D RIVEN UEP 

A. Semantic Model Architecture and Split Design 

We consider a time-series IoT sensing task in which an edge device performs local feature extraction and collaborates with a server for inference. The semantic component is im-plemented as a two-layer long short-term memory (LSTM) network followed by a fully connected (FC) layer. The first LSTM layer, deployed at the edge, serves as fsem , while the second LSTM and FC serve as gsem . This split enables the edge to extract task-relevant features locally. Each LSTM layer employs K = 10 hidden units. The FC layer maps the final hidden state to a scalar output ˆT . For benchmarking, a centralized baseline is considered, in which all semantic com-ponents execute at the server without edge-side preprocessing. To verify that the proposed approach is not tied to a specific latent dimension, we additionally evaluate K ∈ {20 , 40 };results are reported in Section IV-C (Table I, top part). 

B. AE-Based PHY Layer and Communication Interface 

The semantic encoder output Z = fsem (X) is processed by channel encoder fch , yielding transmit signal S = fch (Z),normalized to satisfy an average power constraint. The channel encoder is a FC network with a single hidden layer of 10 neurons and batch normalization. The output dimension 

n controls communication bandwidth. We use two transmit dimensions, n ∈ { 5, 15 }.The channel is modeled as a real-valued Rayleigh block-fading channel: ˜S = C(S) = h ⊙ S + η, where h ∈ Rn

has entries hℓ ∼ Rayleigh(1) and η ∈ Rn is AWGN with ηℓ ∼ N (0 , σ 2). Fading coefficients vary independently across blocks. This standard abstraction [9] enables controlled evaluation of the proposed method. Unlike conventional re-ceivers, the proposed AE-based PHY layer does not require explicit CSI estimation; instead, gch learns an implicit in-verse of the channel statistics through end-to-end training [6], with robustness determined by the match between training 3

and deployment channel distributions. Practical extensions to distributional channel mismatch, hardware impairments, and comparisons with standardized channel coding schemes under latency constraints are left for future work. Notably, the pro-posed MI-guided prioritization operates at the representation level and is agnostic to the specific transmission scheme. At the server, ˜S is processed by channel decoder gch ,producing ˆZ = gch ( ˜S). Although there is no explicit power allocation layer, reliability differentiation emerges through the 

learned representation geometry : by assigning larger recon-struction weights to selected latent components in the loss, the optimizer maps those components to channel configurations with higher effective signal energy [7]. 

C. Learning-Driven UEP and End-to-End Training 

UEP is realized through a learning-driven mechanism that prioritizes task-relevant latent components. The importance of individual components is identified during training based on their task contribution. The L most task-relevant components of Z (L < K , L is a hyperparameter) are identified using MI between Z and target T . A neural discriminator fθ (Z, T ) (two-layer FC with ReLU) approximates I(Z; T ) via the Donsker– Varadhan (DV) bound [11]: 

ˆI(Z; T ) = EP (Z,T )[fθ (Z, T )] −log EP (Z)P (T )

h

efθ (Z,T ′)i

. (4) This criterion admits an information-theoretic interpretation. Since I(Z; T ) = H(T )−H(T |Z) and H(T ) is independent of 

Z, we have ∂I (Z; T )/∂z k = −∂H (T |Z)/∂z k. Consequently, 

|∂ ˆI/∂z k| provides a local, differential surrogate for the sufficient-statistics criterion underlying the IB principle. Under the first-order approximation, ∆ ˆI(Z; T ) ≈ P

> k

(∂ ˆI/∂z k)∆ zk,components with larger gradient magnitudes correspond to perturbation directions that most strongly affect task-relevant information. This interpretation is most reliable when latent components are not highly redundant, and perturbations re-main within the local linear regime. Importantly, the DV estimate is used only for relative ranking rather than absolute MI estimation, making the prioritization largely insensitive to estimator bias. The stability of this mechanism is empirically validated in Section IV-C (Fig. 4). Per batch, the gradient of ˆI(Z; T ) w.r.t. Z is computed, and the L components with the largest gradient magnitudes are marked as task-critical. The protection mechanism is fully learned and integrated: identification of Kimp and the update of the channel encoder/decoder are performed in a single, unified backward pass, with no separate or manually configured stage. The AE reconstruction loss follows Eq. (2), with Kimp 

(|K imp | = L) determined adaptively using MI-based ranking. 

Training procedure: All components fsem , fch , gch , gsem ,and fθ are trained jointly. The overall loss is: 

Ltotal = Ltask + LUEP + LMI , (5) where Ltask = E[( ˆT − T (X)) 2] and LMI is the negative DV bound for the MI estimator. Training uses Adam [17] with 

α = 9 × 10 −4, batch size 64, and 100 epochs. Per-batch SNR is sampled uniformly from [5 , 10] dB. The discriminator fθ

introduces only a modest training overhead: the MI estimator requires ≈ 7.1 × 10 4 floating point operations (FLOPs) per sample compared to the base model complexity of ≈ 8.5×10 4

FLOPs, corresponding to an 11 .7% increase in processing time per batch (measured wall-clock training time). For on-device personalization scenarios, the discriminator’s ≈ 3.6 × 10 4

parameters ( ≈144 kB at FP32) impose a modest memory footprint, with retraining overhead remaining in the same 

∼12% range; a detailed hardware-level characterization is left for future work. This overhead is incurred only during offline training; the deployed edge model retains the same runtime footprint as the baseline SL architecture, imposing no additional computation on IoT devices at inference time. IV. P ERFORMANCE EVALUATION 

A. Experimental Setup 

The proposed framework is evaluated using data from a real-world IoT environmental monitoring system on the Danube river near Novi Sad, Serbia. The dataset consists of 3,264 daily measurements over nine years (2013–2022), including dissolved oxygen, temperature, pH, and conductivity. The pre-diction task forecasts daily dissolved oxygen levels. Each input consists of N = 20 historical measurements, with the target the next-day value. Min–max scaling to [−1, 1] is applied. The dataset is split chronologically into 70% training/30% testing. 

B. Numerical Results 

We evaluate the impact of semantic preprocessing, latent dimension n, and UEP on task-level prediction and repre-sentation robustness over a Rayleigh block-fading channel. EEP corresponds to λ = 0.5 in Eq. (2) and MI part is neglected. As an additional UEP baseline, we consider a fixed prioritization scheme in which the first five semantic components are predefined as important for the case n = 15 .

Task-level prediction performance: Fig. 2 reports predic-tion MSE. Under EEP (left panel), semantic preprocessing consistently improves prediction accuracy across all SNRs. For both n ∈ { 5, 15 }, the LSTM encoder outperforms raw input transmission, highlighting the benefit of task-oriented feature extraction. The trade-off between bandwidth and robustness to channel impairments is also evident. The LSTM-based SL approach achieves lower prediction MSE than the centralized (raw input) baseline for both n = 5 and n = 15 .With UEP enabled (right panel), prediction performance improves further. Protecting more components ( L = 5 vs. 

L = 3 ) yields additional gains. The prioritization weight λ

plays a critical role: moderate prioritization ( λ = 0 .7) achieves the lowest prediction MSE, whereas stronger prioritization (λ = 0 .9) degrades performance toward EEP, indicating that over-concentrating protection on a small subset is suboptimal. MI-guided UEP significantly outperforms the fixed baseline with consistently lower prediction MSE across SNRs. The task-relevance of the MI-gradient ranking was empiri-cally confirmed via a transmission ablation study: transmitting only the top-5 ranked components yields MSE = 0 .00382 ,vs. 0.00741 for random selection and 0.014 for bottom-

5 (at SNR=10 dB) – a >4× gap. A perturbation analysis shows that injecting noise into the top-ranked components 4

2 4 6 8 10 12 10 −2.5

10 −2

SNR [dB] MSE 

EEP Baseline (λ = 0 .5)        

> Raw input -n= 5
> Raw input -n= 15
> LSTM–based input -n= 5
> LSTM–based input -n= 15

2 4 6 8 10 12 

SNR [dB] 

UEP (n = 15 )                

> L= 3 ,λ= 0 .7
> L= 5 ,λ= 0 .7
> L= 3 ,λ= 0 .9
> L= 5 ,λ= 0 .9
> Fixed UEP -L= 5 ,λ= 0 .9

Fig. 2: Prediction MSE, E

h

( ˆT − xt+1 )2

i

, vs. SNR under Rayleigh fading – Left: EEP baseline; Right: UEP ( n = 15 ). 

2 4 6 8 10 12 10 −3

10 −2

SNR [dB] AE Rec. MSE 

EEP Baseline ( λ = 0 .5)        

> Raw input -n= 5
> Raw input -n= 15
> LSTM–based input -n= 5
> LSTM–based input -n= 15

2 4 6 8 10 12 

SNR [dB] 

UEP (n = 15)                 

> L= 3 ,λ= 0 .7
> L= 5 ,λ= 0 .7
> L= 3 ,λ= 0 .9
> L= 5 ,λ= 0 .9
> Fixed UEP -L= 5 ,λ= 0 .9

Fig. 3: AE reconstruction MSE, ∥Z − ˆZ∥22, vs. SNR under Rayleigh fading – Left: EEP baseline; Right: UEP ( n = 15 ). (σ = 0 .1) increases MSE to 0.00890, compared with 0.00627 for the bottom-ranked components, confirming that the MI-gradient criterion correctly identifies task-critical latent direc-tions. Sensitivity analysis over L ∈ { 1, . . . , 10 } at SNR ∈{5, 10 , 12 } dB shows a broad optimum around L = 5 , with near-optimal performance for L ∈ { 4, 6} across all tested SNRs, indicating that per-SNR retuning is unnecessary. 

Representation reconstruction performance: Fig. 3 shows AE reconstruction MSE. Under EEP (left panel), semantic preprocessing improves reconstruction across all SNRs and maintains competitive performance even under reduced di-mensionality. With UEP (right panel), reconstruction accu-racy improves across the full SNR range. Unlike task-level performance, reconstruction error continues to benefit from stronger prioritization (λ = 0.9), revealing a divergence between semantic fidelity and end-task accuracy. That fixed UEP achieves similar reconstruction MSE to MI-guided UEP is consistent with the proposed approach: both methods protect the same number L of components with the same λ, so the aggregate reconstruction loss is similar regardless of which 

components are protected. The reconstruction metric cannot distinguish this; task MSE can. The superiority of MI-guided UEP is therefore visible only in task-level performance (Fig. 2, right), which is the metric that matters for semantic inference. The divergence between reconstruction MSE and task MSE reflects a fundamental property of task-oriented systems: mini-mizing reconstruction error does not guarantee preservation of the task-relevant information required for predicting T [18]. Channel perturbations can disrupt task-critical latent compo-nents while leaving the average reconstruction error largely unchanged, resulting in disproportionate degradation of task accuracy. The proposed MI-based prioritization directly ad-dresses this issue by protecting the components that contribute most to I(Z; T ). Together, the results show that semantic preprocessing improves robustness, while UEP provides tar-geted protection, highlighting their complementary roles and the importance of tuning UEP to the end task. 

20 40 60 80 100 0123

Epoch    

> ˆI(Z; T )
> Batch = 32 Batch = 64 Batch = 128 Batch = 64 (InfoNCE) Batch = 512

Fig. 4: Estimated MI ˆI(Z; T ) during training for different batch sizes and estimator types - Mean (line) ± std (shaded). 

C. Analysis of the Proposed UEP Mechanism 

DV estimator stability: Fig. 4 shows ˆI(Z; T ) during training for batch sizes {32 , 64 , 128 , 512 } (mean ± std). The estimate converges stably across all configurations, with batch 64 closely tracking the smoother trajectories of larger 5

2 4 6 8 10 12 10 −2

10 −1.5

SNR [dB] Prediction MSE  Raw input LSTM-based input UEP ( L = 5 , λ = 0 .7)UEP ( L = 5 , λ = 0 .9)

Fig. 5: Prediction MSE, E

h

( ˆT − xt+1 )2

i

, vs. SNR under Rayleigh fading - Amazon dataset ( n = 15 ). batches. The mean gradient standard deviation drops from 

0.119 at batch 32 to 0.051 at batch 64, and the Top-5 ranking consistency exceeds 88% for all batch sizes, confirming that the induced ranking is not driven by stochastic fluctuations of the DV estimator. To verify robustness to estimator choice, we repeated training with an InfoNCE-based estimator [19] (Fig. 4). Both estimators converge stably, produce the same dominant Top-5 ranking, and achieve nearly identical task MSE, confirming that the ranking is not estimator-specific. 

Latent dimension ablation: Table I (top part) reports task MSE and FLOPs for K ∈ { 10 , 20 , 40 }. Increasing K yields only marginal accuracy improvements at significantly higher complexity, and the relative gains of the proposed task-aware UEP remain consistent across all values of K, confirming that the observed gains originate from the task-aware UEP mechanism rather than low-dimensional bottleneck effects. 

PHY-layer differentiation: Table I (bottom part) reports the mean transmitted symbol energy per component group after training with MI-guided UEP. The top-ranked components receive +21% more energy than the EEP baseline, confirm-ing that the weighted loss translates into genuine PHY-layer reliability differentiation rather than merely a loss-level effect. This learned redistribution approximates task-aware resource allocation but does not constitute a proof of globally optimal power allocation, which lies beyond the scope of this work. TABLE I: (Top) Impact of K on task MSE and FLOPs (SN R = 10 dB). (Bottom) Symbol energy per component group under MI-guided UEP ( λ = 0 .7, n = 15 ). 

K Task MSE FLOPs 

10 0.00382 8.5 × 10 4

20 0.00327 14 .2 × 10 4

40 0.0029 37 .2 × 10 4

Component group E[s2 

> i

] vs. EEP 

Top-5 (protected) 1.24 +21% 

Bottom-5 0.86 −16% 

EEP baseline 1.02 —

Generalization: The proposed framework was further eval-uated on the Amazon stock dataset (6,516 daily observations) for next-day Open value prediction using the previous N = 20 

observations (Fig. 5). MI-guided UEP consistently outper-formed EEP across all tested SNRs. We further evaluated the proposed framework on the UCI HAR classification bench-mark [20]. MI-guided UEP also consistently outperformed both EEP and fixed UEP across all tested SNRs, demonstrating robustness across both regression and classification tasks. V. C ONCLUSION 

This letter proposes semantic SL for wireless edge–cloud inference with a learning-driven UEP mechanism at the split interface. Task-aware prioritization of latent representations in an AE-based PHY layer enables adaptive reliability allocation under fading channels. Numerical results show consistent im-provements over EEP and fixed-UEP baselines across SNRs. Results on a second dataset and across latent dimensions confirm the generality of the approach. Future work will con-sider larger edge–cloud deployments, extension to multimodal collaborative settings, dynamic selection of the protection budget L and integration with sparsification techniques. REFERENCES [1] W. Saad, M. Bennis and M. Chen, “A Vision of 6G Wireless Systems,” 

IEEE Netw., vol. 34, no. 3, pp. 134–142, 2020. [2] D. G¨ und¨ uz et al. , “Beyond Transmitting Bits: Context, Semantics, and Task-Oriented Communications,” IEEE J. Sel. Areas Commun. , vol. 41, no. 1, pp. 5–41, 2023. [3] W. Weaver, “Recent contributions to the mathematical theory of com-munication,” ETC: Rev. Gen. Semantics, pp. 261–281, 1953. [4] M. Kountouris and N. Pappas, “Semantics-empowered communication for networked intelligent systems,” IEEE Commun. Mag., vol. 59, no. 6, pp. 96–102, 2021. [5] O. Gupta and R. Raskar, “Distributed learning of deep neural network over multiple agents,” J. Netw. Comput. Appl., vol. 116, pp. 1–8, 2018. [6] T. O’Shea and J. Hoydis, “An introduction to deep learning for the physical layer,” IEEE Trans. Cogn. Commun. Netw., vol. 3, no. 4, pp. 563–575, 2017. [7] V. Ninkovic and D. Vukobratovic, “Structured superposition of autoen-coders for UEP codes at intermediate blocklengths,” IEEE Commun. Lett., early access, 2025. [8] Y. He, G. Yu, and Y. Cai, “Rate-adaptive coding mechanism for semantic communications with multi-modal data,” IEEE Trans. Commun., vol. 72, no. 3, pp. 1385–1400, 2024. [9] J. Xu et al. , “Deep Joint Source-Channel Coding for Semantic Commu-nications,” IEEE Commun. Mag., vol. 61, no. 11, pp. 42–48, 2023. [10] J. Huang et al. , “Robust Multimodal Information Bottleneck for Satellite-to-Ground Task-Oriented Communication,” in Proc. IEEE Symp. Comput. Commun., Bologna, Italy, 2025, pp. 1–7. [11] R. Fritschek et al. , “Deep Learning for Channel Coding via Neural Mutual Information Estimation,” in Proc. Int. Workshop Signal Process. Adv. Wireless Commun., Cannes, France, 2019, pp. 1–5. [12] K. Zhang et al. , “TokCom-UEP: Semantic Importance-Matched Unequal Error Protection for Resilient Image Transmission,” IEEE Wireless Commun. Lett., vol. 15, pp. 2809–2813, 2026. [13] Y. Liu et al. , ”Large AI Model and Loss Variation-Empowered Dual-Importance Prioritized Semantic Transmission,” IEEE J. Sel. Areas Commun., vol. 44, pp. 2274-2287, 2026. [14] W. Liu et al. , “Deep learning-based bit-wise flexible error protection modulation,” IEEE Commun. Lett., vol. 30, pp. 722–726, 2026. [15] N. Tishby, F. C. Pereira, and W. Bialek, “The information bottleneck method,” arXiv preprint arXiv:physics/0004057, 2000. [16] N. Fayaz et al. , “Semantic-aided image transmission system with un-equal error protection for next-generation communication networks,” in 

Proc. IEEE Wireless Commun. Netw. Conf., Dubai, UAE, 2024, pp. 1–6. [17] D. P. Kingma and J. L. Ba, “Adam: A method for stochastic optimiza-tion,” in Proc. Int. Conf. Learn. Representation, San Diego, CA, USA, 2015, pp. 1–41. [18] Y. Shao, Q. Cao, and D. G¨ und¨ uz, “A theory of semantic communication,” 

IEEE Trans. Mobile Comput., vol. 23, no. 12, pp. 12211–12228, 2024. [19] A. van den Oord, Y. Li, and O. Vinyals, “Representation learning with contrastive predictive coding,” arXiv preprint arXiv:1807.03748, 2019. [20] J. Reyes-Ortiz et al. “Human Activity Recognition Using Smartphones,” UCI ML Repository, dataset, 2013. doi: 10.24432/C54S4K.