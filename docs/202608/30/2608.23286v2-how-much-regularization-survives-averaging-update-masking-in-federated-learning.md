---
title: How Much Regularization Survives Averaging? Update Masking in Federated Learning
title_zh: 平均后还剩多少正则化？联邦学习中的更新掩码
authors: "Wenhao Yan, Fu Kuroda, Yucheng Jin, Zhenke Chen"
date: 2026-08-24
pdf: "https://arxiv.org/pdf/2608.23286v2"
tags: ["query:mlsys"]
score: 7.0
evidence: 联邦平均与非IID分布式训练中的正则化机制
tldr: 联邦学习面对非IID数据时常追求平坦极小值，但由更新噪声带来的隐式正则化在联邦平均下会被削弱。本文分析更新掩码的作用，证明各客户端独立掩码会被客户端数稀释，而共享掩码只能按梯度多样性部分恢复。CIFAR-10上该系数仅1.19/10，去掉小批量采样可升至8.96；但能保留正则的设置训练效果都很差。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.23286v2/fig-001.webp\", \"caption\": \"\", \"page\": 4, \"index\": 1, \"width\": 1020, \"height\": 870}]"
motivation: 探索更新噪声式正则化能否像中央训练那样在联邦学习中起效，并解释其失效原因。
method: 对客户端更新加入掩码并做联邦平均，理论分析独立掩码与共享掩码下的正则强度变化。
result: 证明独立掩码的正则被cohort size精确削弱，共享掩码仅按梯度多样性回升；CIFAR-10上仅1.19。
conclusion: 更新掩码虽能提供平坦化压力，但在联邦场景里可用配置几乎保不住正则与训练性能。
---

## 摘要
面向非 IID 数据的联邦学习旨在寻找平坦极小值，以便在不同客户端之间实现良好泛化；现有方法则借鉴了中心化训练中的 sharpness-aware minimization。达到平坦极小值还有另一种途径：通过对参数更新中加入噪声而自然获得正则化，但这种方式从未作为一种隐式正则化被迁移到联邦场景中。我们揭示其原因。掩码会对优化器沿尖锐方向移动进行“收费”。我们证明，当每个客户端各自采样一个掩码时，联邦平均会将这种“收费”恰好削弱为客户端组大小的相应倍数；而让所有客户端使用相同掩码，则会以该组梯度多样性倒数的因子将其恢复。在 CIFAR-10 上的实验设置中，该因子为 1.19，而理论上可达到 10。关闭小批量采样后，这一因子升至 8.96；而将数据异质性改变一千倍，仅使其保持在 1.17 到 1.50 之间。能够保留这种正则化的配置在训练上都差得远到无法实际使用。

## Abstract
Federated learning on non-IID data seeks flat minima to generalize across clients, and existing methods borrow sharpness-aware minimization from centralized training. There is a second way to reach flat minima, in which the regularization comes for free from noise added to the parameter updates, and it has never been carried over to the federated setting as an implicit regularizer. We show the reason. Masking charges the optimizer for moving in sharp directions. We prove that when each client draws its own mask, federated averaging weakens that charge by exactly the cohort size, and that giving every client the same mask brings it back by a factor equal to the inverse gradient diversity of the cohort. In our experiment setting on CIFAR-10, that factor is 1.19 out of a possible 10. Turning off minibatch sampling raises it to 8.96, while changing data heterogeneity a thousandfold leaves it between 1.17 and 1.50. The configurations keeping the regularization train far too poorly to use.