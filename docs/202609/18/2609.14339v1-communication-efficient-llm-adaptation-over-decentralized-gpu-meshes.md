---
title: Communication-Efficient LLM Adaptation over Decentralized GPU Meshes
title_zh: 去中心化 GPU 网格上的高通信效率大语言模型适配
authors: "Sameera Ramasinghe, Shamane Siriwardhana, Thalaiyasingam Ajanthan, Hadi Mohaghegh Dolatabadi, Chamin P Hewa Koneputugodage, Gil Avraham, Violetta Shevchenko, James Snewin, Karol Pajak, Harry Xi, Alexander Long"
date: 2026-09-13
pdf: "https://arxiv.org/pdf/2609.14339v1"
tags: ["query:mlsys"]
score: 10.0
evidence: 去中心化GPU网格上的异步流水线与数据并行训练
tldr: 在低端GPU与约200Mbps互联网连接组成的去中心化训练环境中，数据并行与流水线并行通信成为大模型适配的主要瓶颈。论文提出异步双回路系统：快速压缩训练回路通过激活掩码和数据并行压缩提升吞吐，慢速锚点回路在非关键路径执行少量未掩码前向—反向计算。进一步利用延迟锚点先验进行谱校正，去噪掩码梯度。实验显示，仅流水线压缩最高带来9倍吞吐提升，结合数据并行压缩可超过40倍，并在领域适配和持续预训练中保持接近无压缩训练的效果，说明高压缩通信可被可靠用于大模型后预训练适配。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.14339v1/fig-001.webp\", \"caption\": \"\", \"page\": 31, \"index\": 1, \"width\": 1213, \"height\": 738}]"
motivation: 去中心化低端GPU训练受互联网级带宽限制，数据并行与流水线并行通信共同吞噬适配吞吐。
method: 采用异步双回路架构，以激活掩码和压缩同步驱动快速训练，并用慢速未掩码锚点结合谱校正去噪延迟梯度。
result: 在约200Mbps连接下，流水线压缩最高提速9倍，叠加数据并行压缩后超过40倍，同时保持接近无压缩基线的性能。
conclusion: 锚点先验能显著缓解激进激活压缩的梯度噪声，使高压缩率通信适用于领域适配与持续预训练。
---

## 摘要
去中心化训练使得利用低端 GPU 和互联网级连接进行大模型训练成为可能，但沿数据并行和流水线并行两个维度的通信均成为主要瓶颈。我们研究了这一设置下的预训练后适配问题。我们提出了一种异步双回路系统：快速压缩训练回路通过对流水线并行（PP）传输采用激活掩码，并对数据并行（DP）同步进行压缩，以提升吞吐量；与此同时，慢速锚定回路在不处于关键路径的情况下，偶尔执行未掩码的前向—反向传播。随后，我们引入一种谱校正优化器，利用延迟获得的锚定先验对掩码梯度进行去噪，而无需阻塞快速数据流。尽管已有研究发现激进的激活压缩并不可靠，但我们表明，在这种锚定机制下，掩码方法能够以较高压缩率支持预训练后适配。仅采用流水线并行压缩即可使吞吐量提升至多 9 倍；在互联网级约 200 Mbps 的连接上，将其与数据并行压缩结合后，吞吐量提升可超过 40 倍，同时在领域适配和持续预训练任务中达到与稠密未压缩方法相当的性能。

## Abstract
Decentralized training enables large-model training over low-end GPUs and internet-grade connections, but communication along both data-parallel and pipeline-parallel axes becomes the primary bottleneck. We study post-pretraining adaptation in this setting. We propose an asynchronous two-circuit system: a fast compressed training circuit drives throughput using activation masking for pipeline-parallel (PP) transfer and compressed data-parallel (DP) synchronization, while a slow anchor circuit runs occasional unmasked forward--backward passes off the critical path. Then, we introduce a spectral correction optimizer that uses these delayed anchor priors to denoise masked gradients without blocking the fast stream. Although prior work has found aggressive activation compression unreliable, we show that masking supports post-pretraining adaptation at high compression rates when anchored this way. Pipeline-parallel compression alone yields up to a $9\times$ throughput gain, and combining it with data-parallel compression increases beyond $40\times$ over internet-grade $\sim 200$Mbps connections, while matching dense uncompressed performance across domain adaptation and continual pretraining.