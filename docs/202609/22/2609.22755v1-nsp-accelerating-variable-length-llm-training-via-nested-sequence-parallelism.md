---
title: "NSP: Accelerating Variable-Length LLM Training via Nested Sequence Parallelism"
title_zh: NSP：通过嵌套式序列并行加速变长 LLM 训练
authors: "Yi'ou Wang, Xiaoyang Li, Yijie Zheng, Shouda Liu, Yuxuan Wang"
date: 2026-09-19
pdf: "https://arxiv.org/pdf/2609.22755v1"
tags: ["query:mlsys"]
score: 10.0
evidence: 面向可变长度大语言模型训练的嵌套序列并行
tldr: 长尾语料中的序列长度差异，使固定序列并行度难以同时兼顾负载均衡与通信开销；现有动态方案按互斥GPU组运行，仍存在组间失衡和微批次代价。NSP在同一迭代中将不同规模的序列并行组嵌套到共享GPU上，并通过树状路由、分层流水与树级重计算高效执行。在Qwen3-MoE、最长384K上下文的多数据集实验中，NSP较静态SP最高提速1.48倍，较FlexSP最高提速1.16倍。该方法无需修改模型，为异构长上下文训练提供了更细粒度的通信与负载协同。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.22755v1/fig-001.webp\", \"caption\": \"\", \"page\": 1, \"index\": 1, \"width\": 5186, \"height\": 584}, {\"url\": \"assets/figures/arxiv/2609.22755v1/fig-002.webp\", \"caption\": \"\", \"page\": 12, \"index\": 2, \"width\": 380, \"height\": 934}]"
motivation: 长尾序列长度导致单一并行度难以平衡通信成本与GPU负载，动态SP的互斥分组又引入组间失衡。
method: NSP在共享GPU上嵌套不同规模的序列并行组，结合内存约束下的树状路由、跨层阶段流式执行和树级重计算。
result: 在Qwen3-MoE及最长384K上下文的多数据集上，NSP较静态SP最高提速1.48倍，较FlexSP最高提速1.16倍。
conclusion: NSP兼容常见序列并行后端且无需改动模型，能按序列长度精准投入通信资源并提升端到端训练吞吐。
---

## 摘要
在长尾语料上进行长上下文 LLM 训练时，核心问题在于通信开销与负载均衡之间的权衡。序列长度的异质性使得采用单一的序列并行（SP）度数难以适配工作负载：较小的并行度会导致少量长序列严重失衡，而较大的并行度则会使占据工作负载主体的大量短序列承担过高的通信开销。现有的动态 SP 系统在一个批次内混合使用不同的 SP 度数，但为了同时运行多个分组，它们会将 GPU 划分为互不相交的组，从而再次引入组间负载不均衡，并迫使系统采用代价高昂的微批次处理方案。我们提出 NSP，一种序列并行训练系统，通过在单次训练迭代中将不同规模的 SP 组嵌套在共享 GPU 上，解决这一权衡问题。该方法使长序列能够使用更大的 SP 组，同时让短序列使用较小的 SP 组，从而仅在必要之处产生通信开销，并实现逐 GPU 而非逐组的负载均衡。NSP 通过树结构路由规划器在内存约束下分配序列，并通过执行器利用由此形成的层次结构，实现跨层级阶段流水与树级重计算。NSP 支持常见的 SP 后端，且无需修改模型。我们在内部生产 GPU 集群上，针对多个长尾数据集上的 Qwen3-MoE 工作负载进行了评估，所使用的上下文长度最高达 384K 个 token。在这些设置下，NSP 始终能够提升端到端训练吞吐，相比 Static SP 的最高加速比达到 1.48 倍，相比 FlexSP 的最高加速比达到 1.16 倍。

## Abstract
Long-context LLM training on long-tailed corpora faces a central communication--balance tradeoff. Such sequence-length heterogeneity makes any single sequence-parallelism (SP) degree a poor fit for the workload: a small degree leaves the few long sequences badly imbalanced, while a large degree forces the many short sequences that dominate the workload to pay excessive communication. Existing dynamic-SP systems mix SP degrees within a batch, but to run several groups at once they partition the GPUs into disjoint groups, which reintroduces imbalance across groups and forces costly micro-batch workarounds.   We present NSP, a sequence-parallel training system that resolves this tradeoff by nesting differently sized SP groups on shared GPUs within a single training iteration. This lets long sequences use larger SP groups while keeping short sequences on smaller ones, so communication is incurred only where needed and load is balanced per GPU rather than per group. NSP realizes this idea with a tree-structured routing planner that assigns sequences under memory constraints and an executor that exploits the resulting hierarchy through inter-level phase streaming and tree-level recomputation. NSP supports common SP backends and requires no model changes. We evaluate NSP on Qwen3-MoE workloads with up to 384K-token contexts across multiple long-tail datasets on an internal production GPU cluster. Across these settings, NSP consistently improves end-to-end training throughput, outperforming Static SP by up to 1.48x and FlexSP by up to 1.16x.