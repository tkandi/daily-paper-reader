---
title: "NeuroFlex: Lossless Element-Level ANN-SNN Co-Execution for Efficient Sparse Inference"
title_zh: NeuroFlex：面向高效稀疏推理的无损元素级 ANN-SNN 协同执行
authors: "Varun Manjunath, Pranav Ramesh, Gopalakrishnan Srinivasan"
date: 2026-09-12
pdf: "https://arxiv.org/pdf/2609.14092v1"
tags: ["query:mlsys"]
score: 6.0
evidence: 面向高效深度神经网络执行的细粒度调度与加速器架构
tldr: "现有稀疏神经网络加速器通常只支持人工神经网络或脉冲神经网络，按层或分块切换又会造成另一类处理单元空闲。NeuroFlex将整数精确的两类网络等价性扩展到单个输出元素，为每个元素独立选择执行模式，并以代价引导调度完成处理单元负载打包。在多种视觉、语言和Transformer任务上，处理单元利用率提升至97%–99%，能量延迟积降低57%–67%，相较随机分配吞吐量提高16%–19%。该设计实现无精度损失的细粒度协同执行，兼顾能效、延迟与资源利用率。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.14092v1/fig-001.webp\", \"caption\": \"\", \"page\": 3, \"index\": 1, \"width\": 704, \"height\": 384}]"
motivation: 工作负载在层内变化显著，固定采用单一网络模式会牺牲能效或延迟，而按层、按分块混合执行又导致大量处理单元闲置。
method: NeuroFlex证明人工神经网络与脉冲神经网络可在单个输出元素级整数精确等价，并用离线代价引导调度器按能量—延迟边际收益分配和打包任务。
result: "相比人工神经网络基线，能量延迟积降低57%–67%；相比双稀疏脉冲网络基线，最高加速2.5倍，处理单元利用率达97%–99%。"
conclusion: 元素级模式选择消除了混合执行中的核心空转，在不损失准确率的前提下显著提升稀疏推理的吞吐与能效。
---

## 摘要
稀疏深度神经网络（DNN）加速器通常专门支持人工神经网络（ANN）或脉冲神经网络（SNN）的执行；当工作负载特征在同一层内发生变化时，这种设计会造成能耗或延迟方面的性能损失。按层或按瓦片粒度切换模式的混合加速器设计会导致处理单元（PE）利用率较低，因为当其中一种核心类型处于工作状态时，另一种核心类型始终处于空闲状态。NeuroFlex 首次实现了对每个输出元素独立分配 ANN 或 SNN 执行模式，且不会造成任何精度损失。我们将整数精确的 ANN-SNN 等价性从层级扩展到单个输出元素，从而实现无转换误差的模式切换。一个离线的成本引导型调度器根据每个元素在能耗与延迟权衡方面的边际代价进行评分，并将任务分配到各个 PE 上；与按层进行模式切换的混合设计相比，该方法可将 PE 利用率从 40%–45% 提升至 97%–99%。相较于性能强劲的纯 ANN 基线，NeuroFlex 可将能耗延迟积（EDP）降低 57%–67%；相较于双稀疏纯 SNN 基线，其加速比最高可达 2.5 倍。在视觉、语言和 Transformer 工作负载上，我们的成本引导型调度器较随机元素分配可将吞吐量提升 16%–19%。

## Abstract
Sparse DNN accelerators specialize in ANN or SNN execution, leaving energy or latency on the table when workload characteristics vary within a layer. Hybrid accelerator designs that switch modes at layer or tile granularity suffer from low PE utilization since one core type idles whenever the other is active. NeuroFlex is the first accelerator to assign every output element independently to ANN or SNN execution mode with zero accuracy loss. We extend integer-exact ANN-SNN equivalence from layers to individual output elements, thereby enabling mode switching with no conversion error. An offline cost-guided scheduler scores each element by its marginal energy-delay trade-off and packs work across PEs, achieving 97-99% PE utilization compared to 40-45% for layer-wise hybrids. NeuroFlex reduces EDP by 57-67% over a strong ANN-only baseline and delivers up to 2.5x speedup over a dual-sparse SNN-only baseline. Our cost-guided scheduler improves throughput by 16-19% over random element assignment across vision, language, and transformer workloads.