Title: RAPTOR: Role-Aware Private Training for Mixture-of-Experts

URL Source: https://arxiv.org/pdf/2609.05770v1

Published Time: Wed, 09 Sep 2026 00:24:11 GMT

Number of Pages: 39

Markdown Content:
# RAPTOR: Role-Aware Private Training for Mixture-of-Experts 

Duc Dm ⋆ 1, Khai Le-Duc ⋆ 2,3,4, Nguyen Do ⋆ 5,Minh Son Hoang 1, Florent Draye 6, Thai Hoang 7, Hoang Phuong Dam 1, Jiarui Liu 8,Chris Ngo 4, Terry Jingchen Zhang 9,10 , Anh Le Duc Tran 11 , Nhat Do Minh 12 , Minh Ngoc Le 2,3

My T. Thai 5, Ran Xu 7, Silvio Savarese 7,13 , Mona Diab 8,Bernhard Schölkopf 6,14 , Zhijing Jin 6,10 , Huy L. Nguyen 15 , Daeyoung Kim 11 KAIST 2 University of Toronto 3 Vector Institute 4 Knovel Engineering Lab  

> 5

University of Florida 6 MPI for Intelligent Systems, Tübingen  

> 7

Salesforce AI Research 8 Carnegie Mellon University  

> 9

University of Oxford 10 Jinesis Lab, University of Toronto & Vector Institute  

> 11

Hanoi University of Science and Technology 12 Vietnam National University, Hanoi  

> 13

Stanford University 14 ELLIS Institute Tübingen 15 Northeastern University 

> ⋆

Co-first authors # ducdm200158@kaist.ac.kr # duckhai.le@mail.utoronto.ca 

# © leduckhai/ RAPTOR 

Abstract 

Differentially private (DP) fine-tuning methods treat sparse Mixture-of-Experts (MoE) models as a single dense block, ignoring that shared layers see all data while experts only see routed records. We identify and formally characterize three resulting failure modes: global clipping suppresses expert gradients, batch-level normal-ization dilutes sparse expert updates, and fixed privacy noise degrades signal-to-noise ratio on low-load experts. We introduce RAPTOR -a Role-Aware Private Training framework, which alternates shared and expert optimiza-tion and targets each failure directly, using expert-specific clipping and noise together with a public expected-owner denominator and a count-independent update schedule that avoids conditioning on private, realized expert counts. We prove the resulting mechanism satisfies 

(ε, δ )-DP: because each record is assigned to exactly one owner expert, per-expert mecha-nisms within a layer compose in parallel, so updating all E experts costs no more, in pri-vacy terms, than updating one, with shared and expert streams composing sequentially across training. We further derive a bias-variance decomposition of the public-denominator es-timator showing its bias grows predictably with routing imbalance, yielding a privacy-free rule for selecting which layer to protect from rout-ing entropy measured on a small public cor-pus. Experiments on Switch Transformer and OLMoE fine-tuning across GLUE tasks, and on the DeepSeek-VL2-Tiny, show consistent gains over standard DP baselines across sev-eral privacy levels ( ε), with the largest margins typically at the tightest budgets. 

1 Introduction 

Sparse Mixture-of-Experts (MoE) architectures de-couple model capacity from per-record computa-tion, making conditional sparsity a central strat-egy for scaling foundation models (Shazeer et al., 2017; Fedus et al., 2022; Jiang et al., 2024). Yet many of their highest-value adaptations - on medi-cal records, enterprise documents, personal inter-actions, and other proprietary corpora - require formal protection against memorization, for which differential privacy (DP) (Dwork and Roth, 2014), enforced via per-record gradient clipping and Gaus-sian noise (Abadi et al., 2016), is the prevailing standard. Combining the two is not a straightfor-ward application of existing tools: DP optimizers were designed for dense models, where every pa-rameter receives signal from every record, so one clipping bound, one noise scale, and one normal-ization denominator suffice. Sparse routing breaks this premise - shared components still see every record, while each expert sees only the records routed to it - and the only prior work on private MoE training (Tholoniat et al., 2024) sidesteps the asymmetry by treating the model as a single dense block. We show this mismatch is a struc-tural incompatibility, not an implementation detail (Fig. 1, left): monolithic DP training suppresses expert gradients dominated by denser blocks ( clip-ping interference , panel 1), dilutes sparse expert updates through full-batch normalization ( update-scale dilution , panel 2), and concentrates fixed pri-vacy noise on low-load experts ( load-dependent 

1arXiv:2609.05770v1 [cs.LG] 4 Sep 2026 Figure 1: Applying differential privacy to sparse MoE is not straightforward : treating the model as dense leads to three structural failures - clipping interference, update-scale dilution, and load-dependent signal degradation. Our role-aware method is designed to address these mismatches. 

SNR degradation , panel 3) - together undermining the expert specialization that makes MoEs useful (Sec. 4). To address these limitations, we propose 

RAPTOR (Role-Aware Private Training ), which aligns the DP mechanism with the shared/-expert role structure rather than ignoring it (Fig. 1, right). The shared stream updates dense compo-nents on all records via standard per-record clip-ping and noise. The expert stream updates each ex-pert only on its owner records, with expert-specific clipping Ce and noise σe (resolving clipping in-terference, panel 1), a public expected-owner de-nominator De = B/E that never touches realized counts (resolving dilution without leaking routing decisions, panel 2), a count-independent schedule that updates every expert at every step, and privacy-free entropy-based layer selection (resolving load-dependent SNR degradation, panel 3). Fig. 2 gives an overview. 

Contributions. 

1. Diagnostic analysis. We identify and for-mally characterize three structural failure modes of monolithic DP-MoE training - clip-ping interference, update-scale dilution, and load-dependent SNR degradation (Sec. 4). 2. Role-aware optimization. We propose Role-Aware Private Training, resolving all three failures via alternating shared/expert streams, expert-specific clipping and noise, and a count-independent update schedule (Sec. 5). 3. Privacy guarantee. We prove (ε, δ )-DP via parallel composition over routing-induced owner groups: updating all E experts within a layer costs no more than updating one (Sec. 6). 4. Utility analysis and layer selection. We bound the public-denominator estimator’s bias by routing imbalance, yielding the privacy-free rule l⋆ = arg max l H(q(l)) on a small public corpus, and show this bias is stable under bounded representation drift via a Voronoi abstraction (Sec. 7). 5. Empirical validation. Gains up to +3 .24 

points over monolithic and matched-scope DP baselines on Switch Transformer (four GLUE tasks, ε ∈ { 1, 4, 8}), with consistent improve-ments on OLMoE and on the vision-language DeepSeek-VL2-Tiny model (Sec. 8). 2Figure 2: Overview of RAPTOR at the selected sparse layer l⋆. (a) The frozen router performs the base MoE model’s standard token-level top-k routing. Our framework aggregates gate weights across all tokens of record xi and selects one deterministic owner expert al⋆ (xi). The forward pass is unchanged: all activated experts contribute, but only the owner receives the expert-parameter gradient; activated non-owner experts use stop-gradient. This assignment partitions Dpriv into disjoint owner groups Dl⋆,e = {(xi, y i) : al⋆ (xi) = e}. (b) Each cycle draws independent Poisson subsamples for the shared and expert streams. Shared updates use clipping Cs, noise multiplier σs, and denominator B. Expert records are partitioned by owner and updated using Ce, σe, and the public denominator De = B/E , with noise-only updates for empty subsets. Expert mechanisms compose in parallel within l⋆, while shared and expert releases compose sequentially, with εsh = ρε and εexp = (1 − ρ)ε.

2 Related Work 

Private fine-tuning of pretrained LMs - full-parameter (Li et al., 2021) or parameter-efficient via LoRA (Yu et al., 2021; Hu et al., 2022) - applies one clipping bound, noise scale, and denominator uniformly across all parameters, a reasonable sim-plification for dense models where every parameter sees every record. This breaks under MoE routing, where shared components see every record but each expert sees only its routed subset; to our knowledge, no DP fine-tuning method distinguishes these expo-sure patterns. Separately, non-private MoE training stabilizes routing via batch-level load-balancing losses, which do not translate to record-level DP: their gradients break per-record sensitivity, and they depend on realized expert counts, which are private and cannot enter normalization or control flow without leaking routing decisions. The only prior work on private MoE training, Tholoniat et al. (2024), sidesteps this by treating MoE as a single dense block - feasible, but leaving shared, routing, and expert parameters structurally indistinguish-able, which suppresses expert gradients, dilutes sparse updates, and concentrates noise on low-load experts. We address this by freezing the router, replacing realized counts with a public expected-owner denominator, and analyzing expert updates via parallel composition over disjoint owner groups - closing the gap left by dense DP methods (Li et al., 2021; Yu et al., 2021; Hu et al., 2022), which ignore routing entirely, and by Tholoniat et al., which re-tains a monolithic optimizer despite it. Additional related work is in Appendix A. 

3 Preliminaries 

3.1 Setup and Notation 

Let Dpriv = {(xi, y i)}ni=1 be the private dataset. A sparse MoE model has E experts per sparse layer ([E] = {1, . . . , E }) over layers L, with parame-ters θ = ( θsh , θ rt , {θl,e }). Each expert carries a role r(e) ∈ { ROUTED , SHARED }: shared experts (e.g., DeepSeek-MoE’s always-active experts (Dai et al., 2024)) see every token and fold into θsh ;routed experts see only assigned tokens. Unless stated otherwise, r(e) = ROUTED for all e. At step t, Bexp  

> t

⊆ Dpriv is Poisson-subsampled, so its realized size is random and private; we write B =

E[|Bexp  

> t

|] for the fixed, public target size and nor-malize by B - never the realized count - the princi-ple underlying De = B/E (Sec. 5). For gradient g

and radius C> 0, clip( g, C ) = g·min {1, C/ ∥g∥2};subscripts s, e distinguish shared-/expert-stream quantities, e.g. (Cs, σ s, η s) vs. (Ce, σ e, η e).

3.2 Differential Privacy Definition 1. (Dwork and Roth, 2014) A is (ε, δ )-DP if for all neighboring D, D′ and output sets S,

Pr[ A(D) ∈ S] ≤ eε Pr[ A(D′) ∈ S] + δ.

DP-SGD (Abadi et al., 2016) privatizes each step’s gradient via per-record clipping and Gaus-3sian noise: for Bt of expected size B,

¯gt = 1

B

X

> i∈Bt

clip( gi,t , C ) + N(0 , σ 2C2I)

!

.

(1) We privatize every release this way, but optimize with Adam - ¯gt feeds Adam’s moment estimates in place of the raw gradient, giving DP-Adam . This is post-processing of the already-released sequence and adds no privacy cost beyond the T -fold com-position already charged (Sec. 6.2). 

3.3 Sparse MoE Models and Load Balancing 

Sparse MoE Transformers (Shazeer et al., 2017; Fedus et al., 2022) replace dense FFNs with E

experts and a router assigning each token to k of them; shared experts fold into the shared stream, routed experts into the expert stream, and sparse activation causes load imbalance. Our method as-signs each record deterministically to one routed expert per layer, its owner expert - the expert re-ceiving the most of the record’s tokens across the k

assignments, reducing to the standard case at k=1 

- the structure our parallel-composition analysis re-lies on (Sec. 6). Auxiliary load-balancing losses 

Laux depend on batch-level routing statistics, giv-ing per-record gradients that reflect other records’ decisions and break the per-record sensitivity DP-SGD requires; we therefore freeze the router and omit Laux .

4 Diagnosing Monolithic Private Optimization Failures 

Monolithic private optimization applies one clip-ping bound, one noise scale, and one normaliza-tion denominator to all MoE parameters - as if the model were dense. This ignores that shared layers, routers, and task heads receive signal from every record, while each expert receives signal only from its routed records. Under DP, this asymmetry causes three structural failures, each a property of the mechanism definition rather than any hyperpa-rameter choice. Appendix G formalizes this claim algebraically and reports empirical role-mismatch and per-expert SNR diagnostics (Tab. 8) that cor-roborate it directionally. 

4.1 Clipping Interference 

The per-record gradient gfull  

> i,t

=[gsh , g rt , g head , g exp ]i,t is clipped by a single factor αfull  

> i,t

= min(1 , C/ ∥gfull  

> i,t

∥2), dominated by the three dense blocks, so it suppresses the expert component even when ∥gexp  

> i,t

∥2 alone is within C.Relaxing C does not resolve the coupling: noise scales with C, so any value permissive enough for experts inflates dense-block noise. The natural resolution is a separate clipping bound for the expert stream, as in our method. 

4.2 Update-Scale Dilution 

Let mt,l,e = |Bt,l,e | be the number of records routed to expert e in batch Bexp  

> t

. The monolithic update 

bgmono  

> t,l,e

= 1

B

 X

> i∈Bt,l,e

egexp  

> i,t,l,e

+ ξt,l,e 

 (2) normalizes by B despite only mt,l,e records con-tributing signal, rescaling expert gradients toward zero by mt,l,e /B . Normalizing by the realized count mt,l,e is not DP-free: it is a private quantity whose use in normalization or control flow leaks routing information. We instead use the public expected-owner denominator De = B/E , which is data-independent and unbiased under uniform routing. 

4.3 Load-Dependent Signal Degradation 

Privacy noise per expert is fixed regardless of load; useful signal scales with mt,l,e . Uneven routing therefore concentrates noise on low-load experts. We track this with the routing entropy: 

Ht,l = − 1log E

> E

X

> e=1

qt,l,e log qt,l,e , (3) where qt,l,e = mt,l,e /B , τ = 10 −8 denotes a small constant added for numerical stability in ratio de-nominators. Sec. 7 connects Ht,l and the entropy deficit log E − Ht,l to the imbalance bias of the public-denominator expert estimator, motivating routing entropy as a candidate, privacy-free layer-selection diagnostic, whose relationship to Adam-based training utility Sec. 7 examines directly (Re-mark 2); 

5 Role-Aware Private Training 

5.1 Public Layer Selection 

We protect a single sparse layer l⋆ per stage. This choice keeps simple the parallel-composition ar-gument we prove later (Lemma 1, Sec. 6): updat-ing all E experts within a protected layer costs no 4more, in privacy terms, than updating one, provided the owner map - the deterministic assignment of each private record to a single owner expert at the protected layer, formalized in Sec. 5.2 (Eq. 5) -is fixed before training and does not change dur-ing the stage. This condition holds trivially when only one layer is trained. Multi-layer extension would require a staged scheme - train and freeze one layer’s owner map before the next, composing privacy costs sequentially - left to future work; the main paper scopes to L = {l⋆}. Before private training, we select one sparse layer using external public unlabeled data Cpub (disjoint from Dpriv ,format-matched to the task, e.g. unlabeled IMDb reviews for sentiment, SQuAD contexts for entail-ment). For each candidate layer l, we apply the frozen router to Cpub , compute the empirical owner distribution q(l), and set 

l⋆ = arg max  

> l

H



q(l)

. (4) Layer selection (Eq. 4) requires a disjoint, format-matched public corpus; without one, we default to the last sparse layer. Tab. 1 shows first-/last-sparse trailing entropy-selection on less balanced tasks -but in one setting lacking a public corpus it still improved over the monolithic DP baseline, suggest-ing the core mechanism does not strictly require entropy-based selection. Since Cpub is public and the router is frozen, this consumes no privacy bud-get and does not touch Dpriv . Sec. 7 shows entropy provably shrinks the public-denominator estima-tor’s bias βe (Theorem 2); Remark 2 clarifies this does not extend to a guarantee on per-expert signal-to-noise ratio, whose benefit for Adam-based train-ing we support empirically (Sec. 8) rather than by an analogous bound. 

5.2 Record-Level Expert Ownership 

At layer l, the frozen router gives each token 

t ∈ Tok( xi) a top-k expert set with gate weights 

gl,e (xi, t ) ≥ 0 (gl,e = 0 off the top-k, P 

> e

gl,e = 1 ). We lift this to a deterministic record-level owner map by gate-weighted plurality, 

al(xi) = arg max 

> e∈[E]

X

> t∈Tok( xi)

gl,e (xi, t ), (5) ties broken by lowest index; replacing gl,e by 

⊮[e ∈ top-k] recovers the majority-vote rule at 

k = 1. This induces a deterministic partition of Dpriv into owner groups Dl,e , one per expert 

e ∈ [E], with S 

> e

Dl,e = Dpriv for every k ≥ 1.The forward pass uses standard top-k routing, but the expert stream deposits gradient only into the owner expert’s parameters via a stop-gradient sg( ·)

on all non-owner experts: 

ai := al(xi),gexp  

> i,l

= ∇θl,a i ℓexp 



xi; θl,a i , θ sg 

> l, ¬ai



, (6) so each record contributes to exactly one owner expert; only the attribution of private expert updates is restricted. Under add/remove adjacency, one record changes only group Dl,a l(x0), by a single clipped contribution of norm ≤ Ce. Hence ∆l,e =

Ce/D e = EC e/B , independent of |Dl,e | and k,and conditions (i)-(iii) of Lemma 1 (Sec. 6.1) hold verbatim for any k. The owner map and realized counts |Dl,e | are never released; al⋆ is computed once from the frozen router before training and fixed thereafter. 

Remark 1 (Why masking, not just joint clipping) .

Joint-clipping a record’s concatenated per-expert gradients to one radius C would also give sensi-tivity C without exclusivity, avoiding Lemma 1’s condition (ii) cost. We reject this for two reasons independent of composition cost: it reintroduces clipping interference (Sec. 4.1) among experts, and it makes expert e’s clip factor depend on other experts’ gradient norms, breaking the per-group independence Theorem 1 and the entropy criterion rely on. 

5.3 Shared and Expert Stream Updates Shared stream. Every record contributes to the shared loss, so the shared stream computes a stan-dard privatized estimate over its own subsample: 

¯gsh  

> t

= 1

B

 X

> i∈Bsh
> t

clip( gsh 

> i,t

, C s) + N(0 , σ 2 

> s

C2 

> s

I)

 .

(7) 

Residual objective. Let zsh  

> i

∈ R|Y| be the output logits for record i under the current shared param-eters θsh (expert parameters {θl⋆,e } held fixed, no gradient this pass). Define the residual weight 

wi = 1 − softmax( zsh  

> i

)yi ∈ [0 , 1] , (8) the shared stream’s predicted error probability on the true class: wi ≈ 0 when confidently correct, 

wi ≈ 1 when not. The expert loss ℓexp = wi ℓcls 

concentrates expert capacity on records the shared stream has not yet classified confidently. Since wi

enters before clipping to Ce, sensitivity is unaf-fected. 5Privatized expert update. For owner subset 

Bt,l,e = {i ∈ Bexp  

> t

: al(xi) = e} and public denominator De = B/E , the expert stream com-putes its own privatized estimate over the owner subset: 

¯gexp  

> t,l,e

= 1

De

X

> i∈Bt,l,e

clip 



gexp 

> i,t,l,e

, C e



+ N(0 , σ 2 

> e

C2 

> e

I)

! (9) Three properties follow: (i) expert clipping decou-ples from the shared stream, resolving the suppres-sion of Sec. 4.1; (ii) De depends only on the public 

B, E , so realized counts are never revealed; (iii) the normalized sensitivity Ce/D e = EC e/B feeds di-rectly into the accountant of Sec. 6. 

Empty owner subsets. If Bt,l,e = ∅, the update reduces to noise alone, ¯gexp  

> t,l,e

= D−1 

> e

N(0 , σ 2 

> e

C2 

> e

I):every expert updates at every step with no count-dependent skipping or logging, which would leak realized counts and require additional accounting. 

5.4 Alternating Optimization and Layer Scope 

Each outer cycle - one iteration of the for t loop in Algorithm 1 (lines 4-6 and the nested expert loop that follows) - draws two independent Poisson sub-samples, Bsh  

> t

and Bexp  

> t

- rather than reusing one sample for both streams, which costs no additional data access since the expert stream already needs its own forward pass to compute wi - then per-forms one shared-stream update over Bsh  

> t

followed by one expert-stream update over Bexp  

> t

for all E

experts. Alternating lets shared representations and expert residual corrections co-adapt: each shared step updates the wi seen by the next expert step, and each expert step changes the residual for the next shared step. Privacy accounting for the two streams remains independent of the downstream optimizer, since the independent subsamples make their per-step releases independent random mecha-nisms (Sec. 6). 

6 Privacy Analysis 

We analyze our framework under (ε, δ )-DP with add/remove adjacency; only the final model is re-leased; owner assignments, expert counts, residual weights, and intermediate gradients are never ex-posed. 

6.1 Parallel Composition Across Experts Lemma 1 (Parallel composition across owner groups) . Fix layer l. Suppose (i) al is deterministic and stage-fixed; (ii) every record belongs to exactly one owner group Dl,e (the partition cell of records assigned to expert e by al, Sec. 5.2); and (iii) Ml,e 

depends only on Dl,e and public quantities. If each Ml,e is (εl,e , δ l,e )-DP on its owner group, the joint mechanism Ml = ( Ml, 1, . . . , M l,E ) is 

(max e εl,e , max e δl,e )-DP. 

All three conditions hold by construction: al

is stage-fixed (Sec. 5.2); the public denominator 

De = B/E and count-independent schedule make 

Ml,e depend only on Dl,e and public quantities; and per-group DP follows from subsampled-Gaussian accounting with sensitivity ∆l,e (Eq. (20)). Full proofs are in Appendix B.3. 

6.2 Composition Across Streams and Layers 

Interleaved shared/expert updates compose adap-tively under the PRV framework (Gopi et al., 2021): since Bsh  

> t

and Bexp  

> t

are independent Poisson sub-samples drawn afresh each cycle, per-step mech-anisms are conditionally independent given θt, so standard PRV convolution applies without a joint privacy-loss variable. Writing ω⊛T for T -fold self-convolution: the shared stream has per-step PRV 

ωsh set by (Cs, σ s, γ ); each layer’s expert mecha-nisms, by Lemma 1, compose in parallel (disjoint owner groups, no realized counts released), giving a single layer-level PRV ωl = ωl,e (homogeneous 

Ce, σ e, D e=B/E across experts) rather than an E-fold convolution. Sequential composition across shared updates and trained layers then gives 

ωtot = ω⊛Tsh  

> sh

⊛ ⊛

> l∈Ltrain

ω⊛Texp ,l  

> l

, (10) with Ltrain ⊆ L the privately-updated layers and 

ε(δ) = inf {ε ≥ 0 : Pr[ ωtot > ε ] ≤ δ}.

Budget split. A fraction ρ ∈ (0 , 1) of ε goes to the shared stream, 1−ρ to the experts; we solve for σs, σ e jointly via binary search over the PRV accountant, using ρ = 0 .9 at E = 8 (Appendix H); sensitivity to ρ and larger E is in Appendix E.3. 

7 Utility Analysis of the Expert Estimator 

7.1 Bias-Variance Decomposition and Layer Selection Setup. Fix one selected layer (suppress layer in-dex). The owner map assigns each record to ex-6Algorithm 1 RAPTOR 

Input: Dpriv (n records), Cpub , frozen router, candidate layers, B, E, p =B/n, C s, C e, σ s, σ e, ρ 

1: l⋆ ← arg max l H(q(l)) on Cpub ▷ no privacy cost 2: al⋆ (xi) ← arg max e∈[E]

P 

> t∈Tok( xi)

gl⋆,e (xi, t ), ∀xi ∈ Dpriv ▷ once; never released 3: for t = 1 , . . . , T do 

4: Bsh  

> t

← { i : ush  

> i

=1 }, ush 

> i
> iid

∼ Bern( p) ▷ shared subsample 5: ¯gsh  

> t

← 1

> B

  P  

> i∈Bsh
> t

clip( gsh 

> i,t

, C s) + N(0 , σ 2 

> s

C2 

> s

I); DP-Adam on θsh 

6: Bexp  

> t

← { i : uexp  

> i

=1 }, uexp 

> i
> iid

∼ Bern( p), {uexp  

> i

} ⊥ { ush  

> i

} ▷ independent subsample 7: for e ∈ [E] do 

8: Bt,e ← { i ∈ Bexp  

> t

: al⋆ (xi)= e}

9: ¯gexp  

> t,e

← 1

> De

  P  

> i∈Bt,e

clip( gexp 

> i,t,e

, C e) + N(0 , σ 2 

> e

C2 

> e

I); DP-Adam on θl⋆,e ▷ D e=B/E ; noise-only if empty 10: end for 

11: end for Output: θT

actly one owner expert via gate-weighted plural-ity over its tokens (Eq. (5)), so it partitions Dpriv 

into disjoint groups De for any k ≥ 1 - the for-ward pass may still route a record’s tokens to k

experts, but only the owner expert receives its gradient. Groups have sizes ne, empirical fre-quencies qe = ne/n , and target means μe =

n−1

> e

P 

> i∈De

hexp  

> i

, where hexp  

> i

= clip( gexp  

> i

, C e).Write u = (1 /E, . . . , 1/E ) for the uniform distri-bution, Σe for the within-group covariance; under Poisson subsampling at rate p = B/n , E|Bt,e | =

Bq e.

Theorem 1 (Bias-variance decomposition) . Under Poisson subsampling with rate p = B/n , letting d

denote the parameter dimension, E∥¯gexp  

> t,e

− μe∥22 =

β2 

> e

+ V samp  

> e

+ V DP  

> e

, where 

β2 

> e

= ( Eq e − 1) 2∥μe∥22, (11) 

V samp  

> e

= E2p n e(1 − p)

B2

 ∥μe∥22 + tr Σ e

, (12) 

V DP  

> e

= E2σ2 

> e

C2 

> e

dB2 . (13) The squared bias β2 

> e

= ( Eq e − 1) 2∥μe∥22 van-ishes exactly when qe = 1 /E - the public de-nominator De = B/E is unbiased under uniform routing, with bias growing quadratically under im-balance – and the E2 factor in V DP  

> e

reflects the normalized sensitivity EC e/B . Proofs are in Ap-pendix B.5. 

Corollary 1 (Balanced routing) . If qe = 1 /E , then 

βe = 0 and 

q

E ¯gexp  

> t,e

− μe 22 ≤ Ce

r EB + Eσ eCe

√dB ,

(14) 

separating non-private sampling error from the DP cost; scaling and comparison to the standard DP mean-estimation rate are in Appendix B.8. 

Remark 2. The bias βe is optimizer-agnostic - van-ishing under uniform routing (Theorem 1), bounded under imbalance (Theorem 2) - but does not by itself determine DP-Adam’s training utility. It is a rescaling of μe by the constant Eq e, fixed throughout training, to which DP-Adam’s normal-ization is largely invariant away from the ϵAdam 

floor; and β2 

> e

is symmetric around qe = 1/E ,so an expert at qe = 2 /E has the same β2 

> e

as one at qe = 0 , though these are opposite out-comes for training. The closer proxy for Adam-based utility is the per-expert signal-to-noise ratio 

SNR e ∝ q2 

> e

∥μe∥22/(σ2 

> e

C2 

> e

d) – monotonic in qe and consistent with Tab. 8 - but no bound 1/SNR e ≤

f (H(q)) holds for any finite f , since a starved expert contributes only O(1 /E ) to χ2(q∥u) ∈

[0 , E −1] and can coexist with near-maximal en-tropy while SNR e → 0 (Appendix B.11). We there-fore present l⋆ = arg max l H(q(l)) as a privacy-free, empirically validated heuristic - supported by its falsifiability and gains in Tab. 1 and Appendix F - not a rule Theorem 2 certifies optimal for DP-Adam dynamics. 

Routing imbalance and chi-squared divergence. 

Aggregating β2 

> e

across experts via P

> e

(Eq e−1) 2 =

Eχ 2(q∥u) (Appendix B.6) gives: 

Theorem 2 (Aggregate imbalance bias) . Let ¯G2 =max e ∥μe∥22. Then 

1

E

> E

X

> e=1

β2 

> e

≤ ¯G2χ2(q∥u), (15) 

> E

X

> e=1

qeβ2 

> e

≤ ¯G2Eχ 2(q∥u). (16) 

Entropy as a layer-selection proxy. Chi-squared divergence is harder to estimate stably on small 7public samples than entropy; via Pinsker’s inequal-ity and χ2(q∥u) = E∥q − u∥22 ≤ E∥q − u∥21,

χ2(q ∥ u) ≤ 2E  log E − H(q), (17) combining with Eq. (16) gives 

X

> e

qe β2 

> e

≤ 2 ¯G2E2  log E − H(q). (18) The right-hand side of Eq. (18) is the only lever we can pull without touching private data: ¯G2 and E

are fixed by the model, so the bound on the aggre-gate bias P 

> e

qeβ2 

> e

shrinks exactly as log E − H(q)

shrinks. Minimizing the guaranteed bias there-fore reduces to maximizing H(q) over candidate layers, which is computable from a public corpus alone: this is the entire justification for the crite-rion l⋆ = arg max l H(q(l)). A layer with higher 

H(q) thus has a smaller imbalance-bias guaran-tee. This gives a privacy-free, computable criterion: 

l⋆ = arg max l H(q(l)) needs only Cpub and the frozen router, with its connection to DP-Adam util-ity scoped in Remark 2. 

7.2 Stability of Expert Ownership 

The owner map al⋆ is frozen before training and never recomputed as θsh updates; this does not af-fect privacy (Lemma 1 needs only stage-fixedness), but bears on whether it stays a faithful specializa-tion proxy as the representation drifts. Under a Voronoi abstraction, we bound this drift (proofs in Appendix B.9–B.10): 

Theorem 3 (Ownership stability, informal) . If the representation moves by at most r, only records with margin ≤ 2∆ Cr can change owner ( ∆C: max prototype distance); under a mild boundary-mass condition (rate κ), both reassignment probability and routing-mass drift scale linearly in r - only near-boundary records can flip, and how many there are grows gracefully with drift, not catas-trophically. 

Theorem 4 (Estimation error under drift, informal) .

Assuming an Lg-Lipschitz gradient field, 

E∥¯ge − νe∥22 ≤ V samp  

> e

+ V DP 

> e

+ 2( Eq e − 1) 2∥μe∥22 + 2 L2

> g

r2 

> e

,r2 

> e

the mean squared distance from a record to its owning prototype. The first three terms match Theorem 1; the last is the extra cost of ownership being a coarse proxy rather than exact, vanishing as owner groups tighten around their prototype. 

8 Experiments 

8.1 Setup Model and tasks. We fine-tune 

google/switch-base-8 (Fedus et al., 2022) (E = 8, six encoder MoE layers at blocks 

{1, 3, 5, 7, 9, 11 }) on four GLUE tasks (Wang et al., 2018) (SST-2, MNLI, QNLI, QQP), report-ing dev-set accuracy. We additionally evaluate 

OLMoE-1B-7B (Muennighoff et al., 2025) ( E = 64 ,top-8 routing) to exercise the primary-expert own-ership rule for k > 1 (Sec. 5.2). We additionally test DeepSeek-VL2-Tiny (Wu et al., 2024), a vision-language MoE with both shared and routed experts (Sec. 3.3), fine-tuned on ScienceQA (Lu et al., 2022), to test generalization beyond text-only models (Appendix D). Hyperparameter grids are in Appendix C; runtime/memory in Appendix I; ablations in Appendix E - component ablation (clipping, denominator, alternating schedule, residual objective), matched-scope diagnostics (shared-/expert-only, global DP at Ours’s scope), and budget-split ρ sensitivity. 

Privacy. All private methods use ε ∈ { 1, 4, 8}

under (ε, δ )-DP with δ = 1 /|Dpriv |1.1, matched across methods. 

Layer selection. l⋆ = arg max l H(q(l)), com-puted from the frozen router and external public data only (no privacy cost), yields block 1 (SST-2), block 7 (MNLI), block 11 (QNLI), block 9 (QQP) - see Appendix F for the full sweep. We also re-port first- and last-sparse-layer variants to make the entropy prediction falsifiable. 

Baselines. Non-private upper bounds: LoRA, full fine-tuning without noise. Monolithic DP: 

DP-Adam LoRA (Yu et al., 2021) and DP-Adam (Tholoniat et al., 2024), both treating all parameters as one dense block. 

8.2 Results on Switch Transformer 

Tab. 1 reports results at ε ∈ { 1, 4, 8}.

RAPTOR vs. monolithic DP. Ours attains the highest accuracy on all twelve task-budget cells. Gains over Monolithic DP-Adam LoRA range 

+0 .23 to +3 .24 pts (SST-2/MNLI/QNLI/QQP) at 

ε = 8 and +1 .51 to +2 .92 at ε = 1 , growing at tighter budgets on SST-2/QNLI but not MN-LI/QQP - the high-noise advantage of Corollary 1 is baseline- and task-dependent, not uniform. 8ε Method SST-2 MNLI QNLI QQP                                                       

> ∞Non-priv. LoRA 94.69 86.55 91.20 90.96 Non-priv. FFN 94.50 86.36 91.09 91.28
> 1
> Monolith. DP-Adam LoRA 90.94 76.91 80.38 81.19 Monolith. DP-Adam 87.04 72.72 81.44 81.84 Ours, first sparse 92.90 77.40 82.60 83.20 Ours, last sparse 92.66 77.87 83.30 83.80
> Ours, entropy-sel. 92.90 78.42 83.30 84.08
> 4
> Monolith. DP-Adam LoRA 92.66 78.33 81.95 82.26 Monolith. DP-Adam 90.48 75.90 82.41 83.32 Ours, first sparse 94.05 78.13 83.60 84.50 Ours, last sparse 93.46 78.50 84.70 84.78
> Ours, entropy-sel. 94.05 79.03 84.70 84.78
> 8
> Monolith. DP-Adam LoRA 93.92 79.36 82.48 82.68 Monolith. DP-Adam 90.37 76.70 82.61 84.09 Ours, first sparse 94.15 79.43 83.60 85.39 Ours, last sparse 93.50 80.58 85.26 85.73
> Ours, entropy-sel. 94.15 81.32 85.26 85.92

Table 1: S WITCH -BASE -8 under (ε, δ )-DP with δ =1/|Dpriv |1.1. Entropy-selected layers fixed from pub-lic routing statistics before training: block 1 (SST-2), block 7 (MNLI), block 11 (QNLI), block 9 (QQP). Bold :best per task–budget; underline: second-best distinct method. 

Entropy criterion. Selection is non-trivially fal-sifiable on MNLI (block 7) and QQP (block 9), neither first nor last; entropy-selected matches or exceeds both variants in every cell, with the largest gains on MNLI and QQP, where chosen layers dif-fer most from the runner-up. 

8.3 Results on OLMoE 

Tab. 2 reports OLMoE-1B-7B results at ε ∈ { 1, 4, 8},with records assigned via majority-vote ownership for k > 1 (Sec. 5.2). Ours (entropy-selected) at-tains the highest accuracy in all twelve task–budget cells. At ε = 8 , gains over Monolithic DP-Adam LoRA are +0 .69 (SST-2), +2 .18 (MNLI), +1 .74 

(QNLI), +0 .32 (QQP); at ε = 1 , +2 .63 , +0 .84 ,

+4 .50 , +0 .70 respectively. SST-2, QNLI, and QQP gains grow at tighter budgets, consistent with the high-noise regime prediction of Corollary 1 and mirroring Sec. 8.2; the MNLI gain instead shrinks from +2 .18 to +0 .84 .

8.4 Empirical Privacy Auditing via Membership Inference 

We audit QNLI (S WITCH -BASE -8) with a loss-threshold attack and shadow-model LiRA (Car-lini et al., 2022) ( 64 shadow models; neval =20 ,000 : 10 k members, 10 k non-members), re-porting TPR@0 .1%FPR alongside AUC since average-case AUC can mask worst-case success                            

> εMethod SST-2 MNLI QNLI QQP
> ∞Non-priv. LoRA 96.67 90.19 94.65 92.03
> Non-priv. FFN 95.18 89.72 93.13 91.07
> 1Monolith. DP-Adam LoRA 92.78 85.31 84.24 85.89
> Ours, entropy-sel. 95.41 86.15 88.74 86.59
> 4Monolith. DP-Adam LoRA 95.07 85.74 88.56 86.43
> Ours, entropy-sel. 95.30 87.44 90.44 86.89
> 8Monolith. DP-Adam LoRA 95.07 86.08 89.00 86.62
> Ours, entropy-sel. 95.76 88.26 90.74 86.94

Table 2: OLMoE-1B-7B ( E = 64 , top-8 routing) under 

(ε, δ )-DP, δ = 1 /|Dpriv |1.1. Entropy-selected layers fixed from public routing statistics: block 1 (SST-2, QNLI, QQP), block 15 (MNLI). 

(Tab. 3). All DP methods sharply reduce LiRA TPR vs. non-private training, with attack success growing monotonically in ε. Our framework at-tains the highest TPR/AUC among DP methods on both LiRA metrics and loss-attack AUC at every 

ε (tying Monolithic DP-LoRA on loss-attack TPR at ε=1 ), with the gap widening as ε grows - not a DP violation, but a signature of the same higher per-expert SNR (Theorem 1) driving its accuracy gains. 

9 Summary 

Monolithic DP mismatches sparse MoE’s shared/-expert structure. RAPTOR fixes this via al-ternating streams, expert-specific clipping/noise, public normalization, and entropy-based layer se-lection, under a parallel-composition DP guarantee. Gains hold across Switch, OLMoE, and DeepSeek-VL2-Tiny, typically largest at tight budgets. 

Limitations 

Frozen router. Fixing owner groups requires freez-ing the router, which precludes privately updating routing parameters within a stage. Adaptive router training could improve utility but would require re-accounting for ownership changes. 

Public denominator. De = B/E is privacy-safe but not oracle-optimal: under heavy imbalance it may over- or under-scale expert updates. Adaptive private normalization is left to future work. 

Residual weighting and outlier amplification. 

The residual weight wi = 1 − softmax( zsh  

> i

)yi

(Eq. (8)) up-weights low-confidence records – a superset of genuinely hard subpopulations and atyp-ical or mislabeled ones, which tend to retain high 

wi once the shared stream reliably disagrees with 9Method ε Acc. Loss attack LiRA AUC TPR@0.1%FPR AUC TPR@0.1%FPR Non-priv. LoRA ∞ 91.20 0.568 0.45% 0.620 3.00% Monolithic DP-LoRA 1 80.38 0.501 0.10% 0.504 0.13% Matched-scope Global DP 1 82.90 0.502 0.11% 0.505 0.14%                                           

> RAPTOR 183.30 0.502 0.10% 0.506 0.15%
> Monolithic DP-LoRA 481.95 0.505 0.12% 0.513 0.23% Matched-scope Global DP 484.68 0.505 0.13% 0.514 0.26%
> RAPTOR 484.70 0.506 0.13% 0.516 0.28%
> Monolithic DP-LoRA 882.48 0.510 0.16% 0.524 0.45% Matched-scope Global DP 884.90 0.510 0.16% 0.527 0.50%
> RAPTOR 885.26 0.512 0.18% 0.530 0.58%

Table 3: MIA audit on QNLI (S WITCH -BASE -8). 64 shadow models; neval = 20 ,000 (10 k/ 10 k members/non-members). Single run per cell. Bold : highest attack success (AUC/TPR) among DP methods at that ε - the least 

favorable privacy result, not the best method; underline: second-highest/tied. Acc. bolding follows the opposite convention (highest accuracy), as in Tab. 1. 

their label. Clipping is applied after the wi multi-plication (Lemma 2), so the sensitivity bound Ce

and the (ε, δ )-DP guarantee hold regardless of wi

- a signal-composition effect, not a sensitivity vi-olation. But a small owner group’s clipped sum is then more likely dominated by its highest-wi

member, which may fit label noise over genuine structure, compounds with the SNR degradation of Sec. 4 on low-load experts, and concentrates signal on records associated with elevated mem-orization risk even under a fixed nominal ε. We did not run label-noise or subgroup diagnostics to quantify this; a bounded variant ( wγi , γ < 1, or an EMA-discounted wi) costs no extra privacy budget and is a natural robustness knob for future work. 

Extensions. Future work includes private router adaptation, adaptive normalization, and extension to generative and larger-scale multimodal MoEs. 10 References 

Martín Abadi, Andy Chu, Ian J. Goodfellow, H. Brendan McMahan, Ilya Mironov, Kunal Talwar, and Li Zhang. 2016. Deep learning with differential privacy. In Proceedings of the 2016 ACM SIGSAC Conference on Computer and Communications Security, Vienna, Austria, October 24-28, 2016 , pages 308–318. ACM. Nicholas Carlini, Steve Chien, Milad Nasr, Shuang Song, Andreas Terzis, and Florian Tramer. 2022. Membership inference attacks from first principles. In 2022 IEEE symposium on security and privacy (SP) , pages 1897–1914. IEEE. Zewen Chi, Li Dong, Shaohan Huang, Damai Dai, Shuming Ma, Barun Patra, Saksham Singhal, Payal Bajaj, Xia Song, Xian-Ling Mao, Heyan Huang, and Furu Wei. 2022. On the representation collapse of sparse mixture of experts. Damai Dai, Chengqi Deng, Chenggang Zhao, RX Xu, Huazuo Gao, Deli Chen, Jiashi Li, Wangding Zeng, Xingkai Yu, Yu Wu, et al. 2024. Deepseekmoe: Towards ultimate expert specialization in mixture-of-experts language models. In Proceedings of the 62nd Annual Meeting of the Association for Computational Linguistics (Volume 1: Long Papers) , pages 1280–1297. DeepSeek-AI, Aixin Liu, Bei Feng, Bing Xue, Bingxuan Wang, Bochao Wu, Chengda Lu, Chenggang Zhao, Chengqi Deng, Chenyu Zhang, Chong Ruan, Damai Dai, Daya Guo, Dejian Yang, Deli Chen, Dongjie Ji, Erhang Li, Fangyun Lin, Fucong Dai, Fuli Luo, Guangbo Hao, Guanting Chen, Guowei Li, H. Zhang, Han Bao, Hanwei Xu, Haocheng Wang, Haowei Zhang, Honghui Ding, Huajian Xin, Huazuo Gao, Hui Li, Hui Qu, J. L. Cai, Jian Liang, Jianzhong Guo, Jiaqi Ni, Jiashi Li, Jiawei Wang, Jin Chen, Jingchang Chen, Jingyang Yuan, Junjie Qiu, Junlong Li, Junxiao Song, Kai Dong, Kai Hu, Kaige Gao, Kang Guan, Kexin Huang, Kuai Yu, Lean Wang, Lecong Zhang, Lei Xu, Leyi Xia, Liang Zhao, Litong Wang, Liyue Zhang, Meng Li, Miaojun Wang, Mingchuan Zhang, Minghua Zhang, Minghui Tang, Mingming Li, Ning Tian, Panpan Huang, Peiyi Wang, Peng Zhang, Qiancheng Wang, Qihao Zhu, Qinyu Chen, Qiushi Du, R. J. Chen, R. L. Jin, Ruiqi Ge, Ruisong Zhang, Ruizhe Pan, Runji Wang, Runxin Xu, Ruoyu Zhang, Ruyi Chen, S. S. Li, Shanghao Lu, Shangyan Zhou, Shanhuang Chen, Shaoqing Wu, Shengfeng Ye, Shengfeng Ye, Shirong Ma, Shiyu Wang, Shuang Zhou, Shuiping Yu, Shunfeng Zhou, Shuting Pan, T. Wang, Tao Yun, Tian Pei, Tianyu Sun, W. L. Xiao, Wangding Zeng, Wanjia Zhao, Wei An, Wen Liu, Wenfeng Liang, Wenjun Gao, Wenqin Yu, Wentao Zhang, X. Q. Li, Xiangyue Jin, Xianzu Wang, Xiao Bi, Xiaodong Liu, Xiaohan Wang, Xiaojin Shen, Xiaokang Chen, Xiaokang Zhang, Xiaosha Chen, Xiaotao Nie, Xiaowen Sun, Xiaoxiang Wang, Xin Cheng, Xin Liu, Xin Xie, Xingchao Liu, Xingkai Yu, Xinnan Song, Xinxia Shan, Xinyi Zhou, Xinyu Yang, Xinyuan Li, Xuecheng Su, Xuheng Lin, Y. K. Li, Y. Q. Wang, Y. X. Wei, Y. X. Zhu, Yang Zhang, Yanhong Xu, Yanhong Xu, Yanping Huang, Yao Li, Yao Zhao, Yaofeng Sun, Yaohui Li, Yaohui Wang, Yi Yu, Yi Zheng, Yichao Zhang, Yifan Shi, Yiliang Xiong, Ying He, Ying Tang, Yishi Piao, Yisong Wang, Yixuan Tan, Yiyang Ma, Yiyuan Liu, Yongqiang Guo, Yu Wu, Yuan Ou, Yuchen Zhu, Yuduan Wang, Yue Gong, Yuheng Zou, Yujia He, Yukun Zha, Yunfan Xiong, Yunxian Ma, Yuting Yan, Yuxiang Luo, Yuxiang You, Yuxuan Liu, Yuyang Zhou, Z. F. Wu, Z. Z. Ren, Zehui Ren, Zhangli Sha, Zhe Fu, Zhean Xu, Zhen Huang, Zhen Zhang, Zhenda Xie, Zhengyan Zhang, Zhewen Hao, Zhibin Gou, Zhicheng Ma, Zhigang Yan, Zhihong Shao, Zhipeng Xu, Zhiyu Wu, Zhongyu Zhang, Zhuoshu Li, Zihui Gu, Zijia Zhu, Zijun Liu, Zilin Li, Ziwei Xie, Ziyang Song, Ziyi Gao, and Zizheng Pan. 2025. Deepseek-v3 technical report. Nan Du, Yanping Huang, Andrew M. Dai, Simon Tong, Dmitry Lepikhin, Yuanzhong Xu, Maxim Krikun, Yanqi Zhou, Adams Wei Yu, Orhan Firat, Barret Zoph, Liam Fedus, Maarten Bosma, Zongwei Zhou, Tao Wang, Yu Emma Wang, Kellie Webster, Marie Pellat, Kevin Robinson, Kathleen Meier-Hellstern, Toju Duke, Lucas Dixon, Kun Zhang, Quoc V Le, Yonghui Wu, Zhifeng Chen, and Claire Cui. 2022. Glam: Efficient scaling of language models with mixture-of-experts. Cynthia Dwork, Krishnaram Kenthapadi, Frank McSherry, Ilya Mironov, and Moni Naor. 2006. Our data, ourselves: Privacy via distributed noise generation. In Annual international conference on the theory and applications of cryptographic techniques , pages 486–503. Springer. Cynthia Dwork and Aaron Roth. 2014. The algorithmic foundations of differential privacy. Foundations and trends® in theoretical computer science , 9(3-4):211–487. Cynthia Dwork, Guy N Rothblum, and Salil Vadhan. 2010. Boosting and differential privacy. In 2010 IEEE 51st annual symposium on foundations of computer science , pages 51–60. IEEE. William Fedus, Barret Zoph, and Noam Shazeer. 2022. Switch transformers: Scaling to trillion parameter models with simple and efficient sparsity. Journal of Machine Learning Research , 23(120):1–39. Sivakanth Gopi, Yin Tat Lee, and Lukas Wutschitz. 2021. Numerical composition of differential privacy. Advances in Neural Information Processing Systems , 34:11631–11642. 

11 Edward J Hu, Yelong Shen, Phillip Wallis, Zeyuan Allen-Zhu, Yuanzhi Li, Shean Wang, Liang Wang, Weizhu Chen, et al. 2022. Lora: Low-rank adaptation of large language models. Iclr , 1(2):3. Albert Q. Jiang, Alexandre Sablayrolles, Antoine Roux, Arthur Mensch, Blanche Savary, Chris Bamford, De-vendra Singh Chaplot, Diego de Las Casas, Emma Bou Hanna, Florian Bressand, Gianna Lengyel, Guillaume Bour, Guillaume Lample, Lélio Renard Lavaud, Lucile Saulnier, Marie-Anne Lachaux, Pierre Stock, Sandeep Subramanian, Sophia Yang, Szymon Antoniak, Teven Le Scao, Théophile Gervet, Thibaut Lavril, Thomas Wang, Timothée Lacroix, and William El Sayed. 2024. Mixtral of experts. CoRR , abs/2401.04088. Shiva Prasad Kasiviswanathan, Homin K Lee, Kobbi Nissim, Sofya Raskhodnikova, and Adam Smith. 2011. What can we learn privately? SIAM Journal on Computing , 40(3):793–826. Dmitry Lepikhin, HyoukJoong Lee, Yuanzhong Xu, Dehao Chen, Orhan Firat, Yanping Huang, Maxim Krikun, Noam Shazeer, and Zhifeng Chen. 2020. Gshard: Scaling giant models with conditional computation and automatic sharding. Mike Lewis, Shruti Bhosale, Tim Dettmers, Naman Goyal, and Luke Zettlemoyer. 2021. Base layers: Simplifying training of large, sparse models. Xuechen Li, Florian Tramer, Percy Liang, and Tatsunori Hashimoto. 2021. Large language models can be strong differentially private learners. arXiv preprint arXiv:2110.05679 .Pan Lu, Swaroop Mishra, Tanglin Xia, Liang Qiu, Kai-Wei Chang, Song-Chun Zhu, Oyvind Tafjord, Peter Clark, and Ashwin Kalyan. 2022. Learn to explain: Multimodal reasoning via thought chains for science question answering. Advances in neural information processing systems , 35:2507–2521. Andrew Maas, Raymond E Daly, Peter T Pham, Dan Huang, Andrew Y Ng, and Christopher Potts. 2011. Learn-ing word vectors for sentiment analysis. In Proceedings of the 49th annual meeting of the association for computational linguistics: Human language technologies , pages 142–150. Brendan McMahan, Eider Moore, Daniel Ramage, Seth Hampson, and Blaise Aguera y Arcas. 2017. Communication-efficient learning of deep networks from decentralized data. In Artificial intelligence and statistics , pages 1273–1282. Pmlr. Frank McSherry and Kunal Talwar. 2007. Mechanism design via differential privacy. In 48th Annual IEEE Symposium on Foundations of Computer Science (FOCS’07) , pages 94–103. IEEE. Niklas Muennighoff, Luca Soldaini, Dirk Groeneveld, Kyle Lo, Jacob Morrison, Sewon Min, Weijia Shi, Evan Pete Walsh, Oyvind Tafjord, Nathan Lambert, Yuling Gu, Shane Arora, Akshita Bhagia, Dustin Schwenk, David Wadden, Alexander Wettig, Binyuan Hui, Tim Dettmers, Douwe Kiela, Ali Farhadi, and et al. 2025. Olmoe: Open mixture-of-experts language models. In The Thirteenth International Conference on Learning Representations, ICLR 2025, Singapore, April 24-28, 2025 . OpenReview.net. Yixin Nie, Adina Williams, Emily Dinan, Mohit Bansal, Jason Weston, and Douwe Kiela. 2020. Adversarial nli: A new benchmark for natural language understanding. In Proceedings of the 58th annual meeting of the association for computational linguistics , pages 4885–4901. Adam Paszke, Sam Gross, Francisco Massa, Adam Lerer, James Bradbury, Gregory Chanan, Trevor Killeen, Zeming Lin, Natalia Gimelshein, Luca Antiga, Alban Desmaison, Andreas Köpf, Edward Z. Yang, Zachary DeVito, Martin Raison, Alykhan Tejani, Sasank Chilamkurthy, Benoit Steiner, Lu Fang, Junjie Bai, and Soumith Chintala. 2019. Pytorch: An imperative style, high-performance deep learning library. In Advances in Neural Information Processing Systems 32: Annual Conference on Neural Information Processing Systems 2019, NeurIPS 2019, December 8-14, 2019, Vancouver, BC, Canada , pages 8024–8035. Samyam Rajbhandari, Conglong Li, Zhewei Yao, Minjia Zhang, Reza Yazdani Aminabadi, Ammar Ahmad Awan, Jeff Rasley, and Yuxiong He. 2022. Deepspeed-moe: Advancing mixture-of-experts inference and training to power next-generation ai scale. Pranav Rajpurkar, Jian Zhang, Konstantin Lopyrev, and Percy Liang. 2016. Squad: 100,000+ questions for machine comprehension of text. In Proceedings of the 2016 conference on empirical methods in natural language processing , pages 2383–2392. Carlos Riquelme, Joan Puigcerver, Basil Mustafa, Maxim Neumann, Rodolphe Jenatton, AndrÃl’ Susano Pinto, Daniel Keysers, and Neil Houlsby. 2021. Scaling vision with sparse mixture of experts. Ryan Rogers, Aaron Roth, Adam Smith, and Om Thakkar. 2016. Max-information, differential privacy, and post-selection hypothesis testing. In 2016 IEEE 57th Annual Symposium on Foundations of Computer Science (FOCS) , pages 487–494. IEEE. 

12 Stephen Roller, Sainbayar Sukhbaatar, Arthur Szlam, and Jason Weston. 2021. Hash layers for large sparse models. Tanik Saikh, Tirthankar Ghosal, Amish Mittal, Asif Ekbal, and Pushpak Bhattacharyya. 2022. Scienceqa: A novel resource for question answering on scholarly articles. International Journal on Digital Libraries , 23(3):289–301. Noam Shazeer, Azalia Mirhoseini, Krzysztof Maziarz, Andy Davis, Quoc Le, Geoffrey Hinton, and Jeff Dean. 2017. Outrageously large neural networks: The sparsely-gated mixture-of-experts layer. arXiv preprint arXiv:1701.06538 .Adam Smith. 2011. Privacy-preserving statistical estimation with optimal convergence rates. In Proceedings of the forty-third annual ACM symposium on Theory of computing , pages 813–822. Om Thakkar, Galen Andrew, and H Brendan McMahan. 2019. Differentially private learning with adaptive clipping. 

arXiv e-prints , pages arXiv–1905. Pierre Tholoniat, Huseyin A Inan, Janardhan Kulkarni, and Robert Sim. 2024. Differentially private training of mixture of experts models. arXiv preprint arXiv:2402.07334 .Salil Vadhan. 2017. The complexity of differential privacy. In Tutorials on the Foundations of Cryptography: Dedicated to Oded Goldreich , pages 347–450. Springer. Alex Wang, Amanpreet Singh, Julian Michael, Felix Hill, Omer Levy, and Samuel Bowman. 2018. Glue: A multi-task benchmark and analysis platform for natural language understanding. In Proceedings of the 2018 EMNLP workshop BlackboxNLP: Analyzing and interpreting neural networks for NLP , pages 353–355. Lean Wang, Huazuo Gao, Chenggang Zhao, Xu Sun, and Damai Dai. 2024. Auxiliary-loss-free load balancing strategy for mixture-of-experts. Zhiyu Wu, Xiaokang Chen, Zizheng Pan, Xingchao Liu, Wen Liu, Damai Dai, Huazuo Gao, Yiyang Ma, Chengyue Wu, Bingxuan Wang, et al. 2024. Deepseek-vl2: Mixture-of-experts vision-language models for advanced multimodal understanding. arXiv preprint arXiv:2412.10302 .Ashkan Yousefpour, Igor Shilov, Alexandre Sablayrolles, Davide Testuggine, Karthik Prasad, Mani Malek, John Nguyen, Sayan Ghosh, Akash Bharadwaj, Jessica Zhao, Graham Cormode, and Ilya Mironov. 2021. Opacus: User-friendly differential privacy library in pytorch. CoRR , abs/2109.12298. Da Yu, Saurabh Naik, Arturs Backurs, Sivakanth Gopi, Huseyin A Inan, Gautam Kamath, Janardhan Kulkarni, Yin Tat Lee, Andre Manoel, Lukas Wutschitz, et al. 2021. Differentially private fine-tuning of language models. 

arXiv preprint arXiv:2110.06500 .Xinwei Zhang, Zhiqi Bu, Borja Balle, Mingyi Hong, Meisam Razaviyayn, and Vahab Mirrokni. 2025. Disk: Differentially private optimizer with simplified kalman filter for noise reduction. In International Conference on Learning Representations , volume 2025, pages 80289–80316. Lele Zheng, Xiang Wang, Tao Zhang, Yang Cao, Ke Cheng, and Yulong Shen. 2026. Differentially private subspace fine-tuning for large language models. In Fortieth AAAI Conference on Artificial Intelligence, Thirty-Eighth Conference on Innovative Applications of Artificial Intelligence, Sixteenth Symposium on Educational Advances in Artificial Intelligence, AAAI 2026, Singapore, January 20-27, 2026 , pages 28830–28838. AAAI Press. Yanqi Zhou, Tao Lei, Hanxiao Liu, Nan Du, Yanping Huang, Vincent Zhao, Andrew Dai, Zhifeng Chen, Quoc Le, and James Laudon. 2022. Mixture-of-experts with expert choice routing. 

13 RAPTOR Supplementary 

Contents 

A More Related Works 15 

A.1 Structured Private Optimization . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 15 A.2 Partition-Based DP Composition . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 15 A.3 Sparse Mixture-of-Expert Models . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 15 

B Theory Analysis 17 

B.1 Public and Private Quantities . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 17 B.2 Per-Update Sensitivity . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 17 B.3 Proof of Lemma 1 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 17 B.4 Proof of Lemma 2 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 18 B.5 Proof of Theorem 1 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 19 B.6 Proof of Theorem 2 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 19 B.7 SGD-Surrogate Implication . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 20 B.8 Scaling of the DP Term . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 20 B.9 Stability of expert ownership . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 20 B.10 Voronoi-aware expert estimator Error . . . . . . . . . . . . . . . . . . . . . . . . . . . . 22 B.11 Scope of the Bias-SNR Connection (Remark 2 details) . . . . . . . . . . . . . . . . . . 25 

C Experiment Details 26 D More Expriments on Other Models 27 E Ablation Study 28 

E.1 Component Ablation . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 28 E.2 Matched-Scope Diagnostics . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 28 E.3 Sensitivity to the Shared/Expert Budget Split ρ . . . . . . . . . . . . . . . . . . . . . . 29 

F Empirical Routing Entropy Analysis 31 G Mechanism Diagnostics 35 H Budget Allocation: Surrogate Analysis and Empirical Sensitivity 37 I Runtime and Memory 39 

14 A More Related Works 

A.1 Structured Private Optimization 

A growing body of work exploits structure in the private update. Gradient filtering and noise-shaping methods reduce effective noise variance by suppressing or temporally smoothing noise components (Zhang et al., 2025). Parameter-efficient methods shrink the noised dimension via adapters (Yu et al., 2021) or restrict updates to low-rank or learned subspaces (Zheng et al., 2026). Group-wise clipping (Thakkar et al., 2019) calibrates the clipping bound to parameter subsets with related gradient scale - the closest prior idea to our expert-specific clipping, but applied to layer-wise or client-wise groups in a dense setting rather than to routing-induced sparse owner partitions. These methods operate on the temporal, low-rank, or layer-wise structure of the gradient. We exploit a different axis: the sparse routing structure of MoE computation. Rather than reshaping the gradient subspace or noise trajectory, we align clipping, normalization, and privacy composition with the shared-expert role decomposition. Critically, our parallel-composition analysis (Lemma 1) requires disjoint, deterministic, data-independent owner partitions - a structural property not present in group-wise clipping for dense models, where groups are fixed by architecture rather than by private routing decisions. Subspace, low-rank, and noise-shaping methods are orthogonal to our framework and could in principle be applied within each role-aware stream; we leave this to future work. 

A.2 Partition-Based DP Composition 

Parallel composition over disjoint data partitions is a classical result in differential privacy (McSherry and Talwar, 2007), and has been applied in federated learning (McMahan et al., 2017), local DP (Ka-siviswanathan et al., 2011), and multi-party computation (Dwork et al., 2010). Our use of parallel composition differs in two respects. First, the partition is induced by a private routing mechanism: owner groups {Dl,e } are determined by the frozen router applied to private inputs, raising the question of whether the partition itself leaks information. We sidestep this by fixing the router before private training and computing the owner map once from the frozen pretrained router, so the partition is a deterministic function of public model weights and private inputs - analogous to a data-independent hash partition conditioned on the (public) router (Vadhan, 2017). Second, the partition changes across layers; we handle this via sequential composition across layers with stage-fixed owner maps (Sec. 6.2). Data-dependent partitions - where the partition itself depends on the private dataset - require additional privacy accounting for the partitioning step (Dwork et al., 2006; Rogers et al., 2016). Our design avoids this: the owner map al is a deterministic function of the frozen public router and the private inputs, and the router parameters are public. The privacy cost of computing al is zero (it is post-processing of the public router applied to private inputs, producing internal bookkeeping that is never released). This is stated formally in Appendix B.1 and is the key distinction from settings where the partition must itself be privately released (Smith, 2011). 

A.3 Sparse Mixture-of-Expert Models 

Sparse MoE layers replace a dense feed-forward block with a collection of E experts and a router that activates only a small and token-dependent subset, separating parameter count from per-token compute (Shazeer et al., 2017). The recipe scales through GShard (Lepikhin et al., 2020), the Switch Transformer (Fedus et al., 2022), and GLaM (Du et al., 2022), and is employed by open flagship LLMs such as Mixtral (Jiang et al., 2024), DeepSeek-V3 (DeepSeek-AI et al., 2025), and OLMoE (Muennighoff et al., 2025), the last of which we fine-tune in Sec. 8.3. A central design axis is token-to-expert assignment: token-choice top-k gating (Shazeer et al., 2017; Fedus et al., 2022) leaves per-expert load uncontrolled, whereas expert-choice routing (Zhou et al., 2022) and deterministic schemes such as BASE layers (Lewis et al., 2021) and Hash layers (Roller et al., 2021) enforce balance by changing the assignment mechanism. Learned routing tends to collapse onto a handful of experts (Chi et al., 2022), which has motivated remedies from auxiliary losses that balance load across the batch (Shazeer et al., 2017; Fedus et al., 2022) to the bias adjustment of DeepSeek (Wang et al., 2024), which balances load without any auxiliary loss. Each of these depends on routing statistics aggregated over the batch and on the realized load 15 of each expert. These quantities depend on private data and thus break the sensitivity of individual records that DP-SGD (Abadi et al., 2016) requires. For this reason, we freeze the router, drop Laux 

(Sec. 3.3), and normalize by the public denominator De = B/E rather than the realized count (Sec. 5.3). A complementary line of work isolates shared knowledge in experts that are always active so that the routed experts can specialize (Dai et al., 2024; Rajbhandari et al., 2022). This is exactly the asymmetry our framework makes explicit: experts that see every record join the shared stream, while only the conditionally routed experts enter the expert stream (Sec. 3.3). Routing entropy has also been a way to measure how evenly experts are used in language and vision MoEs (Riquelme et al., 2021). We reuse it to select a layer from public data at no privacy cost (Sec. 7) and connect it to the bias of the estimator based on the public denominator (Theorem 2). 16 B Theory Analysis 

B.1 Public and Private Quantities 

The privacy guarantee is with respect to Dpriv . Public quantities include: the pretrained checkpoint, frozen router, candidate layer set, B, E, Cs, Ce, σs, σe, the alternating update schedule, and Cpub (disjoint from 

Dpriv ). All DP statements are conditioned on these and on l⋆.The owner map al : X → [E] and the realized owner counts |Dl,e | are private internal quantities : al

is computed once before private training using only the frozen public router, is fixed for the duration of the stage, and is never released. Realized counts |Bt,l,e | are never read by the update rule, never released, and never used in branching logic. Under add/remove adjacency, a one-record change affects exactly one owner group, which is the structural fact that enables Lemma 1. If the router or checkpoint were trained on Dpriv without DP, that step would incur additional privacy cost. Our protocol avoids this via a frozen public router and public-only layer selection. 

B.2 Per-Update Sensitivity Shared stream. Each per-record gradient is clipped to Cs, so the clipped sum changes by at most Cs

under add/remove. Normalizing by B:

∆sh = Cs

B . (19) 

Expert stream. Each per-record contribution is clipped to Ce; normalizing by De = B/E :

∆l,e = Ce

De

= EC e

B . (20) This is independent of the realized count |Bt,l,e |. Residual weights wi do not affect sensitivity since they enter before clipping; the clipped contribution is at most Ce regardless of wi ∈ [0 , 1] .

Adam. Moment updates are deterministic functions of privatized gradients and the public schedule, hence post-processing that consumes no additional budget. 

B.3 Proof of Lemma 1 Remark 3 (Adaptive alternation does not break parallel composition) . Lemma 1 requires each Ml,e to depend only on Dl,e and public quantities. We verify this holds under alternating updates in three steps. 

(i) Model state as post-processing. At any step t, the model parameters θt are a deterministic function of the sequence of previously released privatized gradients {¯gsh 1 , ¯gexp 1,l,e , . . . , ¯gsh 

> t−1

, ¯gexp 

> t−1,l,e

} and the public learning rate schedule. By the post-processing property of DP, θt is (ε<t , δ <t )-DP with respect to any record, where (ε<t , δ <t ) is the privacy cost of the steps up to t − 1. Conditioning on θt in the subsequent expert-stream step does not consume additional budget beyond what is already accounted for in the sequential composition across steps. 

(ii) Residual weights are pre-clipping. The residual weight wi = 1 − softmax( zsh  

> i

)yi depends on 

θt and the private record (xi, y i). However, wi is applied before clipping to Ce, so the clipped per-record contribution clip( wigexp 

> i,t,l,e

, C e) has sensitivity at most Ce regardless of wi ∈ [0 , 1] (Appendix B.2, Eq. (20) ). The expert mechanism Ml,e therefore remains a function only of Dl,e , the public θt-as-post-processing, and public quantities. Condition (iii) of Lemma 1 is satisfied. 

(iii) No data-dependent control flow. The update schedule - which experts are updated, in what order, and at which steps - is fixed before training and depends only on public quantities ( E, T , the alternating pattern). Empty owner subsets receive a noise-only update rather than being skipped; the decision to apply this update does not branch on any private quantity. Realized expert counts |Bt,l,e | are never read, released, or used in branching logic. Proof. Let D, D′ differ by one record (x0, y 0), which belongs to exactly one group e0 = al(x0) by conditions (i)–(ii). For all e̸ = e0, the group Dl,e is identical under both datasets, so by condition (iii) the output distribution of Ml,e is unchanged. The joint Ml therefore differs only through Ml,e 0 , which is 

(εl,e 0 , δ l,e 0 )-DP. Taking the worst case over e0 gives the stated bound. We additionally verify that Ml,e 

17 satisfies condition (iii) under alternating optimization. The expert mechanism at step t takes as input: (a) the owner subset Bt,l,e ⊆ Dl,e , (b) the current model state θt, and (c) public hyperparameters. Since θt

is a deterministic post-processing of previously privatized gradients (Remark 3), it constitutes a public quantity conditioned on the released mechanism outputs. No private quantity outside Dl,e enters Ml,e 

directly; the only cross-group information flows through the DP-protected θt, which is already accounted for in the sequential composition across steps (Sec. 6.2). 

B.4 Proof of Lemma 2 Lemma 2 (Residual weighting does not break parallel composition) . Fix step t and layer ℓ⋆. Let θt be the model state after t − 1 complete alternating cycles, and let 

wi = 1 − softmax  fsh (xi; θt) 

> yi

∈ [0 , 1] (21) 

be the residual weight for record i. Define the expert mechanism at step t as 

M (t) 

> ℓ,e

(Dℓ,e ) = 1

De

X

> i∈Bt,ℓ,e

clip  wi · gexp 

> i,t,ℓ,e

, C e



+ N 0, σ 2 

> e

C2 

> e

I!

.

(22) 

Then M (t) 

> ℓ,e

satisfies condition (iii) of Lemma 1: it depends on Dℓ,e and effectively public quantities, enabling parallel composition across owner groups. Proof. We proceed in three parts. 

Part A: θt is a released DP output, not a free cross-group channel. 

By the post-processing property of differential privacy (Dwork and Roth, 2014), any deterministic function of an (ε, δ )-DP output is itself (ε, δ )-DP. The sequence of released privatized gradients up to step 

t is 

τ<t =  ¯gsh 1 , {¯gexp 1,ℓ,e }e∈[E],. . . , ¯gsh 

> t−1

, {¯gexp 

> t−1,ℓ,e

}e∈[E] . (23) By sequential composition (Sec. 6.2), τ<t is (ε<t , δ <t )-DP, and this cost is already fully accounted for in 

ωtot (Eq. 10). The model state θt = optimizer( τ<t , public schedule ) is a deterministic function of τ<t 

and public hyperparameters; by post-processing it is therefore (ε<t , δ <t )-DP and carries no additional privacy cost beyond what sequential composition already charges. Crucially, θt is a function of already-released mechanism outputs, not a new query into Dpriv . Condi-tioning on θt in the definition of M (t) 

> ℓ,e

is therefore equivalent to conditioning on a public side-channel whose privacy cost is already captured. 

Part B: wi does not introduce cross-group record dependencies at step t.

We decompose the dependence of M (t) 

> ℓ,e

on Dpriv into two channels. 

(i) Direct channel. M (t) 

> ℓ,e

reads records in Bt,ℓ,e ⊆ Dℓ,e directly to compute per-record gradients. By the disjoint ownership condition (Sec. 5.2), no record outside Dℓ,e appears in Bt,ℓ,e .

(ii) Indirect channel via θt. The residual weight wi depends on θt, which was influenced by records outside Dℓ,e through prior shared-stream steps. However, this influence has already been privatized: every record xj /∈ Dℓ,e that contributed to θt did so only through the released privatized gradients τ<t , whose privacy cost is captured in ε<t . The weight 

wi = 1 − softmax  fsh (xi; θt) 

> yi

(24) is therefore a post-processing of previously released DP outputs composed with the private record (xi, y i),where (xi, y i) ∈ Dℓ,e by construction. No record xj /∈ Dℓ,e is read afresh at step t through wi; its influence is confined to the already-accounted θt.18 Part C: Sensitivity is unaffected by wi.

Since wi ∈ [0 , 1] and clipping is applied after multiplication, 

clip  wi · gexp 

> i,t,ℓ,e

, C e

 

> 2

≤ Ce ∀ wi ∈ [0 , 1] . (25) The per-update sensitivity ∆ℓ,e = EC e/B (Eq. (25)) holds regardless of wi. The noise magnitude σeCe is calibrated to ∆ℓ,e , so the Gaussian mechanism guarantees (εe, δ e)-DP on Dℓ,e with the same parameters as the wi = 1 case. 

Conclusion. M (t) 

> ℓ,e

reads Dℓ,e directly (Parts A–B), has bounded sensitivity (Part C), and its dependence on records outside Dℓ,e is fully mediated through the DP-protected θt whose cost is already accounted for in sequential composition. Condition (iii) of Lemma 1 is therefore satisfied, and parallel composition applies. 

Remark 4 (No double-counting of privacy budget) . One might worry that using θt to compute wi “uses up” additional privacy budget beyond ε<t . This is not the case. The post-processing property guarantees that any computation on a DP output-including evaluating the model on a private record (xi, y i) to obtain 

wi-does not increase privacy cost, provided the result is used only inside the per-record clipping operation and is never released directly. Here, wi is used solely as a scalar multiplier before clip( · , C e); it is never released, logged, or used in control flow. The only released quantity at step t is ¯gexp 

> t,ℓ,e

, whose privacy guarantee is certified by the Gaussian mechanism with sensitivity ∆ℓ,e .Composition across steps is adaptive in the standard sense: each step’s mechanism depends on θt, itself a function of previously released outputs. What makes sequential composition via the PRV accountant (Eq. 10) valid here is not the absence of adaptivity but that every step’s mechanism is a subsampled Gaussian with the same, data-independent parameters (Ce, σ e, D e) fixed before training (Sec. 5.4). Each release is therefore governed by the same privacy-loss distribution regardless of the realized θt or wi,exactly as in the moments-accountant analysis of DP-SGD - so standard PRV composition applies without a joint privacy-loss variable spanning steps. 

B.5 Proof of Theorem 1 

Proof. Write Se = P 

> i∈De

bihexp  

> i

with bi iid 

∼ Bernoulli( p). Then ESe = pn eμe = Bq eμe, so 

E¯gexp  

> t,e

= EB ESe = Eq eμe, (26) giving bias (Eq e − 1) μe and β2 

> e

= ( Eq e − 1) 2∥μe∥22. Bernoulli sampling gives Cov( Se) = p(1 −

p)ne(μeμ⊤ 

> e

+ Σ e). Since ξt,e ⊥ Se,

Cov(¯ gexp  

> t,e

) = E2

B2

p(1 − p)ne(μeμ⊤ 

> e

+ Σ e) + σ2 

> e

C2 

> e

Id

. (27) Taking traces and applying the bias-variance identity yields Eqs. (11)–(13). 

B.6 Proof of Theorem 2 

Proof. From Theorem 1, β2 

> e

≤ ¯G2(Eq e − 1) 2.

Eq. (15) (unweighted average). Recall χ2(q∥u) = E P

> e

(qe − 1/E )2 = E−1 P

> e

(Eq e − 1) 2, so P

> e

(Eq e − 1) 2 = Eχ 2(q∥u). Averaging: 

> 1
> E

X

> e

β2 

> e

≤ ¯G2

> E

X

> e

(Eq e − 1) 2 = ¯G2χ2(q∥u).

Eq. (16) (frequency-weighted). Because β2 

> e

≤ ¯G2(Eq e − 1) 2,

X

> e

qeβ2 

> e

≤ ¯G2X

> e

qe(Eq e − 1) 2. (28) 19 Expanding (Eq e − 1) 2 = E2q2 

> e

− 2Eq e + 1 :

X

> e

qe(Eq e −1) 2 = E2X

> e

q3 

> e

− 2EX

> e

q2 

> e

+ 1 . (29) Applying q3 

> e

≤ q2 

> e

(since qe ≤ 1): (29) ≤ (E2 −2E)X

> e

q2 

> e

+ 1 . (30) The identity χ2(q∥u) = E P 

> e

q2 

> e

− 1 gives P 

> e

q2 

> e

= ( χ2(q∥u) + 1) /E , so: (30) = ( E −2) χ2(q∥u) + ( E −1) . (31) Combining (28)–(31): X

> e

qeβ2 

> e

≤ ¯G2(E −2) χ2(q∥u) + ( E −1)  . (32) Since E ≥ 2 both coefficients are non-negative. A coarser path via qe ≤ 1 directly gives: 

X

> e

qe(Eq e −1) 2 ≤ X

> e

(Eq e −1) 2 = Eχ 2(q∥u), (33) recovering P 

> e

qeβ2 

> e

≤ ¯G2Eχ 2(q∥u), i.e. Eq. (16). Bound (32) is tighter for small χ2; bound (33) is simpler and suffices for the entropy criterion in Sec. 7. 

B.7 SGD-Surrogate Implication 

Let Fbal be L-smooth and PL with parameter β > 0. For bias bt and noise zt with E∥zt∥22 ≤ V ,

E∥bt∥22 ≤ Bimb , and η ≤ 1/L :

E[Fbal (θT )] − F ∗

> bal

≤ (1 − ηβ )T ∆0 + O

 LηV β



+ O

 LB imb 

> β2



, (34) where ∆0 = Fbal (θ0) − F ∗

> bal

. Bounding Bimb via Theorem 2 and Eq. (17) shows routing imbalance contributes an error vanishing at uniform routing. This is an explanatory bound within a surrogate model, not a convergence guarantee for DP-Adam on non-convex MoE networks. 

B.8 Scaling of the DP Term 

Under balanced routing, Corollary 1 gives 

q

E∥¯gexp  

> t,e

− μe∥22 ≤ Ce

pE/B + Eσ eCe

√d/B . With σe =Θ( plog(1 /δ )/ε step ), the privacy term scales as 

EC e

pd log(1 /δ )

B ε step 

, (35) matching the standard per-expert DP mean-estimation rate up to log factors when B = Θ( n). This is a single-estimator result, not a convergence guarantee for DP-Adam. 

B.9 Stability of expert ownership 

This appendix formalizes the stability claim of Sec. 7.2: because the owner map al⋆ is frozen before training and never recomputed, we ask how far the induced ownership partition can drift as the upstream representation ϕ = θsh (·) evolves, under a Voronoi abstraction of ownership. 

Definition 2 (Voronoi expert ownership) . Let ϕ : X → Rm be a frozen representation map and let 

C = {ce}Ee=1 ⊂ Rm be expert prototypes. The Voronoi cell of expert e is Ve = {z : ∥z − ce∥22 ≤∥z − cj ∥22, ∀j ∈ [E]}. The induced owner map is aC (x) = arg min e∈[E] ∥ϕ(x) − ce∥22, with deterministic tie-breaking. The routing mass is qe = P(aC (X) = e).

20 Definition 3 (Voronoi margin and boundary regularity) . For z = ϕ(x), let a = aC (x) and define the margin γC (z) = min j̸ =a{∥ z − cj ∥22 − ∥ z − ca∥22}. Let ∆C = max j,k ∥cj − ck∥2. We say that the representation distribution satisfies (κ, C )-boundary regularity if P(γC (ϕ(X)) ≤ t) ≤ κt for all t > 0.

Definition 4 (Perturbed ownership) . Given another representation map ˜ϕ : X → Rm, define ˜aC (x) = arg min e∈[E] ∥ ˜ϕ(x) − ce∥22 and ˜qe = P(˜ aC (X) = e), with the same deterministic tie-breaking. 

Theorem 5 (Voronoi stability of expert ownership) . If ∥ ˜ϕ(x) − ϕ(x)∥2 ≤ r for all x, then ˜aC (x) = aC (x)

for every x such that γC (ϕ(x)) > 2∆ C r. Moreover, under (κ, C )-boundary regularity, P(˜ aC (X)̸ =

aC (X)) ≤ 2κ∆C r and ∥˜q − q∥1 ≤ 4κ∆C r.Proof. Fix x ∈ X and write z = ϕ(x), ˜z = ˜ϕ(x), and a = aC (x). For each j̸ = a, define 

ha,j (u) = ∥u − cj ∥22 − ∥ u − ca∥22.

Since a = arg min e ∥z − ce∥22, ha,j (z) ≥ 0 for all j̸ = a, and 

γC (z) = min   

> j̸=a

ha,j (z).

Expanding the squared norms gives 

ha,j (u) = −2⟨u, c j ⟩ + ∥cj ∥22 + 2 ⟨u, c a⟩ − ∥ ca∥22

= 2 ⟨u, c a − cj ⟩ + ∥cj ∥22 − ∥ ca∥22.

Therefore 

ha,j (˜ z) − ha,j (z) = 2 ⟨˜z − z, c a − cj ⟩.

By Cauchy–Schwarz and ∆C = max p,q ∥cp − cq∥2,

ha,j (˜ z) = ha,j (z) + 2 ⟨˜z − z, c a − cj ⟩≥ ha,j (z) − 2∥˜z − z∥2∥ca − cj ∥2

≥ ha,j (z) − 2r∆C .

Taking the minimum over j̸ = a yields 

min   

> j̸=a

ha,j (˜ z) ≥ min   

> j̸=a

ha,j (z) − 2r∆C

= γC (z) − 2r∆C .

Thus, if γC (z) > 2r∆C , then ha,j (˜ z) > 0 for every j̸ = a. Equivalently, 

∥˜z − ca∥22 < ∥˜z − cj ∥22, ∀j̸ = a. 

Hence ˜aC (x) = aC (x). Therefore 

{˜aC (X)̸ = aC (X)} ⊆ { γC (ϕ(X)) ≤ 2r∆C }.

By (κ, C )-boundary regularity, 

P(˜ aC (X)̸ = aC (X)) ≤ P(γC (ϕ(X)) ≤ 2r∆C )

≤ 2κr ∆C .

It remains to control the routing mass. Let A = aC (X) and ˜A = ˜ aC (X). For each expert e,

|˜qe − qe| = P( ˜A = e) − P(A = e)

= E[1{ ˜A = e} − 1{A = e}]

≤ E 1{ ˜A = e} − 1{A = e} .

21 The two indicators differ only when the assignments disagree, and more explicitly, 

1{ ˜A = e} − 1{A = e} ≤ 1{ ˜A = e, A ̸ = e}

+ 1{A = e, ˜A̸ = e}.

Summing over e gives 

∥˜q − q∥1 =

> E

X

> e=1

|˜qe − qe|≤

> E

X

> e=1

P( ˜A = e, A ̸ = e)+

> E

X

> e=1

P(A = e, ˜A̸ = e).

Since exactly one value of e satisfies ˜A = e and exactly one value satisfies A = e,

> E

X

> e=1

P( ˜A = e, A ̸ = e) = P( ˜A̸ = A),

and EX

> e=1

P(A = e, ˜A̸ = e) = P(A̸ = ˜A).

Consequently, 

∥˜q − q∥1 ≤ 2P( ˜A̸ = A)

≤ 4κr ∆C .

The theorem follows. 

B.10 Voronoi-aware expert estimator Error Definition 5 (Voronoi cell radius and gradient field) . Let Zi = ϕ(xi) and Ai = aC (xi). For each expert 

e, define 

Ie = {i : Ai = e}, ne = |Ie|, qe = ne/n. 

Let hi = clip( gexp  

> i

, C e) ∈ Rd be the clipped expert gradient, and define 

μe = 1

ne

X

> i∈Ie

hi,

Σe = 1

ne

X

> i∈Ie

(hi − μe)( hi − μe)⊤.

Assume there exists an Lg-Lipschitz field ψ : Rm → Rd such that hi = ψ(Zi). Define 

νe = ψ(ce), r2 

> e

= 1

ne

X

> i∈Ie

∥Zi − ce∥22.

Definition 6 (Public-denominator expert estimator) . Under Poisson subsampling Si ∼ Bernoulli( p) with 

B = pn , define 

¯ge = EB

X

> i∈Ie

Sihi + ξe

 ,

where 

ξe ∼ N(0 , σ 2 

> e

C2 

> e

Id).

22 Theorem 6 (Voronoi-aware expert estimation error) . For each expert e,

E∥¯ge − νe∥22 ≤ V samp  

> e

+ V DP  

> e

+ 2( Eq e − 1) 2∥μe∥22 + 2 L2

> g

r2 

> e

,

where 

V samp  

> e

= E2p(1 − p)ne

B2

 ∥μe∥22 + tr Σ e

 ,

and 

V DP  

> e

= E2σ2 

> e

C2 

> e

dB2 .

Consequently, if ¯G2 = max e ∥μe∥22, then 

1

E

> E

X

> e=1

E∥¯ge − νe∥22 ≤ 1

E

> E

X

> e=1

 V samp  

> e

+ V DP 

> e



+ 2 ¯G2χ2(q∥u) + 2L2

> g

E

> E

X

> e=1

r2 

> e

.

Proof. Fix an expert e. All expectations below are taken over the Poisson subsampling variables {Si} and the Gaussian noise ξe, conditional on the dataset. By Definition 6, 

¯ge = EB

X

> i∈Ie

Sihi + EB ξe.

Since Si ∼ Bernoulli( p) and E[ξe] = 0 ,

E[¯ ge] = EB

X

> i∈Ie

E[Si]hi

= Ep B

X

> i∈Ie

hi.

Using B = pn and P 

> i∈Ie

hi = neμe, we obtain 

E[¯ ge] = En e

n μe = Eq eμe.

Hence the bias relative to the cell mean is 

E[¯ ge] − μe = ( Eq e − 1) μe.

We next compute the variance. Independence of the Bernoulli variables gives 

Var 

 EB

X

> i∈Ie

Sihi

 = E2

B2

X

> i∈Ie

Var( Sihi).

Since hi is fixed conditional on the dataset, 

Var( Sihi) = p(1 − p)hih⊤ 

> i

.

Therefore, 

tr Var 

 EB

X

> i∈Ie

Sihi

 = E2p(1 − p)

B2

X

> i∈Ie

∥hi∥22.

23 By the empirical second-moment identity, 

1

ne

X

> i∈Ie

∥hi∥22 = ∥μe∥22 + tr Σ e.

Thus 

tr Var 

 EB

X

> i∈Ie

Sihi

 = V samp  

> e

.

The DP noise is independent of the sampling term, and 

tr Var 

 EB ξe



= E2

B2 tr  σ2 

> e

C2 

> e

Id



= E2σ2 

> e

C2 

> e

dB2 = V DP  

> e

.

Therefore, 

E ∥¯ge − E[¯ ge]∥22 = V samp  

> e

+ V DP  

> e

.

Now decompose the error relative to the prototype gradient νe:

E∥¯ge − νe∥22 = E ∥¯ge − E[¯ ge]∥22 + ∥E[¯ ge] − νe∥22 .

Using E[¯ ge] = Eq eμe,

E[¯ ge] − νe = ( Eq e − 1) μe + ( μe − νe).

Hence, by ∥a + b∥22 ≤ 2∥a∥22 + 2 ∥b∥22,

∥E[¯ ge] − νe∥22 ≤ 2( Eq e − 1) 2∥μe∥22 + 2 ∥μe − νe∥22.

It remains to control ∥μe − νe∥22. Since hi = ψ(Zi) and νe = ψ(ce),

μe − νe = 1

ne

X

> i∈Ie

(ψ(Zi) − ψ(ce)) .

By Jensen’s inequality, 

∥μe − νe∥22 ≤ 1

ne

X

> i∈Ie

∥ψ(Zi) − ψ(ce)∥22.

Since ψ is Lg-Lipschitz, 

∥ψ(Zi) − ψ(ce)∥22 ≤ L2

> g

∥Zi − ce∥22.

Therefore, 

∥μe − νe∥22 ≤ L2

> g

ne

X

> i∈Ie

∥Zi − ce∥22

= L2

> g

r2 

> e

.

Combining the preceding bounds gives 

E∥¯ge − νe∥22 ≤ V samp  

> e

+ V DP  

> e

+ 2( Eq e − 1) 2∥μe∥22 + 2 L2

> g

r2 

> e

.

We now prove the aggregate bound. Let ¯G2 = max e ∥μe∥22. Then 

1

E

> E

X

> e=1

(Eq e − 1) 2∥μe∥22 ≤ ¯G2

E

> E

X

> e=1

(Eq e − 1) 2.

24 Since ue = 1 /E ,

χ2(q∥u) = 

> E

X

> e=1

(qe − ue)2

ue

= E

> E

X

> e=1



qe − 1

E

2

.

Moreover, 

1

E

> E

X

> e=1

(Eq e − 1) 2 = E

> E

X

> e=1



qe − 1

E

2

= χ2(q∥u).

Therefore, 

1

E

> E

X

> e=1

(Eq e − 1) 2∥μe∥22 ≤ ¯G2χ2(q∥u).

Averaging the per-expert inequality over e yields 

1

E

> E

X

> e=1

E∥¯ge − νe∥22 ≤ 1

E

> E

X

> e=1

 V samp  

> e

+ V DP 

> e



+ 2 ¯G2χ2(q∥u) + 2L2

> g

E

> E

X

> e=1

r2 

> e

.

This proves the theorem. 

B.11 Scope of the Bias-SNR Connection (Remark 2 details) 

This appendix expands the two claims of Remark 2. 

Non-monotonicity. β2 

> e

= ( Eq e − 1) 2∥μe∥22 is symmetric around qe = 1 /E : an expert at qe = 2 /E 

has the same β2 

> e

as one at qe = 0 , though these are opposite outcomes for training. βe measures deviation from a normalization convention, not training quality. 

Impossibility of an entropy bound on SNR. No function f satisfies 1/SNR e ≤ f (H(q)) for all valid 

(q, e ). Since χ2(q∥u) ∈ [0 , E −1] for every valid q, take qe = δ for one expert and qe′ = (1 − δ)/(E−1) 

for the rest. As δ → 0+, χ2(q∥u) → 1/(E−1) - near its own minimum - while H(q) → log( E−1) ,near-maximal, and SNR e ∝ q2 

> e

→ 0. A starved expert’s contribution to the aggregate divergence is only 

O(1 /E ), so it can coexist with near-maximal entropy while its own SNR vanishes; no finite f can bound 

1/SNR e by H(q).25 C Experiment Details 

Code and reproducibility. All experiments use PyTorch (Paszke et al., 2019) with PRV account-ing (Gopi et al., 2021) via Opacus (Yousefpour et al., 2021) v1.6.0 (Python 3.12, CUDA 12.6, PyTorch 2.9). 

Hardware and training. Each trial runs on a single B200 (196 GB). At each optimiser step, every record i ∈ Dpriv is independently included in Bexp  

> t

with probability p = B/n , giving an expected batch size of B = pn . Because the realised |Bexp  

> t

| may be large, we accumulate per-record clipped gradients across memory-efficient microbatches within a single optimiser step; no gradients are accumulated across 

steps. The denominators B (shared stream) and De = B/E (expert stream) are fixed expected values independent of the realised |Bexp  

> t

|. Privacy budgets are computed via the PRV accountant (Gopi et al., 2021) with Poisson rate p and add/remove adjacency. 

Hyperparameters and tuning. Primary hyperparameters are epochs, batch size B, learning rate η,clipping threshold C, and noise multiplier σDP (set by the PRV accountant). Each method-including Baselines A, B, and C-is tuned independently on SST-2 using the same search grid (Tab. 4) and the same budget of Ntotal = 100 trials; the best-performing configuration per method is then frozen for MNLI, QNLI, and QQP. SST-2 results therefore reflect in-task tuning performance, while the remaining three tasks measure cross-task transfer under a fixed, task-agnostic configuration. Tab. 4 lists the search grid; selected values are bolded. Specifically, Baseline A treats C and σDP as free variables over the full grid and does not inherit Cs

from the proposed method; the PRV accountant targets the full budget ε (i.e. ρ = 1 ) and solves for σDP 

jointly with C via binary search, matching the accounting procedure of the proposed method. Baselines B and C likewise search over all grid values of η, C, B, and epochs independently. The proposed method applies LoRA to the shared stream θsh and each expert θl⋆,e ; the role-aware mechanism (ownership map, public denominator De, residual objective) is independent of this parameterisation choice.                          

> Hyperparameter Search grid Epochs {10 ,20 ,30 }
> Batch size B{500 ,1000 ,2000 ,3000 }
> Learning rate η{10 ,5,3,1,0.5} × 10 −4
> Clipping C{0.1,1.0,10 }
> Budget split ρ{0.7,0.8,0.9,0.95 }
> Table 4: Hyperparameter search grid (SST-2). Selected values bolded; configuration frozen for all other tasks.

Clipping and δ. We use per-record ℓ2 clipping clip( g, C ) = g · min(1 , C/ ∥g∥2) and set δ = 1 /N 1.1

for each task. 26 D More Expriments on Other Models       

> Figure 3: Validation accuracy on ScienceQA for D EEP SEEK -VL2-T INY across ε∈ { 1,4,8}. Single run per ε.

As a secondary check of generalization beyond text-only encoder MoEs, we fine-tune 

DeepSeek-VL2-Tiny (Wu et al., 2024) on ScienceQA (Saikh et al., 2022), a vision-language task. 

DeepSeek-VL2-Tiny has both always-active shared experts and routed experts (Sec. 3); shared experts fold into the shared stream, and RAPTOR is applied to the last routed-expert layer as in the main experiments. No disjoint, format-matched public corpus was available for this setting, so we protect the last sparse layer by fixed default rather than via entropy-based selection (Sec. 5.4). Fig. 3 reports validation accuracy at ε ∈ { 1, 4, 8}. At ε = 1 , Ours reaches 86 .98% vs. 85 .12% for Monolithic DP-Adam LoRA ( +1 .86 ), consistent with the high-noise pattern on Switch and OLMoE; the gap closes with ε,tying at 4 (87 .17% ) and marginally exceeding non-private LoRA at 8 (87 .27% vs. 87 .22% ) - a single-run artifact (Reporting, Sec. 8.1) rather than evidence that private training exceeds its non-private upper bound. That gains persist under a fixed-layer fallback, without entropy-based selection, suggests RAPTOR ’s core mechanism does not strictly depend on that selection step (Sec. 5.1). 27 E Ablation Study 

E.1 Component Ablation 

Tab. 1 attributes Ours ’s overall gains to the update rule as a whole, but does not show which of the four components introduced in Sec. 5 - expert-specific clipping, the public denominator De=B/E , the alternating shared/expert schedule, and the residual objective - is doing the work. To isolate each one’s contribution, we ablate Ours on S WITCH -BASE -8 by removing or replacing exactly one component at a time while holding the entropy-selected sparse layer fixed: w/ De=B replaces the public denominator with the realized-count-free but naive full-batch denominator; w/ full-model clip replaces separate shared/expert clipping with a single global bound C; w/o alternating updates shared and expert parameters jointly in one step rather than in alternation (Sec. 5.4); and w/o residual obj. drops the residual weighting wi (Eq. (8)), training the expert stream on the raw classification loss instead. Tab. 5 reports this ablation at ε ≈ 8

and ε = 1 side by side, so that each component’s contribution can be compared directly across privacy budgets. The alternating schedule is the largest contributor: removing it costs up to 1.97 pts at ε ≈ 8 (QNLI) and 2.09 pts at ε = 1 (QQP), and on QNLI/QQP at ε ≈ 8 the no-alternating variant falls below the matched-scope global baseline (Tab. 1), showing that role-aware clipping and normalization alone are not sufficient without the two-stream schedule. The public denominator De = B/E contributes 0.05 -0.98 pts, with the largest single gain on QNLI; consistent with Corollary 1, this gain is proportionally larger relative to the other components at ε = 1 , where the bias from De = B under high noise is more damaging. Expert-specific clipping contributes 0.06 -0.62 pts, and the residual objective contributes 0.04 -0.66 pts, smallest on SST-2, where the shared stream alone suffices for sentiment classification. Relative to ε ≈ 8,component contributions grow at ε = 1 on SST-2, MNLI, and QQP but shrink on QNLI: the high-noise amplification suggested by Corollary 1 is task-dependent, not uniform. Together, all components yield 

0.23 -3.24 pts over Monolithic DP-Adam LoRA (Tab. 1).                                                                        

> ε≈8ε= 1
> Method SST-2 MNLI QNLI QQP SST-2 MNLI QNLI QQP Monolithic DP-Adam LoRA 93 .92 79 .36 82 .48 82 .68 90 .94 76 .91 80 .38 81 .19
> RAPTOR (full) 94 .15 81 .32 85 .26 85 .92 92 .90 78 .42 83 .30 84 .08
> w/ De=B94 .10 80 .96 84 .28 85 .55 92 .83 77 .98 82 .79 83 .55
> w/ full-model clip 94 .09 81 .25 84 .64 85 .84 92 .82 78 .34 83 .22 83 .99
> w/o alternating 94 .02 80 .21 83 .29 84 .09 92 .75 77 .15 81 .68 81 .99
> w/o residual obj. 94 .07 81 .22 84 .60 85 .82 92 .86 78 .30 83 .18 83 .96
> Table 5: Component ablation on S WITCH -BASE -8 at ε≈8and ε= 1 . Bold: full RAPTOR . Each row removes or replaces one component; all variants use the entropy-selected sparse layer.

E.2 Matched-Scope Diagnostics 

The gains reported in Tab. 1 compare RAPTOR against Monolithic DP-Adam and Monolithic DP-Adam LoRA, both of which differ from Ours along two axes at once: they use a different update rule (one clipping bound, one noise scale, one denominator, applied to the full model) and a different training scope (all trainable parameters, rather than the shared stream plus one selected expert layer). This leaves an open question: how much of Ours’s advantage comes from the role-aware update rule itself - separate clipping, the public denominator De=B/E , and the alternating schedule - versus simply from training a smaller, more targeted set of parameters under a matched privacy budget? To isolate this, we construct three matched-scope baselines that fix Ours’s layer, total privacy budget, and step count, and vary only the update rule applied within that scope: Baseline A trains the same shared and expert parameters as Ours but with one global clipping bound, one noise scale, and one denominator (i.e. monolithic DP applied to Ours’s reduced scope); Baseline B trains only the shared stream, with the selected expert layer frozen; and Baseline C trains only the selected expert layer, with the shared stream frozen. Comparing Ours against these three, rather than against the full-scope monolithic baselines alone, lets us attribute the accuracy 28 gains to the update rule specifically, rather than to scope or parameter count. Tab. 6 reports Baselines A-C on S WITCH -BASE -8 (same layer, scope, budget, and steps as Ours; update rule only varies; Sec. 8.1) alongside Monolithic DP-Adam LoRA and Ours (entropy-selected) for reference, across all three privacy budgets. Baseline A (global DP) isolates whether matching training scope to Ours alone explains the gains; it trails Ours by 0.36 –3.85 points on MNLI, QNLI, QQP at ε = 8 ,and by comparable margins at ε ∈ { 1, 4} - confirming the remaining gap is attributable to role-aware clipping, normalization, and the alternating schedule (Tab. 5), not scope alone. Baseline B (shared-only, expert parameters frozen) is competitive with Ours on SST-2 but trails on QNLI and QQP at every budget, showing expert updates contribute beyond what the shared stream alone achieves. Baseline C (expert-only, shared stream frozen) collapses toward random accuracy on MNLI ( 32 –36% ) and QNLI ( ≈ 50% ) across all budgets, confirming the shared stream is necessary, not merely helpful.                                                                  

> εMethod SST-2 MNLI QNLI QQP
> 1
> Monolith. DP-Adam LoRA 90.94 76.91 80.38 81.19 Baseline A (global) 92.55 76.80 82.90 83.10 Baseline B (shared-only) 91.80 76.40 82.50 82.90 Baseline C (expert-only) 67.78 32.75 49.72 63.66
> Ours, entropy-sel. 92.90 78.42 83.30 84.08
> 4
> Monolith. DP-Adam LoRA 92.66 78.33 81.95 82.26 Baseline A (global) 93.32 77.10 84.68 84.40 Baseline B (shared-only) 93.20 77.70 84.68 84.78 Baseline C (expert-only) 68.69 34.91 49.75 64.20
> Ours, entropy-sel. 94.05 79.03 84.70 84.78
> 8
> Monolith. DP-Adam LoRA 93.92 79.36 82.48 82.68 Baseline A (global) 93.46 77.47 84.90 85.20 Baseline B (shared-only) 93.50 78.10 84.83 85.50 Baseline C (expert-only) 68.81 35.57 49.73 64.47
> Ours, entropy-sel. 94.15 81.32 85.26 85.92

Table 6: Matched-scope diagnostics on S WITCH -BASE -8: Baselines A–C share Ours’s layer, scope, budget, and step count, varying only the update rule (Sec. 8.1). Monolithic DP-Adam LoRA and Ours (entropy-selected) shown for reference; full results including first-/last-sparse variants and non-private upper bounds are in Tab. 1. Single run per cell. 

E.3 Sensitivity to the Shared/Expert Budget Split ρ

RAPTOR introduces one hyperparameter with no analogue in monolithic DP fine-tuning: ρ ∈ (0 , 1) ,which splits the total privacy budget ε between the shared stream ( ρε ) and the expert streams ( (1 −ρ)ε,Sec. 6). Because the two streams have different sensitivities and noise requirements - the expert stream’s noise variance carries an extra E2 factor from the normalized sensitivity EC e/B (Theorem 1) - this split is not a cosmetic tuning knob: an unfavorable choice of ρ could starve either stream of budget and erase the method’s advantage entirely. Proposition 1 gives qualitative guidance (shared-dominant ρ under the surrogate analysis of Appendix H), but does not certify a specific value; we therefore validate the choice of ρ = 0 .9 empirically here by sweeping ρ against ε and checking that accuracy is not unduly sensitive to this choice within a reasonable range. Fig. 4 shows SST-2 accuracy across ρ ∈ { 0.7, 0.8, 0.9, 0.95 } and ε ∈ { 1, 2, 4, 8}. Accuracy increases monotonically with ε for ρ ≤ 0.9; at ρ = 0 .95 accuracy instead drops from ε = 4 to ε = 8 (93 .46 →

92 .09 ), an inversion that only appears at the most shared-dominant split tested. Sensitivity to ρ is highest at 

ε = 1 , where the tightest overall budget makes misallocation between streams most costly, and shrinks at larger ε, where both streams have enough budget that the split matters less. Setting ρ = 0 .95 hurts at ε = 8 

(92 .09 vs. 94 .15 for ρ = 0 .9), showing that under-budgeting the expert stream degrades expert-specific learning even when the total budget is generous. We use ρ = 0 .9 as the default throughout, as it is uniformly at or near the best setting across all four values of ε tested. 29 Figure 4: SST-2 accuracy (%) of RAPTOR on S WITCH -BASE -8 across ε ∈ {1, 2, 4, 8} and ρ ∈{0.7, 0.8, 0.9, 0.95 }.

30 F Empirical Routing Entropy Analysis 

We verify whether routing entropy, estimated on external public corpora, provides a reliable privacy-free signal for layer selection. Per Sec. 7, higher H(q(l)) implies smaller public-denominator bias at layer l.

Calibration corpora. For each task we use an external unlabeled corpus disjoint from all GLUE splits and from Dpriv : IMDb reviews for SST-2 (Maas et al., 2011), ANLI R2 pairs for MNLI (Nie et al., 2020), SQuAD contexts for QNLI (Rajpurkar et al., 2016). For QQP, we use PAWS-Wiki sentence pairs from the PAWS labeled_final training split, with labels ignored. Exact string-match verification finds zero pair overlap with GLUE QQP, QNLI, MNLI, SST-2 train. 

Protocol. For each candidate layer l ∈ { 0, . . . , 5} (encoder blocks {1, 3, 5, 7, 9, 11 }), we apply the frozen S WITCH -BASE -8 router to Cpub and compute the empirical owner distribution 

q(l) 

> e

= |{ xi ∈ Cpub : al(xi) = e}| |Cpub | . (36) Tab. 7 reports H(q)/ log E, log E − H(q), and χ2(q∥u) for all 24 layer-task combinations. 

Results. Routing balance varies substantially across tasks and layers. On SST-2, block 1 is by far the most balanced ( H/ log E = 0 .972 , χ2 = 0 .118 ), while block 9 collapses ( H/ log E = 0 .304 ). On MNLI, block 1 collapses ( χ2 = 4 .946 ) and block 7 is most balanced ( H/ log E = 0 .682 , χ2 = 1 .262 ). On QNLI, block 11 leads ( H/ log E = 0 .902 , χ2 = 0 .393 ). On QQP, blocks 5 and 7 collapse ( χ2 > 2.9) while block 9 is most balanced ( H/ log E = 0 .837 , χ2 = 0 .683 ). Applying l⋆ = arg max l H(q(l)) selects block 1 (SST-2), block 7 (MNLI), block 11 (QNLI), and block 9 (QQP) - all unambiguous, with margins of at least 0.029 nats over the runner-up. Within each task, entropy and χ2 criteria agree, consistent with the Pinsker bound (Eq. (17)). A fixed “last-layer” heuristic would miss the optimal layer for SST-2 (by 0.311 nats) and QQP. These selections were fixed before any private training; Sec. 8.2 tests them directly. 

Corpus-size sensitivity. Fig. 5 and Fig. 6 show that the minimum n for stable layer selection correlates inversely with the entropy margin between the top two layers (Tab. 7): SST-2 stabilises at n = 10 (margin 

0.144 nats), QNLI at n ≥ 100 (0.021 nats), MNLI at n ≥ 500 (0.008 nats), and QQP at n ≥ 1,000 (0.023 

nats, three-way near-tie). All four tasks select the correct layer at n = 1 ,000 , under 2.2% of the smallest calibration corpus used. We recommend n ≥ 1,000 as a conservative default; when the entropy margin at that size exceeds 0.05 nats, increasing n further changes neither the ranking nor the selected block. 

Public-to-private entropy transfer. The entropy-based layer-selection rule (Sec. 5.1) relies on routing statistics computed on a public corpus Cpub to proxy the routing balance on the private training set 

Dpriv . Fig. 7 checks this transfer empirically by plotting, for each of the 24 layer-task combinations, the normalised routing entropy H(q)/ log E estimated on Cpub against the corresponding entropy estimated on the GLUE development set. Transfer is close for QNLI and, more loosely, for SST-2 and QQP; MNLI is the exception, with all six layers sitting visibly above the diagonal (dev-set entropy exceeding public-corpus entropy throughout), a systematic gap rather than an isolated outlier. The largest single deviation is MNLI block 1 (low public entropy but high dev-set entropy), followed by SST-2 blocks 9 and 7; none of these three is selected by arg max l H(q(l)) for its task. Entropy-selected layers sit closer to the diagonal than collapsed, non-selected ones across all four tasks - e.g. SST-2’s selected block 1 ( H/ log E = 0 .972 

publicly) deviates far less than its own block 9. On SST-2, QNLI, and QQP, the public-corpus argmax also matches the dev-set ranking among top layers. On MNLI, blocks 3 and 7 are separated by only 0.008 

nats on Cpub (Tab. 7), a margin narrow enough that the dev-set ranking between these two layers may not agree with the public-corpus one; Tab. 1 shows the publicly-selected block 7 nonetheless yields the best downstream accuracy on MNLI. We read this as evidence that public-corpus entropy is a directionally reliable but imperfect proxy for private routing balance, consistent with Remark 2’s broader caution that entropy is a validated heuristic rather than a certified optimum. 31 Task Calibration source n Sparse layer Encoder block H(q)/log E ↑ log E −H(q) ↓ χ2(q∥u) ↓

MNLI ANLI R2 45,460 0 1 0.307 1.442 4.946 MNLI ANLI R2 45,460 1 3 0.674 0.677 1.580 MNLI ANLI R2 45,460 2 5 0.641 0.746 1.617 MNLI ANLI R2 45,460 3 7 0.682 0.660 1.262 

MNLI ANLI R2 45,460 4 9 0.643 0.743 1.356 MNLI ANLI R2 45,460 5 11 0.646 0.736 1.381 QNLI SQuAD context 87,599 0 1 0.761 0.498 1.257 QNLI SQuAD context 87,599 1 3 0.881 0.246 0.387 QNLI SQuAD context 87,599 2 5 0.873 0.264 0.505 QNLI SQuAD context 87,599 3 7 0.852 0.307 0.543 QNLI SQuAD context 87,599 4 9 0.807 0.401 0.851 QNLI SQuAD context 87,599 5 11 0.902 0.204 0.393 

QQP PAWS-Wiki 49,289 0 1 0.727 0.568 1.481 QQP PAWS-Wiki 49,289 1 3 0.814 0.388 1.008 QQP PAWS-Wiki 49,289 2 5 0.574 0.885 2.922 QQP PAWS-Wiki 49,289 3 7 0.498 1.044 3.379 QQP PAWS-Wiki 49,289 4 9 0.837 0.338 0.683 

QQP PAWS-Wiki 49,289 5 11 0.825 0.364 0.721 SST-2 IMDb unsup. 50,000 0 1 0.972 0.058 0.118 

SST-2 IMDb unsup. 50,000 1 3 0.828 0.358 0.876 SST-2 IMDb unsup. 50,000 2 5 0.767 0.485 0.972 SST-2 IMDb unsup. 50,000 3 7 0.876 0.259 0.405 SST-2 IMDb unsup. 50,000 4 9 0.304 1.447 4.774 SST-2 IMDb unsup. 50,000 5 11 0.661 0.706 1.763 

Table 7: Routing statistics for all six sparse encoder layers of S WITCH -BASE -8 on external public corpora (labels ignored; disjoint from GLUE splits and Dpriv ). H(q)/ log E (↑), entropy deficit log E − H(q) in nats ( ↓), and 

χ2(q∥u) (↓) for E = 8 experts. Bold : entropy-selected layer l⋆ per task. All selections are unambiguous; no privacy budget is consumed. 

32 Figure 5: Normalised routing entropy H(q(l))/ log E as a function of calibration examples n (log scale) for all six sparse encoder layers of S WITCH -BASE -8, across four tasks. Each line is one candidate layer; the entropy-selected layer at the full corpus size is marked in bold in Tab. 7. Entropy estimates stabilise by n ≈ 100 –500 for most layers; the main exception is MNLI block 1 (blue), which is heavily collapsed and grows slowly even at large n. The frozen router is applied to unlabelled inputs only; no private data are accessed. 

Figure 6: Public-calibration vs. GLUE-development normalised routing entropy H(q)/ log E for all 24 layer-task combinations (six sparse encoder layers × four tasks). Each point is labelled with its encoder block index. The dashed line is the diagonal y = x. Transfer is closest for QNLI; MNLI shows a systematic gap, with all six layers above the diagonal. The largest deviations are MNLI block 1 and SST-2 blocks 7 and 9, none of which is selected by the entropy criterion; entropy-selected layers sit closer to the diagonal than collapsed, non-selected ones. 

33 Figure 7: Public-calibration vs. GLUE-development normalised routing entropy H(q)/ log E for all 24 layer–task combinations (six sparse encoder layers × four tasks). Each point is labelled with its encoder block index. The dashed line is the diagonal y = x. Points near the diagonal indicate that entropy estimated on the public corpus transfers to the private training distribution. The two largest off-diagonal deviations (SST-2 blocks 7 and 9, blue circles) are collapsed layers not selected by the entropy criterion; the entropy-selected layer for each task lies near or above the diagonal. 

34 G Mechanism Diagnostics 

The structural failures diagnosed in Sec. 4 follow from the algebraic form of monolithic private opti-mization: a single clipping factor couples dense and sparse roles, the full-batch denominator dilutes expert updates, and fixed privacy noise interacts unfavorably with routing imbalance. These failures are properties of the mechanism definition and do not require empirical validation to exist. Empirical diagnostics are nevertheless useful for measuring how strongly they manifest under the actual gradient distribution, routing frequencies, and noise realizations of a trained model. Tab. 8 reports three such diagnostics at ε ≈ 8 across GLUE tasks. These results should be read as mechanism-level evidence that is directionally informative but not statistically powered: diagnostic runs were completed for a limited number of configurations per task, and the table is intended to corroborate the algebraic arguments of Sec. 4 rather than to constitute a statistically controlled comparison.                                            

> Task Method Mrole ↓SNR ↑SNR -CV ↓
> SST-2 Monolithic DP-Adam LoRA 0.320 1.08 ×10 −42.35
> Monolithic DP-Adam 0.591 6.38 ×10 −61.20
> RAPTOR 0.401 1.80 ×10 −51.69
> MNLI Monolithic DP-Adam LoRA 0.494 9.65 ×10 −53.54
> Monolithic DP-Adam 0.560 3.04 ×10 −51.24
> RAPTOR 0.337 2.25 ×10 −41.86
> QNLI Monolithic DP-Adam LoRA 0.723 2.64 ×10 −51.69
> Monolithic DP-Adam 0.619 9.58 ×10 −61.17
> RAPTOR 0.397 7.19 ×10 −51.62
> QQP Monolithic DP-Adam LoRA 0.701 5.98 ×10 −52.03
> Monolithic DP-Adam 0.573 1.81 ×10 −51.31
> RAPTOR 0.324 1.14 ×10 −42.53

Table 8: Mechanism diagnostics on S WITCH -BASE -8 at ε ≈ 8. Mrole : total variation distance between gradient-norm share and parameter-count share across roles (lower = more proportionate signal allocation). SNR : mean per-expert signal-to-noise ratio (higher = stronger expert update signal). SNR -CV: coefficient of variation of per-expert SNR (lower = more uniform update quality across experts). 

Role mismatch. For each top-level role r (shared parameters, router, classifier, and experts), let snorm 

> r

be the share of gradient norm assigned to that role and sparam  

> r

be its share of trainable parameters. We define 

Mrole = 12

X

> r

snorm  

> r

− sparam  

> r

. (37) Lower values indicate that gradient signal is more proportionate to the trainable parameter mass across roles. Across all tasks with available Monolithic DP-Adam diagnostics, RAPTOR substantially reduces Mrole relative to Monolithic DP-Adam: from 0.591 to 0.401 on SST-2, from 0.560 to 0.337 on MNLI, and from 0.619 to 0.397 on QNLI. Relative to Monolithic DP-Adam LoRA, role mismatch is reduced on MNLI, QNLI, and QQP, though not on SST-2. The role-aware decomposition therefore does not uniformly minimize this diagnostic, but it consistently avoids the pronounced role mismatch observed under full monolithic DP-Adam. 

Mean expert SNR. The column SNR measures the average per-expert signal-to-noise ratio, capturing the absolute strength of the expert update signal after accounting for privacy noise. On MNLI, QNLI, and QQP, RAPTOR achieves the largest mean expert SNR among the available configurations: 2.25 ×10 −4

on MNLI, 7.19 ×10 −5 on QNLI, and 1.14 ×10 −4 on QQP. On SST-2, Monolithic DP-Adam LoRA records the highest mean SNR, while our framework still improves over full Monolithic DP-Adam. This pattern is consistent with the intended effect of role-aware expert updates: the method is most beneficial when monolithic optimization gives experts weak or diluted signal, whereas simpler tasks such as SST-2 can already be served adequately by a strong LoRA baseline. 35 Expert SNR variation. The SNR-CV column reports the coefficient of variation of per-expert SNR; lower values indicate more uniform update quality across experts. RAPTOR reduces SNR-CV relative to Monolithic DP-Adam LoRA on SST-2, MNLI, and QNLI, but does not always achieve the lowest SNR-CV overall: full Monolithic DP-Adam has lower SNR-CV on SST-2, MNLI, and QNLI, and Monolithic DP-Adam LoRA has lower SNR-CV than our framework on QQP. This does not contradict the utility results: a low SNR-CV can arise from uniformly weak expert updates rather than uniformly useful ones. Full Monolithic DP-Adam exhibits low SNR-CV on SST-2 and QNLI precisely because its mean expert SNR is far lower than that of our framework. We therefore interpret SNR-CV jointly with 

SNR : role-aware training primarily improves absolute expert signal strength and role proportionality, while only partially reducing cross-expert SNR variation. 

Summary. Tab. 8 provides directional support for the mechanism account developed in Sec. 4. 

RAPTOR is not uniformly best on every diagnostic, and the limited number of completed runs means that the reported values should be treated as indicative rather than conclusive. Nevertheless, the method consistently reduces the large role mismatch of full monolithic DP-Adam and substantially increases mean expert SNR on the harder GLUE tasks. These trends are directionally consistent with the downstream accuracy gains in Tab. 1: the advantage of role-aware private training stems less from eliminating all expert imbalance and more from preventing expert updates from being suppressed or diluted by a dense monolithic private optimizer. 

Corpus-Level Routing Diagnostics Replacing the training batch Bexp  

> t

with the full calibration corpus 

Cpub (Sec. 5.1) gives corpus-level estimates dCV l and bHl, computed once from the frozen router before any private training begins. Tab. 9 reports these for the entropy-selected and most-collapsed layers across all four tasks. In every case the selected layer has lower dCV l and higher bHl than the collapsed layer, confirming that the two statistics are consistent and that high routing entropy coincides with more uniform load.                               

> Task Public corpus Layer (block) dCV lbHl/log E
> SST-2 IMDb unsup. selected (1) 0.344 0.972 collapsed (9) 2.185 0.304 MNLI ANLI R2 selected (7) 1.123 0.682 collapsed (1) 2.224 0.307 QNLI SQuAD context selected (11) 0.627 0.902 collapsed (1) 1.121 0.761 QQP PAWS-Wiki selected (9) 0.700 0.837 collapsed (7) 1.763 0.498

Table 9: Corpus-level routing diagnostics for the entropy-selected and most-collapsed sparse layers of S WITCH -

> BASE

-8, computed on external public calibration corpora with labels ignored (see Appendix F for corpus details). Higher dCV l and lower bHl indicate stronger routing imbalance; the two statistics are consistent across all tasks. Values are identical across private training methods because the router is frozen. 

36 H Budget Allocation: Surrogate Analysis and Empirical Sensitivity 

Surrogate analysis. The bias-variance decomposition of Theorem 1 shows that privacy noise enters the shared and expert streams through separate variance terms, V DP  

> s

∝ C2 

> s

/(ρε )2 and V DP  

> e

∝ E2C2 

> e

/((1 −

ρ)ε)2, whose relative magnitudes depend on the budget split ρ. To obtain a tractable criterion for choosing 

ρ, we weight these terms by the downstream sensitivity of the task loss to each stream. 

Proposition 1 (Budget allocation under shared-dominant routing) . Under balanced routing ( qe = 1 /E ), define the single-step surrogate 

L(ρ) = λsC2

> s

ρ2 + λeE2C2

> e

(1 − ρ)2 , ρ ∈ (0 , 1) , (38) 

where λs, λ e > 0 weight the task loss sensitivity to shared- and expert-stream perturbations. The unique minimizer is 

ρ∗ = 11 + 

 λeE2C2

> e

λsC2

> s

1/3 . (39) 

Proof. Setting A = λsC2 

> s

and Bc = λeE2C2 

> e

, differentiating L(ρ) = A/ρ 2 + Bc/(1 − ρ)2 and equating to zero gives ((1 − ρ)/ρ )3 = Bc/A , yielding Eq. (39). Strict convexity ( d2L/dρ 2 = 6 A/ρ 4 + 6 Bc/(1 −

ρ)4 > 0) confirms uniqueness. 

Interpretation. Eq. (39) gives two qualitative directions. First, ρ∗ increases with λs/λ e: higher shared-stream sensitivity concentrates the budget on the shared stream. Second, ρ∗ decreases with E: the E2

amplification of expert-stream noise makes expert-side budget more valuable per unit of noise reduction under the surrogate, pulling ρ∗ toward 0 as E grows. The shared-dominant regime ρ∗ → 1 we adopt therefore does not come from E - it comes from the sensitivity asymmetry: the empirical estimate 

λe/λ s ≲ 6.3 × 10 −3 (Tab. 10) shows the task loss is far more sensitive to shared-stream perturbations, which must outweigh the E2 factor for ρ∗ to sit near 1. Proposition 1 provides qualitative guidance, not a certified value of ρ∗: the sensitivity ratio λe/λ s required to formally conclude ρ∗ ≥ 0.9 (Eq. (41)) is stringent and, per Tab. 10, does not hold for this model. We therefore treat ρ = 0 .9 as an empirically validated shared-dominant choice (Sec. 8), not a certified optimum. 

Empirical sensitivity estimate. For each role r ∈ { shared , experts }, we inject Gaussian noise ξr ∼

N(0 , σ 2 

> r

I) matched to the DP noise magnitude at ε ≈ 8, apply it to θr, and record ∆Lval . We define the one-sided sensitivity estimate 

λ+ = max(∆ Lval , 0) 

E∥ξr∥22

, (40) flooring non-positive values at zero: a negative or near-zero ∆Lval indicates that the role lies below the measurement noise floor.                       

> Role Params ∆Lval λ+
> Shared 9,438 ,722 2.175 ×10 −42.305 ×10 −5
> Experts 7,864 ,320 −1.361 ×10 −60
> Table 10: Noise-injection sensitivity on SST-2 at ε≈8.λ+floors non-positive ∆Lval at zero (Eq. (40)). A non-positive expert value indicates sensitivity below the measurement noise floor.

Tab. 10 shows clear shared-stream sensitivity ( λs = 2 .305 × 10 −5) and negligible expert sensitivity. Using the magnitude of the signed expert estimate as a conservative upper bound, 

λe

λs

≲ 1.361 × 10 −6

2.175 × 10 −4 ≈ 6.3 × 10 −3. (41) For E = 8 and Cs = Ce, Eq. (41) requires λe/λ s ≤ 2.1 × 10 −5. The empirical bound exceeds this threshold, so Proposition 1 does not formally certify ρ∗ = 0 .9 for this model. We therefore treat ρ = 0 .9

37 as a shared-dominant initialization: motivated by the surrogate and consistent with the near-zero measured expert sensitivity, but validated primarily by the empirical sweep in Sec. E.3. 38 I Runtime and Memory 

Tab. 11 reports implementation-level runtime and memory for fine-tuning S WITCH -BASE -8 on SST-2 at 

ε ≈ 8, comparing RAPTOR against the two Monolithic DP baselines from Tab. 1. We include this because Role-Aware changes training scope as well as the DP mechanism, so accuracy gains alone don’t show whether they come at a compute cost. Global DP-Adam trains the full model ( 307 .85 M params, 

100% ); Global DP-Adam LoRA trains only adapters ( 14 .16 M, 4.60% ); our framework trains the shared stream plus one expert layer ( 17 .30 M, 5.62% ) - comparable in scope to LoRA, ∼ 18 × smaller than the full model. Our framework’s gains over Monolithic DP-Adam (Tab. 1) are thus not explained by training more of the model. Peak memory is lowest for RAPTOR (112 .16 GB), below both LoRA ( 130 .74 GB) and the full model ( 165 .51 GB), plausibly because expert-stream clipping operates on owner subsets Bt,l,e rather than the full batch at once. Per-step time and total GPU-hours are highest for the full model ( 7.23 s, 2.61 

GPU-hrs) and lowest for LoRA ( 4.07 s, 1.47 ); our framework sits close to LoRA ( 4.51 s, 1.65 GPU-hrs) despite the extra forward pass for the residual weight wi and the alternating schedule (Sec. 5.4) - about 

37% cheaper in total compute than the full-model baseline, at higher accuracy.                     

> Method Trainable params % params Peak GB Sec./step GPU-hours Global DP-Adam LoRA 14 .16 M4.60% 130 .74 4.07 1.47
> Global DP-Adam 307 .85 M100 .00% 165 .51 7.23 2.61
> RAPTOR 17 .30 M5.62% 112 .16 4.51 1.65

Table 11: Implementation footprint of fine-tuning S WITCH -BASE -8 on SST-2 at ε ≈ 8. Raw implementation-level costs, not a controlled same-scope benchmark—scope differs by design. Percentages relative to the full trainable SWITCH -BASE -8 model used by Global DP-Adam. Bold: lowest peak memory. 

39