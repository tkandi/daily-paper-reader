---
title: "RegionFed: Federated Learning for Personalized Query Understanding in Heterogeneous Retail Environments"
title_zh: RegionFed：面向异构零售环境中个性化查询理解的联邦学习
authors: "Quoc H. Nguyen, Ali Lafzi, Abhijeet Phatak, Siddharth Pratap Singh, Rohit Upadhyay, Yogananda Domlur Seetharama, Chittaranjan Tripathy"
date: 2026-09-04
pdf: "https://arxiv.org/pdf/2609.05403v1"
tags: ["query:mlsys"]
score: 7.0
evidence: 面向异构变压器训练的架构鲁棒联邦学习框架
tldr: "零售搜索面向不同地区时，查询词汇、搜索模式与商品偏好差异显著，联邦学习又难以兼顾隐私和个性化。RegionFed完全在梯度层面工作，以区域梯度与全局梯度的二范数冲突统一诊断异质性、选择个性化策略并调节强度。该方法适配T5、RoBERTa和CNN，在三类数据集上稳定提升，RegionFed-Meta达到92.27%，并以约0.60的差分隐私预算接近集中式上界。其贡献在于避免现代Transformer参数级个性化崩溃，实现架构鲁棒的区域化联邦训练。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.05403v1/fig-001.webp\", \"caption\": \"\", \"page\": 17, \"index\": 1, \"width\": 3928, \"height\": 1330}]"
motivation: 零售区域间数据高度异质，标准联邦模型牺牲区域性能，而参数级个性化方法在绑定嵌入和层归一化影响下会导致Transformer性能崩溃。
method: 利用区域与全局梯度的二范数冲突识别异质性，按需路由个性化策略并自适应控制强度，模型仅作为可微黑盒处理。
result: "在Amazon ESCI、Amazon Reviews和LEAF-FEMNIST及四种架构上验证，RegionFed-Meta达到92.27%，差分隐私预算约为0.60。"
conclusion: 梯度级设计无需改动模型代码即可跨架构部署，显著提升Transformer和CNN的区域性能，并具备隐私保护与O(1/√T)收敛保证。
---

## 摘要
零售搜索系统服务于具有不同查询模式、词汇和产品偏好的多样化地理区域，由此产生显著的数据异质性，对隐私保护训练和模型个性化都提出了挑战。联邦学习为隐私保护提供了自然的解决方案，但标准联邦学习方法生成的全局模型会牺牲区域性能；而现有的个性化联邦学习方法在参数层面运行，在现代 Transformer 上会因嵌入权重共享和 LayerNorm 交互而发生灾难性崩溃（在 T5 上准确率低于 10\%）。我们提出 RegionFed，一种具有\textit{架构鲁棒性}的联邦学习框架，通过完全在梯度层面运行来规避这一失效问题。RegionFed 使用区域梯度与全局梯度之间的 $\ell_2$ 冲突作为统一信号，用于：（i）诊断异质性；（ii）将各区域路由至成本最低且足够有效的个性化策略；（iii）自适应地控制个性化强度。由于将模型视为可微黑盒，RegionFed 无需修改代码即可部署于 T5-Small、T5-3B、RoBERTa 和 CNN，在参数级方法失效的 Transformer 上取得显著提升，并在 CNN 上持续带来改进。在三个公开数据集（Amazon ESCI、Amazon Reviews 和 LEAF-FEMNIST）及四种架构上，RegionFed-Meta 达到 92.27\% 的性能，缩小了与侵犯隐私的集中式上界之间的差距（集中式 + 区域加权：92.04\%，$\Delta$=0.23 个百分点，在 1$\sigma$ 范围内），同时提供 $(\varepsilon{\approx}0.60)$-差分隐私和 $\mathcal{O}(1/\sqrt{T})$ 收敛性。

## Abstract
Retail search systems serve diverse geographic regions with distinct query patterns, vocabularies, and product preferences, creating significant data heterogeneity that challenges both privacy-preserving training and model personalization. Federated learning offers a natural solution for privacy, but standard FL methods produce global models that sacrifice regional performance, while existing personalized FL approaches operate at the parameter level and catastrophically collapse on modern transformers (below 10\% accuracy on T5) due to tied embeddings and LayerNorm interactions. We introduce RegionFed, an \textit{architecture-robust} federated learning framework that sidesteps this failure by operating entirely at the gradient level. RegionFed uses the $\ell_2$ conflict between regional and global gradients as a unified signal that (i) diagnoses heterogeneity, (ii) routes each region to the cheapest sufficient personalization strategy, and (iii) adaptively controls personalization strength. Because it treats models as differentiable black boxes, RegionFed deploys on T5-Small, T5-3B, RoBERTa, and CNN with zero code changes, providing large gains on transformers (where parameter-level methods collapse) and consistent improvements on CNNs. Across three public datasets (Amazon ESCI, Amazon Reviews, LEAF-FEMNIST) and four architectures, RegionFed-Meta achieves 92.27\%, closing the gap to the privacy-violating centralized upper bound (Centralized + Regional Weighting: 92.04\%, $Δ$=0.23pp, within 1$σ$) while providing $(ε{\approx}0.60)$-differential privacy and $\mathcal{O}(1/\sqrt{T})$ convergence.