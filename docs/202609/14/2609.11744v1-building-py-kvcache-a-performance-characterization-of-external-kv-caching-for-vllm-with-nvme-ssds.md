---
title: "Building py-kvcache: A Performance Characterization of External KV Caching for vLLM with NVMe SSDs"
title_zh: 构建 py-kvcache：使用 NVMe SSD 对 vLLM 外部 KV 缓存的性能表征
authors: "Joseph Kanichai, Tiziano De Matteis, Animesh Trivedi"
date: 2026-09-10
pdf: "https://arxiv.org/pdf/2609.11744v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 跨GPU、CPU与NVMe的vLLM外部KV缓存基础设施
tldr: 论文系统刻画了 vLLM 在 GPU、CPU 与 NVMe SSD 多级缓存下复用 KV 状态的性能权衡：短前缀或快 GPU 上，读取外部缓存可能不如重算。作者提出 py-kvcache，通过异步直接 I/O、有界共享暂存区和调度器感知预加载，让磁盘读取与计算重叠。80k token 时磁盘加载较 LMCache 快 2.0 倍，综合缓存快 1.23 倍，并接近原生实现。结果表明，外部 KV 缓存收益取决于传输粒度、内存开销、调度时机和具体硬件，应按场景决定是否启用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.11744v1/fig-001.webp\", \"caption\": \"\", \"page\": 9, \"index\": 1, \"width\": 1331, \"height\": 592}, {\"url\": \"assets/figures/arxiv/2609.11744v1/fig-002.webp\", \"caption\": \"\", \"page\": 12, \"index\": 2, \"width\": 583, \"height\": 704}, {\"url\": \"assets/figures/arxiv/2609.11744v1/fig-003.webp\", \"caption\": \"\", \"page\": 12, \"index\": 3, \"width\": 582, \"height\": 704}, {\"url\": \"assets/figures/arxiv/2609.11744v1/fig-004.webp\", \"caption\": \"\", \"page\": 12, \"index\": 4, \"width\": 583, \"height\": 704}]"
motivation: 现有分析常以设备带宽解释缓存收益，但短前缀、快 GPU 与不同调度时机可能使外部加载反而慢于重算。
method: 实现 vLLM KV Offload connector，结合异步直接 I/O、有界共享暂存和请求等待期预加载，覆盖 GPU、CPU、NVMe 多级缓存。
result: 80k token 下磁盘加载较 LMCache 快 2.0 倍；启用三级缓存快 1.23 倍，LongBench 与 SCBench 也验证了收益。
conclusion: 缓存效果高度依赖工作负载与硬件：弱 GPU 可降低 TTFT，而 H100 上常无需外部缓存，应进行场景化准入决策。
---

## 摘要
前缀缓存通过复用先前计算得到的键值（KV）状态，可以缩短长上下文大语言模型（LLM）请求的首个 token 生成时间（TTFT）；但对于较短的前缀或速度较快的 GPU，从外部缓存加载数据可能不如重新计算快。我们在 vLLM 中使用合成工作负载、长上下文基准测试和生产环境追踪数据，刻画了 GPU、CPU 与 NVMe 层级之间的这一权衡，并发现缓存性能不仅取决于设备带宽，还取决于传输粒度、中间内存使用情况以及传输何时进入请求调度。基于这些发现，我们设计并实现了 py-kvcache——一种 vLLM KV Offload 连接器，具备异步直接 I/O、有界共享暂存区和感知调度器的预加载机制；该机制能够在请求仍处于等待状态时启动磁盘读取，并与计算过程重叠。在 80k tokens 的场景下，py-kvcache 从磁盘加载的速度比 LMCache 快 2.0 倍，其中预加载贡献了 1.34 倍的加速。在启用 GPU、CPU 和磁盘缓存时，py-kvcache 比 LMCache 快 1.23 倍，并且与 vLLM 原生 KV Offload 实现的性能差距约为 4% 以内。LongBench 和 SCBench 的结果表明，这些收益能够扩展到不规则前缀链和多轮工作负载。Bailian 追踪数据重放在较弱的 GPU 上能够改善 TTFT；但在 H100 上，平均请求的性能低于盈亏平衡点，仅依靠 GPU 内存就足以保留足够的前缀。因此，外部 KV 缓存应被视为一项针对具体部署配置的启用决策。py-kvcache 的实现见：https://github.com/atlarge-research/py-kvcache。

## Abstract
Prefix caching can reduce the time to first token (TTFT) of long-context LLM requests by reusing previously computed key-value (KV) states, but for short prefixes or fast GPUs, recomputation can be faster than loading from an external cache. We characterize this tradeoff in vLLM across GPU, CPU, and NVMe tiers using synthetic workloads, long-context benchmarks, production traces, and find that cache performance depends on transfer granularity, intermediate memory use, and when transfers enter the request schedule, not only on device bandwidth. These findings motivate py-kvcache, a vLLM KV Offload connector with asynchronous direct I/O, bounded shared staging, and scheduler-aware preloading, which starts disk reads while requests are still waiting, overlapping with compute. At 80k tokens, py-kvcache loading from disk is 2.0x faster than LMCache, with preloading contributing 1.34x. With GPU, CPU, and disk caching enabled, it is 1.23x faster than LMCache and within approximately 4% of the native vLLM KV Offload implementation. LongBench and SCBench show that these benefits extend to irregular prefix chains and multi-turn workloads. Bailian trace replays improve TTFT on a weaker GPU, but on an H100 the average request falls below the break-even point and GPU memory alone retains enough prefixes. External KV caching should therefore be treated as a setup specific admission decision. The py-kvcacheimplementation is available at: https://github.com/atlarge-research/py-kvcache.