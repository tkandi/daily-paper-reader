---
title: Fast Gauss Sums via Flash Attention
title_zh: 通过 Flash Attention 快速计算高斯和
authors: "Nicolaj Rux, Sebastian Neumayer"
date: 2026-09-04
pdf: "https://arxiv.org/pdf/2609.04910v1"
tags: ["query:mlsys"]
score: 6.0
evidence: 利用闪存注意力实现硬件感知的图形处理器加速
tldr: 高斯核求和是最大均值差异、核梯度流和斯坦变分梯度下降等方法的计算瓶颈，而注意力算子已获得高度优化的硬件实现。论文通过两项小型输入增广，将归一化软最大注意力的归约转换为带任意符号权重的非归一化高斯核和。无需编写定制图形处理器代码，即可直接调用闪存注意力实现。在半精度、特征维度大于8时，其速度、内存开销和精度通常优于编译后的PyTorch与PyKeOps，且内存保持线性扩展。
source: arxiv
selection_source: fresh_fetch
motivation: 核方法广泛依赖高斯核求和，但其计算代价高；与此同时，软最大注意力已拥有极成熟的硬件感知优化实现。
method: 对输入进行两项轻量增广，利用注意力归一化归约重构分子与分母，从而计算任意带符号权重的非归一化高斯核和。
result: 在半精度且特征维度大于8时，该方法在速度、内存额外开销与数值精度上常优于编译PyTorch和PyKeOps。
conclusion: 闪存注意力可直接复用于高斯核计算，无需定制图形处理器内核，并在保持线性内存扩展的同时提升性能。
---

## 摘要
高斯核和是最大均值差异（MMD）、核梯度流、Stein 变分梯度下降（SVGD）以及许多其他核方法的计算核心。与此同时，softmax 注意力机制获得了大量面向硬件的代码工程优化，最终发展出 Flash Attention。我们表明，具有任意带符号权重的高斯核和可以通过 Flash Attention 进行计算：只需对输入进行两处小幅扩增，便可将归一化的 softmax 归约转换为未归一化的高斯和，而无需编写任何自定义 GPU 代码。对于 fp16 精度下特征维度 D>8 的情形，该方法在速度、内存开销和准确性方面均优于编译后的 PyTorch 代码以及 PyKeOps 内核（通常优势显著）。此外，其内存复杂度仍保持线性增长。

## Abstract
Gaussian kernel sums are the computational core of maximum mean discrepancies (MMDs), kernel gradient flows, Stein variational gradient descent (SVGD), and many other kernel methods. At the same time, softmax attention has received an extraordinary amount of hardware-aware code engineering, culminating in flash attention. We show that Gauss kernel sums with arbitrary, signed weights can be evaluated via flash attention: two small input augmentations turn the normalized softmax reduction into the unnormalized Gauss sum, without writing a single line of custom GPU code. For feature dimension D>8 in fp16, this approach beats compiled PyTorch code as well as PyKeOps kernels (often significantly) in speed, memory-overhead and accuracy. Indeed, its memory scaling remains linear.