---
title: "FleetOpt: Analytical Fleet Provisioning for LLM Inference with Compress-and-Route as Implementation Mechanism"
title_zh: FleetOpt：以“压缩与路由”为实现机制的大语言模型推理分析化集群配置
authors: "Huamin Chen, Xunzhuo Liu, Yuhan Liu, Junchen Jiang, Bowei He, Xue Liu"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16514v1"
tags: ["query:mlsys"]
score: 9.0
evidence: LLM推理的分析型集群配置与GPU容量优化
tldr: "针对LLM推理中GPU集群常按最差情况配置导致KV缓存浪费的问题，本文提出FleetOpt框架。该框架基于M/G/c排队论模型，通过分析工作负载分布和SLO目标，推导出由长短上下文池组成的双池最优配置。为解决边界处的成本突变，引入“压缩并路由”机制，利用抽取式压缩将边缘请求修剪至边界以下。实验表明，该方案在生产负载下可降低6-82%的GPU成本，且模型预测误差极低。"
selection_source: fresh_fetch
motivation: 现代LLM集群通常为极少数的长上下文请求预留过多资源，导致大量GPU容量因闲置的KV缓存位而被浪费。
method: 提出一种基于排队论的分析框架，结合“压缩并路由”机制，通过抽取式压缩优化请求分配并平滑硬件边界成本。
result: "在三种生产追踪数据上，该框架相比同构集群可降低6%至82%的GPU总成本，且利用率预测误差小于3%。"
conclusion: FleetOpt证明了通过数学建模与软件层压缩相结合，可以显著提升LLM推理集群的资源利用率并大幅降低运营成本。
---

## 摘要
现代大语言模型（LLM）GPU 集群通常针对绝大多数请求从未达到的最坏情况上下文长度进行配置，导致 GPU 容量在空闲的 KV 缓存槽位上被浪费。我们提出了 FleetOpt，这是一个从第一性原理出发的框架：给定工作负载的提示词长度累积分布函数（CDF）和 P99 首字延迟（TTFT）目标，通过分析推导出成本最低的集群配置，并将其应用于实际部署。其分析核心将每个资源池建模为 M/G/c 排队模型，并推导出最低成本集群采用双池架构——即短上下文池和长上下文池——且存在一个最优边界 B*，使得两个池之间的边际 GPU 成本相等。实现 B* 的根本障碍是“成本悬崖”：在硬路由步骤中，略高于 B* 的请求所消耗的 GPU 容量是略低于 B* 请求的 8 到 42 倍（取决于上下文窗口比例），这在结构上阻碍了边界的降低。“压缩与路由”（C&R）是解决这一障碍的实现机制。网关层的抽取式压缩在推理引擎接收请求之前，将处于边界附近的请求修剪至 B* 以下，从而将硬性的硬件边界转化为从工作负载 CDF 中读取的软件参数。这两个组件在 FleetOpt 离线规划器中得到统一：给定 CDF 和服务水平目标（SLO），它能在 1 毫秒内返回最优参数 (n_s*, n_l*, B*, gamma*)。在三个生产环境追踪数据上，该组合框架相比同构集群可降低 6% 至 82% 的总 GPU 成本，其中 C&R 根据工作负载类型的不同，在普通池路由的基础上额外贡献了 1 到 44 个百分点的降幅。该分析模型通过离散事件模拟器（inference-fleet-sim）进行了验证，在所有资源池和工作负载中，预测 GPU 利用率的误差均小于等于 3%。

## Abstract
Modern LLM GPU fleets are provisioned for worst-case context lengths that the vast majority of requests never approach, wasting GPU capacity on idle KV-cache slots. We present FleetOpt, a framework that starts from first principles: given a workload's prompt-length CDF and a P99 TTFT target, derive the minimum-cost fleet analytically, then deploy it in practice.   The analytical core models each pool as an M/G/c queue and derives that the minimum-cost fleet is a two-pool architecture -- a short-context pool and a long-context pool -- with an optimal boundary B* satisfying an equal marginal GPU cost condition across both pools. The fundamental barrier to achieving B* is the cost cliff: a hard routing step where requests just above B* consume 8x--42x more GPU capacity than requests just below it (depending on the context window ratio), creating a structural disincentive to lower the boundary.   Compress-and-Route (C&R) is the implementation mechanism that resolves this barrier. Gateway-layer extractive compression trims borderline requests below B* before the engine ever sees them, converting the hard hardware boundary into a software parameter read from the workload CDF. The two components are unified in the FleetOpt offline planner: given a CDF and SLO, it returns the optimal (n_s*, n_l*, B*, gamma*) in under 1 ms.   On three production traces, the combined framework reduces total GPU cost by 6--82% versus a homogeneous fleet, with C&R contributing 1--44 percentage points beyond plain pool routing depending on workload archetype. The analytical model is validated against a discrete-event simulator (inference-fleet-sim) with <= 3% error on predicted GPU utilization across all pools and workloads.

---

## 论文详细总结（自动生成）

### FleetOpt 论文深度总结

#### 1. 核心问题与整体含义
*   **研究背景**：现代大语言模型（LLM）支持极长的上下文（如 128K 标记），但生产环境中的绝大多数请求（约 80%）其实很短（< 2K）。
*   **核心矛盾**：为了兼容极少数的长请求，GPU 集群通常按最坏情况配置 KV 缓存。这导致了**“成本悬崖”（Cost Cliff）**现象：一个请求如果只比短文本阈值多出一个标记，就必须进入长文本池，从而消耗 8 到 42 倍的 GPU 资源。
*   **研究目的**：提出 FleetOpt 框架，通过数学建模和“压缩与路由”机制，在满足服务水平目标（SLO）的前提下，最小化 GPU 集群的配置成本。

#### 2. 方法论
*   **核心思想**：将集群配置从“经验驱动”转为“分析驱动”。首先通过排队论推导最优配置，再通过软件层面的压缩技术消除硬件边界带来的成本突变。
*   **关键技术细节**：
    *   **M/G/c 排队模型**：将 GPU 集群建模为多个资源池，利用 Erlang-C 公式和 Kimura 近似法计算 P99 等待时间，确保满足首字延迟（TTFT）的 SLO。
    *   **最优边界推导**：推导出“等边际 GPU 成本条件”，即当两个资源池的边际成本相等时，集群总成本最低。
    *   **压缩与路由（C&R）机制**：在网关层引入**抽取式压缩**。对于处于边界附近的“边缘请求”，在进入引擎前将其压缩至短文本池的阈值以下。
    *   **FleetOpt 离线规划器**：一个快速算法，输入工作负载的长度分布（CDF）、到达率和 SLO，在 1 毫秒内输出最优的 GPU 数量、资源池边界和压缩比例。
*   **算法流程**：规划器遍历候选边界和压缩比，校准各池的服务率，通过 Erlang-C 逆运算确定所需的 GPU 数量，最终选择总成本最低的组合。

#### 3. 实验设计
*   **数据集/场景**：
    *   **Azure LLM Trace (2023)**：代表典型的生产环境对话负载。
    *   **LMSYS-Chat-1M**：多轮对话负载。
    *   **Agent-heavy**：合成负载，模拟包含 RAG、工具调用和代码任务的复杂智能体场景。
*   **Benchmark（对比方法）**：
    *   **Homogeneous**：传统的同构集群（统一按长上下文配置）。
    *   **Pool Routing (PR)**：简单的长短池路由（无压缩）。
    *   **PR + C&R (Retrofit)**：在现有路由基础上后期加装压缩。
    *   **FleetOpt (Co-design)**：本文提出的协同设计优化方案。
*   **验证方式**：使用离散事件模拟器 `inference-fleet-sim` 验证分析模型的准确性。

#### 4. 资源与算力
*   **硬件配置**：实验基于 **NVIDIA A100-80GB GPU** 进行校准。
*   **模型参数**：以 **Llama-3-70B (fp16)** 为基准，KV 缓存每标记占用 320 KB。
*   **算力消耗**：论文主要关注推理集群的配置优化，未涉及模型训练。压缩组件运行在 CPU（Intel Xeon Platinum 8568Y+）上，单次压缩延迟仅为 2-7 毫秒。

#### 5. 实验数量与充分性
*   **实验规模**：针对三种不同特征的工作负载（集中型、分散型、长文本集中型）进行了详尽测试。
*   **充分性**：
    *   进行了**到达率敏感性分析**（从 100 到 2000 req/s）。
    *   对压缩质量进行了**保真度研究**（使用 BERTScore、ROUGE-L 等指标测试了 300 个样本）。
    *   通过模拟器验证了分析模型，误差控制在 3% 以内。
*   **客观性**：实验涵盖了代码排除（Code Exclusion）等实际限制，确保了结果的真实性。

#### 6. 主要结论与发现
*   **成本大幅降低**：相比同构集群，FleetOpt 可降低 **6% 至 82%** 的 GPU 成本。
*   **压缩的价值**：在 Azure 负载下，C&R 机制在普通路由基础上额外贡献了 43.7% 的成本降幅。
*   **协同设计优势**：证明了“先规划再部署”的协同设计优于“先部署再加装压缩”的方案。
*   **模型准确性**：排队论模型在多服务器环境下非常精确，能够有效指导实际的容量规划。

#### 7. 优点
*   **理论与实践结合**：从第一性原理出发，将复杂的排队论转化为实用的集群规划工具。
*   **解决成本悬崖**：敏锐捕捉到硬件配置中的不连续性，并用软件手段（压缩）平滑了这一成本突变。
*   **极低开销**：规划器运行极快（<1ms），压缩延迟对用户几乎不可见（平均增加 <0.5ms）。

#### 8. 不足与局限
*   **内容局限性**：抽取式压缩不适用于代码等对结构极其敏感的内容（文中已通过分类器规避，但缩小了适用范围）。
*   **分布依赖性**：规划依赖于准确的工作负载 CDF，如果流量特征发生剧烈且不可预测的波动，可能需要频繁重新规划。
*   **语义损失风险**：尽管保真度指标较高，但抽取式压缩仍存在丢失细微语义信息的风险，可能影响极少数复杂任务的推理质量。

（完）
