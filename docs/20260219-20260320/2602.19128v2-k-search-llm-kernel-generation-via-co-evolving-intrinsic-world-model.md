---
title: "K-Search: LLM Kernel Generation via Co-Evolving Intrinsic World Model"
title_zh: K-Search：通过协同演化内在世界模型实现 LLM 算子生成
authors: "Shiyi Cao, Ziming Mao, Joseph E. Gonzalez, Ion Stoica"
date: 2026-02-22
pdf: "https://arxiv.org/pdf/2602.19128v2"
tags: ["query:mlsys"]
score: 9.0
evidence: 针对机器学习系统优化GPU算子内核
tldr: 针对GPU算子优化中LLM缺乏规划能力且易受中间实现错误干扰的问题，本文提出K-Search框架。该框架引入协同演化的内在世界模型，将高层算法规划与底层代码实例化解耦，利用LLM的领域知识引导搜索。在FlashInfer和GPUMode等复杂算子任务中，K-Search显著优于现有进化搜索方法，甚至超越了人类设计的方案，展现了强大的非单调优化路径探索能力。
selection_source: fresh_fetch
motivation: 现有的自动化算子优化方法缺乏显式规划能力，且容易因中间代码实现缺陷而丢弃具有潜力的优化策略。
method: 提出基于协同演化世界模型的搜索框架，通过解耦高层规划与底层实现，利用LLM的先验知识动态引导优化空间探索。
result: 在MoE等复杂算子上实现最高14.3倍的性能提升，并在H100 TriMul任务中刷新了SOTA记录，超越了人类专家设计。
conclusion: K-Search证明了通过引入世界模型和解耦规划，LLM能够有效解决复杂的GPU算子自动优化难题。
---

## 摘要
优化 GPU 算子对于高效的现代机器学习系统至关重要，但由于设计因素之间复杂的相互作用以及硬件的快速更迭，这仍然是一项挑战。现有的自动化方法通常仅将大语言模型（LLMs）视为启发式引导演化循环中的随机代码生成器。由于缺乏显式的规划能力，且经常因中间实现效率低下或错误而放弃具有潜力的策略，这些方法在处理需要协调的多步结构变换的复杂算子时往往难以奏效。为了解决这一问题，我们提出了“通过协同演化世界模型进行搜索”的方法，并基于此构建了 K-Search。通过使用协同演化的世界模型取代静态搜索启发式算法，我们的框架利用 LLM 的先验领域知识来引导搜索，主动探索优化空间。该方法将高层算法规划与底层程序实例化显式解耦，使系统能够探索非单调的优化路径，同时对临时的实现缺陷保持韧性。我们在来自 FlashInfer 的多种复杂算子（包括 GQA、MLA 和 MoE 算子）上对 K-Search 进行了评估。结果显示，K-Search 显著优于最先进的演化搜索方法，在复杂 MoE 算子上实现了平均 2.10 倍的提升，最高增益达 14.3 倍。在 GPUMode TriMul 任务中，K-Search 在 H100 上达到了最先进的性能，耗时 1030us，超越了先前的演化方法和人工设计的解决方案。

## Abstract
Optimizing GPU kernels is critical for efficient modern machine learning systems yet remains challenging due to the complex interplay of design factors and rapid hardware evolution. Existing automated approaches typically treat Large Language Models (LLMs) merely as stochastic code generators within heuristic-guided evolutionary loops. These methods often struggle with complex kernels requiring coordinated, multi-step structural transformations, as they lack explicit planning capabilities and frequently discard promising strategies due to inefficient or incorrect intermediate implementations. To address this, we propose Search via Co-Evolving World Model and build K-Search based on this method. By replacing static search heuristics with a co-evolving world model, our framework leverages LLMs' prior domain knowledge to guide the search, actively exploring the optimization space. This approach explicitly decouples high-level algorithmic planning from low-level program instantiation, enabling the system to navigate non-monotonic optimization paths while remaining resilient to temporary implementation defects. We evaluate K-Search on diverse, complex kernels from FlashInfer, including GQA, MLA, and MoE kernels. Our results show that K-Search significantly outperforms state-of-the-art evolutionary search methods, achieving an average 2.10x improvement and up to a 14.3x gain on complex MoE kernels. On the GPUMode TriMul task, K-Search achieves state-of-the-art performance on H100, reaching 1030us and surpassing both prior evolution and human-designed solutions.

---

## 论文详细总结（自动生成）

### K-Search: 基于协同演化内在世界模型的 LLM 算子生成

#### 1. 核心问题与整体含义（研究动机和背景）
高效的 GPU 算子（Kernel）是现代机器学习系统的基石，但其优化面临三大挑战：
*   **设计空间巨大**：涉及分块（Tiling）、内存布局、同步及架构特定原语的复杂权衡。
*   **硬件更迭迅速**：从 Hopper 到 Blackwell，硬件特性的变化使旧优化策略失效。
*   **现有方法局限**：目前的 LLM 自动优化方法（如 OpenEvolve）通常将 LLM 视为随机代码生成器，嵌入在启发式演化循环中。这种方式缺乏显式规划，且容易因为中间实现过程中的临时编译错误或性能回退（非单调路径）而过早放弃正确的优化方向。

**K-Search 的核心动机**是将“高层算法规划”与“底层代码实现”解耦，利用 LLM 作为“世界模型”来引导搜索，从而能够处理需要多步协同变换的复杂优化任务。

#### 2. 方法论：核心思想与算法流程
K-Search 提出了**基于协同演化世界模型的搜索（Search via Co-Evolving World Model）**框架：
*   **核心思想**：将算子生成建模为结构化搜索树上的规划问题。LLM 不仅生成代码，还负责维护搜索前沿、估计优化意图的优先级，并根据执行反馈动态调整对优化空间的认知。
*   **关键技术细节**：
    *   **搜索状态（Search State）**：维护一棵树，包含“已关闭节点”（已实现的程序）和“前沿节点”（待探索的优化意图，如“通过填充解决银行冲突”）。
    *   **优先级评分 $V(a|S_t)$**：世界模型为每个意图分配 0 到 1 之间的分数，代表其潜力。
*   **算法流程（三个阶段迭代）**：
    1.  **动作选择（Action Selection）**：从前沿节点中选择优先级评分最高的优化意图。
    2.  **局部细化（Local Refinement）**：针对选定意图，利用随机策略 $\pi_{code}$ 多次尝试实例化代码，直到达到停滞限制（Stagnation Limit $K$），确保不会因偶然的语法错误丢弃好点子。
    3.  **世界模型演化（World Model Update）**：根据执行结果（正确性、延迟、元数据），通过上下文学习（In-context Learning）更新搜索树，包括**插入**新意图、**更新**现有节点评分、**剪枝**无效分支。

#### 3. 实验设计
*   **实验场景**：针对 FlashInfer 库中的高性能算子，包括：
    *   **GQA Paged Decode**（分组查询注意力解码）
    *   **MLA Paged Prefill/Decode**（多级注意力预填充/解码，DeepSeek-V3 核心算子）
    *   **FP8 MoE**（混合专家模型算子，针对 Blackwell 架构）
*   **Benchmark**：以人类专家高度优化的 **FlashInfer** 算子作为性能基准（Score = 100 代表达到专家水平）。
*   **对比方法**：
    *   **OpenEvolve**：基于质量-多样性搜索（MAP-Elites）的演化方法。
    *   **ShinkaEvolve**：结合性能驱动选择与新颖性拒绝的演化框架。
*   **竞赛任务**：参加了 **GPUMode TriMul** 挑战赛（AlphaFold3 核心模块）。

#### 4. 资源与算力
*   **硬件环境**：NVIDIA **H100** (80GB HBM3) 和 **B200** GPU。
*   **软件栈**：CUDA 12.8, FlashInfer 0.5.3, PyTorch 2.8.0。
*   **模型使用**：主要实验使用 `gemini-3-pro-preview`；TriMul 任务使用了 `GPT-5.2` 和 `Gemini-3-Pro`。
*   **搜索预算**：FlashInfer 任务固定为 **120 次迭代**（每次迭代评估一个候选算子）；TriMul 任务为 300 次迭代。
*   **训练时长**：文中未明确给出总耗时，但由于是基于推理的搜索而非模型训练，耗时主要取决于算子编译和基准测试的时间。

#### 5. 实验数量与充分性
*   **实验组数**：每个方法在每个算子上重复运行 **3 次**，并报告了均值、最小值和最大值的阴影区间。
*   **覆盖范围**：涵盖了 4 种代表性算子，共计 **152 个测试 Workload**（涵盖不同 Batch Size、序列长度等）。
*   **客观性与公平性**：所有对比方法使用相同的初始程序、相同的编译工具链、相同的验证套件和相同的提示词模板（见附录 A.2）。
*   **充分性**：实验不仅对比了最终性能，还通过搜索轨迹可视化（Case Study）展示了世界模型的演化过程，并进行了详细的算子实现分析（如寄存器驻留、双缓冲预取等），论证了性能提升的来源。

#### 6. 主要结论与发现
*   **性能大幅领先**：K-Search 在所有测试算子上均显著优于基准。相比 OpenEvolve，平均实现 **2.10 倍**性能提升；在复杂的 MoE 算子上，提升高达 **14.3 倍**。
*   **超越人类专家**：在 GPUMode TriMul 任务中，K-Search 达到了 **1030 μs** 的延迟，刷新了 SOTA 记录，超越了所有已知的人工设计和自动化方案。
*   **非单调路径探索**：实验证明，解耦规划与实现后，系统能够忍受中间步骤的暂时失败，最终发现深层结构优化（如 MLA 中的寄存器驻留片段重用）。
*   **策略适应性**：K-Search 生成的算子展现了高级优化技巧，如自适应 Split-K 并行、深度预取流水线（三缓冲）等。

#### 7. 优点
*   **解耦架构**：将逻辑推理与代码编写分离，解决了 LLM 在长链优化中容易“迷失”的问题。
*   **动态演化**：世界模型通过 ICL 实时校准搜索策略，比静态启发式算法更具灵活性。
*   **鲁棒性强**：局部细化机制（Local Refinement）有效过滤了 LLM 随机生成的代码噪声。
*   **实用性高**：在 DeepSeek-V3 等前沿模型所需的 MLA 和 MoE 算子上表现卓越。

#### 8. 不足与局限
*   **小 Batch 场景开销**：在 GQA 任务中，K-Search 倾向于生成复杂的 Split-K 算子，这在 Batch Size=1 时由于同步开销，性能反而略低于简单的单块实现。
*   **依赖先验知识**：世界模型的效能高度依赖于底层 LLM 对 CUDA 优化的先验理解。
*   **演化方式单一**：目前世界模型的演化仅通过上下文学习实现，尚未探索通过微调（Fine-tuning）来永久性提升模型作为世界模型的规划能力。
*   **搜索成本**：虽然比强化学习高效，但 120-300 次的编译评估循环在某些快速迭代场景下仍显昂贵。

（完）
