---
title: "d-HNSW: A High-performance Vector Search Engine on Disaggregated Memory"
title_zh: d-HNSW：一种基于解耦内存的高性能向量搜索引擎
authors: "Fei Fang, Yi Liu, Chen Qian"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13591v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 存算分离架构下的向量搜索引擎
tldr: 针对大模型时代向量搜索在存算分离架构下面临的挑战，本文提出了首个基于RDMA的高性能向量搜索引擎d-HNSW。该系统通过硬件与算法协同设计，解决了图索引在解耦内存中的指针追踪效率低、内存布局不连续及资源利用率不足等问题。d-HNSW引入了平衡聚类、RDMA友好布局和流水线执行等技术，在保持高召回率的同时，显著提升了检索性能和系统吞吐量。
selection_source: fresh_fetch
motivation: 传统的单体架构难以满足大规模向量搜索的扩展性需求，而存算分离架构在处理图索引时面临网络延迟和内存布局等挑战。
method: 提出了d-HNSW，通过平衡聚类、RDMA友好型图布局、查询感知数据加载和流水线执行模型来优化存算分离环境下的向量检索。
result: "在公有云评估中，d-HNSW 相比基准方案实现了延迟降低两个数量级以上、吞吐量提升百倍以上，并保持了 94% 的高召回率。"
conclusion: d-HNSW 证明了通过硬件与算法协同设计，可以在存算分离架构上实现高效且可扩展的高性能向量搜索。
---

## 摘要
高效的向量搜索对于驱动大规模 AI 应用（如大语言模型 LLM）至关重要。现有的解决方案主要针对计算与内存紧密耦合的单体架构设计。近年来，解耦架构通过将计算和内存资源分离为独立可扩展的资源池，打破了这种耦合，从而提高了资源利用率。然而，由于向量数据库采用基于图的索引，在解耦内存系统上应用向量数据库给系统设计带来了独特的挑战。我们提出了 d-HNSW，这是首个针对解耦内存系统优化的基于 RDMA 的向量搜索引擎。d-HNSW 在保持 HNSW 高准确率的同时，解决了由解耦引入的新系统级挑战：1) 指针追踪遍历导致的网络低效；2) 动态插入导致的非连续远程内存布局；3) 批处理负载中的冗余数据传输；以及 4) 顺序执行导致的资源利用不足。d-HNSW 通过一系列硬件-算法协同设计的技术来应对这些挑战，包括：1) 带有轻量级代表索引的均衡聚类，以减少网络往返并确保可预测的延迟；2) 一种 RDMA 友好的图布局，在动态插入下仍能保持数据连续性；3) 查询感知的数据加载，以消除批处理查询中的冗余获取；以及 4) 一种流水线执行模型，将 RDMA 传输与计算重叠，以隐藏网络延迟并提高吞吐量。我们在公有云上的评估结果表明，与其它基准方案相比，d-HNSW 实现了低至 < 10^-2 倍的查询延迟和超过 100 倍的查询吞吐量，同时保持了 94% 的高召回率。

## Abstract
Efficient vector search is essential for powering large-scale AI applications, such as LLMs. Existing solutions are designed for monolithic architectures where compute and memory are tightly coupled. Recently, disaggregated architecture breaks this coupling by separating compution and memory resources into independently scalable pools to improve utilization. However, applying vector database on disaggregated memory system brings unique challenges to system design due to its graph-based index. We present d-HNSW, the first RDMA-based vector search engine optimized for disaggregated memory systems. d-HNSW preserves HNSW's high accuracy while addressing the new system-level challenges introduced by disaggregation: 1) network inefficiency from pointer-chasing traversals, 2) non-contiguous remote memory layout induced by dynamic insertions, 3) redundant data transfers in batch workloads, and 4) resource underutilization due to sequential execution. d-HNSW tackles these challenges through a set of hardware-algorithm co-designed techniques, including 1) balanced clustering with a lightweight representative index to reduce network round-trips and ensure predictable latency, 2) an RDMA-friendly graph layout that preserves data contiguity under dynamic insertions, 3) query-aware data loading to eliminate redundant fetches across batch queries, and 4) a pipelined execution model that overlaps RDMA transfers with computation to hide network latency and improve throughput. Our evaluation results in a public cloud show that d-HNSW achieves up to < 10-2x query latency and > 100x query throughput compared to other baselines, while maintaining a high recall of 94%.

---

## 论文详细总结（自动生成）

以下是对论文《d-HNSW: A High-performance Vector Search Engine on Disaggregated Memory》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）和推荐系统的爆发，大规模向量搜索成为核心基础设施。传统的**单体架构（Monolithic Architecture）**将计算和内存紧密耦合，导致资源利用率低下且难以独立扩展。
**解耦内存（Disaggregated Memory）**架构通过 RDMA 网络将计算池和内存池分离，提供了极高的扩展性。然而，将最流行的向量索引算法 **HNSW** 移植到解耦架构面临四大挑战：
*   **网络延迟：** HNSW 的贪婪搜索存在严重的“指针追踪（Pointer-chasing）”现象，每次跳转都会产生独立的网络往返。
*   **内存布局不连续：** 动态插入会导致远程内存碎片化，增加 RDMA 读取次数。
*   **冗余传输：** 批处理查询中，不同查询可能重复请求相同的索引分区。
*   **资源闲置：** 传统的顺序执行模式导致 CPU 在等待网络数据传输时处于空闲状态。

### 2. 核心方法论
d-HNSW 采用了**硬件-算法协同设计**的思想，提出了以下关键技术：
*   **代表性索引缓存（Meta-HNSW）：** 将数据集划分为多个平衡的子集（Sub-HNSW），并在计算节点缓存一个轻量级的“元索引（Meta-HNSW）”。查询时先在本地定位潜在的分区，从而将随机的网络跳转转化为批量的数据获取。
*   **RDMA 友好型图布局：** 在远程内存中采用“双层预留空间”设计（内部间隙+共享溢出区）。新插入的向量会被放置在预留空间内，确保即使在动态更新后，整个分区仍能通过单次 RDMA_READ 连续读取。
*   **查询感知批量加载：** 在处理一批查询时，系统会分析所有查询所需的分区，确保每个 Sub-HNSW 分区在一次批处理中仅被拉取一次，并利用 LRU 缓存减少跨批次的重复传输。
*   **流水线执行模型：** 将执行过程分为获取、反序列化、搜索三个阶段。通过多线程并行，使当前分区的计算与下一个分区的网络传输重叠，隐藏网络延迟。
*   **在线重建机制：** 采用基于纪元（Epoch）的影子拷贝技术，在后台重建索引的同时不中断前端查询服务。

### 3. 实验设计
*   **数据集：** 涵盖了 1M 到 10M 规模的五个标准数据集：TEXT10M（200维）、SIFT10M（128维）、DEEP10M（96维）、GIST1M（960维）、SIFT1M（128维）。
*   **基准测试（Benchmarks）：**
    *   **d-Pyramid：** 将现有的分布式 HNSW 框架 Pyramid 移植到 RDMA 环境。
    *   **d-HNSW RPC：** 基于传统 RPC 模式的解耦 HNSW 实现（涉及远程 CPU）。
    *   **distributed-faiss：** 基于 gRPC 的标准分布式向量搜索系统（非解耦架构）。
*   **评价指标：** 召回率（Recall）、查询延迟（Latency）、吞吐量（Throughput）、扩展性。

### 4. 资源与算力
*   **硬件环境：** 使用了 CloudLab 平台上的 4 台 Dell PowerEdge R650 服务器。
*   **配置：** 每台服务器配备两颗 36 核 Intel Xeon Platinum CPU，256GB RAM，以及 **Mellanox ConnectX-6 100Gb NIC**（支持 RDMA）。
*   **算力说明：** 该系统主要依赖 CPU 和 RDMA 网络，**未提及使用 GPU**。实验中使用了多达 30 个计算 worker 线程。

### 5. 实验数量与充分性
论文进行了多维度的详尽实验：
*   **性能对比：** 在 5 个数据集上对比了 Recall-Latency 曲线。
*   **吞吐量扩展性：** 测试了从 5 到 30 个 worker 时的性能提升。
*   **消融实验：** 逐步验证了重组布局、Doorbell 批量处理、流水线设计对性能的贡献。
*   **动态负载：** 测试了不同搜索/插入比例下的延迟表现。
*   **索引重建：** 验证了重建期间系统的吞吐量稳定性。
*   **充分性评价：** 实验设计覆盖了从算法精度到系统吞吐、从静态检索到动态更新的全场景，对比方法具有代表性，实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **性能飞跃：** 相比于基准方案，d-HNSW 的查询延迟降低了两个数量级（< 10^-2x），吞吐量提升了 100 倍以上。
*   **高准确性：** 在极低延迟下仍能保持约 94% 的高召回率。
*   **高效隐藏延迟：** 流水线模型成功将网络传输开销隐藏在计算之后，使解耦架构的性能接近本地内存搜索。
*   **动态支持：** RDMA 友好布局有效解决了动态插入导致的性能退化问题。

### 7. 优点
*   **首创性：** 填补了解耦内存架构下缺乏高效向量搜索引擎的空白。
*   **协同设计：** 并非简单的移植，而是针对 RDMA 的单边操作（One-sided）特性重新设计了数据结构和执行流。
*   **实用性强：** 解决了工业界关注的动态更新和批处理效率问题，且不依赖昂贵的定制硬件（如 FPGA/SmartNIC）。

### 8. 不足与局限
*   **规模限制：** 实验数据集最大为 10M，虽然在学术论文中常见，但工业界（如百亿级向量）可能需要更复杂的索引分层或压缩技术。
*   **内存开销：** 为了保持 RDMA 连续读取，预留的内部间隙和溢出空间会带来一定的内存浪费（Overhead）。
*   **元索引依赖：** 计算节点需要缓存 Meta-HNSW，如果分区数量极多，元索引的维护和同步开销可能会增加。

（完）
