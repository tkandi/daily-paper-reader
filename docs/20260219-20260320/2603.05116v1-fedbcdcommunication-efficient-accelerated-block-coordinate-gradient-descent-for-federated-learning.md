---
title: "FedBCD:Communication-Efficient Accelerated Block Coordinate Gradient Descent for Federated Learning"
title_zh: FedBCD：用于联邦学习的通信高效加速块坐标梯度下降法
authors: "Junkang Liu, Fanhua Shang, Yuanyuan Liu, Hongying Liu, Yuangang Li, YunXiang Gong"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05116v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 针对大规模模型的高效通信块坐标梯度下降
tldr: 针对大规模模型在联邦学习中面临的高通信开销问题，本文提出了一种联邦块坐标梯度下降方法（FedBCGD）。该方法通过将模型参数划分为多个块并允许客户端仅上传特定参数块，显著降低了通信复杂度。此外，作者还开发了带有漂移控制和方差缩减的加速版本FedBCGD+。理论证明其通信复杂度比现有方法降低了1/N，且实验结果验证了其在训练大规模深度模型时的优越性。
selection_source: fresh_fetch
motivation: 联邦学习在训练大规模模型（如ViT）时面临极高的单轮通信开销，限制了其在资源受限环境下的应用。
method: 提出将模型参数分块并仅上传特定块的FedBCGD方法，并结合漂移控制与方差缩减技术开发了加速版FedBCGD+。
result: 理论分析显示该算法的通信复杂度比现有方法降低了1/N，且实验证明其收敛速度和性能均优于现有最先进算法。
conclusion: 本文是首个在大规模深度模型训练中引入参数块通信的工作，为实现通信高效的联邦学习提供了新的理论支持和实践方案。
---

## 摘要
尽管联邦学习在近年来得到了广泛研究，但对于 Vision Transformer 等大规模模型，每一轮通信仍存在高昂的开销。为了降低通信复杂度，我们提出了一种新型的联邦块坐标梯度下降（FedBCGD）方法以提高通信效率。该方法将模型参数划分为多个块（包括一个共享块），并允许每个客户端上传特定的参数块，从而显著降低通信开销。此外，我们还开发了一种带有客户端漂移控制和随机方差缩减的加速 FedBCGD 算法（称为 FedBCGD+）。据我们所知，本文是首个针对大规模深度模型训练采用参数块通信的研究工作。我们还为所提算法提供了收敛性分析。理论结果表明，我们算法的通信复杂度比现有方法低 1/N 倍（其中 N 是参数块的数量），且收敛速度比同类方法快得多。实验结果表明，与现有最先进算法相比，所提算法具有优越性。代码可在 https://github.com/junkangLiu0/FedBCGD 获取。

## Abstract
Although Federated Learning has been widely studied in recent years, there are still high overhead expenses in each communication round for large-scale models such as Vision Transformer. To lower the communication complexity, we propose a novel Federated Block Coordinate Gradient Descent (FedBCGD) method for communication efficiency. The proposed method splits model parameters into several blocks, including a shared block and enables uploading a specific parameter block by each client, which can significantly reduce communication overhead. Moreover, we also develop an accelerated FedBCGD algorithm (called FedBCGD+) with client drift control and stochastic variance reduction. To the best of our knowledge, this paper is the first work on parameter block communication for training large-scale deep models. We also provide the convergence analysis for the proposed algorithms. Our theoretical results show that the communication complexities of our algorithms are a factor $1/N$ lower than those of existing methods, where $N$ is the number of parameter blocks, and they enjoy much faster convergence than their counterparts. Empirical results indicate the superiority of the proposed algorithms compared to state-of-the-art algorithms.   The code is available at https://github.com/junkangLiu0/FedBCGD.

---

## 论文详细总结（自动生成）

以下是对论文《FedBCGD: Communication-Efficient Accelerated Block Coordinate Gradient Descent for Federated Learning》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在联邦学习（FL）中，随着模型规模（如 Vision Transformer, ViT）的急剧增加，客户端与服务器之间每一轮通信的开销变得异常沉重。由于客户端的上行带宽通常远低于下行带宽，上传完整模型参数成为了训练瓶颈。
*   **核心问题**：如何在保证模型性能和收敛速度的前提下，显著降低联邦学习中的上行通信复杂度？
*   **研究动机**：现有的通信压缩技术（如量化、稀疏化）虽有成效，但尚未充分探索将“块坐标下降（BCD）”引入水平联邦学习。本文旨在通过参数分块上传和加速算法设计，实现更高效的分布式训练。

### 2. 方法论
论文提出了 **FedBCGD** 及其增强版 **FedBCGD+**：
*   **核心思想**：将模型参数 $\mathbf{x}$ 划分为 $N$ 个参数块和一个共享块（通常是分类器层）。在每一轮中，客户端在本地更新**所有**参数，但仅向服务器上传**一个指定的参数块**和**共享块**。
*   **关键技术细节**：
    *   **参数分块与分配**：将 $S$ 个客户端分为 $N$ 组，每组负责上传不同的参数块，服务器聚合后拼成完整模型。
    *   **本地全量更新（非冻结）**：实验发现，若本地仅更新上传块而冻结其他块会导致严重漂移。因此，FedBCGD 要求本地更新所有参数，仅在上传阶段进行“块选择”。
    *   **服务器端动量补偿**：为了补偿未上传块的信息缺失，服务器端引入动量机制来平滑更新方向。
    *   **FedBCGD+（加速版）**：在 FedBCGD 基础上引入了**客户端漂移控制变量**（借鉴 SCAFFOLD）和**随机方差缩减技术**（借鉴 SVRG），以应对数据异构性（Non-IID）带来的收敛挑战。

### 3. 实验设计
*   **数据集**：CIFAR-10, CIFAR-100, Tiny ImageNet（非凸问题）；EMNIST（凸问题/逻辑回归）。
*   **模型架构**：LeNet-5, VGG-11/19, ResNet-18, 以及大规模模型 Vision Transformer (ViT-Base)。
*   **数据分布**：通过 Dirichlet 分布（$\rho=0.6, 0.1$）模拟不同程度的 Non-IID 场景。
*   **对比方法（Baselines）**：
    *   基础算法：FedAvg, FedAvgM。
    *   自适应/漂移修正算法：FedAdam, SCAFFOLD, FedDC。
    *   通信压缩算法：TOP-k, FedPAQ, QSGD。

### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号、数量及总训练时长。
*   **实现细节**：作者提供了 GitHub 代码库，实验设置中提到客户端数量为 100，每轮参与率为 10%，本地 Epoch 为 1 或 5。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从小型 CNN 到大型 ViT 的多种架构，并在 4 个主流数据集上进行了测试。
*   **消融实验**：
    *   对比了“参数冻结”与“全量更新”的效果。
    *   研究了参数块数量 $N$ 对收敛速度的影响（测试了 $N=1$ 到 $N=20$）。
    *   分析了动量参数 $\lambda$ 的敏感性。
*   **充分性评价**：实验设计较为全面，不仅验证了非凸深度学习任务，还通过凸优化问题验证了理论分析的正确性。对比了多种 SOTA 算法，证明了在相同通信量（Floats sent）下，该方法具有更高的精度和更快的收敛速度。

### 6. 主要结论与发现
*   **通信效率**：理论证明 FedBCGD 的通信复杂度比现有方法降低了 $1/N$ 倍。
*   **收敛性能**：FedBCGD+ 在强凸和非凸设置下均达到了目前已知的最优收敛速率。
*   **大规模模型优势**：在 ViT-Base 模型上，FedBCGD 在 Tiny ImageNet 上的收敛速度比 FedAvg 快 11.5 倍以上。
*   **泛化能力**：在某些 Non-IID 场景下，FedBCGD 的最终测试准确率甚至超过了中心化 SGD，显示出更好的泛化性能（可能由于分布式更新的正则化效应）。

### 7. 优点
*   **首创性**：首次将块坐标下降（BCD）应用于水平联邦学习的大规模深度模型训练。
*   **理论与实践结合**：提供了严谨的收敛性证明，且理论增益（$1/N$）在实验中得到了直观体现。
*   **兼容性强**：该方法可以与现有的量化（如 QSGD）等压缩技术叠加使用，进一步提升效率。
*   **共享块设计**：通过保留一小部分共享参数（分类器），显著缓解了分块更新带来的性能损失。

### 8. 不足与局限
*   **分块策略**：论文中的参数分块主要基于网络层手动划分，缺乏一种自动化的、针对不同模型架构的最优分块策略。
*   **存储开销**：FedBCGD+ 需要在客户端和服务器端维护额外的控制变量（Control Variates），对于显存极度受限的设备，这可能会增加负担。
*   **异构计算未考虑**：虽然考虑了通信异构（将小块分配给弱通信客户端），但未深入探讨客户端计算能力异构对同步聚合的影响。

（完）
