---
title: "Fold-CP: A Context Parallelism Framework for Biomolecular Modeling"
title_zh: Fold-CP：一种用于生物分子建模的上下文并行框架
authors: "Dejun Lin, Simon Chu, Vishanth Iyer, Youhan Lee, John St John, Kevin Boyd, Brian Roland, Xiaowei Ren, Guoqing Zhou, Zhonglin Cao, Polina Binder, Yuliya Zhautouskaya, Jakub Zakrzewski, Maximilian Stadler, Kyle Gion, Yuxing Peng, Xi Chen, Tianjing Zhang, Philipp Junk, Michelle Dimon, Paweł Gniewek, Fabian Ortega, McKinley Polen, Ivan Grubisic, Ali Bashir, Graham Holt, Danny Kovtun, Matthias Grass, Luca Naef, Rui Wang, Jian Peng, Anthony Costa, Saee Paliwal, Eddie Calleja, Timur Rvachov, Neha Tadimeti, Roy Tal, Emine Kucukbenli"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14806v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 用于分布式训练流水线的上下文并行框架
tldr: NVIDIA推出BioNeMo Fold-CP框架，旨在解决AlphaFold 3等模型在处理大型生物分子组装体时的显存瓶颈。通过在多GPU间分布推理和训练流水线，并优化三角更新与局部注意力机制，该框架实现了显存的线性扩展。它支持超过3万个残基的结构预测，为大规模蛋白质复合物建模和虚拟细胞研究提供了高效的可扩展方案。
selection_source: fresh_fetch
motivation: 现有的生物分子建模工具受限于单GPU显存，难以处理包含数千个以上残基的大型复杂系统。
method: 开发了Fold-CP框架，利用上下文并行技术将计算任务分布至多GPU，并针对三角更新和不规则局部注意力设计了高效的并行原语。
result: "实现了显存随GPU数量增加而线性下降，成功在64块GPU上预测了超过3万个残基的结构，并覆盖了CORUM数据库中90%以上的复合物。"
conclusion: Fold-CP为大规模生物系统建模提供了可扩展的路径，是实现全细胞尺度模拟的重要一步。
---

## 摘要
理解细胞机制需要对大型生物分子复合物进行原子尺度的重建。然而，预测这些系统的结构一直受到 AlphaFold 3 等模型对硬件显存需求的限制，导致单张 GPU 能够处理的残基数量存在几千个的实际上限。在此，我们介绍了 NVIDIA BioNeMo Fold-CP，这是一个上下文并行框架，通过将协同折叠模型的推理和训练流水线分布到多个 GPU 上来克服这一障碍。我们以 Boltz 模型作为开源参考架构，并实现了自定义的多维原语，从而有效地并行化了密集三角形更新以及窗口批处理局部注意力的不规则、数据依赖模式。我们的方法实现了高效的显存扩展；对于分布在 P 个 GPU 上的 N 个 token 输入，单设备显存占用按 O(N²/P) 缩放，使得在 64 个 NVIDIA B300 GPU 上能够预测超过 30,000 个残基的复合物结构。我们通过成功的开发者用例展示了该方法的科学实用性：Fold-CP 实现了对哺乳动物蛋白复合物综合资源 (CORUM) 数据库中 90% 以上数据的评分，并完成了与内在无序区域结合的疾病相关 PI4KA 脂质激酶复合物的折叠，且无需进行裁剪。通过为具有完整全局上下文的大规模系统建模提供可扩展的路径，Fold-CP 代表了迈向实现虚拟细胞的重要一步。

## Abstract
Understanding cellular machinery requires atomic-scale reconstruction of large biomolecular assemblies. However, predicting the structures of these systems has been constrained by hardware memory requirements of models like AlphaFold 3, imposing a practical ceiling of a few thousand residues that can be processed on a single GPU. Here we present NVIDIA BioNeMo Fold-CP, a context parallelism framework that overcomes this barrier by distributing the inference and training pipelines of co-folding models across multiple GPUs. We use the Boltz models as open source reference architectures and implement custom multidimensional primitives that efficiently parallelize both the dense triangular updates and the irregular, data-dependent pattern of window-batched local attention. Our approach achieves efficient memory scaling; for an N-token input distributed across P GPUs, per-device memory scales as $O(N^2/P)$, enabling the structure prediction of assemblies exceeding 30,000 residues on 64 NVIDIA B300 GPUs. We demonstrate the scientific utility of this approach through successful developer use cases: Fold-CP enabled the scoring of over 90% of Comprehensive Resource of Mammalian protein complexes (CORUM) database, as well as folding of disease-relevant PI4KA lipid kinase complex bound to an intrinsically disordered region without cropping. By providing a scalable pathway for modeling massive systems with full global context, Fold-CP represents a significant step toward the realization of a virtual cell.

---

## 论文详细总结（自动生成）

### Fold-CP：用于生物分子建模的上下文并行框架总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现代生物分子结构预测模型（如 AlphaFold 3、Boltz-1）在处理大型生物组装体时面临严重的显存瓶颈。由于模型采用密集的成对表示（Pair Representation），其显存需求随序列长度 $N$ 呈平方级（$O(N^2)$）增长，计算量呈立方级（$O(N^3)$）增长。
*   **核心问题**：单张 GPU（如 A100/H100）通常只能处理约 2000-3000 个残基，而超过 70% 的哺乳动物蛋白复合物超过此限制。现有的裁剪（Cropping）或线性注意力近似方法会损失全局上下文或降低精度。
*   **整体含义**：NVIDIA 提出的 Fold-CP 旨在通过分布式计算打破单机显存墙，在不牺牲精度和不使用近似算法的前提下，实现超大规模生物分子（如病毒衣壳、大型信号复合物）的全尺寸建模。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：引入上下文并行（Context Parallelism, CP），将单个样本的激活值分布在 $\sqrt{P} \times \sqrt{P}$ 的二维设备网格上，使单设备显存占用从 $O(N^2)$ 降至 $O(N^2/P)$。
*   **关键技术细节**：
    *   **多维分片策略**：对成对张量（Pair Tensor）进行二维平铺（Tiling），对多序列比对（MSA）进行深度和 Token 双向分片，并实现原子（Atom）与 Token 的协同分片。
    *   **分布式算法原语**：
        *   **三角注意力/乘法**：采用类似 Cannon 算法的二维环形（Ring）通信，结合分块 Softmax（Tiled Softmax）确保数值等价性。
        *   **外积平均（Outer Product Mean）**：通过二维环形累加和 All-reduce 实现高效的 MSA 到 Pair 的投影。
        *   **窗口批处理优化**：利用 Toeplitz 矩阵结构的平移对称性，将原子级注意力的构建从 $O(A^2)$ 优化为线性复杂度，并仅需极小的光环交换（Halo Exchange）通信。
    *   **自定义 Autograd 算子**：为防止 PyTorch 原生分布式张量在反向传播时自动聚合全局张量导致 OOM，Fold-CP 为所有核心模块实现了显式定义通信逻辑的自定义算子。
    *   **内核融合**：集成 `cuEquivariance` 和 `FlexAttention` 融合内核，进一步提升计算效率。

#### 3. 实验设计
*   **数据集/场景**：
    *   **基准测试**：使用 Boltz-1 测试集验证推理一致性。
    *   **科学用例**：CORUM 数据库（哺乳动物蛋白复合物）、PI4KA 脂质激酶复合物、人类 Elongator 复合物、5YZO 同源四聚体。
*   **Benchmark**：
    *   **扩展性**：测量不同 GPU 数量下的最大 Token 处理能力。
    *   **精度**：对比单机数据并行（DP）与 Fold-CP 的 lDDT 分数和训练曲线。
*   **对比方法**：主要对比单 GPU 基准（DP 模式）以及传统的序列裁剪方法。

#### 4. 资源与算力
*   **GPU 型号与数量**：
    *   使用了 **NVIDIA B300**（最高 64 块）进行 Boltz-2 CP 的扩展性测试。
    *   使用了 **NVIDIA H100 80GB**（最高 144 块）进行 Boltz-1 CP 的推理和训练验证。
*   **性能表现**：在 64 块 B300 上实现了超过 **32,000 个 Token** 的结构预测；在 144 块 H100 上实现了 24,000 个 Token 的预测。
*   **训练时长**：文中展示了 5.5k 步的训练一致性曲线，但未详细说明完整训练的总时长。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从数千到三万残基的不同规模，包括了推理一致性验证、训练收敛性验证以及多个真实的生物学案例研究。
*   **充分性与客观性**：实验设计较为充分。通过 Pearson 相关系数（R=0.97）和 Wilcoxon 符号秩检验验证了数值等价性；通过多个独立机构（Rezo, Proxima, Earendil Labs）的第三方验证，证明了框架的通用性和科学价值。

#### 6. 主要结论与发现
*   **线性扩展**：Fold-CP 实现了显存占用的线性扩展，最大上下文长度随 $\sqrt{P}$ 线性增长。
*   **精度保持**：分布式执行与单机执行在数值上高度一致，训练轨迹完全匹配。
*   **科学突破**：成功预测了 PI4KA 复合物中此前因裁剪而无法发现的相互作用；覆盖了 CORUM 数据库中 90% 以上的复合物结构。
*   **模型局限发现**：发现短上下文训练的模型在长上下文推理时会出现“链重叠（Clumping）”现象，强调了长上下文训练的必要性。

#### 7. 优点
*   **无损并行**：不依赖线性注意力等近似手段，完全保留了 AlphaFold 类模型的物理一致性。
*   **系统级优化**：通过自定义 Autograd 和内核融合，极大地提高了算力利用率，减少了通信开销。
*   **高度实用**：直接支持开源的 Boltz 模型，且已在药物研发和基础生物学研究中产生实际成果。

#### 8. 不足与局限
*   **拓扑限制**：目前要求 GPU 数量必须满足平方拓扑（如 4, 16, 64），这在某些非标准集群配置下不够灵活。
*   **数据稀缺**：虽然框架支持超长序列，但 PDB 数据库中缺乏超大规模的真值结构，限制了模型的进一步微调。
*   **模型伪影**：在超长序列下，模型仍可能预测出物理上不可能的原子重叠，这属于模型架构或训练数据的局限，而非并行框架本身的问题。
*   **硬件依赖**：高性能表现高度依赖于 NVLink 等高速互连技术。

（完）
