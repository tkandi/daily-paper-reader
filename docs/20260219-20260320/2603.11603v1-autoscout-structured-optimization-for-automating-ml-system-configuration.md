---
title: "AutoScout: Structured Optimization for Automating ML System Configuration"
title_zh: AutoScout：用于自动化机器学习系统配置的结构化优化
authors: "Jimmy Shong, Yuhan Ding, Yihan Jiang, Liheng Jing, Haonan Chen, Gaokai Zhang, Aditya Akella, Fan Lai"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11603v1"
tags: ["query:mlsys"]
score: 10.0
evidence: 自动化机器学习系统的并行与通信配置
tldr: 机器学习系统配置空间日益复杂，涉及并行策略、通信优化等，且存在异构特征和层级依赖，导致性能优化困难。本文提出 AutoScout，一个通用的 ML 系统配置器。它将配置建模为具有层级依赖的混合离散/连续优化问题，通过混合优化框架协同优化结构决策与执行参数，并利用多保真度模拟器降低评估成本。实验表明，AutoScout 在多种任务中比专家调优提升了 2.7-3.0 倍的训练速度。
selection_source: fresh_fetch
motivation: 现有的机器学习系统配置优化方法难以处理日益增长的异构配置空间、复杂的层级依赖关系以及高昂的性能评估成本。
method: AutoScout 采用混合优化框架，结合层级依赖建模，通过自适应特征优先级排序和多保真度模拟器来高效搜索最优配置。
result: 在多种模型和硬件平台上，AutoScout 相比专家手动调优实现了 2.7 至 3.0 倍的训练加速。
conclusion: AutoScout 证明了通过结构化优化和多保真度模拟，可以有效自动化并显著提升复杂机器学习系统的端到端运行效率。
---

## 摘要
机器学习 (ML) 系统呈现出快速扩张的配置空间，涵盖了模型并行策略、通信优化以及底层运行时参数。端到端系统效率对这些选择高度敏感，但由于异构特征类型（如稀疏和密集参数）、条件依赖关系（如仅在特定上游决策下有效的执行参数）以及高昂的搜索（性能分析）成本，识别高性能配置具有挑战性。现有方法要么仅优化配置维度的狭窄子集，要么依赖于随着配置空间持续增长而难以泛化的临时启发式方法。我们提出了 AutoScout，这是一种用于机器学习训练、微调和推理的通用系统配置器。它将系统配置建模为具有层次依赖关系的混合离散/连续优化问题，并引入了一种混合优化框架，共同优化稀疏结构决策和密集执行参数。为了降低性能分析成本，AutoScout 自适应地优先处理高影响力的配置特征，并集成了具有不同保真度的模拟器。在不同的模型、硬件平台和部署目标中，AutoScout 始终能识别出高性能配置，与专家调优的设置相比，实现了 2.7-3.0 倍的训练加速。

## Abstract
Machine learning (ML) systems expose a rapidly expanding configuration space spanning model-parallelism strategies, communication optimizations, and low-level runtime parameters. End-to-end system efficiency is highly sensitive to these choices, yet identifying high-performance configurations is challenging due to heterogeneous feature types (e.g., sparse and dense parameters), conditional dependencies (e.g., valid execution parameters only under specific upstream decisions), and the high search (profiling) cost. Existing approaches either optimize a narrow subset of configuration dimensions or rely on ad-hoc heuristics that fail to generalize as configuration spaces continue to grow. We present AutoScout, a general-purpose systems configurator for ML training, fine-tuning, and inference. It formulates the system configuration as a mixed-discrete/continuous optimization problem with hierarchical dependencies and introduces a hybrid optimization framework that jointly refines sparse structural decisions and dense execution parameters. To reduce profiling cost, AutoScout adaptively prioritizes high-impact configuration features and ensembles simulators with varying fidelity. Across diverse models, hardware platforms, and deployment objectives, AutoScout consistently identifies high-performance configurations, achieving 2.7-3.0$\times$ training speedup over expert-tuned settings.

---

## 论文详细总结（自动生成）

### AutoScout: 用于自动化机器学习系统配置的结构化优化 —— 论文总结

#### 1. 核心问题与研究动机
随着机器学习（ML）系统（如 vLLM, Megatron-LM）的演进，其配置空间呈爆炸式增长，涵盖了模型并行策略、通信优化及底层运行时参数。
*   **研究背景**：最优配置对系统效率至关重要（性能差异可达 40 倍），但手动调优依赖专家经验，且难以跨硬件和模型泛化。
*   **核心挑战**：
    1.  **异构特征**：同时包含离散/稀疏决策（如并行模式）和连续/密集参数（如缓存大小）。
    2.  **层级依赖**：参数间存在强耦合，某些参数仅在特定上游决策下有效。
    3.  **高昂成本**：在真实 GPU 上评估配置极其耗时耗力。
*   **现有局限**：现有工具（如 Metis, Alpa）多关注 3D 并行的狭窄子集，或依赖难以维护的启发式规则，且在处理混合离散-连续空间时效率低下。

#### 2. 方法论
AutoScout 提出了一种**混合结构化优化框架**，将配置过程分解为协同工作的多个组件：
*   **稀疏优化器（Sparse Optimizer）**：针对具有层级依赖的离散决策，采用**蒙特卡洛树搜索（MCTS）**。通过“锦标赛机制”在线调整特征优先级，优先搜索对性能影响大的维度。
*   **密集优化器（Dense Optimizer）**：针对连续执行参数，采用**坐标下降法（Coordinate-wise SGD）**。它利用掩码函数（Masking）确保参数调整符合稀疏优化器确定的结构约束。
*   **优化编排器（Orchestrator）**：基于**多臂老虎机（UCB1 算法）**动态分配搜索预算，决定当前应侧重于探索新结构（稀疏）还是精炼现有参数（密集）。
*   **自适应评估器（Adaptive Evaluator）**：集成多个轻量级模拟器（线性回归模型）。当模拟器预测误差（MAPE）超过阈值时，自动切换到真实性能分析（Profiling），平衡搜索速度与反馈精度。

#### 3. 实验设计
*   **模型与负载**：涵盖训练与推理任务。包括 Llama-3.2-3B、Llama-3.1-Nemotron-8B、Qwen-30B-A3B 以及 Meta-Llama-3-8B。
*   **数据集**：使用 LMSYS-Chat-1M 真实对话数据集。
*   **对比基准（Benchmarks）**：
    *   **专家调优**：vLLM 和 Megatron-LM 的默认/专家设置。
    *   **自动并行系统**：Metis（专注于 3D 并行）。
    *   **通用优化器**：UDO（基于 RL/MCTS）、CherryPick（基于贝叶斯优化）。
*   **评估指标**：训练延迟（s/iter）、推理吞吐量（ms/token）、搜索步数及墙钟时间。

#### 4. 资源与算力
*   **硬件环境**：实验在一个包含 **8 台 NVIDIA A100 GPU** 和 **4 台 NVIDIA A40 GPU** 的集群上完成。
*   **搜索预算**：实验中设定了不同的时间预算（如 2000 秒）或步数限制，以验证算法的收敛速度。

#### 5. 实验数量与充分性
*   **实验规模**：论文在多种模型架构（密集模型与 MoE 模型）、不同硬件平台及不同任务（训练/推理）上进行了测试。
*   **消融实验**：非常充分。分别验证了锦标赛机制、密集优化器、编排器以及模拟器保真度对最终性能的影响。
*   **客观性**：所有结果均为 **20 次独立运行的平均值**，并展示了搜索过程中的收敛曲线，对比过程考虑了搜索成本（时间/步数），具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能提升**：AutoScout 发现的配置在训练任务中比专家调优设置快 **2.7–3.0 倍**。
*   **搜索效率**：在达到相同性能水平时，AutoScout 的搜索速度比现有自动配置器快 **13.7–16.5 倍**，且搜索步数减少了 28%–93%。
*   **鲁棒性**：即使在模拟器存在高达 80% 噪声的情况下，自适应评估机制仍能引导系统收敛至近优配置。
*   **超越并行优化**：实验证明，仅优化 3D 并行是不够的，执行层的密集参数调整能额外带来 1.4–4 倍的性能增益。

#### 7. 优点
*   **结构化建模**：通过 MCTS 显式处理配置间的层级依赖，比黑盒优化更符合系统逻辑。
*   **冷启动优化**：锦标赛机制有效解决了高维空间中特征排序难的问题。
*   **多保真度融合**：巧妙结合了低成本模拟与高成本分析，极大地降低了对昂贵 GPU 资源的依赖。
*   **通用性强**：不局限于特定框架，可扩展至训练、微调及推理等多种场景。

#### 8. 不足与局限
*   **模拟器依赖**：虽然有自适应切换，但初始搜索效率仍受限于模拟器的基本质量；若模拟器与实际硬件表现完全背离，可能导致初期搜索误导。
*   **搜索空间定义**：仍需要预先定义配置参数的范围和依赖关系（Knobs），对于完全未知的黑盒系统，前期建模仍有一定工作量。
*   **硬件覆盖**：实验主要集中在 NVIDIA A100/A40，对于更广泛的异构硬件（如国产芯片或不同互连拓扑）的泛化能力尚待进一步验证。

（完）
