---
title: "MONET: Modeling and Optimization of neural NEtwork Training from Edge to Data Centers"
title_zh: MONET：从边缘到数据中心的神经网络训练建模与优化
authors: "Jérémy Morlier, Robin Geens, Stef Cuyckens, Arne Symons, Marian Verhelst, Vincent Gripon, Mathieu Léonardon"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15002v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 加速器上神经网络训练的建模与优化
tldr: 针对现有工具在神经网络训练建模方面的不足，本文提出MONET框架。该框架扩展了Stream推理建模工具，专门针对训练过程中的反向传播复杂性和内存限制进行优化。通过在异构数据流加速器上对ResNet-18和GPT-2进行建模，MONET能够自动探索层融合配置，并利用遗传算法优化激活检查点策略，为从边缘到数据中心的训练任务提供高效的软硬件协同设计方案。
selection_source: fresh_fetch
motivation: 现有的神经网络建模工具主要关注推理阶段，无法有效捕捉训练过程中特有的内存占用和反向传播复杂性。
method: 基于Stream框架开发了MONET，通过集成层融合优化和遗传算法驱动的激活检查点策略，实现对异构加速器训练负载的建模。
result: 在ResNet-18和GPT-2上的实验表明，MONET能有效探索硬件架构设计空间，并在层融合与激活检查点权衡中找到更优配置。
conclusion: 采用整体的软硬件协同设计方法对于实现可扩展且高效的深度学习训练部署至关重要。
---

## 摘要
虽然软硬件协同设计显著提高了神经网络推理的效率，但对训练阶段的建模仍然是一个关键且尚未得到充分探索的挑战。训练工作负载施加了独特的约束，特别是在内存占用和反向传播复杂性方面，而现有的专注于推理的工具无法捕捉到这些约束。本文介绍了 MONET，这是一个旨在对异构数据流加速器上的神经网络训练进行建模的框架。MONET 基于 Stream 构建，Stream 是一个经过实验验证的框架，用于对具有层融合的异构数据流加速器上的神经网络推理进行建模。利用 MONET，我们探索了 ResNet-18 和小型 GPT-2 的设计空间，展示了该框架建模训练工作流并寻找更优硬件架构的能力。随后，我们进一步研究了由于设计空间更大而在神经网络训练中变得更加复杂的问题，例如确定最佳的层融合配置。此外，我们借助遗传算法，利用该框架在激活检查点（activation checkpointing）中寻找有趣的权衡。我们的研究结果强调了整体软硬件协同设计方法对于可扩展且高效的深度学习部署的重要性。

## Abstract
While hardware-software co-design has significantly improved the efficiency of neural network inference, modeling the training phase remains a critical yet underexplored challenge. Training workloads impose distinct constraints, particularly regarding memory footprint and backpropagation complexity, which existing inference-focused tools fail to capture. This paper introduces MONET, a framework designed to model the training of neural networks on heterogeneous dataflow accelerators. MONET builds upon Stream, an experimentally verified framework that that models the inference of neural networks on heterogeneous dataflow accelerators with layer fusion. Using MONET, we explore the design space of ResNet-18 and a small GPT-2, demonstrating the framework's capability to model training workflows and find better hardware architectures. We then further examine problems that become more complex in neural network training due to the larger design space, such as determining the best layer-fusion configuration. Additionally, we use our framework to find interesting trade-offs in activation checkpointing, with the help of a genetic algorithm. Our findings highlight the importance of a holistic approach to hardware-software co-design for scalable and efficient deep learning deployment.

---

## 论文详细总结（自动生成）

以下是对论文《MONET: Modeling and Optimization of neural NEtwork Training from Edge to Data Centers》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的神经网络硬件加速器建模工具（如 Timeloop, ZigZag 等）主要针对**推理（Inference）**阶段，无法有效处理**训练（Training）**阶段特有的复杂性。
*   **研究动机**：训练过程涉及反向传播、梯度计算、权重更新以及复杂的内存管理（如激活值缓存、优化器状态），其计算量和内存需求远超推理。
*   **整体含义**：本文提出了 **MONET** 框架，旨在填补这一空白，实现从边缘设备到数据中心异构加速器上训练任务的性能建模与软硬件协同优化。

### 2. 方法论
*   **核心思想**：基于已验证的推理建模框架 **Stream** 进行扩展，通过自动化的 ONNX 转换流程，将训练任务（前向+反向+优化器）映射到异构数据流加速器（HDA）上。
*   **关键技术细节**：
    *   **训练图生成**：利用 ONNX Runtime Training 将 PyTorch 模型导出为包含梯度计算和优化器步骤的完整计算图，并引入自定义 Pass 将复杂的梯度算子（如 ConvGrad）分解为细粒度原语。
    *   **约束驱动的层融合（Layer Fusion）**：提出一种基于**整数规划（Integer Programming）**的求解器，在满足内存容量、算子类型和磁贴（Tiling）兼容性的约束下，自动寻找最优的算子融合方案，以减少访存开销。
    *   **激活检查点（Activation Checkpointing）优化**：针对层融合环境下重计算成本的非线性特征，采用**遗传算法（NSGA-II）**在内存节省、延迟和功耗之间寻找 Pareto 最优解。

### 3. 实验设计
*   **场景与模型**：
    *   **边缘场景**：使用 **ResNet-18** 模型，目标硬件为类 **Edge TPU** 的异构加速器。
    *   **数据中心场景**：使用小型 **GPT-2** 模型，目标硬件为 **FuseMax**（一种针对 Attention 优化的加速器）。
*   **Benchmark 与对比**：
    *   对比了推理与训练在相同硬件配置下的能量-延迟分布差异。
    *   在层融合实验中，对比了“逐层执行（Base）”、“人工设计融合（Manual）”以及 MONET 自动生成的不同限制下的融合策略。
    *   在激活检查点实验中，对比了全量存储（Baseline）与不同重计算策略的收益。

### 4. 资源与算力
*   **建模工具运行环境**：文中提到部分内存测量是在 **NVIDIA RTX 3090** 上进行的（用于获取 ResNet-50 的基准内存分布）。
*   **算力支持**：致谢部分提到使用了 **GENCI-IDRIS** 的 AI 资源。
*   **说明**：由于 MONET 本身是一个**建模与仿真框架**，其核心产出是性能预测数据（周期数、焦耳、内存占用），而非实际的大规模模型训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 Edge TPU 进行了大规模硬件设计空间搜索（涉及 PE 数量、SIMD 单元、内存大小等多个维度）。
    *   对层融合算法进行了消融实验（改变子图长度限制）。
    *   通过遗传算法演化了多代激活检查点策略。
*   **充分性评价**：实验涵盖了 CNN 和 Transformer 两大主流架构，且对比了边缘与云端两种截然不同的硬件环境，实验设计较为全面且客观。通过 Pareto 前沿分析证明了训练优化空间与推理的显著不同。

### 6. 主要结论与发现
*   **训练与推理的非对称性**：针对推理优化的硬件架构在训练任务上往往不是最优的，证明了开发专门针对训练的建模工具的必要性。
*   **自动融合的优越性**：MONET 自动生成的层融合策略在延迟和能效上均优于专家手动设计的配置。
*   **重计算的非线性**：在层融合架构中，重计算多个算子的成本并不等于各算子单独重计算成本之和，这使得传统的线性规划（MILP）失效，而遗传算法能有效处理这种非线性权衡。

### 7. 优点
*   **全栈建模**：从 PyTorch 模型到硬件底层的端到端映射，支持前向、反向及多种优化器（SGD, Adam）。
*   **自动化程度高**：引入约束求解器和遗传算法，减少了人工调优硬件映射和融合策略的工作量。
*   **灵活性**：支持异构加速器建模，能够处理复杂的空间和时间并行策略。

### 8. 不足与局限
*   **控制逻辑抽象**：作为数据流建模工具，对 GPU 或 TPU 中复杂的控制流机制（如 Warp 调度、分支预测）做了简化处理，可能存在一定的精度偏差。
*   **硬件验证局限**：虽然 Stream 框架在推理上经过验证，但 MONET 在**实际训练芯片**上的周期精确度（Cycle-accuracy）验证在文中体现较少。
*   **应用限制**：目前主要针对静态计算图，对于具有动态形状（Dynamic Shapes）或复杂条件分支的训练任务支持尚不明确。

（完）
