---
title: "stratum: A System Infrastructure for Massive Agent-Centric ML Workloads"
title_zh: stratum：面向大规模以智能体为中心的机器学习工作负载的系统基础设施
authors: "Arnab Phani, Elias Strauss, Sebastian Schelter"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03589v2"
tags: ["query:mlsys"]
score: 9.0
evidence: 面向大规模智能体中心化机器学习负载的系统基础设施
tldr: 随着大语言模型的发展，智能体驱动的机器学习流水线搜索成为新趋势，但现有的 Python 生态系统因其交互式和顺序执行的设计，难以应对智能体产生的大规模、高并发工作负载。本文提出 Stratum 系统基础设施，通过将流水线执行与规划解耦，将批量流水线编译为优化执行图，并引入 Rust 编写的高性能运行时，实现了与现有 Python 库的无缝集成。Stratum 显著提升了智能体搜索效率，为大规模自动化机器学习提供了关键的底层支撑。
selection_source: fresh_fetch
motivation: 现有的 Python 机器学习生态系统主要为人类交互设计，无法高效处理 LLM 智能体在自动化流水线搜索中产生的大规模、迭代式工作负载。
method: 提出 Stratum 架构，通过解耦执行与规划、编译批量流水线为优化执行图，并利用 Rust 编写的高性能运行时来加速处理。
result: 初步实验结果显示，Stratum 在处理大规模智能体驱动的流水线搜索任务时，性能提升最高可达 16.6 倍。
conclusion: Stratum 成功解决了智能体驱动的 ML 工作负载与现有系统基础设施之间的不匹配问题，为高效的自动化机器学习搜索提供了新方案。
---

## 摘要
大语言模型（LLM）的最新进展改变了机器学习（ML）流水线的开发和评估方式。LLM 催生了一种新型工作负载——智能体流水线搜索（agentic pipeline search），其中自主或半自主智能体负责生成、验证和优化完整的 ML 流水线。这些智能体主要在流行的 Python ML 库上运行，并表现出高度的探索性行为。这导致了成千上万次用于数据分析、流水线生成以及流水线阶段迭代优化的执行。然而，现有的基于 Python 的 ML 生态系统是围绕 Pandas 和 scikit-learn 等库构建的，这些库专为以人为中心、交互式、顺序的工作流而设计，并受到 Python 解释执行模型、库级隔离以及对执行大量流水线的有限运行时支持的限制。同时，系统社区提出的许多高性能 ML 系统要么针对狭窄的工作负载类别，要么需要专门的编程模型，这限制了它们与 Python ML 生态系统的集成，并使其在很大程度上不适用于基于 LLM 的智能体。这种日益增长的不匹配暴露了在支持大规模智能体流水线搜索方面的一个根本性系统挑战。因此，我们提出了 stratum，这是一个统一的系统基础设施，它在智能体流水线搜索过程中将流水线执行与规划和推理相解耦。Stratum 与现有的 Python 库无缝集成，将成批的流水线编译成优化的执行图，并在包括新型基于 Rust 的运行时在内的异构后端上高效执行。我们展示了 stratum 的架构愿景以及早期原型，讨论了关键的设计决策，并概述了开放的挑战和研究方向。最后，初步实验表明，stratum 可以显著加速大规模智能体流水线搜索，最高可达 16.6 倍。

## Abstract
Recent advances in large language models (LLMs) transform how machine learning (ML) pipelines are developed and evaluated. LLMs enable a new type of workload, agentic pipeline search, in which autonomous or semi-autonomous agents generate, validate, and optimize complete ML pipelines. These agents predominantly operate over popular Python ML libraries and exhibit highly exploratory behavior. This results in thousands of executions for data profiling, pipeline generation, and iterative refinement of pipeline stages. However, the existing Python-based ML ecosystem is built around libraries such as Pandas and scikit-learn, which are designed for human-centric, interactive, sequential workflows and remain constrained by Python's interpretive execution model, library-level isolation, and limited runtime support for executing large numbers of pipelines. Meanwhile, many high-performance ML systems proposed by the systems community either target narrow workload classes or require specialized programming models, which limits their integration with the Python ML ecosystem and makes them largely ill-suited for LLM-based agents. This growing mismatch exposes a fundamental systems challenge in supporting agentic pipeline search at scale. We therefore propose stratum, a unified system infrastructure that decouples pipeline execution from planning and reasoning during agentic pipeline search. Stratum integrates seamlessly with existing Python libraries, compiles batches of pipelines into optimized execution graphs, and efficiently executes them across heterogeneous backends, including a novel Rust-based runtime. We present stratum's architectural vision along with an early prototype, discuss key design decisions, and outline open challenges and research directions. Finally, preliminary experiments show that stratum can significantly speed up large-scale agentic pipeline search up to 16.6x.

---

## 论文详细总结（自动生成）

这是一份关于论文《stratum: A System Infrastructure for Massive Agent-Centric ML Workloads》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：随着大语言模型（LLM）的发展，出现了“机器学习工程智能体”（MLE Agents）。这些智能体通过自主生成、验证和迭代成千上万个 Python 机器学习流水线（Pipelines）来寻找最优解。
*   **核心问题**：现有的 Python ML 生态系统（如 Pandas, scikit-learn）是为人类交互式、顺序操作设计的。当面对智能体产生的大规模、高并发、高度冗余的探索性工作负载时，存在以下瓶颈：
    *   **执行低效**：Python 的全局解释器锁（GIL）限制了并发。
    *   **资源浪费**：缺乏跨流水线的全局优化，导致重复计算和内存溢出（OOM）。
    *   **系统不匹配**：高性能 ML 系统通常需要专门的 DSL，而 LLM 智能体更擅长生成通用的 Python 代码。
*   **整体含义**：本文提出了 **stratum**，这是一个专门为“以智能体为中心”的工作负载设计的系统基础设施，旨在弥合高层 Python API 与底层高性能执行之间的鸿沟。

### 2. 方法论
*   **核心思想**：将流水线的“规划（智能体负责）”与“执行（系统负责）”解耦。Stratum 接收批量流水线，将其编译为统一的执行图并进行全局优化。
*   **关键技术细节**：
    *   **声明式抽象**：利用 `skrub` 库将任意 Python ML 代码包装成延迟计算的有向无环图（DAG），称为 DataOps。
    *   **逻辑优化器**：
        *   **元数据收集**：提取数据形状、类型等信息。
        *   **重写规则**：应用谓词下推、列裁剪、**公共子表达式消除（CSE）**（识别并合并不同流水线间的重复步骤）。
        *   **算子下放（Lowering）**：将复杂算子拆解为细粒度步骤，以扩大优化空间。
    *   **算子选择与 Rust 后端**：
        *   提供多层物理实现，优先选择基于 **Rust** 编写的高性能内核。
        *   Rust 后端支持显式内存管理、零拷贝数据访问，并释放 GIL 以实现真正的原生并行。
    *   **运行时优化**：
        *   **多级并行**：结合算子间（Inter-op）和算子内（Intra-op）并行。
        *   **中间结果复用**：建立细粒度的缓存机制，跨迭代复用已计算的中间特征或模型状态。

### 3. 实验设计
*   **数据集/场景**：使用 Kaggle 上的英国房价预测数据集（UK Housing dataset），模拟一个真实的智能体流水线搜索场景。
*   **工作负载**：由 AIDE 智能体生成的两轮迭代：第一轮探索多种预处理和模型组合；第二轮进行超参数微调。
*   **Benchmark 与对比方法**：
    *   `Base`：AIDE 默认的顺序执行模式。
    *   `Base_par`：基于 Python 多进程（multiprocessing）的朴素并行模式。
    *   `stratum`：开启所有优化的本文系统。

### 4. 资源与算力
*   **硬件环境**：单节点服务器，配备 **AMD EPYC 7443P CPU**（24 核心/48 线程），**256 GB RAM**。
*   **算力说明**：论文重点在于 CPU 上的表格数据处理优化，**未提及 GPU 使用情况**（这是其未来针对 DNN 负载的研究方向）。

### 5. 实验数量与充分性
*   **实验规模**：进行了端到端性能对比实验（改变数据规模从 200k 到 1M 行）以及消融实验（逐步开启逻辑优化、算子选择、并行化）。
*   **充分性评价**：作为一篇 **Vision（愿景）型论文**，其实验部分属于初步验证（Early Prototype）。虽然涵盖了扩展性和核心组件分析，但目前仅针对单一智能体（AIDE）和单一典型任务（表格回归），实验的多样性尚有提升空间，但对于证明其设计理念的有效性是充分且客观的。

### 6. 论文的主要结论与发现
*   **显著加速**：Stratum 相比顺序执行（Base）实现了最高 **16.6 倍** 的加速，相比朴素并行（Base_par）实现了 **7.8 倍** 的加速。
*   **内存效率**：朴素并行模式由于进程间数据复制导致内存消耗激增（高出 8 倍），而 Stratum 通过线程级并行和共享内存有效控制了内存开销。
*   **优化贡献**：消融实验显示，**公共子表达式消除（CSE）** 和 **原生算子选择（Rust/Polars）** 是性能提升的主要来源。

### 7. 优点
*   **无缝集成**：不需要智能体改变编程习惯，直接支持现有的 Python ML 库（scikit-learn, Pandas）。
*   **全局视角**：通过批量融合流水线，捕捉到了人类开发者难以察觉的跨任务冗余。
*   **高性能后端**：引入 Rust 解决了 Python 长期以来的执行效率和并发瓶颈。

### 8. 不足与局限
*   **原型阶段**：成本估算（Cost Estimation）目前依赖启发式规则，尚不够精确。
*   **覆盖范围限制**：目前主要针对表格数据和 CPU 负载，对深度学习（DNN）和 GPU 算子的集成仍处于探索阶段。
*   **黑盒 UDF 挑战**：对于完全自定义的 Python 函数（UDF），系统仍将其视为黑盒，限制了进一步的逻辑优化。
*   **智能体协同**：目前系统是被动接收流水线，尚未实现智能体与系统之间深度的“系统感知型”双向交互。

（完）
