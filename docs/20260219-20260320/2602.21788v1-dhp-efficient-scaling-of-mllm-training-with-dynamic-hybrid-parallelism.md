---
title: "DHP: Efficient Scaling of MLLM Training with Dynamic Hybrid Parallelism"
title_zh: DHP：利用动态混合并行实现多模态大语言模型训练的高效扩展
authors: "Yifan Niu, Han Xiao, Dongyi Liu, Wei Zhou, Jia Li"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.21788v1"
tags: ["query:mlsys"]
score: 10.0
evidence: 用于扩展多模态大模型训练的动态混合并行
tldr: 针对多模态大模型（MLLM）训练中由于数据异构性导致的负载不均衡和硬件利用率低的问题，本文提出了动态混合并行（DHP）策略。DHP能够根据训练批次动态调整通信组和并行度，并支持非2幂次的并行配置。通过多项式时间算法生成近优策略，DHP在保持极低开销的同时，显著提升了长文本多模态数据的训练效率，在NPU集群上实现了高达1.36倍的吞吐量提升。
selection_source: fresh_fetch
motivation: 现有的静态并行策略在处理高度异构的多模态数据时，存在严重的负载不均衡和冗余通信问题。
method: 提出动态混合并行（DHP）框架，通过多项式时间算法动态配置非2幂次的并行度与通信组。
result: 在大规模NPU集群上，DHP相比Megatron-LM和DeepSpeed实现了高达1.36倍的训练吞吐量提升。
conclusion: DHP通过自适应调整并行策略，有效解决了多模态模型训练中的异构性挑战，实现了高效的线性扩展。
---

## 摘要
扩展长上下文能力对于多模态大语言模型（MLLM）至关重要。然而，现实世界中的多模态数据集具有极强的异构性。现有的训练框架主要依赖静态并行策略，在数据异构的情况下，这些策略面临严重的负载不均衡、冗余通信以及硬件利用率低的问题。在这项工作中，我们提出了动态混合并行（DHP），这是一种在 MLLM 训练过程中自适应重新配置通信组和并行度的并行策略。我们将非 2 的幂次并行度进行了泛化，并开发了一种多项式时间算法，能够以每训练批次仅毫秒级的开销生成近乎最优的并行策略。即使在极端的数据变异性下，DHP 也能保持较高的硬件效率。实验结果表明，DHP 的性能显著优于 Megatron-LM 和 DeepSpeed，在保持大规模 NPU 集群近线性扩展效率的同时，实现了高达 1.36 倍的训练吞吐量提升。

## Abstract
Scaling long-context capabilities is crucial for Multimodal Large Language Models (MLLMs). However, real-world multimodal datasets are extremely heterogeneous. Existing training frameworks predominantly rely on static parallelism strategies, which suffer from severe load imbalance, redundant communication, and suboptimal hardware utilization under data heterogeneity. In this work, we propose Dynamic Hybrid Parallelism (DHP), an efficient parallelism strategy that adaptively reconfigures communication groups and parallelism degrees during MLLM training. We generalize the non-power-of-two parallelism degrees and develop a polynomial-time algorithm to generate near-optimal parallelism strategies with only millisecond-level overhead per training batch. DHP is able to maintain high hardware efficiency even under extreme data variability. Experimental results demonstrate that DHP significantly outperforms Megatron-LM and DeepSpeed, achieving up to 1.36 $\times$ speedup in training throughput while maintaining near-linear scaling efficiency across large-scale NPU clusters.

---

## 论文详细总结（自动生成）

### 论文总结：DHP：利用动态混合并行实现多模态大语言模型训练的高效扩展

#### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）在处理高分辨率图像和长视频时，面临计算复杂度随序列长度呈平方增长（$O(L^2)$）的挑战。现实世界的多模态数据（如视频时长）呈现极端的异构性和长尾分布。
*   **现有痛点**：传统的静态并行框架（如 Megatron-LM, DeepSpeed）采用固定的并行策略（通常要求并行度为 2 的幂次），在处理长短不一的异构序列时，会导致严重的负载不均衡、硬件空闲（Bubble）以及短序列在过大并行组中的冗余通信。
*   **研究目标**：开发一种能够根据数据特征动态调整并行度、消除负载不均衡并最大化硬件利用率的训练框架。

#### 2. 方法论：核心思想与关键技术
DHP 提出了**动态混合并行（Dynamic Hybrid Parallelism）**，其核心在于为每个微批次（micro-batch）自适应地重新配置通信组和并行度。
*   **非 2 幂次并行度**：利用 Ring-style 上下文并行（CP）的灵活性，支持任意整数并行度，从而更精细地匹配序列长度，减少冗余通信。
*   **两阶段调度算法**：
    1.  **内存感知序列打包（BFD）**：使用“最佳适应递减”（Best-Fit Decreasing）策略，将异构序列打包成“原子序列组”，以减少决策变量并防止内存碎片。
    2.  **2D 动态规划（DP）资源分配**：通过求解一个最小化最大执行时间（Makespan）的优化问题，决定每个组的最优并行度。该算法复杂度为 $O(K'N^2)$，耗时仅为毫秒级。
*   **异步调度实现**：将调度逻辑（CPU 执行）与模型计算（NPU 执行）解耦。在 NPU 计算当前批次时，CPU 预先计算下一批次的并行策略，并利用“通信组池”缓存已创建的通信环，实现调度开销的完全掩盖。

#### 3. 实验设计
*   **数据集**：使用了三个具有不同分布特征的多模态数据集：**MSRVTT**（相对均匀）、**InternVid**（大规模视频）、**OpenVid**（高度异构、长尾分布）。
*   **基准模型**：InternVL3 (2B, 8B)、InternVL2.5 (4B) 以及 Qwen3VL (2B, 4B, 8B)。
*   **对比方法（Baselines）**：
    *   **Megatron-LM**：支持 4D 并行（TP, PP, DP, CP）。
    *   **DeepSpeed**：支持 ZeRO 优化和 Ulysses-style 序列并行。
*   **评估指标**：端到端训练迭代时间、单设备 Token 吞吐量、扩展效率。

#### 4. 资源与算力
*   **硬件环境**：实验在由 **8 个节点**组成的集群上完成。
*   **单节点配置**：每个节点配备 **8 张昇腾（Ascend）910B NPU**（每张 64GB 显存），节点内通过 HCCS 互联。
*   **集群规模**：总计 **64 张 NPU**。
*   **网络**：节点间通过 100 Gbps InfiniBand 网络连接。
*   **训练时长**：文中未明确给出总训练时长，但记录了预热 5 步后连续 10 步的平均性能数据。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 种模型规模（2B 到 8B）与 3 种数据集的组合，共计 **18 组端到端性能对比**。
*   **维度丰富性**：
    *   **扩展性实验**：测试了从 8 到 64 张 NPU 的扩展效率。
    *   **阶段性实验**：对比了冻结与不冻结视觉编码器时的性能表现。
    *   **开销分析**：详细记录了调度器（Scheduler）和求解器（Solver）的耗时。
    *   **误差测试**：验证了成本估算器（Cost Estimator）的准确性（误差 < 8%）。
*   **评价**：实验设计较为全面，通过不同分布的数据集验证了动态并行的必要性，对比基准均为工业界主流框架，结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **显著加速**：DHP 在所有配置下均优于基准框架，最高实现 **1.36 倍**的吞吐量提升。
*   **数据敏感性**：在数据异构性越高的场景（如 OpenVid），DHP 的优势越明显。
*   **高效扩展**：在从 8 NPU 扩展到 64 NPU 时，DHP 保持了近乎线性的扩展效率，且相对基准方法的领先优势随规模扩大而增加。
*   **极低开销**：调度算法耗时（< 100ms）远小于计算耗时，且通过异步机制实现了零调度延迟。

#### 7. 优点
*   **灵活性高**：打破了并行度必须为 2 的幂次的限制，能够更精准地处理长尾数据。
*   **非侵入性设计**：作为 Megatron-LM 的插件模块实现，无需修改核心代码，易于集成到现有工作流。
*   **成本建模精准**：考虑了 MLLM 中视觉编码器的全注意力（Full Attention）与语言模型的因果注意力（Causal Attention）的差异。

#### 8. 不足与局限
*   **硬件平台局限**：实验主要基于昇腾 NPU 集群，虽然理论通用，但在 NVIDIA GPU（使用 NCCL）上的表现及通信组频繁切换的开销尚未验证。
*   **并行空间限制**：DHP 目前主要优化 CP 和 DP 的动态组合，而张量并行（TP）和流水线并行（PP）仍保持静态，这在极端模型规模下可能限制了进一步优化的空间。
*   **显存预估依赖**：调度算法高度依赖于成本估算器的准确性，对于某些动态算子或复杂的内存管理机制，预估误差可能会影响策略的最优性。

（完）
