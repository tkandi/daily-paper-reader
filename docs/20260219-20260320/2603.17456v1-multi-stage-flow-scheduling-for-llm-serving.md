---
title: Multi-stage Flow Scheduling for LLM Serving
title_zh: 面向 LLM 推理服务的多阶段流调度
authors: "Yijun Sun, Xudong Liao, Songrun Xie, Hao Chen, Han Tian, Wenxue Li, Yiming Zhang, Kai Chen"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17456v1"
tags: ["query:mlsys"]
score: 10.0
evidence: LLM推理服务基础设施的多阶段流调度
tldr: 针对大语言模型（LLM）推理中严格的首字延迟（TTFT）需求，本文提出了多阶段流调度机制MFS。在解耦架构下，复杂的KV块检索和通信流常因网络竞争导致SLO违规。MFS通过“延迟与提升”原则和反向多级队列（RMLQ）结构，近似实现最小松弛度优先调度，动态提升紧急任务优先级。实验表明，MFS在vLLM集成测试中显著提升了TTFT SLO达成率。
selection_source: fresh_fetch
motivation: 现有的LLM推理系统在多阶段工作流中缺乏阶段感知的流调度，导致网络竞争严重并破坏了首字延迟（TTFT）的SLO。
method: 提出MFS机制，利用反向多级队列（RMLQ）实现“延迟与提升”原则，在无需精确松弛度信息的情况下优先处理紧急流。
result: 在8台服务器、32块GPU的测试床及大规模仿真中，MFS将TTFT SLO达成率提高了1.2倍至2.4倍。
conclusion: MFS通过协同多阶段网络流调度，有效解决了LLM推理中的网络瓶颈问题，是提升服务质量的关键技术。
---

## 摘要
满足严格的首字延迟（TTFT）要求对于大语言模型（LLM）应用至关重要。为了提高效率，现代 LLM 推理服务系统采用具有多种并行模式的解耦架构，引入了涉及可重用 KV 块检索、集合通信和 P2D 传输的复杂多阶段工作流。来自依赖阶段的流在共享瓶颈链路上跨请求重叠，使得 TTFT 极易受到网络争用的影响，因此需要感知阶段的调度。遗憾的是，大多数现有工作以阶段无关的方式调度流，导致了不协调的争用，这是违反服务等级目标（SLO）的主要原因。在本文中，我们提出了 MFS，一种旨在最大化 TTFT SLO 达成率的整体多阶段流调度机制。其核心在于，MFS 在不需要精确掌握请求剩余松弛时间的情况下，近似了最小松弛度优先（LLF）调度策略。它通过一种基于反向多级队列（RMLQ）结构实现的“延迟与提升”（Defer-and-Promote）原则来达成这一目标。通过随着有效松弛度的减少动态提升任务优先级，MFS 优先处理松弛度较小的流，同时防止 SLO 宽松的请求过早消耗网络带宽。我们将 MFS 实现为集成到 vLLM 中的可插拔模块，并在包含 8 台服务器、32 个 GPU 的测试平台以及大规模仿真中对其进行了评估。实验结果表明，MFS 有效地优于现有最先进的基准方案，将 TTFT SLO 达成率提高了 1.2 倍至 2.4 倍。

## Abstract
Meeting stringent Time-To-First-Token (TTFT) requirements is crucial for LLM applications. To improve efficiency, modern LLM serving systems adopt disaggregated architectures with diverse parallelisms, introducing complex multi-stage workflows involving reusable KV-block retrieval, collective communication, and P2D transfer. Flows from dependent stages overlap within and across requests on shared bottleneck links, making TTFT highly susceptible to network contention and necessitating stage-aware scheduling. Unfortunately, most existing works schedule flows in a stage-agnostic manner, leading to uncoordinated contention that constitutes a primary cause of SLO violations.   In this paper, we present MFS, a holistic multi-stage flow scheduling mechanism designed to maximize TTFT SLO attainment. At its core, MFS approximates the Least-Laxity-First (LLF) scheduling policy without requiring precise knowledge of a request's remaining slack. It achieves this through a Defer-and-Promote principle implemented through a Reverse Multi-Level Queue (RMLQ) structure. By dynamically promoting task precedence as effective laxity diminishes, MFS prioritizes flows with less laxity while preventing requests with loose SLOs from prematurely consuming network bandwidth. We implement MFS as a pluggable module integrated into vLLM, and evaluate it on a 8-server, 32-GPU testbed as well as through large-scale simulations. Our results demonstrate that MFS effectively outperforms state-of-the-art baselines, improving the TTFT SLO attainment by 1.2x--2.4x.

---

## 论文详细总结（自动生成）

### 论文总结：Multi-stage Flow Scheduling for LLM Serving (MFS)

#### 1. 论文的核心问题与整体含义
随着大语言模型（LLM）推理服务向解耦架构（Prefill-Decode Disaggregation）演进，**首字延迟（TTFT）**成为衡量服务质量（SLO）的核心指标。论文指出，Prefill阶段包含三个关键的通信阶段：
*   **阶段1：KV缓存重用**（从远程获取历史KV块）。
*   **阶段2：集合通信**（模型并行产生的中间激活同步，如All-to-All）。
*   **阶段3：P2D传输**（将生成的KV缓存传给解码节点）。

**核心问题**：这三类流在共享的网络链路上存在严重的**层内与层间竞争**。现有的流调度器（如SJF、EDF）是“阶段无关”的，无法识别不同流之间的依赖关系，导致关键路径上的通信被阻塞，从而引发大规模的TTFT SLO违规。

#### 2. 论文提出的方法论：MFS 机制
MFS 的核心思想是**“延迟与提升”（Defer-and-Promote）**，旨在不依赖精确松弛度（Slack）信息的情况下，近似实现**最小松弛度优先（LLF）**调度。

*   **核心结构：反向多级队列（RMLQ）**
    与传统的MLFQ（随时间降级）相反，MFS 将流初始化在低优先级队列（强制延迟），仅在松弛度减小时才提升其优先级。
*   **关键技术细节**：
    *   **显式截止日期调度（针对阶段3）**：引入**最小链路利用率（MLU）**指标。当剩余带宽仅够勉强满足截止日期时，才触发优先级提升，实现“准时完成”而非“过早完成”，为早期阶段腾出带宽。
    *   **隐式截止日期调度（针对阶段1&2）**：利用**相对层索引（RLI）**。RLI越小（即数据越快被计算层需要），优先级越高。
    *   **鲁棒的请求间调度**：提出**鲁棒有效截止日期（RED）**，通过识别批处理中的“离群值”防止优先级劫持（Piggyback Effect），并结合**可行性检查**剔除必然违规的请求（Black Hole Effect）。
*   **系统集成**：作为可插拔模块集成到 vLLM、NCCL 和 Mooncake 中，通过 DSCP 标签在商用交换机上强制执行优先级。

#### 3. 实验设计
*   **场景与数据集**：
    *   **测试床实验**：使用 Mixtral 8x7B 模型，基于生产环境衍生的 **Qwen 轨迹**（包含对话 Conv 和智能体 Agent 两类负载）。
    *   **大规模仿真**：模拟 256 个服务器节点，涵盖多种 MoE 模型（Mixtral 8x22B, DBRX, Qwen3-Coder, Grok2）和长文本 Dense 模型（Llama3-8B）。
*   **Benchmark 与对比方法**：
    *   **Fair Sharing (FS)**：公平共享带宽。
    *   **Shortest Job First (SJF)**：短作业优先。
    *   **Earliest Deadline First (EDF)**：最早截止日期优先。
    *   **Karuna**：针对混合流的截止日期感知调度算法。

#### 4. 资源与算力
*   **物理测试床**：8 台服务器，共 **32 块 NVIDIA RTX 3090 GPU**，配备 100Gbps Mellanox NIC 和 NVIDIA SN2700 交换机。
*   **仿真环境**：基于 Vidur 和 flowsim 扩展，模拟 A100 GPU 性能配置，网络为 200Gbps Fat-tree 拓扑。
*   **训练/运行时间**：论文未明确说明具体运行总时长，但强调了 MFS 模块的轻量级特性（约 10,000 行代码）。

#### 5. 实验数量与充分性
实验设计非常充分且具有客观性：
*   **多维度评估**：涵盖了从 32 GPU 的真实物理环境到 2048 GPU 规模的仿真。
*   **模型多样性**：测试了不同专家并行（EP）和序列并行（SP）配置下的 MoE 及 Dense 模型。
*   **负载真实性**：使用了真实的生产环境轨迹（Qwen 和 Mooncake 数据集），并考虑了不同的请求到达率（RPS）。
*   **消融与微观分析**：详细分析了集合通信时间（CCT）和请求早熟度（Earliness），证明了性能提升源于对竞争的有效协调。

#### 6. 论文的主要结论与发现
*   **SLO 提升**：在各种负载下，MFS 将 TTFT SLO 达成率提高了 **1.2倍至2.4倍**。
*   **延迟降低**：相比最强的基准方案，MFS 能在保持相同 SLO 的前提下，支持高出 **1.17倍至1.46倍** 的请求吞吐量。
*   **关键发现**：通过战略性地延迟非紧急的 P2D 传输，可以显著缩短处于计算关键路径上的集合通信时间（平均缩短约 50%）。

#### 7. 优点
*   **阶段感知（Stage-aware）**：首次系统性地解决了 LLM 推理中多阶段通信流的依赖与竞争问题。
*   **实用性强**：不要求精确的松弛度预测，且能与现有的 vLLM、NCCL 等主流框架无缝集成，无需修改硬件。
*   **理论支撑**：通过数学证明（定理1）确认了“最小 RLI 优先”策略在最小化 Prefill 完工时间上的最优性。

#### 8. 不足与局限
*   **算力平台局限**：物理实验主要基于 RTX 3090，虽然仿真涵盖了 A100，但未在 H100/B200 等具备更强硬件调度特性的最新集群上进行实测。
*   **多模型部署**：讨论部分提到了多模型共享集群的潜力，但实验部分主要集中在单模型多实例场景，未深入探讨异构模型间的复杂竞争。
*   **剪枝风险**：其“选择性剪枝”机制虽然保护了系统稳定性，但在极端过载下可能会导致部分请求被过早放弃，需更精细的补偿机制。

（完）
