---
title: "Covenant-72B: Pre-Training a 72B LLM with Trustless Peers Over-the-Internet"
title_zh: Covenant-72B：通过互联网利用无需信任的对等节点预训练 72B 大语言模型
authors: "Joel Lidin, Amir Sarfi, Erfan Miahi, Quentin Anthony, Shivam Chauhan, Evangelos Pappas, Benjamin Thérien, Eugene Belilovsky, Samuel Dare"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08163v2"
tags: ["query:mlsys"]
score: 10.0
evidence: 72B大语言模型的全球分布式预训练
tldr: 本研究推出了Covenant-72B，这是目前规模最大的全球分布式预训练大语言模型。针对现有分布式训练规模较小且仅限于白名单参与者的问题，该项目利用区块链协议支持完全开放、无许可的全球协作。通过采用SparseLoCo通信高效优化器，模型在1.1T token上完成了预训练，证明了利用去中心化算力构建大规模基础模型的可行性与竞争力。
selection_source: fresh_fetch
motivation: 旨在解决现有分布式训练规模受限且缺乏真正开放性的问题，实现去中心化的全球算力协同。
method: 利用SparseLoCo优化器和区块链协议，支持全球节点在互联网环境下动态、无许可地参与72B规模模型的预训练。
result: 该模型在1.1T token上训练后，其性能表现与使用同等或更高计算预算的中心化预训练模型相当。
conclusion: 研究证明了完全民主化、非白名单的全球分布式预训练在大规模参数量级下是切实可行且高效的。
---

## 摘要
最近，全球分布式训练引起了越来越多的关注，它有望降低训练成本，并使参与构建大规模基础模型的过程更加民主化。然而，现有的以全球分布式方式训练的模型规模相对较小，且仅由白名单参与者进行训练。因此，它们尚未完全实现民主化参与的愿景。在本报告中，我们介绍了 Covenant-72B，这是一个由规模最大（无论是在计算量还是模型规模方面）的协作式全球分布式预训练运行产生的大语言模型（LLM），该运行同时允许由实时区块链协议支持的开放、无许可参与。我们利用了最先进的通信高效优化器 SparseLoCo，支持对等节点自由加入和退出的动态参与。我们的模型在约 1.1T token 上进行了预训练，其性能与在相似或更高计算预算下预训练的全集中式模型相当，这证明了完全民主化、非白名单的参与不仅是可行的，而且可以在全球分布式预训练运行中以史无前例的规模实现。

## Abstract
Recently, there has been increased interest in globally distributed training, which has the promise to both reduce training costs and democratize participation in building large-scale foundation models. However, existing models trained in a globally distributed manner are relatively small in scale and have only been trained with whitelisted participants. Therefore, they do not yet realize the full promise of democratized participation. In this report, we describe Covenant-72B, an LLM produced by the largest collaborative globally distributed pre-training run (in terms of both compute and model scale), which simultaneously allowed open, permissionless participation supported by a live blockchain protocol. We utilized a state-of-the-art communication-efficient optimizer, SparseLoCo, supporting dynamic participation with peers joining and leaving freely. Our model, pre-trained on approximately 1.1T tokens, performs competitively with fully centralized models pre-trained on similar or higher compute budgets, demonstrating that fully democratized, non-whitelisted participation is not only feasible, but can be achieved at unprecedented scale for a globally distributed pre-training run.

---

## 论文详细总结（自动生成）

以下是对论文《Covenant-72B: Pre-Training a 72B LLM with Trustless Peers Over-the-Internet》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在**互联网环境**下，利用**无需信任（Trustless）且无许可（Permissionless）**的全球分布式算力，预训练大规模（72B参数）的语言模型？
*   **研究背景**：
    *   **中心化困境**：当前最先进的LLM预训练依赖昂贵的中心化基础设施（如数千个通过高带宽低延迟互连的加速器）。
    *   **去中心化现状**：现有的分布式训练项目（如INTELLECT-1）规模较小（约10B），且通常依赖“白名单”机制（仅限受信任节点），未能真正实现算力参与的民主化。
*   **整体含义**：该研究证明了通过激进的通信压缩技术和区块链激励机制，可以在不稳定的商品互联网上，由互不信任的参与者协作训练出性能媲美中心化训练的72B大模型。

### 2. 方法论：核心思想与关键技术
该模型结合了通信高效优化器 **SparseLoCo** 和激励验证机制 **Gauntlet**。
*   **SparseLoCo（通信高效优化器）**：
    *   **核心思想**：采用局部更新（Local-update）策略。每个节点在本地运行 $H$ 步（本实验中 $H=30$）优化器，然后仅交换压缩后的“伪梯度”。
    *   **压缩技术**：结合了 **块状 Top-k 稀疏化（Chunk-wise Top-k）**、**误差反馈（Error-feedback）** 和 **2-bit 量化**。
    *   **分块策略**：将张量划分为 64x64 或 4096 的块，独立进行 Top-k 选择，这不仅降低了索引传输开销，还与 FSDP（完全分片数据并行）天然兼容。
    *   **压缩率**：相对于密集梯度通信，实现了超过 **146倍** 的压缩比。
*   **Gauntlet（激励与验证机制）**：
    *   **核心思想**：运行在 Bittensor 区块链（Subnet 3）上，通过验证者（Validator）对参与者提交的伪梯度进行评分。
    *   **评分机制（LossScore）**：验证者在小规模数据子集上计算应用该更新前后的损失函数差异。
    *   **防作弊**：通过比较分配数据与随机数据的损失改善情况，防止参与者通过复制他人结果或提交重复行为来骗取奖励。
*   **系统设计**：
    *   使用 **Cloudflare R2 对象存储** 作为通信骨干，支持异步上传和全局分发，避免了点对点连接的复杂性。
    *   **动态 FSDP**：在计算阶段和通信阶段动态换入/换出优化器状态和误差反馈缓冲区，以节省显存。

### 3. 实验设计
*   **数据集**：
    *   **预训练阶段**：约 1.09T tokens 的 DCLM 网页文本。
    *   **退火阶段（Annealing）**：14.2B tokens 的高质量混合数据（指令、代码、数学、科学等）。
    *   **SFT 阶段**：14.8B tokens 的聊天、代码、数学和 STEM 数据。
*   **Benchmark（基准测试）**：
    *   **基础模型**：ARC-C/E, PIQA, OpenBookQA, HellaSwag, WinoGrande, MMLU。
    *   **对话模型**：GSM8K, BBH CoT, IFEval, MATH, MMLU-Pro, MuSR。
*   **对比方法**：
    *   **去中心化模型**：INTELLECT-1 (10B), Psyche Consilience (40B)。
    *   **中心化模型**：LLM360 K2 (65B), LLaMA-2-7B/70B。

### 4. 资源与算力
*   **模型规模**：72.7B 参数，Llama-3 架构风格。
*   **硬件配置**：参与节点通常配备 **8× NVIDIA B200 GPU**。
*   **参与情况**：
    *   平均每轮有 **16.9 个** 贡献节点（上限设为 20 个）。
    *   整个训练过程中至少有 **70 个唯一节点（UID）** 参与。
*   **训练效率**：在 500 Mb/s 下行和 110 Mb/s 上行的带宽限制下，每轮计算 20 分钟，通信仅需 70 秒，**计算利用率高达 94.5%**。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了一个完整的 1.1T token 预训练全过程，包括主训练、退火和 SFT 三个阶段。
*   **充分性评价**：
    *   **客观性**：使用了统一的评估框架（lm-eval）对比了多个主流开源模型，结果具有参考价值。
    *   **消融/对比**：对比了退火前后的性能变化（见附录 B），并详细分析了与 INTELLECT-1 的通信效率差异。
    *   **局限性**：由于是单次大规模运行报告，缺乏针对不同压缩率或不同节点数量对最终收敛影响的系统性消融实验。

### 6. 主要结论与发现
*   **可行性**：证明了在无需信任的开放网络环境下，预训练 70B+ 规模的模型是完全可行的。
*   **竞争力**：Covenant-72B 在多个基准测试上优于或持平于中心化训练的 K2-65B 和 LLaMA-2-70B，尤其在 MMLU 和数学推理（SFT 后）方面表现突出。
*   **鲁棒性**：SparseLoCo 算法能够有效处理节点的动态加入和退出（Churn），平均贡献节点数保持稳定。

### 7. 优点
*   **规模突破**：这是目前已知的规模最大的全球分布式、无许可预训练运行。
*   **极高的通信效率**：通过块状 Top-k 和误差反馈，在极低带宽下维持了极高的计算利用率（>94%）。
*   **真正的民主化**：结合区块链激励，允许任何拥有算力的人参与，无需预先审核，降低了基础模型的准入门槛。

### 8. 不足与局限
*   **上下文长度限制**：预训练上下文仅为 2048，SFT 阶段虽扩展至 8192，但相比当前主流模型的 128k+ 仍显不足。
*   **硬件门槛**：虽然是分布式，但单个节点仍需 8×B200 级别的重型设备，对普通个人参与者仍有门槛。
*   **数据依赖**：性能受限于 DCLM 数据的质量，部分常识性任务（如 HellaSwag）略逊于在更多 token 上训练的中心化模型。
*   **验证开销**：Gauntlet 机制虽然高效，但在极端对抗环境下（如大量恶意节点）的防御上限仍需进一步验证。

（完）
