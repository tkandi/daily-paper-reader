---
title: Joint Training on AMD and NVIDIA GPUs
title_zh: AMD 与 NVIDIA GPU 上的联合训练
authors: "Jon Hu, Thomas Jia, Jing Zhu, Zhendong Yu"
date: 2026-02-20
pdf: "https://arxiv.org/pdf/2602.18007v1"
tags: ["query:mlsys"]
score: 10.0
evidence: AMD-NVIDIA环境下的LLM异构混合训练
tldr: "随着大语言模型规模扩大，单一厂商的同构集群难以满足算力需求。本文提出了一种在AMD和NVIDIA混合环境下进行异构训练的技术方案。研究首先通过基于CPU转发的通信方式确保兼容性，随后提出Device-Direct通信方法，利用CPU卸载的P2P机制实现跨厂商GPU直接数据传输。实验证明，该方案在保持训练稳定性的同时，吞吐量可达纯NVIDIA集群的98%，有效解决了异构算力协同的难题。"
selection_source: fresh_fetch
motivation: 针对大模型训练对算力需求的激增，解决单一厂商同构集群扩展受限的问题，实现AMD与NVIDIA GPU的混合训练。
method: 设计了基于CPU转发的兼容性通信方案，并进一步提出Device-Direct通信机制以实现跨厂商GPU间的直接数据传输。
result: "在LLaMA-8B和Qwen2-7B模型上的测试表明，该异构训练方案的吞吐量最高可达纯NVIDIA同构系统的98%。"
conclusion: 该方案成功实现了跨厂商GPU的高效协同训练，在保证模型正确性的前提下提供了极高的性能表现。
---

## 摘要
随着大语言模型规模的不断扩大，对计算和系统容量的训练需求迅速增长，使得单一供应商的同构集群已不足以满足需求。本文提出了一种针对 AMD-NVIDIA 环境下异构混合训练的技术解决方案。我们首先采用了一种基于 CPU 转发通信（CPU-Forwarding Communication）的面向兼容性的方法，在不同并行组之间选择差异化的通信后端，并实现多网卡并行数据传输。为了获得更高的性能，我们进一步提出了一种设备直连通信（Device-Direct Communication）方法，集成了一种 CPU 卸载 P2P 机制，实现了无需主机内存暂存的跨供应商 GPU 直接数据传输。在 LLaMA-8B 和 Qwen2-7B 上的实验表明，所提出的设备直连通信方法达到了 NVIDIA 同构系统吞吐量的 98%，同时保持了训练的稳定性和正确性。

## Abstract
As large language models continue to scale, training demands on compute and system capacity grow rapidly, making single-vendor homogeneous clusters insufficient. This paper presents a technical solution for heterogeneous mixed training in AMD-NVIDIA environments. We first adopt a compatibility-oriented approach based on CPU-Forwarding Communication, with differentiated communication back-end selection across parallel groups and multi-NIC parallel data transfer. To achieve higher performance, we further propose another Device-Direct Communication approach, integrating a CPU-offloading P2P mechanism to enable direct cross-vendor GPU data transfer without host-memory staging. Experiments on LLaMA-8B and Qwen2-7B demonstrate that the proposed Device-Direct Communication approach achieves up to 98% of the throughput of an NVIDIA homogeneous system, while preserving training stability and correctness.

---

## 论文详细总结（自动生成）

以下是对论文《Joint Training on AMD and NVIDIA GPUs》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）规模迈向万亿参数，单一厂商的同构计算集群（如纯 NVIDIA）因供应链多样化、硬件迭代周期缩短及算力需求指数级增长，已难以满足训练要求。现代数据中心正趋向于包含不同厂商（NVIDIA、AMD）和不同代际硬件的异构环境。
**核心问题：** 如何有效聚合 AMD 和 NVIDIA 这类跨厂商的异构资源，克服内存容量、互联带宽及软件生态的差异，实现高效、稳定且数值正确的联合训练。

### 2. 论文提出的方法论
论文提出了两种递进的技术方案：

*   **CPU 转发通信（CPU-Forwarding Communication）：**
    *   **核心思想：** 利用 CPU 作为中转站绕过硬件兼容性壁垒。
    *   **关键技术：** 
        1. **差异化后端选择 (DCBS)：** 在流水线并行（PP）组使用 Gloo 后端处理跨厂商通信，而在同构的流水线阶段内部（DP/TP 组）保留厂商原生库（NCCL/RCCL）以维持高性能。
        2. **多网卡并行传输 (MPDT)：** 为每个 GPU 分配独立网卡进行点对点（P2P）传输，物理上并行化 PP 流量。
*   **设备直连通信（Device-Direct Communication）：**
    *   **核心思想：** 消除 CPU 中转开销，实现跨厂商 GPU 间的直接数据交换。
    *   **关键技术：**
        1. **CPU 卸载 P2P 机制：** 将控制平面（连接管理、事件处理）留在 CPU，而数据平面通过 GPUDirect RDMA (GDR) 直接在 GPU 和网卡间传输。
        2. **多适配器架构：** 设计了 Device（硬件抽象）、Net-Plugin（网络适配）和 CCL（集合通信适配）三层架构，统一了 CUDA 和 ROCm 的底层差异。
        3. **混合调用流：** 同构子组内使用原生 CCL，跨集群同步则通过 CPU 卸载的 P2P 接口完成。

### 3. 实验设计
*   **数据集/场景：** 使用 LLaMA-8B 和 Qwen2-7B 模型进行预训练测试。
*   **Benchmark（基准）：** 
    1. **NVIDIA-Homo：** 纯 NVIDIA H200 同构节点。
    2. **AMD-Homo：** 纯 AMD MI325X 同构节点。
    3. **Global Gloo-Hetero：** 基于原生 Megatron-Gloo 的异构方案。
*   **对比维度：** 吞吐量（Throughput）、训练稳定性（500 轮迭代的波动）、数值正确性（Loss 收敛曲线）。

### 4. 资源与算力
*   **NVIDIA 节点：** 8 × NVIDIA H200 GPU，NVLink 互联（900 GB/s）。
*   **AMD 节点：** 8 × AMD Instinct MI325X GPU，Infinity Fabric 互联（128 GB/s）。
*   **网络配置：** 每个节点配备 8 × NVIDIA BlueField-3 DPU（100 GB/s）。
*   **异构规模：** 总计 16 个 GPU 跨两个物理节点。
*   **并行策略：** TP=1, PP=2, DP=8（异构模式）。

### 5. 实验数量与充分性
*   **实验组设置：** 针对两个主流模型（LLaMA 和 Qwen）分别在三种硬件配置下进行了对比实验。
*   **充分性评估：** 
    *   **性能实验：** 涵盖了吞吐量对比，证明了 Device-Direct 优于 CPU 转发方案。
    *   **稳定性实验：** 进行了 500 次迭代的连续监控，展示了异构训练的鲁棒性。
    *   **消融/对比：** 对比了模型层数分配（非对称划分）对负载均衡的影响。
    *   **评价：** 实验设计较为客观，直接对比了当前工业界最先进的两种 GPU 芯片，具有很强的实战参考价值。

### 6. 论文的主要结论与发现
*   **高性能：** Device-Direct 方案在 LLaMA-8B 上达到了纯 NVIDIA 集群 **98.2%** 的吞吐量，在 Qwen2-7B 上达到 **94.4%**。
*   **超越单厂商：** 异构混合训练的吞吐量高于纯 AMD 节点（提升约 0.9%~2.3%），证明了异构资源聚合的有效性。
*   **稳定性与正确性：** 异构训练的 Loss 收敛曲线与同构基准完全重合，且 500 轮迭代中未出现性能大幅波动或挂死。
*   **负载均衡至关重要：** 由于 AMD 和 NVIDIA 算力差异，必须采用非对称的模型层数划分（如 AMD 分配较少层）才能避免流水线气泡。

### 7. 优点
*   **实战性强：** 针对当前算力紧缺的背景，提供了一套可落地的跨厂商联合训练方案。
*   **性能损耗极低：** 通过 Device-Direct 技术将异构通信损耗降至最低，接近同构性能。
*   **架构解耦：** 适配器架构使得该方案易于扩展到其他厂商的加速器。

### 8. 不足与局限
*   **异构范围受限：** 目前仅在流水线并行（PP）组引入异构性，DP 和 TP 组仍需保持同构，这限制了更小规模集群或更复杂并行策略下的灵活性。
*   **模型划分依赖人工：** 性能最优依赖于精确的模型层数划分，缺乏自动化的负载均衡调度算法。
*   **工程复杂度：** 论文提到在开发过程中遇到了频繁的系统挂死（Hang）问题，说明跨厂商 CCL 同步在底层实现上仍存在极高的工程门槛。

（完）
