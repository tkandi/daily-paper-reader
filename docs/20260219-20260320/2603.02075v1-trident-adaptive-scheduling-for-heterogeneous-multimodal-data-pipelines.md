---
title: "Trident: Adaptive Scheduling for Heterogeneous Multimodal Data Pipelines"
title_zh: Trident：异构多模态数据流水线的自适应调度
authors: "Ding Pan, Zhuangzhuang Zhou, Long Qian, Binhang Yuan"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.02075v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 多模态AI基础设施的自适应调度
tldr: 针对多模态数据准备流水线中负载波动大、易发生内存溢出（OOM）等挑战，本文提出了 Trident 调度框架。该框架通过高斯过程回归观测算子吞吐量，利用贝叶斯优化推荐安全配置，并结合混合整数线性规划优化算子并行度与放置。在 Ray Data 上的实验表明，Trident 显著提升了 PDF 和视频处理流水线的端到端吞吐量，实现了高效的异构资源调度。
selection_source: fresh_fetch
motivation: 现有的多模态数据流水线调度器难以应对非平稳负载和异构计算环境下的性能波动及内存溢出问题。
method: 提出包含观测层、自适应层和调度层的三层闭环框架，利用高斯过程回归和混合整数线性规划动态优化算子配置与资源分配。
result: 在 PDF 和视频处理流水线上，Trident 相比静态基准分别实现了高达 2.01 倍和 1.88 倍的吞吐量提升。
conclusion: Trident 通过在线自适应优化，有效解决了异构多模态流水线中的效率与稳定性问题，显著提升了大规模 AI 数据处理的性能。
---

## 摘要
大语言模型和多模态基础模型的快速普及使得多模态数据准备流水线成为关键的 AI 基础设施。这些流水线将 CPU 密集型预处理与加速器（GPU/NPU/TPU）支持的推理交织在一起，并产生海量的中间产物。实现高吞吐量非常困难，因为工作负载具有高度的非平稳性：状态偏移（regime shifts）、依赖输入的推理以及瞬时内存峰值会导致剧烈的性能波动和内存溢出（OOM）故障。现有的调度器通常依赖于基于阈值的自动扩缩容，或者假设算子是同步且同构的，从而导致效率低下。我们提出了 Trident，这是一个针对固定资源集群上异构多模态流水线的自适应调度框架。Trident 在三个耦合层之间实现了闭环：(i) 观测层，通过带有异常过滤的高斯过程回归（Gaussian Process regression）估算异步算子的单算子可持续吞吐量；(ii) 适配层，在线检测工作负载偏移，并执行受内存限制的贝叶斯优化，以推荐 OOM 安全的配置；(iii) 调度层，通过求解混合整数线性规划，在异构计算和带宽约束下联合优化算子并行度、放置位置和配置转换，并通过滚动更新考虑冷启动开销。决策会触发样本失效和模型刷新，以保持估算结果与当前配置的一致性。Trident 在 Ray Data 上实现，与静态基准相比，在文档整理（PDF）流水线上将端到端吞吐量提升了高达 2.01 倍，在视频整理流水线上提升了 1.88 倍，且开销较低，适用于在线重新优化。

## Abstract
The rapid adoption of large language models and multimodal foundation models has made multimodal data preparation pipelines critical AI infrastructure. These pipelines interleave CPU-heavy preprocessing with accelerator-backed (GPU/NPU/TPU) inference and produce massive intermediate artifacts. Achieving high throughput is difficult because workloads are highly non-stationary: regime shifts, input-dependent inference, and transient memory spikes cause rapid performance fluctuations and out-of-memory (OOM) failures. Existing schedulers typically rely on threshold-based autoscaling or assume synchronous, homogeneous operators, leading to poor efficiency. We present Trident, an adaptive scheduling framework for heterogeneous multimodal pipelines on fixed-resource clusters. Trident closes the loop across three coupled layers: (i) an observation layer that estimates per-operator sustainable throughput for asynchronous operators via Gaussian Process regression with anomaly filtering; (ii) an adaptation layer that detects workload shifts online and performs memory-constrained Bayesian optimization to recommend OOM-safe configurations; and (iii) a scheduling layer that solves a mixed-integer linear program to jointly optimize operator parallelism, placement, and configuration transitions under heterogeneous compute and bandwidth constraints, accounting for cold-start overhead via rolling updates. Decisions trigger sample invalidation and model refresh to keep estimates consistent with the active configuration. Implemented on Ray Data, Trident improves end-to-end throughput by up to 2.01x on a document curation (PDF) pipeline and 1.88x on a video curation pipeline over a static baseline, with low overhead suitable for online re-optimization.

---

## 论文详细总结（自动生成）

### 论文总结：Trident：异构多模态数据流水线的自适应调度

#### 1. 论文的核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）和多模态基础模型（MFM）的爆发，高质量训练数据的准备（如处理数 PB 的文档、图像和视频）成为关键瓶颈。多模态数据流水线具有以下挑战：
*   **高度非平稳性**：工作负载随输入内容（如视频长度、文档复杂度）剧烈波动，且推理算子常伴随瞬时内存峰值，易导致内存溢出（OOM）。
*   **异步算子建模难**：现代算子（如 LLM 推理）通常采用连续批处理（Continuous Batching）和异步执行，传统的基于“有用时间”的同步性能估算方法失效。
*   **资源异构与受限**：流水线交织了 CPU 密集型（预处理）和加速器密集型（推理）任务，且在固定资源集群中，算子并行度、节点放置和配置选择之间存在复杂的权衡。
**Trident** 旨在通过一个三层闭环自适应框架，在保证内存安全的前提下，最大化异构集群上的端到端吞吐量。

#### 2. 论文提出的方法论：核心思想与关键技术
Trident 提出了一个由三个紧密耦合层组成的闭环控制系统：
*   **观测层（Observation Layer）**：
    *   **核心思想**：利用**高斯过程回归（GPR）**建立从工作负载特征到吞吐量的非线性映射。
    *   **关键技术**：引入**两阶段异常过滤**。第一阶段基于信号（如利用率、队列长度）剔除因上游饥饿或下游背压导致的非稳态样本；第二阶段基于模型残差剔除离群点，确保估算的是算子的“可持续容量”。
*   **适配层（Adaptation Layer）**：
    *   **核心思想**：在线识别负载模式并优化算子配置（如 Batch Size）。
    *   **关键技术**：使用**在线聚类**跟踪负载偏移；采用**受内存约束的贝叶斯优化（Constrained BO）**，将峰值内存视为黑盒约束，在探索高性能配置的同时最小化 OOM 风险。
*   **调度层（Scheduling Layer）**：
    *   **核心思想**：将资源分配建模为**混合整数线性规划（MILP）**。
    *   **关键技术**：联合优化并行度、算子放置（考虑带宽约束和协同放置以减少跨节点传输）以及配置转换。引入**滚动更新机制**，权衡配置切换带来的吞吐量增益与冷启动（重启）开销。

#### 3. 实验设计
*   **数据集与场景**：
    1.  **PDF 流水线**：包含文件 I/O、布局检测、OCR（基于 LLM）等 17 个算子，处理约 20 万份学术论文、年报等文档。
    2.  **视频流水线**：包含场景切分、美学过滤、文本检测、字幕生成（Qwen2.5-VL）等 9 个算子，处理约 41 万个视频剪辑。
*   **Benchmark（对比方法）**：
    *   **Static**：手动调优的固定分配。
    *   **Ray Data**：默认的基于阈值的自动扩缩容。
    *   **DS2 / ContTune**：基于同步假设或保守贝叶斯优化的流处理调度器。
    *   **SCOOT**：针对 LLM 推理算子的离线参数调优方法。

#### 4. 资源与算力
*   **硬件环境**：实验在一个由 **8 台服务器**组成的集群上运行。
*   **单机配置**：每台服务器配备 **8 块华为昇腾（Ascend）910B NPU**（共 64 块 NPU）、256 核 CPU 和 1 TB 内存。
*   **网络**：100 Gbps 互联。
*   **软件**：基于 Ray Data v2.46.0 扩展实现。

#### 5. 实验数量与充分性
论文进行了多维度的深入实验：
*   **端到端性能对比**：在两个大型流水线上验证了整体吞吐量提升。
*   **调度层受控对比**：固定观测和适配输入，专门验证 MILP 调度的优越性。
*   **准确性验证**：测试了观测层对异步算子吞吐量估算的误差（MAPE）。
*   **安全性验证**：测试了适配层在配置搜索过程中对 OOM 事件的抑制效果。
*   **消融实验**：分别移除了观测层、适配层、放置感知和滚动更新，验证各组件贡献。
*   **系统开销分析**：测量了 MILP 求解时间和各层运行延迟。
**评价**：实验设计非常充分且客观，通过受控变量法清晰地展示了 Trident 相比现有系统在处理多模态异步负载时的优势。

#### 6. 论文的主要结论与发现
*   **吞吐量大幅提升**：相比静态基准，Trident 在 PDF 流水线上实现 **2.01x** 提升，在视频流水线上实现 **1.88x** 提升。
*   **建模准确性**：Trident 的两阶段过滤 GPR 模型将容量估算误差降至 **5% 以下**，远优于传统方法（误差 >50%）。
*   **内存安全性**：受约束的贝叶斯优化减少了约 **80% 的 OOM 事件**，显著降低了因重启导致的系统停机时间。
*   **调度效率**：MILP 求解在秒级内完成，对于分钟级的重新优化周期而言，开销几乎可以忽略不计。

#### 7. 优点
*   **闭环系统设计**：将观测、适配和调度整合，解决了配置更改与性能建模之间的不一致性问题。
*   **处理异步性**：专门针对现代 AI 算子的异步和动态批处理特性设计了鲁棒的观测模型。
*   **内存安全意识**：在追求高性能的同时，将内存溢出作为硬约束纳入在线优化过程，这在生产环境中极具实用价值。
*   **异构资源联合优化**：不仅考虑并行度，还考虑了节点放置和网络带宽，适合大规模分布式环境。

#### 8. 不足与局限
*   **拓扑限制**：目前的 MILP 模型主要针对线性数据流（Linear Dataflow），虽然提到支持 DAG，但在复杂分支和循环拓扑下的表现未详细讨论。
*   **冷启动假设**：冷启动开销（$h_{cold}$）被视为已知常数，但在极端负载下，容器启动和模型加载时间可能会有波动。
*   **特征工程依赖**：观测层和适配层依赖于预定义的算子特征（如 token 长度、分辨率），对于全新类型的算子，可能需要人工介入定义特征向量。

（完）
