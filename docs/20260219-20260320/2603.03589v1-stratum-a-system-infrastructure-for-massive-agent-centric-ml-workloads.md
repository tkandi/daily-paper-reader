---
title: "stratum: A System Infrastructure for Massive Agent-Centric ML Workloads"
title_zh: stratum：面向大规模以智能体为中心的机器学习工作负载的系统基础设施
authors: "Arnab Phani, Elias Strauss, Sebastian Schelter"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03589v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 针对大规模机器学习工作负载和流水线优化的系统基础设施
tldr: 随着大语言模型的发展，智能体驱动的机器学习流水线搜索成为新趋势，但现有的 Python 生态系统因其交互式和顺序执行的设计，难以应对智能体产生的大规模、高并发工作负载。本文提出 Stratum 系统基础设施，通过将流水线执行与规划解耦，将批量流水线编译为优化执行图，并引入 Rust 编写的高性能运行时，实现了与现有 Python 库的无缝集成。实验证明，Stratum 能显著提升智能体搜索效率，最高可达 16.6 倍加速。
selection_source: fresh_fetch
motivation: 现有的 Python 机器学习生态系统主要为人类交互设计，无法高效处理 LLM 智能体在自动化流水线搜索中产生的大规模、迭代式工作负载。
method: 提出 Stratum 架构，通过解耦执行与规划、编译批量流水线为优化执行图，并利用 Rust 编写的高性能运行时来加速处理。
result: 初步实验结果显示，Stratum 在大规模智能体流水线搜索任务中相比现有方案可实现高达 16.6 倍的性能提升。
conclusion: Stratum 成功解决了智能体驱动的 ML 工作负载与现有系统基础设施之间的不匹配问题，为大规模自动化机器学习提供了高效支撑。
---

## 摘要
大语言模型（LLM）的最新进展改变了机器学习（ML）流水线的开发和评估方式。LLM 催生了一种新型工作负载——智能体流水线搜索（agentic pipeline search），其中自主或半自主智能体负责生成、验证和优化完整的 ML 流水线。这些智能体主要在流行的 Python ML 库上运行，并表现出高度的探索性行为。这导致了成千上万次用于数据分析、流水线生成以及流水线阶段迭代优化的执行。然而，现有的基于 Python 的 ML 生态系统围绕 Pandas 和 scikit-learn 等库构建，这些库是为以人为中心、交互式、顺序的工作流设计的，并受到 Python 解释执行模型、库级隔离以及对执行大量流水线的运行时支持有限等因素的制约。同时，系统社区提出的许多高性能 ML 系统要么针对狭窄的工作负载类别，要么需要专门的编程模型，这限制了它们与 Python ML 生态系统的集成，使其在很大程度上不适用于基于 LLM 的智能体。这种日益增长的不匹配暴露了在支持大规模智能体流水线搜索方面的一个根本性系统挑战。因此，我们提出了 stratum，这是一个统一的系统基础设施，它在智能体流水线搜索过程中将流水线执行与规划和推理相解耦。Stratum 与现有的 Python 库无缝集成，将成批的流水线编译为优化的执行图，并在包括新型基于 Rust 的运行时在内的异构后端上高效执行。我们展示了 stratum 的架构愿景及早期原型，讨论了关键的设计决策，并概述了开放的挑战和研究方向。最后，初步实验表明，stratum 可以显著加速大规模智能体流水线搜索，提升幅度高达 16.6 倍。

## Abstract
Recent advances in large language models (LLMs) transform how machine learning (ML) pipelines are developed and evaluated. LLMs enable a new type of workload, agentic pipeline search, in which autonomous or semi-autonomous agents generate, validate, and optimize complete ML pipelines. These agents predominantly operate over popular Python ML libraries and exhibit highly exploratory behavior. This results in thousands of executions for data profiling, pipeline generation, and iterative refinement of pipeline stages. However, the existing Python-based ML ecosystem is built around libraries such as Pandas and scikit-learn, which are designed for human-centric, interactive, sequential workflows and remain constrained by Python's interpretive execution model, library-level isolation, and limited runtime support for executing large numbers of pipelines. Meanwhile, many high-performance ML systems proposed by the systems community either target narrow workload classes or require specialized programming models, which limits their integration with the Python ML ecosystem and makes them largely ill-suited for LLM-based agents. This growing mismatch exposes a fundamental systems challenge in supporting agentic pipeline search at scale. We therefore propose stratum, a unified system infrastructure that decouples pipeline execution from planning and reasoning during agentic pipeline search. Stratum integrates seamlessly with existing Python libraries, compiles batches of pipelines into optimized execution graphs, and efficiently executes them across heterogeneous backends, including a novel Rust-based runtime. We present stratum's architectural vision along with an early prototype, discuss key design decisions, and outline open challenges and research directions. Finally, preliminary experiments show that stratum can significantly speed up large-scale agentic pipeline search up to 16.6x.

---

## 论文详细总结（自动生成）

以下是对论文《stratum: A System Infrastructure for Massive Agent-Centric ML Workloads》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：随着大语言模型（LLM）驱动的机器学习工程智能体（MLE Agents）的兴起，产生了一种全新的工作负载——**大规模智能体流水线搜索**。这种工作负载具有高频率、高冗余、高度探索性的特点，每秒可能生成数千个 Python 脚本。
*   **研究背景**：现有的 Python ML 生态系统（如 Pandas, scikit-learn）是为人类交互式、顺序执行设计的，存在 Python 全局解释器锁（GIL）限制、库级隔离、缺乏整体优化等问题。而现有的高性能 ML 系统（如 SystemDS）通常需要专门的编程语言，难以被基于 LLM 的智能体直接利用。
*   **整体含义**：论文提出了 **stratum**，旨在构建一个专门针对智能体工作负载的系统基础设施，通过将“规划/推理”与“执行”解耦，在保持与现有 Python 库兼容的同时，实现大规模流水线的高效编译与执行。

### 2. 论文提出的方法论
*   **核心思想**：将成批的、由智能体生成的流水线视为一个整体，通过延迟计算（Lazy Evaluation）构建统一的有向无环图（DAG），并应用数据库式的逻辑优化和高性能运行时技术。
*   **关键技术细节**：
    *   **声明式抽象**：利用 `skrub` 库将任意 Python ML 代码包装成 `DataOps` 算子，形成可优化的 DAG。
    *   **逻辑优化器**：进行元数据收集，应用规则重写（如谓词下推、列裁剪、常量折叠），特别是通过**公共子表达式消除（CSE）**来消除不同流水线间的冗余计算。
    *   **算子下放（Lowering）**：将高层算子（如交叉验证、自动特征编码）拆解为细粒度算子，以扩大优化空间。
    *   **算子选择**：根据成本模型，在多个后端（如原生 Rust 内核、Polars、scikit-learn）中选择最优实现。
    *   **Rust 后端**：使用 Rust 编写高性能内核，通过 `PyO3` 与 Python 交互，释放 GIL 实现真正的多线程并行，并支持零拷贝数据访问。
    *   **运行时优化**：包括算子间/算子内并行调度、中间结果缓存（基于哈希校验的粗粒度和细粒度重用）。

### 3. 实验设计
*   **数据集/场景**：使用 Kaggle 上的英国房价数据集（UK Housing dataset），数据规模从 20 万行缩放到 100 万行。
*   **工作负载**：模拟 AIDE 智能体生成的真实工作负载，包含两轮迭代：第一轮探索多种预处理策略与模型的组合，第二轮进行超参数调优。
*   **Benchmark 与对比方法**：
    *   `Base`：顺序执行（AIDE 默认模式）。
    *   `Base_par`：基于多进程的朴素并行执行。
    *   `stratum`：开启所有优化的本文系统。
*   **消融实验**：分别测试了逻辑优化、算子选择和算子间并行对性能的贡献。

### 4. 资源与算力
*   **硬件环境**：单节点服务器，配备 **AMD EPYC 7443P CPU**（24 个物理核心 / 48 个线程），**256 GB RAM**。
*   **软件环境**：Ubuntu 20.04, Python 3.11, scikit-learn 1.8, skrub 0.6.2。
*   **算力说明**：文中未提及 GPU 使用情况，因为该 Vision 论文目前主要针对表格数据和 CPU 密集型的 ML 任务。

### 5. 实验数量与充分性
*   **实验规模**：主要进行了端到端性能对比实验（随数据规模变化）和一组详细的消融实验。
*   **充分性评价**：作为一篇 **Vision（愿景）类论文**，其实验部分旨在验证核心设计的可行性。虽然实验场景较单一（仅一个数据集和一种回归任务），但它使用了真实的智能体工作负载（AIDE），对比了工业界常用的并行方案，初步证明了其设计的有效性。

### 6. 论文的主要结论与发现
*   **显著加速**：在 100 万行数据规模下，`stratum` 比顺序执行快 **16.6 倍**，比朴素并行快 **7.8 倍**。
*   **内存效率**：朴素并行（Base_par）由于多进程数据复制，内存消耗是 `stratum` 的 8 倍。
*   **优化贡献**：消融实验显示，逻辑优化（如 CSE）带来了约 2.2 倍加速，而引入高性能算子选择（Rust/Polars）进一步将加速比提升至 4.5 倍以上。

### 7. 优点
*   **无缝集成**：不需要智能体改变编写 Python 代码的习惯，通过包装现有库实现优化。
*   **整体视角**：不同于传统系统只优化单个流水线，`stratum` 能够跨流水线批次进行冗余消除。
*   **高性能运行时**：通过 Rust 后端有效解决了 Python 在处理大规模并发任务时的性能瓶颈。

### 8. 不足与局限
*   **原型阶段**：目前 Rust 内核库还不完整，仅覆盖了部分常用算子。
*   **成本估算困难**：由于 Python 库的黑盒特性，准确估算算子的内存和计算成本仍具挑战。
*   **应用范围限制**：目前主要针对表格数据，对深度学习（DNN）和 GPU 负载的支持尚处于早期探索阶段。
*   **UDF 优化受限**：对于用户自定义函数（UDF），系统仍将其视为黑盒，限制了进一步的语义优化。

（完）
