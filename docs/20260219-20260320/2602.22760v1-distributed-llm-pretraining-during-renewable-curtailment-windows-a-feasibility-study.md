---
title: "Distributed LLM Pretraining During Renewable Curtailment Windows: A Feasibility Study"
title_zh: 可再生能源弃电窗口期间的分布式大语言模型预训练：可行性研究
authors: "Philipp Wiesner, Soeren Becker, Brett Cornick, Dominik Scheinert, Alexander Acker, Odej Kao"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.22760v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 跨地理分布式GPU集群的全参数大模型训练系统
tldr: "本研究针对大语言模型（LLM）训练的高能耗问题，提出利用可再生能源弃电窗口进行地理分布式预训练。通过开发一套弹性系统，在多个 GPU 集群间根据弃电时段动态切换本地训练与联邦同步模式。实验表明，该方法在保证 561M 参数模型训练质量的同时，将运营碳排放降至单站点基准的 5%-12%，为实现低碳、低成本的 LLM 训练提供了可行方案。"
selection_source: fresh_fetch
motivation: LLM 训练能耗巨大，而可再生能源弃电现象普遍，利用这些廉价且清洁的闲置电力可降低训练成本和碳排放。
method: 开发了一个分布式系统，在多个地理位置的 GPU 集群间利用弃电窗口进行弹性训练，支持本地训练与联邦同步的动态切换。
result: "原型系统在三个集群上成功训练了 561M 参数模型，碳排放仅为单站点基准的 5% 到 12%，且保证了训练质量。"
conclusion: 该研究证明了利用弃电窗口进行地理分布式 LLM 预训练在技术上可行，且在减少环境影响方面具有巨大潜力。
---

## 摘要
训练大语言模型（LLM）需要大量的计算资源和能源。与此同时，可再生能源产生的电力经常超过电网的吸收能力，导致“弃电”（curtailment），即为了避免浪费而刻意减少清洁能源发电。这些时段代表了一个机会：如果将训练与弃电窗口对齐，则可以使用既清洁又廉价的电力对 LLM 进行预训练。本技术报告介绍了一个在区域弃电窗口期间，跨地理分布式 GPU 集群执行全参数 LLM 训练的系统。该系统根据站点的可用性，在本地单站点训练和联邦多站点同步之间进行弹性切换。我们的原型系统使用 Flower 联邦学习框架，在三个集群上训练了一个拥有 5.61 亿参数的 Transformer 模型，弃电期源自真实世界的边际碳强度追踪数据。初步结果表明，弃电感知调度在保持训练质量的同时，将运营排放降低至单站点基准的 5-12%。

## Abstract
Training large language models (LLMs) requires substantial compute and energy. At the same time, renewable energy sources regularly produce more electricity than the grid can absorb, leading to curtailment, the deliberate reduction of clean generation that would otherwise go to waste. These periods represent an opportunity: if training is aligned with curtailment windows, LLMs can be pretrained using electricity that is both clean and cheap. This technical report presents a system that performs full-parameter LLM training across geo-distributed GPU clusters during regional curtailment windows, elastically switching between local single-site training and federated multi-site synchronization as sites become available or unavailable. Our prototype trains a 561M-parameter transformer model across three clusters using the Flower federated learning framework, with curtailment periods derived from real-world marginal carbon intensity traces. Preliminary results show that curtailment-aware scheduling preserves training quality while reducing operational emissions to 5-12% of single-site baselines.

---

## 论文详细总结（自动生成）

这篇论文探讨了利用可再生能源“弃电”（Curtailment）窗口进行大语言模型（LLM）分布式预训练的可行性。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：LLM 预训练能耗巨大且碳排放高，而电网中存在大量因供过于求而被浪费的清洁电力（弃电）。论文研究如何构建一个弹性分布式系统，动态捕捉这些零散、不稳定的弃电窗口来完成 LLM 预训练。
*   **研究动机**：
    *   **环境压力**：AI 训练的碳足迹日益受到关注。
    *   **资源浪费**：可再生能源（风能、太阳能）弃电量巨大（如加州 2024 年弃电达 3.4 TWh）。
    *   **技术挑战**：预训练是高度同步且状态化的过程，而弃电窗口具有随机性、短时性和地理分散性，传统的“暂停-恢复”机制开销较大。

### 2. 方法论
论文提出了一种**反应式分布式系统设计**，核心思想是根据各区域电网的实时弃电信号弹性调整训练模式：
*   **核心架构**：基于 Exalsius 控制平面（负责 GPU 节点调度）和 Flower 联邦学习框架（负责模型同步）。
*   **三种执行模式**：
    1.  **挂起模式**：无任何区域处于弃电状态，训练停止，不消耗资源。
    2.  **本地模式**：仅一个区域弃电，该站点持续进行本地训练，无通信开销。
    3.  **联邦模式**：多个区域同时弃电，各站点并行训练，由协调器定期进行模型聚合（FedAvg）。
*   **关键技术细节**：
    *   **弃电感知配置**：引入滞后时间（Hysteresis）参数 $\tau \uparrow$ 和 $\tau \downarrow$，防止因信号微小波动导致频繁开关机。
    *   **定时同步轮次**：采用基于墙钟时间（如 600 秒）而非步数的轮次，以适应异构硬件和动态加入的站点。
    *   **数据管理**：协调器维护全局进度向量，采用分片（Sharding）机制确保数据在动态参与的站点间不重复消费。

### 3. 实验设计
*   **实验场景**：模拟跨地理分布的 GPU 集群，回放真实电网的边际碳强度（MOER）数据。
*   **数据集**：使用 FineWebEdu-100B 数据集的 12.8B token 进行预训练。
*   **对比基准（Benchmark）**：
    1.  **集中式单站点训练**（Centralized）：不考虑碳排放，连续运行。
    2.  **持续双站点联邦学习**（2-Site FL）：两个站点始终在线进行联邦训练。
    3.  **本方案（Ours）**：仅在弃电窗口运行，动态切换单/多站点模式。
*   **区域选择**：选取了加州（CAISO）、德州（SPP）、南澳大利亚和德国四个具有代表性弃电特征的区域。

### 4. 资源与算力
*   **GPU 型号与数量**：使用了 3 个地理分布的集群，每个集群配备 **4 块 NVIDIA A100 GPU**（总计 12 块 GPU）。
*   **模型规模**：nanochat d20 模型，拥有 **5.61 亿（561M）参数**。
*   **训练时长**：
    *   集中式基准：17.8 小时。
    *   本方案：14.6 小时（由于多站点并行，总时长缩短，但跨越的自然时间取决于弃电窗口分布）。
*   **能耗**：总能耗约为 36-38 kWh。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了一次完整的、基于历史数据回放的端到端预训练实验。
*   **充分性评价**：
    *   作为**可行性研究（Feasibility Study）**，实验涵盖了从底层基础设施调度到高层模型收敛的全流程，证明了系统在动态环境下的稳定性。
    *   **局限性**：实验仅针对 561M 的小模型进行了单次长时运行验证，缺乏针对不同模型规模、不同同步频率或极端网络环境下的消融实验。

### 6. 主要结论与发现
*   **碳减排显著**：该方案 97% 的能源来自弃电窗口，运营碳排放仅为单站点基准的 **5%–12%**。
*   **训练质量稳定**：尽管存在频繁的暂停、恢复和模式切换，模型的困惑度（Perplexity）收敛曲线与集中式训练基本一致（最终困惑度 15.1 vs 14.8）。
*   **系统开销可控**：在 600 秒的同步轮次下，计算利用率约为 80%，证明了联邦预训练在处理弃电窗口时的系统可行性。

### 7. 优点
*   **真实性高**：使用了真实的电网碳强度追踪数据和端到端的系统实现，而非纯理论模拟。
*   **弹性设计**：巧妙结合了本地训练和联邦学习，最大化利用了单站点弃电和多站点重叠弃电的时间。
*   **开源贡献**：提供了完整的原型系统代码和 Kubernetes 算子。

### 8. 不足与局限
*   **模型规模较小**：561M 参数模型与当前主流的 7B+ 模型相比仍有差距，大模型的参数同步开销（GB 级别）可能会显著降低弃电窗口的利用率。
*   **区域选择偏乐观**：实验选取的区域弃电频率较高，若在弃电稀少的地区，训练周期可能会被拉长到难以接受的程度。
*   **数据分布假设**：假设数据可以自由分发（IID 分布），未深入探讨非独立同分布（Non-IID）数据对预训练收敛的影响。
*   **硬件成本**：虽然电力便宜，但 GPU 集群在非弃电时段的闲置成本（资本支出 CAPEX）未在文中详细讨论。

（完）
