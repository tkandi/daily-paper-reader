---
title: "GPU Memory and Utilization Estimation for Training-Aware Resource Management: Opportunities and Limitations"
title_zh: 面向训练感知资源管理的 GPU 显存与利用率估算：机遇与局限
authors: "Ehsan Yousefzadeh-Asl-Miandoab, Reza Karimzadeh, Danyal Yorulmaz, Bulat Ibragimov, Pınar Tözün"
date: 2026-02-19
pdf: "https://arxiv.org/pdf/2602.17817v2"
tags: ["query:mlsys"]
score: 9.0
evidence: 用于资源管理的GPU显存与利用率估算
tldr: 本研究探讨了深度学习训练中GPU显存与利用率估算的关键挑战。为解决多任务共存时的显存溢出和资源竞争问题，作者系统评估了分析模型（Horus）、CPU端库（FakeTensor）及机器学习估算器三种范式。通过构建涵盖多种架构的合成数据集，研究揭示了各方法在准确性、泛化能力及开销上的权衡。研究指出，现有方法在应对硬件异构性和复杂模型优化时仍存在显著局限，并发布了相关数据集以推动该领域发展。
selection_source: fresh_fetch
motivation: 针对深度学习任务共存时易发生的显存溢出和资源竞争问题，探索准确估算GPU资源以实现高效调度的方案。
method: 对比分析了Horus、PyTorch FakeTensor及自研机器学习估算器在不同模型架构和硬件环境下的性能表现。
result: 评估发现分析模型缺乏泛化性，CPU端库集成开销大，而机器学习方法在跨架构预测和硬件异构性处理上仍面临挑战。
conclusion: 现有GPU资源估算技术在准确性与通用性之间存在显著权衡，亟需更具鲁棒性的方法来应对不断演进的硬件与模型架构。
---

## 摘要
协同部署深度学习训练任务可以提高 GPU 利用率，但同时也带来了资源竞争、严重减速以及显存溢出（OOM）故障的风险。准确的显存估算是实现稳健协同部署的基础，而作为竞争关键代理指标的 GPU 利用率估算则能够实现干扰感知调度。现有的 GPU 显存估算器涵盖了三种范式：分析模型、CPU 端库和基于机器学习（ML）的估算器，每种范式都存在独特的局限性，如依赖详细的模型规范、侵入式集成、泛化能力差以及不同的延迟开销。GPU 异构性进一步增加了估算的复杂性，因为相同的任务在不同代硬件上可能表现出不同的显存占用。相比之下，GPU 利用率的研究仍相对不足，且由于非加性利用率指标和 GPU 异构性而变得更加复杂。我们对每种范式的代表性估算器（Horus、PyTorch FakeTensor 以及我们轻量级的基于 ML 的估算器）进行了系统分析，评估了它们的准确性、泛化能力和开销。我们构建了一个涵盖 MLP、CNN 和 Transformer 且具有受控架构变化的合成数据集，训练了基于 MLP 和 Transformer 的估算器用于显存预测，并进行了利用率估算实验。我们的评估揭示了关键的权衡，并在现实世界的未知模型上验证了估算器。重大挑战依然存在：分析模型缺乏泛化性，难以扩展到新的 GPU 架构，且无法准确反映显存优化带来的节省；CPU 端库带来了侵入式的集成开销；而分析模型和基于 ML 的估算器均依赖于模型规范或计算图，限制了在不同架构和硬件变体之间的泛化。我们发布了所有数据集、工具和研究产物以支持进一步研究。

## Abstract
Collocating deep learning training tasks improves GPU utilization but risks resource contention, severe slowdowns, and out-of-memory (OOM) failures. Accurate memory estimation is essential for robust collocation, and GPU utilization estimation -- a key proxy for contention -- enables interference-aware scheduling. Existing GPU memory estimators span three paradigms -- analytical models, CPU-side libraries, and ML-based estimators -- each with distinct limitations: dependence on detailed model specifications, intrusive integration, poor generalization, and varying latency overhead. GPU heterogeneity further complicates estimation, as identical tasks can exhibit different memory footprints across hardware generations. GPU utilization remains comparatively understudied, further complicated by non-additive utilization metrics and GPU heterogeneity. We conduct a systematic analysis of representative estimators from each paradigm -- Horus, PyTorch FakeTensor, and our lightweight ML-based estimator -- evaluating accuracy, generalizability, and overhead. We construct a synthetic dataset spanning MLPs, CNNs, and Transformers with controlled architectural variations, and train MLP- and Transformer-based estimators for memory prediction, and experiment with utilization estimation. Our evaluation reveals key tradeoffs and validates estimators against real-world unseen models. Significant challenges remain: analytical models lack generalization and cannot easily be extended to new GPU architectures or accurately reflect memory optimization savings; CPU-side libraries impose intrusive integration overhead; and both analytical and ML-based estimators rely on model specifications or computation graphs, limiting generalization across diverse architectures and hardware variants. We release all datasets, tools, and artifacts to support further research.

---

## 论文详细总结（自动生成）

以下是对论文《GPU Memory and Utilization Estimation for Training-Aware Resource Management: Opportunities and Limitations》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在共享 GPU 集群中，如何准确预估深度学习训练任务的 **显存占用（Memory）** 和 **利用率（Utilization）**，以实现高效的任务协同部署（Collocation），同时避免显存溢出（OOM）和资源竞争导致的性能下降。
*   **研究背景**：
    *   **协同部署的风险**：提高利用率的同时，容易引发 OOM 崩溃或严重的计算干扰。
    *   **现有方法的缺陷**：分析模型（如 Horus）依赖详细规格且泛化性差；CPU 端库（如 FakeTensor）集成侵入性强且开销大；现有 ML 方法对复杂架构和硬件异构性处理不足。
    *   **利用率研究空白**：利用率指标具有非加性（Non-additive）和硬件相关性，目前缺乏深入研究。

### 2. 方法论
*   **核心思想**：将显存和利用率预估视为**分类任务**而非回归任务。原因在于显存增长呈现“阶梯状”模式，回归模型在阶梯边缘容易产生剧烈波动和过拟合。
*   **关键技术细节**：
    *   **数据生成（Data Curation）**：开发了自动化脚本，针对 MLP、CNN 和 Transformer 三大架构族生成合成模型。通过控制层数、神经元数、Batch Size、激活函数等超参数，确保数据集的多样性和现实代表性。
    *   **GPUMemNet & GPUUtilNet**：
        *   **特征工程**：提取 Batch Size、参数量、激活量、层类型序列等特征。
        *   **模型架构**：采用**集成学习（Ensemble）**策略，结合了多个随机结构的 MLP 分类器和基于 Transformer Encoder 的分类器，以捕捉层级间的结构依赖。
    *   **离散化处理**：将连续的显存值划分为固定大小的桶（如 1GB 或 8GB 桶），将利用率划分为低、中、高三个等级。

### 3. 实验设计
*   **数据集/场景**：
    *   **合成数据集**：包含数千个随机生成的 MLP、CNN 和 Transformer 模型配置。
    *   **现实世界模型（Unseen Models）**：包括 ResNet50、VGG16、Inception、BERT、GPT-2、DLRM 等，用于验证泛化能力。
*   **Benchmark 与对比方法**：
    *   **Horus**：代表分析模型范式。
    *   **PyTorch FakeTensor**：代表 CPU 端库/符号执行范式。
    *   **GPUMemNet**：作者提出的 ML 范式。
*   **评估指标**：准确率（Accuracy）、F1-score、推理延迟、绝对误差。

### 4. 资源与算力
*   **硬件环境**：使用了一台 **NVIDIA DGX Station A100**，配备 **4 块 NVIDIA A100 GPU (40GB HBM2)**。
*   **训练细节**：
    *   每个合成模型在数据采集阶段实际运行 **1 分钟** 以获取真实的监控日志（通过 `nvidia-smi` 和 `dcgmi`）。
    *   GPUMemNet 的推理延迟极低：在 A100 上约为 **16ms**，在 AMD EPYC CPU 上约为 **32ms**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   MLP 数据集包含 3000 个随机配置。
    *   对 TIMM 库中的 2030 个模型运行了 FakeTensor 评估。
    *   采用了 **3 折交叉验证（3-fold cross-validation）**。
*   **充分性评价**：实验设计较为客观，涵盖了从微基准测试到现实复杂模型的迁移测试。通过 PCA（主成分分析）可视化了分类边界的合理性，并对比了不同范式的优劣，实验结果具有说服力。

### 6. 主要结论与发现
*   **显存预估**：
    *   **GPUMemNet** 在已知架构族上表现优异（MLP 准确率 97%，CNN/Transformer 82-88%），且倾向于保守估计（过高估计），有效避免了 OOM。
    *   **FakeTensor** 经常低估峰值显存（未考虑缓存分配器开销），且在复杂 Transformer 架构上常因不支持某些算子而失败。
    *   **Horus** 存在系统性过高估计，限制了协同部署的密度。
*   **利用率预估**：极具挑战性。由于利用率类别边界模糊且具有非加性特征，模型在 CNN 和 Transformer 上的表现显著下降（准确率仅 60-70% 左右）。
*   **泛化局限**：ML 模型对从未见过的算子（如 DLRM 中的特定层）完全失效，说明 ML 方法高度依赖训练数据的覆盖范围。

### 7. 优点与亮点
*   **系统性对比**：首次对分析、库、ML 三种显存预估范式进行了端到端的定量对比。
*   **分类视角**：巧妙地利用显存增长的阶梯特性，将回归转为分类，提高了模型的鲁棒性。
*   **开源贡献**：发布了完整的合成数据生成工具、数据集和预训练模型，填补了该领域高质量公开数据集的空白。
*   **轻量级**：GPUMemNet 推理开销极小，适合集成到实时调度器的关键路径中。

### 8. 不足与局限
*   **架构依赖**：GPUMemNet 仍依赖 `torchsummary` 提取特征，对自定义层或非标准架构的支持有限。
*   **利用率预估效果不佳**：未能解决利用率非加性带来的协同干扰预测问题，目前仅能预测单任务利用率。
*   **数据漂移风险**：框架版本更新或底层显存优化算法（如层融合）的改变可能导致训练好的 ML 模型失效，需要重新采集数据。
*   **硬件异构性处理**：虽然讨论了异构性，但实验主要集中在 A100 上，缺乏跨不同代 GPU（如从 V100 到 H100）的大规模迁移验证。

（完）
