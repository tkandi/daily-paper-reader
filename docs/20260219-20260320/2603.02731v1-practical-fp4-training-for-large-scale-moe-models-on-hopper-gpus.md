---
title: Practical FP4 Training for Large-Scale MoE Models on Hopper GPUs
title_zh: Hopper GPU 上大规模 MoE 模型的实用 FP4 训练
authors: "Wuyue Zhang, Chongdong Huang, Chunbo You, Cheng Gu, Fengjuan Wang, Mou Sun"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02731v1"
tags: ["query:mlsys"]
score: 10.0
evidence: 在Hopper GPU上使用FP4优化大规模MoE模型的训练
tldr: "针对大规模MoE模型在Hopper GPU上训练时面临的显存和通信瓶颈，本文提出了一种实用的FP4训练方案。尽管Hopper架构缺乏原生FP4支持，研究者通过引入FP8与FP4之间的直接转换技术及缩放感知的行列转换机制，实现了在FP8计算核心基础上利用MXFP4压缩激活值和专家并行通信。该方法在671B参数模型上减少了14.8%的激活显存，并将吞吐量提升了12.5%，且不影响模型收敛。"
selection_source: fresh_fetch
motivation: 大规模MoE模型训练受限于激活显存和专家并行通信带宽，且Hopper GPU缺乏原生FP4硬件支持。
method: 提出一种将FP4集成到BF16/FP8混合训练流水线的方法，通过直接的FP8-FP4转换和缩放感知的行列转换实现高效压缩。
result: "在671B参数规模下，该方法减少了14.8%的峰值激活显存，并将训练吞吐量从每秒1157个token提升至1302个。"
conclusion: 通过软硬件协同设计，即使在缺乏原生硬件支持的情况下，也能在大型MoE模型训练中实现FP4级别的效率提升。
---

## 摘要
大规模混合专家（MoE）模型的训练受限于激活内存和专家并行通信，然而在缺乏原生 MXFP4 或 NVFP4 支持的 Hopper 类 GPU 上，FP4 训练仍然不切实际。在这项工作中，我们提出了一种训练方案，使 MoE 模型在没有原生 4 位计算支持的 Hopper 架构上也能实现 MXFP4 的效率。一个核心挑战是将 FP4 集成到现有的 BF16/FP8 混合训练流水线中，而不产生昂贵的精度往返转换（例如 FP4 ↔ BF16 ↔ FP8）。我们通过引入直接的 FP8 到 FP4 量化与反量化，以及感知缩放的 FP4 行转列转换来解决这一挑战，从而以极小的开销实现 FP4 激活和专家并行通信。核心 MoE 计算在 FP8 中执行，而激活和专家并行通信则使用 MXFP4 进行压缩，在不降低收敛性的情况下实现了显著的内存和带宽节省。在 671B 参数规模下，我们的方法实现了与强 FP8 基准相当的端到端训练性能，同时将峰值激活内存降低了 14.8% (11.8 GB)，并将训练吞吐量提高了 12.5%，从每 GPU 每秒 1157 个 token 增加到 1302 个。这些结果表明，通过精心的软硬件协同设计，即使在没有原生 FP4 Tensor Core 支持的情况下，大规模 MoE 训练也能实际实现 FP4 的效率。

## Abstract
Training large-scale Mixture-of-Experts (MoE) models is bottlenecked by activation memory and expert-parallel communication, yet FP4 training remains impractical on Hopper-class GPUs without native MXFP4 or NVFP4 support. In this work, we present a training recipe that enables MXFP4 efficiency for MoE models on Hopper architectures without native 4-bit computation support. A central challenge is to integrate FP4 into an existing BF16/FP8 hybrid training pipeline without incurring costly precision round-trips (e.g., FP4 $\leftrightarrow$ BF16 $\leftrightarrow$ FP8). We address this challenge by introducing direct FP8-to-FP4 quantization and de-quantization, together with scaling-aware FP4 row-wise to column-wise conversion, enabling FP4 activations and expert-parallel communication with minimal overhead. Core MoE computations are executed in FP8, while activations and expert-parallel communication are compressed using MXFP4, achieving substantial memory and bandwidth savings without degrading convergence. At the 671B parameter scale, our method achieves end-to-end training performance comparable to strong FP8 baselines, while reducing peak activation memory by 14.8\% (11.8 GB) and improving training throughput by 12.5\%, from 1157 to 1302 tokens per GPU per second. These results show that FP4 efficiency can be practically realized for large-scale MoE training through careful software-hardware co-design, even without native FP4 Tensor Core support.

---

## 论文详细总结（自动生成）

这是一份关于论文《Practical FP4 Training for Large-Scale MoE Models on Hopper GPUs》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在不具备原生 FP4 硬件支持的 NVIDIA Hopper GPU（如 H100/H800）上，实现 4 位浮点数（FP4）带来的显存节省和通信加速，从而提升超大规模混合专家（MoE）模型的训练效率。
*   **研究背景**：
    *   **MoE 瓶颈**：MoE 模型在扩展到万亿参数时，激活显存（Activation Memory）和专家并行（Expert-Parallel）产生的全对全（All-to-All）通信成为主要瓶颈。
    *   **硬件代差**：最新的 Blackwell 架构原生支持 FP4，但目前主流的 Hopper 架构仅原生支持 FP8。
    *   **技术挑战**：在 Hopper 上模拟 FP4 通常需要经过 BF16 中转（FP4 ↔ BF16 ↔ FP8），这会带来巨大的计算延迟和精度损失，抵消了低精度带来的收益。

### 2. 方法论：MXFP4 混合精度训练框架
论文提出了一种“软实现”的 MXFP4 训练方案，核心思想是**解耦存储/通信精度与计算精度**：
*   **混合精度策略**：
    *   **计算**：核心的矩阵乘法（GEMM）依然在 FP8 精度下执行，利用 Hopper 的 Tensor Core 加速。
    *   **存储与通信**：在前向传播中，将激活值量化为 MXFP4 进行缓存（用于重计算）和跨 GPU 通信（All-to-All），从而减少 50% 的激活显存和通信带宽。
*   **关键技术细节**：
    *   **直接 FP8-FP4 转换算法**：设计了位级（Bit-wise）转换逻辑，跳过 BF16 中转。通过整数域的位移操作处理指数偏差（Bias）和尾数对齐。
    *   **层级缩放对齐（Hierarchical Scaling Alignment）**：解决 MXFP4（32 个元素共享缩放因子）与 FP8（128 个元素共享）之间块大小不匹配的问题，通过取最大值的方式实现无损转换。
    *   **算子融合（Kernel Fusion）**：开发了 `FP4RowToFP8Col` 等算子，将反量化、转置和重构融合在一个 CUDA Kernel 中，减少了 3 倍的全局内存访问。
    *   **非对称设计**：仅在前向传播使用 FP4，反向传播由于梯度对精度敏感且反量化开销较高，主动回退到 FP8 流程。

### 3. 实验设计
*   **实验场景**：大规模 MoE 模型预训练。
*   **模型配置**：
    *   **671B MoE**：采用 MLA（多头潜在注意力）架构，模拟 DeepSeek-V3 的配置。
    *   **236B MoE**：用于受控变量分析。
    *   **16B 模型**：用于长周期的收敛性验证。
*   **基准测试（Benchmark）**：
    *   **BF16**：标准的 Megatron-LM 混合精度训练。
    *   **FP8**：基于 NVIDIA Transformer Engine 的标准 FP8 训练方案。
*   **对比指标**：每秒处理 Token 数（TGS）、峰值显存占用、收敛曲线（Loss）。

### 4. 资源与算力
*   **硬件环境**：使用了一个包含 **32 个节点、共 256 块 NVIDIA Hopper GPU (80GB HBM3)** 的集群。
*   **互联技术**：节点内使用 NVSwitch，节点间使用 InfiniBand。
*   **训练时长**：收敛性实验在 16B 模型上运行了 160B Token 的训练量。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 16B 到 671B 不同量级的模型，验证了方案的可扩展性。
*   **消融实验**：针对算子融合效率、量化开销、不同重计算策略下的显存表现进行了详细对比。
*   **充分性评价**：实验设计较为充分。不仅关注了吞吐量，还通过长周期的 Loss 曲线证明了 MXFP4 在大规模训练中的数值稳定性。对比实验涵盖了目前业界最强的 FP8 基准，具有较高的参考价值。

### 6. 主要结论与发现
*   **显存节省**：在 671B 模型上，相比 FP8 基准，峰值激活显存降低了 **14.8% (11.8 GB)**。
*   **吞吐量提升**：由于显存释放，可以减少昂贵的“重计算（Recomputation）”操作。在相同硬件上，吞吐量从 1157 提升至 1302 TGS，增幅达 **12.5%**。
*   **收敛性**：MXFP4 的 Loss 轨迹与 BF16 几乎重合，相对误差仅为 +0.61%，证明了该方案在生产环境中的可行性。

### 7. 优点
*   **实用性极强**：无需等待下一代 Blackwell 硬件，直接在现有 Hopper 集群上榨取性能。
*   **软硬协同优化**：通过精巧的位运算和算子融合，克服了硬件不支持原生 4 位计算的限制。
*   **端到端集成**：方案完整覆盖了计算、存储和通信（集成到了 DeepEP 库），是一个闭环的系统级优化。

### 8. 不足与局限
*   **非全流程 FP4**：反向传播仍需回退到 FP8，未能实现全路径的 4 位训练。
*   **算子开销**：在标准的稠密线性层（Standard Linear）中，由于量化/反量化开销，相比高度优化的 TE 库仍有轻微的性能损失（约 0.3x 速度），主要增益来自于 MoE 专家层的分组计算优化。
*   **应用限制**：该方案高度依赖于 Hopper 架构的特性，对于其他厂商或旧型号 GPU 的迁移性未做探讨。

（完）
