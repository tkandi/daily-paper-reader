---
title: Scalable Training of Mixture-of-Experts Models with Megatron Core
title_zh: 使用 Megatron Core 进行混合专家模型的可扩展训练
authors: "Zijie Yan, Hongxiao Bai, Xin Yao, Dennis Liu, Tong Liu, Hongbin Liu, Pingtian Li, Evan Wu, Shiqing Fan, Li Tao, Robin Zhang, Yuzhong Wang, Shifang Xu, Jack Chang, Xuwen Chen, Kunlun Li, Yan Bai, Gao Deng, Nan Zheng, Vijay Anand Korthikanti, Abhinav Khattar, Ethan He, Soham Govande, Sangkug Lym, Zhongbo Zhu, Qi Zhang, Haochen Yuan, Xiaowei Ren, Deyu Fu, Tailai Ma, Shunkang Zhang, Jiang Shao, Ray Wang, Vasudevan Rengasamy, Rachit Garg, Santosh Bhavani, Xipeng Li, Chandler Zhou, David Wu, Yingcan Wei, Ashwath Aithal, Michael Andersch, Mohammad Shoeybi, Jiajie Yao, June Yang"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07685v2"
tags: ["query:mlsys"]
score: 10.0
evidence: 基于Megatron Core的可扩展MoE训练，涉及内存、通信和计算的协同设计
tldr: 本研究针对混合专家模型（MoE）训练中的内存、通信和计算耦合挑战，在 Megatron Core 框架中提出了一套集成的系统级优化方案。通过细粒度重计算、优化调度器、Grouped GEMM 及低精度训练（FP8/NVFP4）等技术，实现了高效的大规模 MoE 训练。该方案支持万亿级参数模型在数千个 GPU 上的扩展，显著提升了 DeepSeek-V3 和 Qwen3 等模型的训练吞吐量，为学术界和工业界提供了高性能、生产就绪的开源解决方案。
selection_source: fresh_fetch
motivation: 解决 MoE 模型训练中由于稀疏性导致的内存、通信与计算之间的耦合约束及系统瓶颈。
method: 在 Megatron Core 中集成了内存优化、通信重叠、Grouped GEMM 计算及多维并行折叠等全栈系统级优化技术。
result: "在 NVIDIA GB300 上，DeepSeek-V3 和 Qwen3 模型分别达到了 1,233 和 974 TFLOPS/GPU 的极高训练性能。"
conclusion: 该开源框架为从数十亿到万亿参数规模的 MoE 模型提供了可扩展且高效的生产级训练支撑。
---

## 摘要
扩展混合专家（MoE）训练引入了稠密模型中不存在的系统挑战。由于每个 token 仅激活一部分专家，这种稀疏性使得总参数量的增长速度远快于单 token 计算量，从而在内存、通信和计算之间产生了耦合约束。优化其中一个维度往往会将压力转移到另一个维度，因此需要跨整个系统栈进行协同设计。我们通过涵盖内存（细粒度重计算、卸载等）、通信（优化的调度器、重叠等）和计算（Grouped GEMM、融合、CUDA Graphs 等）的集成优化，解决了 MoE 训练中的这些挑战。该框架还提供了用于灵活多维并行的 Parallel Folding、对 FP8 和 NVFP4 的低精度训练支持，以及高效的长文本训练。在 NVIDIA GB300 和 GB200 上，它为 DeepSeek-V3-685B 实现了 1,233/1,048 TFLOPS/GPU 的性能，为 Qwen3-235B 实现了 974/919 TFLOPS/GPU 的性能。作为一个高性能、可扩展且生产就绪的开源解决方案，它已被学术界和工业界用于在扩展至数千个 GPU 的集群上训练从数十亿到数万亿参数不等的 MoE 模型。本报告解释了这些技术的工作原理、权衡及其在系统层面的相互作用，为使用 Megatron Core 扩展 MoE 模型提供了实践指导。

## Abstract
Scaling Mixture-of-Experts (MoE) training introduces systems challenges absent in dense models. Because each token activates only a subset of experts, this sparsity allows total parameters to grow much faster than per-token computation, creating coupled constraints across memory, communication, and computation. Optimizing one dimension often shifts pressure to another, demanding co-design across the full system stack.   We address these challenges for MoE training through integrated optimizations spanning memory (fine-grained recomputation, offloading, etc.), communication (optimized dispatchers, overlapping, etc.), and computation (Grouped GEMM, fusions, CUDA Graphs, etc.). The framework also provides Parallel Folding for flexible multi-dimensional parallelism, low-precision training support for FP8 and NVFP4, and efficient long-context training. On NVIDIA GB300 and GB200, it achieves 1,233/1,048 TFLOPS/GPU for DeepSeek-V3-685B and 974/919 TFLOPS/GPU for Qwen3-235B. As a performant, scalable, and production-ready open-source solution, it has been used across academia and industry for training MoE models ranging from billions to trillions of parameters on clusters scaling up to thousands of GPUs.   This report explains how these techniques work, their trade-offs, and their interactions at the systems level, providing practical guidance for scaling MoE models with Megatron Core.

---

## 论文详细总结（自动生成）

### 论文总结：使用 Megatron Core 进行混合专家模型（MoE）的可扩展训练

#### 1. 核心问题与整体含义（研究动机和背景）
混合专家模型（MoE）通过稀疏激活实现了模型容量与计算量的解耦，但在大规模训练中面临严峻的系统挑战。论文指出，MoE 训练存在**“三面墙”**：
*   **内存墙**：参数量巨大（如 DeepSeek-V3 达 685B），即使只有部分激活，所有专家的参数、梯度和优化器状态也必须驻留内存。
*   **通信墙**：专家并行（EP）引入了大量的 All-to-All 通信，在跨节点时带宽受限。
*   **计算效率墙**：细粒度专家导致 GEMM 算子变小，且动态路由引入了严重的宿主端（CPU）开销和负载不均衡。
论文的研究动机在于通过 Megatron Core 框架，提供一套集成的系统级优化方案，解决这些耦合约束，实现万亿级 MoE 模型的高效训练。

#### 2. 论文提出的方法论
论文提出了一套跨越并行策略、内存、通信和计算的全栈优化方法：
*   **Parallel Folding（并行折叠）**：核心思想是解耦 Attention 层和 MoE 层的并行配置。允许 Attention 使用高 TP/CP，而 MoE 使用高 EP，打破了传统框架中 EP ≤ DP 的限制。
*   **内存优化**：
    *   **细粒度重计算与卸载**：针对不同模块（如 LayerNorm、专家激活函数）进行选择性重计算或异步卸载到 CPU。
    *   **内存高效排列（Memory-Efficient Permutation）**：通过代数变换减少中间张量存储。
    *   **FSDP + EP**：利用双 DeviceMesh 设计，将 FSDP 与专家并行无缝结合。
*   **通信优化**：
    *   **高性能调度器**：集成 DeepEP 和 HybridEP，利用硬件原语（如 TMA、IBGDA）最大化 NVLink 和 IB 带宽。
    *   **1F1B 通信重叠**：将 All-to-All 通信隐藏在相邻微批次的计算之后。
*   **计算效率优化**：
    *   **Grouped GEMM 与内核融合**：合并多个专家的计算，减少算子启动次数。
    *   **无同步（Sync-Free）执行**：针对 Dropless MoE，通过 **ECHO（弹性专家克隆）**平衡负载，并利用 **Paged Stashing（分页暂存）**管理动态形状内存，使整个 MoE 层支持 CUDA Graphs。
*   **低精度训练**：支持 FP8（Hopper 架构）和 MXFP8/NVFP4（Blackwell 架构）的全流程训练。

#### 3. 实验设计
*   **模型对象**：主要针对 DeepSeek-V3 (685B) 和 Qwen3-235B 两种具有代表性的细粒度 MoE 架构。
*   **对比场景**：
    *   不同硬件平台：NVIDIA GB300、GB200、H100。
    *   不同精度：BF16 vs. FP8/MXFP8。
    *   不同序列长度：从标准的 4K 到长文本 128K。
*   **Benchmark**：以每秒处理的 Token 数（Tokens/s/GPU）和计算吞吐量（TFLOPS/GPU）为核心指标。

#### 4. 资源与算力
论文展示了在大规模集群上的实测性能：
*   **GPU 型号**：NVIDIA GB300、GB200 NVL72、H100。
*   **GPU 数量**：实验涵盖了 128、256 到 1024 个 GPU 的规模。
*   **具体表现**：在 GB300 上，DeepSeek-V3 达到了 1,233 TFLOPS/GPU；在 H100 上，1024 卡规模下 DeepSeek-V3 达到 368 TFLOPS/GPU。
*   **训练时长**：文中未明确说明完整模型的总训练时长，主要侧重于单位时间的吞吐量性能评估。

#### 5. 实验数量与充分性
论文进行了多维度的实验验证：
*   **跨架构验证**：涵盖了 DeepSeek 和 Qwen 两种不同的 MoE 实现。
*   **消融与案例研究**：详细分析了内存卸载、通信重叠、CUDA Graphs 等单一技术对性能的提升。
*   **长文本测试**：专门针对 128K 极长序列进行了压力测试。
*   **充分性评价**：实验设计非常充分，不仅提供了宏观的吞吐量数据，还深入到了算子层面的延迟分析（如 All-to-All 延迟对比），展示了在不同硬件拓扑下的性能差异，具有很高的客观性和参考价值。

#### 6. 主要结论与发现
*   **硬件红利**：Blackwell 架构（GB200/GB300）凭借更高的内存带宽和原生 MXFP8/NVFP4 支持，其 Token 吞吐量达到 Hopper（H100）的 3 倍以上。
*   **系统协同的重要性**：单纯优化通信或计算是不够的，必须通过 Parallel Folding 等手段解决层间的并行冲突。
*   **低精度是必然趋势**：FP8/FP4 不仅缓解了内存压力，还通过减少通信量和提升 Tensor Core 利用率，成为实现万亿参数模型高效训练的关键。
*   **长文本挑战转移**：在长文本场景下，计算重心从 MoE 转向 Attention，优化策略需随之调整（如使用 Context Parallelism）。

#### 7. 优点
*   **系统性极强**：不仅是单一算法的改进，而是对整个分布式训练栈（内存、通信、计算、并行策略）的重构。
*   **生产就绪**：所有技术均已集成在开源的 Megatron Core 中，且经过了 DeepSeek 等顶级大模型的实战检验。
*   **前瞻性**：率先披露了在 Blackwell 架构上使用 NVFP4 进行大规模训练的技术细节。

#### 8. 不足与局限
*   **硬件绑定**：优化方案高度依赖 NVIDIA 的硬件特性（如 NVLink、TMA、Blackwell 特有指令），在其他硬件平台上的可移植性较差。
*   **配置复杂度**：虽然提供了最佳实践建议，但多维并行（TP/PP/CP/EP/DP/VPP）的超参数空间巨大，普通用户寻找最优配置的门槛依然很高。
*   **收敛性细节较少**：作为技术报告，侧重于系统性能（吞吐量），对于 NVFP4 等极低精度在万亿规模下对模型最终收敛精度的长期影响，披露的实验数据相对有限。

（完）
