---
title: Scaling State-Space Models on Multiple GPUs with Tensor Parallelism
title_zh: 利用张量并行在多 GPU 上扩展状态空间模型
authors: "Anurag Dutt, Nimit Shah, Hazem Masarani, Anshul Gandhi"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.21144v1"
tags: ["query:mlsys"]
score: 10.0
evidence: 多 GPU 模型扩展的张量并行技术
tldr: 随着选择性状态空间模型（SSM）在长文本处理中的兴起，单GPU的内存和带宽限制了其推理性能。本文提出了一种针对SSM的高效张量并行（TP）设计，通过跨预填充和解码阶段的状态缓存、优化的参数张量划分以及量化AllReduce通信，解决了SSM混合器中投影与序列更新耦合带来的并行化难题。在Mamba等模型上的实验表明，该方法显著提升了多GPU环境下的推理吞吐量，尤其在长文本场景下表现优异。
selection_source: fresh_fetch
motivation: 现有的张量并行技术难以直接应用于SSM，因为其混合器结构将大投影与序列递归状态更新耦合在一起，导致多GPU扩展效率低下。
method: 提出了一种通信高效的TP方案，包括跨阶段的状态缓存、保持局部性的参数划分策略以及降低同步开销的量化AllReduce。
result: "在2块和4块GPU上，Mamba模型的推理吞吐量分别提升了约1.6-2.1倍和2.6-4.0倍，且量化通信进一步带来了10-18%的性能增益。"
conclusion: 该研究证明了通过合理的张量并行设计，可以有效克服SSM的硬件限制，实现大规模SSM模型在多GPU上的高效扩展。
---

## 摘要
选择性状态空间模型 (SSM) 已迅速成为大语言模型中极具吸引力的骨干网络，尤其适用于长上下文工作负载。然而在部署中，其推理性能往往受限于单 GPU 的显存容量、带宽和延迟限制，使得多 GPU 执行变得日益必要。尽管张量并行 (TP) 已广泛用于扩展 Transformer 推理，但将其应用于选择性 SSM 模块并非易事，因为 SSM 混合器将大型投影与序列级循环状态更新及局部混合耦合在一起，其效率取决于保持局部性并避免关键路径中的同步。本文提出了一种用于选择性 SSM 推理的高通信效率 TP 设计，解决了三个实际工程挑战：通过跨预填充和解码阶段的 SSM 状态缓存实现首字生成时间 (TTFT) 的优化；对混合器的打包参数张量进行分区，使循环更新保持局部化，同时最小化通信；以及通过量化 AllReduce 减少 TP 聚合开销。我们在 NVIDIA A6000 和 A100 集群上，针对涵盖纯 SSM 和混合架构的三种代表性 SSM 大语言模型（Mamba、Falcon-Mamba 和 Zamba）进行了评估。实验表明，张量并行 SSM 推理带来了显著的吞吐量提升，对于 Mamba 模型，在 2 个 GPU 上批量请求吞吐量提高了约 1.6-2.1 倍，在 4 个 GPU 上提高了约 2.6-4.0 倍，且在长上下文长度下收益最大；此外，通过降低同步带宽开销，量化 AllReduce 进一步实现了约 10-18% 的吞吐量提升。

## Abstract
Selective state space models (SSMs) have rapidly become a compelling backbone for large language models, especially for long-context workloads. Yet in deployment, their inference performance is often bounded by the memory capacity, bandwidth, and latency limits of a single GPU, making multi-GPU execution increasingly necessary. Although tensor parallelism (TP) is widely used to scale Transformer inference, applying it to selective SSM blocks is non-trivial because the SSM mixer couples large projections with a sequence-wise recurrent state update and local mixing whose efficiency depends on preserving locality and avoiding synchronization in the critical path.   This paper presents a communication-efficient TP design for selective SSM inference that addresses three practical engineering challenges: enabling TTFT improvements via an SSM state cache across prefill and decode, partitioning the mixer's packed parameter tensor so that recurrent updates remain local while minimizing communication, and reducing TP aggregation overhead with quantized AllReduce. We evaluate on three representative SSM-based LLMs spanning pure-SSM and hybrid architectures - Mamba, Falcon-Mamba, and Zamba - on NVIDIA A6000 and A100 clusters. Our experiments show substantial throughput gains from tensor-parallel SSM inference, improving batch-request throughput by ~1.6-2.1x on 2 GPUs and ~2.6-4.0x on 4 GPUs for Mamba, with the largest benefits at long context lengths, and achieving a further ~10-18% throughput improvement from quantized all-reduce by lowering synchronization bandwidth overhead.

---

## 论文详细总结（自动生成）

以下是对论文《Scaling State-Space Models on Multiple GPUs with Tensor Parallelism》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：选择性状态空间模型（SSM，如 Mamba）虽然在处理长文本时具有线性复杂度优势，但在实际部署中受限于单块 GPU 的显存容量、带宽和延迟。
*   **研究动机**：
    *   **现有技术局限**：传统的张量并行（TP）方案是为 Transformer 设计的，主要针对大矩阵乘法（GEMM）。
    *   **SSM 的特殊性**：SSM 混合器将大型投影与序列递归状态更新、局部卷积耦合在一起。直接套用 Transformer 的 TP 模板会导致频繁的跨卡同步和数据重组，破坏计算局部性，严重降低效率。
    *   **扩展需求**：随着模型规模增长，迫切需要一种能高效利用多 GPU 资源、支持更长上下文的 SSM 并行推理方案。

### 2. 方法论：核心思想与关键技术
论文提出了一种通信高效的 SSM 张量并行设计，包含四个关键组件：
*   **SSM 状态缓存（SSM Cache）**：
    *   引入分布式缓存机制，存储每层的 SSM 状态和卷积历史。
    *   将缓存按通道（Channel）分片存储在不同 GPU 上，确保预填充（Prefill）和解码（Decode）阶段的衔接无需额外的跨卡通信。
*   **智能通道拆分（Channel-wise Splitting）**：
    *   利用 SSM 中 1D 卷积的通道可分离特性，按通道维度拆分权重。
    *   通过这种对齐，使得深度卷积和 SSM 扫描（Scan）操作可以在 GPU 本地完成，将每个模块的 AllReduce 同步次数从 4 次减少到 2 次。
*   **打包参数处理（Packed Parameter Handling）**：
    *   针对 SSM 混合器中打包生成的参数（$\Delta, A, B, C, D$），显式地对 $\Delta$ 进行分片，同时确保每个 GPU 拥有其本地更新所需的参数副本，避免了在调用融合内核前进行昂贵的数据重组。
*   **量化 AllReduce（Quantized AllReduce）**：
    *   在必须进行的同步步骤中，将通信负载从 FP32 量化为 FP16，降低同步带宽开销，进一步提升吞吐量。

### 3. 实验设计
*   **实验场景**：多 GPU 推理服务性能评估。
*   **数据集**：Simple English Wikipedia (SimpleWiki) 语料库。
*   **评估模型**：涵盖纯 SSM 和混合架构的四个代表性模型：Mamba、Mamba-2、Falcon-Mamba、Zamba。
*   **Benchmark 与对比方法**：
    *   **Default (1x)**：单 GPU 推理（基准）。
    *   **Data Parallelism (DP)**：数据并行，在多 GPU 上复制完整模型。
    *   **评估指标**：吞吐量（Throughput）、首字生成时间（TTFT）、每 token 生成时间（TPOT）、最大输入序列长度。

### 4. 资源与算力
论文在两个典型的 GPU 集群上进行了测试：
*   **NVIDIA RTX A6000 工作站**：4 块 GPU，通过 PCIe 4.0 连接。
*   **NVIDIA A100 集群**：4 块 GPU，通过 NVLINK 高速互联。
*   **训练时长**：本文侧重于推理优化，未提及模型训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 2 GPU 和 4 GPU 的扩展性实验。
    *   进行了不同输入长度（$L_{in}$）和输出长度（$L_{out}$）的全面扫描（从 16 到 1024 tokens）。
    *   包含针对缓存、拆分方案和量化的**消融实验**。
    *   进行了**精度损失评估**，验证量化对模型输出准确性的影响。
*   **充分性评价**：实验设计较为充分，对比了不同互联带宽（PCIe vs NVLINK）下的表现，涵盖了当前主流的 SSM 变体，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **吞吐量大幅提升**：在 Mamba 模型上，2 GPU 提升约 1.6–2.1 倍，4 GPU 提升约 2.6–4.0 倍。
*   **长文本优势明显**：上下文越长，TP 带来的收益越高，且支持的输入长度比单卡/数据并行高出 2–4 倍。
*   **量化增益**：量化 AllReduce 在不显著损失精度的情况下，额外贡献了 10–18% 的吞吐量提升。
*   **延迟优化**：消融实验显示，状态缓存和智能拆分分别将解码延迟降低了数倍，有效解决了 SSM 推理的瓶颈。

### 7. 优点
*   **针对性强**：深入分析了 SSM 算子的局部性需求，而非盲目套用 Transformer 的并行模式。
*   **工程落地性高**：提出的方案直接解决了 SSM 融合内核在并行环境下的数据布局问题。
*   **兼容性广**：方案成功适配了从纯 SSM 到混合架构的多种主流模型。
*   **通信优化**：通过减少同步次数和量化通信，有效缓解了多 GPU 扩展中的通信瓶颈。

### 8. 不足与局限
*   **硬件依赖**：虽然在 PCIe 上表现良好，但 TP 的效率仍高度依赖 GPU 间的互联带宽，在极大规模分布式系统（如跨节点）中的表现未深入探讨。
*   **精度风险**：虽然实验显示量化对 Top-1 准确率影响较小，但在对数值精度极度敏感的任务中，FP16 量化可能仍存在潜在风险。
*   **模型覆盖**：虽然涵盖了主流 SSM，但对于一些更新的、结构更复杂的混合架构（如包含更多 Attention 层的模型），TP 的复杂度和收益平衡可能需要重新评估。

（完）
