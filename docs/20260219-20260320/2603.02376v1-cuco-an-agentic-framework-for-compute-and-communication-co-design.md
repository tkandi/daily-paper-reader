---
title: "CUCo: An Agentic Framework for Compute and Communication Co-design"
title_zh: CUCo：一种计算与通信协同设计的智能体框架
authors: "Bodun Hu, Yoga Sri Varshan, Saurabh Agarwal, Aditya Akella"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.02376v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 分布式大模型训练中计算与通信的协同设计
tldr: 针对大规模分布式LLM训练和推理中CUDA算子开发难度大且计算与通信协同优化不足的问题，本文提出了CUCo框架。这是一个无需训练的智能体驱动工作流，能够自动生成协同编排计算与通信的高性能CUDA内核。通过打破传统上计算与通信分离的优化局限，CUCo实现了端到端延迟最高1.57倍的降低，显著提升了GPU利用率。
selection_source: fresh_fetch
motivation: 现有的CUDA内核优化主要集中在计算层面，忽视了占据大量执行时间的通信环节，且手动编写协同优化的内核极其耗时且易错。
method: 提出了一种基于智能体的工作流，通过自动化的方式协同设计并生成能够同时管理计算与通信任务的高性能CUDA内核。
result: 实验表明，CUCo生成的内核性能优于现有基准，在实际应用中可将端到端延迟降低高达1.57倍。
conclusion: CUCo证明了计算与通信协同设计的自动化方法在提升分布式深度学习系统性能方面具有巨大潜力。
---

## 摘要
定制化 CUDA 算子开发对于在大规模分布式大语言模型（LLM）训练和推理中最大化 GPU 利用率至关重要，然而，手动编写能够同时利用计算和通信的算子仍然是一个劳动密集且易于出错的过程。先前的算子优化工作几乎完全集中在计算上，而通信算子在很大程度上未被触及，尽管它们在总执行时间中占有很大比例。我们介绍了 CUCo，这是一种无需训练的智能体驱动工作流，能够自动生成共同编排计算与通信的高性能 CUDA 算子。通过对这些传统上相互独立的组件进行协同优化，CUCo 开启了现有方法无法实现的全新优化机会，其性能优于最先进的基准方法，并将端到端延迟降低了高达 1.57 倍。

## Abstract
Custom CUDA kernel development is essential for maximizing GPU utilization in large-scale distributed LLM training and inference, yet manually writing kernels that jointly leverage both computation and communication remains a labor-intensive and error-prone process. Prior work on kernel optimization has focused almost exclusively on computation, leaving communication kernels largely untouched even though they constitute a significant share of total execution time. We introduce CUCo, a training-free agent-driven workflow that automatically generates high-performance CUDA kernels that jointly orchestrate computation and communication. By co-optimizing these traditionally disjoint components, CUCo unlocks new optimization opportunities unavailable to existing approaches, outperforming state-of-the-art baselines and reducing end-to-end latency by up to $1.57\times$.

---

## 论文详细总结（自动生成）

### CUCo: 计算与通信协同设计的智能体框架论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：在分布式大语言模型（LLM）的训练和推理中，GPU利用率受限于计算与通信的串行执行。虽然现代硬件（如NVSHMEM、NCCL Device-Side API）允许在GPU内核中直接触发通信，但手动编写这种“计算-通信融合”的CUDA内核极其困难，且对硬件拓扑高度敏感。
*   **研究动机**：
    *   **开发门槛高**：手动实现计算与通信的精细重叠（Overlap）需要处理复杂的同步、分块和资源分配，极易出错。
    *   **现有工具局限**：传统编译器（如XLA、Triton）通常将通信视为算子边界外的“副作用”，无法实现内核级的深度融合。
    *   **硬件异构性**：最优的内核配置随网络带宽、延迟和拓扑结构而变化，手动调优不可持续。

#### 2. 方法论
CUCo 是一个无需训练（Training-free）、由智能体驱动的自动化工作流，其核心由三个部分组成：
*   **设计空间规范（Design Space Specification）**：
    *   将优化决策形式化为五个维度：$C = B \times P \times S \times I \times G$（后端选择、放置策略、同步范围、发射粒度、分块大小）。
    *   通过“优化指令”（Optimization Directive）引导智能体进行结构化推理，避免盲目生成代码。
*   **快路径智能体（Fast-Path Agent）**：
    *   **目标**：优先保证正确性，生成基准内核。
    *   **流程**：静态分析原始CUDA代码 -> 提取通信依赖图 -> 将主机端（Host-side）NCCL调用转换为设备端（Device-initiated）原语 -> 标注可进化代码块（Evolve-Blocks）。
*   **慢路径智能体（Slow-Path Agent）**：
    *   **目标**：通过性能反馈迭代优化。
    *   **机制**：采用**多岛屿进化搜索（Multi-island Evolutionary Search）**。
    *   **关键技术**：
        *   **LLM变异算子**：利用LLM进行语义感知的代码修改（Diff、重写、杂交）。
        *   **级联评估**：按“编译 -> 验证 -> 基准测试”顺序过滤无效候选者。
        *   **元总结器（Meta-Summarizer）**：分析历史失败和成功案例，为后续变异提供策略建议。

#### 3. 实验设计
*   **实验场景**：
    1.  **Flash Attention + 上下文并行**：在4个GPU上通过环形通信（Ring Attention）旋转KV块。
    2.  **DeepSeek-V3 MoE 调度与合并**：模拟专家并行中的 All-to-All 通信，处理负载不均衡场景。
    3.  **KV-Cache 传输**：在分离式预填-解码架构中传输 K/V 张量。
    4.  **GEMM + AllGather**：计算密集型算子后接全局聚合。
*   **Benchmark 与对比方法**：
    *   **基准（Baseline）**：基于主机端调度的标准 NCCL 通信实现。
    *   **对比指标**：端到端延迟（Latency）。

#### 4. 资源与算力
*   **硬件环境**：
    *   2台服务器，每台配备 4 张 **NVIDIA A100 (80GB) GPU**。
    *   互联：节点内使用 **NVLink**，节点间使用 **RoCE** (RDMA over Converged Ethernet)。
*   **智能体算力**：
    *   使用 **Anthropic Claude 3.5 Sonnet** 作为底层大模型驱动智能体。
    *   论文未详细列出总的API调用次数或搜索总时长，但提到快路径通常在 2-4 次迭代内收敛。

#### 5. 实验数量与充分性
*   **实验规模**：针对 4 种典型工作负载，在不同序列长度、隐藏维度和专家不平衡率下进行了多组实验。
*   **消融实验**：
    *   验证了“快路径”的必要性：没有快路径，进化搜索在初期会浪费大量预算在无法编译的代码上。
    *   验证了“探索-利用（Explore-Exploit）”两阶段策略的有效性。
*   **客观性**：实验涵盖了节点内和节点间两种通信环境，并使用了 Nsight Systems 进行深度剖析（Case Study），解释了性能提升的底层物理原因（如消除流水线气泡、减少API开销）。

#### 6. 主要结论与发现
*   **性能提升**：CUCo 生成的融合内核相比主机端基准实现了 **5.3% 至 26.2%** 的延迟降低，在特定极端情况下端到端加速比高达 **1.57倍**。
*   **优化来源**：性能增益主要来自消除内核启动间隙（Launch Gaps）、减少主机端代理开销以及实现更细粒度的分块流水线（Per-tile Pipelining）。
*   **自动化潜力**：证明了智能体能够发现人类开发者难以直观构思的复杂同步和重叠策略。

#### 7. 优点
*   **结构化搜索**：通过定义 $C$ 空间，将开放的代码生成问题转化为受约束的搜索问题，显著提高了 LLM 的成功率。
*   **闭环反馈**：引入了编译和运行时的真实反馈，使智能体能够根据硬件特性自我演进。
*   **无需训练**：不需要针对特定算子库进行微调，具有较好的通用性和可移植性。

#### 8. 不足与局限
*   **模型依赖**：高度依赖高性能闭源模型（如 Claude 3.5），开源模型在处理此类复杂逻辑时的表现尚未验证。
*   **搜索成本**：进化算法涉及多次编译和运行，对于超大规模算子的搜索时间可能较长。
*   **硬件覆盖**：目前仅在 A100 上验证，尚未展示在 H100 或其他架构（如 AMD Instinct）上的泛化能力。
*   **应用限制**：主要针对通信密集型场景，若计算占比极高，融合带来的边际收益会减小。

（完）
