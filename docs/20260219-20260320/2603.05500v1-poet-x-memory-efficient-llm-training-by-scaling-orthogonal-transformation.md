---
title: "POET-X: Memory-efficient LLM Training by Scaling Orthogonal Transformation"
title_zh: POET-X：通过缩放正交变换实现显存高效的大语言模型训练
authors: "Zeju Qiu, Lixin Liu, Adrian Weller, Han Shi, Weiyang Liu"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05500v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 通过可扩展正交变换实现显存高效的LLM训练
tldr: 针对大语言模型训练中的稳定性和效率挑战，本文提出了POET-X。该方法改进了原有的POET框架，通过优化正交等价变换显著降低了内存消耗和计算开销。POET-X在保持训练稳定性和泛化能力的同时，大幅提升了吞吐量，成功实现了在单张H100显卡上预训练十亿参数规模的模型，解决了AdamW等标准优化器在同等条件下显存溢出的问题。
selection_source: fresh_fetch
motivation: 原始POET框架虽然能提供训练稳定性，但其高昂的内存占用和计算开销限制了在大规模模型上的应用。
method: 提出POET-X，通过可扩展的正交等价变换减少矩阵乘法操作，从而优化内存效率和计算成本。
result: 实验证明POET-X能在单张Nvidia H100 GPU上预训练十亿参数级LLM，而AdamW在相同配置下会发生显存溢出。
conclusion: POET-X是一种高效且稳定的LLM训练方案，在显著降低资源需求的同时保持了优异的训练性能。
---

## 摘要
高效且稳定地训练大语言模型（LLMs）仍然是现代机器学习系统中的核心挑战。为了应对这一挑战，研究者提出了重参数化正交等价训练（POET），这是一种通过正交等价变换优化每个权重矩阵的光谱保持（spectrum-preserving）框架。尽管 POET 提供了强大的训练稳定性，但由于密集的矩阵乘法，其原始实现会导致高昂的内存消耗和计算开销。为了克服这些局限性，我们引入了 POET-X，这是一种可扩展且显存高效的变体，它在执行正交等价变换时显著降低了计算成本。POET-X 在保持 POET 泛化和稳定性优势的同时，在吞吐量和内存效率方面实现了实质性的提升。在我们的实验中，POET-X 能够在单块 Nvidia H100 GPU 上预训练十亿参数规模的大语言模型；相比之下，AdamW 等标准优化器在相同设置下会出现显存不足（OOM）的情况。

## Abstract
Efficient and stable training of large language models (LLMs) remains a core challenge in modern machine learning systems. To address this challenge, Reparameterized Orthogonal Equivalence Training (POET), a spectrum-preserving framework that optimizes each weight matrix through orthogonal equivalence transformation, has been proposed. Although POET provides strong training stability, its original implementation incurs high memory consumption and computational overhead due to intensive matrix multiplications. To overcome these limitations, we introduce POET-X, a scalable and memory-efficient variant that performs orthogonal equivalence transformations with significantly reduced computational cost. POET-X maintains the generalization and stability benefits of POET while achieving substantial improvements in throughput and memory efficiency. In our experiments, POET-X enables the pretraining of billion-parameter LLMs on a single Nvidia H100 GPU, and in contrast, standard optimizers such as AdamW run out of memory under the same settings.

---

## 论文详细总结（自动生成）

以下是对论文《POET-X: Memory-efficient LLM Training by Scaling Orthogonal Transformation》的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：大语言模型（LLM）的训练面临稳定性差和计算资源消耗巨大的双重挑战。
*   **核心问题**：此前提出的 POET 框架通过“正交等价变换”保持权重矩阵的光谱特性（Spectrum-preserving），虽能显著提升训练稳定性，但其原始实现涉及大规模矩阵乘法，导致显存占用极高且运行速度远慢于 AdamW。
*   **研究动机**：开发一种快速、可扩展且显存高效的算法，在保留 POET 稳定性优势的同时，使其能够实际应用于百亿参数规模模型的预训练。

### 2. 方法论：POET-X 核心技术
POET-X 的核心思想是将正交变换过程从“以权重为中心”转变为“以输入为中心”，并结合底层算子优化：
*   **以输入为中心的实现（Input-centric）**：借鉴无矩阵计算思想，将 $W \leftarrow RWP$ 的更新转化为一系列矩阵-向量乘法，避免存储与权重矩阵相关的中间激活值。
*   **置换加速与缩减**：
    *   开发自定义 CUDA 内核执行索引映射，避免显式构造置换矩阵。
    *   通过预先将置换合并到权重矩阵 $W$ 中，将前向传播中的置换操作从 4 次减少到 2 次。
*   **分块并行计算**：利用正交矩阵的分块对角稀疏结构，跳过全矩阵构造，采用批处理方式并行计算各对角块。
*   **高效 Cayley-Neumann 参数化（CNP）**：
    *   仅存储反对称矩阵的上三角部分，使优化器状态和梯度显存减半。
    *   使用 Triton 开发内核融合（Kernel Fusion）算子，减少数据在显存与缓存间的传输。
*   **变体设计**：
    *   **POET-X fast**：标准自动微分逻辑，速度快。
    *   **POET-X mem**：引入梯度检查点（Checkpointing），显存效率极高。
    *   **POET-XQ**：支持量化训练，仅存储低比特权重并在运行时解量化。

### 3. 实验设计
*   **数据集**：使用 C4（Colossal Clean Crawled Corpus）大规模语料库进行预训练。
*   **模型规模**：涵盖 Llama-3B、8B 和 13B 三种规格。
*   **Benchmark 与对比方法**：
    *   **标准优化器**：AdamW、Muon（Kimi 团队提出）。
    *   **显存高效方法**：GaLore、APOLLO、LoRA（作为显存基准）。
*   **评估指标**：验证集困惑度（Validation Perplexity）、吞吐量（tokens/s）、峰值显存占用。

### 4. 资源与算力
*   **硬件环境**：NVIDIA HGX H100 (80GB) 系统。
*   **算力规模**：
    *   单机实验：1 张 H100。
    *   分布式实验：使用 32 张 H100（4 节点 × 8 GPU）进行 Llama-8B 训练。
    *   扩展性测试：从 1 张扩展至 64 张 H100（8 节点）。
*   **训练时长**：实验涉及 5B 到 60B tokens 的训练过程（例如 Llama-3B 在 60B tokens 上训练以遵循 Chinchilla 定律）。

### 5. 实验数量与充分性
*   **实验覆盖面**：论文进行了单层算子性能分析、多节点大规模预训练、量化训练实验以及深入的效率消融研究。
*   **充分性评价**：实验设计较为全面。通过对比不同序列长度（512 到 2048）和不同模型规模，验证了算法的鲁棒性。对比了当前最前沿的内存优化器（如 GaLore 和 Muon），实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **显存突破**：POET-X 实现了在**单张 H100 GPU 上预训练 13B 参数模型**，而 AdamW 在同等条件下会发生显存溢出（OOM）。
*   **性能提升**：相比原始 POET，POET-X 实现了 **3 倍的显存缩减**和 **8 倍的运行加速**。
*   **收敛质量**：在相同计算预算下，POET-X 的验证集困惑度优于 AdamW、GaLore 和 APOLLO，展现了更好的泛化能力。
*   **扩展性**：在分布式环境下，由于显存占用低，POET-X 可以使用 DDP 而非复杂的 FSDP，从而减少通信开销，提升吞吐量。

### 7. 优点与亮点
*   **底层优化深入**：不仅在数学形式上做了简化，还深入到 CUDA 和 Triton 算子层级进行融合优化，实用性极强。
*   **零推理开销**：训练完成后，正交矩阵可合并回原始权重，推理阶段与标准模型完全一致。
*   **显存与性能兼得**：达到了 LoRA 级别的显存效率，同时在预训练效果上优于大多数参数高效微调（PEFT）方法。

### 8. 不足与局限
*   **超参数依赖**：算法引入了分块大小 $b$ 等超参数，虽然文中给出了建议值（256/512），但在不同架构上的最优选择可能需要额外调优。
*   **实现复杂度**：相比于直接调用 AdamW，POET-X 需要集成自定义的 CUDA/Triton 内核，对开发环境和框架集成有一定要求。
*   **应用范围限制**：目前主要针对 Transformer 架构中的线性层进行优化，对于其他非线性结构或特殊算子的适用性尚未广泛验证。

（完）
