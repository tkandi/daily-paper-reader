---
title: "Covenant-72B: Pre-Training a 72B LLM with Trustless Peers Over-the-Internet"
title_zh: Covenant-72B：通过互联网利用无需信任的对等节点预训练 72B 大语言模型
authors: "Joel Lidin, Amir Sarfi, Erfan Miahi, Quentin Anthony, Shivam Chauhan, Evangelos Pappas, Benjamin Thérien, Eugene Belilovsky, Samuel Dare"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08163v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 互联网环境下72B大模型的全球分布式预训练
tldr: 本研究推出了Covenant-72B，这是目前规模最大的全球分布式预训练大语言模型。针对现有分布式训练规模较小且仅限于白名单参与者的问题，该项目利用区块链协议支持完全开放、无许可的全球协作。通过采用SparseLoCo通信高效优化器，模型在1.1T token上完成了预训练，证明了利用去中心化算力构建大规模基础模型的可行性与竞争力。
selection_source: fresh_fetch
motivation: 旨在解决现有分布式训练规模受限且缺乏真正开放性的问题，实现去中心化的全球算力协同。
method: 利用SparseLoCo优化器和区块链协议，支持全球节点在互联网环境下动态、无许可地参与72B规模模型的预训练。
result: 该模型在1.1T token上训练后，其性能表现与使用同等或更高计算预算的中心化预训练模型相当。
conclusion: 研究证明了完全民主化、非白名单的全球分布式预训练在大规模参数量级下是切实可行且高效的。
---

## 摘要
最近，全球分布式训练引起了越来越多的关注，它有望降低训练成本，并使大规模基础模型的构建过程更加民主化。然而，现有的以全球分布式方式训练的模型规模相对较小，且仅由白名单参与者进行训练。因此，它们尚未完全实现民主化参与的愿景。在本报告中，我们介绍了 Covenant-72B，这是一个由规模最大（无论是在计算量还是模型规模方面）的协作式全球分布式预训练运行产生的大语言模型（LLM），该运行同时允许由实时区块链协议支持的开放、无许可参与。我们利用了最先进的通信高效优化器 SparseLoCo，支持对等节点自由加入和退出的动态参与。我们的模型在约 1.1T token 上进行了预训练，其性能与在相似或更高计算预算下预训练的全集中式模型相当，这证明了完全民主化、非白名单的参与不仅是可行的，而且可以在全球分布式预训练运行中以史无前例的规模实现。

## Abstract
Recently, there has been increased interest in globally distributed training, which has the promise to both reduce training costs and democratize participation in building large-scale foundation models. However, existing models trained in a globally distributed manner are relatively small in scale and have only been trained with whitelisted participants. Therefore, they do not yet realize the full promise of democratized participation. In this report, we describe Covenant-72B, an LLM produced by the largest collaborative globally distributed pre-training run (in terms of both compute and model scale), which simultaneously allowed open, permissionless participation supported by a live blockchain protocol. We utilized a state-of-the-art communication-efficient optimizer, SparseLoCo, supporting dynamic participation with peers joining and leaving freely. Our model, pre-trained on approximately 1.1T tokens, performs competitively with fully centralized models pre-trained on similar or higher compute budgets, demonstrating that fully democratized, non-whitelisted participation is not only feasible, but can be achieved at unprecedented scale for a globally distributed pre-training run.

---

## 论文详细总结（自动生成）

以下是对论文《Covenant-72B: Pre-Training a 72B LLM with Trustless Peers Over-the-Internet》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何打破大模型训练对高度中心化、昂贵硬件基础设施（如数千块通过高带宽低延迟互连的加速器）的依赖，实现真正的“民主化”训练？
*   **研究背景**：现有的分布式训练（Over-the-Internet）大多依赖“白名单”机制（即参与者必须互相信任），且模型规模较小。
*   **整体含义**：本文推出了 **Covenant-72B**，这是目前全球规模最大、首次在互联网环境下利用“无需信任（Trustless）”且“无许可（Permissionless）”的对等节点协作完成的 72B 参数大模型预训练项目。它证明了在动态、不稳定的互联网环境下，通过去中心化算力也能训练出性能媲美中心化集群的顶级模型。

### 2. 方法论：核心思想与关键技术
该模型结合了通信高效的优化算法与基于区块链的激励机制：
*   **SparseLoCo 优化器**：
    *   **核心思想**：采用局部更新（Local-update）策略。每个节点在本地运行 $H$ 步（本文为 30 步）优化，然后仅交换高度压缩的“伪梯度”。
    *   **压缩技术**：结合了 **块状 Top-k 稀疏化**（将张量分块处理以降低索引开销）、**误差反馈（Error-feedback）**（补偿舍弃的梯度信息）和 **2-bit 量化**。
    *   **效率**：实现了超过 **146 倍** 的通信压缩比，使得在普通互联网带宽下训练 72B 模型成为可能。
*   **Gauntlet 激励与验证机制**：
    *   **核心思想**：在 Bittensor 区块链（Subnet 3）上运行，解决“无需信任”环境下的作弊问题。
    *   **验证逻辑**：验证者通过计算节点提交的伪梯度在小规模数据上的损失下降值（LossScore）来评分。如果节点在未分配数据上的表现优于分配数据，则判定为抄袭或作弊并予以惩罚。
*   **系统设计**：
    *   利用 Cloudflare R2 对象存储作为通信骨干，避免了复杂的点对点（P2P）连接。
    *   使用动态 FSDP（全分片数据并行）在节点内部管理 8×B200 GPU 的显存，通过显存换入换出（Offloading）技术平衡计算与通信。

### 3. 实验设计：数据集与 Benchmark
*   **数据集**：
    *   **主预训练阶段**：约 1.09T tokens，主要来自 DCLM 网页抓取数据。
    *   **退火（Annealing）阶段**：约 14.2B tokens，包含高质量指令、合成网页、代码、数学和科学数据。
    *   **SFT 阶段**：约 14.8B tokens，涵盖聊天、代码、数学、STEM 等。
*   **Benchmark（基准测试）**：
    *   **基础模型**：ARC-C/E, PIQA, OpenBookQA, HellaSwag, WinoGrande, MMLU。
    *   **对话模型**：GSM8K, MATH, IFEval, MMLU-Pro 等。
*   **对比方法**：
    *   **去中心化基准**：INTELLECT-1 (10B), Psyche Consilience (40B)。
    *   **中心化基准**：LLM360 K2 (65B), LLaMA-2-70B, LLaMA-2-7B。

### 4. 资源与算力
*   **GPU 型号与规模**：每个参与节点通常配备 **8× NVIDIA B200 GPU**。
*   **参与人数**：训练过程中平均有 **16.9 个** 贡献节点（上限设为 20 个），整个运行期间至少有 **70 个唯一节点 ID** 参与过贡献。
*   **训练时长与规模**：总计训练约 **1.1T tokens**。
*   **通信开销**：在 500Mb/s 下行和 110Mb/s 上行的带宽限制下，每轮同步仅需约 70 秒，计算利用率高达 **94.5%**。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了从预训练到退火再到 SFT 的全流程实验。
*   **充分性评价**：
    *   **客观性**：对比了同量级的顶级开源模型（如 LLaMA-2-70B），证明了其在 ARC 和 MMLU 等硬指标上的领先。
    *   **消融/对比**：对比了退火前后的性能变化（见附录），并详细记录了动态参与者的波动情况。
    *   **局限性**：由于是单次大规模运行的报告，缺乏针对不同超参数（如不同 $H$ 步数）的大规模消融实验，但其作为“可行性证明”是非常充分的。

### 6. 主要结论与发现
*   **性能达标**：Covenant-72B 在多个基准测试中优于或持平于中心化训练的 K2 和 LLaMA-2-70B，尽管后者的训练 Token 数更多（2T）。
*   **去中心化可行性**：证明了在无许可、无需信任的互联网环境下，通过激进的梯度压缩和区块链激励，可以高效完成 70B+ 规模模型的预训练。
*   **高利用率**：SparseLoCo 算法在互联网延迟下依然能保持 90% 以上的计算利用率，远超之前的分布式训练方案。

### 7. 优点与亮点
*   **规模突破**：将无许可分布式训练的规模从 10B 提升到了 72B，具有里程碑意义。
*   **通信效率极高**：146 倍的压缩比和 2-bit 量化技术非常出色，解决了互联网带宽瓶颈。
*   **鲁棒性强**：成功应对了节点自由加入/退出的动态环境，平均 16.9 个节点的参与保证了训练的持续性。
*   **开源贡献**：开源了中间检查点、最终模型及 SFT 模型，促进了社区研究。

### 8. 不足与局限
*   **上下文长度限制**：预训练仅支持 2k 上下文，SFT 扩展到 8k，相比目前主流模型的 128k+ 显得较短。
*   **数据混合偏差**：由于与对比模型（如 LLaMA-2）使用的数据集不完全一致，很难纯粹从算法层面评估性能差异。
*   **硬件门槛**：虽然是分布式，但每个节点仍需 8×B200 这种顶级配置，对于真正的“个人”参与者门槛依然较高。
*   **验证成本**：Gauntlet 机制虽然有效，但验证者仍需承担一定的计算开销来评估参与者的贡献。

（完）
