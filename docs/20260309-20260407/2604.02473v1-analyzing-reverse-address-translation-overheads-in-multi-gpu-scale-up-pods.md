---
title: Analyzing Reverse Address Translation Overheads in Multi-GPU Scale-Up Pods
title_zh: 分析多 GPU 纵向扩展集群中的反向地址转换开销
authors: "Amel Fatima, Tuan Ta, Bradford M. Beckmann"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02473v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 分析多GPU扩展集群中的地址转换开销
tldr: 本研究针对多GPU扩展集群中新兴互连技术（如NVLink）引入的“反向地址转换”（NPA到SPA）开销进行了首次系统性分析。通过扩展ASTRA-sim框架模拟Link MMU和TLB，研究揭示了TLB冷启动对小规模集合通信性能的显著负面影响。为此，作者提出了融合预转换算子和软件引导的TLB预取技术，旨在隐藏转换延迟，提升大规模多GPU系统在推理等负载下的吞吐量与可扩展性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02473v1/fig-001.webp\", \"caption\": \"Table 1: Simulation Setup\", \"page\": 4, \"index\": 1, \"width\": 531, \"height\": 857}]"
motivation: 随着NVLink等互连技术实现跨节点内存直接访问，目标端的反向地址转换成为影响分布式机器学习性能的关键但尚未被深入研究的瓶颈。
method: 利用扩展的ASTRA-sim框架结合Omnet++网络后端，对Link MMU和TLB进行建模，并评估其在不同规模All-to-All通信中的表现。
result: 实验发现TLB冷缺失导致小规模集合通信性能下降高达1.4倍，而大规模通信则受TLB容量影响较小。
conclusion: 通过融合预转换内核和软件预取机制可以有效隐藏反向地址转换延迟，是优化未来大规模多GPU系统通信效率的重要方向。
---

## 摘要
分布式机器学习工作负载严重依赖于多 GPU、多节点系统间的集合通信。新兴的纵向扩展织构（如 NVLink 和 UALink）实现了跨节点的直接内存访问，但引入了一个关键的目标端转换步骤：将网络物理地址 (NPA) 转换为系统物理地址 (SPA)，我们称之为反向转换（反向地址转换）。尽管其具有重要性，但反向地址转换对性能的影响仍未得到充分理解。在这项工作中，我们对大规模 GPU 集群中的反向地址转换进行了首次系统性研究。我们使用以 Omnet++ 作为网络后端的扩展 ASTRA-sim 框架，对链路 MMU 和链路 TLB 进行了建模，并评估了它们在不同输入规模和 GPU 数量下对 All-to-All 集合通信的影响。我们的分析表明，对于小规模、延迟敏感的集合通信，冷启动 TLB 缺失主导了延迟，导致高达 1.4 倍的性能下降；而大规模集合通信则受益于预热后的缓存，且从过大的 TLB 中获得的收益递减。基于这些观察，我们提出了两种优化途径：将反向地址转换与计算重叠的融合预转换算子，以及主动填充可能需要的条目的软件引导 TLB 预取。这些技术旨在隐藏转换延迟，特别是针对小规模集合通信，从而提高推理工作负载的吞吐量和可扩展性。我们的研究为在大规模多 GPU 系统中设计高效的目标端转换机制奠定了基础。

## Abstract
Distributed ML workloads rely heavily on collective communication across multi-GPU, multi-node systems. Emerging scale-up fabrics, such as NVLink and UALink, enable direct memory access across nodes but introduce a critical destination-side translation step: translating Network Physical Addresses (NPAs) to System Physical Addresses (SPAs), which we term Reverse Translation (Reverse Address Translation). Despite its importance, the performance impact of Reverse Address Translation remains poorly understood. In this work, we present the first systematic study of Reverse Address Translation in large-scale GPU clusters. Using an extended ASTRA-sim framework with Omnet++ as the network backend, we model Link MMUs and Link TLBs and evaluate their effect on All-to-All collective communication across varying input sizes and GPU counts. Our analysis shows that cold TLB misses dominate latency for small, latency-sensitive collectives, causing up to 1.4x performance degradation, while larger collectives benefit from warmed caches and experience diminishing returns from over sized TLBs. Based on these observations, we propose two avenues for optimization: fused pre-translation kernels that overlap Reverse Address Translation with computation and software-guided TLB prefetching to proactively populate likely-needed entries. These techniques aim to hide translation latency, particularly for small collectives, improving throughput and scalability for inference workloads. Our study establishes a foundation for designing efficient destination-side translation mechanisms in large-scale multi-GPU systems.

---

## 论文详细总结（自动生成）

### 论文总结：多 GPU 纵向扩展集群中的反向地址转换开销分析

#### 1. 核心问题与研究背景
随着大语言模型（LLM）规模的爆炸式增长，单机多 GPU 已无法满足需求，促使系统向多节点、多 GPU 的“纵向扩展集群”（Scale-Up Pods）演进。新兴的互连技术（如 NVIDIA 的 NVLink Network 和行业标准 UALink）允许跨节点的 GPU 直接进行内存访问。
*   **核心问题**：跨节点访问引入了**网络物理地址（NPA）到系统物理地址（SPA）**的转换过程，即“**反向地址转换**”（Reverse Address Translation）。
*   **研究动机**：以往研究多关注源端的虚拟地址转换，而忽略了目标端（接收端）转换对性能的影响。在高速互连网络中，这种转换可能成为分布式机器学习（ML）集合通信的新瓶颈。

#### 2. 核心方法论
本文对反向地址转换机制进行了首次系统性建模和性能表征：
*   **硬件建模**：在目标 GPU 端设计并模拟了一套完整的转换层次结构，包括：
    *   **L1 Link TLB**：每个 UALink 站点的私有缓存。
    *   **L2 Link TLB**：所有站点共享的二级缓存。
    *   **Page Walk Cache (PWC)**：页表走读缓存。
    *   **Page Table Walker (PTW)**：硬件页表走读器。
*   **模拟框架**：扩展了 **ASTRA-sim 2.0**，并集成 **Omnet++** 作为网络后端，实现了数据包级别的精确模拟，能够捕捉链路竞争、排队延迟及地址转换的停顿。
*   **优化提议**：针对发现的瓶颈，提出了**融合预转换内核**（将转换请求嵌入计算任务以重叠延迟）和**软件引导的 TLB 预取**（利用通信模式的可预测性提前填充 TLB）。

#### 3. 实验设计
*   **工作负载**：重点评估了 **All-to-All** 集合通信算子（广泛用于混合专家模型 MoE 和模型并行）。
*   **对比基准（Benchmark）**：
    *   **理想情况（Ideal）**：假设反向地址转换开销为零。
    *   **基准配置（Baseline）**：包含 L1/L2 TLB 和页表走读延迟的实际硬件模型。
*   **变量控制**：
    *   GPU 规模：8, 16, 32, 64 个 GPU。
    *   消息大小：从 1MB 到 4GB 不等。
    *   TLB 容量：从 16 条目到 32,768 条目不等。

#### 4. 资源与算力
*   **算力平台**：本研究基于**模拟器**完成，未涉及真实的大规模 GPU 集群训练时长。
*   **模拟参数**：模拟了类似 AMD Instinct MI350X 级别的 GPU 配置（256 个计算单元，HBM 访问延迟 150ns），互连网络采用 UALink 单层 Clos 拓扑，单链路带宽 800 Gbps。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的扫描实验，包括 4 种 GPU 规模、13 种输入数据大小以及 5 种不同的 L2-TLB 容量配置。
*   **充分性评价**：实验设计较为充分，涵盖了从延迟敏感的小规模通信到带宽敏感的大规模通信场景。通过分解延迟组成（如 L1-MSHR 命中率、页表走读占比等），深入揭示了性能下降的根本原因，实验逻辑客观、公平。

#### 6. 主要结论与发现
*   **小规模通信受损严重**：对于 1MB 的小规模集合通信，反向地址转换导致的性能下降高达 **1.4 倍**。这是因为冷启动导致的 TLB 缺失和页表走读延迟占总通信时间的 30% 以上。
*   **大规模通信具有摊销效应**：随着数据量增加（如 16MB 以上），转换开销被大量数据传输平摊，性能下降降至 1.1 倍左右。
*   **时间局部性极低**：ML 集合通信呈现流式访问特征，数据页在访问后很少被重复利用。因此，盲目增大 L2-TLB 容量（超过参与 GPU 的数量）对性能提升几乎没有帮助。
*   **推理负载更敏感**：由于推理任务通常采用小 Batch，对延迟极度敏感，反向地址转换将成为推理集群扩展的主要障碍。

#### 7. 论文优点
*   **开创性**：填补了多 GPU 纵向扩展架构中目标端地址转换研究的空白。
*   **高精度模拟**：结合 ASTRA-sim 和 Omnet++，实现了从算子层到物理数据包层的全栈性能分析。
*   **洞察深刻**：指出了 TLB 容量并非万能药，强调了“冷启动”和“预取”在未来硬件设计中的重要性。

#### 8. 不足与局限
*   **算子覆盖有限**：主要集中在 All-to-All 算子，虽然其具有代表性，但未详细讨论 All-Reduce 或 Reduce-Scatter 等其他重要算子的差异。
*   **缺乏真实硬件验证**：由于 UALink 1.0 标准刚发布，相关硬件尚未普及，研究完全依赖模拟，可能与未来实际硅片的复杂特性存在偏差。
*   **软件优化未量化**：论文提出了预取和融合内核的建议，但未在模拟中给出这些优化手段的具体性能提升百分比。

（完）
