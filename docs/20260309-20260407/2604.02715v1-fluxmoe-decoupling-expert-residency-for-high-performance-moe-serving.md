---
title: "FluxMoE: Decoupling Expert Residency for High-Performance MoE Serving"
title_zh: FluxMoE：解耦专家驻留以实现高性能 MoE 推理服务
authors: "Qingxiu Liu, Cyril Y. He, Hanser Jiang, Zion Wang, Alan Zhao, Patrick P. C. Lee"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02715v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 高性能 MoE 推理服务系统架构
tldr: FluxMoE 针对混合专家模型（MoE）推理中专家权重长期占用 GPU 显存导致 KV 缓存受限的问题，提出了一种解耦专家驻留的推理系统。通过引入专家分页机制，将权重视为按需加载、用后即弃的流式资源，从而释放显存给关键的运行时状态。该系统基于 vLLM 实现，在显存受限的情况下，相比原系统可提升高达 3.0 倍的吞吐量，且不损失模型精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02715v1/fig-001.webp\", \"caption\": \"Figure 5. Exponent and mantissa distribution analysis of MoE expert parameters.\", \"page\": 6, \"index\": 1, \"width\": 477, \"height\": 391}, {\"url\": \"assets/figures/arxiv/2604.02715v1/fig-002.webp\", \"caption\": \"Figure 9. (Exp#3) Throughput stability during runtime residency adaptation.\", \"page\": 11, \"index\": 2, \"width\": 487, \"height\": 416}, {\"url\": \"assets/figures/arxiv/2604.02715v1/fig-003.webp\", \"caption\": \"Figure 4. FluxMoE architecture.\", \"page\": 4, \"index\": 3, \"width\": 388, \"height\": 365}]"
motivation: MoE 模型中大量闲置的专家权重占用了宝贵的 GPU 显存，限制了决定吞吐量的 KV 缓存容量。
method: 提出专家分页（expert paging）抽象，将专家权重作为瞬态资源按需加载并立即释放，优先保障 KV 缓存的显存分配。
result: 在显存密集型场景下，FluxMoE 相比 vLLM 实现了高达 3.0 倍的吞吐量提升。
conclusion: 通过解耦专家权重的持久驻留，FluxMoE 有效解决了大规模 MoE 模型推理中的显存竞争问题，显著优化了服务性能。
---

## 摘要
混合专家（MoE）模型已成为扩展大语言模型的主流范式，但其快速增长的参数规模在推理过程中引入了根本性的低效：大多数专家权重在 GPU 显存中保持闲置，同时与键值（KV）缓存等关键性能运行时状态竞争资源。由于 KV 缓存容量直接决定了推理服务的吞吐量，这种不匹配导致了显存利用不足和性能下降。在本文中，我们提出了 FluxMoE，这是一种新型 MoE 推理系统，它将专家参数与持久的 GPU 驻留解耦。FluxMoE 引入了一种专家分页抽象，将专家权重视为流式的瞬态资源，按需实例化并在使用后立即驱逐，从而允许 GPU 显存优先分配给对吞吐量至关重要的运行时状态。我们在 vLLM 之上实现了 FluxMoE，以实现在严苛显存限制下的高效 MoE 推理。实验结果表明，在显存密集型场景下，FluxMoE 在不损害模型保真度的情况下，比 vLLM 实现了高达 3.0 倍的吞吐量提升。

## Abstract
Mixture-of-Experts (MoE) models have become a dominant paradigm for scaling large language models, but their rapidly growing parameter sizes introduce a fundamental inefficiency during inference: most expert weights remain idle in GPU memory while competing with performance-critical runtime state such as the key-value (KV) cache. Since KV cache capacity directly determines serving throughput, this mismatch leads to underutilized memory and degraded performance. In this paper, we present FluxMoE, a new MoE inference system that decouples expert parameters from persistent GPU residency. FluxMoE introduces an expert paging abstraction that treats expert weights as streamed, transient resources, materializing them on demand and evicting them immediately after use, allowing GPU memory to be preferentially allocated to throughput-critical runtime state. We implement FluxMoE atop vLLM to enable efficient MoE inference under severe memory constraints. Experimental results demonstrate that FluxMoE achieves up to 3.0$\times$ throughput gains over vLLM in memory-intensive regimes, without compromising model fidelity.

---

## 论文详细总结（自动生成）

### 论文总结：FluxMoE - 解耦专家驻留以实现高性能 MoE 推理服务

#### 1. 核心问题与整体含义（研究动机和背景）
混合专家模型（MoE）通过增加参数量显著提升了模型容量，但在推理服务中面临严重的显存效率问题：
*   **参数闲置与竞争**：MoE 模型（如 DeepSeek-R1）拥有数千亿参数，但在任何给定的推理步骤中，只有极少数专家被激活。绝大多数专家权重长期驻留在 GPU 显存中处于闲置状态。
*   **KV Cache 受限**：GPU 显存是有限的，被闲置权重占用的空间直接挤占了 KV Cache。而 KV Cache 的容量决定了系统能处理的 Batch Size 和上下文长度，是吞吐量的核心瓶颈。
*   **研究动机**：打破“模型权重必须持久驻留 GPU”的传统假设，将专家权重视为“流式资源”，从而释放显存给 KV Cache，提升推理吞吐量。

#### 2. 方法论：核心思想与关键技术
FluxMoE 提出了**专家分页（Expert Paging）**执行模型，核心思想是：**模型 = 计算图 + 流式参数**。

*   **PagedTensor（张量虚拟化）**：
    *   借鉴操作系统虚拟内存思想，为专家张量分配稳定的虚拟地址。
    *   通过 CUDA VMM API 动态地将物理内存块（物理池）映射到当前层所需的专家虚拟地址上。
    *   **优点**：无需修改现有的计算算子（如 PyTorch 或 Triton 算子），对上层透明。
*   **带宽平衡的存储层级（Storage Hierarchy）**：
    *   将专家池分布在**压缩 GPU 显存**和**主机 DRAM**中。
    *   **按带宽比例放置**：根据 PCIe 带宽和 GPU 解压带宽的比例分配参数，确保各后端加载时间同步完成，最大化聚合加载速率。
    *   **无损压缩**：利用 MoE 权重指数位（Exponent）分布集中的特性，采用选择性哈夫曼编码实现约 20% 的无损压缩。
*   **预算感知驻留规划器（Residency Planner）**：
    *   **闭环控制**：监控“计算时间”与“加载时间”的比率（$\rho$）。
    *   **动态调整**：如果计算快于加载（$\rho > 1$），则减少 GPU 驻留比例以释放显存；如果加载成为瓶颈（$\rho < 0.9$），则增加驻留比例。
    *   **优先级**：始终优先保障 KV Cache，仅在显存富余时增加专家驻留。

#### 3. 实验设计
*   **数据集/场景**：使用真实的对话数据集 **ShareGPT**，涵盖 1024 到 4096 的上下文长度，Batch Size 范围为 32 到 256。
*   **Benchmark 模型**：
    *   Mixtral-8x7B-Instruct (47B 参数)
    *   Qwen3-Next-80B-A3B-Instruct (80B 参数)
*   **对比方法**：
    *   **vLLM**：工业界标准的权重驻留框架。
    *   **vLLM-O**：带有简单专家卸载（Offloading）功能的 vLLM 变体。
    *   **FluxMoE-H**：消融实验版本，采用粗粒度的层级放置策略。

#### 4. 资源与算力
*   **硬件环境**：单节点服务器，配置 Intel Xeon Platinum 8358 CPU (128 vCPUs) 和 2 TiB DRAM。
*   **GPU 配置**：**4 张 NVIDIA L40 (48GB GDDR6)**，通过 PCIe 连接。
*   **并行策略**：使用张量并行（Tensor Parallelism, TP=2 或 TP=4）。
*   **训练/推理时长**：论文未提及训练，推理实验基于持续生成的吞吐量测试。

#### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **端到端吞吐量对比**：在性能受限（Exp#1）和容量受限（Exp#2）两种场景下进行测试。
*   **动态适应性实验**：展示了规划器在长序列推理过程中动态调整驻留水平的稳定性。
*   **开销分析**：专门测试了 PagedTensor 虚拟化机制带来的额外延迟（约 3%）。
*   **充分性评价**：实验设计较为全面，涵盖了不同规模的模型和多样的负载情况。对比基准选择合理，通过消融实验验证了带宽平衡和动态规划的有效性，结果具有较强的说服力。

#### 6. 主要结论与发现
*   **吞吐量大幅提升**：在显存密集型任务中，FluxMoE 相比 vLLM 实现了高达 **3.0 倍**的吞吐量增长。
*   **显存回收显著**：通过解耦驻留，系统能回收数 GB 显存用于 KV Cache，有效避免了因显存不足导致的频繁换入换出（Swapping）。
*   **性能/容量平衡**：证明了通过流水线化（Pipelining）专家加载与前向计算，可以在不损失精度的情况下，在较小显存的硬件上运行超大规模 MoE 模型。
*   **低开销**：虚拟化层引入的性能损耗极低（<3%），完全可被吞吐量收益抵消。

#### 7. 优点
*   **架构解耦**：将专家管理与计算逻辑分离，具有很强的通用性，易于集成到 vLLM 等现有框架。
*   **动态闭环**：不是静态分配显存，而是根据运行时压力动态调整，适应性强。
*   **无损精度**：相比量化或剪枝，FluxMoE 通过系统层面的优化保证了模型输出的完全一致性。
*   **带宽优化**：科学地利用了异构存储带宽，避免了单一路径（如仅 PCIe）成为瓶颈。

#### 8. 不足与局限
*   **硬件依赖性**：实验主要在 PCIe 接口的 L40 上完成，在拥有高速 NVLink 的 H100/B200 集群上，带宽瓶颈可能转移，收益比例可能发生变化。
*   **Prefill 阶段优化有限**：系统主要针对 Decode（生成）阶段的显存压力，对于 Prefill 阶段的 TTFT（首字延迟）优化提及较少。
*   **功耗考虑**：频繁的参数流转（从 DRAM 到 GPU）会显著增加 PCIe 总线和内存控制器的功耗，论文未对此进行评估。
*   **应用限制**：主要适用于 MoE 模型，对于 Dense（稠密）模型，由于所有参数每步都要用，该方法不适用。

（完）
