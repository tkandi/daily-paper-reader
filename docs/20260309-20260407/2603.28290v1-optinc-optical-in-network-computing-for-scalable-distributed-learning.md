---
title: "OptINC: Optical In-Network-Computing for Scalable Distributed Learning"
title_zh: OptINC：面向可扩展分布式学习的光学网络内计算
authors: "Sijie Fei, Grace Li Zhang, Bing Li, Ulf Schlichtmann"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28290v1"
tags: ["query:mlsys"]
score: 10.0
evidence: 用于分布式学习的光学在网计算
tldr: 针对分布式学习中通信开销巨大的问题，本文提出OptINC架构，利用马赫-曾德尔干涉仪（MZI）等光学器件将梯度聚合与量化等计算任务卸载到光互连网络中。通过构建光学神经网络（ONN）并结合硬件感知训练算法，该方案在保持模型精度的同时，消除了传统环形全归约（ring all-reduce）带来的通信瓶颈，显著提升了大规模分布式训练的效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28290v1/fig-001.webp\", \"caption\": \"Fig. 7: (a) The trained accuracy and loss, and (b) the overall latency breakdown for one epoch or step of ResNet50 on CIFAR100 and the LLaMA-based network on Wikipedia-1B.\", \"page\": 6, \"index\": 1, \"width\": 501, \"height\": 288}, {\"url\": \"assets/figures/arxiv/2603.28290v1/fig-002.webp\", \"caption\": \"TABLE I: Experimental results under different scenarios with various bit widths and server counts.\", \"page\": 5, \"index\": 2, \"width\": 1000, \"height\": 247}]"
motivation: 传统分布式学习中的通信算法在服务器间产生沉重开销，限制了大规模模型训练的扩展性。
method: 提出一种基于光学神经网络的在网计算架构，利用MZI器件在光域执行梯度平均与量化，并采用硬件感知算法优化权重矩阵以降低成本。
result: 在ResNet50和LLaMA等任务上的实验表明，该方案在达到与基准测试相当的训练精度的同时，有效消除了通信开销。
conclusion: OptINC通过将计算集成到光互连中，为解决大规模分布式学习的通信瓶颈提供了一种高效且可扩展的硬件加速方案。
---

## 摘要
分布式学习广泛用于在大规模数据集上训练大模型，其通过将模型或数据集的部分内容分布到多个设备上，并聚合计算结果以进行后续计算或参数更新。现有的分布式学习通信算法（如 ring all-reduce）会导致服务器之间产生沉重的通信开销。由于大规模系统中的通信使用光纤，我们提出了一种光学网络内计算（OptINC）架构，将服务器中的计算卸载到光学互连上。为了在光域执行梯度平均和量化，我们将马赫-曾德尔干涉仪（MZI）等光学器件集成到互连中。这种事实上的光学神经网络（ONN）可以有效减少现有分布式训练方案中的通信开销。为了降低训练该神经网络的数据集复杂度，还提出了一种在光域实现的预处理算法。通过使用酉矩阵和对角矩阵近似光学神经网络的权重矩阵，降低了硬件成本，同时通过提出的硬件感知训练算法保持了准确性。所提方案在真实的分布式学习任务上进行了评估，包括 CIFAR-100 上的 ResNet50 以及 Wikipedia-1B 上的基于 LLaMA 的网络。在这两种情况下，所提框架都能达到与 ring all-reduce 基准相当的训练准确度，同时消除了通信开销。

## Abstract
Distributed learning is widely used for training large models on large datasets by distributing parts of the model or dataset across multiple devices and aggregating the computed results for subsequent computations or parameter updates. Existing communication algorithms for distributed learning such as ring all-reduce result in heavy communication overhead between servers. Since communication in large-scale systems uses optical fibers, we propose an Optical In-Network-Computing (OptINC) architecture to offload the computation in servers onto the optical interconnects. To execute gradient averaging and quantization in the optical domain, we incorporate optical devices such as Mach-Zehnder-Interferometers (MZIs) into the interconnects. Such a de facto optical neural network (ONN) can effectively reduce the communication overhead in existing distributed training solutions. To reduce dataset complexity for training this neural network, a preprocessing algorithm implemented in the optical domain is also proposed. Hardware cost is lowered by approximating the weight matrices of the optical neural network with unitary and diagonal matrices, while the accuracy is maintained by a proposed hardware-aware training algorithm. The proposed solution was evaluated on real distributed learning tasks, including ResNet50 on CIFAR-100, and a LLaMA-based network on Wikipedia-1B. In both cases, the proposed framework can achieve comparable training accuracy to the ring all-reduce baseline, while eliminating communication overhead.

---

## 论文详细总结（自动生成）

### OptINC：面向可扩展分布式学习的光学网络内计算论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
随着深度学习模型（如大语言模型 LLMs）规模的剧增，单机训练已无法满足需求，分布式学习成为主流。然而，现有的分布式通信算法（如 **Ring All-Reduce**）存在显著瓶颈：
*   **通信开销大**：在 $N$ 个节点的集群中，Ring All-Reduce 需要 $2(N-1)$ 轮通信，通信开销占比接近 100%，成为计算能力的瓶颈。
*   **电学在网计算（INC）的缺陷**：虽然电学交换机可以加速聚合，但频繁的**光-电-光（O-E-O）转换**带来了高能耗和额外延迟，且电学缓冲区易溢出。
*   **光学优势未充分利用**：光路交换（OCS）虽能避免 O-E-O 转换，但其潜在的计算能力（如执行梯度聚合）尚未被有效开发。

**OptINC** 的核心目标是利用光学器件在光互连网络中直接完成梯度平均和量化，从而消除通信轮次开销。

#### 2. 方法论：核心思想与关键技术
OptINC 提出将计算任务从服务器卸载到由**马赫-曾德尔干涉仪（MZI）**构成的光学神经网络（ONN）中。
*   **核心思想**：将梯度聚合建模为一个非线性映射任务，利用 ONN 在光信号传输过程中同步完成“平均+量化”操作。
*   **关键技术细节**：
    *   **信号编码**：将 B-bit 梯度切分为 2-bit 段，映射为 **PAM4** 光信号进行传输。
    *   **预处理单元（P）**：为了避免训练 ONN 时输入组合爆炸（$2^{MN}$），提出在光域先对部分信号求均值，将数据复杂度从指数级降至 $O(2^K)$。
    *   **硬件优化（矩阵近似）**：传统的 $M \times N$ 权重矩阵需要大量 MZI。论文采用矩阵分解与近似技术，将子矩阵近似为“一个对角矩阵 + 一个酉矩阵”（$\Sigma_a U_a$），相比标准 SVD 分解减少了近 50% 的 MZI 使用量。
    *   **硬件感知训练**：提出两阶段损失函数，第一阶段关注原始输出 MSE，第二阶段关注重构梯度的 MSE，并通过周期性应用矩阵近似来确保训练出的权重适配光学硬件。
    *   **可扩展架构**：设计了级联拓扑（Cascading Topology），通过多级 OptINC 单元支持更多服务器（如 $N^2$ 台），并针对多级量化误差改进了训练数据集。

#### 3. 实验设计
*   **数据集与场景**：
    *   **CV 任务**：在 **CIFAR-100** 数据集上训练 **ResNet50**。
    *   **NLP 任务**：在 **Wikipedia-1B** 数据集上训练基于 **LLaMA** 的网络（8层，384隐藏维度）。
*   **Benchmark（基准）**：传统的 **Ring All-Reduce** 算法。
*   **对比维度**：
    *   不同位宽（8-bit, 16-bit）和服务器数量（4, 8, 16）。
    *   有无矩阵近似（Area Ratio）的硬件成本对比。
    *   ONN 预测准确率与最终模型训练精度的对比。

#### 4. 资源与算力
*   **训练算力**：使用 **NVIDIA A100 Tensor Core GPU** 并在 PyTorch 环境下训练 ONN 模型。
*   **性能建模算力**：延迟分析基于 **NVIDIA H100 GPU** 的规格（60 TFLOPs 算力，0.6 利用率）以及 800 Gb/s 带宽的收发器进行建模。
*   **训练时长**：文中未明确给出 ONN 本身的具体训练时长，但提到 ResNet50 训练了 300 epochs，LLaMA 训练了 50,000 steps。

#### 5. 实验数量与充分性
*   **实验组数**：论文设计了 4 种主要场景（不同位宽与服务器组合），并针对第四种场景（16-bit, 4 servers）进行了 5 组不同层级的矩阵近似消融实验。
*   **充分性评价**：实验涵盖了从底层硬件面积优化、ONN 数学准确性到顶层真实模型训练的全流程模拟。通过引入误差注入（Error Injection）来模拟光学计算的不精确性，增强了实验的客观性和说服力。

#### 6. 主要结论与发现
*   **通信消除**：OptINC 理论上消除了 Ring All-Reduce 的 $N-2/N$ 通信开销，实现了数据在传输过程中的“即传即算”。
*   **硬件节省**：通过矩阵近似，硬件面积（MZI 数量）可减少至原始方案的 **39.2% – 49.3%**。
*   **精度保持**：尽管存在硬件近似和量化误差，ResNet50 的精度仅下降 **0.55%**，LLaMA 的 Loss 仅增加 **0.02**，处于可接受范围内。
*   **延迟提升**：在 H100 集群模拟中，ResNet50 的总延迟降低了 **25%** 以上，LLaMA 降低了约 **17%**。

#### 7. 优点（亮点）
*   **架构创新**：首次提出将 ONN 集成到光互连网络中执行分布式学习的梯度聚合，打破了传统电学 INC 的能效瓶颈。
*   **软硬协同**：不仅有硬件设计，还配套了硬件感知训练算法和矩阵近似策略，解决了光学器件规模受限的问题。
*   **可扩展性强**：级联方案为支持大规模 GPU 集群提供了可行路径。

#### 8. 不足与局限
*   **物理层非理想性**：论文主要基于数学模拟和理想光学模型，尚未深入探讨激光噪声、相位漂移、热串扰等实际物理环境下的鲁棒性。
*   **应用限制**：目前主要针对数据并行（Data Parallelism）中的梯度平均，对于更复杂的模型并行或混合并行策略的适配性有待研究。
*   **重编程成本**：虽然文中提到分布式学习模式固定，但若训练任务频繁变动，MZI 的重编程延迟（微秒至毫秒级）可能成为新的干扰因素。

（完）
