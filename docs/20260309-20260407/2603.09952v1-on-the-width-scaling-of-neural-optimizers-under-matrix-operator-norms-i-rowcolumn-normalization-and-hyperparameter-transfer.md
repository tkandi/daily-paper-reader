---
title: "On the Width Scaling of Neural Optimizers Under Matrix Operator Norms I: Row/Column Normalization and Hyperparameter Transfer"
title_zh: 关于矩阵算子范数下神经优化器的宽度缩放 I：行/列归一化与超参数迁移
authors: "Ruihan Xu, Jiajin Li, Yiping Lu"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09952v1"
tags: ["query:mlsys"]
score: 6.0
evidence: 深度学习中针对宽度扩展的神经网络优化器优化
tldr: 本研究探讨了神经网络宽度增加时优化器的稳定性问题。通过将AdamW和Muon等优化器解释为矩阵算子范数下的最速下降法，作者揭示了优化器几何与网络Lipschitz结构的关系。为解决标准算子范数在深层架构中缺乏层级组合性的问题，提出了均值归一化算子范数，并推导出支持跨宽度学习率迁移的缩放规则。基于此提出的MOGA优化器在GPT-2和LLaMA预训练中表现优异，实现了比Muon更稳定的宽度缩放。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.09952v1/fig-001.webp\", \"caption\": \"Figure 4: Standard Token Budget Experiment. Performance of MOGA on GPT-2 small model and LLaMA-130M model compared to Muon and AdamW with 1×chinchilla optimal token budget.\", \"page\": 21, \"index\": 1, \"width\": 979, \"height\": 407}, {\"url\": \"assets/figures/arxiv/2603.09952v1/fig-002.webp\", \"caption\": \"Figure 5: Large Token Budget Experiment. Performance of MOGA on GPT-2 small model and LLaMA-130M model compared to Muon and AdamW. MOGA exhibits a faster convergence speed in the low-loss stage.\", \"page\": 22, \"index\": 2, \"width\": 976, \"height\": 407}, {\"url\": \"assets/figures/arxiv/2603.09952v1/fig-003.webp\", \"caption\": \"Figure 2: Embedding layer geometry. One-hot inputs place embedding training in the 1→ (𝑞,mean) geometry, where both column normalization and SignGD are effective.\", \"page\": 18, \"index\": 3, \"width\": 490, \"height\": 369}, {\"url\": \"assets/figures/arxiv/2603.09952v1/fig-004.webp\", \"caption\": \"Figure 1: Operators Should Play Nice Together. Chaining layer-wise stability bounds under ∥ · ∥ 𝑝→𝑞 requires ∥ · ∥ 𝑝 ≤ ∥ · ∥𝑞 . This fails for classical 𝑝 → 𝑞 norms when 𝑝 ≤ 𝑞 but holds for (𝑝,mean) → (𝑞,mean) norms, yielding dimension-independent bounds.\", \"page\": 8, \"index\": 4, \"width\": 877, \"height\": 295}, {\"url\": \"assets/figures/arxiv/2603.09952v1/fig-005.webp\", \"caption\": \"Figure 3: Learning Rate Transfer. Performance of MOGA from GPT-2 Small to GPT-XL. The optimal learning rate of MOGA is invariant to width.\", \"page\": 20, \"index\": 5, \"width\": 979, \"height\": 254}]"
motivation: 旨在解决现有优化器在神经网络宽度增加时稳定性不足以及学习率难以跨宽度迁移的问题。
method: 引入均值归一化算子范数以实现层级组合性，并据此提出基于行/列归一化的MOGA优化器。
result: 实验证明MOGA在GPT-2和LLaMA的大规模预训练中与Muon竞争激烈，且在低损失阶段表现更优。
conclusion: 均值归一化算子范数为设计具有宽度独立平滑性保证的优化器提供了理论框架，并实现了高效的学习率迁移。
---

## 摘要
现代深度学习中的一个核心问题是如何设计在网络宽度 $w$ 增加时行为保持稳定的优化器。我们通过将包括 \textrm{AdamW} 和 \textrm{Muon} 在内的几种广泛使用的神经网络优化器解释为矩阵算子范数下的最速下降实例来探讨这一问题。这一视角将优化器几何与网络前向映射的 Lipschitz 结构联系起来，并实现了对 Lipschitz 常数和光滑性常数的宽度无关控制。然而，由标准 $p \to q$ 算子范数诱导的最速下降规则缺乏逐层可组合性，因此无法在深层架构中提供宽度无关的界限。我们通过引入一类均值归一化算子范数（记为 $\pmean \to \qmean$）克服了这一局限性，该范数支持逐层可组合性，产生宽度无关的光滑性界限，并衍生出诸如重缩放 \textrm{AdamW}、行归一化和列归一化等实用优化器。由此产生的学习率宽度感知缩放规则将 $\mu$P 缩放

## Abstract
A central question in modern deep learning is how to design optimizers whose behavior remains stable as the network width $w$ increases. We address this question by interpreting several widely used neural-network optimizers, including \textrm{AdamW} and \textrm{Muon}, as instances of steepest descent under matrix operator norms. This perspective links optimizer geometry with the Lipschitz structure of the network forward map, and enables width-independent control of both Lipschitz and smoothness constants. However, steepest-descent rules induced by standard $p \to q$ operator norms lack layerwise composability and therefore cannot provide width-independent bounds in deep architectures. We overcome this limitation by introducing a family of mean-normalized operator norms, denoted $\pmean \to \qmean$, that admit layerwise composability, yield width-independent smoothness bounds, and give rise to practical optimizers such as \emph{rescaled} \textrm{AdamW}, row normalization, and column normalization. The resulting learning rate width-aware scaling rules recover $μ$P scaling~\cite{yang2021tensor} as a special case and provide a principled mechanism for cross-width learning-rate transfer across a broad class of optimizers. We further show that \textrm{Muon} can suffer an $\mathcal{O}(\sqrt{w})$ worst-case growth in the smoothness constant, whereas a new family of row-normalized optimizers we propose achieves width-independent smoothness guarantees. Based on the observations, we propose MOGA (Matrix Operator Geometry Aware), a width-aware optimizer based only on row/column-wise normalization that enables stable learning-rate transfer across model widths. Large-scale pre-training on GPT-2 and LLaMA shows that MOGA, especially with row normalization, is competitive with Muon while being notably faster in large-token and low-loss regimes.