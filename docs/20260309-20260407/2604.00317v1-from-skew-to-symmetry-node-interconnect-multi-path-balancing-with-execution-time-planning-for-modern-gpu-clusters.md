---
title: "From Skew to Symmetry: Node-Interconnect Multi-Path Balancing with Execution-time Planning for Modern GPU Clusters"
title_zh: 从偏斜到对称：针对现代 GPU 集群的执行时规划节点互连多路径平衡
authors: "Jinghan Yao, Kaushik Kandadi, Bharath Ramesh, Hari Subramoni, Dhabaleswar K. Panda"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00317v1"
tags: ["query:mlsys"]
score: 10.0
evidence: GPU集群的多路径平衡与执行时间规划
tldr: 针对现代GPU集群中由于流量倾斜导致的带宽利用率低和拥塞问题，本文提出了NIMBLE系统。NIMBLE是一个运行时通信编排系统，通过将流量重分布到所有可用的节点内和节点间路径来平衡链路利用率。它采用乘法权重算法解决最小拥塞优化问题，并利用CUDA感知的GPU内核RDMA流水线技术。该系统在保持低开销和透明集成的同时，显著提升了All-to-Allv和LLM MoE等工作负载的通信性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00317v1/fig-001.webp\", \"caption\": \"Fig. 1: In skewed workload, heavy-load links (both intranode and inter-node fabrics) dominate the overall latency. Our proposed NIMBLE is able to evaluate load imbalance and automatically redirect message transfer through less busy paths, reducing the overall maximum latency across all links.\", \"page\": 1, \"index\": 1, \"width\": 508, \"height\": 543}, {\"url\": \"assets/figures/arxiv/2604.00317v1/fig-002.webp\", \"caption\": \"Fig. 5: On every forwarding GPU, we allocate a small amount of peer-to-peer buffer and register RDMA memory region if NIC is involved in the path. These intermediate buffers are much smaller than the actual data buffer on the sender and receiver side, they serve as the buffer pool in the pipeline.\", \"page\": 7, \"index\": 2, \"width\": 434, \"height\": 595}, {\"url\": \"assets/figures/arxiv/2604.00317v1/fig-003.webp\", \"caption\": \"Fig. 8: MoE end-to-end latency breakdown for NCCL vs. NIMBLE. For each token size, paired stacks show NCCL (left: blue dispatch, green compute, light-blue combine) and NIMBLE (right: orange dispatch, green compute, red combine); the purple trace overlays end-to-end speedup. Compute is identical across methods; gains come from slimmer dispatch/combine.\", \"page\": 10, \"index\": 3, \"width\": 990, \"height\": 450}, {\"url\": \"assets/figures/arxiv/2604.00317v1/fig-004.webp\", \"caption\": \"Fig. 2: NIMBLE system architecture. Positioned between applications and hardware fabrics, NIMBLE intercepts communication calls and orchestrates dynamic link-aware data transfers across intra- and inter-node interconnects.\", \"page\": 4, \"index\": 4, \"width\": 434, \"height\": 522}, {\"url\": \"assets/figures/arxiv/2604.00317v1/fig-005.webp\", \"caption\": \"Fig. 6: Demonstration of speedup from utilizing additional communication paths and the efficiency in intra-node and inter-node multi-hop paths. For intra-node, we have a theoretical speedup of 3x as we have two more idling GPUs; for inter-node, we have a theoretical speedup of 4x as we have 4 NICs in total. The number of NICs in (b) is the per-node involved NICs. The number of NICs in (d) is the rail-matched NIC pair that sits on the sender and receiver nodes.\", \"page\": 9, \"index\": 5, \"width\": 1076, \"height\": 221}, {\"url\": \"assets/figures/arxiv/2604.00317v1/fig-006.webp\", \"caption\": \"Fig. 7: Skewed All-to-Allv communication. Each rank sends variable-sized messages to all peers; we model skew by directing a fraction (the hotspot ratio) of each rank’s payload to a single “hot” destination, with the remainder distributed evenly.\", \"page\": 9, \"index\": 6, \"width\": 1076, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2604.00317v1/fig-007.webp\", \"caption\": \"Fig. 4: Given 4-GPU 4-NIC computing nodes, intra-node link balancing tries to utilize all available links that direct the path from sources GPU to destination GPU; Inter-node link balancing leverages the rail-matched path among nodes as well as intra-node links on both nodes.\", \"page\": 5, \"index\": 7, \"width\": 500, \"height\": 316}]"
motivation: 现有的通信框架依赖静态路由或哈希分流，在面对非均匀流量分布时会导致严重的链路拥塞和带宽浪费。
method: 提出NIMBLE系统，通过乘法权重算法动态优化流量分配，并结合GPU内核驱动的RDMA流水线实现多路径并行传输。
result: 在H100集群上，NIMBLE在倾斜的All-to-Allv任务中比NCCL快5.2倍，在LLM MoE端到端任务中提升了1.35倍性能。
conclusion: NIMBLE通过动态多路径平衡有效解决了GPU集群的通信瓶颈，为现代大规模分布式深度学习提供了高效的通信支撑。
---

## 摘要
现代基于 GPU 的高性能计算集群通过异构的节点内互连和节点间网络提供了前所未有的通信带宽。然而，尽管总带宽很高，许多现实世界的通信模式仍无法充分利用现有的硬件。流量偏斜（Traffic skew）通常会导致一小部分链路过度饱和，而其他链路则处于低负载状态，从而引发拥塞、延迟激增和可扩展性差等问题。现有的通信框架（如 NCCL 和带有 UCX 的 MPI）通常依赖于静态最快路径路由或基于哈希的多轨条带化（multi-rail striping），当运行时流量偏离预期分布时，会导致大量带宽闲置。为了解决这些局限性，我们提出了 NIMBLE（节点互连多路径平衡与执行时编排），这是一个运行时通信编排系统，能够动态重新分配流量，以平衡所有可用的节点内和节点间路径的链路利用率。NIMBLE 将此建模为一个容量归一化的最小拥塞优化问题，并使用乘法权重算法高效求解。它进一步采用了基于 CUDA 感知 GPU 核函数的 RDMA 流水线技术，通过中间 GPU 和轨道匹配的网卡（NIC）进行流量路由。该系统由端点驱动，可透明地与现有通信库集成而无需修改应用程序，并保持了顺序性、确定性和低开销。在具有全连接 NVLink 和四个 NDR400 轨道的 H100-SXM4 节点上，与单路径基准相比，NIMBLE 实现了高达 2.3 倍的节点内带宽和 3.8 倍的节点间吞吐量。在偏斜的 All-to-Allv 工作负载上，它的性能优于 NCCL 和 MPI 高达 5.2 倍；在端到端大语言模型（LLM）混合专家模型（MoE）工作负载上优于 1.35 倍，同时在平衡流量下与基准性能持平。

## Abstract
Modern GPU-based high-performance computing clusters offer unprecedented communication bandwidth through heterogeneous intra-node interconnects and inter-node networks. However, despite this high aggregate bandwidth, many real-world communication patterns fail to fully utilize the available hardware. Traffic skew often leads to situations where a small subset of links becomes oversaturated while others remain underutilized, resulting in congestion, latency spikes, and poor scalability. Existing communication frameworks such as NCCL and MPI with UCX typically rely on static fastest-path routing or hashing-based multi-rail striping, which leaves significant bandwidth unused when runtime traffic deviates from expected distributions. To address these limitations, we propose NIMBLE (Node-Interconnect Multi-path Balancing with Execution-time orchestration), a runtime communication orchestration system that dynamically redistributes traffic to balance link utilization across all available intra-node and inter-node paths. NIMBLE formulates this as a capacity-normalized minimum-congestion optimization problem and solves it efficiently using a multiplicative-weights algorithm. It further employs CUDA-aware GPU kernel-based RDMA pipelining to route traffic through intermediate GPUs and rail-matched NICs. The system is endpoint-driven, integrates transparently with existing communication libraries without requiring application changes, and preserves ordering, determinism, and low overhead. On H100-SXM4 nodes with fully connected NVLink and four NDR400 rails, NIMBLE achieves up to 2.3x higher intra-node bandwidth and 3.8x higher inter-node throughput compared to single-path baselines. It outperforms NCCL and MPI by up to 5.2x on skewed All-to-Allv workloads and 1.35x on end-to-end LLM MoE workloads, while matching baseline performance under balanced traffic.

---

## 论文详细总结（自动生成）

### 论文总结：From Skew to Symmetry (NIMBLE)

#### 1. 核心问题与整体含义（研究动机和背景）
现代 GPU 集群（如 H100 平台）拥有极高的理论带宽，包括节点内的 NVLink/NVSwitch 和节点间的多轨（Multi-rail）InfiniBand/RoCE 网络。然而，在实际应用（如大模型 MoE 架构、图计算、稀疏代数）中，通信模式往往是**非均匀且动态变化**的。
*   **痛点：** 现有的通信库（如 NCCL、MPI/UCX）主要依赖静态的最快路径路由或简单的哈希分流。当出现流量偏斜（Traffic Skew）时，少数链路会因过载而成为瓶颈（拥塞、延迟激增），而大量其他可用链路却处于闲置状态。
*   **目标：** 开发一个运行时的通信编排系统，能够动态感知链路负载，并将流量重新分配到所有可用的节点内和节点间路径，实现“从偏斜到对称”的平衡。

#### 2. 方法论：核心思想与关键技术
论文提出了 **NIMBLE** 系统，其核心由三个部分组成：
*   **最小拥塞优化算法：** 将流量分配建模为容量归一化的最小拥塞多商品流（MCF）问题。为了满足运行时（毫秒级）的实时性要求，NIMBLE 放弃了复杂的整数规划，转而采用**迭代近似算法（基于乘法权重更新 MWU）**。该算法通过动态更新链路成本（Cost），优先选择负载较低的路径。
*   **GPU 内核驱动的 RDMA 流水线：** 
    *   **多跳转发：** 利用中间 GPU 作为中转站，通过内核（Kernel）控制数据在 NVLink 和 NIC 之间的流动。
    *   **非阻塞流水线：** 在中间 GPU 上分配极小的 P2P 缓冲区，通过细粒度的计数器同步机制，实现发送方、中转方和接收方的并行数据传输，避免了主机端（CPU）干预。
*   **运行时策略：** 包含小消息阈值（防止过度分片）、基于目的地的重组队列（保证顺序）以及迟滞（Hysteresis）机制（防止路径频繁震荡）。

#### 3. 实验设计
*   **测试场景：**
    1.  **微基准测试：** 节点内/节点间点对点（P2P）带宽测试。
    2.  **偏斜 All-to-Allv：** 模拟 MoE 中的专家路由，通过调整“热点比例”（Hotspot Ratio，0.4 到 1.0）来改变偏斜程度。
    3.  **端到端 MoE 块：** 模拟 DeepSeek-V3 等模型的 Token 调度过程。
*   **对比基准（Benchmarks）：** 
    *   **NCCL v2.26**（行业标准）。
    *   **OpenMPI v5.0.7 + UCX v1.18.0**（支持 CUDA-Aware）。
*   **评估指标：** 吞吐量（GB/s）、端到端延迟、算法开销。

#### 4. 资源与算力
*   **硬件环境：** 
    *   **GPU：** NVIDIA H100 (Hopper) SXM5，每张卡 94GB HBM2e。
    *   **节点配置：** 每个节点 4 张 H100 GPU，通过 NVLink 4 全连接。
    *   **网络：** 每个节点配备 4 个 NDR400 (400 Gbps) InfiniBand 网卡（HCA），支持 GPUDirect RDMA。
    *   **CPU：** Intel Xeon Platinum 8470。
*   **规模：** 实验主要在 2 个节点（共 8 张 GPU）的规模上进行。

#### 5. 实验数量与充分性
*   **实验量：** 论文涵盖了从底层链路带宽到上层端到端应用的完整测试。针对 All-to-Allv 做了 7 组不同偏斜比例的对比实验，针对 MoE 做了不同 Token 数量（2K 到 64K）的扫描。
*   **充分性：** 实验设计较为全面，不仅展示了性能提升，还详细分析了算法本身的计算开销（微秒级）以及多跳转发带来的额外延迟。
*   **客观性：** 实验对比了当前主流的开源通信库，并明确指出了在小消息或平衡流量下 NIMBLE 并不具备优势，表现出客观的科学态度。

#### 6. 主要结论与发现
*   **带宽提升：** 在 H100 节点上，NIMBLE 将节点内峰值带宽从 120 GB/s（单链路）提升至 278.2 GB/s（多路径）；节点间聚合带宽达到 170 GB/s。
*   **应用加速：** 在极度偏斜（Hotspot Ratio=1.0）的 All-to-Allv 任务中，比 NCCL 快 **5.2 倍**。在端到端 MoE 任务中，实现高达 **1.35 倍** 的加速。
*   **开销控制：** 编排算法的耗时仅占总通信时间的极小比例（如 256MB 传输中，算法耗时约 0.04ms，而通信耗时 >2ms）。

#### 7. 优点
*   **动态自适应：** 突破了 NCCL 等库静态拓扑的限制，能实时响应流量变化。
*   **透明集成：** 封装在标准 NCCL API 之下，用户无需修改应用层逻辑即可使用。
*   **高效流水线：** 纯 GPU 内核控制的 RDMA 转发最大程度减少了 CPU 调度带来的抖动。

#### 8. 不足与局限
*   **硬件架构限制：** 论文指出在 NVIDIA DGX（使用 NVSwitch 全连接）架构下，由于所有 GPU 共享单一交换平面，节点内多路径转发的空间有限，主要增益来自于节点间多网卡的平衡。
*   **小消息开销：** 对于小于 1MB 的消息，多路径拆分带来的同步开销可能超过带宽收益，因此 NIMBLE 在此场景下会自动退化为单路径模式。
*   **规模扩展性：** 实验目前仅展示了 2 个节点的测试结果，在更大规模（如千卡集群）下的 MWU 算法收敛速度和全局拥塞控制效果仍需进一步验证。

（完）
