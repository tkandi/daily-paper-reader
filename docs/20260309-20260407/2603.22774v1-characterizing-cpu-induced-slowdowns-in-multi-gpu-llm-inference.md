---
title: Characterizing CPU-Induced Slowdowns in Multi-GPU LLM Inference
title_zh: 表征多 GPU 大语言模型推理中由 CPU 引起的性能下降
authors: "Euijun Chung, Yuxiao Jia, Aaron Jezghani, Hyesoon Kim"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22774v1"
tags: ["query:mlsys"]
score: 9.0
evidence: 多GPU系统中的CPU诱导减速与GPU利用率不足
tldr: 本研究深入分析了多GPU大语言模型（LLM）推理中常被忽视的CPU瓶颈问题。研究发现，即使GPU资源充足，CPU分配不足也会导致内核启动延迟、通信停顿及分词延迟，严重影响GPU利用率。通过系统性评估，本文揭示了增加CPU核心数能以极低成本显著提升推理性能和稳定性，将首字延迟（TTFT）降低1.36至5.40倍，为多GPU推理系统的资源配置提供了关键指导。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22774v1/fig-001.webp\", \"caption\": \"Fig. 8: Time-to-first-token (TTFT) latency of sequential victim requests under attacker load (8 & 16 requests per second, 114k tokens). As attacker requests accumulate, victim latency grows, but higher CPU allocations reduce this slowdown.\", \"page\": 6, \"index\": 1, \"width\": 504, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2603.22774v1/fig-002.webp\", \"caption\": \"Fig. 7: Latency of Llama 3.1 8B and Qwen 2.5 14B on 4 & 8 GPUs under CPU load (8 & 16 requests per second), varying attacker sequence length and CPU core counts. Red × marks denote timeouts. Red arrows indicate latency improvements from the smallest to the best CPU allocation, with corresponding speedups annotated. Measured on the Blackwell system.\", \"page\": 6, \"index\": 2, \"width\": 1048, \"height\": 416}, {\"url\": \"assets/figures/arxiv/2603.22774v1/fig-003.webp\", \"caption\": \"Fig. 9: Heatmap showing the best speedup among CPU-abundant configurations (2×#GPUs, 4×#GPUs, 8×#GPUs) relative to the least-CPU case (#GPUs + 1). ∞ indicates timeouts in the minimal-CPU configuration.\", \"page\": 7, \"index\": 3, \"width\": 968, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2603.22774v1/fig-004.webp\", \"caption\": \"Fig. 10: CPU utilization with Llama across different core allocations for 4-GPU and 8-GPU configurations. While all cases achieve near-100% utilization, utilizing more CPU cores results in shorter periods of high utilization.\", \"page\": 7, \"index\": 4, \"width\": 502, \"height\": 413}, {\"url\": \"assets/figures/arxiv/2603.22774v1/fig-005.webp\", \"caption\": \"Fig. 3: CDF of users’ CPU-to-GPU allocation ratios in the instructional cluster, weighted by GPU hours. Vertical lines mark percentiles; users manually set CPU and GPU counts.\", \"page\": 3, \"index\": 5, \"width\": 510, \"height\": 388}, {\"url\": \"assets/figures/arxiv/2603.22774v1/fig-006.webp\", \"caption\": \"Fig. 12: Illustration of CPU oversubscription causing busywaiting GPUs in multi-GPU workloads. When multiple CPU processes contend for limited cores, kernel launches are executed sequentially, leading to severe underutilization of GPUs. Gray boxes indicate subsequent kernel calls.\", \"page\": 8, \"index\": 6, \"width\": 521, \"height\": 452}, {\"url\": \"assets/figures/arxiv/2603.22774v1/fig-007.webp\", \"caption\": \"Fig. 11: CPU and GPU utilization with Llama for the 4-GPU setup across varying CPU core allocations, illustrating the relationship between CPU saturation and GPU underutilization.\", \"page\": 8, \"index\": 7, \"width\": 504, \"height\": 385}, {\"url\": \"assets/figures/arxiv/2603.22774v1/fig-008.webp\", \"caption\": \"Fig. 4: CDF of users’ CPU-to-GPU allocation ratios in the research cluster, weighted by GPU hours. Vertical lines mark percentiles; CPU cores are set proportionally to GPUs unless users override the setting.\", \"page\": 4, \"index\": 8, \"width\": 511, \"height\": 388}, {\"url\": \"assets/figures/arxiv/2603.22774v1/fig-009.webp\", \"caption\": \"Fig. 6: (a) Baseline LLM inference with no load. (b) LLM inference under concurrent ”attacker” load. 1. Tokenizer spawns multiple threads, 2. CPU cores are saturated, 3. Delayed kernel launches underutilize GPUs and degrade overall performance.\", \"page\": 5, \"index\": 9, \"width\": 517, \"height\": 258}]"
motivation: 尽管多GPU系统广泛应用，但CPU性能不足往往成为限制LLM推理效率的隐形瓶颈，导致GPU资源无法被充分利用。
method: 通过对现代LLM推理负载进行系统性实验，分析了在不同CPU资源分配下内核启动、通信同步及分词过程对系统性能的影响。
result: 实验表明，充足的CPU资源能将首字延迟（TTFT）降低1.36至5.40倍，并显著改善高负载下的服务响应稳定性。
conclusion: CPU配置是多GPU推理系统性能优化的关键因素，合理增加CPU核心数能以极低成本有效消除控制端瓶颈。
---

## 摘要
大规模机器学习工作负载日益依赖多 GPU 系统，但其性能往往受限于一个被忽视的组件：CPU。通过对现代大语言模型 (LLM) 推理和服务工作负载的详细研究，我们发现多 GPU 性能下降通常不是因为 GPU 饱和，而是因为 CPU 无法使 GPU 保持繁忙。在 CPU 分配受限的情况下，系统表现出诸如内核启动延迟、通信停滞和分词 (tokenization) 延迟增加等症状，导致即使在 GPU 资源充足时也会出现严重的 GPU 利用率不足。本研究对多 GPU LLM 推理中由 CPU 引起的性能下降进行了系统分析。我们表明，即使在采用进程级隔离和现代 GPU 端优化（如 CUDA Graphs）的服务栈中，这些瓶颈依然存在。鉴于额外 CPU 核心的边际成本相对于 GPU 实例定价而言较低，我们的评估表明，增加 CPU 核心数量可以以极小的额外成本显著提高性能和稳定性。在中等服务负载下，我们观察到 CPU 资源匮乏的配置经常超时，而提供充足的 CPU 资源可恢复响应能力，并在不同配置下将首字延迟 (TTFT) 降低 1.36-5.40 倍，且无需额外的 GPU。本研究表明，CPU 资源配置是多 GPU LLM 推理配置中的关键因素，有助于防止控制侧瓶颈。

## Abstract
Large-scale machine learning workloads increasingly rely on multi-GPU systems, yet their performance is often limited by an overlooked component: the CPU. Through a detailed study of modern large language model (LLM) inference and serving workloads, we find that multi-GPU performance frequently degrades not because GPUs are saturated, but because CPUs fail to keep the GPUs busy. Under limited CPU allocations, systems exhibit symptoms such as delayed kernel launch, stalled communication, and increased tokenization latency, leading to severe GPU underutilization even when ample GPU resources are available. This work presents a systematic analysis of CPU-induced slowdowns in multi-GPU LLM inference. We show that these bottlenecks persist even in serving stacks that employ process-level separation and modern GPU-side optimizations such as CUDA Graphs. Since the marginal cost of additional CPU cores is small relative to GPU instance pricing, our evaluation indicates that increasing the number of CPU cores can substantially improve performance and stability at minimal additional cost. Under moderate serving load, we observe that CPU-starved configurations frequently time out, while providing adequate CPU resources restores responsiveness and reduces time-to-first-token (TTFT) latency by 1.36-5.40x across configurations, all without requiring additional GPUs. This work shows that CPU provisioning is a crucial factor in multi-GPU LLM inference configuration, helping prevent control-side bottlenecks.

---

## 论文详细总结（自动生成）

### 论文总结：表征多 GPU LLM 推理中由 CPU 引起的性能下降

#### 1. 核心问题与研究动机
*   **核心问题**：在多 GPU 大语言模型（LLM）推理场景中，业界普遍认为 GPU 是唯一的性能瓶颈。然而，本文揭示了一个被忽视的现象：**CPU 资源分配不足（Under-provisioning）会导致严重的 GPU 闲置和推理延迟**。
*   **研究动机**：随着多租户云环境和共享集群的普及，用户往往为了节省成本或由于缺乏经验，在申请 GPU 的同时只配置极少数的 CPU 核心。研究旨在量化这种 CPU-GPU 失衡对 LLM 服务质量（SLO）的影响，并分析其背后的底层机制。

#### 2. 核心方法论
论文通过系统性表征和微基准测试，分析了 CPU 在推理流水线中的三大关键任务及其瓶颈：
*   **分词（Tokenization）负载**：分词是 CPU 密集型任务，且位于推理的关键路径上。在高并发请求下，多线程分词器会抢占 CPU 资源，延迟后续的 GPU 任务调度。
*   **内核启动与同步（Kernel Launch & Sync）**：CPU 负责向 GPU 发送指令。在 CPU 资源匮乏时，内核启动变慢，且由于 NCCL 等通信库依赖屏障（Barrier）同步，一个 CPU 核心的延迟会引发所有 GPU 的集体停顿（Straggler Effect）。
*   **共享内存广播竞争（Shared Memory Contention）**：分析了 vLLM 等框架采用的“1 写 N 读”广播模式。即使是无锁设计，在 CPU 核心受限时，写者（调度进程）和读者（GPU 工作进程）之间的频繁轮询（Polling）会产生严重的竞争，导致调度元数据传输延迟高达 19 倍。
*   **攻击者-受害者模型（Attacker-Victim Model）**：设计了一种实验方法，通过引入背景请求（攻击者）产生 CPU 负载，测量目标请求（受害者）的延迟变化，以此隔离 CPU 竞争的影响。

#### 3. 实验设计
*   **模型与框架**：使用 vLLM (V1 引擎)，测试了 Llama 3.1 8B 和 Qwen 2.5 14B 模型。
*   **场景模拟**：
    *   **在线服务负载**：改变每秒请求数（RPS）和输入序列长度（SL）。
    *   **资源配置变量**：改变 CPU 核心数（从最小的 `#GPUs + 1` 到充足的 `8 × #GPUs`）。
*   **基准对比**：对比了不同 CPU 分配方案下的首字延迟（TTFT）、GPU 利用率和系统超时率。
*   **真实世界证据**：分析了某大学研究与教学集群 2024 年全年的 **465 万条 Slurm 作业日志**，统计真实的 CPU-to-GPU 分配比例。

#### 4. 资源与算力
实验涵盖了多种现代高性能计算节点：
*   **NVIDIA H100/H200**：8 卡 NVLink 系统，配 Intel Xeon Platinum 8480CL CPU。
*   **NVIDIA RTX Pro 6000 (Blackwell)**：8 卡 PCIe 5.0 系统，配双路 Intel Xeon 6737P CPU。
*   **算力规模**：实验主要在单节点多卡（4 卡或 8 卡）配置下进行，未涉及超大规模跨节点训练。

#### 5. 实验数量与充分性
*   **实验充分性**：研究不仅在受控实验室环境下进行了多组消融实验（涵盖不同 GPU 架构、模型大小、RPS 负载），还结合了大规模真实集群的日志分析，证明了问题的普遍性。
*   **客观性**：实验禁用了超线程（SMT）以确保测量结果的确定性，并对比了 Hopper 和 Blackwell 两种不同架构，结果具有较强的说服力。

#### 6. 主要结论与发现
*   **显著提速**：在不增加 GPU 的情况下，仅通过增加 CPU 核心数，可将 TTFT 延迟降低 **1.36–5.40 倍**。
*   **资源失衡普遍存在**：日志分析显示，大量用户为 4-8 块 GPU 仅配置 1-2 个 CPU 核心，导致 P25 分位数的 CPU/GPU 比例低至 0.25。
*   **GPU 并非总是瓶颈**：在高负载下，CPU 饱和会导致 GPU 即使有空闲算力也无法获得任务，系统会因 CPU 端的调度阻塞而频繁超时。
*   **成本效益极高**：由于 CPU 核心的租赁成本仅为 GPU 的 1/100 到 1/1600，增加 CPU 资源是提升系统性价比最有效的手段。

#### 7. 亮点
*   **视角独特**：在学术界普遍关注 GPU 算力和显存优化时，精准捕捉到了“控制侧”CPU 瓶颈这一实际工程痛点。
*   **软硬结合分析**：深入到了 Linux 共享内存机制和 CUDA 内核启动的底层细节，解释了宏观延迟背后的微观原因。
*   **数据支撑扎实**：465 万条真实作业记录的分析为论文提供了极强的现实意义。

#### 8. 不足与局限
*   **硬件覆盖有限**：实验主要基于 NVIDIA GPU 和 Intel CPU，未测试 AMD GPU (ROCm) 或 ARM 架构 CPU（如 NVIDIA Grace）的表现。
*   **软件栈单一**：主要基于 vLLM 框架，虽然其架构具有代表性，但不同推理引擎（如 TensorRT-LLM 或 SGLang）的 CPU 敏感度可能存在差异。
*   **训练场景缺失**：论文明确指出研究重点在于推理，未深入探讨训练工作负载中的 CPU 瓶颈（尽管提到了视觉训练中的数据增强瓶颈）。

（完）
